<!-- SPDX-License-Identifier: MIT — Copyright (c) 2026 Paul Richeson -->
# Archived §RESUME history — completed backlog work

> **Completed increments only.** Everything here shipped and is closed; the six `docs/backlog/BACKLOG-*.md` phase files now carry open rows only. Each entry is condensed to one line — date, section, ship SHA, headline.

> Full prose for recent closes is in [`plan-archive.md`](../backlog/plan-archive.md); the cross-phase chronology index is in [`BACKLOG.md`](../backlog/BACKLOG.md). Anything not in either is recoverable from `git log -- docs/backlog/`.

> Moved out of the phase backlogs on 2026-08-25.

---

## Phase 1 — Playable Truth

| # | Date | Increment | Ship | Headline |
|---:|---|---|---|---|
| 1 | 2026-09-07 | §DX-02bd | `fe385f8` | `_questsByNode` folds `activateNode` and `waypointNode` into one map and pushed per field, so a quest naming one node in both was listed **twice** — and every consumer inherited it, including `_deps.node()`, the DELETE guard, which **refused the delete for the right reason with the wrong arithmetic**. Invisible to every gate: each id resolves, the list is non-empty, and a duplicate shows only if you compare `length` against `new Set(...).size`. Row's numbers reproduced exactly — **49** nodes, **97** entries, `NUE` **186 → 177**. Built through a `Set` now, so duplication is impossible by construction. `check:arraypatch` **51 → 56**, all properties: a corpus-wide uniqueness claim, a **non-vacuity control that rebuilds the old index and requires the check to fail on it**, and a proof only repeats were removed. |
| 2 | 2026-09-07 | §AUDIT-03ap | `38f0f62` | `hollow_hands_guard` dropped an item with the **exact name and icon** of the quest item Yva grants, in a pool whose node is primary — and the copy set no flag, while the delivery step's name-filter **silently ate it alongside the real seal**. Filed by its own lab report 110 days ago. Now `Hollow Hands Tally`, 🧾, **8gp** — it was also the collection's **only** `sell:0` row, against `mechanics-combat.md`'s own rule. **Option (1), a second item route, rejected:** the sibling arc's `scholars_guard`/`quest_wm_01` really does have that shape, but its two routes are *a letter* or *three seals*; here it would be *50gp* or *one easy kill*, which dominates rather than offers — and the route it would dominate is §VM-01-G4a's `cost`-leaf showcase. `sell:0` rows in `MONSTER_DROPS` **1 → 0**. |
| 3 | 2026-09-07 | §AUDIT-03as | `5100d18` | a 2026-06-04 data audit filled the *Void Shaman's Sanctum*'s empty roster with `P.void_shaman` — a key whose `name` is **The Warden**, a unique character in **Kazrath's** room, `name:'The Warden'` existing (says the report) *"so the battle overlay reads as a named encounter rather than a type."* The ground amended the row's *remove it*: **all twenty epic battlegrounds carry exactly three monsters**, so the slot went to `void_walker`, a generic hard-tier Void creature the corpus already pools twice. **The premise re-derived to one line of `CELL_GRID`**, not the quest data: `VS` is shadowed by `VBY` at (12,198), so the block holding every Visby button never runs — already §AUDIT-03x's, so no new row. Places you can meet The Warden: **0 before, 0 after** — what left was a statline wearing his name with no dialogue, no Token and no choice. `grep -c "P.void_shaman"` **1 → 0**. |
| 4 | 2026-09-07 | §AUDIT-03av | `4bebb6c` | `NUE`'s label reads *Scholar's Quarter — Weimar* and the file said Weimar **516** times against Nuremberg **five**, four of them on this node — including the Warrant's Board rumor that recruits the player into the arc. Weimar won on every name the arc owns (`NUE:'weimar'`, `quest_wm_*`, `wmDoc1Read`, Isolde Voss herself); the airport code stays. Three strings went through `put`; the fourth sat inside an `onComplete` carrying five §BOARD-01-FU6 annotations, so **the API grew the operation it could not express** — `substituteText` / `./bin/api sub`, which edits source text in place and matches only inside string literals. `check:arraypatch` **40 → 51**. `grep -c Nuremberg play.html` **5 → 1**. |
| 5 | 2026-09-07 | §AUDIT-03au | `ffcdbc2` | `quest_wm_04` completed on a flag whose only writer was its own `onComplete` — born that way at `194a810` and migrated faithfully into UQF. The flag is written where the quest's hint puts it, the unredacted read of Document 3, and **the row's separate consequence (c) was the same three lines**: an unsealed document had a non-null body and the Read button is built only where the body is null, so the render loop had no state for *the unsealed file is here and you have not read it yet*. Proved by lifting the live `WM_ARCHIVE_DOCS` loop against a stub DOM at HEAD and after. **`Marta Eilene Vass` renders for the first time**; the Annotated Copy and the only carrier of `atkWhileQuestActive` are granted; NG+ now carries the flag beside `entry42Written`, which `_vaReady` required in the same expression. **The Weimar arc closes at 4 of 4** — third of three rows in one session, in the order each asked for |
| 6 | 2026-09-07 | §AUDIT-03at | `70edc2d` | the reading circle counted sessions against `S_story.dayCounter` — **one occurrence, one commit, no writer ever** — so `today` was `0` on every render and the button locked at **1/3** on *"The circle meets again tomorrow."* → `S_story.gameDay`. Proved by lifting the live block against a stub DOM: `0/3 → 1/3 → 2/3`, sessions `[1,2,3]`, complete on the third attendance; with the read's clock left un-advanced it pushes `0` once and never opens. **Arc 2 of 4 → 3 of 4**, and it lights §AUDIT-03ar's `set:2` from three commits earlier — Benedikt reaches Dear Friend and §XXI's close at `NUE` is reachable. `quest_wm_04` activates and still cannot complete (§AUDIT-03au). Three maintained docs re-synced, two of which repeated the typo back as spec |
| 7 | 2026-09-07 | §AUDIT-03ar | `2a6b4f9` | `quest_wm_03` wrote `set:1` (Friendly) beside its own narrative bit saying *"Benedikt is **Dear Friend**"*, against two design docs and both live thresholds that begin Dear Friend at 2 — only the write said 1. **`set:1` → `set:2`** through `./bin/api`. Inert until §AUDIT-03at gives the reading circle a working clock, exactly as the row says. `check:npcregs` gains **phase 7** (every `_npcFavor(k) >= N` and `favorMin:{k:N}` at or below the NPC's favor ceiling — 26 of 27 were reachable, the 27th this site) and **phase 8**, the row's own class note made executable: a favor bit must write the tier its entry announces to the player — 7 real pairs, 6 already agreeing. Selftest 7 → 10 checks. §GR-FU2's phase 6 then demanded a Covenant Ceremony line for him, unprompted, and he has one. The sweep had refiled this as §DX-02je; `check:anchors` caught the duplicate and §DX-02je was deleted |
| 8 | 2026-08-24 | §DX-02gv + §WAVE-01 | `4bac567` | the Act VIII wall had a 210-HP statblock, a `sell:95` shard and no fight. `void_warlord` occurred **twice** in 38,700 lines — its `MONSTER_POOL` row `@5789` and its… |
| 9 | 2026-08-24 | §AUDIT-03bi | `c0888aa` | the fifth ending stops being downstream of a console command |
| 10 | 2026-08-24 | §AUDIT-03bk | `7cac4d2` | the quest that could be offered and never finished, and the host record its design lost |
| 11 | 2026-08-24 | §AUDIT-03bj | `3571a64` | the prover that spent 27 days proving the world is not finishable and reporting success |
| 12 | 2026-08-24 | §DX-02go | `5e17dc2` | the last inert field on a granted readable, and an anchor with a known expiry date |
| 13 | 2026-08-24 | §DX-02ge | `6237606` | the field four files agreed to carry and no code path ever looked at |
| 14 | 2026-08-24 | §DX-02gb + §DX-02fc | `763c70f` | the Dear-Friend step is granted once, and either order earns it |
| 15 | 2026-08-24 | §DX-02aj | `1222d8d` | the latch that was always already set, and the harness that was always already rendered |
| 16 | 2026-08-24 | §DX-02gd | `898c692` | eleven items, three spellings of one field, and a tooltip that was never rendered |
| 17 | 2026-08-24 | §DX-02cm | `d117b2f` | the exception that outlived the deferral, and the difference between debt and an unexercised bug |
| 18 | 2026-08-23 | §DX-02cy | `724682c` | the counter that had been monotone at zero for ninety days, and the gate that refused to let the docs stay wrong |

**14 completed entries.**

---

## Phase 2 — Engine & Systems

| # | Date | Increment | Ship | Headline |
|---:|---|---|---|---|
| 1 | 2026-09-07 | §DROP-02-FU | `57701fe` | §DROP-02 scoped Luck to the fishing type/rarity roll and a commit that mentioned none of it put all six sites back the same afternoon, uncaught for 68 days. The five survivors are re-applied — `_luckMod()` call sites **9 → 4** — and `check:invariants` gains **I5**, which declares every remaining site with its reason and is red in both directions (proved by planting the death-save term back). The row's census was one site short (the bait DC has a title twin) and its recommended default was **disproved**: `luckScale` in `_lakeMagicBonuses` is non-zero on 7 of 8 lake-magic items and reaches AC, attack, first strike and all-ability, so re-applying the removals could not make *"Luck's sole mechanical role"* true. All three home docs corrected; the lake-magic conflict filed as §DROP-02-FU2. Bare Hook catch: P(hit/cast) 0.835 → 0.736 |
| 2 | 2026-09-06 | §DX-02dl (partial) | `d90bc9e` | the Void meter's imminent milestone was `=== 9` while its two siblings were `>=`, so a +3 writer stepped over it; now `>= 9`, and the message interpolates the live pressure because firing at 10 would have printed "9/10, one more point". The Ceremonia +3 routes through the helper (direct writes 4 → 3). The two quest `_legacy_fn` writers are blocked — the API cannot write a function value — so the row stays open behind §DX-02iv |
| 3 | 2026-09-06 | §DX-02dg/dh | `9d3b11a` | the sidebar and the objective chip disagreed about the day on 15 of 49 days; both now read `_dayAlarm(day)` off offsets from DAY_DEADLINE, sidebar colours byte-identical, chip regains the danger step its CSS already styled, and the assertion §PLAY-01-C deleted in the same hunk that broke the agreement is restored as a 49-day sweep that is red at 0568969. courierMapSeen moved from the seed literal into _S_DEFAULTS so a reset can clear it. §DX-02di left open — a vocabulary decision, not a mechanical one |
| 4 | 2026-09-06 | §DX-02iu | `73eb02e` | `itemsAll` ported from the completion leaf to the activation leaf (only that one — the fuzzy `items` sits in an OR-group and would mean AND here); the last six closure gates migrated, LEGACY_GATE_CEILING 6 → 0. The rod trio needed no third matching rule — one item name contains "fishing rod" and the closures were only compensating for keyword casing; `_hasItem('Trade Seal')` did need care, its item being 'Trade Seal (Shard #1)'. 9,372 equivalence checks, 0 mismatches; check:gateast 76 → 84 assertions |
| 5 | 2026-09-06 | §DX-02dy | `ae3bb24` | gate:{_legacyFn:true} 14 → 6 — six closures were `() => true`, two were counter comparisons the already-shipped `countMin` leaf expresses; 240 quest×state equivalence checks, 0 mismatches. `removeExprField` added to wbapi-core first (§2.5) so the API could clear a function-valued field; check:questgraph now reports the gates it cannot see behind a down-only ceiling. The six survivors are inventory tests the activation leaf has no term for → §DX-02iu |
| 6 | 2026-09-06 | §DX-02eg | `7efb8f6` | the reward ceiling acted on a pool of 1,063 while the player read 8, so 60 days at Warrant's Own showed a premium bounty on none; a slate seat is now reserved for work the base ceiling would hide, taken in the day's hash order so it rotates rather than pinning the richest card. Premium-bearing days 0 → 60 at every lifted rung, tier 0 still 0. Reuses §DX-02ei's seat closure; §9's guard now asserts from the shown set and is red at 68bd466 |
| 7 | 2026-09-06 | §DX-02ei | `818e9c7` | the Warrant's Board reserves one slate seat for the `BOARD_LOCAL_PLACES` nearest destinations with work, taken in the day's own hash order so it still rotates; the worst day's nearest card at `TLL` falls 25 → 12 legs and a host's worst best-card 65 → 15 across all 39 hosts. The row's absolute "≤10 legs daily" floor was disproved on the ground — 12 of 1,063 eligible quests at `TLL` are that near — so the guarantee ships relative and the population is filed as §DX-02it. `_roadGridNearest` shares `_roadGridPathCore`'s relaxation through the extracted `_roadGridWalk` |
| 8 | 2026-09-05 | §DX-02eq | `911348b` | five pit perks wrote five flags nothing read; Weckmann's Lesson is wired against the existing exhaustion-DIS clause — spent only when exhaustion bites, latched per encounter, returned by a long rest — and the other four writes are deleted. `readTheRoom` could not be granted: the pre-combat threat tier it promised already ships to everyone, so wiring it would mean withholding it. Flanking/shove/regen are §DX-02is |
| 9 | 2026-09-05 | §DX-02ex | `1273d2f` | `couperiDebtDegraded` carried both writers' opposite meanings, so a run that never took Quill's quest was philosophised at about releasing a debt it left alone; `couperiDebtReleased` split out, and Beat 3 + both epilogue conditions + the dialogue injection read it, so the neglect branch finally speaks S30's authored line. Beat 3 was also unreachable after a degraded run and now is not; saves migrate by `quillQuestComplete`, which tells the two writers apart exactly |
| 10 | 2026-09-03 | §DX-02ew | `47cd24b` | the inn ledger's ceiling is a balanced book at last — pantry +3→+2 closes at exactly 0 in all six orders, per §VII and world.md agreeing; the pantry narration stops asserting a sign it cannot know; the printed-figures mismatch withheld as §DX-02iq because it needs the author |
| 11 | 2026-09-03 | §DX-02et | `16ce65b` | Layer 44's six world notes get `S_story.worldLog` and a panel section under the design's own stranded `.journal-entry.world` rule, out of the movement breadcrumb array; storyMsg declined because the notes are meant to be missable; old saves migrated on load |
| 12 | 2026-09-03 | §DX-02fv | `2cf3462` | `choice` gets its second consumer surface: TL Vonn sliced into NODE_VERBS with the answer as the choice prompt, and a self-guarding `when` — the driver's closing re-render draws verbs before quest completion, so a status-only guard redrew a payable button |
| 13 | 2026-09-03 | §DX-02ip | `62fd7dc` | the failed-check effort grant scaled off the first `reward` bit rather than the sum the pass actually pays; now a `reduce`, provably behaviour-preserving at 0 multi-reward checks in the corpus, with a fixture red at HEAD |
| 14 | 2026-09-03 | §AUDIT-03bm | `12007f2` | the XP economy names its own edge: the failed-check dial is proportional, not a floor, so token-only checks pay a mission bit rather than XP by design; a flat `mission_bit` grant declined at 24,500 XP against the corpus's 22,307 XP of quest reward, and the census re-derived per run |
| 15 | 2026-09-03 | §DX-02aq | `a1d97fa` | the inventory panel splits `S_story.knowledge` by shape into Necklace beads and Field Notes, so lore notes no longer render as `undefined` |
| 16 | 2026-09-03 | §DX-02bt | `b545133` | the custom-waypoint and corpse quest cards build on `textContent` like the main renderer; the two dialogue maps name each other |
| 17 | 2026-09-03 | §DX-02do | `fdf9589` | Entry 42 and the Defiant Fields secret, the two player-typed strings, reached the DOM as markup; both now escape through `_mpEsc` |
| 18 | 2026-09-03 | §DX-02ae | `6464da3` | the Act VIII Brynn farewell accessor wrote a progression flag while rendering; now pure, with the write at the delivery site beside the latch, and the Yael scene comment names its own guard |
| 19 | 2026-09-03 | §DX-02ac | `15ba42a` | seven dead `abilityScores` fallbacks named a starting character the game never made; collapsed, and the four documents that believed them corrected |
| 20 | 2026-08-23 | §DOC-02dc + §VM-01-G4 | `f340143` | the child design pass whose one missing opcode was the last one the VM ever needed, whose only carried number is its only wrong one, and whose nominated pilot for the… |
| 21 | 2026-08-23 | §DOC-02db + §VM-01 | — | the design that survived eleven slices and 26 days at 100% of its symbols, whose one unmodelled shape was a button on the wrong side of a `<div>`, and whose act number… |
| 22 | 2026-08-06 | §11f + §11 | `3338def` | `NWI`'s Warmth Eel panel (named **first** in §11's own census table) and the `HKG` Void Below chip were censused and then dropped by the arc partition; the third… |
| 23 | 2026-08-22 | §DOC-02cv + §BOARD-01 | `8dcca67` | the origin lock with a perfect anchor score and a census that measured half the corpus, whose two "pure transliteration" increments required two invented edits, and… |
| 24 | 2026-08-22 | §DOC-02cu + §BOARD-01-FU7 | `e0051a8` | the design lock whose data table shipped byte-identical and is still byte-identical a month later, whose spec omitted the one edit that made half of it work, and whose… |
| 25 | 2026-08-22 | §DOC-02ct + §BOARD-01-FU8 | `4af74b4` | the design lock that named an invariant in its first design call and violated it in its fourth section, and the prover that could not see the violation because it could… |
| 26 | 2026-08-22 | §DOC-02cs + §VM-01-F | `c6be7f8` | eighteen of twenty-one anchors exact and the three misses are exactly the three the author hedged with a tilde; the gate learned to say `or`, has had one speaker for a… |
| 27 | 2026-08-22 | §DOC-02cr + §3 | `549d6b4` | the design lock whose code shipped byte-identical and is still byte-identical a month later, and whose speedup is 3.6× rather than the thousand-fold its own big-O… |
| 28 | 2026-08-22 | §DOC-02cq + §10 | `354b20a` | every one of the prover's own seven corpus figures reproduced exactly on the pinned build, and the increment credited with consuming the kernel turns out to have… |
| 29 | 2026-07-28 | §RESUME + §DOC-02 | `549d6b4` | `check:anchors` ✅ **4,191 → 4,248 across 100 → 101 docs, +57, 0 dead** — **45 in the report** (which carried none before: it used the bare-number form throughout, so the… |
| 30 | 2026-08-22 | §DOC-02cp | — | eighteen line numbers, eighteen exact — the cleanest anchor result the program has scored — and the one promise the kernel did not keep is the one that rolls the dice |
| 31 | 2026-07-28 | §DX-02dv + §RESUME | — | `check:anchors` ✅ **4,149 → 4,191 across 99 → 100 docs, +42, 0 dead** — **23 in the report** and **19 across the two new rows, the §DX-02dv resolution, the program row… |
| 32 | 2026-08-21 | §DOC-02co + §VM-01-C | `c22f4f0` | the destination file names the source — `src/scripts/check-questgraph.js:35` reads *"the written-by-nothing / read-by-nothing detector §VM-01-C deferred here."* **§6.1… |
| 33 | 2026-08-21 | §DOC-02cn | — | 27 of 28 line numbers correct against a build that was never committed, and all four wrong figures are in the prose that frames them |
| 34 | 2026-08-21 | §DOC-02cm | — | every code block byte-exact, 33 of 33 anchors resolving, the acceptance suite still 5/5 thirty days on — and all three wrong numbers in one paragraph, the one written… |
| 35 | 2026-08-21 | §DOC-02cj | — | the report got four of its own counts wrong and three of its four open questions right, and the repo built every one of them |
| 36 | 2026-08-21 | §DOC-02ci | — | the parent review shipped five of seven faces the day it was written, and every one of its four errors is a citation, three of them in the tier marked "no design call" |
| 37 | 2026-08-18 | §DOC-02cg | — | the increment shipped ten of ten and deferred five of five, and the sentence it put on the opening screen is false in the direction that kills the player |
| 38 | 2026-08-18 | §DOC-02cf | — | the lock has not drifted a byte in 37 days, and half of what it shipped has never once returned true |
| 39 | 2026-08-18 | §DOC-02ce + §PLAY-01-A | — | §PLAY-01-A shipped the goal whole, and then a design reversal landed on one of its two surfaces |
| 40 | 2026-08-18 | §DOC-02cd | — | the corpus's first perfect dating result, on a feature that ships the XP and withholds the telling |
| 41 | 2026-08-18 | §DOC-02cc | — | the cleanest ship in the corpus, and every failure in it is a claim about its own surroundings |
| 42 | 2026-08-13 | §DOC-02bc | — | the one row it certified in its own words is the one row its own line numbers disprove |
| 43 | 2026-08-13 | §DOC-02bb | — | everything it read is right, and everything it added up is wrong |
| 44 | 2026-08-13 | §DOC-02ba | — | the diagnosis outlived the remedy by two months; the remedy lasted four hours |
| 45 | 2026-08-12 | §DOC-02am | — | the defect was a missing CALL, not a missing SYMBOL |
| 46 | 2026-08-12 | §DOC-02aa | — | a flag written and read eleven lines apart deleted the middle layer of a three-layer design, and nobody noticed for 79 days because the line still arrives |
| 47 | 2026-08-12 | §DOC-02w | — | the mechanism is perfect, the design is unreachable, and the decision that scoped it was reverted by a commit about book imports |
| 48 | 2026-08-12 | §DOC-02v | — | the migration shipped complete, was silently reverted the same afternoon by a commit about book imports, and came back two-thirds |
| 49 | 2026-08-12 | §DOC-02t | `3deef54` | the design the report explicitly FORBADE is the design that shipped — and two Layer-18 stat fields drifted away from their only readers |
| 50 | 2026-08-12 | §DOC-02r | `33c2166` | a 100 % symbol census over a feature that is 55 % unreachable — and the arc's hardest check is a button |
| 51 | 2026-08-12 | §DOC-02n | `c0a24eb` | the design was a TRADE — take magic weapons away from monsters, give them back through fishing — and only the taking half shipped |
| 52 | 2026-08-12 | §DOC-02m | `33f09ad` | the report's design premise was right, was built, and was then extended fourfold — and nobody can reach any of it, because an arctic node is declared 59 lines before the… |
| 53 | 2026-08-11 | §DOC-02j | `7e62f43` | a report whose own appendix says its subject was never built, and six of its seven mechanics are live code — while the table it wrote to persuade you names two monsters… |
| 54 | 2026-08-11 | §DOC-02e + §DESIGN-03 | `3d0ab1d` | §7 of the report specified *"show the level-up modal immediately"* on a passing roll, and **HEAD does not do it** — `_checkLevelUp()` is a mutator that advances… |
| 55 | 2026-08-06 | §VM-01-G-FU-f2 + §11 | `9c9fe42` | TLS authors `finalBattle:{minLevel:20,minShards:7}` (via `WBAPI.editStructuredField` in-process — 1-line data diff) and `_finalBattleReady(code)` replaces the three… |
| 56 | 2026-08-06 | §SPARK-01-FU + §LXX-01-FU | `3338def` | stripping the button side alone was insufficient for the §LXX trio because their completions were *arrival-satisfiable* (`sunken_02` flags+itemsAll+atNode · `depth_01`… |
| 57 | 2026-08-05 | §VM-01-G-FU-f + §XLV | `6a571de` | git diff +128/−124 (124 verbatim + 2-line wrapper + 1 registry + 1 dispatch), line-multiset 124/124 zero unmatched; 16-combo golden HEAD-vs-after over the full… |
| 58 | 2026-08-05 | §VM-01-G-FU-e + §LXX | `3dfdc26` | `quest_sunken_02`/`quest_depth_01`/`quest_forge_02` each carry the button's entire effect in `onComplete` and auto-complete on the arrival that draws the button… |
| 59 | 2026-08-05 | §VM-01-G-FU-d + §SPARK-01 | `8cdda7b` | 49-combo golden HEAD-vs-after over sibling DOM + bboxes + msg text/class + overlay/`_preBattNode` + state incl. 13 click combos — **46/49 byte-identical, the 3 deltas… |
| 60 | 2026-08-05 | §VM-01-G-FU-c + §ALCHEMY-01 | `7d8cb39` | 37-combo golden HEAD-vs-after over sibling DOM + bounding boxes + msg text/class + pre-battle overlay/node + state (roen chain, wisdom pages, knowledge, inventory),… |
| 61 | 2026-08-05 | §VM-01-G-FU-b + §HUNT-01 | `cf2fec8` | 22-combo golden HEAD-vs-after over sibling DOM + bounding boxes + msg text/class + pre-battle overlay/node + state, incl. 4 click-outcome combos — **22/22… |
| 62 | 2026-08-05 | §VM-01-G-FU-a + §CROWN-01 | `56c08f1` | 26-combo golden HEAD-vs-after over DOM+bbox+msg+overlay+state — **14 byte-identical · 8 id-only (`story-*`→`verb-*`, every bounding box equal) · 4 named deltas, two of… |
| 63 | 2026-08-05 | §VM-01-G-FU + §CROWN-01 | `02ff4aa` | after the generic sections sits a SECOND special-case stack, ~1,200 lines / ~38 blocks, accreted by the NEWER arcs (§CROWN-01 · §LXX-family · the… |
| 64 | 2026-08-05 | §VM-01-G4d + §VM-01-G4 | `b0c2478` | CDG's three boss confrontations as `NODE_VERBS` group `cdg-boss-menu` rendered into the same `#cq-boss-buttons` container (`_renderNodeVerbs` grew the 4th `container`… |
| 65 | 2026-08-04 | §VM-01-G4c + §VM-01-G4c-FU | `4c2a831` | four D1 surfaces as `NODE_VERBS` button entries — Sweelinck's S49 scene at `NUE`, Ori at `STN`, Yva at `TRD`, Brynn's firewood at `TLL` — plus the Junction Vignette's… |
| 66 | 2026-08-04 | §VM-01-G4b + §9 | `f7a60a5` | `NODE_VERBS` + `_renderNodeVerbs` — the third small vocabulary beside `NODE_PANELS` (flavour a node *shows*) and `NODE_HOOKS` (an interface a node *owns*) — and Kern &… |
| 67 | 2026-08-04 | §VM-01-G4a + §10 | `b905733` | the **`cost`** opcode, registered next to `reward` because it is `reward`'s inverse and because `reward` is precisely how you get this wrong — `gold:-50` computes… |
| 68 | 2026-08-04 | §VM-01-G2b + §AUDIT-03 | `bedf2c1` | no **top-level `return`** (all 13 sit inside click handlers — one at block level would have gone from exiting nothing to exiting a hook early) and no **`var`** (nothing… |
| 69 | 2026-07-31 | §DX-02g + §AUDIT-03d | `f229ede` | `void_rat_swarm → easy` settled itself (6 of 8 nearest stat-neighbours `easy`; exact AC/HP/atk twin is Jackalwere), while `void_shaman` was a real fork — its two… |
| 70 | 2026-07-28 | §VM-01-G3 + §XX | `a721254` | (1) most of the 15 quests ALREADY carried an `activateNode` — appended as a DUPLICATE key below the authored `activateNode:null` by the `a721254`/`ea02faf` audit waves… |
| 71 | 2026-07-28 | §VM-01-G2 + §D01-10 | `8973fd7` | the sketched single dispatch loop is order-UNSAFE — Corelli's gate is the computed `_checkCorelliAppearance` schedule (any of 5 nodes), so no single dispatch position… |
| 72 | 2026-07-28 | §VM-01-G + §7 | `cf2c17c` | table order = old source order and every extracted block must precede, in old source, every block that can co-fire on its node — which excludes the BK approach panel… |
| 73 | 2026-08-06 | §VM-01-G-FU-f2 | `9c9fe42` | The three engine specials — all three design asks answered |
| 74 | 2026-08-06 | §SPARK-01-FU | `3338def` | The Aldous confrontation double-pays |
| 75 | 2026-08-06 | §LXX-01-FU | `3338def` | All three §LXX-family button/quest pairs double-pay |

**74 completed entries.**

---

## Phase 3 — Content & Narrative

| # | Date | Increment | Ship | Headline |
|---:|---|---|---|---|
| 1 | 2026-09-07 | §DX-02da | `3727b0c` | the row said a failed corridor check says nothing; it says `failText`, in the h-card's `dmg` slot, and always did — so its `onFail:[]` is correct and the proposed narrative bit would have doubled the sentence. **The defect beside it was real:** `execBits(sc.onFail …)` carried no `pushMsg`, so any narrative bit on the fail path went to `storyMsg` and was overwritten by the `storyRender` two lines down — §DX-02gc's mechanism one surface over. **2,636 skill_check bits · 34 with a non-empty onFail · 6 with a narrative bit in it**, `quest_wis_01`…`06`, each a full authored paragraph never shown. Fixed by giving the fail branch the sink the pass branch has had since §BOARD-01-FU6; `check:invariants` gains **I6**, red before at 1/2 |
| 2 | 2026-09-07 | §AUDIT-03bk (part) | `800b5ae` | the navigability half shipped and the conversion half was **disproved**. `completion.atNode` is an AND term, so it can only withhold completion — the row's stated hazard (a quest firing early) is the one thing it cannot do, and the unstated one is that a quest satisfied elsewhere can never complete until the player walks back. Its remedy also conflates two fields: of the 19 quests with both, **15 are `atNode == waypointNode`** and **4 are `atNode == activateNode`** — fetch-and-return, where the waypoint is the errand and the giver is the finish. Retagged 🟢 → ASK with the sorting rule written out. What shipped: `check:questgraph`'s arrival-route census, red at HEAD on **`quest_sea_01`**, which completes only at `NWI`, is given at `SEN` and published no waypoint — the Navigate button never appeared for the one quest whose whole completion condition is going there. **unrouted 1 → 0** |
| 3 | 2026-09-07 | §DX-02x | `2d94547` | the raw ledger write this row was filed against had already closed at §DX-02cm (`grep -c` **0** at HEAD), so what shipped was the sweep it asked for and the fence under it. **Four assignments to `S_story.npcFavorability`, all four already inside the promotion path** — the setter, the Dear Friend upgrade it calls, and `storyNewGamePlus` restoring the map across the reset. No raw writer survives, so the census decided nothing, which is the answer. `check:npcregs` phase **10** `writers` declares each with its reason, because the reason is the argument: the setter is absolute, refuses to lower a level, and is the only thing that speaks the tier line — a promotion written past it is relative, unguarded and silent at once. Stale-exemption and no-write-found both go red; selftest 14 → 16 plants |
| 4 | 2026-09-07 | §DX-02gc | `3339c14` | a favor promotion announced the database key at the player — **6 of 15 favor keys had no `BIRKA_NPC_PROFILES` entry**, named by 8 bits, and they are **§NPC-01-SF2's own list** of dialogue-only NPCs. The fix was an **extraction**: `_talkToNpc` already resolved the name correctly thirty lines below, so the chain existed and was simply not the one the promotion used. **6 slugs → 0**, measured by running the shipped resolver over all 15 keys. **The second half is why the first went unseen:** `storyMsg` replaces the strip, and a completion-driven tier line was overwritten in the same synchronous render — the corpus had been hand-writing the acknowledgment into a sibling `narrative` bit for months. Widened through the grammar (`favor(bit, ctx)` → `E.setFavor(..., say)`), `QUEST:CORE` re-inlined byte-identical, driven end to end in Node. `check:npcregs` phase **9** `spoken` asks what phase 3 does not — whether a key resolves to a **name** — with the modelled registries lifted from the resolver's own body; selftest 10 → 14 plants. Two of the row's numbers were wrong (`BIRKA_NPC_PROFILES` is **204** keys, not 9) and are corrected in the close |
| 5 | 2026-09-07 | §GR-FU2 | `a1753de` | the ceremony names whoever sits at `fav >= 2` and `SWEELINCK_NAMING_LINES` was not the set of people who can get there — `emmer`, `gret` and `rennau` each stood at the exact threshold with no line. **7 → 10**, tier 2 only for all three (none has a `DEAR_FRIEND_BITS` entry, so nothing can raise them past their arcs' `set:2`). The call is *the ceremony names everyone the game raises to its own threshold*, because that is already the rule the builder implements. `check:npcregs` gains **phase 6**: it reads the threshold out of the builder's own `fav >= N` and computes each NPC's favor ceiling the way the runtime does — red at `ba29134` with exactly the three, green after, selftest 6 phases → 7. Repaired `audit03n-npc-registries.test.js`, whose *"exactly the six canonical keys"* assertion had been red since `3c816a5` and unread because `npm test` fails as a whole on this musl host. Filed **§GR-FU3** (three authored `3:` states the corpus cannot reach) and **§DX-02jd** (427 browser-free tests nothing runs) |
| 6 | 2026-09-07 | §GR-FU | `3c816a5` | the La Riva arc raises Kenickie to Dear Friend and `SWEELINCK_NAMING_LINES` held six keys, none of them his, so the Covenant Ceremony could not name the person the arc exists to have helped — **6 → 7**. The lab report's deferred text (*"who kept the account book. Who knows every name in it"*) was **not** used: Aldo kept the book and the delivery narrative says Kenickie *"does not open it"*; shipped *"Kenickie, who took the book without opening it. Who is still holding onto it."* Tier 2 deliberately absent — nothing can put him at 2 — with one comment saying so. `check:npcregs` accepts the key unchanged. The row's *"check the other five"* grew into **§GR-FU2**: `emmer`, `gret` and `rennau` are written to fav 2, the ceremony's own threshold, and none is in the table |
| 7 | 2026-08-23 | §DOC-02cz | `32c10c5` | the most completely-shipped lock the program has scored, whose best Froberger trace has never been read because a hardcoded `2` sits three hundred lines away from a gate… |
| 8 | 2026-08-23 | §DOC-02cy + §DOC-02cx | `32c10c5` | the most faithfully-transcribed lock the program has scored, whose off-screen character is named by nobody, whose six journal notes are posted into the movement… |
| 9 | 2026-08-22 | §DOC-02cx | `32c10c5` | the design lock whose every string survived ninety days byte-for-byte, whose best ending has never once fired, and whose five perks are congratulated, badged, carried… |
| 10 | 2026-08-21 | §DOC-02cl | — | the thesis was right, the plan was right, it shipped whole in nine hours — and the one census row it counted with a regex instead of a parser became a shipping defect… |
| 11 | 2026-08-21 | §DOC-02ck | — | the same uncommitted user recolor is still in the tree (HP-bar gradient, light-green playfield, theme-ink die numerals, self-labelled *"user recolor 2026-08-06"*) —… |
| 12 | 2026-08-18 | §DOC-02ch | — | the string is byte-exact at 37 days, the reason given for the fix beside it is false, and the road it signposts is closed at both ends |
| 13 | 2026-08-18 | §DOC-02cb | `27c38f5` | the most faithfully executed lock in the corpus, describing a feature no player has ever reached |
| 14 | 2026-08-17 | §DOC-02bz | `fcb79dc` | the arc is built exactly as written, and it has never been playable |
| 15 | 2026-05-25 | §DOC-02ar + §XVI | — | four corrections in maintained docs, all measured.** `world.md`'s Void Archaeology heading read ⚠️ **PLANNED** while `story.md:1632` read ✅ *Implemented 2026-05-25* —… |
| 16 | 2026-08-14 | §DOC-02bg | — | 59 of 62 citations exact and six node codes wrong, and the engine's own comments are why |
| 17 | 2026-08-13 | §DOC-02au | — | the phases were right and the brakes were prose — every loop cap in the "All limits prevent runaway" table is understated by four orders of magnitude |
| 18 | 2026-06-09 | §DOC-02as | — | ⟶⟶ §DOC-02as ✅ SHIPPED: the last file in the `2026-06-09 11:06:54` block, and the corpus's best node-code result sits on top of an arc that has never been playable for… |
| 19 | 2026-08-13 | §DOC-02ar | — | a 100 % identifier census over an arc that has never been completable past its second quest, and the report is the evidence that the engine has a typo |
| 20 | 2026-05-25 | §Weimar + §Layer | — | `_applyTomeBonuses()` (0 hits; ships as `_tomeBonuses()`), `tome_rasp_annotated` (0 hits), and *"NG+-persistent"* tomes (NG+ resets inventory from `_S_DEFAULTS()`). The… |
| 21 | 2026-08-12 | §DOC-02an | — | a 100 % census beside an arc that has never been played |
| 22 | 2026-08-12 | §DOC-02al | — | its drafts died and its ideas shipped |
| 23 | 2026-08-12 | §DOC-02aj | `7de58fb` | a spec that enumerates names ships its names; a spec that specifies voice ships nothing but its names |
| 24 | 2026-08-12 | §DOC-02ai + §DOC-02aj | `05cfa22` | a world where Saul of Tarsus walks from Jerusalem to Damascus and is healed by a man named Anath.** *(Its THIRD finding — the Thorn — was withdrawn by §DOC-02aj above:… |
| 25 | 2026-08-12 | §DOC-02ae | — | the roadmap that said "none of these can be started in this session," and shipped one of them thirty-three minutes later |
| 26 | 2026-08-12 | §DOC-02ad | — | a finished instrument, a silent game, and a default registration that is a Flute wearing a Principal's label |
| 27 | 2026-08-12 | §DOC-02ab | — | the schema shipped and scaled 35×, the script shipped 0 of 120 lines, and the ending it was written to earn now accuses the player who earned it |
| 28 | 2026-08-12 | §DOC-02y | — | three companion arcs almost perfectly intact, and an engine comment that denies eight node records while quoting their labels |
| 29 | 2026-08-12 | §DOC-02u | `6ab2a08` | a near-perfect implementation of an arc that no player can enter — the program's FIRST 100 % casualty |
| 30 | 2026-08-12 | §DOC-02s | `f622342` | the strongest survival the program has measured, fully reachable — and the arc's own stated payoff is the one thing that never shipped |
| 31 | 2026-07-07 | §XI | — | a node-text swap reading *"Two of the three blocks are still rubble. The third has a market in it — one stall…"* — so **the flag went from write-only to read**,… |
| 32 | 2026-05-26 | §DOC-02n + §III | — | §III's five-act prose was **compressed to a design table** because `story.md §GRIEF AND CORRUPTION` holds the prose verbatim and marks it *"✅ Implemented 2026-05-26"* —… |
| 33 | 2026-08-12 | §DOC-02q | — | a shop that works exactly as written, and a statistics ledger that forgets everything at the one boundary it was built to survive |
| 34 | 2026-08-12 | §DOC-02p | — | the origin specification of the Curse Score, implemented almost perfectly, describing a system that has not been able to run for 75 days |
| 35 | 2026-08-12 | §DOC-02o | — | the report's thesis names a payoff, and the payoff is unreachable — the game declares four endings and can produce exactly one, the failure one |
| 36 | 2026-08-12 | §DOC-02l | `62b162a` | the spec shipped almost perfectly, then a rename broke the one link nobody greps for — the engine COMPUTES its quest ids, and forty of them stopped existing 75 days ago |
| 37 | 2026-06-09 | §7 + §DOC-02m | — | IEEE-shaped (abstract · 6-instrument method · as-built inventory · node-code translation · **35-row two-way delta table** · 5 findings · 20-row NPC contract register ·… |
| 38 | 2026-08-11 | §DOC-02k | `9d3e06f` | the one field-name this design lock got wrong is the one it spells two ways WITHIN ITSELF — and the engine followed the table, not the prose |
| 39 | 2026-06-09 | §DOC-02f + §7 | — | IEEE-shaped, **326 → 295 lines**; ~75 lines of pre-implementation prose replaced by a **41-row two-way delta table**, six numbered findings, a decision register and a… |
| 40 | 2026-08-11 | §DOC-02h | `8986fa8` | the most faithfully implemented arc in the program is 70% unplayable, and the report got every coordinate right |
| 41 | 2026-08-11 | §DOC-02g + §DOC-02d | `70d49a0` | legacy `BK` = Broken Tooth Tavern, Visby = live `VBY`; `BK` still resolves, as a **Birka beach**, so `check:noderegs` passes while Corelli says *"Visby. Well."* and… |
| 42 | 2026-08-11 | §DOC-02d + §DOC-02 | `bb9bd58` | the same uncommitted user recolor is still in the tree (HP-bar gradient / light-green playfield / theme-ink die numerals, self-labelled *"user recolor 2026-08-06"*) —… |
| 43 | 2026-08-11 | §DOC-02c + §DOC-02 | `cc4a07e` | the same uncommitted user recolor is still in the tree (HP-bar gradient / light-green playfield / theme-ink die numerals, self-labelled *"user recolor 2026-08-06"*) —… |
| 44 | 2026-08-11 | §DOC-02a + §DOC-02 | `e784721` | `play.html` carries an **uncommitted user recolor** (HP bar → brown/gold gradient, playfield zones → light green, die numerals → theme ink, all self-labelled *"user… |
| 45 | 2026-08-04 | §AUDIT-03p + §AUDIT-03d | `ea19a88` | `check:noderegs` **phase 5** applies the same explicit `LOCAL_NODE_KEYED`/`NOT_NODE_KEYED` classification to **indented** `const/let/var NAME = {` literals — phases 1–2… |
| 46 | 2026-08-04 | §AUDIT-03k + §AUDIT-03d | `1c4f547` | `WBAPI.NPC_ALIASES` + `npcCanonicalKey()`; the seven slugs **out of `npcKeyVocab()`** (297→290), so a quest still carrying one advise-warns rather than validating beside… |
| 47 | 2026-07-31 | §AUDIT-03n + §AUDIT-03d | `1be43be` | the epilogue's `npcOrder` and **five `_npcFavor('bruhns')` gate sites** — the §XXXVII CO scene at TLS, the S6 Weckmann/Auros joint conversation (**which ANDs a live key… |
| 48 | 2026-07-30 | §AUDIT-03h + §AUDIT-03d | `f96b4e5` | `guide_01–05`→`emmer` · `guide_06`→`the_fisherman` · `scar_01/03/04`→`gret` · `scar_02`→`pier`, each corroborated twice (profiles at the arcs' own `activateNode`s… |
| 49 | 2026-07-29 | §AUDIT-03g + §AUDIT-03d | `f08f70f` | the load-bearing discovery is that **a §D02 Epic-Battleground *approach arc* inherits its battleground's giver** — `quest_ea_primary`/`_return` at RAI already carry… |
| 50 | 2026-07-29 | §AUDIT-03b + §AUDIT-03g | `221a1ef` | the ~12 OPEN per-quest calls resolved by reading each quest's text; the whole 203-quest mapping re-derived and applied (**203/203 correct · 7 genuine Silver quests kept… |
| 51 | 2026-07-28 | §NPC-01-SF4 + §NPC-01 | — | each cluster's arc has exactly ONE render-gate node in the live file — **CQ→CDG** (Layer 44 Ally Cat Arc gates on `node.code==='CDG'`; Jimmy is CDG's own NODE_MAP… |
| 52 | 2026-07-28 | §NPC-01 | `76ad683` | The Derivable NPC Card Map (fully closed; promoted §POT-R…) |

**48 completed entries.**

---

## Phase 4 — World & Navigation

| # | Date | Increment | Ship | Headline |
|---:|---|---|---|---|
| 1 | 2026-08-22 | §DOC-02cw + §KG-01 | `65d65c0` | the content report whose every number survived 45 days, whose road was laid by the method it forbids, whose sea-cell violation hid behind a red that was already red, and… |
| 2 | 2026-08-17 | §DOC-02by | `7a4570e` | a design lock written 23 minutes before the code, exact in every number six weeks on, and wrong about one room's occupant |
| 3 | 2026-08-17 | §DOC-02bt | — | every statistic in the diagnosis re-derives exactly, and the one sentence nobody filed as a row cost four red tests for six weeks |
| 4 | 2026-07-05 | §ARCH-01 | — | expected `docs/lab-reports/lab-report-uqf-migration-playbook.md` (`2026-07-05 14:42:46`), the §ARCH-01 UQF migration playbook. It is a **method** document rather than a… |
| 5 | 2026-08-14 | §DOC-02bn + §AUDIT-03x | — | the corpus's most accurate report, and the one sentence in it nobody measured is the origin of §AUDIT-03x |
| 6 | 2026-08-14 | §DOC-02bl | `c946a01` | the removal shipped completely, and was undone twelve days later by a track that reused every one of its names |
| 7 | 2026-08-14 | §DOC-02bk | — | the program's first FULLY-SHIPPED verdict, its highest symbol score (43/43 node codes), and the one exception it asked for was taken and then given back |
| 8 | 2026-08-14 | §DOC-02bj | — | its geography is byte-perfect and its arithmetic reverses its own thesis. THE 2026-06-16 BLOCK IS NOW EMPTY |
| 9 | 2026-08-13 | §DOC-02be | — | an unusually strong transcriber and an unreliable narrator — 31 of 31 citations exact, and every error in a sentence the author composed |
| 10 | 2026-08-14 | §DOC-02bd | — | everything it borrowed is exact, everything it counted is wrong, and the world it describes is three times too small because of a space before a brace |
| 11 | 2026-08-13 | §DOC-02az | — | the report is correct and the ground moved |
| 12 | 2026-08-12 | §DOC-02z | — | the program's most faithful content spec, into a world that moved out from under four of its nodes |
| 13 | 2026-08-11 | §DOC-02f + §DOC-02 | `ffe7720` | the uncommitted user recolor (HP-bar gradient / light-green playfield, self-labelled *"user recolor 2026-08-06"*) is still in the tree — **left untouched and unstaged**;… |
| 14 | 2026-07-31 | §AUDIT-03j + §AUDIT-03d | `c5f9079` | (1) **the table's other column was dead too** — its npc keys were the profiles' SURNAMES (`couperin`=Bard Tomas Couperin=`quill`, `weckmann`=Pit Master Weckmann=`crov`,… |
| 15 | 2026-07-29 | §AUDIT-03l + §AUDIT-03c | `d42a707` | `check:walk` **12/12 exit 0**; new `audit03l-node-index.test.js` **5/5**; **two negative controls** — a bogus row appended to the committed index, and a real node… |
| 16 | 2026-07-29 | §AUDIT-03c + §AUDIT-03d | `710bb75` | `CQ` = **`CDG`** (The Cat Quarter) and `FR` = **`AMS`** (Fishmonger's Row) are both **right**, corroborated three ways each — the nodes' own labels/inline NPCs, and the… |
| 17 | 2026-07-29 | §DX-02e + §KG | `8fc9a2e` | auto-travel routes with `_roadGridPathCore` (§NAV-01d road-weighted Dijkstra, road/sea-lane cost 1, everything else 2), and all six legs `TLL→SPB→KMS→ZVD→FBR→TVR→SVO`… |
| 18 | 2026-07-29 | §AUDIT-03e + §KG | `2da7fbb` | one normalisation line after `NODE_MAP:END` (the data section keeps its 129 authored `code:` fields — not 287 new ones) + the `NODE_CODE_BACKFILLED` set captured just… |

**18 completed entries.**

---

## Phase 5 — Platform & Tooling

| # | Date | Increment | Ship | Headline |
|---:|---|---|---|---|
| 1 | 2026-09-07 | §DX-02jm | `06eb1bf` | `pgrep -f`/`pkill -f` matched the shell that invoked them — demonstrated twice: `monitor: UP` against **0** running monitors, and `./run.sh stop` **killing its own caller** (rc 143). All three fixes the row proposed are rejected on the ground (a pin cannot serve two callers launching by different paths; a PID file the monitor never writes; `--ignore-ancestors` is procps-ng only, the §DX-02jc mistake repeated). Ships `src/bin/procmatch.sh` — `match_pids` walks `ps -o ppid=` from `$$` and drops **ancestors** while keeping **descendants**, which the toggle needs. Wired into `run.sh` and `wbapi-toggle.sh`, one of whose sites is a `kill -- -PGID`. **§DX-02jo** filed and it corrects an earlier cause in this table: `say.sh` launches `$ROOT/sayd.sh` while the daemon is `src/bin/sayd.sh`, streams discarded, so nothing has ever been spoken |
| 2 | 2026-09-07 | §DX-02jn | `f39f538` | `GET /api/help/cli` executed `ROOT/api.sh`, a binary §DX-02gh records as deleted, and answered **200** with `live:true` and **0 bytes** — the one topic that promises to be current was the only empty one. Now `bin/api`, with **502** when the child fails or produces nothing. **0 → 54,804 bytes**; 502 proven by moving `bin/api` aside and 200 by restoring it. **Neither help instrument could see it:** gate #23 starts no server, and the harness read an empty body as nothing to check — the vacuity its other extractors are all guarded against. `[help/topics]` now fails an empty 200 (selftest **19 → 20**), and the fix widens the harness by itself: **79 → 83** GET calls over **43 → 46** paths |
| 3 | 2026-09-07 | §DX-02ie | `b7c7e0b` | `bin/wbapi` forwarded its arguments to `make` as targets, so `./bin/wbapi restart` started a server and then failed with `No rule to make target 'restart'`, exit 2 — the one hazard-avoidance step the standing procedure names had no working command behind it. Real subcommands (**start·stop·restart·status**, usage + exit 2 on anything else), `restart` and a scoped `stop` added to `run.sh`, `make restart` added. **The sweep behind it was wider than "docs": 34 refs over 21 files**, including `src/bin/coc`'s two **broken** paths and **7 live strings telling a PLAYER** to run a script that is not there. Overlap with **§DX-02gh** declared: its `wbapi-toggle` leg → **0**, its `./api.sh` leg re-derived at **483** and still open. **§DX-02jn** filed: `/api/help/cli` executes the same deleted `api.sh` and answers 200 with 0 bytes |
| 4 | 2026-09-07 | §DX-02jc | `b1cb2f7` | `./run.sh status` reported `monitor: down` unconditionally, because `pgrep -qf` printed a usage block and exited **2 whether or not a match existed** — the one line the command exists to produce was the one that could not be true. **The row's cause is disproved:** the host is procps-ng 4.0.6, not busybox; `-q` is **BSD** pgrep's spelling and procps-ng has no short form. `pgrep -f "…" >/dev/null 2>&1` is portable to all three. **A second site with the same flag** — `say.sh:35`, the `sayd.sh` fork guard behind the mandated announcement, its usage block swallowed by `2>/dev/null` — fixed in the same increment. **§DX-02jm** filed: `pgrep -f`/`pkill -f` also match the **invoking shell**, and the `pkill` sites would kill it |
| 5 | 2026-09-07 | §DX-02jk | `7d6f2f5` | `GET /api/help/<any name at all>` answered **200** with the index under the name that was asked for, so a typo was indistinguishable from a topic — the fallback that defeated §DX-02jg(b)'s first assertion. **The design call was whether 404 breaks a caller, and it is settled by census:** `src/api/wb.js` **0** references (the row's named suspect), `play.html` **0**, `bin/` **0**; the one caller, `edit.html`, prints the body at three sites and reads `.status` at none. Unknown topic → **404** naming the real topics; `topics:` corrected **18 → 19** because `cli` is served above the lookup and a corrective list must not omit a topic that works. The harness drops the title-based workaround for the status and gains the **control** that keeps it from being vacuous (selftest **17 → 19**) |
| 6 | 2026-09-07 | §DX-02jg | `e1495ff` + `d06adc2` | the help stopped being prose nothing executes — **(a)** a `check:walk` gate comparing three documented tables against the constants beside them, **(b)** a harness that boots a throwaway server and *calls* what the rest claims (**79 GET calls over 43 distinct paths**, every `export` collection, every nonce `type`). **Six mutations of the real source measure the boundary and neither instrument covers the other:** (a) alone sees a TTL five times short and a value dropped from the help's `type:` line; (b) alone sees an endpoint that has never been a route, an index entry naming a topic the server does not have, and an example naming an entity the corpus no longer holds. **(b) defeated its own first assertion** — `HELP[topic] || HELP['index']` answers **200** and echoes the *requested* name back, so only the title distinguishes a typo from a topic (**§DX-02jk**). Two design calls handed on with their measurements rather than pre-decided: **§DX-02ji** (39 field names diverge from `SCHEMAS` across four topics) and **§DX-02jj** (quest `type` is five lists, no two agreeing, `delivery` 57 quests in neither validator) |
| 7 | 2026-09-07 | §DX-02jf | `2de1a72` | the onboarding example taught the exact write §AUDIT-03av had just undone, and the search above it matched **0 of 416** nodes — but the row's four strings were four of **twenty-six**. Its closing sentence asked for *a sweep for any other help example naming a place the corpus has since renamed*; every example in both help surfaces was then run against a server booted on the edited source. **Four more searches return nothing** (`--q "wolsey"` 0, `--arc shk --q "inventory"` 0 against 28 unfiltered, `--arc shk --q "visby"` 0 against 88 occurrences of *visby* in `play.html`, and `--act 2 --q "birka"` 0 because every *birka* node is act 1) — replaced with terms that land on the very entities the next line of each block fetches, `shk6_act1` and `shk6_act2`. **`CY` at seven sites, including the quick-start banner printed on every server start**, plus `FR`, `EMA` and `coastal_market` — all 404, and `EMA` appears in `play.html` only inside the word `SCHEMA`, one step after the same walkthrough says *ALL activateNode codes must exist first*. **`/api/read/node/CY` is not a route at all** (400 `Unknown type "read"`) — §DX-02jg's own verify condition for its unbuilt option (b), met by hand. 45 distinct GET paths across all 18 live help topics now resolve; the one 404 left is created by the walkthrough that names it. Two further contradictions in `help/node` left standing for §DX-02jg, including `x, y` — fields carried by **0 of 416** nodes |
| 8 | 2026-09-07 | §DX-02fp/fq/fr/fs + §DX-02bb | `ec604a3` | the server's `/api/help` disagreed with the server five ways, and **the two rows saying so were the same row** — §DX-02bb(a) and §DX-02fp share both anchors, filed ten days apart into two phase files. Grepping to disprove the Phase-1 row found the Phase-5 family (§DX-02je's lesson, caught early this time). The row's own fix would not have worked: `"expires"` is not a field — the endpoint returns `expiresAt`. Also `terrain` in a type list that `400`s, a save step retired by §DX-02k, and **three** missing export collections across two lists plus a stale fourth copy in the CLI. **Verified by calling the server**, not by reading it. Gate deferred to **§DX-02jg**: a literal fence would have caught 2 of 5. |
| 9 | 2026-09-07 | §DX-02ix | `40832d2` | `editStructuredField` replaces a field's whole literal, so every comment inside it was deleted with `ok:true` — 23 (entry, field) pairs in `QUEST_DB`, 47 comments, 17 also holding a function, re-derived with a census that reuses `patchLiteralField`'s own token walk. The call was refuse / carry / accept: **refuse, plus an acknowledgement**, because a comment has no preserving escape the way a closure has `{__fn:'<source>'}` and a bare refusal would have made those 23 fields unwritable. `--drop-comments` / `?dropComments=1` accepts the loss and the result reports `droppedComments`. `check:arraypatch` 33 → 40 checks, the corpus half a property rather than a pinned count. Also corrected §DX-02iv: `removeFns` wrote the closure's trailing whitespace into the `{__fn}` marker while the serializer trimmed it, so *byte-identical* had only held for closures that had none |
| 10 | 2026-08-23 | §DOC-02da | — | the report whose architecture survived 85 days and a 3.15× file intact, whose operations were reversed four ways by a single track, and whose documented safety net turns… |
| 11 | 2026-08-18 | §DOC-02ca | `d4a31d7` | four of five recommendations shipped inside 28 days, every derived number is wrong, and the fifth recommendation has a 138-quest bill the CI gate prints and passes |
| 12 | 2026-08-17 | §DOC-02bx | — | seven locked decisions, four non-goals, forty-two days, and the only thing that drifted was the labels |
| 13 | 2026-08-06 | §RESUME + §DOC-02bw | — | `check:anchors` **3,301 → 3,338 across 81 → 82 docs, +37 — 25 in the report, 5 in the three new rows and 7 in this §RESUME entry** (the entry's seven were counted… |
| 14 | 2026-08-17 | §DOC-02bw | — | every increment it planned shipped in four days, every constant and formula still measures exact, and the two mechanisms it named to protect its invariants were never… |
| 15 | 2026-07-06 | §6 | `c78eb4d` | instrument 84 fired at scale.** The report has **fourteen** commits: locked by `c78eb4d` and amended thirteen times as increments shipped, the last four days later. Its… |
| 16 | 2026-08-17 | §DOC-02bv | — | the corpus's best-verified report, and the only one whose entire forward ladder was walked in four days — including the rung that retired its own safety sentence |
| 17 | 2026-08-17 | §DOC-02bu | `1e5de0d` | a method document scored against the corpus it produced — the tables are perfect, the census is wrong by 324, and one commit hash points at the wrong wave while… |
| 18 | 2026-06-30 | §1 + §ARCH-01 | `ab996f1` | the header's *"all 284 QUEST_DB entries"* survived after §1 voided that survey; epics are **Wave 5** in the sequencing rules and risk table but **Wave 6** in the wave… |
| 19 | 2026-08-17 | §DOC-02bs | — | the report was exact on every number it counted, and its subject was deleted six days after it closed |
| 20 | 2026-07-03 | §NAV-01 + §DX-02e | — | expected `docs/lab-reports/lab-report-nav01-navigable-world.md` (`2026-07-03 12:47:32`), §NAV-01, the ROAD_RUNS net / auto-travel / MUD-parity redesign. **It is the… |
| 21 | 2026-08-17 | §DOC-02br | `4a97e50` | a design lock that scored twelve of twelve and had its runtime three minutes later — and the one button its integration section never listed was the only one that left… |
| 22 | 2026-08-17 | §DOC-02bq | — | the design was right, the encoding was inert, and the engine had been demonstrating the correct form several hundred times in the same file |
| 23 | 2026-08-17 | §DOC-02bp | — | the miscount was fixed inside a day, in a gate, and four documents were never told |
| 24 | 2026-08-14 | §DOC-02bo | — | the report's own census counts a sentence of prose as a field, and the patcher it designs is immune to exactly that mistake |
| 25 | 2026-08-13 | §DOC-02aq | — | the most accurate document the program has measured, dated to twenty-two minutes by a file inside its own commit — and its one table that does not add up was reporting a… |
| 26 | 2026-08-13 | §DOC-02ap | — | the tightest dating result in the corpus, a reference manual whose invented examples are the healthy part, and a retrofit that laundered a dead mechanism |
| 27 | 2026-08-12 | §DOC-02ah | `c76b658` | the panel whose whole premise is being exact shipped a line count that was already 1,158 lines stale, and the wizard it argued against was built anyway |
| 28 | 2026-08-12 | §DOC-02ag | `d81a78e` | the document that turned quest content from code into data, and the one section of it that describes something that already existed |
| 29 | 2026-08-04 | §DX-02l-FU + §AUDIT-03d | `3684b67` | the assertion loops over four docs and `expect` aborts at the first failure, so it only ever named `prompt.md:197` — `CONTRIBUTING.md:145`, Hazard #7 itself, is a… |
| 30 | 2026-08-03 | §DX-02l + §AUDIT-03d | `91d3650` | `GET /api/snapshots` (dir, per-file size/mtime, total, and whether the patch chain already holds each one) and `DELETE /api/snapshots`, nonce-guarded like every other… |
| 31 | 2026-08-03 | §DX-02k + §AUDIT-03d | `2ead185` | `save(dest)` writes exactly where told and **refuses with no argument**, naming the alternative in the error text so the next author cannot just pass `getStampedName()`… |
| 32 | 2026-08-03 | §DX-02h + §DX-02i | `a047348` | `world.md` stated in the present tense that both were *"injected into terrain encounter tables at runtime when `voidPressure ≥ 6` via `_applyVoidPressureMonsters()`"* —… |
| 33 | 2026-07-30 | §DX-01c + §AUDIT-03d | `44b67d8` | `WBAPI._sectionCloseIdx` (stops at the first nested START; `insertBeforeSectionClose` delegates, so all 12 create call-sites are covered and the class cannot recur);… |
| 34 | 2026-07-28 | §AUDIT-03f + §ARCH-01 | `6a69087` | it processed comments as code — the §ARCH-01 Wave-1m/1n migration comments' arrow-fn examples (`activateCond:()=>true`, `completeFn:()=>…`) triggered the fn-stripper,… |

**25 completed entries.**

---

## Phase 6 — Verification & Documentation

| # | Date | Increment | Ship | Headline |
|---:|---|---|---|---|
| 1 | 2026-09-07 | §DX-02iw + §DX-02cr | `ea0f156` | four `check:*` scripts were defined in `package.json` and absent from `GATES`, so `check:walk` printed 22/22 over a chain 26 scripts wide. All four are in `GATES` and in `check:walk:serial` now, and `run-gates.js` refuses to start when a `check:*` script is in neither `GATES` nor the named `GATE_EXEMPT` map, or when the serial chain stops mirroring `GATES` — selftest 4 checks → 7, both proved by planting. The row said the four were run by nobody; CI disproved that for three, and `check:duelparity` — the third parity-fenced kernel — was the one run by nothing at all, which is §DX-02cr, closed here with `src/js/duel.js` into both `paths:` filters. The four bespoke CI steps collapse into `check:walk`. 22/22 wall 25.6 s → **26/26 wall 21.1 s** |
| 2 | 2026-08-24 | §DX-02gn + §DX-02eu | `41ffaae` | the suite's screenshot overwrote a tracked file, and the fix was to move the write rather than untrack the file. `living-world-l44.spec.js:210` wrote to… |
| 3 | 2026-08-24 | §DX-02gt + §AUDIT-03bj | `a72688a` | the suite was red at HEAD and the gate chain was green, because they do not test the same thing. `npm test --prefix src` measured **2 failed / 1016 passed**: Waves 2q… |
| 4 | 2026-08-24 | §DX-02bs + §PAUL-01 | `b152350` | the soft-lock gate learns the one writer idiom it could not see, and the row's own count of its false positives was one short. `scanFlagWrites` folded five host writer… |
| 5 | 2026-08-23 | §DOC-METHOD | — | the resumption method written down, and the corpus measured against the window that has to read it |
| 6 | 2026-08-23 | §RELEASE-01 | — | §RELEASE-01 ✅ the infrastructure record landed, and the remedy it prescribes is the one thing this repo does not do |
| 7 | 2026-08-23 | §DX-01g + §DX-02 | — | `BACKLOG.md` split into six phase backlogs by subsystem; the 2.2 MB single file is now a 37 KB routing index.** On *"read backlog. its big. we need to split it."* The… |
| 8 | 2026-08-14 | §DOC-02bm | — | two hours from spec to closed series, twelve of twelve pointers exact, and the one assertion that is red today is the one it wrote to close the series |
| 9 | 2026-06-26 | §DOC-02bk | `7f4b73c` | `7f4b73c` 11:41 → `95c4143` 13:33 on 2026-06-26, one hour fifty-two minutes for spec + 4 increments + an unplanned follow-up.** (§DOC-02bk's "under 20 hours" was the… |
| 10 | 2026-08-14 | §DOC-02bi | — | every filename it catalogued is real, and it counted sixty-five of seventy-one |
| 11 | 2026-08-12 | §DOC-02ak | `7add1d7` | a verification pass tests what it CHANGES and assumes what it LEAVES |
| 12 | 2026-08-12 | §DOC-02x | — | the report that wrote the repo's operating manual, audited by the program it recommended |
| 13 | 2026-08-11 | §DOC-02i | `a123904` | the report that designed the documentation system was right about every number it measured and wrong about the one it cited — and the instrument it recommended is now… |
| 14 | 2026-05-25 | §DX-01e + §DOC-02 | — | FC01 ✅ · FC03 ✅ (`docs/mechanics/mechanics-combat.md` + `-economy.md`) · **FC05 half-shipped** — 93 `// → doc:` pointers live, 48 top-level CAPS consts still without… |
| 15 | 2026-08-11 | §DOC-02b + §DOC-02 | `d5b1cb9` | the same uncommitted user recolor is still in the tree (HP bar gradient / light-green playfield / theme-ink die numerals, self-labelled *"user recolor 2026-08-06"*) —… |
| 16 | 2026-08-04 | §AUDIT-03q + §AUDIT-03d | `28c6f8a` | phase 2 in `src/scripts/legacy-codes.js` (+`eachProseLine`/`localCue` extracted so both phases share one scanner), `BORN_DEAD` + `NOT_A_NODE_CODE` explicit tables with a… |
| 17 | 2026-08-04 | §AUDIT-03m-FU + §AUDIT-03d | `79987d3` | `story-arc-ngplus.md` annotated **8** references and was left carrying **11** — because `nodeContextLine` is a **LINE** test, and *"Write Entry 42 at CI"*, the commonest… |
| 18 | 2026-08-04 | §AUDIT-03m + §AUDIT-03d | `b399318` | `src/scripts/legacy-codes.js` / `npm run legacy`, which reads the LEGACY CODE MAP out of the **generated** node index (never re-typed) and counts a token only in a node… |
| 19 | 2026-08-03 | §DX-01e-FU + §AUDIT-03d | `f4bc624` | `21722`, cited for `execBits`, is now a Mother-Mimic quest body; `25558`, cited for `BOSS_COMMANDER_AUROS`, an `sbo-refocus-bar` style write; `26822` is past the end of… |
| 20 | 2026-07-31 | §DX-01e + §AUDIT-03d | `57d79d7` | the **symbol** is the pointer (any literal substring of the line meant, optionally file-qualified — `` `src/js/wbapi-server.js:function seededNext@1147` ``), the… |
| 21 | 2026-07-30 | §DX-01d + §DX-01i | `e4c03a0` | `WBAPI.deleteEntrySource(section,key)`, one source-level excision for all four types with **verify-or-revert** (the section's depth-1 key *multiset* must lose exactly… |
| 22 | 2026-07-30 | §DX-02f + §7 | `dfeeff4` | `effort-xp` forces the fail through the engine's own dial via a scoped `forceFail(id, fn)` that raises `sc.dc` to 999 and restores in `finally` — the same idiom… |
| 23 | 2026-07-28 | §AUDIT-03a + §DX-01a | — | `check:dupkeys` is gate #11 of `check:walk`; the last-key-wins rot class is now CI-fenced.** Scanner details + the 2 corpus survivors… |
| 24 | 2026-07-28 | §DX-01a + §JUNK-01 | `7f75c54` | J14/J15 deleted (hand-edit per Hazard #4, server confirmed stopped; zero other live refs by repo grep), then `build-roads.js --apply` (the canonical do-not-hand-edit… |
| 25 | 2026-07-28 | §DX-01f + §RESUME-tail | `c9f3946` | 276 lines of closed content moved **verbatim** (scripted line-range surgery — no paraphrase, boundaries asserted before cutting) to plan-archive.md §"Archived 2026-07-28… |

**24 completed entries.**

---

*© 2026 Paul Richeson — MIT License.*
