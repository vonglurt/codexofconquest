<!-- SPDX-License-Identifier: MIT — Copyright (c) 2026 Paul Richeson -->
# Archived §RESUME history — completed backlog work

> **Completed increments only.** Everything here shipped and is closed; the six `docs/backlog/BACKLOG-*.md` phase files now carry open rows only. Each entry is condensed to one line — date, section, ship SHA, headline.

> Full prose for recent closes is in [`plan-archive.md`](../backlog/plan-archive.md); the cross-phase chronology index is in [`BACKLOG.md`](../backlog/BACKLOG.md). Anything not in either is recoverable from `git log -- docs/backlog/`.

> Moved out of the phase backlogs on 2026-08-25.

---

## Phase 1 — Playable Truth

| # | Date | Increment | Ship | Headline |
|---:|---|---|---|---|
| 1 | 2026-09-14 | §DX-02ia | `80ebdfb` | all four mesh tests asserted inside a 2s offline poll's window, not just the one that was seen flaking; `meshPoll` is freezable and the flag gates the handlers because an in-flight fetch rejects after the caller renders. A fifth test waits longer than the interval, so the fix is distinguishable from luck. Finding (1) closed as already shipped. |
| 2 | 2026-09-14 | §DX-02ic | `5674495` | the monitor half of the tooling resolved a root `milepoints/` while the server half resolved `build/milepoints/`, so three of its four constants named files nothing wrote — including a `say.lock` documented as shared with `sayd.sh` and pointing somewhere else. One root now, a legacy glob so a pre-change host strands nothing, and a basename sort because three source dirs broke the chronological-order invariant. |
| 3 | 2026-09-14 | §DX-02fd | `5ff7450` | the patrol table declared its loosest condition first, so four authored lines were even-day-only and then starved one by one as their permanent latches set; reordered newest arc beat first with parity as the fallback, the suppress-the-home-card option ruled out because it would make the Layer 74 LHR scene unreachable, and world.md §Yael's Patrol Arc written. |
| 4 | 2026-09-14 | §DX-02kd | `a115ea6` | seven player-facing strings said *"the DF node"* for a code that resolves to nothing over 416 ids; five took one phrase (*the back lanes*), `desc` names the Cat Quarter because the mission list is read elsewhere, and the seventh needed the place the ward table actually names — the Defiant Fields. `grep -c 'DF node'` 6 → 0. Filed §DX-02kg. |
| 5 | 2026-09-14 | §DX-01k | `b5783a2` | one glance bullet was 234,636 B — 100 accreted `§DOC-02` sub-items on a single line, in the section meant to be skimmed first; split to Track records one per line, 63 further lines wrapped losslessly outside code spans, and `check:linewidth` added as gate #28 with two limits (4,000 B prose, 20,000 B table row, which cannot carry a soft break). Longest line 234,636 B → 19,121 B. |
| 6 | 2026-09-07 | §DX-02bd | `fe385f8` | `_questsByNode` folds `activateNode` and `waypointNode` into one map and pushed per field, so a quest naming one node in both was listed **twice** — and every consumer inherited it, including `_deps.node()`, the DELETE guard, which **refused the delete for the right reason with the wrong arithmetic**. Invisible to every gate: each id resolves, the list is non-empty, and a duplicate shows only if you compare `length` against `new Set(...).size`. Row's numbers reproduced exactly — **49** nodes, **97** entries, `NUE` **186 → 177**. Built through a `Set` now, so duplication is impossible by construction. `check:arraypatch` **51 → 56**, all properties: a corpus-wide uniqueness claim, a **non-vacuity control that rebuilds the old index and requires the check to fail on it**, and a proof only repeats were removed. |
| 7 | 2026-09-07 | §AUDIT-03ap | `38f0f62` | `hollow_hands_guard` dropped an item with the **exact name and icon** of the quest item Yva grants, in a pool whose node is primary — and the copy set no flag, while the delivery step's name-filter **silently ate it alongside the real seal**. Filed by its own lab report 110 days ago. Now `Hollow Hands Tally`, 🧾, **8gp** — it was also the collection's **only** `sell:0` row, against `mechanics-combat.md`'s own rule. **Option (1), a second item route, rejected:** the sibling arc's `scholars_guard`/`quest_wm_01` really does have that shape, but its two routes are *a letter* or *three seals*; here it would be *50gp* or *one easy kill*, which dominates rather than offers — and the route it would dominate is §VM-01-G4a's `cost`-leaf showcase. `sell:0` rows in `MONSTER_DROPS` **1 → 0**. |
| 8 | 2026-09-07 | §AUDIT-03as | `5100d18` | a 2026-06-04 data audit filled the *Void Shaman's Sanctum*'s empty roster with `P.void_shaman` — a key whose `name` is **The Warden**, a unique character in **Kazrath's** room, `name:'The Warden'` existing (says the report) *"so the battle overlay reads as a named encounter rather than a type."* The ground amended the row's *remove it*: **all twenty epic battlegrounds carry exactly three monsters**, so the slot went to `void_walker`, a generic hard-tier Void creature the corpus already pools twice. **The premise re-derived to one line of `CELL_GRID`**, not the quest data: `VS` is shadowed by `VBY` at (12,198), so the block holding every Visby button never runs — already §AUDIT-03x's, so no new row. Places you can meet The Warden: **0 before, 0 after** — what left was a statline wearing his name with no dialogue, no Token and no choice. `grep -c "P.void_shaman"` **1 → 0**. |
| 9 | 2026-09-07 | §AUDIT-03av | `4bebb6c` | `NUE`'s label reads *Scholar's Quarter — Weimar* and the file said Weimar **516** times against Nuremberg **five**, four of them on this node — including the Warrant's Board rumor that recruits the player into the arc. Weimar won on every name the arc owns (`NUE:'weimar'`, `quest_wm_*`, `wmDoc1Read`, Isolde Voss herself); the airport code stays. Three strings went through `put`; the fourth sat inside an `onComplete` carrying five §BOARD-01-FU6 annotations, so **the API grew the operation it could not express** — `substituteText` / `./bin/api sub`, which edits source text in place and matches only inside string literals. `check:arraypatch` **40 → 51**. `grep -c Nuremberg play.html` **5 → 1**. |
| 10 | 2026-09-07 | §AUDIT-03au | `ffcdbc2` | `quest_wm_04` completed on a flag whose only writer was its own `onComplete` — born that way at `194a810` and migrated faithfully into UQF. The flag is written where the quest's hint puts it, the unredacted read of Document 3, and **the row's separate consequence (c) was the same three lines**: an unsealed document had a non-null body and the Read button is built only where the body is null, so the render loop had no state for *the unsealed file is here and you have not read it yet*. Proved by lifting the live `WM_ARCHIVE_DOCS` loop against a stub DOM at HEAD and after. **`Marta Eilene Vass` renders for the first time**; the Annotated Copy and the only carrier of `atkWhileQuestActive` are granted; NG+ now carries the flag beside `entry42Written`, which `_vaReady` required in the same expression. **The Weimar arc closes at 4 of 4** — third of three rows in one session, in the order each asked for |
| 11 | 2026-09-07 | §AUDIT-03at | `70edc2d` | the reading circle counted sessions against `S_story.dayCounter` — **one occurrence, one commit, no writer ever** — so `today` was `0` on every render and the button locked at **1/3** on *"The circle meets again tomorrow."* → `S_story.gameDay`. Proved by lifting the live block against a stub DOM: `0/3 → 1/3 → 2/3`, sessions `[1,2,3]`, complete on the third attendance; with the read's clock left un-advanced it pushes `0` once and never opens. **Arc 2 of 4 → 3 of 4**, and it lights §AUDIT-03ar's `set:2` from three commits earlier — Benedikt reaches Dear Friend and §XXI's close at `NUE` is reachable. `quest_wm_04` activates and still cannot complete (§AUDIT-03au). Three maintained docs re-synced, two of which repeated the typo back as spec |
| 12 | 2026-09-07 | §AUDIT-03ar | `2a6b4f9` | `quest_wm_03` wrote `set:1` (Friendly) beside its own narrative bit saying *"Benedikt is **Dear Friend**"*, against two design docs and both live thresholds that begin Dear Friend at 2 — only the write said 1. **`set:1` → `set:2`** through `./bin/api`. Inert until §AUDIT-03at gives the reading circle a working clock, exactly as the row says. `check:npcregs` gains **phase 7** (every `_npcFavor(k) >= N` and `favorMin:{k:N}` at or below the NPC's favor ceiling — 26 of 27 were reachable, the 27th this site) and **phase 8**, the row's own class note made executable: a favor bit must write the tier its entry announces to the player — 7 real pairs, 6 already agreeing. Selftest 7 → 10 checks. §GR-FU2's phase 6 then demanded a Covenant Ceremony line for him, unprompted, and he has one. The sweep had refiled this as §DX-02je; `check:anchors` caught the duplicate and §DX-02je was deleted |
| 13 | 2026-08-26 | §DX-02ee | `3608bc2` | `retryGateDays: 0` was **not a value the engine could read**: the sole consumer is `S_story.day < att.lastDay + (q.retryGateDays // 1)@6822`, and `0 // 1` is `1`, so **42 quests carried the one-day co |
| 14 | 2026-08-25 | §DX-02en | `83db904` | the game's best ending was **one point out of reach from the repository's first commit**: `_curseScore()`'s floor is −5 and both `Covenant Keeper` and the *Covenant Keeper (True)* ending asked for ≤ − |
| 15 | 2026-08-25 | §AUDIT-03al | `c13178b` | a section written so a validator would go green, describing a constant retired two days later. `story.md §Gate Locks` was SP4's **one new doc section**, and at HEAD `GATE_LOCKS` and `_canTravelTo` are |
| 16 | 2026-08-25 | §AUDIT-03ak | `37ace66` | the residual set the row measured is not the residual set at HEAD. The row named **four** `⚠️ PLANNED` headings in `world.md` describing features live since 2026-05-26. **Two of the four had already b |
| 17 | 2026-08-25 | §FUTURE-01-FU2 | `7a47700` | the arc's first mission card, and the quest that had already answered the question. The row said `quest_road_damascus` — the first §LIX text a player reads — addresses the player **as Saul** in two ad |
| 18 | 2026-08-25 | §DX-02hc | `e8c3317` | the update half of a bug the repo had already closed twice. The row said `PUT /api/monster/:key/drop` **corrupts** the **13** weighted-array drops. Grounding it found that was the *second* defect, and |
| 19 | 2026-08-25 | §DX-02ha | `d581940` | the drop was there all along, in a shape the measurement could not see. The row said `void_shaman` is fought at `TRD` and carries no `MONSTER_DROPS` row. **Both halves are false.** `MONSTER_DROPS@5796 |
| 20 | 2026-08-25 | §DX-02fy | `62f4803` | the battle whose monster was never written, and the gate that could not see its own fix. `RUH` was the only one of **47** battle nodes whose `battle.key` resolved in neither pool; `desert_wanderer` oc |
| 21 | 2026-08-25 | §DX-02gw (2 of 3) + §DX-02gx | `5d0bee8` | the row's own premise did not survive being measured, and that was the increment. Took `dragon_of_fyresdal` meaning to host it at `PDL` on the strength of *"island territory"*. **Two measurements kill |
| 22 | 2026-08-25 | §DX-02gw (1 of 3) | `5d0bee8` | the west wing gets the bruxa the docs already fought there. `bruxa_corvo_bianco` occurred **twice** in `play.html` — pool row `@5787`, drop row `@6264` — and no `battle.key`, `P.<key>` or `bossKey` re |
| 23 | 2026-08-24 | §DX-02gv + §WAVE-01 | `4bac567` | the Act VIII wall had a 210-HP statblock, a `sell:95` shard and no fight. `void_warlord` occurred **twice** in 38,700 lines — its `MONSTER_POOL` row `@5789` and its… |
| 24 | 2026-08-24 | §AUDIT-03bi | `c0888aa` | the fifth ending stops being downstream of a console command |
| 25 | 2026-08-24 | §AUDIT-03bk | `7cac4d2` | the quest that could be offered and never finished, and the host record its design lost |
| 26 | 2026-08-24 | §AUDIT-03bj | `3571a64` | the prover that spent 27 days proving the world is not finishable and reporting success |
| 27 | 2026-08-24 | §DX-02go | `5e17dc2` | the last inert field on a granted readable, and an anchor with a known expiry date |
| 28 | 2026-08-24 | §DX-02ge | `6237606` | the field four files agreed to carry and no code path ever looked at |
| 29 | 2026-08-24 | §DX-02gb + §DX-02fc | `763c70f` | the Dear-Friend step is granted once, and either order earns it |
| 30 | 2026-08-24 | §DX-02aj | `1222d8d` | the latch that was always already set, and the harness that was always already rendered |
| 31 | 2026-08-24 | §DX-02gd | `898c692` | eleven items, three spellings of one field, and a tooltip that was never rendered |
| 32 | 2026-08-24 | §DX-02cm | `d117b2f` | the exception that outlived the deferral, and the difference between debt and an unexercised bug |
| 33 | 2026-08-24 | §DX-02gl + §DX-02gb | `7e020de` | the Dear-Friend act table names only acts that can grant the step, and the ending's score did not move by a bit. `DEAR_FRIEND_BITS.auros` was unreachable by… |
| 34 | 2026-08-24 | §DX-02gk + §DX-02gb | `d9c4cad` | the ending scorer reads the Dear-Friend act table instead of respelling it, and the fix's real cost was paid in doc anchors, not in code. `_missionComplete` spelled **6… |
| 35 | 2026-08-23 | §DX-02cy | `724682c` | the counter that had been monotone at zero for ninety days, and the gate that refused to let the docs stay wrong |
| 36 | 2026-08-23 | §DX-02fb | `407b31f` | `crov` reaches Dear Friend + 3, and the row's own prescription was the wrong fix. One bit — `{kind:'favor',npc:'crov',add:2}` appended to `quest_pit_training.onComplete`… |

**36 completed entries.**

---

## Phase 2 — Engine & Systems

| # | Date | Increment | Ship | Headline |
|---:|---|---|---|---|
| 1 | 2026-09-25 | §DX-02lh | `858aef6` | the Pip button's inline +150 XP deleted; The Overture pays alone. `check:doublepay` 1 → 0. The harbour test that asserted 300 under a "+150 once" title now asserts 150 |
| 2 | 2026-09-14 | §DX-02kt | `267635d` | `targetMonsterKeys` (12 declarations, 0 reads) and `questComplete` (1) removed — and the row's mechanism, `put field=null`, cannot delete a key: null clears a scalar and §DX-02ee refuses an array. The API grew `removeField` / `DELETE /api/{type}/{id}/field/{field}` / `./bin/api unset`, verified by reading the entry's field literals either side of the excision through the same token walk and refusing unless exactly one key left and every neighbour is byte-identical. `check:schema` (shipped the increment before) went red the moment the last carrier left. `check:arraypatch` repointed off its second swept fixture field onto a synthetic key, 78 → 90 checks. |
| 3 | 2026-09-13 | §DX-02jz | `64f80ae` | `plague_exposed` has one writer (the 1367 plague vignette's CON DC 13 save) and no reader, while its declaration promised an Exhaustion mission bit and a cure quest; neither has a site and no design lock names them, so the flag was declared a record, like the 1367 tracks beside it: the two untrue clauses deleted, the two true ones kept. 2 → 0 · 27/27 · `npm test` 152/1083, identical to baseline (+3 untracked §DX-02fh tests at browser launch) |
| 4 | 2026-09-13 | §DX-02ka | `7fa563e` | the four 1367 faith and faction tracks are written by the vignettes and read once between them (`quest_lxvii67`’s `faith_folk` gate), while their declarations promised 12 effects across quests, dialogue, ports and prices. Disproof failed (no dynamic read, no design lock; `potential.md` already seeds the tracks-with-teeth idea), so they were declared a record: *Affects …* sentences deleted, range kept, the jester gate named. 12 → 0 · 27/27 · `npm test` 152/1080, identical to baseline |
| 5 | 2026-09-07 | §DROP-02-FU | `57701fe` | §DROP-02 scoped Luck to the fishing type/rarity roll and a commit that mentioned none of it put all six sites back the same afternoon, uncaught for 68 days. The five survivors are re-applied — `_luckMod()` call sites **9 → 4** — and `check:invariants` gains **I5**, which declares every remaining site with its reason and is red in both directions (proved by planting the death-save term back). The row's census was one site short (the bait DC has a title twin) and its recommended default was **disproved**: `luckScale` in `_lakeMagicBonuses` is non-zero on 7 of 8 lake-magic items and reaches AC, attack, first strike and all-ability, so re-applying the removals could not make *"Luck's sole mechanical role"* true. All three home docs corrected; the lake-magic conflict filed as §DROP-02-FU2. Bare Hook catch: P(hit/cast) 0.835 → 0.736 |
| 6 | 2026-09-06 | §DX-02dl (partial) | `d90bc9e` | the Void meter's imminent milestone was `=== 9` while its two siblings were `>=`, so a +3 writer stepped over it; now `>= 9`, and the message interpolates the live pressure because firing at 10 would have printed "9/10, one more point". The Ceremonia +3 routes through the helper (direct writes 4 → 3). The two quest `_legacy_fn` writers are blocked — the API cannot write a function value — so the row stays open behind §DX-02iv |
| 7 | 2026-09-06 | §DX-02dg/dh | `9d3b11a` | the sidebar and the objective chip disagreed about the day on 15 of 49 days; both now read `_dayAlarm(day)` off offsets from DAY_DEADLINE, sidebar colours byte-identical, chip regains the danger step its CSS already styled, and the assertion §PLAY-01-C deleted in the same hunk that broke the agreement is restored as a 49-day sweep that is red at 0568969. courierMapSeen moved from the seed literal into _S_DEFAULTS so a reset can clear it. §DX-02di left open — a vocabulary decision, not a mechanical one |
| 8 | 2026-09-06 | §DX-02iu | `73eb02e` | `itemsAll` ported from the completion leaf to the activation leaf (only that one — the fuzzy `items` sits in an OR-group and would mean AND here); the last six closure gates migrated, LEGACY_GATE_CEILING 6 → 0. The rod trio needed no third matching rule — one item name contains "fishing rod" and the closures were only compensating for keyword casing; `_hasItem('Trade Seal')` did need care, its item being 'Trade Seal (Shard #1)'. 9,372 equivalence checks, 0 mismatches; check:gateast 76 → 84 assertions |
| 9 | 2026-09-06 | §DX-02dy | `ae3bb24` | gate:{_legacyFn:true} 14 → 6 — six closures were `() => true`, two were counter comparisons the already-shipped `countMin` leaf expresses; 240 quest×state equivalence checks, 0 mismatches. `removeExprField` added to wbapi-core first (§2.5) so the API could clear a function-valued field; check:questgraph now reports the gates it cannot see behind a down-only ceiling. The six survivors are inventory tests the activation leaf has no term for → §DX-02iu |
| 10 | 2026-09-06 | §DX-02eg | `7efb8f6` | the reward ceiling acted on a pool of 1,063 while the player read 8, so 60 days at Warrant's Own showed a premium bounty on none; a slate seat is now reserved for work the base ceiling would hide, taken in the day's hash order so it rotates rather than pinning the richest card. Premium-bearing days 0 → 60 at every lifted rung, tier 0 still 0. Reuses §DX-02ei's seat closure; §9's guard now asserts from the shown set and is red at 68bd466 |
| 11 | 2026-09-06 | §DX-02ei | `818e9c7` | the Warrant's Board reserves one slate seat for the `BOARD_LOCAL_PLACES` nearest destinations with work, taken in the day's own hash order so it still rotates; the worst day's nearest card at `TLL` falls 25 → 12 legs and a host's worst best-card 65 → 15 across all 39 hosts. The row's absolute "≤10 legs daily" floor was disproved on the ground — 12 of 1,063 eligible quests at `TLL` are that near — so the guarantee ships relative and the population is filed as §DX-02it. `_roadGridNearest` shares `_roadGridPathCore`'s relaxation through the extracted `_roadGridWalk` |
| 12 | 2026-09-06 | §DX-02bz | `be051f7` | A card that advertised an hour the game never took, and the two cards that made it a lie rather than a flourish. |
| 13 | 2026-09-06 | §DX-02di | `8245f26` | Half a shipped classifier, and the gate class that could not see it. |
| 14 | 2026-09-05 | §DX-02eq | `911348b` | five pit perks wrote five flags nothing read; Weckmann's Lesson is wired against the existing exhaustion-DIS clause — spent only when exhaustion bites, latched per encounter, returned by a long rest — and the other four writes are deleted. `readTheRoom` could not be granted: the pre-combat threat tier it promised already ships to everyone, so wiring it would mean withholding it. Flanking/shove/regen are §DX-02is |
| 15 | 2026-09-05 | §DX-02ex | `1273d2f` | `couperiDebtDegraded` carried both writers' opposite meanings, so a run that never took Quill's quest was philosophised at about releasing a debt it left alone; `couperiDebtReleased` split out, and Beat 3 + both epilogue conditions + the dialogue injection read it, so the neglect branch finally speaks S30's authored line. Beat 3 was also unreachable after a degraded run and now is not; saves migrate by `quillQuestComplete`, which tells the two writers apart exactly |
| 16 | 2026-09-03 | §DX-02ew | `47cd24b` | the inn ledger's ceiling is a balanced book at last — pantry +3→+2 closes at exactly 0 in all six orders, per §VII and world.md agreeing; the pantry narration stops asserting a sign it cannot know; the printed-figures mismatch withheld as §DX-02iq because it needs the author |
| 17 | 2026-09-03 | §DX-02et | `16ce65b` | Layer 44's six world notes get `S_story.worldLog` and a panel section under the design's own stranded `.journal-entry.world` rule, out of the movement breadcrumb array; storyMsg declined because the notes are meant to be missable; old saves migrated on load |
| 18 | 2026-09-03 | §DX-02fv | `2cf3462` | `choice` gets its second consumer surface: TL Vonn sliced into NODE_VERBS with the answer as the choice prompt, and a self-guarding `when` — the driver's closing re-render draws verbs before quest completion, so a status-only guard redrew a payable button |
| 19 | 2026-09-03 | §DX-02ip | `62fd7dc` | the failed-check effort grant scaled off the first `reward` bit rather than the sum the pass actually pays; now a `reduce`, provably behaviour-preserving at 0 multi-reward checks in the corpus, with a fixture red at HEAD |
| 20 | 2026-09-03 | §AUDIT-03bm | `12007f2` | the XP economy names its own edge: the failed-check dial is proportional, not a floor, so token-only checks pay a mission bit rather than XP by design; a flat `mission_bit` grant declined at 24,500 XP against the corpus's 22,307 XP of quest reward, and the census re-derived per run |
| 21 | 2026-09-03 | §DX-02aq | `a1d97fa` | the inventory panel splits `S_story.knowledge` by shape into Necklace beads and Field Notes, so lore notes no longer render as `undefined` |
| 22 | 2026-09-03 | §DX-02bt | `b545133` | the custom-waypoint and corpse quest cards build on `textContent` like the main renderer; the two dialogue maps name each other |
| 23 | 2026-09-03 | §DX-02do | `fdf9589` | Entry 42 and the Defiant Fields secret, the two player-typed strings, reached the DOM as markup; both now escape through `_mpEsc` |
| 24 | 2026-09-03 | §DX-02ae | `6464da3` | the Act VIII Brynn farewell accessor wrote a progression flag while rendering; now pure, with the write at the delivery site beside the latch, and the Yael scene comment names its own guard |
| 25 | 2026-09-03 | §DX-02ac | `15ba42a` | seven dead `abilityScores` fallbacks named a starting character the game never made; collapsed, and the four documents that believed them corrected |
| 26 | 2026-09-03 | §DX-02ai | `19afc7a` | Two double-pays named, one measured, one that never was. |
| 27 | 2026-09-03 | §CHRON-01 | `a24e32e` | The cross-life ledger was wiped by the only transition it was built for, and the sheet hid the wipe. |
| 28 | 2026-09-03 | §DX-02cl | `0d014ef` | The answer was thirteen lines below the question for 47 days. |
| 29 | 2026-09-03 | §DX-02dp | `e374893` | The contract said *between two elements*, and the second element was never a sibling. |
| 30 | 2026-09-03 | §DX-02dx | `c6aff86` | A parity-fenced kernel had a consumer on paper and not in code. |
| 31 | 2026-09-03 | §DX-02dw | `0100717` | A kernel whose rule is *guards live in the host* called one effect bare, and its report promised a fallback nobody wrote. |
| 32 | 2026-09-03 | §DX-02ea | `4cab8a2` | The index built to retire an O(n) scan paid an O(n) guard on every read, twice per arrival — and the row's fix would have broken its own test. |
| 33 | 2026-09-03 | §DX-02eh | `5e04940` | A rank the player earned and could not see. |
| 34 | 2026-09-03 | §DX-02ej | `4ae4b8b` | The start node has described a notice board for thirty-two days, and now it has one. |
| 35 | 2026-09-03 | §AUDIT-03bn | `312b717` | A catfish announced to the player as the Void, and a Death Knight that broke and ran. |
| 36 | 2026-09-03 | §DX-02dj | `312b717` | The alignment field that sat on two monsters with no reader is now the authority. |
| 37 | 2026-08-26 | §AUDIT-03bo | `55ad0f7` | A quest-giver with a name, a home node, two acts of a five-part chain — and no voice, so her card rendered nothing. |
| 38 | 2026-08-26 | §AUDIT-03bp | `aeca8c7` | The Cat Quarter's antagonist has rendered on no node since 2026-07-23, and he was the one face of the registry-mismatch class no check could see. |
| 39 | 2026-08-26 | §AUDIT-03bl | `5119ed4` | A corridor that advertised L1→~L6 and landed at Level 5, and a contingency that could not have closed the gap it was written for. |
| 40 | 2026-08-26 | §VM-01-B-FU | `41b62ce` | The fight is now replayable on request, and unchanged by default. |
| 41 | 2026-08-26 | §ASI-01 | `145938d` | A d6 table nothing rolled, six docs that documented the roll, and a fact that had already propagated into three rows. |
| 42 | 2026-08-26 | §SIMLEAK-01 | `5551abf` | The simulator's weapon panel did not read the equipped weapon — it overwrote it. |
| 43 | 2026-08-26 | §ATKAB-01 | `53c5cf6` | A field in the dice-roller panel was added to every story attack roll, and no story surface could see it. |
| 44 | 2026-08-26 | §NEXT-2026-08-26 | `1d76637` | A hand-off note retired by taking its contents up, not by deleting it. |
| 45 | 2026-08-26 | §DROP-01-FU | `1d884ad` | A table nothing rolled for 82 days, and a comment its own generator overwrites. |
| 46 | 2026-08-26 | §DX-02er | `ef94628` | A hole in the seeded fence, and a dead doc pointer that turned out to be ten. |
| 47 | 2026-08-26 | §WEAP-FIN-FU | `bd72b19` | One sync for two callers — and an off-hand that was never the dagger you equipped. |
| 48 | 2026-08-26 | §WEAP-RANGED | `87286fa` | A bow that fired at STR, and a spec line that had said otherwise all along. |
| 49 | 2026-08-26 | §WEAP-FIN | `603c45a` | One sync for two callers — and an off-hand that was never the dagger you equipped. |
| 50 | 2026-08-26 | §XP-FLEE | `17f1bc3` | The last swing the engine neither counted nor paid for. |
| 51 | 2026-08-26 | §DX-02eo | `ff6a18c` | A second ledger nothing read, and the best line in a four-state table with no way to reach it. |
| 52 | 2026-08-26 | §DX-02fu | `d656434` | A "complete" that was 94%. |
| 53 | 2026-08-26 | §DX-02de/df | `57f2e06` | §XP-01 shipped the XP and withheld the telling. |
| 54 | 2026-08-26 | §DX-02dd | `39c5261` | The one §DEATH-01 surface touching the map renderer was the one surface with no test, and it had two blind spots for 37 days. |
| 55 | 2026-08-26 | §DX-02dc | `bbb201d` | The fix was right, the reasoning was wrong, and the difference is the finding. |
| 56 | 2026-08-26 | §DX-02cn | `0156472` | An undeclared field is not reset — and this one carried the item ledger. |
| 57 | 2026-08-26 | §AUDIT-03ad | `bf34ce2` | A string that is correct five times before it lies once. |
| 58 | 2026-08-26 | §DX-02z | `8a4a296` | A missing price and a cheap price were the same value at the call site. |
| 59 | 2026-08-26 | §AUDIT-03ae | `801f1e8` | Four surfaces disagreed about what a story attack roll is, and every identifier involved resolved. |
| 60 | 2026-08-26 | §DX-02y | `9633e73` | `S_story.acBonus` was a **read-only field: two declarations, exactly one reader — the `(S_story.acBonus // 0)` term inside `_calcPlayerAc()` — and zero writers in the entire history of the repository* |
| 61 | 2026-08-23 | §DOC-02dc + §VM-01-G4 | `f340143` | the child design pass whose one missing opcode was the last one the VM ever needed, whose only carried number is its only wrong one, and whose nominated pilot for the… |
| 62 | 2026-08-23 | §DOC-02db + §VM-01 | — | the design that survived eleven slices and 26 days at 100% of its symbols, whose one unmodelled shape was a button on the wrong side of a `<div>`, and whose act number… |
| 63 | 2026-08-22 | §DOC-02cv + §BOARD-01 | `8dcca67` | the origin lock with a perfect anchor score and a census that measured half the corpus, whose two "pure transliteration" increments required two invented edits, and… |
| 64 | 2026-08-22 | §DOC-02cu + §BOARD-01-FU7 | `e0051a8` | the design lock whose data table shipped byte-identical and is still byte-identical a month later, whose spec omitted the one edit that made half of it work, and whose… |
| 65 | 2026-08-22 | §DOC-02ct + §BOARD-01-FU8 | `4af74b4` | the design lock that named an invariant in its first design call and violated it in its fourth section, and the prover that could not see the violation because it could… |
| 66 | 2026-08-22 | §DOC-02cs + §VM-01-F | `c6be7f8` | eighteen of twenty-one anchors exact and the three misses are exactly the three the author hedged with a tilde; the gate learned to say `or`, has had one speaker for a… |
| 67 | 2026-08-22 | §DOC-02cr + §3 | `549d6b4` | the design lock whose code shipped byte-identical and is still byte-identical a month later, and whose speedup is 3.6× rather than the thousand-fold its own big-O… |
| 68 | 2026-08-22 | §DOC-02cq + §10 | `354b20a` | every one of the prover's own seven corpus figures reproduced exactly on the pinned build, and the increment credited with consuming the kernel turns out to have… |
| 69 | 2026-08-22 | §DOC-02cp | — | eighteen line numbers, eighteen exact — the cleanest anchor result the program has scored — and the one promise the kernel did not keep is the one that rolls the dice |
| 70 | 2026-08-21 | §DOC-02co + §VM-01-C | `c22f4f0` | the destination file names the source — `src/scripts/check-questgraph.js:35` reads *"the written-by-nothing / read-by-nothing detector §VM-01-C deferred here."* **§6.1… |
| 71 | 2026-08-21 | §DOC-02cn | — | 27 of 28 line numbers correct against a build that was never committed, and all four wrong figures are in the prose that frames them |
| 72 | 2026-08-21 | §DOC-02cm | — | every code block byte-exact, 33 of 33 anchors resolving, the acceptance suite still 5/5 thirty days on — and all three wrong numbers in one paragraph, the one written… |
| 73 | 2026-08-21 | §DOC-02cj | — | the report got four of its own counts wrong and three of its four open questions right, and the repo built every one of them |
| 74 | 2026-08-21 | §DOC-02ci | — | the parent review shipped five of seven faces the day it was written, and every one of its four errors is a citation, three of them in the tier marked "no design call" |
| 75 | 2026-08-18 | §DOC-02cg | — | the increment shipped ten of ten and deferred five of five, and the sentence it put on the opening screen is false in the direction that kills the player |
| 76 | 2026-08-18 | §DOC-02cf | — | the lock has not drifted a byte in 37 days, and half of what it shipped has never once returned true |
| 77 | 2026-08-18 | §DOC-02ce + §PLAY-01-A | — | §PLAY-01-A shipped the goal whole, and then a design reversal landed on one of its two surfaces |
| 78 | 2026-08-18 | §DOC-02cd | — | the corpus's first perfect dating result, on a feature that ships the XP and withholds the telling |
| 79 | 2026-08-18 | §DOC-02cc | — | the cleanest ship in the corpus, and every failure in it is a claim about its own surroundings |
| 80 | 2026-08-13 | §DOC-02bc | — | the one row it certified in its own words is the one row its own line numbers disprove |
| 81 | 2026-08-13 | §DOC-02bb | — | everything it read is right, and everything it added up is wrong |
| 82 | 2026-08-13 | §DOC-02ba | — | the diagnosis outlived the remedy by two months; the remedy lasted four hours |
| 83 | 2026-08-12 | §DOC-02am | — | the defect was a missing CALL, not a missing SYMBOL |
| 84 | 2026-08-12 | §DOC-02aa | — | a flag written and read eleven lines apart deleted the middle layer of a three-layer design, and nobody noticed for 79 days because the line still arrives |
| 85 | 2026-08-12 | §DOC-02w | — | the mechanism is perfect, the design is unreachable, and the decision that scoped it was reverted by a commit about book imports |
| 86 | 2026-08-12 | §DOC-02v | — | the migration shipped complete, was silently reverted the same afternoon by a commit about book imports, and came back two-thirds |
| 87 | 2026-08-12 | §DOC-02t | `3deef54` | the design the report explicitly FORBADE is the design that shipped — and two Layer-18 stat fields drifted away from their only readers |
| 88 | 2026-08-12 | §DOC-02r | `33c2166` | a 100 % symbol census over a feature that is 55 % unreachable — and the arc's hardest check is a button |
| 89 | 2026-08-12 | §DOC-02n | `c0a24eb` | the design was a TRADE — take magic weapons away from monsters, give them back through fishing — and only the taking half shipped |
| 90 | 2026-08-12 | §DOC-02m | `33f09ad` | the report's design premise was right, was built, and was then extended fourfold — and nobody can reach any of it, because an arctic node is declared 59 lines before the… |
| 91 | 2026-08-11 | §DOC-02j | `7e62f43` | a report whose own appendix says its subject was never built, and six of its seven mechanics are live code — while the table it wrote to persuade you names two monsters… |
| 92 | 2026-08-11 | §DOC-02e + §DESIGN-03 | `3d0ab1d` | §7 of the report specified *"show the level-up modal immediately"* on a passing roll, and **HEAD does not do it** — `_checkLevelUp()` is a mutator that advances… |
| 93 | 2026-08-06 | §11f + §11 | `3338def` | `NWI`'s Warmth Eel panel (named **first** in §11's own census table) and the `HKG` Void Below chip were censused and then dropped by the arc partition; the third… |
| 94 | 2026-08-06 | §VM-01-G-FU-f2 + §11 | `9c9fe42` | TLS authors `finalBattle:{minLevel:20,minShards:7}` (via `WBAPI.editStructuredField` in-process — 1-line data diff) and `_finalBattleReady(code)` replaces the three… |
| 95 | 2026-08-06 | §SPARK-01-FU + §LXX-01-FU | `3338def` | stripping the button side alone was insufficient for the §LXX trio because their completions were *arrival-satisfiable* (`sunken_02` flags+itemsAll+atNode · `depth_01`… |
| 96 | 2026-08-06 | §VM-01-G-FU-f2 | `9c9fe42` | The three engine specials — all three design asks answered |
| 97 | 2026-08-06 | §SPARK-01-FU | `3338def` | The Aldous confrontation double-pays |
| 98 | 2026-08-06 | §LXX-01-FU | `3338def` | All three §LXX-family button/quest pairs double-pay |
| 99 | 2026-08-05 | §VM-01-G-FU-f + §XLV | `6a571de` | git diff +128/−124 (124 verbatim + 2-line wrapper + 1 registry + 1 dispatch), line-multiset 124/124 zero unmatched; 16-combo golden HEAD-vs-after over the full… |
| 100 | 2026-08-05 | §VM-01-G-FU-e + §LXX | `3dfdc26` | `quest_sunken_02`/`quest_depth_01`/`quest_forge_02` each carry the button's entire effect in `onComplete` and auto-complete on the arrival that draws the button… |
| 101 | 2026-08-05 | §VM-01-G-FU-d + §SPARK-01 | `8cdda7b` | 49-combo golden HEAD-vs-after over sibling DOM + bboxes + msg text/class + overlay/`_preBattNode` + state incl. 13 click combos — **46/49 byte-identical, the 3 deltas… |
| 102 | 2026-08-05 | §VM-01-G-FU-c + §ALCHEMY-01 | `7d8cb39` | 37-combo golden HEAD-vs-after over sibling DOM + bounding boxes + msg text/class + pre-battle overlay/node + state (roen chain, wisdom pages, knowledge, inventory),… |
| 103 | 2026-08-05 | §VM-01-G-FU-b + §HUNT-01 | `cf2fec8` | 22-combo golden HEAD-vs-after over sibling DOM + bounding boxes + msg text/class + pre-battle overlay/node + state, incl. 4 click-outcome combos — **22/22… |
| 104 | 2026-08-05 | §VM-01-G-FU-a + §CROWN-01 | `56c08f1` | 26-combo golden HEAD-vs-after over DOM+bbox+msg+overlay+state — **14 byte-identical · 8 id-only (`story-*`→`verb-*`, every bounding box equal) · 4 named deltas, two of… |
| 105 | 2026-08-05 | §VM-01-G-FU + §CROWN-01 | `02ff4aa` | after the generic sections sits a SECOND special-case stack, ~1,200 lines / ~38 blocks, accreted by the NEWER arcs (§CROWN-01 · §LXX-family · the… |
| 106 | 2026-08-05 | §VM-01-G4d + §VM-01-G4 | `b0c2478` | CDG's three boss confrontations as `NODE_VERBS` group `cdg-boss-menu` rendered into the same `#cq-boss-buttons` container (`_renderNodeVerbs` grew the 4th `container`… |
| 107 | 2026-08-04 | §VM-01-G4c + §VM-01-G4c-FU | `4c2a831` | four D1 surfaces as `NODE_VERBS` button entries — Sweelinck's S49 scene at `NUE`, Ori at `STN`, Yva at `TRD`, Brynn's firewood at `TLL` — plus the Junction Vignette's… |
| 108 | 2026-08-04 | §VM-01-G4b + §9 | `f7a60a5` | `NODE_VERBS` + `_renderNodeVerbs` — the third small vocabulary beside `NODE_PANELS` (flavour a node *shows*) and `NODE_HOOKS` (an interface a node *owns*) — and Kern &… |
| 109 | 2026-08-04 | §VM-01-G4a + §10 | `b905733` | the **`cost`** opcode, registered next to `reward` because it is `reward`'s inverse and because `reward` is precisely how you get this wrong — `gold:-50` computes… |
| 110 | 2026-08-04 | §VM-01-G2b + §AUDIT-03 | `bedf2c1` | no **top-level `return`** (all 13 sit inside click handlers — one at block level would have gone from exiting nothing to exiting a hook early) and no **`var`** (nothing… |
| 111 | 2026-08-04 | §VM-01-G4 | `248d416` | the button mode gets its first consumers, and "13 D1 verbs" measured down to 4. On "continue" took the bo… |
| 112 | 2026-07-31 | §DX-02g + §AUDIT-03d | `f229ede` | `void_rat_swarm → easy` settled itself (6 of 8 nearest stat-neighbours `easy`; exact AC/HP/atk twin is Jackalwere), while `void_shaman` was a real fork — its two… |
| 113 | 2026-07-28 | §RESUME + §DOC-02 | `549d6b4` | `check:anchors` ✅ **4,191 → 4,248 across 100 → 101 docs, +57, 0 dead** — **45 in the report** (which carried none before: it used the bare-number form throughout, so the… |
| 114 | 2026-07-28 | §DX-02dv + §RESUME | — | `check:anchors` ✅ **4,149 → 4,191 across 99 → 100 docs, +42, 0 dead** — **23 in the report** and **19 across the two new rows, the §DX-02dv resolution, the program row… |
| 115 | 2026-07-28 | §VM-01-G3 + §XX | `a721254` | (1) most of the 15 quests ALREADY carried an `activateNode` — appended as a DUPLICATE key below the authored `activateNode:null` by the `a721254`/`ea02faf` audit waves… |
| 116 | 2026-07-28 | §VM-01-G2 + §D01-10 | `8973fd7` | the sketched single dispatch loop is order-UNSAFE — Corelli's gate is the computed `_checkCorelliAppearance` schedule (any of 5 nodes), so no single dispatch position… |
| 117 | 2026-07-28 | §VM-01-G + §7 | `cf2c17c` | table order = old source order and every extracted block must precede, in old source, every block that can co-fire on its node — which excludes the BK approach panel… |

**117 completed entries.**

---

## Phase 3 — Content & Narrative

| # | Date | Increment | Ship | Headline |
|---:|---|---|---|---|
| 1 | 2026-09-14 | §DX-02ku | `748cf40` | seven quests ended a skill_check pass with an authoring-template token shown to the player — *"He files. He writes: questComplete."* — in the Sweelinck archive register. The replacement was quoted rather than invented: *"He writes: closed."* is already Sweelinck's own shipped closing in `gci_07_act5`, in the identical position. Three grammatical positions handled per sentence, not per token; written through `./bin/api sub`. `grep -c questComplete` 8 → 0 — the field half was §DX-02kt, closed an hour earlier in the same loop. |
| 1 | 2026-09-14 | §DX-02fe | `65c8c49` | there is one favor scale, not two: `NPC_FAVOR_CAP = 3`, and `_setNpcFavor` speaks only two tier lines while `_checkDearFriendUpgrade` speaks the **level-2** one — Dear Friend is 2, Dear Friend+ is 3. **The row's claim that tier-3 content never fires is disproved**: every tier-3 gate lands on an NPC whose ceiling is 3. The row was overtaken by §GR-FU2 (ceilings) and §AUDIT-03ar (`check-npcregs` phase 7, which is the gate it proposes), and `mechanics.md` already published the tier table. Two residues shipped: the one comment naming 3 *Dear Friend* — caught by the `QUEST:CORE` parity fence when edited in `play.html` alone — and phase 7's blindness to `minFav`, the field the row names first, now scanned with a selftest plant. |
| 2 | 2026-09-14 | §DX-02cv | `1bb4a71` | the 🧙 chip renders on `node.npc`, the Talk button gates on node-keyed `NPC_DIALOGUE`, and six nodes were in one set and not the other. Row's census reproduced exactly (71 / 68 / 6). Went against the row's recommendation to route `VBY`/`AMS` through the npc key: the file itself says plural `NPC_DIALOGUES` is the favor-keyed profile and singular is the resident voice, so a fallback pipes the wrong register into the surface — option (a) for all six, (b) left filed. All six already spoke in their own node text. **6 → 0 missing, 68 → 74 keys.** Filed §DX-02km — the registry has no API verb, so §2.5 could not be satisfied. |
| 3 | 2026-09-14 | §DX-02kj | `91f09d8` | `CI` is a **live** node — *Chancery Court — The Officer's Pen*, `num:429`, `{r:17,c:190}` — and 21 sites used it to mean `LHR` at `{r:10,c:197}`: §DX-02jt's class with the polarity reversed, a code that resolves and misdirects rather than one that strands. **Four were player-facing**, not the one filed. Node 429 is legitimate (`erf_01_act5` is *Falada Speaks — The Officer's Pen*), and the row duplicated §AUDIT-03ba, whose 47-comment census this ships the `CI` subset of. Each banner was two edits, body and `_runNodeHook` repeat. **25 → 4**; leading comments **8 → 0**. Filed §DX-02kl on a mesh-test flake in the verification run. |
| 4 | 2026-09-14 | §DX-02kh | `15c56f0` | two quest hints said *the City Inn*; `LHR`'s label is *City Streets — Birka* and the Entry 42 panel is gated on `LHR` under a comment reading *blank journal page at CI* — so *City Inn* is the node's own retired name. `TLL` is labelled **The First Inn**, in the same city, and is where Brynn hands over the forty-one-entry journal, so the hint sent the player to the wrong inn rather than to nowhere. Both hints moved to the label's distinctive half; `quest_va_01`'s list is five resolvable places for the first time. **2 → 0.** Filed §DX-02kj — `CI` is a **live** node (*Chancery Court*) used at fourteen sites to mean `LHR`, one of them player-facing. |
| 5 | 2026-09-14 | §DX-02kg | `d08670a` | `ZRH` is labelled *The Unbanked Quarter* and **9** occurrences of *Defiant Fields* — not the row's 8 — named it otherwise, **7** of them player-facing, not 4. The row's one argument for the old name was the terrain key; `terrain.label` for `defi_land` is **`DeFi Land`**, a crypto pun over `pepe_rat`/`wagmi`/`rug_spider`, so the terrain was never on that side. Three maintained docs carry *the Defiant Fields battle* at `ZRH` as canon, so not a rename to zero: **the label owns every present-tense string, and the old name survives at one site — the `ZRH` ward text — as the name the ground had two hundred years ago**, with a block built over it. **9 → 1.** Filed §DX-02kh (*City Inn* names no node either) and §DX-02ki (`BMA`'s prose bearing was twelve rows backwards, and no gate reads one). |
| 11 | 2026-09-13 | §DX-02ev(c) | `e0f3fa0` | `INV` was the one Epic Battleground return of 20 with no quiet receipt; Shepherd Rona's line is written from her Q53 entry (the churchyard where she buried Coll's coat, the loch that is just water now), receipts missing **1 → 0** |
| 12 | 2026-09-13 | §DX-02dm | `acdda86` | §PLAY-01-D forces Yael's Impartial welcome on a fresh run's first meeting, and the card badged it **Quest Active** from the Slums quest's state; the onboarding branch now returns `tier: 'impartial'` and the badge defers to it, Quest Active → Impartial on that card and nowhere else |
| 13 | 2026-09-13 | §DX-02dq | `516b595` | `_talkToNpc` called `_getNPCDialogue` for a null check, so every Talk, refused ones included, advanced `npcVisitCounts` and spent whichever one-time line was due (a Froberger trace, Brynn's Room 6 line) without showing it; it now checks `NPC_DIALOGUES[key]`, and Talk-driven visits measure **2 → 0** and **5 → 0** through the real functions |
| 14 | 2026-09-13 | §DX-02eu | `f3f5053` | the caption code wrote an inline `position: relative` over the ending map's `position: fixed`, so the closing image rendered 262 px down the page under the UI; the line is deleted, the caption is still contained by the fixed overlay, and `living-world-l44.spec.js`'s F9 case now asserts the fixed state. Inline writes 1 → 0 · 27/27 · `npm test` 152 passed / 1083 failed, identical to baseline — 1081 browser-launch, the 2 real ones are §DX-02js’s |
| 15 | 2026-09-13 | §DX-02fh | `028e6a2` | a delivered Froberger trace was shown once and never again, where Layer 45 §II puts it back in the NPC's pool; `_getNPCDialogue` now appends it to the Dear Friend pool from the saved delivery flag while favor meets its `minFav`, so the authored table is untouched and it survives a reload. `yael` 1× → 11× in 40 visits · 27/27 · `npm test` 152 passed / 1083 failed, identical to baseline — 1078 browser-launch (§DX-02ir) plus this row's 3 new tests, which cannot launch a browser here; the 2 real ones are §DX-02js’s |
| 16 | 2026-09-13 | §DX-02jy | `1e31192` | `quest_1367_f_plague.failText`’s *"gain plague_exposed"* — the state key printed at the player, the one identifier-shaped token left in any player-text field after §DX-02jv — became *"or carry the sickness out with you"*. The CON DC 13 save is real and stays; the sentence promises no effect because `plague_exposed` has one writer and no reader, and its declaration names an Exhaustion bit and a cure quest that do not exist. Round trip 1 → 0 · 27/27 · `npm test` 152/1080, identical to baseline. Filed **§DX-02jz** (the exposure) and **§DX-02ka** (the four 1367 tracks, one reader between them). |
| 17 | 2026-09-13 | §DX-02jv | `a20a77f` | **The census came first, as the row asked, and it made the call.** 2,806 of 2,853 quests carry a `desc`; the row's term list flagged **279**, of which **267 are `DC` in `skill_check` descs** — the engine's own player register — and 8 are plain English. **8 genuine (0.29%)**: 3 the term list caught (`quest_d0207_a2`, `_a3`, `quest_va_01`) and 5 only an identifier scan could see (`quest_d0209_a3`'s bracketed flag name, four 1367 vignettes' state-key tags). All eight rewritten through `./bin/api sub`; `desc` stays a player field. The tags described real `_legacy_fn` effects that `export` strips, but no surface shows those tracks, so they were dropped. Round trip 8 → 0 · 27/27 · `npm test` 152/1080, identical to baseline. Filed **§DX-02jy** (`quest_1367_f_plague.failText` prints `plague_exposed`, the one identifier left in any other player-text field). |
| 18 | 2026-09-08 | §DX-02ju | `a1717b6` | **`AC` is `HAJ`, The Antecedent Chamber** — the terrain key is literally `antecedent_chamber`, `quest_antecedent_01` runs `HKG` → `HAJ` which *is* the archival chain, and `AC` is in neither half of the LEGACY CODE MAP so it never was a retired code. The row's guess, the Blue Shutters Archive, is at `LHR` and not on the chain. **The precedent overturned the row's other recommendation:** only two quests in the world print live node codes at the player, and `quest_city_watch_patrol`'s *"City Streets (LHR) → The First Inn (TLL) → Birka Tavern (MHQ)"* is **name-first, code in parentheses**. `quest_depth_01` was the only code-only string. So the codes stayed and the names joined them, each name read from the node's own `label`. Verified from disk: four of four resolve, `\bAC\b` → **0**, both writes via `./bin/api sub` · 27/27 · audit 0 errors · `npm test` 152/1080, identical to baseline. Filed **§DX-02jx** — the naive unresolvable-token check is unusable (73 tokens / 468 hits, led by `AC` 75 = Armor Class), but the adjacency form returns **3 hits, all nameable**. |
| 19 | 2026-09-08 | §DX-02jr | `8d8d8fb` | **The call was settled by the ground, not by taste.** `docs/maps/node-index.md`'s LEGACY CODE MAP has `| CY | 6 | HKG | Neon Undercity |`, corroborated by number and label; `get node CY` is *not found* against 416; the terrain key is `cyberpunk_streets`; `maps.md` lists the rename; a test comment already records `CY→HKG`. **Nothing defines `CY` as an in-world name.** The replacement is not `HKG` — prose gets the label, **the Undercity**, a word two of the swept strings already used. **Register decides:** `quest_depth_01`'s `knowledge`/`msg` speak in codes beside the **live** `DSF`/`DA3`, so they took `HKG`; **`AC` names no node** → §DX-02ju. **The census, taken structurally over the exported collections, corrects the row twice:** 13 API-owned hits, and `desc` is neither ×4 nor author-facing — `play.html:31013` renders it in the mission list. **21 sites swept (13 via `./bin/api sub`, 8 by hand); 43 → 22, all survivors comments**, 0 code lines and 0 string values. Title re-cut a second time: *Undercity Fighting Pit*, rule 41 → 48. Filed §DX-02ju, §DX-02jv, §DX-02jt, §DX-02jw. Round-trip 0 `CY` in re-exported data · 27/27 · audit 0 errors · **`npm test` 152/1080, identical to baseline, no new red**. |
| 20 | 2026-09-08 | §DX-02jq | `585b9c7` | `CROV'S TRAINING LOG — CY Fighting Pit` titled the object §DX-02fg had just made readable, and `crov` is the `NPC_DIALOGUES` key where `meta.name`, the registry row, all five perk attributions and perk 5's title say **Pit Master Weckmann**. Through the real `_buildWeckmannLog()` at 0 wins and 5 + champion: `[Cc]rov` in the rendered log **1 → 0**. **The row's Verify clause cannot be satisfied** — `grep -c "\bCrov\b"` is **0** and always was, since `act8FarewellCrov` has no boundary before `Crov`; `grep -o Crov | wc -l` is **2 → 1**, the survivor a state flag. The em-dash rule beneath the title, unmentioned by the row, re-cut **37 → 41** to match. `CY Fighting Pit` left for §DX-02jr. **The third leg ran:** 1232 collected, **152 passed / 1080 failed**, 1078 of them `browserType.launch … ENOENT`, **2 real and pre-existing** → §DX-02js, which also records that `playwright test --list` returns 0 tests at the repo root and 1232 in `src/`. |
| 21 | 2026-09-07 | §DX-02fg | `fdae2b8` | `[years of entries — fighters' names, brief notes, outcomes]` shipped verbatim inside `WECKMANN_TRAINING_LOG`, in the same bracket convention the log uses for prose, so a player opening Weckmann's notebook read an instruction to whoever was meant to fill it in. Four entries covering Year 15–22, the *"running legal fights only"* era, with **Reincken, Kerll, Roberday** — the log's own 17th-century organist register, each **0** occurrences in `play.html` and `docs/` before use. Bracketed spans **2 → 1** through the real `_buildWeckmannLog()` at 0 wins and at 5 + champion. **Written against four things Weckmann already says:** *"forty-six fighters, three lasted a decade"* (none of the three was on the page — Reincken is now the third), *"best student I've had since him"* (so nobody may outrank Bruna), the fixed fight he closed the pit a week to refuse (**canon, previously unwritten** — now Year 22, placing the present at Year 23), and Year 12's *"check the footwork"*, applied in Year 17. The lab report's own §F5 misquoted the string as *twelve years* two lines above its correct *Measured* line; corrected at the source. Two findings filed from the header this row did not touch: **§DX-02jq** (`Crov`, the database key, printed where 54 other sites say Weckmann) and **§DX-02jr** (`CY`, not a node against 416, still spoken to the player in six field types). |
| 22 | 2026-09-07 | §DX-02da | `3727b0c` | the row said a failed corridor check says nothing; it says `failText`, in the h-card's `dmg` slot, and always did — so its `onFail:[]` is correct and the proposed narrative bit would have doubled the sentence. **The defect beside it was real:** `execBits(sc.onFail …)` carried no `pushMsg`, so any narrative bit on the fail path went to `storyMsg` and was overwritten by the `storyRender` two lines down — §DX-02gc's mechanism one surface over. **2,636 skill_check bits · 34 with a non-empty onFail · 6 with a narrative bit in it**, `quest_wis_01`…`06`, each a full authored paragraph never shown. Fixed by giving the fail branch the sink the pass branch has had since §BOARD-01-FU6; `check:invariants` gains **I6**, red before at 1/2 |
| 23 | 2026-09-07 | §AUDIT-03bk (part) | `800b5ae` | the navigability half shipped and the conversion half was **disproved**. `completion.atNode` is an AND term, so it can only withhold completion — the row's stated hazard (a quest firing early) is the one thing it cannot do, and the unstated one is that a quest satisfied elsewhere can never complete until the player walks back. Its remedy also conflates two fields: of the 19 quests with both, **15 are `atNode == waypointNode`** and **4 are `atNode == activateNode`** — fetch-and-return, where the waypoint is the errand and the giver is the finish. Retagged 🟢 → ASK with the sorting rule written out. What shipped: `check:questgraph`'s arrival-route census, red at HEAD on **`quest_sea_01`**, which completes only at `NWI`, is given at `SEN` and published no waypoint — the Navigate button never appeared for the one quest whose whole completion condition is going there. **unrouted 1 → 0** |
| 24 | 2026-09-07 | §DX-02x | `2d94547` | the raw ledger write this row was filed against had already closed at §DX-02cm (`grep -c` **0** at HEAD), so what shipped was the sweep it asked for and the fence under it. **Four assignments to `S_story.npcFavorability`, all four already inside the promotion path** — the setter, the Dear Friend upgrade it calls, and `storyNewGamePlus` restoring the map across the reset. No raw writer survives, so the census decided nothing, which is the answer. `check:npcregs` phase **10** `writers` declares each with its reason, because the reason is the argument: the setter is absolute, refuses to lower a level, and is the only thing that speaks the tier line — a promotion written past it is relative, unguarded and silent at once. Stale-exemption and no-write-found both go red; selftest 14 → 16 plants |
| 25 | 2026-09-07 | §DX-02gc | `3339c14` | a favor promotion announced the database key at the player — **6 of 15 favor keys had no `BIRKA_NPC_PROFILES` entry**, named by 8 bits, and they are **§NPC-01-SF2's own list** of dialogue-only NPCs. The fix was an **extraction**: `_talkToNpc` already resolved the name correctly thirty lines below, so the chain existed and was simply not the one the promotion used. **6 slugs → 0**, measured by running the shipped resolver over all 15 keys. **The second half is why the first went unseen:** `storyMsg` replaces the strip, and a completion-driven tier line was overwritten in the same synchronous render — the corpus had been hand-writing the acknowledgment into a sibling `narrative` bit for months. Widened through the grammar (`favor(bit, ctx)` → `E.setFavor(..., say)`), `QUEST:CORE` re-inlined byte-identical, driven end to end in Node. `check:npcregs` phase **9** `spoken` asks what phase 3 does not — whether a key resolves to a **name** — with the modelled registries lifted from the resolver's own body; selftest 10 → 14 plants. Two of the row's numbers were wrong (`BIRKA_NPC_PROFILES` is **204** keys, not 9) and are corrected in the close |
| 26 | 2026-09-07 | §GR-FU2 | `a1753de` | the ceremony names whoever sits at `fav >= 2` and `SWEELINCK_NAMING_LINES` was not the set of people who can get there — `emmer`, `gret` and `rennau` each stood at the exact threshold with no line. **7 → 10**, tier 2 only for all three (none has a `DEAR_FRIEND_BITS` entry, so nothing can raise them past their arcs' `set:2`). The call is *the ceremony names everyone the game raises to its own threshold*, because that is already the rule the builder implements. `check:npcregs` gains **phase 6**: it reads the threshold out of the builder's own `fav >= N` and computes each NPC's favor ceiling the way the runtime does — red at `ba29134` with exactly the three, green after, selftest 6 phases → 7. Repaired `audit03n-npc-registries.test.js`, whose *"exactly the six canonical keys"* assertion had been red since `3c816a5` and unread because `npm test` fails as a whole on this musl host. Filed **§GR-FU3** (three authored `3:` states the corpus cannot reach) and **§DX-02jd** (427 browser-free tests nothing runs) |
| 27 | 2026-09-07 | §GR-FU | `3c816a5` | the La Riva arc raises Kenickie to Dear Friend and `SWEELINCK_NAMING_LINES` held six keys, none of them his, so the Covenant Ceremony could not name the person the arc exists to have helped — **6 → 7**. The lab report's deferred text (*"who kept the account book. Who knows every name in it"*) was **not** used: Aldo kept the book and the delivery narrative says Kenickie *"does not open it"*; shipped *"Kenickie, who took the book without opening it. Who is still holding onto it."* Tier 2 deliberately absent — nothing can put him at 2 — with one comment saying so. `check:npcregs` accepts the key unchanged. The row's *"check the other five"* grew into **§GR-FU2**: `emmer`, `gret` and `rennau` are written to fav 2, the ceremony's own threshold, and none is in the table |
| 28 | 2026-08-23 | §DOC-02cz | `32c10c5` | the most completely-shipped lock the program has scored, whose best Froberger trace has never been read because a hardcoded `2` sits three hundred lines away from a gate… |
| 29 | 2026-08-23 | §DOC-02cy + §DOC-02cx | `32c10c5` | the most faithfully-transcribed lock the program has scored, whose off-screen character is named by nobody, whose six journal notes are posted into the movement… |
| 30 | 2026-08-22 | §DOC-02cx | `32c10c5` | the design lock whose every string survived ninety days byte-for-byte, whose best ending has never once fired, and whose five perks are congratulated, badged, carried… |
| 31 | 2026-08-21 | §DOC-02cl | — | the thesis was right, the plan was right, it shipped whole in nine hours — and the one census row it counted with a regex instead of a parser became a shipping defect… |
| 32 | 2026-08-21 | §DOC-02ck | — | the same uncommitted user recolor is still in the tree (HP-bar gradient, light-green playfield, theme-ink die numerals, self-labelled *"user recolor 2026-08-06"*) —… |
| 33 | 2026-08-18 | §DOC-02ch | — | the string is byte-exact at 37 days, the reason given for the fix beside it is false, and the road it signposts is closed at both ends |
| 34 | 2026-08-18 | §DOC-02cb | `27c38f5` | the most faithfully executed lock in the corpus, describing a feature no player has ever reached |
| 35 | 2026-08-17 | §DOC-02bz | `fcb79dc` | the arc is built exactly as written, and it has never been playable |
| 36 | 2026-05-25 | §DOC-02ar + §XVI | — | four corrections in maintained docs, all measured.** `world.md`'s Void Archaeology heading read ⚠️ **PLANNED** while `story.md:1632` read ✅ *Implemented 2026-05-25* —… |
| 37 | 2026-08-14 | §DOC-02bg | — | 59 of 62 citations exact and six node codes wrong, and the engine's own comments are why |
| 38 | 2026-08-13 | §DOC-02au | — | the phases were right and the brakes were prose — every loop cap in the "All limits prevent runaway" table is understated by four orders of magnitude |
| 39 | 2026-06-09 | §DOC-02as | — | ⟶⟶ §DOC-02as ✅ SHIPPED: the last file in the `2026-06-09 11:06:54` block, and the corpus's best node-code result sits on top of an arc that has never been playable for… |
| 40 | 2026-08-13 | §DOC-02ar | — | a 100 % identifier census over an arc that has never been completable past its second quest, and the report is the evidence that the engine has a typo |
| 41 | 2026-05-25 | §Weimar + §Layer | — | `_applyTomeBonuses()` (0 hits; ships as `_tomeBonuses()`), `tome_rasp_annotated` (0 hits), and *"NG+-persistent"* tomes (NG+ resets inventory from `_S_DEFAULTS()`). The… |
| 42 | 2026-08-12 | §DOC-02an | — | a 100 % census beside an arc that has never been played |
| 43 | 2026-08-12 | §DOC-02al | — | its drafts died and its ideas shipped |
| 44 | 2026-08-12 | §DOC-02aj | `7de58fb` | a spec that enumerates names ships its names; a spec that specifies voice ships nothing but its names |
| 45 | 2026-08-12 | §DOC-02ai + §DOC-02aj | `05cfa22` | a world where Saul of Tarsus walks from Jerusalem to Damascus and is healed by a man named Anath.** *(Its THIRD finding — the Thorn — was withdrawn by §DOC-02aj above:… |
| 46 | 2026-08-12 | §DOC-02ae | — | the roadmap that said "none of these can be started in this session," and shipped one of them thirty-three minutes later |
| 47 | 2026-08-12 | §DOC-02ad | — | a finished instrument, a silent game, and a default registration that is a Flute wearing a Principal's label |
| 48 | 2026-08-12 | §DOC-02ab | — | the schema shipped and scaled 35×, the script shipped 0 of 120 lines, and the ending it was written to earn now accuses the player who earned it |
| 49 | 2026-08-12 | §DOC-02y | — | three companion arcs almost perfectly intact, and an engine comment that denies eight node records while quoting their labels |
| 50 | 2026-08-12 | §DOC-02u | `6ab2a08` | a near-perfect implementation of an arc that no player can enter — the program's FIRST 100 % casualty |
| 51 | 2026-08-12 | §DOC-02s | `f622342` | the strongest survival the program has measured, fully reachable — and the arc's own stated payoff is the one thing that never shipped |
| 52 | 2026-07-07 | §XI | — | a node-text swap reading *"Two of the three blocks are still rubble. The third has a market in it — one stall…"* — so **the flag went from write-only to read**,… |
| 53 | 2026-05-26 | §DOC-02n + §III | — | §III's five-act prose was **compressed to a design table** because `story.md §GRIEF AND CORRUPTION` holds the prose verbatim and marks it *"✅ Implemented 2026-05-26"* —… |
| 54 | 2026-08-12 | §DOC-02q | — | a shop that works exactly as written, and a statistics ledger that forgets everything at the one boundary it was built to survive |
| 55 | 2026-08-12 | §DOC-02p | — | the origin specification of the Curse Score, implemented almost perfectly, describing a system that has not been able to run for 75 days |
| 56 | 2026-08-12 | §DOC-02o | — | the report's thesis names a payoff, and the payoff is unreachable — the game declares four endings and can produce exactly one, the failure one |
| 57 | 2026-08-12 | §DOC-02l | `62b162a` | the spec shipped almost perfectly, then a rename broke the one link nobody greps for — the engine COMPUTES its quest ids, and forty of them stopped existing 75 days ago |
| 58 | 2026-06-09 | §7 + §DOC-02m | — | IEEE-shaped (abstract · 6-instrument method · as-built inventory · node-code translation · **35-row two-way delta table** · 5 findings · 20-row NPC contract register ·… |
| 59 | 2026-08-11 | §DOC-02k | `9d3e06f` | the one field-name this design lock got wrong is the one it spells two ways WITHIN ITSELF — and the engine followed the table, not the prose |
| 60 | 2026-06-09 | §DOC-02f + §7 | — | IEEE-shaped, **326 → 295 lines**; ~75 lines of pre-implementation prose replaced by a **41-row two-way delta table**, six numbered findings, a decision register and a… |
| 61 | 2026-08-11 | §DOC-02h | `8986fa8` | the most faithfully implemented arc in the program is 70% unplayable, and the report got every coordinate right |
| 62 | 2026-08-11 | §DOC-02g + §DOC-02d | `70d49a0` | legacy `BK` = Broken Tooth Tavern, Visby = live `VBY`; `BK` still resolves, as a **Birka beach**, so `check:noderegs` passes while Corelli says *"Visby. Well."* and… |
| 63 | 2026-08-11 | §DOC-02d + §DOC-02 | `bb9bd58` | the same uncommitted user recolor is still in the tree (HP-bar gradient / light-green playfield / theme-ink die numerals, self-labelled *"user recolor 2026-08-06"*) —… |
| 64 | 2026-08-11 | §DOC-02c + §DOC-02 | `cc4a07e` | the same uncommitted user recolor is still in the tree (HP-bar gradient / light-green playfield / theme-ink die numerals, self-labelled *"user recolor 2026-08-06"*) —… |
| 65 | 2026-08-11 | §DOC-02a + §DOC-02 | `e784721` | `play.html` carries an **uncommitted user recolor** (HP bar → brown/gold gradient, playfield zones → light green, die numerals → theme ink, all self-labelled *"user… |
| 66 | 2026-08-04 | §AUDIT-03p + §AUDIT-03d | `ea19a88` | `check:noderegs` **phase 5** applies the same explicit `LOCAL_NODE_KEYED`/`NOT_NODE_KEYED` classification to **indented** `const/let/var NAME = {` literals — phases 1–2… |
| 67 | 2026-08-04 | §AUDIT-03k + §AUDIT-03d | `1c4f547` | `WBAPI.NPC_ALIASES` + `npcCanonicalKey()`; the seven slugs **out of `npcKeyVocab()`** (297→290), so a quest still carrying one advise-warns rather than validating beside… |
| 68 | 2026-07-31 | §AUDIT-03n + §AUDIT-03d | `1be43be` | the epilogue's `npcOrder` and **five `_npcFavor('bruhns')` gate sites** — the §XXXVII CO scene at TLS, the S6 Weckmann/Auros joint conversation (**which ANDs a live key… |
| 69 | 2026-07-30 | §AUDIT-03h + §AUDIT-03d | `f96b4e5` | `guide_01–05`→`emmer` · `guide_06`→`the_fisherman` · `scar_01/03/04`→`gret` · `scar_02`→`pier`, each corroborated twice (profiles at the arcs' own `activateNode`s… |
| 70 | 2026-07-29 | §AUDIT-03g + §AUDIT-03d | `f08f70f` | the load-bearing discovery is that **a §D02 Epic-Battleground *approach arc* inherits its battleground's giver** — `quest_ea_primary`/`_return` at RAI already carry… |
| 71 | 2026-07-29 | §AUDIT-03b + §AUDIT-03g | `221a1ef` | the ~12 OPEN per-quest calls resolved by reading each quest's text; the whole 203-quest mapping re-derived and applied (**203/203 correct · 7 genuine Silver quests kept… |
| 72 | 2026-07-28 | §NPC-01-SF4 + §NPC-01 | — | each cluster's arc has exactly ONE render-gate node in the live file — **CQ→CDG** (Layer 44 Ally Cat Arc gates on `node.code==='CDG'`; Jimmy is CDG's own NODE_MAP… |
| 73 | 2026-07-28 | §NPC-01 | `76ad683` | The Derivable NPC Card Map (fully closed; promoted §POT-R…) |

**69 completed entries.**

---

## Phase 4 — World & Navigation

| # | Date | Increment | Ship | Headline |
|---:|---|---|---|---|
| 5 | 2026-09-14 | §DX-02ki | `cf44432` | §CELL-01 retired the `N/E/S/W` pointers, so a compass bearing in authored prose is the only direction the game still states to the player and no gate read one; the census is 84 of 216 `text` fields over 118 sentences, 21 beside a resolvable node name, and **11 of those assert nothing** (FEATURE — *"Damascus's southern gate"*), excluded by construction. `WRO`'s carved signpost said *East, Birka* and Birka is **dr −8, dc 0 — due north**; `SEA`'s four-quadrant sweep had Malta and Philippi **swapped**. `check:bearings` is gate #29 (20 selftest checks), placed last so §DX-02jb's hand-maintained gate numbers do not shift; **2 → 0** contradicted, `EXEMPT` empty. Filed §DX-02ke2 |
| 6 | 2026-09-13 | §DX-02by | `a37a15b` | `MoveResult.via` restated `ok` since the ferry branch was deleted, written on every step and read nowhere (both callers reach the kernel as `Mover.move`, and neither reads it or sends the object); removed from both `MOVER:CORE` copies, parity 1,847 → 1,823 bytes identical |
| 7 | 2026-09-13 | §AUDIT-03bc | `68171ef` | the GEO tables' `label` is the real-world city and nothing said so; a header on each in `worldmap.js` and `wbapi-server.js` says it, and that all 155 codes are live nodes. Shared labels are two pairs (*Jerusalem*, *Palermo*), and the two tables have drifted on `NUE` |
| 8 | 2026-09-13 | §DX-02cb | `5513728` | seven live nodes had no lat/lon in any source and geo-seed reported only a count; seven gazetteer entries on their live cells take skipped **7 → 0** (placed 409 → 416), the server names skipped codes and refuses to apply around a gap, and four math-realm nodes that disagree with their anchor are filed as §DX-02kc |
| 9 | 2026-09-13 | §DX-02el | `58983d4` | the five §KG terrains carried their node's own label, so each header said the name twice; relabelled to the kind of place through `./bin/api`, stutter **26 → 21**, and `check:invariants` I7 fails on any new one while the 21 left (§DX-02kb) may only shrink |
| 10 | 2026-08-22 | §DOC-02cw + §KG-01 | `65d65c0` | the content report whose every number survived 45 days, whose road was laid by the method it forbids, whose sea-cell violation hid behind a red that was already red, and… |
| 11 | 2026-08-17 | §DOC-02by | `7a4570e` | a design lock written 23 minutes before the code, exact in every number six weeks on, and wrong about one room's occupant |
| 12 | 2026-08-17 | §DOC-02bt | — | every statistic in the diagnosis re-derives exactly, and the one sentence nobody filed as a row cost four red tests for six weeks |
| 13 | 2026-07-05 | §ARCH-01 | — | expected `docs/lab-reports/lab-report-uqf-migration-playbook.md` (`2026-07-05 14:42:46`), the §ARCH-01 UQF migration playbook. It is a **method** document rather than a… |
| 14 | 2026-08-14 | §DOC-02bn + §AUDIT-03x | — | the corpus's most accurate report, and the one sentence in it nobody measured is the origin of §AUDIT-03x |
| 15 | 2026-08-14 | §DOC-02bl | `c946a01` | the removal shipped completely, and was undone twelve days later by a track that reused every one of its names |
| 16 | 2026-08-14 | §DOC-02bk | — | the program's first FULLY-SHIPPED verdict, its highest symbol score (43/43 node codes), and the one exception it asked for was taken and then given back |
| 17 | 2026-08-14 | §DOC-02bj | — | its geography is byte-perfect and its arithmetic reverses its own thesis. THE 2026-06-16 BLOCK IS NOW EMPTY |
| 18 | 2026-08-13 | §DOC-02be | — | an unusually strong transcriber and an unreliable narrator — 31 of 31 citations exact, and every error in a sentence the author composed |
| 19 | 2026-08-14 | §DOC-02bd | — | everything it borrowed is exact, everything it counted is wrong, and the world it describes is three times too small because of a space before a brace |
| 20 | 2026-08-13 | §DOC-02az | — | the report is correct and the ground moved |
| 21 | 2026-08-12 | §DOC-02z | — | the program's most faithful content spec, into a world that moved out from under four of its nodes |
| 22 | 2026-08-11 | §DOC-02f + §DOC-02 | `ffe7720` | the uncommitted user recolor (HP-bar gradient / light-green playfield, self-labelled *"user recolor 2026-08-06"*) is still in the tree — **left untouched and unstaged**;… |
| 23 | 2026-07-31 | §AUDIT-03j + §AUDIT-03d | `c5f9079` | (1) **the table's other column was dead too** — its npc keys were the profiles' SURNAMES (`couperin`=Bard Tomas Couperin=`quill`, `weckmann`=Pit Master Weckmann=`crov`,… |
| 24 | 2026-07-29 | §AUDIT-03l + §AUDIT-03c | `d42a707` | `check:walk` **12/12 exit 0**; new `audit03l-node-index.test.js` **5/5**; **two negative controls** — a bogus row appended to the committed index, and a real node… |
| 25 | 2026-07-29 | §AUDIT-03c + §AUDIT-03d | `710bb75` | `CQ` = **`CDG`** (The Cat Quarter) and `FR` = **`AMS`** (Fishmonger's Row) are both **right**, corroborated three ways each — the nodes' own labels/inline NPCs, and the… |
| 26 | 2026-07-29 | §DX-02e + §KG | `8fc9a2e` | auto-travel routes with `_roadGridPathCore` (§NAV-01d road-weighted Dijkstra, road/sea-lane cost 1, everything else 2), and all six legs `TLL→SPB→KMS→ZVD→FBR→TVR→SVO`… |
| 27 | 2026-07-29 | §AUDIT-03e + §KG | `2da7fbb` | one normalisation line after `NODE_MAP:END` (the data section keeps its 129 authored `code:` fields — not 287 new ones) + the `NODE_CODE_BACKFILLED` set captured just… |

**23 completed entries.**

---

## Phase 5 — Platform & Tooling

| # | Date | Increment | Ship | Headline |
|---:|---|---|---|---|
| 1 | 2026-09-25 | §EDITOR-04 inc 1–2 | `4737258` | `GET /api/context` + `./bin/api context`: a node's or arc's questline with unwritten flags, deadlocks and cell primacy, reusing `check:questgraph`'s exported scanners (unwritten set = gate's; primacy 416/244/172). Row stays open at increment 3 (ASK) |
| 2 | 2026-09-25 | §DX-02bq | `cf48219` | nine of `/api/audit/map`'s twelve checks read link fields that number 0 across 416 nodes, and are now reported as `structurallySatisfied` from a runtime census (not retired, which would pre-empt §DX-02ky). The last-wins shadow `buildCellGrid` was never called and is deleted |
| 3 | 2026-09-25 | §DX-02at | `48a385b` | `wbapi-core`'s `OPERAND_CONTRACTS` now derives from `BIT_CONTRACTS`, keeping only prose; `edit.html`'s typed copy gains `cost` (row and bit card) and a test fence that is red on the old file. Authorable kinds 11 → 12 in both |
| 4 | 2026-09-25 | §DX-02as | `6b30024` | `skill_check.adv` implemented (second seeded d20, keep high/low, one draw as before when absent); `narrative.template` and `unlock.npcs` removed from the contract and both mirrors; `item_check` kept, since §VM-01's coroutine test reads it; the non-UQF comment corrected to the 30 stubs. Corpus validity 2,823/2,853 unchanged; a census of 5,662 authored bits fences the class |
| 5 | 2026-09-25 | §DX-02lf | `6a25d06` | the speak route's header comment still promised prompt caching after §DX-02ak deleted it; reworded to name `WBAPI.npcSpeakSystem`. `grep -ci 'prompt caching'` 1 → 0 |
| 6 | 2026-09-25 | §DX-02al | `0d19f91` | the NPC-speak prompt joins `worldTruth`/`enemy` from `NPC_DIALOGUES` meta onto the profile voice through `WBAPI.npcSpeakSystem`, emitting each only where authored (0 → 204 / 199). 204/204 profiles have a twin, and `check:npcregs` phase 11 fences it |
| 7 | 2026-09-23 | §DX-02le | `7e71553` | gate #32 `check:promptcache`: every server-side `cache_control` property needs a declared model and a measured prefix at or above that model's floor. The floor is per model (4096 for Haiku 4.5, 512 for Opus 5 and Fable) because the endpoint takes `?model=`. Red on `05bb436^`, green at HEAD |
| 8 | 2026-09-23 | §DX-02ak | `05bb436` | the NPC-speak `cache_control` could never fire: Haiku 4.5's floor is 4096 tokens and the largest of 204 per-NPC system blocks is ~694, so all 20 logged calls wrote and read nothing. Deleted with a comment recording the floor, not padded, because padding costs more than it saves. Both lab reports that priced the saving corrected, and the anchor to the deleted line rewritten in history form. The row's asked-for fence is filed as §DX-02le |
| 9 | 2026-09-18 | §DX-02ab | `60cc16c` | a pool fish carries both a `MONSTER_POOL` statline and a `MONSTER_DROPS` trophy, so the monster loop and the trophy loop each emitted it — **432 rows with 25 duplicated**, and the row undercounted at 20 by omitting the night fish. **Its second defect is disproved**: the row says no `nfish_*` key exists so the 5 night fish are dropped; the prefix is `night_`, all 5 carry entries, and they were already returned. **Both of its fixes for the first were lossy** — the two rows carry disjoint halves (`terrains`/`dmgDie`/`weaponDrop` vs `fishRank`/`isNight`) — **and unconditional de-duplication would have broken `fishing=false`**, which legitimately counts fish as monsters. Collapsed only when both sections are shown, with the union carried; every filtered response byte-unchanged. De-duplication tests pool membership, never the `night_*` prefix, because `night_owl` and `night_hag` are land monsters. **432 → 407, duplicates 25 → 0.** Ships the missing `./bin/api loot-drop` and its two doc sections, so the report's five raw-curl smoke criteria run through the tool the directive mandates |
| 10 | 2026-09-18 | §DX-02aa | `775cbfd` | the server rewrites the whole file from the text it loaded, so a write from a server predating a hand-edit reverted it under `ok:true` — Hazard #1, documented since June and never enforced, and the mechanism behind the 2026-06-05 §DROP-01/§DROP-03 reverts. **The `fs.watch` auto-reload added since the row was filed narrows the window to a 200 ms debounce and does not close it**; reproduced with the watcher running. Guarded at `saveGameFile()`, the one choke point, answering **409 `stale:true`** with the recovery named. **Digesting by re-reading the file turned `test:mud` red** — 44 servers in one harness run, one past its 8 s deadline — so the digest is of the held text, which `save()` writes verbatim: equivalent, no I/O on load or save, 3.6 ms against a write's ~180 ms re-parse. Eight consecutive and three concurrent writes still 200; the refused value never reaches disk; `POST /api/reload` recovers; `./bin/api` surfaces it unchanged. `write-acceptance.mjs` gains the Hazard #5 test, editing engine code outside every data section — 3 findings against the unguarded server, selftest 20 → 23 |
| 11 | 2026-09-18 | §DX-02ch | `bbf1503` | `serializeQuestLiteral` walks five enumerated lists and drops anything absent from them with a **201**, while `PUT` dispatches on the value's type and is generic — so a field could be authorable and postable long before it was persistable. **The row called that a coverage gap rather than a red; §DX-02kv had since made `SCHEMAS.quest` exactly equal to the corpus (29 = 29), which made the comparison decidable and the answer five live fields POST silently dropped**: `rumor` 25 · `onActivate` 20 · `killCounter` 6 · `boardExempt` 3 · `vignetteTextAlt` 1, 55 instances, proved live at **24 of 29 surviving** a real POST re-parsed off disk. `hook`/`rewardText` removed (0 carriers, no authoring surface); the eleven remaining undeclared entries classified with a reason each. **Gated statically in `check:schema` and live in `write-acceptance.mjs`** — not `check:arraypatch`, which the row named and which cannot host it because requiring the server binds a port. 5 findings / `[post-dropped]` against the pre-fix serializer, green on restore; selftests 15 → 28 and 17 → 20. Filed **§DX-02ld** (the Quest Creator still authors `targetMonsterKeys`, retired by §DX-02kt, so a worldbuilder quest reddens gate #31) and **§DX-02lc** (`POST` drops an unknown name with 201 where `PUT` refuses it with 400) |
| 12 | 2026-09-18 | §DX-02la | `c6c64f3` | `replaceSection` overwrites a section's whole START→END span and `serializeNpcDialoguesSection()` emitted only the `const`, so every dialogue write deleted the comment above it — the only line distinguishing the npc-keyed `NPC_DIALOGUES` from the node-keyed `NPC_DIALOGUE`, one character apart, and the two comments are a **matched pair** each naming the other. **The census of 13 marked sections found three with a header, and the two safe ones settled the fix:** `NPC_DIALOGUE` is written entry-at-a-time, never whole-span; `_D100_TABLE` is written by `replaceSection` but `serializeD100Table` already re-emits its own four lines — so **the row's preserve-in-replaceSection option would have doubled that header on every `/api/loot` write**. Re-emitted in the serializer instead, with the sibling's `GENERATED` warning. **The row's instrument was wrong too** — `check:arraypatch` is a pure gate and requiring `wbapi-server.js` binds a port — so the property went to `write-acceptance.mjs`, derived from the file and fenced three ways: `[header-lost]`, `[header-doubled]`, `[header-blind]`. 1 finding against the pre-fix server, green on restore; selftest 12 → 17; `play.html` byte-unchanged |
| 13 | 2026-09-14 | §DX-02kv | `8d1c19a` | `GET /api/schema` declared 23 quest fields where `QUEST_DB` carries 31 — omitting `id` on all 2,853 — and declared `N`/`S`/`E`/`W`, carried by 0 of 416. Re-censused through `wbapi-core`'s parser rather than the JSON export, which drops a function value's key and made six live-looking fields unmeasurable. 33 findings; `check-schema.js` is gate #31 and asserts both directions, with classification (`derived`, `written-not-carried`) rather than an exemption list, and a stale classification failing too. Filed §DX-02lb. |
| 14 | 2026-09-14 | §DX-02kw | `aa3c0ae` | the whole-object dialogue merge assigned into `WBAPI.npcDialogues` and patched no section — the only mutating route nothing could persist, since a save writes `_rawSrc` the merge never reached. Made a real write; and because `Object.assign` takes any key, the accepted set is derived from the live corpus (214 entries, exactly the sub-routes' own six names) rather than shipping an unvalidated persistent write. Round-tripped off disk; `test:write` PERSIST_ROUTES 2 → 3, 3 findings against the pre-fix server. Filed §DX-02la. |
| 15 | 2026-09-14 | §DX-02kx | `0a57566` | `./bin/api broken` reported 93 isolated cells while `fix-all-broken`, documented ten lines away, reported 0 — both honest, because the `fix-*` family still iterated the `N`/`S`/`E`/`W` pointers §CELL-01 stripped (6 occurrences each in `play.html`, none in `NODE_MAP`) while `broken` had been migrated to the cell model. Retired rather than migrated: `broken` is the census, `reweave` + `cluster-bridge` are the repair. A `RETIRED` table answers each verb with its reason and replacement at exit 1, because a bare *Unknown command* is what let §DX-02bf go unnoticed. Runnable `./bin/api fix-*` lines in the swept API docs 11 → 0. Filed §DX-02ky and §DX-02kz. |
| 16 | 2026-09-14 | §DX-02kp | `6231e18` | `editField` read *no quoted value* as *no field* and appended a second key that wins by last-key, under `ok:true` — **10,339 (entry,field) pairs** exposed across six sections, including every monster's `ac`/`hp`/`atk` and every quest's `gate`/`bits`. Proved on `hp`: before, `inserted:true` and the value re-parsed as the **string** `"9"`. Routed to `editStructuredField` (which handles scalars and refuses closure drops), presence read with `hasOwnProperty` because `parseSanitized` keeps a function key; terrain refuses by name. 68 → 78 checks |\n| 3 | 2026-09-14 | §DX-02kn | `d70983d` | `location` inlined every related entity — 327,226 B at NUE to learn a coordinate. The row's numbers all held; what it missed is that `terrain.monsters` is full statblocks through the `P.<key>` proxy and the **larger** half of the duplication, worth 3,658 of LHR's 7,941 B after the quest fix. Four collections opt-in, rosters travelling together: worst case **327,226 → 3,781 B**, 0 of 416 over 4,000 B where 282 were, `?with=all` byte-identical by `cmp` against the stashed server. The default was the author's call and `edit.html` — the only consumer — reads five cheap fields, which settles it. help-behaviour gains `[location/depth]`, selftest 20 → 26. Filed §DX-02kr |\n| 4 | 2026-09-14 | §DX-02km | `e567e83` | `NPC_DIALOGUE` had no marker, no parse entry and no verb, so §2.5 could not be obeyed for 74 entries and §DX-02cv hand-edited six; markers + `get`/`put`/`list ids npcdialogue` + `export npc_dialogue` (all: 14 → 15 sections, 5,158 records). The row said 6 closures writing 3 flags — it is **41 of 74** writing **14**. Its own acceptance test caught a `quoteFn` write **inserting a second key** that wins by last-key under `ok:true`, so `quoteFn` reads and does not write. Four copies of the entry-level section map became one. `check:arraypatch` 56 → 68. Filed §DX-02kp, §DX-02kq |\n| 6 | 2026-09-13 | §DX-02fl | `767027a` | the empty `ITEM_DB` section existed only for `POST /api/item`, held nothing since June and was read by nothing in the game; on the user's call both are retired with their help, routes, loader field and gate entry, references 24 → 0, and two doc anchors to the deleted line are rewritten in the resolver's history form |
| 17 | 2026-09-14 | §DX-02cf | `b450046` | Every number in the row held, and the copy count did not: six, not two. |
| 18 | 2026-09-14 | §DX-02gh | `bbe041d` | 494 dead invocations renamed, the class gated, and the row's third clause disproved at the command line. |
| 19 | 2026-09-14 | §DX-02gz | `1d4b1e9` | One of the three named routes had the defect, and twelve routes nobody had counted had it too. |
| 20 | 2026-09-14 | §DX-02gy | `d4059c3` | Both defects reproduced exactly as filed, and the row's own fix would have refused most of the corpus. |
| 21 | 2026-09-14 | §DX-02kq | `44a86ce` | Two of the row's three claims did not survive the grep, and the one that did was the instrument a hard rule depends on. |
| 22 | 2026-09-14 | §DX-02fo | `0f40106` | The row's headline count was right, its member list was stale twice over, and the coincidence is worth naming. |
| 23 | 2026-09-13 | §DX-02fk | `27f2223` | `export condition_items` returned `{}` because `load()` never set `WBAPI.conditionItems`; wired rather than deleted, since the table is live in the game and documented in the help, and the export now returns the 12 entries |
| 24 | 2026-09-13 | §DX-02cs | `5b42dbc` | valid JSON that is not an object reached handlers that read an object and came back as a 500 with a V8 message; `readBody` now rejects it, and since all 44 call sites already turn a rejection into 400 `Invalid JSON`, four endpoints × five bodies go from four 500s, a silent 200 and four 409s to twenty 400s |
| 25 | 2026-09-13 | §DX-02fl | `767027a` | `ITEM_DB` and the item endpoints are retired, by the user's call. |
| 26 | 2026-09-07 | §DX-02jo | `69a38ad` | `say.sh` pointed `DAEMON` at `$ROOT/sayd.sh` while the daemon is `src/bin/sayd.sh`, and the launch discarded **every** stream, so exit **127** read as a started daemon — the tool `AGENTS.md` mandates after every commit had **never spoken on any host**, and `say.queue.d` held **58 undrained messages dated 2026-09-05 onward** to prove it. `DAEMON` now resolves from the script's own directory, a missing daemon prints the path and exits **1** rather than being swallowed, and the daemon's stderr goes to `sayd.log` instead of `/dev/null`. Guard switched to §DX-02jm's `match_pids`, replacing the `pgrep -f` §DX-02jc had patched at this line two rows earlier — this file held the repo's **last raw `pgrep -f`**. Verified on all three of the row's conditions: **0 → exactly 1** daemon, queue **59 → 0**, nonexistent daemon **exit 0 → 1**. The kept stderr found §DX-02jp within seconds: no `say` binary on this host and no `espeak`/`espeak-ng`/`spd-say` either, so the drain logged **62** tracebacks at 18 lines each. |
| 27 | 2026-09-07 | §DX-02jm | `06eb1bf` | `pgrep -f`/`pkill -f` matched the shell that invoked them — demonstrated twice: `monitor: UP` against **0** running monitors, and `./run.sh stop` **killing its own caller** (rc 143). All three fixes the row proposed are rejected on the ground (a pin cannot serve two callers launching by different paths; a PID file the monitor never writes; `--ignore-ancestors` is procps-ng only, the §DX-02jc mistake repeated). Ships `src/bin/procmatch.sh` — `match_pids` walks `ps -o ppid=` from `$$` and drops **ancestors** while keeping **descendants**, which the toggle needs. Wired into `run.sh` and `wbapi-toggle.sh`, one of whose sites is a `kill -- -PGID`. **§DX-02jo** filed and it corrects an earlier cause in this table: `say.sh` launches `$ROOT/sayd.sh` while the daemon is `src/bin/sayd.sh`, streams discarded, so nothing has ever been spoken |
| 28 | 2026-09-07 | §DX-02jn | `f39f538` | `GET /api/help/cli` executed `ROOT/api.sh`, a binary §DX-02gh records as deleted, and answered **200** with `live:true` and **0 bytes** — the one topic that promises to be current was the only empty one. Now `bin/api`, with **502** when the child fails or produces nothing. **0 → 54,804 bytes**; 502 proven by moving `bin/api` aside and 200 by restoring it. **Neither help instrument could see it:** gate #23 starts no server, and the harness read an empty body as nothing to check — the vacuity its other extractors are all guarded against. `[help/topics]` now fails an empty 200 (selftest **19 → 20**), and the fix widens the harness by itself: **79 → 83** GET calls over **43 → 46** paths |
| 29 | 2026-09-07 | §DX-02ie | `b7c7e0b` | `bin/wbapi` forwarded its arguments to `make` as targets, so `./bin/wbapi restart` started a server and then failed with `No rule to make target 'restart'`, exit 2 — the one hazard-avoidance step the standing procedure names had no working command behind it. Real subcommands (**start·stop·restart·status**, usage + exit 2 on anything else), `restart` and a scoped `stop` added to `run.sh`, `make restart` added. **The sweep behind it was wider than "docs": 34 refs over 21 files**, including `src/bin/coc`'s two **broken** paths and **7 live strings telling a PLAYER** to run a script that is not there. Overlap with **§DX-02gh** declared: its `wbapi-toggle` leg → **0**, its `./api.sh` leg re-derived at **483** and still open. **§DX-02jn** filed: `/api/help/cli` executes the same deleted `api.sh` and answers 200 with 0 bytes |
| 30 | 2026-09-07 | §DX-02jc | `b1cb2f7` | `./run.sh status` reported `monitor: down` unconditionally, because `pgrep -qf` printed a usage block and exited **2 whether or not a match existed** — the one line the command exists to produce was the one that could not be true. **The row's cause is disproved:** the host is procps-ng 4.0.6, not busybox; `-q` is **BSD** pgrep's spelling and procps-ng has no short form. `pgrep -f "…" >/dev/null 2>&1` is portable to all three. **A second site with the same flag** — `say.sh:35`, the `sayd.sh` fork guard behind the mandated announcement, its usage block swallowed by `2>/dev/null` — fixed in the same increment. **§DX-02jm** filed: `pgrep -f`/`pkill -f` also match the **invoking shell**, and the `pkill` sites would kill it |
| 31 | 2026-09-07 | §DX-02jk | `7d6f2f5` | `GET /api/help/<any name at all>` answered **200** with the index under the name that was asked for, so a typo was indistinguishable from a topic — the fallback that defeated §DX-02jg(b)'s first assertion. **The design call was whether 404 breaks a caller, and it is settled by census:** `src/api/wb.js` **0** references (the row's named suspect), `play.html` **0**, `bin/` **0**; the one caller, `edit.html`, prints the body at three sites and reads `.status` at none. Unknown topic → **404** naming the real topics; `topics:` corrected **18 → 19** because `cli` is served above the lookup and a corrective list must not omit a topic that works. The harness drops the title-based workaround for the status and gains the **control** that keeps it from being vacuous (selftest **17 → 19**) |
| 32 | 2026-09-07 | §DX-02jg | `e1495ff` + `d06adc2` | the help stopped being prose nothing executes — **(a)** a `check:walk` gate comparing three documented tables against the constants beside them, **(b)** a harness that boots a throwaway server and *calls* what the rest claims (**79 GET calls over 43 distinct paths**, every `export` collection, every nonce `type`). **Six mutations of the real source measure the boundary and neither instrument covers the other:** (a) alone sees a TTL five times short and a value dropped from the help's `type:` line; (b) alone sees an endpoint that has never been a route, an index entry naming a topic the server does not have, and an example naming an entity the corpus no longer holds. **(b) defeated its own first assertion** — `HELP[topic] || HELP['index']` answers **200** and echoes the *requested* name back, so only the title distinguishes a typo from a topic (**§DX-02jk**). Two design calls handed on with their measurements rather than pre-decided: **§DX-02ji** (39 field names diverge from `SCHEMAS` across four topics) and **§DX-02jj** (quest `type` is five lists, no two agreeing, `delivery` 57 quests in neither validator) |
| 33 | 2026-09-07 | §DX-02jf | `2de1a72` | the onboarding example taught the exact write §AUDIT-03av had just undone, and the search above it matched **0 of 416** nodes — but the row's four strings were four of **twenty-six**. Its closing sentence asked for *a sweep for any other help example naming a place the corpus has since renamed*; every example in both help surfaces was then run against a server booted on the edited source. **Four more searches return nothing** (`--q "wolsey"` 0, `--arc shk --q "inventory"` 0 against 28 unfiltered, `--arc shk --q "visby"` 0 against 88 occurrences of *visby* in `play.html`, and `--act 2 --q "birka"` 0 because every *birka* node is act 1) — replaced with terms that land on the very entities the next line of each block fetches, `shk6_act1` and `shk6_act2`. **`CY` at seven sites, including the quick-start banner printed on every server start**, plus `FR`, `EMA` and `coastal_market` — all 404, and `EMA` appears in `play.html` only inside the word `SCHEMA`, one step after the same walkthrough says *ALL activateNode codes must exist first*. **`/api/read/node/CY` is not a route at all** (400 `Unknown type "read"`) — §DX-02jg's own verify condition for its unbuilt option (b), met by hand. 45 distinct GET paths across all 18 live help topics now resolve; the one 404 left is created by the walkthrough that names it. Two further contradictions in `help/node` left standing for §DX-02jg, including `x, y` — fields carried by **0 of 416** nodes |
| 34 | 2026-09-07 | §DX-02fp/fq/fr/fs + §DX-02bb | `ec604a3` | the server's `/api/help` disagreed with the server five ways, and **the two rows saying so were the same row** — §DX-02bb(a) and §DX-02fp share both anchors, filed ten days apart into two phase files. Grepping to disprove the Phase-1 row found the Phase-5 family (§DX-02je's lesson, caught early this time). The row's own fix would not have worked: `"expires"` is not a field — the endpoint returns `expiresAt`. Also `terrain` in a type list that `400`s, a save step retired by §DX-02k, and **three** missing export collections across two lists plus a stale fourth copy in the CLI. **Verified by calling the server**, not by reading it. Gate deferred to **§DX-02jg**: a literal fence would have caught 2 of 5. |
| 35 | 2026-09-07 | §DX-02ix | `40832d2` | `editStructuredField` replaces a field's whole literal, so every comment inside it was deleted with `ok:true` — 23 (entry, field) pairs in `QUEST_DB`, 47 comments, 17 also holding a function, re-derived with a census that reuses `patchLiteralField`'s own token walk. The call was refuse / carry / accept: **refuse, plus an acknowledgement**, because a comment has no preserving escape the way a closure has `{__fn:'<source>'}` and a bare refusal would have made those 23 fields unwritable. `--drop-comments` / `?dropComments=1` accepts the loss and the result reports `droppedComments`. `check:arraypatch` 33 → 40 checks, the corpus half a property rather than a pinned count. Also corrected §DX-02iv: `removeFns` wrote the closure's trailing whitespace into the `{__fn}` marker while the serializer trimmed it, so *byte-identical* had only held for closures that had none |
| 36 | 2026-09-06 | §DX-02iv | `7999bee` | The write path could destroy a function value and could not author one; now it round-trips, and the destruction is refused. |
| 37 | 2026-08-26 | §DX-02ib | `3290e48` | A retry loop that hid a permanent failure, permanently. |
| 38 | 2026-08-25 | §DX-02hd | `ab9e427` | the last symptom of the two-shapes blindness, and there were two readers, not one. The row said `GET /api/monster/:key/drop` logs `undefined  ·  0gp` for the **13** weighted-array drops. **True, and o |
| 39 | 2026-08-23 | §DOC-02da | — | the report whose architecture survived 85 days and a 3.15× file intact, whose operations were reversed four ways by a single track, and whose documented safety net turns… |
| 40 | 2026-08-18 | §DOC-02ca | `d4a31d7` | four of five recommendations shipped inside 28 days, every derived number is wrong, and the fifth recommendation has a 138-quest bill the CI gate prints and passes |
| 41 | 2026-08-17 | §DOC-02bx | — | seven locked decisions, four non-goals, forty-two days, and the only thing that drifted was the labels |
| 42 | 2026-08-17 | §DOC-02bw | — | every increment it planned shipped in four days, every constant and formula still measures exact, and the two mechanisms it named to protect its invariants were never… |
| 43 | 2026-08-17 | §DOC-02bv | — | the corpus's best-verified report, and the only one whose entire forward ladder was walked in four days — including the rung that retired its own safety sentence |
| 44 | 2026-08-17 | §DOC-02bu | `1e5de0d` | a method document scored against the corpus it produced — the tables are perfect, the census is wrong by 324, and one commit hash points at the wrong wave while… |
| 45 | 2026-08-17 | §DOC-02bs | — | the report was exact on every number it counted, and its subject was deleted six days after it closed |
| 46 | 2026-08-17 | §DOC-02br | `4a97e50` | a design lock that scored twelve of twelve and had its runtime three minutes later — and the one button its integration section never listed was the only one that left… |
| 47 | 2026-08-17 | §DOC-02bq | — | the design was right, the encoding was inert, and the engine had been demonstrating the correct form several hundred times in the same file |
| 48 | 2026-08-17 | §DOC-02bp | — | the miscount was fixed inside a day, in a gate, and four documents were never told |
| 49 | 2026-08-14 | §DOC-02bo | — | the report's own census counts a sentence of prose as a field, and the patcher it designs is immune to exactly that mistake |
| 50 | 2026-08-13 | §DOC-02aq | — | the most accurate document the program has measured, dated to twenty-two minutes by a file inside its own commit — and its one table that does not add up was reporting a… |
| 51 | 2026-08-13 | §DOC-02ap | — | the tightest dating result in the corpus, a reference manual whose invented examples are the healthy part, and a retrofit that laundered a dead mechanism |
| 52 | 2026-08-12 | §DOC-02ah | `c76b658` | the panel whose whole premise is being exact shipped a line count that was already 1,158 lines stale, and the wizard it argued against was built anyway |
| 53 | 2026-08-12 | §DOC-02ag | `d81a78e` | the document that turned quest content from code into data, and the one section of it that describes something that already existed |
| 54 | 2026-08-06 | §RESUME + §DOC-02bw | — | `check:anchors` **3,301 → 3,338 across 81 → 82 docs, +37 — 25 in the report, 5 in the three new rows and 7 in this §RESUME entry** (the entry's seven were counted… |
| 55 | 2026-08-04 | §DX-02l-FU + §AUDIT-03d | `3684b67` | the assertion loops over four docs and `expect` aborts at the first failure, so it only ever named `prompt.md:197` — `CONTRIBUTING.md:145`, Hazard #7 itself, is a… |
| 56 | 2026-08-03 | §DX-02l + §AUDIT-03d | `91d3650` | `GET /api/snapshots` (dir, per-file size/mtime, total, and whether the patch chain already holds each one) and `DELETE /api/snapshots`, nonce-guarded like every other… |
| 57 | 2026-08-03 | §DX-02k + §AUDIT-03d | `2ead185` | `save(dest)` writes exactly where told and **refuses with no argument**, naming the alternative in the error text so the next author cannot just pass `getStampedName()`… |
| 58 | 2026-08-03 | §DX-02h + §DX-02i | `a047348` | `world.md` stated in the present tense that both were *"injected into terrain encounter tables at runtime when `voidPressure ≥ 6` via `_applyVoidPressureMonsters()`"* —… |
| 59 | 2026-07-30 | §DX-01c + §AUDIT-03d | `44b67d8` | `WBAPI._sectionCloseIdx` (stops at the first nested START; `insertBeforeSectionClose` delegates, so all 12 create call-sites are covered and the class cannot recur);… |
| 60 | 2026-07-28 | §AUDIT-03f + §ARCH-01 | `6a69087` | it processed comments as code — the §ARCH-01 Wave-1m/1n migration comments' arrow-fn examples (`activateCond:()=>true`, `completeFn:()=>…`) triggered the fn-stripper,… |
| 61 | 2026-07-06 | §6 | `c78eb4d` | instrument 84 fired at scale.** The report has **fourteen** commits: locked by `c78eb4d` and amended thirteen times as increments shipped, the last four days later. Its… |
| 62 | 2026-07-03 | §NAV-01 + §DX-02e | — | expected `docs/lab-reports/lab-report-nav01-navigable-world.md` (`2026-07-03 12:47:32`), §NAV-01, the ROAD_RUNS net / auto-travel / MUD-parity redesign. **It is the… |
| 63 | 2026-06-30 | §1 + §ARCH-01 | `ab996f1` | the header's *"all 284 QUEST_DB entries"* survived after §1 voided that survey; epics are **Wave 5** in the sequencing rules and risk table but **Wave 6** in the wave… |

**63 completed entries.**

---

## Phase 6 — Verification & Documentation

| # | Date | Increment | Ship | Headline |
|---:|---|---|---|---|
| 1 | 2026-09-25 | §DX-02ig | `e8cbd52` | gate #35 `check:doublepay`: 58 button→quest pairs, 1 double pay (Pip, filed §DX-02lh); finds §DX-02ai's hull on `19afc7a^`; unit = innermost block, and numeric `reward` is not a payment |
| 2 | 2026-09-25 | §DX-02ih | `805e5a0` | 87 closes (2026-08-04 → 09-14) backfilled into the history file from their chronology rows with archive- or git-derived SHAs; gate #20 asserts a history line per close and `Closed` cell = table rows (all six read low; now 36/116/69/23/63/57) |
| 3 | 2026-09-25 | §DX-02ja | `7aba0e8` | the CI `paths:` filter is removed, not widened: 11 of 34 gates read inputs outside `src/` (docs, root docs, Makefile, run.sh, edit.html, bin/) and no doc path was listed. CI green on `7aba0e8` |
| 4 | 2026-09-25 | §DX-02jh | `24a484a` | gate #20 now asserts each history section's *completed entries* label and each phase file's *closed increments* line against the table; all 12 read LOW and were corrected. The `Closed` cell is the same population since `6726358` and waits on §DX-02ih's backfill |
| 5 | 2026-09-25 | §DX-02jl | `a2c667b` | gate #34 `check:gaterows`: every `GATES` entry needs an index row, and no gate row restates a count its script prints. The census found 10 of 33 gates without a row and 4 of 16 quoted counts drifted; counts were replaced by commands and ten rows were added |
| 6 | 2026-09-25 | §DX-02jt | `f131eb2` | 18 engine comments reworded `CY` → `HKG` after checking each one's code; `\bCY\b` 21 → 3 (two kept history records, one comment in `QUEST_DB`); the identifiers, the save flag and the data-section comment filed as §DX-02lg |
| 7 | 2026-09-25 | §DX-02jx | `51396d0` | gate #33 `check:prosecodes`: adjacency rule over 13,388 player-facing strings, bit text and titles included, roman numerals skipped; the doc scanner's lists thin only the naive count, because they call `AC` armor class. 3 allowlisted hits; the replanted `HKG→AC` is caught by quest, field and token |
| 8 | 2026-09-25 | §DX-02kz | `8655ccf` | gate #30 checks the `./bin/api` verb against `wb.js`'s `CMD` (50) and `RETIRED`, parsing flags and quotes as the CLI does; 0 findings at HEAD, exactly 12 over the `0445d74` API docs; selftest 18 → 24 |
| 9 | 2026-09-14 | §DX-02kl | `887d4ac` | the mesh flake was §DX-02ia's fix working: the offline line is written by the first poll's rejection handler, which the freeze gates, and `openMeshTab()` froze immediately after the click — so ~4 % of runs never rendered anything and failed at `:71` on *unreachable*, not at `:74` on the fixture as the row recorded. `{awaitOffline}` waits for the line before freezing in the one test that asserts it; **2/50 flaky → 50/50**, 120/120 for the file. A second test pins the unfrozen behaviour, checked red |\n| 6 | 2026-09-07 | §DX-02iw + §DX-02cr | `ea0f156` | four `check:*` scripts were defined in `package.json` and absent from `GATES`, so `check:walk` printed 22/22 over a chain 26 scripts wide. All four are in `GATES` and in `check:walk:serial` now, and `run-gates.js` refuses to start when a `check:*` script is in neither `GATES` nor the named `GATE_EXEMPT` map, or when the serial chain stops mirroring `GATES` — selftest 4 checks → 7, both proved by planting. The row said the four were run by nobody; CI disproved that for three, and `check:duelparity` — the third parity-fenced kernel — was the one run by nothing at all, which is §DX-02cr, closed here with `src/js/duel.js` into both `paths:` filters. The four bespoke CI steps collapse into `check:walk`. 22/22 wall 25.6 s → **26/26 wall 21.1 s** |
| 10 | 2026-09-07 | §DX-02iw + §DX-02cr | `ea0f156` | Four gates outside the wall, and the fence that keeps the next one in. |
| 11 | 2026-08-26 | §DOCPTR-01 | `45cbc6f` | The code→doc direction had no gate, and it had already rotted fourteen ways. |
| 12 | 2026-08-25 | §DX-02fz | `8ca6607` | `check:walk` hung at gate 4 **after printing gate 4's ✓**, intermittently, across three sessions, with three causes proposed and each disproved by the next measurement — the gate's logic, a live WBAPI |
| 13 | 2026-08-25 | §DX-02gi | `4ad8218` | `CONTRIBUTING.md` said *"run `say "<commit subject line>"` … the **subject line only**, via macOS `say`"*; `resume.md` §2.6 and §0 say `src/bin/say.sh`, **never raw macOS `say`**. Both files are bindi |
| 14 | 2026-08-25 | §DX-02gj | `37ff9ee` | `seedAndLoad` + `dismissContinue` was not a neutral starting state — `dismissContinue` reaches `storyRender`, which renders NPC cards, so any state a render *writes* is advanced by one before the test |
| 15 | 2026-08-25 | §DX-02gs | `78fbc21` | the routing table's **Open rows** column was maintained by hand **inside the file it counts**, and the row records five prior drifts. **The sixth was measured at HEAD and it had drifted in both column |
| 16 | 2026-08-25 | §DX-02gu | `56df66e` | the clean-tree invariant — a full `npm test` leaves the tree naming only the increment's own edits — had been restored **by hand three times** (§DX-02gm's 2,883-hint doc sweep, §DX-02gn's screenshot o |
| 17 | 2026-08-25 | §DX-02gx | `4a5e488` | `check:battlepools` direction 2 tested `tier:'deadly'` while 48 monsters at other tiers met the identical condition unremarked, so its exemption list read as a defect ledger. **Re-deriving moved the n |
| 18 | 2026-08-25 | §DX-02hx | `b5cf877` | NOT A DEFECT — my own row, disproved by reading the quest the gate is attached to. |
| 19 | 2026-08-25 | §DX-02hg | `932d0cd` | `world.md`'s Layer 59 heading read **⚠️ PLANNED** for a layer that is mostly live — the fifth such heading after §DOC-02ar, §DOC-02bz and §AUDIT-03ak's four. **The ground extended the row.** It measur |
| 20 | 2026-08-25 | §DX-02hh | `1c53cab` | `spec-engine.md §Gate Locks` said the locks are *"checked inside `cellMove` using `destCode`… identical to the old `storyMove` checks"*. `cellMove` is a thin caller over `Mover.move` with **no gate br |
| 21 | 2026-08-25 | §DX-02hw | `ec14c5d` | a doc could not **quote** a dead anchor: in a code span the quotation is an anchor, so §DX-02hv's own closing commit went red on **7** of them — `ANCHOR_RE` cannot tell a ship record from a live point |
| 22 | 2026-08-25 | §DX-02hv | `e1ca994` | the gate chain and the suite were **already red at HEAD** — `check:walk` 17/18, `npm test` 1031 passed/**2 failed** — on one dead anchor, `` *`edit.html`@5881* ``, found while verifying §DX-02he and c |
| 23 | 2026-08-25 | §DX-02he | `aa61187` | `check-battlepools`'s green line said **`3 exemptions, each owned by an open row`** and one of the three is owned by design, not by a row — `_bruhns`, whose table value is the sentence *"by design — t |
| 24 | 2026-08-25 | §DX-02hu | `78821cb` | the editor specs were racing a world load nobody had asked for. The row said *"wait for `#load-splash` to be hidden"*; a probe spec logging the loader's real timeline on an idle machine says the splas |
| 25 | 2026-08-25 | §DX-02ht | `1995538` | evidence for a flake without paying for it on every run — and it diagnosed three on its first outing. **The row's own plan was measured and rejected:** promoting `retain-on-failure` suite-wide took th |
| 26 | 2026-08-25 | §DX-02hf | `e5ce2c8` | a header said five, the collection held seven — and the shipped dialogue had been saying *"all seven"* the whole time. `SHARD_NOTES@27293`'s `→ doc:` pointer read *"(5 shard_note readable items; one p |
| 27 | 2026-08-25 | §DX-02hs | `a75e703` | `check:spdx`, gate #18 — the licence header now has something behind it. Filed one row earlier, when §DX-02hn's sweep of all 136 tracked JS files established that **nothing would have caught either si |
| 28 | 2026-08-25 | §DX-02hn | `d0d17ab` | the licence header that spent two days on line 2. `multiplayer-presence.test.js` line 1 was a stray `const path = require('path');` sitting **above** `SPDX-License-Identifier` and `'use strict'` — the |
| 29 | 2026-08-25 | §DX-02hp | `2d186cd` | the Count column was remembered, not measured. The row named **one** wrong cell in `design/index.md`'s test table (`multiplayer-presence` said **2 tests**, the file has **7**); following its own *"che |
| 30 | 2026-08-25 | §DX-02hq | `b0c236d` | the suite was seven minutes because a 303-test file was one serial chain. Raised by the author mid-session — *"i want the tests to go quicker"* — and Playwright had been printing the answer at the bot |
| 31 | 2026-08-25 | §DX-02hm | `467dc7f` | one port pair per worker: the presence spec could not be run twice at once. The row measured `--repeat-each=4` at `378f95f` as **`17 passed, EXIT=0`** where 28 were expected; **re-measured at `3c0e9f7 |
| 32 | 2026-08-25 | §DX-02hb | `cb5e23a` | the flake that was never diagnosed, because the suite threw the evidence away. The row said the failure *"is reproducing more often than not"* at HEAD; **two consecutive full runs returned `1033 passe |
| 33 | 2026-08-25 | §DX-02hi | `644b747` | the check-in process had three faults, and the row named a fourth that was not real. **The filed cause — a stale server holding 1367 — is wrong:** no gate in the chain opens a socket or spawns anythin |
| 34 | 2026-08-24 | §DX-02gn + §DX-02eu | `41ffaae` | the suite's screenshot overwrote a tracked file, and the fix was to move the write rather than untrack the file. `living-world-l44.spec.js:210` wrote to… |
| 35 | 2026-08-24 | §DX-02gt + §AUDIT-03bj | `a72688a` | the suite was red at HEAD and the gate chain was green, because they do not test the same thing. `npm test --prefix src` measured **2 failed / 1016 passed**: Waves 2q… |
| 36 | 2026-08-24 | §DX-02bs + §PAUL-01 | `b152350` | the soft-lock gate learns the one writer idiom it could not see, and the row's own count of its false positives was one short. `scanFlagWrites` folded five host writer… |
| 37 | 2026-08-24 | §AUDIT-03bh-FU + §DX-02gt | `8b67832` | the search the hint named already existed, and it set no flag. `quest_tl_01` (*Rennau: The Ledger*) waited on `tlLedgerRead`, written only by its own `onComplete` — 0… |
| 38 | 2026-08-24 | §DX-02gm + §DX-02fz | `d0dfd2d` | the anchor gate's own acceptance test writes only inside its own scope now, and removing its sweep exposed the two assertions the sweep was satisfying.… |
| 39 | 2026-08-23 | §DOC-METHOD | — | the resumption method written down, and the corpus measured against the window that has to read it |
| 40 | 2026-08-23 | §RELEASE-01 | — | §RELEASE-01 ✅ the infrastructure record landed, and the remedy it prescribes is the one thing this repo does not do |
| 41 | 2026-08-23 | §DX-01g + §DX-02 | — | `BACKLOG.md` split into six phase backlogs by subsystem; the 2.2 MB single file is now a 37 KB routing index.** On *"read backlog. its big. we need to split it."* The… |
| 42 | 2026-08-14 | §DOC-02bm | — | two hours from spec to closed series, twelve of twelve pointers exact, and the one assertion that is red today is the one it wrote to close the series |
| 43 | 2026-08-14 | §DOC-02bi | — | every filename it catalogued is real, and it counted sixty-five of seventy-one |
| 44 | 2026-08-12 | §DOC-02ak | `7add1d7` | a verification pass tests what it CHANGES and assumes what it LEAVES |
| 45 | 2026-08-12 | §DOC-02x | — | the report that wrote the repo's operating manual, audited by the program it recommended |
| 46 | 2026-08-11 | §DOC-02i | `a123904` | the report that designed the documentation system was right about every number it measured and wrong about the one it cited — and the instrument it recommended is now… |
| 47 | 2026-08-11 | §DOC-02b + §DOC-02 | `d5b1cb9` | the same uncommitted user recolor is still in the tree (HP bar gradient / light-green playfield / theme-ink die numerals, self-labelled *"user recolor 2026-08-06"*) —… |
| 48 | 2026-08-04 | §AUDIT-03q + §AUDIT-03d | `28c6f8a` | phase 2 in `src/scripts/legacy-codes.js` (+`eachProseLine`/`localCue` extracted so both phases share one scanner), `BORN_DEAD` + `NOT_A_NODE_CODE` explicit tables with a… |
| 49 | 2026-08-04 | §AUDIT-03m-FU + §AUDIT-03d | `79987d3` | `story-arc-ngplus.md` annotated **8** references and was left carrying **11** — because `nodeContextLine` is a **LINE** test, and *"Write Entry 42 at CI"*, the commonest… |
| 50 | 2026-08-04 | §AUDIT-03m + §AUDIT-03d | `b399318` | `src/scripts/legacy-codes.js` / `npm run legacy`, which reads the LEGACY CODE MAP out of the **generated** node index (never re-typed) and counts a token only in a node… |
| 51 | 2026-08-03 | §DX-01e-FU + §AUDIT-03d | `f4bc624` | `21722`, cited for `execBits`, is now a Mother-Mimic quest body; `25558`, cited for `BOSS_COMMANDER_AUROS`, an `sbo-refocus-bar` style write; `26822` is past the end of… |
| 52 | 2026-07-31 | §DX-01e + §AUDIT-03d | `57d79d7` | the **symbol** is the pointer (any literal substring of the line meant, optionally file-qualified — `` `src/js/wbapi-server.js:function seededNext@1147` ``), the… |
| 53 | 2026-07-30 | §DX-01d + §DX-01i | `e4c03a0` | `WBAPI.deleteEntrySource(section,key)`, one source-level excision for all four types with **verify-or-revert** (the section's depth-1 key *multiset* must lose exactly… |
| 54 | 2026-07-30 | §DX-02f + §7 | `dfeeff4` | `effort-xp` forces the fail through the engine's own dial via a scoped `forceFail(id, fn)` that raises `sc.dc` to 999 and restores in `finally` — the same idiom… |
| 55 | 2026-07-28 | §AUDIT-03a + §DX-01a | — | `check:dupkeys` is gate #11 of `check:walk`; the last-key-wins rot class is now CI-fenced.** Scanner details + the 2 corpus survivors… |
| 56 | 2026-07-28 | §DX-01a + §JUNK-01 | `7f75c54` | J14/J15 deleted (hand-edit per Hazard #4, server confirmed stopped; zero other live refs by repo grep), then `build-roads.js --apply` (the canonical do-not-hand-edit… |
| 57 | 2026-07-28 | §DX-01f + §RESUME-tail | `c9f3946` | 276 lines of closed content moved **verbatim** (scripted line-range surgery — no paraphrase, boundaries asserted before cutting) to plan-archive.md §"Archived 2026-07-28… |
| 58 | 2026-06-26 | §DOC-02bk | `7f4b73c` | `7f4b73c` 11:41 → `95c4143` 13:33 on 2026-06-26, one hour fifty-two minutes for spec + 4 increments + an unplanned follow-up.** (§DOC-02bk's "under 20 hours" was the… |
| 59 | 2026-05-25 | §DX-01e + §DOC-02 | — | FC01 ✅ · FC03 ✅ (`docs/mechanics/mechanics-combat.md` + `-economy.md`) · **FC05 half-shipped** — 93 `// → doc:` pointers live, 48 top-level CAPS consts still without… |

**59 completed entries.**

---

*© 2026 Paul Richeson — MIT License.*
