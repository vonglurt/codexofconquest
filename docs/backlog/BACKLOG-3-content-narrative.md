<!-- SPDX-License-Identifier: MIT — Copyright (c) 2026 Paul Richeson -->
# BACKLOG — Phase 3: Content & Narrative — authored text, NPCs, arcs, endings as writing

> **Phase 3 of 6.** Split out of `BACKLOG.md` on 2026-08-23; `BACKLOG.md` is now the routing index and holds the chronological §RESUME map across all six files.
>
> **Scope of this file.** Authored strings and the people who say them: NPC dialogue and relationships, favor scales, arc beats, epilogue and ending prose, lines with no lookup path.
>
> **Siblings:** [1. Playable Truth](BACKLOG-1-playable-truth.md) · [2. Engine & Systems](BACKLOG-2-engine-systems.md) · **3. Content & Narrative** *(this file)* · [4. World & Navigation](BACKLOG-4-world-navigation.md) · [5. Platform & Tooling](BACKLOG-5-platform-tooling.md) · [6. Verification & Documentation](BACKLOG-6-verification-docs.md)

## The Backlog Directive — restated in full

> **This file is the list of outstanding activities for Phase 3 (Content & Narrative).** The *how-we-work* rules live in **[CONTRIBUTING.md](../../CONTRIBUTING.md)**; closed/shipped work is archived in **[plan-archive.md](plan-archive.md)**. Split out of the former `plan.md` on 2026-07-09.
>
> **⚠️ Before starting ANY row — Existing-Work-First (CONTRIBUTING.md § Existing-Work-First Policy):** `git status` + `git log` first (uncommitted/fresh work matching the row = it's done or in progress — finish it, don't redo it); check for a second live session (`ps aux | grep claude`) before editing shared files; grep to *disprove* the row's premise (rows go stale — four rows have closed as ALREADY SHIPPED). A §RESUME entry says where the last session *stopped*, not what the tree looks like *now*.
>
> **Legend:** `[ ]` open · `[~]` in-progress / recurring · `[x]` closed (kept here for context; migrate to plan-archive.md over time).

> **⚠️ This file is one of six.** A row's neighbours may live in a sibling phase file, and the Existing-Work-First grep MUST cover all six — `grep -n '§DX-02xx' BACKLOG*.md` — before you conclude a row is unclaimed. The §RESUME entry that closed a row may sit in a different phase file than the row itself.

## Standing Directive — Code Comments (user directive 2026-08-23)

> **Binding on every increment in this file.** Where this and a §-row's own instructions disagree, this wins.

**The failure this prevents:** writing a comment that narrates the change being made right now. That context is real, but it expires the instant the change merges — the defect it describes no longer exists, so the comment becomes a story about a problem no future reader can observe. It is a changelog entry in the wrong file, and a third copy of text already required in the commit body and the PR description.

- **CC-1 (MUST NOT)** Write a comment describing a change, a fix, a defect, its cause, or what the code used to do. No *"was / now / previously / instead of"*, no *"this fixes"*, no *"needed because otherwise"*, no *"note that we no longer"*.
- **CC-2 (MUST)** Apply the **survival test** to every comment before writing it: *would this still be true and useful to someone reading this file a year from now, who never saw the diff?* If it only makes sense beside the diff, it is changelog — delete it and put it in the commit body.
- **CC-3 (MUST)** Default to **zero** comments. Declarative config — Terraform, DNS records, k8s manifests, CI YAML, Helm values — is self-describing and takes none. A resource named `dmarc-example-com` does not need a comment saying it is the DMARC record.
- **CC-4 (MAY)** Comment only when a future editor would actively break something without it: a non-obvious external constraint, a required out-of-band manual step, an invariant the surrounding code cannot show. **One line.** If it needs a paragraph it belongs in a design doc, not inline.
- **CC-5 (MUST)** Before every commit, re-read the comment lines added: `git diff --cached | grep '^+' | grep -E '#|//|/\*'`. Each hit must pass CC-2 on its own. **Deleting is always an acceptable outcome.** *"I already wrote it"*, *"it is only one line"*, and *"this one is genuinely useful"* are not exemptions — the last one is the exact thought that precedes every violation.
- **CC-6 (MUST)** Applies to comments **edited** as well as added. When a change invalidates an existing comment, the default action is **DELETE**, not rewrite it into a new narrative.

> **⚠️ KNOWN TENSION, flagged not resolved — needs a user call.** This repo currently runs an *opposite* convention in `play.html`: **§AUDIT-03n / §DX-02c "annotate, don't rewrite"**, which deliberately preserves defect history inline (e.g. `const NODE_NPC_KEYS`'s 8-line comment explaining that 21 of 26 rows named retired node codes, or `_renderFinalMap`'s §AUDIT-03e note on why the node filter is held at the historical set). Those are exactly the *"what the code used to do"* narratives CC-1 forbids, and several are load-bearing: they are the only record of why a value is held where it is, and deleting them would re-open closed audit rows. **Until the call is made, the working rule is:** CC-1..CC-6 bind all **new** comments; existing §AUDIT-/§DX-annotated blocks are **not** swept under CC-6 — they are a documented archaeology convention, not accidental changelog. Filed as **§DX-02fa** below.

---

## Open at a Glance — Phase 3 tracks

- **`[~]` §IDEA-01 — Idea Generator** — a recurring process (found-text → quest-theme seeds), not scoped work. See [Game Content](#game-content--major-planned-arcs).

---

## §BACKLOG — Open Items (Phase 3)

### §NPC-01 — The Derivable NPC Card Map (✅ FULLY CLOSED 2026-07-28; promoted §POT-R2)

> *Full closed section (finding · locked design · rows A–D + SF1–6 with all ship records) moved verbatim → plan-archive.md §"Archived 2026-07-28 — §DX-01f". Summary: A lean-profile fallback `76ad683` · B derived render map `33aa15f` (~20 → ~203 card-bearing NPCs) · C ⚔ enemy footer `696539e` · D 💬 Talk verb `4acafa0`/`df990c3` · SF1–SF6 incl. the CQ/SQ/GC→CDG/NUE/TRD remap `8973fd7`; `npc-card-map.test.js` 22/22. **Unblocks §POT-C2 / §POT-P3 / §POT-W1.***

### §GR-FU — the La Riva arc raises Kenickie to Dear Friend and the ending cannot name him (NEW 2026-08-12 during §DOC-02s, 🟢 no design call)

- [ ] **§GR-FU — the arc's own stated payoff is a Covenant Ceremony naming line that was deferred in 2026-05-26 and never written.** `{ kind:'favor', npc:'kenickie', set:3 }@34414` promotes Kenickie past the `>= 2` dearFriend threshold at the moment he receives the Old Tuna Account Book — the arc's final beat. `const SWEELINCK_NAMING_LINES = {@27237` (*"per-NPC fav-gated witness lines"*) holds exactly six keys — `yael · brynn · quill · pachelbel · crov · auros` — and **Kenickie is not among them.**
> **Why this is the arc's thesis and not a nicety.** `story.md §GRIEF AND CORRUPTION` Act V states the purpose in one sentence: *"The Covenant Keeper ending names each person helped by name. This is why. Not to celebrate them. To confirm that the things they were carrying were seen."* The whole five-act structure resolves on **witnessing** rather than restoration — the Row deliberately does not rebuild — so the naming line is the only mechanical acknowledgment the arc was designed to produce, and it is the one thing that did not ship.
> **The lab report filed it correctly as deferred, with a suggested text** (*"Kenickie, who kept the account book. Who knows every name in it."*, **0 occurrences**), which is what makes it a clean **NOT SHIPPED** rather than an oversight found late. 78 days.
> **Compounded by §ENDING-01:** the Covenant Keeper ending is itself currently unreachable (curse-score floor of 20), so the missing line sits behind a screen no player can reach. **Fixing §EPIC-01 → §ENDING-01 surfaces this defect rather than resolving it** — worth writing the line in the same pass, since it is one `SWEELINCK_NAMING_LINES` entry with the fav tiers the arc already sets.
> **Check the other five while you are in there:** every key in the registry is a Birka-curated NPC, and the arc-granted `dearFriend` NPCs outside that set (`kenickie`, and `aldo_sardino` at friendly) are structurally invisible to the ceremony.

### §DX-02x — one arc promotes one NPC through the host and the other by a raw ledger write (NEW 2026-08-12 during §DOC-02s, 🟢 no design call)

- [ ] **§DX-02x — `S_story.npcFavorability['aldo_sardino'] = (S_story.npcFavorability['aldo_sardino'] || 0) + 1;@31907` bypasses `function _setNpcFavor(key, level) {@23462`.** The helper is the canonical setter: **absolute, only ever raises, and emits the tier-change line**. The raw `+ 1` is **relative** (so it can only be correct while the field is known-zero), silent (no ✨ Friendly line — Aldo is promoted with no player-visible acknowledgment), and unguarded by the helper's `if (level <= prev) return;`.
> **The tell is that the SAME ARC does it correctly one node away:** the CDG delivery verb uses the VM opcode `{ kind:'favor', npc:'kenickie', set:3 }@34414`, and its own comment cites `_setNpcFavor` as *"ABSOLUTE, only ever raises, and emits no line at level 3"*. One arc, two nodes, two mechanisms. The §DX-02t class — a path that is right on every line it duplicates and wrong in what it omits.
> **Fix:** replace the raw write with `_setNpcFavor('aldo_sardino', 1)`, or re-express the whole AMS promotion as a `favor` bit (invariant #4 — widen through the grammar). **Then sweep:** census every direct `S_story.npcFavorability[…] =` assignment outside `_setNpcFavor` and decide per site; a `check:deadconsts`-adjacent phase could assert the helper is the only writer, which is exactly the *writers-and-readers-separately* shape §DX-02u already argues for.

### §DX-02cv — six nodes show the player a named NPC and disable the button that would let them speak (NEW 2026-08-17 during §DOC-02by, 🟡 small design call: write the lines, or drop the chip)

- [ ] **§DX-02cv — `node.npc` renders unconditionally; the Talk verb is gated on a different table, and six nodes are in one and not the other.** 🟡 **six short dialogue entries, or six field deletions — the call is which.** The render side is unconditional: `if (node.npc) {@35817` emits the locale chip `'🧙 ' + node.npc`. The interaction side is gated on the **node-code-keyed** table, not on the npc name — `!(node.npc && NPC_DIALOGUE[node.code])@35924` sets the D-pad button's `disabled`, and the click handler repeats the same test before calling `storyShowNpc`. There is exactly one Talk entry point, so a node in the first set and not the second renders a person and refuses to let the player address them. **Measured through `src/js/wbapi-core.js` (the project's own parser, not a line regex): 71 of 416 nodes carry an `npc` field; 68 codes are keys in `NPC_DIALOGUE`; six carry a chip with no entry** — `MOL` Dock Boatman (Lake Approach — West Shore) · `KSU` Harbor Guild Master (The Lake Harbor) · `VBY` Warlord Kael Mordus (Broken Tooth Tavern) · `AMS` Connie Tuna (Fishmonger's Row) · `EHZ` Johannes von Weisheit (Event Horizon — Math Station) · `CNTR` Cantor's Shade (Cantor's Attic). **The six are not equivalent and the fix is not uniform.** Two are §MATH-01's, and both are *speaking characters in their own node text*: `npc:"Johannes von Weisheit"@9383` sits at the console and delivers four paragraphs, ending *"The Hamadani Failure Record is on the desk. It is a failure only technically"* — and the arc's five quests all name `johannes_von_weisheit` as their `npc` anchor, so the authoring layer thinks he is a character while the play layer offers no way to reach him. Two more are arc principals with dialogue *elsewhere*: `VBY`'s Mordus and `AMS`'s Connie Tuna both have `NPC_DIALOGUES` profiles keyed by npc key, which the node-keyed Talk table cannot see — that is the §AUDIT-03n key/heading split showing up one layer down. The remaining two (`MOL`, `KSU`) are functional roles that may simply not need lines. **Why it is worth fixing rather than ignoring:** the chip is a promise. Every other node that shows 🧙 lets the player press Talk, so a dead button reads as a bug, not as an absence — and §PLAY-01's standing finding for this game is exactly *the engine knows things it will not transmit*. **Three options:** (a) **author six `NPC_DIALOGUE` entries** — smallest surface, and for EHZ/CNTR the text already exists in the node body and only needs splitting into a dialogue node; (b) **route the Talk gate through the npc key as a fallback** — `NPC_DIALOGUE[node.code] || NPC_DIALOGUES[keyFor(node.npc)]` — which fixes `VBY`/`AMS` for free and is the structurally right answer, but needs a name→key resolver and touches every node; (c) **drop the `npc` field** on the nodes that will never have lines. **Recommend (a) for `EHZ`/`CNTR`/`MOL`/`KSU` and (b) for `VBY`/`AMS`**, since the second pair's problem is a lookup, not a content gap. **Risk:** (b) can surface a profile written for a different scene — verify per node before wiring. **Verify:** a census assertion that every `NODE_MAP` entry with an `npc` field resolves to a reachable Talk surface; `check:npcregs` stays green.
> **Provenance, and it is the sharper half.** The §MATH-01 lab report's design decision D3 reads *"No `itemChain` — the documents are keepsakes; no recipient NPC exists ('Collect it' is the whole contract)."* **`npc:"Johannes von Weisheit"` was already on the EHZ node at that report's own design-lock build `32d7bb0^`.** The decision that followed — no delivery step, no recipient, no chain — may still be right, but it was reasoned from a fact that a single grep would have contradicted. ***A design decision inherits the confidence of its weakest premise, and a premise phrased as "no X exists" is a census: take it before you write the sentence.***

### §AUDIT-03bk — the arrival-completes-the-quest closure is the game's best interaction and 15 of 161 waypoint quests use it (NEW 2026-08-18 during §DOC-02ca, 🟢 content gap, no design call)

- [ ] **§AUDIT-03bk — the mechanism is shipped, correct and tested; the content did not follow it.** 🟢 **authoring, not engineering.** A quest publishes a destination through `waypointNode`; `const distTag = _wpDistTag(q.waypointNode);@30751` renders **Navigate →** with a live *"(12 steps, NE)"* tag; the auto-travel loop walks the road; and if — and only if — the quest's completion gate carries the `atNode` term (`if (g.atNode && st.currentCode !== g.atNode) return false;@22124`) the final `cellMove` completes it on arrival, fires its `onComplete` bits, and may open the next quest's gate **on the same frame**. *Arriving is completing.* **Measured: 161 quests carry a `waypointNode` at HEAD, 19 quests use `completion.atNode`, and only 15 have both — 9 %.** At the 2026-07-07 build it was 12 of 150, 8 %.
> **Why it is worth a row rather than a shrug.** This is the single loop that makes a 2,853-quest world navigable without a checklist: one click, the character walks, the journal ticks over by itself. The other 146 waypoint quests send the player somewhere and then make them work out for themselves that they have arrived — the §PLAY-01 finding for this game in miniature, *the engine knows things it will not transmit*. It is also the cheapest possible content win in the repo: the engine change is **zero**, and each conversion is one `atNode` key added to an existing `completion` clause through `./api.sh`.
> **Fix:** audit the 146 for the ones whose `hint` already says *"go to X"* and add `atNode:'X'` to their completion gate; leave alone the ones whose waypoint is a *battle* destination (the Battle button path) or a multi-stop errand. **Do not blanket-apply** — a quest that completes on arrival at a node the player passes through incidentally will fire early, which is the failure mode `atNode` exists to prevent in the other direction. **Verify:** a Playwright pin per converted quest, the §MATH-01 shape (activate at A, hold at B, complete at C); `check:questgraph` unchanged.

### §DX-02da — the §KG corridor's two skill checks ship `onFail:[]`, so the arc's own lesson about losing cleanly has no failure text (NEW 2026-08-18 during §DOC-02cb, 🟢 authoring, no design call)

- [ ] **§DX-02da — a failed corridor skill check is silent.** 🟢 **two `onFail` arrays.** `quest_kg_04` (WIS/Insight DC 10, *"Lose Cleanly"*) and `quest_kg_11` (INT/Investigation DC 12, *"Why the Line Went Quiet"*) both carry a populated `onPass` and an **empty `onFail:[]`**; both are `retryable:true, retryGateDays:0`, so the player may re-roll immediately and nothing whatsoever is said when they miss. The lab report specified `onFail:[…]`. Both quests already have authored `failText` sitting unused beside the bit.
> **Why it is worth the ten minutes.** Roshkova's line is the arc's thesis — *"a cadet who cannot lose cleanly cannot win cleanly"* — and `quest_kg_04` is literally the quest that teaches it. The one beat in the corridor that is *about* failing well is the one that renders nothing on failure. A `narrative` bit carrying the existing `failText` is the whole fix; no new prose required.

### §DX-02fd — Yael's patrol is a priority list pretending to be a schedule (NEW 2026-08-23 during §DOC-02cz, 🟡 reorder + ONE DESIGN CALL)

- [ ] **§DX-02fd(a) — first-match-wins with the loosest condition declared first.** 🟡 `function _getYaelLocation()@27660` is `for (const p of YAEL_PATROL_NODES) { if (p.condition()) return p; }`, and `const YAEL_PATROL_NODES = [@27728` declares `(S_story.gameDay || 0) % 2 === 1` **first** — true half the time, dependent on nothing. **Measured with every rival condition simultaneously satisfied** (slums cleanup complete, escort used, named report delivered, yael favor 3, act 5): odd game day → `MSY` / *"Eastern check. You're traveling late."* every time; even day → falls through to `BMA`. **So the Ghetto line, the escort line, *"Checking on Quill. Don't tell him."* and Layer 74's second-report line are reachable only on even game days, one at a time, in declaration order.** The four were written as independent facts about Yael's week; the loop reads them as a ranking. **Fix: move the parity entry last.** 🟢 once the call in (b) is made.
- [ ] **§DX-02fd(b) — Yael is never actually away from home.** 🟡 `function _nodeHookBirkaYaelPatrolLine(node,@32518` runs `if (node.code !== 'LHR')` and only **adds** a field line elsewhere; nothing suppresses her card at `LHR`. §V's whole premise — *"The player who visits CI and doesn't find Yael: she's somewhere else"* — never occurs. She is always home and sometimes also in the field. **THE CALL:** (a) suppress the `LHR` Yael card while `_getYaelLocation()` returns a match — delivers the designed spatial puzzle, but a player who needs Yael for a quest now has to find her, which touches quest pacing; (b) keep the additive behaviour and **correct §V's text** to describe what shipped (Yael has a beat elsewhere, not an absence at home). **(b) is the low-risk default**; (a) is the faithful one and wants a playtest.
> Also **NOT SHIPPED:** the lock's sixth patrol row — Yael near `ER` with *"I know about the Redwater situation…"*. **0** occurrences of "Redwater" in the file, at HEAD and at `32c10c5`.
> **Provenance:** §DOC-02cz, finding F2.

### §DX-02fe — two favor scales in circulation, and the engine's own comments assert both (NEW 2026-08-23 during §DOC-02cz, 🟡 reconcile centrally)

- [ ] **§DX-02fe — is Dear Friend 2 or 3?** 🟡 The file answers both ways in its own comments: `@22305` says *"cap (default **3 = Dear Friend**)"*; `@23088` says *"progress toward **Friendly (fav 1)**"*; `@23507` says *"**Dear Friend (fav 2**, the ✦ worldTruth footer)"*. Content is split across the two scales accordingly. **On the 0–3 scale:** `FROBERGER_TRACES` (`minFav` 2/3), Room 6 (`>= 3`), Layer 44's `_getNodeMapColor` (tiers at 1/2/3) and `weckmann_class` (`>= 3`). **On the 0–2 scale:** `NPC_CROSS_REFS` (`fav` 1/2), the Nivers→Yael line (`>= 2` where the lock says Dear Friend), Froberger-note×Auros (`>= 2` where the lock says Dear Friend), RoughWhiskey×Brynn (`>= 1` where the lock says Friendly), and Layer 44's farewells (`>= 1` where the lock says Friendly) and Brynn maintenance (`>= 1` where the lock says Friendly). **Roughly a dozen gates, split.**
> **This is §DX-02fb and §DX-02fc's root cause, not a separate issue.** Content on the 0–3 scale either fires one tier early or — where the ceiling bites — never fires. **Fix:** pick one scale, publish it in `mechanics.md` with the tier names, and sweep the gates once. If 0–3 wins, every NPC needs a path to 3 (currently only `yael` has one, via `_setNpcFavor('yael', 3)@21429`). If 0–2 wins, four gates drop by one and `_getNodeMapColor`'s top tier retires. **A `check:favortiers` gate is cheap afterward:** assert every `minFav`/`_npcFavor(x) >= n` in the file is ≤ the enumerated reachable ceiling for that NPC — it would have failed on the day Layer 45 was written.
> **Provenance:** §DOC-02cz, finding F8; §DOC-02cy's F7 is the same defect seen one layer up.

### §DX-02ff — two declared relationships with no lines in either direction (NEW 2026-08-23 during §DOC-02cz, 🟡 four strings)

- [ ] **§DX-02ff — the connection map promises eight edges and delivers six.** 🟡 §III of the Layer 45 lock draws a connection map; `const NPC_CROSS_REFS = {@27694` honours six of its eight edges with all **17** authored lines byte-identical since `32c10c5`. Two are empty. **Measured across all 17 lines:** `Yael — Pachelbel` — **0** lines from Yael mentioning Pachelbel, **0** from Pachelbel mentioning Yael. The map describes it as *"professional awareness; Yael knows what Pachelbel does; doesn't move on it"* — dramatically the most interesting relationship on the board, a guard captain who knows a fence's business and has decided not to act, and it is never spoken. `All → Gigault` — **0** mentions of Gigault across all 17.
> ⚠️ **The Gigault half is the other side of §DX-02es, and the pair is the finding.** Layer 44 §II argues Gigault is real *because two people name her*; Layer 45 §III asserts *everyone* names her. Two locks written the same day, each treating the other as the place the naming happens. **Neither shipped it.** Between them the game has one bread stall, three ambient strings, and a woman nobody has ever mentioned. **Fix: two strings for Gigault (shared with §DX-02es), two for the Yael/Pachelbel edge.**
> **Provenance:** §DOC-02cz, finding F3.

### §DX-02fg — the training log renders an authoring stage direction as handwriting (NEW 2026-08-23 during §DOC-02cz, 🟢 one string, NO DESIGN CALL)

- [ ] **§DX-02fg — `[years of entries — fighters' names, brief notes, outcomes]` is player-facing text.** 🟢 The lock uses square brackets for two different things: `[gap of two years]`, which is prose and reads beautifully, and `[twelve years of entries — …]`, which is an instruction to whoever fills the log in. `const WECKMANN_TRAINING_LOG =@27736` shipped **both** verbatim; `function _buildWeckmannLog()@27667` substitutes only `{PLAYER_ENTRIES}` and `{CHAMP_ENTRY}`. **Measured:** the rendered log contains the literal placeholder. A player opening Weckmann's battered notebook — Cabanilles who said he'd be back and wasn't, Bruna pushed too far, the two-year silence, *"Back. Running legal fights only."* — reads an editorial note describing what should have been written there. **Fix: replace one bracketed line with three or four invented fighters and outcomes.** Everything else in §VI shipped exactly, including the player's five progressive entries keyed to `pitTrainingWins` and Layer 64's championship addendum.
> **Provenance:** §DOC-02cz, finding F5.

### §DX-02fh — a delivered Froberger trace never rejoins the pool (NEW 2026-08-23 during §DOC-02cz, 🟢 implement-or-retire)

- [ ] **§DX-02fh — one-time delivery where the lock specifies permanent addition.** 🟢 §II: *"After delivery, the trace text is added to the NPC's permanent Friendly/Dear Friend pool so it can resurface naturally."* `function _checkFrobergerTrace(npcKey)@27648` sets `frobergerTrace_<key>_delivered` and returns the text; the selector returns it as that visit's quote. **Nothing adds it to any pool.** Measured: first call returns the line, second and third return `null` forever. Small, and real — the traces are the layer's best writing, and a memory of a dead man that resurfaces months later unprompted is a different experience from one shown once during a checklist of visits. **Fix:** push the delivered text into the NPC's pool array on delivery, or retire the sentence from §II.
> **Provenance:** §DOC-02cz, finding F6.

### §DX-02es — the off-screen character the design is built around is named by nobody (NEW 2026-08-23 during §DOC-02cy, 🟡 two strings + ONE DESIGN CALL, which node)

- [ ] **§DX-02es — Gigault exists in three set-dressing strings and no character's mouth.** 🟡 `docs/lab-reports/lab-report-living-world.md` §II rests its whole argument on one mechanism and says so: *"She is named by two people. She affects the world. She exists."* The two namings are quoted verbatim in the lock — Yael's (*"She goes home an hour early when there's going to be trouble. I use her schedule to calibrate mine."*) and Brynn's (*"Gigault's the one who notices when the city is wrong before anyone else does."*). **Neither is in the engine.** `grep -c "Gigault" play.html` = **9**, and all nine are the feature's own plumbing (3 stall strings + `function _getGigaultState()@27558` + `function _nodeHookBirkaGigaultStall(node,@32117` + its registration + 2 comments). The stall itself works — `const PETRA_STALL_STATES = [@27398` cycles on `gameDay % 3`, verified in Chromium — so the player sees a blocked counter, then *"Back at ninth bell,"* then *"Tomorrow."* They just have no reason to look, because nobody told them she matters. **§II's own test (*"Gigault is the test"*) cannot be administered.**
> **THE CALL — which node?** The hook registers `nodes:['LLA']` = **The Rough Bar**; the `→ doc:` comment says `CI` (= `LHR`, City Streets); the lock says *"the player can visit the **market** node and see her stall."* A bread stall currently operates inside a bar. (a) **Move it to `LHR`** — matches the lock and the doc pointer, and City Streets is where a market belongs. (b) **Keep `LLA`** and repoint the comment — a bread stall outside a bar is not absurd and the text never names the room. **(a) recommended.** Also worth one line: the constant is named for **Petra**, a character who exists nowhere else in the file — a rename artifact frozen into a symbol name.
> **Fix:** add the two quoted lines to Yael's and Brynn's Friendly pools, pick the node. **Cost: two strings.** They are already written, in quotation marks, in the design document.
> **Provenance:** §DOC-02cy, finding F1, `docs/lab-reports/lab-report-living-world.md` §II.

### §DX-02eu — one line of inline CSS drops the game's closing image out of full-screen (NEW 2026-08-23 during §DOC-02cy, 🟢 delete one line, NO DESIGN CALL)

- [ ] **§DX-02eu — `overlay.style.position = 'relative'` beats the stylesheet's `position: fixed`.** 🟢 `#final-map-overlay@2131` is declared `position: fixed; inset: 0; z-index: 470` — the full-bleed black canvas §XI describes. Then `function _renderFinalMap()@27605` appends Layer 66b's S55 caption and writes `overlay.style.position = 'relative';` so the absolutely-positioned caption has a containing block. **`position: fixed` already provided one** — the line was never needed, and an inline style outranks the rule it overrides. **Measured in Chromium:** computed `position` is `fixed` before the call and `relative` after; the overlay's bounding box starts **262 px down the page**. The last image of every ending variant renders in normal flow *below* the header, the act strip, the compass, three map panels, the exits column and the combat log, and overflows the bottom of the viewport. Rendered proof: `test-results/l44-final-map.png` — the grid itself is correct (129 cells, two warm amber, one `#3a7a5a` green, caption on cue), it is simply drawn in the wrong place with the UI sitting on top of the run's final statement.
> **Fix: delete one line.** Fenced by the new `src/tests/integration/living-world-l44.spec.js` case, which currently asserts the *broken* state — invert its three expectations in the same edit. *(Deferred out of §DOC-02cy because it edits `play.html`, whose working tree carries the user's uncommitted CSS recolor.)*
> **Provenance:** §DOC-02cy, finding F3.

### §DX-02ev — seven authored lines with no lookup path, and one battleground return met with silence (NEW 2026-08-23 during §DOC-02cy, 🟡 implement-or-retire)

- [ ] **§DX-02ev(a) — Auros has farewells and no doorway.** 🟡 `const NPC_FAREWELLS = {@27427` holds an `auros` block of two lines. `function _getFarewell(fromCode, toCode)@27575` sources its owner key from exactly one table — `const npcKey = NODE_NPC_KEYS[fromCode];` — and `const NODE_NPC_KEYS = {@27413` is five rows at HEAD (`LHR:'yael', TLL:'brynn', MHQ:'quill', LLA:'pachelbel', HKG:'crov'`). Auros and Crov share `HKG`; the map gives the doorway to Crov. **The lock anticipated this exactly, and the anticipation is the part that did not ship:** `CY: 'crov', // also 'auros' — use max of both`. **Measured exhaustively** — `_getFarewell` swept over every ordered pair of `NODE_MAP` codes at maximum favor for all six NPCs yields **16 reachable strings** (11 routes + 5 defaults); *"The Ironshell node has subsidence in the northwest corner. Don't stand there."* and *"Document what you see."* are not among them. The same shortfall hides map warmth from her node, since `function _getNodeMapColor(nodeSlug)@27562` reads the identical table. **Fix:** implement the spec's `max of both`, or retire the `auros` block.
- [ ] **§DX-02ev(b) — five quiet receipts no call site can address.** 🟡 `const QUIET_RETURN_RECEIPTS = {@27519` holds **24** keys; its only consumer is `QUIET_RETURN_RECEIPTS[ebCode]` inside `function _storyEbReturnBeat(ebCode)@30358`, where `ebCode` is always an Epic Battleground node code. Nineteen keys are EB codes. The other five — `yael_ghetto`, `quill_debt`, `deacon_redd`, `crov_pit`, `auros_depths` — are the Birka arc returns the lock listed alongside them, and no call site can produce those strings as an argument. *"[Pachelbel holds the receipt for a long time. Then puts it in his coat.]"* has never been held by anyone. (`deacon_redd` also preserves the retired `deacon` prefix in a line that names Pachelbel — harmless while unreachable.) **Fix:** wire the five into the Birka quest-completion path, or retire them.
- [ ] **§DX-02ev(c) — `INV` returns to silence.** 🟢 The file defines **20** EB return quests; exactly one has no receipt: **`INV`** (`quest_eh_return` — *Return: Shepherd Rona*). Not remap damage — the ship build authored 19 legacy keys too (`ER EF ES EW EB EO EI EA EC EL ED EM EE EV EJ ET EK EP EG`, no `EH`), so Rona has been returning to silence since `32c10c5` while her nineteen colleagues each get a sentence. **Fix: one string.**
> **Provenance:** §DOC-02cy, findings F4 + F5.

### §DX-02ez — §X of the Living World lock never shipped, in any part (NEW 2026-08-23 during §DOC-02cy, 🟡 implement-or-retire)

- [ ] **§DX-02ez — the Void's First Sign is absent.** 🟡 `VOID_SIGN_ROW`, `VOID_SIGN_COL`, `void-flicker`, `_renderMinimapCell`: **zero occurrences each** in `play.html`. The three-act arc §X specifies — a barely-off-color pixel on the minimap in Act I, gone by Act III, a one-tile traversable space in Act V reading *"You saw this before. It was waiting for you to be ready."* — is absent in every part. It is the only whole section of the lock with no implementation at all, and 4 of the layer's 29 named symbols are its. **THE CALL:** implement (the minimap render path and `S_story.actNumber` are both live, so the Act I flicker is a CSS keyframe and one cell special-case; the Act V traversable tile is the real work and touches the mover — invariant #1 applies) or **retire the section**, annotating it *specified, never built* in the §AUDIT-03n/§DX-02c form rather than deleting it. **Retire is the honest default** unless the Act V payoff is wanted; a foreshadow nobody was ever shown foreshadows nothing.
> **Provenance:** §DOC-02cy, finding F3b, `docs/lab-reports/lab-report-living-world.md` §X.

### §DX-02ep — eighteen authored NPC lines are reachable only by losing the fight that unlocks them (NEW 2026-08-22 during §DOC-02cx, 🟡 ONE DESIGN CALL)

- [ ] **§DX-02ep — the Rough Whiskey window opens and closes inside one battle.** 🟡 `S_story.roughWhiskeyActive` has **one** writer — `if (_pb41 && _pb41.nodeCode === 'HKG' && !S_story.roughWhiskeyUsed@24688`, i.e. walking into the pit fight at HKG (historical `CY`) with the bottle in the pack, once per run — and two clearers: `S_story.roughWhiskeyActive = false;@25282` on **battle victory**, and `storyConfirmSleep`. `_storyFleeClean`/`_storyFleeMutual` do not clear it. So the **only** way to hear any of the 18 lines in `const ROUGH_WHISKEY_REACTIONS = {@27364` is to flee or lose the drunk pit fight and then go find someone to talk to. Confirmed in Chromium: with the flag off, all six keys return `null`; with it on, all six return their fav-tiered line — the renderer at `const whiskyReaction = _checkRoughWhiskeyReaction(key);@23724` is correct and complete. **The lines are fine. The door is the bug.**
> **And the game promises otherwise, in its own voice:** `storyMsg('🥃 Bought Rough Whiskey. ★ Social — each NPC reacts differently if you visit them@24383` — *"…while it's in effect."* There is no use-item path anywhere; the spec's *"consuming Rough Whiskey before visiting any named NPC"* was never built.
> **THE CALL — how should the bottle be drunk?** (a) **Add a Use button** on the inventory row (`type:'consumable_misc'`) that sets `roughWhiskeyActive` and consumes the item, keeping the existing window rule (*until you rest or win a fight*) — recommended, it is the spec's own design and the two clearers already exist. (b) **Stop clearing on victory** and let the window run until the next rest, so winning the drunk fight still leaves you drunk enough to be talked to — one deleted line, but it makes the flag survive a `storyMsg` that says the fight is over. (c) Buy a **second** bottle: keep the pit path as the "event" and let any further bottle be drinkable. **(a) is the honest default**, and it makes a 5gp vendor item worth buying twice.
> **Note the one thing that is exact:** the drunk fight itself is the spec to the digit — `if (S_story._drunkFight) adv = 'dis';@25044` and `const drunkBonus = S_story._drunkFight ? 3 : 0;@25071`, with Weckmann's *"You absolute idiot. I'm counting it."* and a favor bump to Friendly on the win.
> **Provenance:** §DOC-02cx, finding F3.

### §DX-02dq — the Talk verb advances the passive-visit counter its own design doc refuses to reuse (NEW 2026-08-21 during §DOC-02ck, 🟢 two lines, NO DESIGN CALL)

- [ ] **§DX-02dq — §NPC-01-D built a separate map so that *looking* could not raise favor, then routed every Talk through the function that counts looking.** 🟢 **the fix is a guard reorder; no new state, no design call.** §3 of `docs/lab-reports/lab-report-npc-01-d-talk-verb.md` rejects reusing `npcVisitCounts` in terms: *"that map increments on every passive card/dialogue view, so overloading it would let looking at a card raise favor. Talk must be a deliberate act → its own map."* The separation holds **for favor** — `npcTalk: {}@23088` is its own field and `function _setNpcFavor@23462` is never called from a render path. But `_talkToNpc@23514` opens with `function _getNPCDialogue@23560`, whose third statement bumps `S_story.npcVisitCounts[key]`, and it does so **before** either early return — so a Talk click on an already-Friendly NPC, and a second Talk on the same game-day, both increment the passive counter while doing nothing else at all. **That counter is a content trigger, not a statistic.** `function _checkFrobergerTrace@27648` reads `const visits = (S_story.npcVisitCounts@27654` and gates the six one-time Froberger memory texts in `const FROBERGER_TRACES = {@27685` on `visits >= trace.visitTrigger` (1–3) **plus** `minFav` 2 or 3. Talk caps at fav 1, but `const dearFriendBits = {@23472` auto-promotes to 2 the moment a personal act is already satisfied, so the fav-2 traces (`yael` 3 visits · `quill` 2 · `pachelbel` 2 · `auros` 1) are reachable and their visit gate can now be satisfied by button clicks instead of by returning to the node. The one-time memory of a researcher who passed through is the game's quietest reward for revisiting a place; it should not be purchasable from a chip on the card. **Fix (two lines):** hoist the `_npcFavor(key) >= 1` and `t.lastDay === S_story.day` guards above the `_getNPCDialogue` call, resolving the display name from `BIRKA_NPC_PROFILES` alone on those paths — or read `NPC_DIALOGUES[key]` directly in `_talkToNpc` and leave the counting to the render path that is supposed to own it. **Risk:** low, one function, outside all four parity fences. **Verify:** a Playwright pin that N Talk clicks on one card leave `S_story.npcVisitCounts[key]` unchanged, plus a same-day double-click asserting no increment; extend `src/tests/integration/npc-card-map.test.js`, which already seeds this exact state.

### §DX-02dm — the card that exists to make one NPC honest labels her honest line with the wrong relationship tier (NEW 2026-08-18 during §DOC-02ch, 🟢 no design call)

- [ ] **§DX-02dm — Yael's card renders the `📋 Quest Active` badge above the force-delivered `Impartial` welcome monologue.** 🟢 **one-line mismatch, on the one card §PLAY-01-D exists to make truthful.** `` `function _getNPCDialogue(npcKey) {@23560` `` picks a pool from relationship state, and §PLAY-01-D then bypasses that choice: `` `!(S_story.ngPlusRun > 0) && !S_story.yaelOnboardingSeen) {@23579` `` returns `d.impartial[0]` directly on the first meeting, because `quest_slums_cleanup` ships `gate:{}` and is active from turn one, which would otherwise shadow the onboarding speech with a mid-Slums line. The **badge is computed independently** in `` `function _renderNpcCard(key, container) {@23683` `` from `_hasActiveQuestFor(key)`, which is still true — so the fresh-game card at `LHR` reads **Quest Active** over the Impartial text. **Browser-proved at HEAD** (throwaway probe, run and deleted): `storyNewGame` → `#story-npc-cards-row` renders 5 cards, Yael's carrying both `Yugurt` and `Fisherman`, headed *"Guard Captain Yael Scheidemann 📋 Quest Active"*. Cosmetic, but it is the §PLAY-01 say/do shape in miniature: the tier label and the tier text disagree on the screen the honesty fix was built for. **Fix:** have the guaranteed-delivery branch return a `fav`/tier hint the badge reads (or compute `badgeTxt` from the pool actually used rather than from quest state). **Verify:** extend `src/tests/integration/friendships-with-magic.smoke.test.js` — it already drives the first delivery — with an assertion that the rendered card's badge says Impartial on the run that shows `impartial[0]`.

### §AUDIT-03ah — the game's fifth ending is gated on three preserved Dear Friends, and nothing says so (NEW 2026-08-12 during §DOC-02aa, 🟡 small design call)

- [ ] **§AUDIT-03ah — Entry 42's fourth trigger condition is undocumented and fails silently.** The Layer-50 Entry 42 panel at `LHR` needs `ngPlusRun >= 1` + `priorQuestMinusOne` + `!entry42Written` **and** `if (_e42Dear >= 3) {@34640`, where `const _e42Dear = ['yael','brynn','quill','pachelbel','crov','auros']@34638` counts how many of the six are at `_npcFavor(k) >= 2`. The condition shipped in the birth commit and appears in **no** lab report, home doc, quest hint or on-screen line; §DOC-02aa is the first measurement of it.
> **Blast radius is the whole endgame, not one panel.** `entry42Written` is the gate on `S_story.vaArchitectureKnown = true;@31689`, which is the gate on the **fifth ending** (`// Layer 52: §XVII — fifth ending: vaArchitectureKnown overrides all other questions@28268` — Sweelinck's last question becomes *"What was inside the cage?"*), on the victory-screen four-authors addendum, and on `quest_va_04`. So a player who reaches NG+ having been warm to only two people can never see the game's best ending **and is never told why**: the panel simply does not render, and `quest_ng_02` sits active with a hint pointing at a place where nothing happens.
> **The threshold itself is good design** — it is what makes Layer 50's deferred-reward thesis real rather than rhetorical. The defect is the silence.
> **Three options, small design call.** (1) **Signpost it** (recommended): when the first three conditions hold and `_e42Dear < 3`, render an ambient line instead of nothing — *"The journal is here. The forty-second page is blank. You have the feeling it is waiting for someone who is more here than you are."* — plus a `quest_ng_02` hint that names the relationship requirement. Costs one `else` branch. (2) **Lower or remove the threshold** — cheapest, but it deletes the one mechanic that makes favorability matter across a run boundary. (3) **Document only** — home doc + hint, no engine change.
> **Also fix regardless of the option:** `hint:'Visit the City Inn to find the open page.',@11051` sends the player to an inn; the panel is at `LHR`, City Streets — Birka, and the inn is `TLL` (→ §AUDIT-03s).
> **Risk:** low. **Verify:** a Playwright pin at 2 vs 3 Dear Friends in NG+ (nothing vs the panel; with (1), the ambient line vs the panel).

---

## Theme buckets (Phase 3)

### Game Content — Major Planned Arcs

> Each is design-complete or scoped in a memory file; **all require a `lab-report-*.md` locking data shapes before any HTML edit.** Restored here 2026-06-25 from memory (dropped from the plan during the §WALK rewrite).

> *Five arcs ✅ CLOSED 2026-07-07 and archived — **§GR** "La Riva" (already shipped at Layer 78; + the fishmongerRowRestored payoff, Mechanics note below) · **§DESIGN-03** Ceremonia Roll (already shipped; 104 live skill_check quests) · **§DUNGEON-01** 10 Dungeon Themes (already shipped) · **§MATH-01** Mathematical World (completions shipped `32d7bb0`; nodes moved to the walkable HKG pocket) · **§FUTURE-01** Saul→Paul (already shipped, 18 quests §LIX–§LXIX/§PAUL-01; cloak-with-Carpus residual `6375dcd`; §MBIT-02 bitLabel residual `ce8dbc9`). Four of the five were stale-PLANNED closes — the lesson stands: **grep before building** (`project_data01_reverted`). Full ship records (verbatim, incl. smoke matrices and the WBAPI newline/object-PUT hazards): plan-archive.md §"Archived 2026-07-07".*
> *§1367 (7/7, Events A–G all LIVE) · §GR-D Entry 42 (ALREADY SHIPPED, sim 13/13) · §KG (Inc 1–3: Hunt Mode + SPB→TVR corridor + 11-quest chain) all ✅ CLOSED — rows moved verbatim → plan-archive.md §"Archived 2026-07-28 — §DX-01f".*
- [~] **§IDEA-01 — Idea Generator: found-text → quest-theme seeds** — repeatable process: paste any found-text list (affirmation loops, song lyrics, transcripts) → dedupe repeats + ASR garbles → **every substantive line** becomes a categorized quest-theme one-liner in `docs/notes/brainstorm-one-liners.md`, with a per-line coverage table proving 100% coverage (artifacts like [Music]/timestamps discarded, garbles reconstructed + noted). Seeds are backlog fodder, NOT scoped work — promoting a seed = its own § entry here (+ lab report per policy if it grows to a chain/faction/system). **Processed so far:** S1 "Quiet Power" (AFF-01..24) · S2 "That Girl" (AFF2-01..34) · S3 "Queen of Luxury" (AFF3-01..11) — per-source detail + cross-links to live systems archived 2026-07-03 (plan-archive.md). New sources append as S4+.

---

## §RESUME — Phase 3 history (newest first)

> **41 entries**, carried verbatim from the single `BACKLOG.md` §RESUME log and filtered to this phase's subsystem. Order within this file is newest-first, as in the original. **The full cross-phase chronology — every entry in original order, with the file that now holds it — is the §RESUME index in [BACKLOG.md](BACKLOG.md).** Read that first when you need "what happened last", regardless of subsystem.

> **⟶ PRIOR (2026-08-23) — §DOC-02cz ✅ SHIPPED: the most completely-shipped lock the program has scored, whose best Froberger trace has never been read because a hardcoded `2` sits three hundred lines away from a gate that asks for `3`.** `docs/lab-reports/lab-report-web-of-connections.md`, **402 → 258 lines**, the Layer 45 web-of-connections arc — the other half of the `2026-07-31 09:36:39` mtime tie, resolved alphabetically. Same provenance as cx/cy: dated **2026-05-22**, already whole in `32c10c5` (2026-05-24, 14,377 lines), so the ship build IS the first build; HEAD is **38,712 lines / 5,513,223 bytes**.
>
> **FIDELITY: THE BEST YET. All eight subsystems shipped.** Every authored string is **byte-identical from the initial commit to a 6.4× file** — all **6** Froberger traces, all **17** cross-references, all **5** patrol lines, the entire **50-line** training log, Room 6's four paragraphs, all **3** cross-item connections. Only keys (§AUDIT-03n) and node codes (§AUDIT-03j: `SW`→`MSY`, `SL`→`BMA`, `IN`→`TLL`, `TV`→`MHQ`) moved. §VI/§VII/§VIII shipped with the details a hurried implementation drops first: the second pillow Brynn added and cannot explain, the sigil at eye level, the token Weckmann keeps instead of handing back.
>
> ⚠ **THE HEADLINE IS A CEILING IN A SHARED LEDGER THAT NO SINGLE LAYER CAN SEE.** Every write to `npcFavorability` resolves to four sites; the two auto-upgrade paths (`function _setNpcFavor(key, level)@23462`, `function _checkDearFriendUpgrade(key)@23489`) both write a hardcoded **2**, and `_setNpcFavor` is monotonic. Enumerating every `kind:'favor'` bit per NPC gives the reachable ceilings: **`yael` 3** (via `_setNpcFavor('yael', 3)@21429`, the Ceremonia CHA DC 15), **`brynn` 3 but order-dependent**, and **`quill` / `pachelbel` / `crov` / `auros` — 2.** `FROBERGER_TRACES.crov` asks for **3**. **Measured in Chromium: at his ceiling `_checkFrobergerTrace('crov')` returns `null`.** The unreadable line is the best of the six — *"You still grieve it, don't you. That's why you run it clean."* — and it is exactly the panel §II's Composite Picture calls *"He could see grief from the outside."* → **§DX-02fb** 🟢 *one `add:1` bit fixes it.*
>
> ⚠ **AND IT CORRECTS A CLAIM THIS PROGRAM PUBLISHED YESTERDAY.** §DOC-02cy scored Layer 44's `WORLD_PROGRESSION_EVENTS` as *"all 6 events live and correctly gated."* `weckmann_class` requires `_npcFavor('crov') >= 3`. **Measured at act 8 with crov at his ceiling: `false`.** Five of six. `docs/lab-reports/lab-report-living-world.md` corrected in this increment (Result table + F2 prose), and the cross-layer case is pinned in the new suite. ***A ceiling in a shared ledger is invisible from inside any layer that reads it — which is why it survived two verification passes.***
>
> **SIX FURTHER DELTAS, ALL MEASURED.** **Room 6 is silently order-gated** — ledger→entry-7→firewood reaches 3 and opens it; ledger→firewood→entry-7 stops at 2 and it never appears, losing §VII entire including the layer's best sentence (§DX-02fc 🟡). **Yael's patrol is a priority list pretending to be a schedule** — `_getYaelLocation()` is first-match-wins and declares `gameDay % 2 === 1` first, so with *every* rival condition satisfied she is at `MSY` on every odd day; four lines are even-day-only, and nothing ever removes her from `LHR`, so §V's "she's somewhere else" never occurs (§DX-02fd 🟡). **Two favor scales are in circulation** and the file asserts both in its own comments (`@22305` "3 = Dear Friend" vs `@23507` "Dear Friend (fav 2)"), splitting ~12 gates — the root cause of the two above (§DX-02fe 🟡). **The connection map promises eight edges and delivers six** — `Yael — Pachelbel` has 0 lines either direction, and `All → Gigault` has 0 across all 17: **Layer 44 §II says Gigault is real because two people name her; Layer 45 §III says everyone does; neither shipped it** (§DX-02ff 🟡, pairs with §DX-02es). **The training log renders `[years of entries — …]`, an authoring stage direction, as handwriting** (§DX-02fg 🟢). **A delivered trace never rejoins the pool** as §II specifies (§DX-02fh 🟢).
>
> **Tests, from zero again.** `src/tests/integration/web-of-connections-l45.spec.js` — **11/11 green**, including the cross-layer `weckmann_class` case that exists only because yesterday's published claim was wrong.
>
> ***THE LESSON: a threshold and the value that feeds it must be verified together, or neither is verified.*** §DOC-02cx found an ending unreachable by one point. §DOC-02cy found a promise off by one copper. §DOC-02cz found a relationship tier off by one, in a ledger two layers deep. **Three increments, three off-by-ones, none catchable by any existing gate — because none is a bug in the ordinary sense: every line involved does exactly what it says.** Proposed fence in §DX-02fe: `check:favortiers`, asserting every `minFav` / `_npcFavor(x) >= n` in the file is ≤ that NPC's enumerated reachable ceiling. It would have failed on the day Layer 45 was written.
>
> **Also this increment, per user directive:** the **Standing Directive — Code Comments (CC-1..CC-6)** added at the top of this file, with its conflict against the repo's §AUDIT-03n *"annotate, don't rewrite"* convention flagged rather than silently resolved → **§DX-02fa** 🟡. Docs+tests only; `play.html` untouched (the working tree still carries the user's uncommitted CSS recolor). → **§DX-02fa · §DX-02fb · §DX-02fc · §DX-02fd · §DX-02fe · §DX-02ff · §DX-02fg · §DX-02fh**

> **⟶ PRIOR (2026-08-23) — §DOC-02cy ✅ SHIPPED: the most faithfully-transcribed lock the program has scored, whose off-screen character is named by nobody, whose six journal notes are posted into the movement breadcrumb array, and whose closing image is drawn underneath the user interface.** `docs/lab-reports/lab-report-living-world.md`, **431 → 280 lines**, the Layer 44 living-world arc. Ordering re-derived from `stat`: `2026-07-31 09:36:39`, a **two-way tie** resolved alphabetically (`living-world` before `web-of-connections`). Provenance matches §DOC-02cx exactly — the report is dated **2026-05-22** and the feature set is **already whole in `32c10c5`, the repository's initial commit** (2026-05-24 17:34:49, 14,377 lines / 859,773 bytes), so there is no pinnable parent; the ship build IS the first build. HEAD is **38,712 lines / 5,513,223 bytes** — a **6.4×** file.
>
> **THE FIDELITY IS THE FINDING'S SETUP, AGAIN. 25 of 29 named symbols resolve at HEAD**, and every authored string block is **byte-identical from `32c10c5` to HEAD**: `PETRA_STALL_STATES` (3), `WORLD_PROGRESSION_EVENTS` (6 events + 5 notes), `NPC_ACT_THREE_LINES` (6), `BRYNN_MAINTENANCE_TASKS` (3 tasks × narration + `brynn_after`), `QUIET_RETURN_RECEIPTS` (24), `DEACON_CODE_TEXT` (4 rules), `NPC_FAREWELLS` (18). Only **keys** moved, and only under §AUDIT-03j/n. §XI's ending-map timings — *"3-second fade in / Hold for 5 seconds / Fade to black"* — are matched **to the millisecond** (`final-map-fadein 3s` + `3100 ms` + `5000 ms` + `1.5 s`). Two places the implementer was **better than the lock**: `_getFarewell`'s route composition, which the spec left as `// ... route lookup logic` + `return farewells.default; // simplified`, was finished (all 11 route lines reachable instead of none); and `_getNodeMapColor` correctly dropped the spec's dead `const warmth = fav * 20;`.
>
> ⚠ **THE HEADLINE IS THAT A LAYER DESIGNED TO BE UNOBTRUSIVE FAILED THE WAY IT WAS DESIGNED TO SUCCEED — QUIETLY.** Three load-bearing misses, each a last-mile omission rather than a design error. **(1) §II's premise did not ship.** The section rests on one mechanism and says so — *"She is named by two people. She affects the world. She exists."* — and quotes both namings verbatim. `grep -c "Gigault"` = **9**, all nine the feature's own plumbing. The stall cycles correctly on `gameDay % 3`; nobody in the game has ever said her name, so §II's own test (*"Gigault is the test"*) cannot be administered. It renders at `LLA` — **The Rough Bar** — while its doc pointer says `CI`. → **§DX-02es** 🟡 **(2) §III's rendering did not ship.** `S_story.log` has **eight** consumers: six treat it as the node-code breadcrumb trail (`S_story.log.push(S_story.currentCode)@28359`, capped at 20, read as `new Set(...slice(-20))` in three minimap renders); two `unshift` a `{type:'world', text, day}` **object** into it. **Nothing reads `.text` or `.type`.** `.journal-entry.world@2116` is a 5-line rule for a class the render path never applies — `querySelectorAll('.journal-entry')` returns **0** at every point in a run. **Six authored strings no player has ever seen.** → **§DX-02et** 🟡 **(3) The game's closing image is not full-screen.** `#final-map-overlay@2131` is `position: fixed; inset: 0; z-index: 470`; `function _renderFinalMap()@27605` then writes `overlay.style.position = 'relative'` so Layer 66b's caption has a containing block — which `fixed` **already provided**. Measured: computed position flips `fixed → relative`, bounding box starts **262 px down the page**, and the ending map draws in normal flow beneath the header, act strip, compass, three map panels, exits and combat log. **Delete one line.** → **§DX-02eu** 🟢
>
> **Six further deltas, all measured.** Auros has two farewells and **no doorway** — `NODE_NPC_KEYS` has no `auros` row and the lock's own `// also 'auros' — use max of both` is the part that did not ship; an exhaustive `NODE_MAP²` sweep at max favor yields **16** reachable strings, none hers (§DX-02ev(a)). Five Birka quiet receipts are keys **no call site can address** (§DX-02ev(b)), and `INV` (*Return: Shepherd Rona*) is the **1 of 20** EB returns with no receipt — original, not remap damage (§DX-02ev(c)). Brynn's ledger prints lines summing to **+138** under a `BALANCE` reading **−8** from an unrelated field, and its three tasks reach **+1** against a spec that says *"can be brought to zero but not into surplus"* — the code even has a green `'+' + bal` branch for the state the design forbids (§DX-02ew). `couperiDebtDegraded` carries **two opposite meanings** — Layer 44 sets it for *debt neglected*, Layer 69 for *debt released* — and the dialogue reader at `@23584` speaks absolution over neglect (§DX-02ex). Five `→ doc:` pointers are wrong in this one layer, and `check:anchors` — green on **4,507** anchors across **109** docs — cannot see them, because it audits `symbol@line` only (§DX-02ey). §X, the Void's First Sign, **never shipped in any part** (§DX-02ez).
>
> **Test coverage, from zero.** No test had ever touched a Layer 44 symbol. `src/tests/integration/living-world-l44.spec.js` now pins **nine** behaviours through the engine's own functions — the breadcrumb write and the dead CSS rule, the unreachable Auros farewells via the exhaustive sweep, the favor threshold (`fav >= 1`, one tier below the lock's `>= 2`, in **two** systems), the ledger arithmetic and its `+1` ceiling, the receipt coverage set, the five warmth tiers and the three-day stall cycle, the 129-cell ending map with its caption timing, the lost `position: fixed`, and the Act III body class. **9/9 green** in Chromium; rendered proof at `test-results/l44-final-map.png`. Two of the nine initially failed on **my** arithmetic, not the engine's (printed sum, cell count) — both corrections made the findings sharper.
>
> ***THE LESSON, AND IT IS THE ROW'S REASON TO EXIST: a system built to be missed needs a test more than a loud one does.*** Every subsystem in Layer 44 is deliberately unobtrusive — no XP, no banner, no gate, nothing that stops the player. A quest that doesn't fire gets reported within an hour. A farewell that doesn't fire, a journal note nobody reads, a woman nobody mentions, an ending map drawn under the UI — these are indistinguishable from working as designed. **The layer's own closing line now holds literally: the map doesn't judge, it just shows. It just needs to be told to cover the screen.** Docs-only increment; `play.html` untouched (the working tree carries the user's uncommitted CSS recolor, so all three code fixes were deferred to rows). → **§DX-02es · §DX-02et · §DX-02eu · §DX-02ev · §DX-02ew · §DX-02ex · §DX-02ey · §DX-02ez**

> **⟶ PRIOR (2026-08-22) — §DOC-02cx ✅ SHIPPED: the design lock whose every string survived ninety days byte-for-byte, whose best ending has never once fired, and whose five perks are congratulated, badged, carried through New Game+, and inert.** `docs/lab-reports/lab-report-endings-and-echoes.md`, **437 → 258 lines**, the Layer 43 covenant/ending arc. Ordering re-derived from `stat`: `2026-07-31 09:36:39`, a **three-way tie** resolved alphabetically. Provenance is unusual — the report is dated **2026-05-22** and the feature set is **already whole in `32c10c5`, the repository's initial commit** (2026-05-24 17:34:49, 14,377 lines / 859,773 bytes), so there is no pinnable parent; the ship build IS the first build.
>
> **THE FIDELITY RESULT IS THE BEST THE PROGRAM HAS RECORDED, AND IT IS THE FINDING'S SETUP. 23 of 23 named symbols resolve at HEAD** — nine subsystems, none missing — and every authored text block is **byte-identical from `32c10c5` to HEAD across a 5.5× file**: `SWEELINCK_NAMING_LINES` (12 lines), `NPC_EPILOGUES` (18), `FROBERGER_EPILOGUE` (4), `ROUGH_WHISKEY_REACTIONS` (18), `PIT_PERK_UNLOCKS` (5), `NPC_NG_PLUS_GREETINGS` (6), `COVENANT_STANDING_LABELS`, `_curseScore`, and the whole 37-line ceremony block in `storyCheckVictory`. Rendered in Chromium with the animations pinned: amber sigil, circle + four interior lines, six Dear-Friend names in italic beneath it — **§II and §III render exactly as specified.** *A design lock that describes code already in the tree looks like this.*
>
> ⚠ **THE HEADLINE IS ARITHMETIC, AND IT WAS WRONG ON THE DAY.** `function _curseScore()@28191` returns `(startedNotReturned * 3) + (neverStarted * 1) - (allComplete ? 5 : 0)`, so over all **231** partitions of the 20 Epic Battlegrounds the reachable set is **`{ −5 } ∪ [1, 60]`** — **0 is unreachable too**. Two consumers ask for **≤ −6**: the top rung of `const COVENANT_STANDING_LABELS = [@27356` and `const _isTrue = missionDone && curse <= -6@28229`. Measured in Chromium **through the engine's own functions**, label hits are Wanderer **186** · Watcher **30** · Keeper **14** · Warden **1** · **Covenant Keeper 0**. The perfect 20-of-20 run — the hardest thing the game asks — renders `Warden · "You carry the work with you. It shows."`, while the line written for it (*"The people you helped are the reason this works."*) sits in the file unreachable. **Both constructs are byte-identical at the initial commit**, so this is not drift: ***a number that is never re-derived is never checked, and a design document is graded on whether the code matches it — never on whether it closes.*** → **§DX-02en** 🟡
>
> ⚠ **AND §VII's PAYOFF — the sentence the whole subsystem exists for — IS IN NO BRANCH.** The report argues: *"the player sees 'Warden' in their character sheet for 40 hours, then Sweelinck says 'you were a Warden' in the final scene — and they understand."* `storyCheckVictory` writes four ending texts and **none names the player's standing**. The word `Warden` is spoken in exactly one branch — the Cursed Seal echo's *"the Warden who arrived here was capable, dedicated, and efficient"* — and that branch fires at `curse >= 15`, where the sheet reads **Wanderer**. ***The connection the design asks the player to make cannot be made.***
>
> ⚠ **A CEREMONY AROUND FIVE NO-OPS.** `function _applyPitPerks(combatState)@28181` sets five booleans at `_showBattleOverlay`; **each of the five names occurs exactly twice in the whole file** — `perkList`, then the assignment — and **nothing reads any of them**. All five §VIII effects are inert (+1 flanking · HP-tier preview · crit shove · 1d4 between rounds · once-per-rest Recompose), while the unlock, the `🏆 PERK UNLOCKED` message in Weckmann's voice, the NG+ carry-over and the `Weckmann's Student` sheet badge all ship correctly. **`world.md` documented the no-op faithfully and that is why nobody caught it** — its *"Combat effect"* column reads `combatState.groundGame = true`, the **write**, presented as the effect. ***A doc that transcribes an assignment has documented a promise, not a behaviour.*** → **§DX-02eq** 🟡
>
> ⚠ **EIGHTEEN LINES BEHIND A DOOR THAT ONLY OPENS WHEN YOU LOSE.** `S_story.roughWhiskeyActive` has **one** writer — entering the HKG pit fight with the bottle (`@24688`) — and two clearers: **battle victory** (`@25282`) and sleep. `_storyFleeClean`/`_storyFleeMutual` do not clear it. So the only way to hear any of `ROUGH_WHISKEY_REACTIONS`' 18 fav-tiered lines is to **flee or lose the drunk pit fight and then go find someone to talk to.** The renderer is correct and complete (`@23724`; flag off → all six keys `null`, flag on → all six speak), and the game promises the feature in its own voice at the vendor: *"★ Social — each NPC reacts differently if you visit them while it's in effect"* (`@24383`). **The spec's actual use path — *"consuming Rough Whiskey before visiting any named NPC"* — was never built.** → **§DX-02ep** 🟡
>
> ⚠ **THE SPEC NAMED A FIELD; THE IMPLEMENTER BUILT IT AND WIRED EVERYTHING TO THE OTHER ONE.** §IV gates on `ebReturnsCompleted >= 10`. That field is declared (`@23086`), written on every return (`@30365`) — and read **zero** times; all nine consumers read the sibling `ebReturnDone`. A perfect parallel ledger maintained for 90 days that nothing has consulted. Same section: the EB block ships as **one summary count line**, not the spec's *"one line each"*, and `FROBERGER_EPILOGUE.cursed` — the best line in its set — **has no selector**, because the state it was written for returns the Groundhog block earlier in the same function. → **§DX-02eo** 🟢
>
> ⚠ **§I NAMES THE WRONG NODE, AND IT WAS WRONG ON THE BUILD IT WAS WRITTEN AGAINST.** *"the covenant at SQ… someone walks out of the cave."* At `32c10c5`, `storyCheckVictory` opens `if (node.code !== 'CO') return;`. **SQ (→ `NUE`) is Sweelinck's library; CO (→ `TLS`) is the Convergence spire platform, where the NPC on station is Bruhns.** It survived 90 days because §I is prose and no gate reads prose — and it is precisely the §AUDIT-03m-FU shape: a legacy sweep would have rewritten `SQ` into a tidy, confident `` `NUE` ``. ***Annotation without verification launders a wrong claim into a live one*** — the HISTORY classification refused the annotation and saved it by construction, not by anyone noticing.
>
> ⚠ **INSTRUMENT — A BARE NUMERIC DOC ANCHOR CAN BE A CORRECT MEASUREMENT THAT DECAYED, AND GATE #15 CANNOT SEE IT.** `world.md`'s *"`PIT_PERK_UNLOCKS`, HTML line 10457"* and `story.md`'s *"`COVENANT_STANDING_LABELS`, HTML line 10480"* both resolve **at offset zero at `32c10c5`** and are ~16,880 lines off at HEAD. Instrument 160's family from the other side — not an inherited prediction, an exact reading that rotted — and invisible to `check:anchors` because they are prose (`HTML line NNNN`), not `` `symbol@line` ``. Both migrated in this increment. **The same sweep found the mirror hole:** the file carries hundreds of `// → doc: <file> §<Section>` comments and **nothing verifies the target exists** — `COVENANT_STANDING_LABELS`' points at `docs/mechanics/mechanics-economy.md §Covenant Standing`, which has **zero** occurrences of the string. A ~40-line gate in the `check:anchors` house style is the mirror of gate #15 (anchors prove doc→code; this proves code→doc). → **§DX-02er** 🟢
>
> **A CREDIT, AND IT IS THE KIND ONLY GEOMETRY CATCHES.** §II's CSS specifies `stroke-dasharray: 600` on every path; the shipped markup carries a per-element `--d` — **251** on the `r=40` circle (2π·40 = 251.3), **80** on each line (axials exactly 80, diagonals 79.2). A flat 600 would have drawn five strokes at five different rates over the same 3 s. The implementer fixed the lock. Three smaller §II claims did not survive: the stroke has been **amber `#FEA712`** since the first commit, never *"white against black"*; there is **no pulse keyframe** (enumerated at runtime: `sigil-fade-in`, `sigil-draw`, `name-fade` and the two `ng-plus-*` rules, nothing else); and *"the ceremony is 8 seconds"* measures **6,500 ms** (no names) or **4,000 + 1,200 n + 2,000** = 13,200 ms at six names, plus an 800 ms fade and 8,800 ms of final map — **16.1 s / 22.8 s** boss-defeat to victory modal.
>
> **PLAYABILITY, and the scale has no neutral.** Measured on the live character sheet in Chromium: Act I / 0 shards → `Unknown`; first shard, EBs untouched → **`Wanderer` · "The Void will open again. Not your fault. Not entirely."** at curse **20**; all 20 started and none returned → Wanderer at 60; all 20 returned → **Warden** at −5. The row unlocks at `shards >= 1` — **Act II**, not the spec's Act III — so *"The Void will open again"* is the **default badge a player carries for six acts**. The first rung above Wanderer costs **6** completed return chains, `Keeper` costs **13**, `Warden` costs **all 20**, and the rung above that cannot be reached. The same threshold gates §III: `missionDone && curse <= 0` is satisfiable **only at −5**, so the naming ceremony — the arc's best idea, and the only place in ~2,850 quests where the game stops measuring and starts reporting — is a 20-of-20 all-or-nothing prize the spec presents as a soft *"curse score ≤ 0"*. **What the arc adds is real and shipped: 54 authored lines (12 naming · 18 epilogue · 18 whiskey · 6 NG+ greetings) that exist for no mechanical purpose and are gated entirely on `npcFavorability` — the payoff that makes NPC favor a system rather than a dialogue tint.** *And 186 of 231 reachable states get the Wanderer text instead.*
>
> **NO TEST EXISTS.** `grep -rl` across `src/tests/` finds nothing touching `_curseScore`, `_covenantStanding`, `_buildEpilogueScroll` or the perk tree — 9 subsystems, 0 specs. The five-line fence (pin `_curseScore()`'s reachable bounds; assert every `COVENANT_STANDING_LABELS` entry is returned by at least one state) **would have failed on 2026-05-24**. Folded into §DX-02en.
>
> **Gates:** `check:anchors` ✅ **4,450 → 4,507 across 107 → 109 docs, +57, 0 dead** — **28 in the report** and **5 in `story.md`**, both of which carried no `symbol@line` anchors at all and so JOIN the anchored set (that is the +2 doc count), 3 in `world.md`, the rest across the five new rows and this §RESUME entry · `check:legacycodes` ✅ OK, 10 swept docs, 18 phase-2 tokens all classified · `check:nodeindex` ✅ 416 nodes · **the other 13 `check:walk` gates read only `play.html`, which this increment did not touch** — the working tree's only HTML diff is the user's pre-existing CSS recolor, unchanged and unstaged · measurement specs **5/5 + 1/1** in Chromium (run through the repo's own `seedAndLoad`/`dismissContinue` harness, then removed — **the suite count is unchanged at 940**). ⚠ **The working-tree `play.html` diff is the user's pre-existing CSS recolor (HP-bar gradient + playfield green) — NOT staged** (§DOC-02cm hazard, fourth session running); §DX-02er(b)'s one-comment fix was deferred to a row for exactly that reason.

> **⟶ PRIOR (2026-08-21) — §DOC-02cl ✅ SHIPPED: the thesis was right, the plan was right, it shipped whole in nine hours — and the one census row it counted with a regex instead of a parser became a shipping defect five hours later.**
>
> On "continue", re-derived the ordering from `stat` first: the `2026-07-28 14:13:06` block held **11**, so the alphabetical tiebreak applied and `docs/lab-reports/lab-report-npc-card-map.md` was next — the §NPC-01 parent of §DOC-02ck's Talk-verb sibling, and their censuses overlap by design. **148 → 371 lines.**
>
> **PARENT BUILD PINNED FIRST (instrument 108).** `fed5ee4` is the ship commit and is **docs-only** (`play.html` untouched), so `git show fed5ee4^:play.html` reproduces the exact **37,812**-line, `coc-3.104.0` file the author was reading — both figures byte-exact in the report. `git diff fed5ee4 HEAD` on the report: **empty**. Zero drift in 29 days.
>
> **12 OF 15 CENSUS ROWS EXACT, 18 OF 22 LINE NUMBERS BYTE-EXACT, AND EVERY STRUCTURAL THESIS HOLDS.** The 14-node map, the single call site, 205 profiles / 204 unique, 213 dialogues with 0 duplicate keys, 213 `meta.worldTruth`, 418 `NODE_MAP` codes, **121** distinct profile `.node` codes with **0 dead**, 203 renderable candidates, 16 favor bits and **5** hardcoded `_setNpcFavor` sites are all exact. The report's central architectural claim — that the NPC→node map did not need deriving from `QUEST_DB` because every profile already declares its own `node` — is correct and was the right call.
>
> **THE THREE MISSES ARE ALL ARITHMETIC, AND ONE OF THEM SHIPPED A BUG.** §1 counts "rich" profiles with `grep -cE '^    neutral:'`. That regex demands exactly four leading spaces; **three** rich profiles declare their whole entry — including their tier objects — inline at two-space indentation and are invisible to it. The truth is **13 rich / 191 lean**, not 10 / ~194. The three it could not see are `the_fisherman`, `connie_tuna` and `aldo_sardino` — and **§3.2's locked override design, which promised the 14 legacy nodes would stay byte-identical, delivered that guarantee by rendering four rich cards nowhere at all.** §NPC-01-SF5 (`d2651af`, 5 h 40 m later) exists to un-shadow The Fisherman by hand; §NPC-01-SF6 (`5f676f1`) exists to generalize the root cause into a curated ∪ derived **merge**. **A census error became a shipping defect by the shortest possible route.** Third miss: `meta.enemy` **202**, not 203 — the raw-`grep` trap (one `enemy:` hit lives outside `NPC_DIALOGUES`), the exact mirror of ck's `worldTruth` 219-vs-213.
>
> **THE NUMBER THAT MATTERED MOST WAS NEVER STATED, AND THE REPORT HAD ALREADY FOUND IT.** *"20 of 213 reachable"* counts map **entries**, not cards. Driven in Chromium at the parent build, calling `function _renderNpcCard@23683` on all twenty mapped keys: **11 rendered, 9 rendered nothing, 0 threw.** The nine silent blanks are the report's own SF2 — filed as a footnote rather than folded into its headline. True pre-migration reach: **11 of 213 (5.2 %)**. Also inherited unchecked: §POT-R2's *"193 of 213 have a full four-tier arc"* is **201**; and the status block's *"all three of `43bd09c`'s claims are stale"* is **one of three** — 401 nodes is stale, but *20 reachable* and *14-key literal* are both exact, and the second **contradicts this report's own §1 row one paragraph later.**
>
> **THE CRASH CLAIM IS EXACT AND WAS PROVED BY EXECUTION, NOT BY READING.** `_renderNpcCard('ser_bardo', div)` at the parent build throws `TypeError: Cannot read properties of undefined (reading 'greeting')`; `'yael'` returns 832 bytes of HTML. All **191** lean profiles are in that class. The increment order the crash dictated — fallback first, widen second — was stated as a hard rule (*"do not ship B before A"*) and obeyed: **A landed 48 minutes after the lock, B nine minutes after A, and the whole A→D chain plus SF1–SF3, SF5 and SF6 shipped inside 9 h 16 m of the same day.**
>
> **TWO LOCKED SHAPES NOT SHIPPED, BOTH SUPERSEDED RATHER THAN ABANDONED.** `NPC_RENDER_OVERRIDES` has **zero hits in any build** (`git log -S … --all`, no pathspec) — replaced by the curated-wins literal and then by SF6's `_curatedGoverned` merge, for the reason above. `src/scripts/check-npc-cardmap.js` also never existed; its three assertions are nonetheless covered — (a) and (c) by `src/tests/integration/npc-card-map.test.js`, (b) by `check:noderegs`, whose `NODE_FIELDS` list carries `node`. And §3.3's greeting **synthesis** shipped as **omission**: `const greetingHtml@23718` renders `''` when no tier object exists, which answers open question 1 by choosing neither of its two options.
>
> **WHAT IT ADDED TO THE GAME, RE-MEASURED LIVE AT HEAD.** Curated ∪ derived spans **125 of 416 nodes (30.0 %)**; **203 of 204** NPCs render a card; **1** blank (`watcher_gvw`, already §AUDIT-03bo); **0** throws. **11 → 203 is an 18.5× widening of the relationship surface, at zero authoring cost** — it renders lines that already shipped. The report predicted "~203" exactly. Its node forecast (*"~107 new nodes"*, = 121 − 14) is **112**: only **9** of the 14 legacy codes are also profile nodes, since `CQ`/`SQ`/`GC` had no `NODE_MAP` entry at all and `STN`/`VS` carry no profile.
>
> **Acceptance re-run in the browser (server stopped):** `npc-card-map.test.js` **22 passed**, covering A/B/C/D and SF2/SF4/SF5/SF6. All four `check:walk` reds the report told the next agent to expect were retired within days of the lock; the gate has been 16/16 since.
>
> **TWO DEFECTS FILED.** **§AUDIT-03bp** — `don_fluffissimo` is the **tenth** name on SF2's list of ten and the one the fix (which correctly says nine) could not reach: dialogue-only, `meta.node:"CDG"`, no profile, and never in `birkaNpcs`, so it renders on no node at HEAD. It is the **third face** of one registry-mismatch class — SF2 closed *dialogue-without-profile-but-mapped*, §AUDIT-03bo is open on *profile-without-dialogue*, and this is *dialogue-without-profile-and-unmapped*, the only face no gate can currently see because both existing checks start from a key some registry already lists. **§DX-02dr** — both wrong counts were **copied out of the HISTORY doc and into the engine's own comments**, where they now read as measurements: `function _renderNpcCard@23683` says *"the ~194 non-Birka NPCs"* (191) and `function _deriveNpcRenderMap@23672` says *"from ~20 NPCs to ~203"* (11). Annotation without verification launders a wrong claim into a confident-looking live one — §AUDIT-03m-FU's lesson, arriving here from the other direction.
>
> **Instruments 139** — *when a report counts with an anchored line regex, re-count with the parser and then check whether the entries it could not see are the ones a later follow-up had to rescue; an invisible entry and an unprotected entry are usually the same entry.* **140** — *a reach figure quoted from a map is a count of keys, not of outcomes; render them and count what appears.* **141** — *a "these inherited claims are stale" list is itself a claim per item — score each one, and check it against the report's own tables before believing it.*
>
> **Evidence:** report **148 → 371 lines** (`wc -l`); `check:anchors` **4,055 across 96 docs, 0 dead** (4,049/95 at ck — **+6**, and the doc joins the anchored set from zero); `check:legacycodes` OK. Docs-only — `play.html` untouched.
>
> **Next: §DOC-02cm** — re-derive from `stat`; the `2026-07-28 14:13:06` block now holds **10**, so the alphabetical tiebreak still applies and `lab-report-vm01a-execbits-coroutine.md` is next — the head of the seven-report §VM-01 series, whose `execBits` coroutine is the host half of everything the UQF reports describe.

> **⟶ PRIOR (2026-08-21) — §DOC-02ck ✅ SHIPPED: the most accurate document in the corpus is wrong about the one sentence its whole surface argument rests on, and the implementation quietly corrected it the same evening.** On "continue", took `docs/lab-reports/lab-report-npc-01-d-talk-verb.md` (`2026-07-28 14:13:06`, re-derived from `stat` first — that block held 12 files, so the alphabetical tiebreak still applies and this was first). **Existing-Work-First:** the same uncommitted user recolor is still in the tree (HP-bar gradient, light-green playfield, theme-ink die numerals, self-labelled *"user recolor 2026-08-06"*) — **left untouched and unstaged**; 9 insertions / 9 deletions, all in place, so no line number moved. WBAPI server confirmed **Stopped** (Hazard #1 moot). This increment is docs-only.
>
> **PARENT BUILD PINNED FIRST (instrument 108).** `4acafa0` is the design-lock commit and is **docs-only**, so `git show 4acafa0:play.html` (37,913 lines) *is* the file the author was reading. **The report has not drifted a byte in 29 days** (`git diff 4acafa0 HEAD --` empty), and the ship record is unusually tight: lock at 19:49, implementation `df990c3` at **20:01 — twelve minutes later** — close-out `a824969` at 20:03. A design-lock whose whole point is to freeze the shapes *before* the edit, and the edit arrived while the ink was wet.
>
> **THE CENSUS IS THE BEST IN THE PROGRAM. 12 of 12 cited line numbers byte-exact** against the parent build — every one, including the four that point into `_renderNpcCard`'s footer block. **24 of 24 named symbols resolve at HEAD. All four locked data shapes shipped under their specified names** (`npcTalk: {}@23088`, `TALK_TO_FRIENDLY = 3@23513`, `_talkToNpc@23514`, the fav-gated chip at `tb.className@23788`), and the ceiling the design existed to protect is intact: `function _setNpcFavor@23462` is called at level 1 and never higher, so `dlg.meta.enemy@23755` became earnable while `dlg.meta.worldTruth@23762` did not move. **Acceptance re-run in the browser: `src/tests/integration/npc-card-map.test.js` 22/22 green** (16 at ship; §NPC-01-SF4 added six since).
>
> **ONE CENSUS FIGURE WRONG, AND WRONG WHEN WRITTEN — and it hides a better fact.** §1 claims `meta.worldTruth` is declared by **219** NPCs. Measured on its own build with the real parser (`src/js/wbapi-core.js`), it is **213** — which is also the total number of `NPC_DIALOGUES` entries. **Every single dialogue entry in the file declares a `worldTruth`;** the field is universal, not merely common, and the report invented six NPCs rather than noticing. Everything else in §1 is exact: `meta.enemy` **202**, `kind:'favor'` bits **16**, distinct NPCs they reach **13**, `dearFriendBits` **exactly 6**, the four absent verb names **0 at both builds**, `DAY_DEADLINE = 49@36175`.
>
> **THE REAL DEFECT IS THE PREMISE OF §2.** It argues the Talk action belongs on the card because the ~203 relationship cards *"currently carry **no interactive controls** (`cursor:default`)."* The `cursor:default` citation is accurate — it is on `card.style.cssText@23723` — but the inference from it is false: `function _renderNpcCard@23683` already contained a `<button>` with an `addEventListener('click')`, the Yael escort button, present since the **earliest surviving commit** (`git log -S "_yaelEscortAction"` bottoms out at `32c10c5`). §3 then prescribes `addEventListener` *"consistent with the card's existing DOM-node construction (`document.createElement`, 23383)"* — citing a generic line while the exact precedent sat 55 lines below it. **The implementation found what the document could not:** the shipped Talk chip copies the Yael button's style string **byte-for-byte**, which is why its border is `#7c4a1a` and not the `#8B4A2A` the spec named — the spec had taken that colour from the **card** border. ***A design doc that cannot see the control already on the screen still ships, because the code reads the screen.***
>
> **SEVEN SPEC→SHIPPED DELTAS, and the interesting ones are not the cosmetic ones.** (1) **NOT SHIPPED:** the escalating talk lines read from `dlg.impartial[]` in order by `count` — every talk still prints the same *warms to you a little (n/3)* line, which is the one place the mechanic feels like a counter rather than a conversation, **and it is also what makes the invariant-#6 clearance vacuous**: the report clears seeded-RNG by arguing those lines are read in order, and those lines do not exist. (2) the spec's bare `storyRender();` inside the handler **threw** on the `node.act` read; the live-click end-to-end test caught it before commit and it shipped as `storyRender(NODE_MAP[S_story.currentCode])` (§7½ earning its keep, on the record). (3) the chip ships **below** the footers rather than above — moot, since the chip needs fav < 1 and the footers fav ≥ 1, so they can never co-render. (6) verify-plan item 5 (a `connie_tuna` un-gating guard) was never added — a non-risk, since Talk mutates favor and never the key list, and the §NPC-01-SF6 AMS tests already assert it. **Two process notes:** §6 says the cadence model must go to the user and *"do NOT default"* while the handler printed in §3 already encodes model (B) in full; and the report letters its cost models **(A)/(B)/(C)** while the BACKLOG row letters its approaches **(a)/(b)/(c)**, so the ship record has to read *"approach (a) + cost model (B)"* to mean anything at all — *option letters minted twice in one increment are option letters for nothing*, the §DOC-02e node-code lesson in a report that never touches a node code.
>
> **WHAT IT ADDED TO THE GAME, RE-MEASURED LIVE.** Driving the real page: the derived render map spans **121 nodes / 204 keys**, and **203 of the 204 get a Talk button**. Favor had been authored for **13**. The verb is patient rather than transactional (three talks on distinct `S_story.day` values, no day spent), it refuses to sell the ✦ tier, and it is the reason two hundred authored `meta.enemy` lines stopped being unreachable.
>
> **TWO DEFECTS FILED.** **§DX-02dq** — §3 rejects reusing `npcVisitCounts` in terms (*"looking at a card would raise favor"*), and then `_talkToNpc@23514` opens by calling `function _getNPCDialogue@23560`, which bumps that very counter **before both early returns** — so a click on an already-Friendly NPC, or a second click on the same game-day, increments it while doing nothing else. That counter is a **content trigger**: `function _checkFrobergerTrace@27648` gates the six one-time Froberger memory texts on `const visits = (S_story.npcVisitCounts@27654`. Two-line fix, no design call. **§AUDIT-03bo** — `watcher_gvw: { key@22992`, The Greenwood Watcher, quest-giver for `clr_01_act3@21941` and `clr_01_act4`, has a full profile, a home node and **no `NPC_DIALOGUES` entry**, so `_renderNpcCard` early-returns and the card is empty: **the one NPC of 204 that renders nothing and gets no Talk button.** It is the unclosed mirror of the case §NPC-01-SF2 fixed (dialogue without profile), and the row asks for the general census gate so the mirror cannot silently reopen.
>
> **Evidence:** report **152 → 174 lines** — **the only rewrite in the program that grew**, because the original was already lean, three of its lines were code blocks now superseded by the as-built inventory, and it had never once been checked against the build it shipped from. **32 `symbol@line` anchors**, all resolving with fresh hints; `check:anchors` **4,033 anchors across 95 docs, 0 dead**; `check:legacycodes` **OK**. Gates 1–15 were not run and do not apply — docs-only, no HTML touched. `npm run test:mud` remains the known **RED 267/2** (§DX-02ca) and is unrelated.
>
> **Next: §DOC-02cl** — re-derive from `stat`; the `2026-07-28 14:13:06` block held 12 and now holds **11**, so the alphabetical tiebreak still applies and `lab-report-npc-card-map.md` is next (the §NPC-01 sibling of this one — its census will overlap and should be fast).

> **⟶ PRIOR (2026-08-18) — §DOC-02ch ✅ SHIPPED: the string is byte-exact at 37 days, the reason given for the fix beside it is false, and the road it signposts is closed at both ends.**
> On "continue", took `docs/lab-reports/lab-report-play-01d-friendships-with-magic.md` (`2026-07-14 17:10:58`, re-derived from `stat` first — the block held two, alphabetical tiebreak). §PLAY-01-D *Friendships With Magic*, face **D** of *The Honest Floor*: one dialogue string rewritten so that Birka's guard captain personally points a new player at the Fisherman and Yugurt Lake, instead of burying the magic path in a stat-grind aside. Rewritten **44 → 349 lines / 1,111 → 3,722 words / 7,603 → 22,222 bytes**; all growth is the verification ledger, the as-built inventory, the spec→shipped delta, the dating appendix and the playability sections the original lacked.
>
> **PARENT BUILD PINNED FIRST (instrument 108), AND THE DATING SPLIT IS THE METHOD RESULT.** Report and code are **one commit**, `cfdeb21` (2026-07-12 14:21:02), so `cfdeb21^` = `a52f9cd` (the §DEATH-01 ship, **37,244** lines; ship 37,254, +10) was extracted before a line number was scored. `git diff cfdeb21 HEAD` is **empty** over the report *and* over its acceptance test. **9 of 10 citations land byte-exact at the parent**; the tenth, `` `const birkaNpcs = { LHR:['yael'], TLL:['brynn']@35139` ``, lands **only at the ship** — and it is precisely the one line the increment moved. ***A report that documents its own repair is written across two builds: the diagnosis sections date to the parent, the fix section dates to the ship. Score both, or one clean citation reads as a miss.***
>
> **WHAT SHIPPED, AND IT HELD.** The rewritten sentence is a **700-character verbatim match** from §2 spec to HEAD. The delivery mechanism — `` `!(S_story.ngPlusRun > 0) && !S_story.yaelOnboardingSeen) {@23579` ``, ten lines inside `` `function _getNPCDialogue(npcKey) {@23560` `` — is **byte-identical ship → HEAD across 37 days and +1,458 lines**, with `` `yaelOnboardingSeen: false,@23171` `` correctly declared in `_S_DEFAULTS()` (§STATE-INIT honoured). Acceptance `friendships-with-magic.smoke.test.js` **1/1 in 1.0 s, never edited since the ship**; regressions `courier-map` 1/1 + `enemy-ai` 4/4 → **6/6 in 6.0 s**. The §2b shadowing diagnosis is exactly right and non-obvious: `quest_slums_cleanup` ships `gate:{}`, is active from turn one, and silently swapped the welcome speech for a mid-arc line.
>
> **⚠ INSTRUMENT 129 — A DEAD-CODE VERDICT IS A CLAIM ABOUT THE DATA SECTION. A NODE'S COORDINATE ENTRY IS EVIDENCE THAT IT EXISTS, NOT THAT IT DOES NOT.**
> §2b: *"a dead pre-§WALK code (no `NODE_MAP` node `CI`; `CI` is only a geo-grid coordinate)."* **`` `CI: { num:429, name:"city"@9229` `` is a LIVE NODE** — *Chancery Court — The Officer's Pen* — minted **2026-06-05** by `76f6133`, the Grimm's Fairy Tales importer, 37 days before the report, with one quest activating there (`erf_01_act5`). And `NODE_COORDS` holds **0 keys that are not `NODE_MAP` keys**, at the parent build and at HEAD: the line cited as proof of absence is proof of presence. **The card really did not render — browser-proved on the parent build, 0 cards at `CI`** — but the cause is that `CI` is one of **291 of 418** `NODE_MAP` entries at the parent build (289 of 416 at HEAD) carrying **no source-level `code:` field**, so `birkaNpcs[node.code]` looked up `undefined`. `` `LHR:{ num:1,  code:'LHR'@8427` `` is one of the 127 that do, which is the only reason the fix worked. **That defect had no name for another 17 days** — `f08f70f`, §AUDIT-03e, *"287 nodes stop sharing one `undefined` state slot"*. ***The fix is right, the rationale is wrong, and the rationale is what got generalised 22 minutes later.*** Mitigating and worth keeping: the file teaches the error — `` `// ── No Fishing Sign at CI ──@32418` `` sits directly above `if (node.code === 'LHR')`, and `src/scripts/legacy-codes.js` lists `CI` among the retired codes and prescribes rewriting it to `` `LHR` (historical `CI`) ``. `docs/maps/node-index.md` already flags the collision, so **no new row** — instrument 7.
>
> **⚠ INSTRUMENT 130 — WHEN A FIX IS ARGUED AS A CHAIN OF N SURFACES, THE ACCEPTANCE TEST MUST OPEN ALL N. THE ONE YOU EDITED IS THE ONE LEAST LIKELY TO BE BROKEN.**
> The report's stated thesis is *"two people, not a menu — Yael points to the Fisherman; the Fisherman gives freely."* It fixed and screenshot-verified **person one**. **Person two rendered no card at all**: `SSJ` is a curated `birkaNpcs` key and the literal shadowed the derived map, with the SSJ list reading `emmerMet ? ['emmer'] : []` and never naming `` `the_fisherman: { key:"the_fisherman"@22950` ``. **Eleven days open**, closed 2026-07-23 by §NPC-01-SF5 — whose own row says *"This closes the §PLAY-01-D thesis (Yael → the Fisherman) at the render layer."* Verified live at HEAD: SSJ renders 2 cards and the Fisherman is one of them.
>
> **THE DESTINATION IS CLOSED, AND THE HONESTY FIX MADE THE BROKEN PROMISE SPECIFIC (§FISH-01 🔴, corroborated not re-filed).** `BOO` shares grid cell `2,194` with `LYR`, declared 59 lines earlier, so arriving renders the arctic node and *Cast a Line* never draws. Exactly **1 node of 416 carries `isFishingLake`**. Since `cfdeb21` the game's **only tutorial speech** names that destination by name — *"Go north to Yugurt, to the cabin … Go to the lake early."* Before the fix the pointer was vague (*"a fishing dock … a slow river"*); measured at HEAD that vagueness pointed at **15 dock/river labels, none fishable**, so naming the lake removed a real misdirection **and** converted an unreachable subsystem into the first instruction a new player receives. §FISH-01 annotated with that consequence.
>
> **AND WHAT WAITS THERE IS NOT WHAT THE SENTENCE PROMISES (§FISH-02, and this is its FIRST PROOF BY EXECUTION).** Prior passes read the code; this one ran it. **20,000 `_rollD100Loot()` at level 20 → 0 mainweapons, 0 daggers, ever**; **20,000 `_rollMonsterWeaponDrop()` across every damage die → 0 positive, 13,369 degraded (66.8 %, against the 66.7 % `` `const deg = Math.min(0, d6 - 5);@24592` `` predicts)**. `WEAPON_ITEMS` = **70**, **56** unreachable; `` `const DAGGER_ITEMS = [@24458` `` = **4, all positive `atkBonus`, whole table dead** (its only reader is the unreachable `_D100_TABLE` branch — new to the row); `` `const LAKE_MAGIC_DB = {@26536` `` = **8 items, 0 weapons**. The shipped line *"The smiths in this city cannot sell it; the lake can"* is **not a lie** — the lake's trinkets carry real attack and AC bonuses — but it is a sentence about **weapons** in a game where the weapon half of §FC06's trade was never built. ***Every sentence in §1 of that report is defensible; the inference all of them support is not.***
>
> **Other findings.** **§DX-02dm NEW 🟢** — the guaranteed delivery returns `impartial[0]` while `` `function _renderNpcCard(key, container) {@23683` `` computes the badge from `_hasActiveQuestFor`, so the fresh-game card reads **📋 Quest Active** above the Impartial welcome speech: the say/do gap in miniature, on the one card the increment exists to make honest. **§AUDIT-03r WIDENED 1 → 14** — real-parser census finds **14 duplicate `num` groups**, `77` plus an unbroken **420–432** block (`429` = CI/RGS), which reads as two importer batches that both started at 420; `docs/maps/node-index.md` uses `Node #` as its matching key, so this is a live ambiguity in the tool that recovers historical codes. **§CODEX-01 annotated with the measurement that argues for it** — the monologue is **2,228 characters / 426 words** and the signpost begins at character **1,089, word 206 of 426, the exact midpoint**: the report diagnosed burial and re-buried at the same depth, so ***the transmission failure was position, and position did not change.*** **Positives worth keeping:** the deferral the report refused to guess (IN/TV/BA/CY — all four genuinely absent from `NODE_MAP`, unlike `CI`) shipped **21 min 52 s later** as §PLAY-01-G `a6a1ce7`, the fastest deferral-to-ship in the corpus; `` `const BOSS_COMMANDER_AUROS = {@26246` `` is byte-exact at AC 22 / HP 300; the geography checks out (LHR row 10 → SSJ row 4 → BOO row 2, and the Fisherman's own line agrees the lake is north); and `` `quest_no_fishing_sign: {@13878` `` survives as the alternate discovery exactly as promised. **Recorded, not filed:** §1 asserts the monologue *is* reliably shown on a first meeting and §2b of the same document refutes it — the two halves were written on either side of the verification pass and were never reconciled.
>
> **Evidence:** `check:anchors` **3,816 → 3,861 across 91 → 92 docs, +45** (the report joined the anchored set — it previously carried none), **0 dead, 117 stale = unchanged baseline**; per-region counts, re-measured against `git show HEAD:BACKLOG.md` **after** this entry was written (bw's lesson: the §RESUME's own anchors are the ones the first draft forgets): report **22** · §DX-02dm 3 · §RESUME **14** · §FISH-02 3 · §AUDIT-03r amendment 1 · §FISH-01 1 · program row 1 · §CODEX-01 0 = **23 in BACKLOG + 22 in the report**. `check:legacycodes` exit 0. ⚠ **`check:walk` DETACHED this time** — the one-call piped-to-`tail` docs-only recipe that ran clean for cf and cg exceeded 600 s here and was killed (SIGTERM, exit 144), with the pipe buffering so the output file stayed at 0 bytes; the documented remedy applied — **the two gates a docs-only increment can affect were run individually and are both green**, and gates 1–15 cover only `play.html`, which this increment did not touch. ***The recipe is not reliable — treat the individual-gate run as the default for docs-only work, not the fallback.*** Four throwaway Playwright probes, run and deleted (including one that loaded the **parent build** in the browser to separate *did not render* from *rendered elsewhere*). **Docs-only; `play.html` untouched** (user's 2026-08-06 recolor still exactly 9+/9−).
>
> **Next: `docs/lab-reports/lab-report-play-review.md`** — the `2026-07-14 17:10:58` block is now down to **ONE**, so no tiebreak: it is the PARENT review this whole §PLAY-01 face series answers, faces A–G in one document. ⚠ **Read the six sibling verifications (§DOC-02cc–ch) before scoring it** — every face has already been measured, so the parent's job is its own framing, its priority table and its unverified negatives, not a seventh pass over the same code. ⚠ **Its §PLAY-01-D section carries the claim that seeded the defect above** — *"all +1…+4 weapons/daggers are fishing-exclusive"* — which §FISH-02 shows was false when written; check whether its `_rollMonsterWeaponDrop` citation (`23740`) lands at ITS parent build. **Confirm with `stat`.**

> **⟶ (2026-08-18) — §DOC-02cb ✅ SHIPPED `27c38f5`: the most faithfully executed lock in the corpus, describing a feature no player has ever reached.**
> On "continue", took `docs/lab-reports/lab-report-kg-corridor-quest-chain.md` (`2026-07-08 19:10:53`, re-derived from `stat` first) — the §KG Increment 3 design lock: eleven honour-central UQF quests carrying a fresh L1 fighter east from the Nevsky Checkpoint to the dead station outside Moscow, plus one reusable per-monster kill counter. **Existing-Work-First:** the uncommitted user recolor of 2026-08-06 is still the only tree change and was left untouched; two claude processes; every figure below re-measured, never read off a doc table.
>
> **PARENT BUILD PINNED FIRST (instrument 108).** The report has exactly **one** commit and it *is* its ship commit (`d6aeefd`), so every line citation was re-read at `dde78ca` as well as at HEAD, and `git diff d6aeefd HEAD` on the report is empty — never amended. **All six HTML citations are byte-exact at the parent build** (24291/24292 catKills · 29566 killGoals · 22294 `_S_DEFAULTS` · 23470 `XP_LEVELS` · 12278 sb_parley), and the `wbapi-core.js ~L797` advise rule lands on the warning line at 796–797. Note the path: `wbapi-core.js` was at the repo **root** on 2026-07-08, so the unprefixed citation is correct for its day.
>
> **THE LOCK HELD, ITEM FOR ITEM.** 11/11 quests exist with the tabled ids, `npc` keys, `activateNode`s, gates and completion shapes; **all 11 XP/gold pairs byte-exact**; all 7 flags; all 5 anchor nodes with the labels and NPC display names assumed; SPB still carries `loot:"Sealed Recruit Manifest"`; both card battles identical; all six item grants present incl. the Field Ration at `heal:20`; the five representative dispositions verbatim (minus emphasis marks and with contractions expanded); the mix exactly **4 cull · 3 delivery · 2 duel · 2 skill_check**; all three engine edits verbatim. The two hyphenated slugs the author paused to reason about — `commissar-instructor_roshkova`, `pit-master_grimka` — still resolve under an audit rule that has since been **widened** (`npcKeyVocab()` now also accepts `NPC_DIALOGUE` keys and aliases).
>
> **THE HEADLINE — `S.opp.key` IS READ AT FOUR SITES AND WRITTEN AT ZERO, IN 38,712 LINES.** The battle-win handler tallies kills under `if (S.opp && S.opp.key)`. `S.opp` is declared with `adv · condition · cond · tier · hp · maxHp · dmgMod` and **no `key`**; the combat loader assigns the monster key to the other half of the pair, `S.enemy.key`. `git log -S "catKills[S.enemy.key]" --all` (no pathspec) returns **no commit ever** — the guard has been false since the Ally Cat arc's own birth commit `4090c82` on **2026-05-25, 85 days ago**. **Proven in the running game, not inferred** (instrument 70): Playwright, real page, no mocks — `loadWorldMonster(MONSTER_POOL.sparring_droid)`, zero both counters, `_storyBattleVictory()` → `S.enemy.key = "sparring_droid"`, `S.opp.key = undefined`, `monsterKills = {}`, `catKills = {}`. **A full battle victory increments nothing.**
>
> **BLAST RADIUS: 12 quests read a dead counter directly, 19 are transitively unreachable** — §KG corridor **11** (both `quest_kg_01` and `quest_cat_01` gate on `{}` and neither can complete, so each chain severs at its first link), Ally Cat / Layer 44 **7**, §GR La Riva **1**. The La Riva case **corrects §DX-02cm's premise**: `frCatKillCount` and the Vincenzo's Net grant sit under the same `S.opp.key === 'corrupted_cat'` test and are that counter's only writer and that item's only source, so `quest_la_riva_02` is not *"the one quest the engine can complete and cannot pay"* — **it cannot complete.** **Why nothing caught it:** `check:questgraph` collects counters as resources and then calls every `countMin` clause *monotone-satisfiable* because a counter only goes up — true, and irrelevant, since **a counter that never moves is monotone too**; and `kg-quest-chain.test.js` plants `S_story.monsterKills` by hand and proves only that the **reader** honours it. The reader was verified four times over; the writer never once.
>
> **SECOND FINDING — INSTRUMENT 66 AT FULL STRENGTH.** Every §6 figure a single lookup answers is exact: the six `AC·maxHP` products (45 · 96 · 196 · 130 · 208 · 390), the L6 threshold of 5,500, the eleven reward pairs, the type mix. **Every figure the author had to add up is wrong** — quest XP **2,990 → 2,790**, battle XP **2,707 → 2,511** (the counter is cumulative and never resets, so kg_05's one `gladiator_bot` counts toward kg_06's ≥3 and the minimum is ×3, not ×4), total **5,697 → 5,301 — 199 SHORT of L6**, gold **≈550 → 450**. The report even wrote the contingency lever — *"kg_09 400→450, kg_11 500→600"* — which is **+150, reaching 5,451: still 49 short.** It was budgeted against the wrong total, so it could not have worked either. §XP-01 and §XP-02-A later added effort and exploration XP that cover the gap by accident.
>
> **THIRD — A COMMIT THAT ANNOUNCES FOUR GAME-FILE EDITS AND CONTAINS NONE OF THEM.** `3c86055` (*"§KG Inc 3 follow-up: wire SPB→Moscow corridor into walkable map + revert generic monsterKills counter"*) enumerates junctions J16–J21, the counter revert, a `ROAD_RUNS` trim and dropping SPB's loot. **Its diff never touches `play.html`** — the file is byte-identical to `d6aeefd`; only the milepoints *snapshot* carries the reverted text. J16–J21 do not exist at HEAD, so `git log -S "monsterKills"` reports a revert that never happened, and corridor walkability from TLL is an open question.
>
> **FOURTH — the invariant nobody stated is the one that failed.** All seven invariants the report chose to list hold (Free-Movement, API-first, reuse-over-duplication, UQF-only, `_S_DEFAULTS` single-source, no mover/node/monster changes, compact scope). The unstated one — *the chain is reachable and completable* — **failed twice, independently**: activation was dead until §AUDIT-03e's `code = key` backfill on 2026-07-28 (`node.code` was `undefined` at SPB, so the chain *"had never activated in live play"*), and completion is dead today. Also corrected: the report's *"entire mechanical footprint"* claim understated the ship by one engine site (the `'heal'` itemChain allow-list, already §DX-02cj's class) and two support files.
>
> **Filed §DX-02cy 🔴 · §DX-02cz 🟡 · §AUDIT-03bl 🟡 · §DX-02da 🟢 · §DX-02db 🟢**, and **corrected three docs that document the broken counter as live** — `quest.md` §KG (*"is LIVE"* → *"SHIPPED but NOT PLAYABLE"*), `mechanics.md` §Kill counters, and `plan-archive.md` §KG Inc 3 (which repeats the wrong XP arithmetic verbatim). **Evidence:** report **169 → 289 lines / 2,766 → 3,061 words** — measured with `wc`, and it **grew**: the original prose was already tight, so the added mass is verification apparatus (a live acceptance test, a 17-row delta table, a re-derived XP model, five filed defects), while the discursive build-order and voice sections were cut. IEEE-shaped (abstract · intent · method · as-built · delta table · blocking defect · XP re-derivation · invariants scored · defects · verdict); **27 new `symbol@line` anchors** (16 in the report, 11 in the five new rows), `check:anchors` **3534 → 3561 across 86 docs, 0 stale, 0 dead** — counted AFTER the §RESUME entry, which carries none by design (a dead identifier is cited by symbol only, never with `@nnnn`); `check:walk` **16/16 green, no FAIL**; `check:legacycodes` OK. **Docs-only — no HTML, no §7½; the one Playwright probe was a throwaway spec, run and deleted, so the 935/4 (940) baseline stands.**
>

> **⟶ (2026-08-17) — §DOC-02bz ✅ SHIPPED `fcb79dc`: the arc is built exactly as written, and it has never been playable.**
> On "continue", took `docs/lab-reports/lab-report-void-archaeology.md` (`2026-07-07 14:27:21` — re-derived from `stat` first), Layer 52 / §XVII, opening the Void family. 271 → 314 lines but **3,250 → 3,216 words / 21,263 → 20,698 bytes**: the prose is shorter and every added line is a table, a verification ledger or the playability section the original lacked.
>
> **VERDICT: the strongest survival result in the Void family.** Re-ran the report's own §VI.D acceptance test at HEAD by extracting the hook verbatim into a stub DOM — **39 assertions, 39 pass**, including the control run where the pre-fix `WM` key still never completes the arc, both tunnel keys, a no-key negative, and one negative control per gate flag. All **five `_vaSites` site texts are byte-identical to the 2026-05-25 birth commit `194a810`**; **12/12 line citations land exact at `0179687`**, the tree the report was revised on (instrument 108 at the opposite pole to §DOC-02by's uniform +30 — here the numbers are perfect and only the sentences drift); 4/4 quoted strings verbatim; all five sites are primary cells (416/244/172 reproduced).
>
> **THE TWO FAILED CLAIMS ARE BOTH COMPOSED, NOT COPIED (instrument 9/12 again).** (1) `quest_va_04`'s reward is **+500gp** plus a `_legacy_fn` annotation on *Benedikt's Annotated Copy*, not the *"narrative only"* the table says — and `reward:500` is present at the quest's **birth commit**, so it was wrong the day it was written, and `story.md:1640` had it right the whole time. (2) The Constructor's Log has **one** discovery path, not two: `quest_va_02` completes **on** `vaLogFound`, whose only writer is the archive modal, so its `itemChain` can only fire after the item is already held — and `if (s.once !== false && inv.some(i => i.name === s.name)) break;@26176` makes the echo a no-op. Harmless, and the reasoning that produced it is the same reasoning that produced §V row 6.
>
> **§VI RE-DERIVED TO THE TOKEN, AND THE STATED CAUSE IS NOT WHAT HAPPENED.** At birth all five `_vaSites` keys were the logical codes; on **2026-05-29 22:45** `c1d5a94` (*"story books"*) swept four to the new world's codes and left `WM`. The report explains the survivors as *"the site text was re-authored"* — it never was, byte-for-byte, before or after. One missed token in a five-line literal killed the Log, the tunnel, the chain and the fifth ending for **39 days**, while this report's own tables agreed with the broken code. **The class is now fenced by construction:** `check:noderegs` phase 5 reads function-local code-keyed literals and `_vaSites` is classified explicitly (`src/scripts/check-noderegs.js:'_vaSites'@80`) — §AUDIT-03p's blind-spot-inside-the-blind-spot arriving two months late and costing less than the outage.
>
> **THE HEADLINE IS UPSTREAM OF EVERYTHING THE REPORT DESCRIBES.** The arc is unreachable at HEAD for **two independent reasons**. (1) `wmFirstResearcherKnown`'s only writer is the `onComplete` of the quest whose `completion` gates on it — §AUDIT-03au, already filed. (2) `entry42Written` needs `priorQuestMinusOne`, and `S_story.questMinusOne` has **exactly one writer in 38,712 lines: a string literal the Quest −1 disclosure prints for the player to retype into a browser console** (`S_story.questMinusOne = true; storyAutoSave();@31410`). That step is deliberate *for Quest −1*; nothing suggests it was meant to become the root of an NG+ layer and the game's best ending. §DOC-02bg's ship record observed this in passing and no row carried it forward — **instrument 92's third instance: a correct, specific, unfiled observation ages into a live defect.**
>
> **FILED THREE.** **§AUDIT-03bh** 🟡 — the deadlock is a **class**: 23 of 2,853 quests write a flag in `onComplete` that their own `completion` waits on, and for **five** that write is the flag's only writer. The new half is a **four-quest cascade on primary cells with no other cause** — `quest_muffat_02` deadlocks, and `quest_muffat_03` → `quest_signal_01` → `quest_antecedent_01` each gate on the previous one's dead flag (~950gp and five NPC quote branches). Wants a `check:questgraph` phase with the 18 inert members classified explicitly, #13/#14 house style. **§AUDIT-03bi** 🟡 — the console gate above, three options, recommend granting `questMinusOne` on the L20 Void Warlord kill *in addition to* the console line so the ritual survives. **§DX-02cw** 🟢 — `index.md` is in `HISTORY_FILES` in `src/scripts/legacy-codes.js` while `prompt.md` §1 calls it one of three governing files and sends you to its State Fields table, so **the constant reference is gate-exempt by classification, not by blind spot**; found via five Void rows reading *"mark found at CI/SL/DF/WM/MT"*, one of which (`CI`) resolves to a different live node.
>
> **CORROBORATED, NOT RE-FILED (instrument 7 scored four rows):** §AUDIT-03au · §AUDIT-03ah (`if (_e42Dear >= 3) {@34640`) · §AUDIT-03u (the Quest −1 panel's stale literals) · §DX-02m (`S_story.frobergerNoteNode = _ebPool@24043`) · §AUDIT-03x/§DOC-02an (Layer 56, and `quest_tl_01`'s `STN` non-primacy — which makes its deadlock a *second* wall, not the only one).
>
> **DOC SYNC — four corrections in maintained docs, all measured.** `world.md`'s Void Archaeology heading read ⚠️ **PLANNED** while `story.md:1632` read ✅ *Implemented 2026-05-25* — the same contradiction §DOC-02ar repaired one layer up at §XVI, now repaired here. In the same section: `void_architect_seal` occurs **only** as the rumour key `void_architect_seal_inv` (the relic is `Antecedent Seal`); `constructor_log` singular is **0 occurrences** (`readableKey:'constructors_log'`); *"both NG+-persistent"* is false (`_S_DEFAULTS()`'s `inventory: []` wipes them, and persistence is not needed since the arc runs inside the NG+ run that grants them); and the four-author chain was written **First Researcher → Froberger → Benedikt → player**, where the engine's own line makes it **First Researcher → Froberger → the player → Entry 42** with Benedikt as the narrator, not a link. Plus `index.md`'s five Void state-field rows.
>
> **GATES:** `check:anchors` **3,377 → 3,431 across 83 → 84 docs, +54 = 38 in the report** (which carried no `symbol@line` anchors at all before — the whole doc-count rise is this file) **+ 10 in the three new rows (5 §AUDIT-03bh · 5 §AUDIT-03bi · 0 §DX-02cw) + 5 in this §RESUME entry + 1 in `world.md`**, 0 dead, 117 stale = unchanged baseline — one dead anchor caught and fixed on the first run (`Document 4: The Constructor's Log…` — the source escapes the apostrophe, so the literal substring stops at `Constructor`). Rows and this entry counted **after** writing them, per bw/by's twice-repeated miss. `check:legacycodes` exit 0. **`play.html` UNTOUCHED** — the user's uncommitted 2026-08-06 combat recolor still exactly 9+/9−.
>
> **NEXT:** `docs/lab-reports/lab-report-page-generation-quests-waypoints-missionbits.md` (`2026-07-07 20:28:12`). **Confirm with `stat` first.**

> **⟶ PRIOR (2026-08-14) — §DOC-02bg ✅ SHIPPED: 59 of 62 citations exact and six node codes wrong, and the engine's own comments are why.**
> `lab-report-synthesis-part6-quest-arcs.md` (`2026-06-16 14:12:13` — the cross-reference of fourteen quest-arc reports), **470 → 368 lines**, rewritten IEEE-shaped. Re-derived from `stat` first; **seventh of the ten-file 2026-06-16 block, three remain.** Reference build `89fa13b` (12:20:47) — **33,721 lines, the stated baseline exact**; the report's own commit `14d8865` lands at 14:12:54, a **41-second** birth window.
> **What held.** 59/62 line citations exact at the reference build; every monster statline (`scholars_guard@5406` AC 14/HP 45, `hollow_hands_guard@5408` AC 13/HP 22), every flag table, every quest-declaration line. **All five node-code corrections the document itself makes are right** — CQ→CDG, SQ→NUE, SF→STN, `quest_tl_01`→LCY, MM→LIM — as are `crovPitTrainingWins >= 3`, the `atkWhileQuestActive` condition-gate, `taz_devil` in the random pool, 5 of 6 Crown coordinates, and a genuinely subtle catch: `_STAT_ZERO()` is a **reset tool**, not the initialiser, because `_S_DEFAULTS()` inlines the zeroes at 21,237–21,238.
> **THE HEADLINE → §AUDIT-03ba NEW 🟢 — the mechanism behind every miss.** The engine's `//` section comments are written in the **retired node vocabulary**, and a documentation author reads comments. `// ── Layer 50: §XV Entry 42 — blank journal page at CI@34633` sits directly above `if (node.code === 'LHR'@34636`. **22 such comments at HEAD** (8 × `CI`, 6 × `CO`, plus `SQ`/`CQ`/`SF`/`GC`/`MM`). The document names **`CO` six times as a live location** and `CO` was **never** a `NODE_MAP` key — refuted by its *own* citation, since line `19,707` reads `activateNode:'TLS'`. **No gate can see it:** #16 scans `*.md`; every scanner is comment-aware **on purpose** (§AUDIT-03f), so comment-awareness protects the scanner and leaves the comments unaudited. Phase 7 wanted. The engine spells the whole mapping out **once**, at `35,140–35,144`, thirty comments away from the ones that need it.
> **SECOND — `_trackStat` has 0 commits ever; it is `_statTally(key, n)@23915`.** The cited range **21,909–21,912 is exactly `_statTally`'s body**, so the pointer is right and the name beside it came from memory. The cost is the document's **durable authoring rule** — *"`_trackStat` is the only write path"* — which sends the next author to a grep that returns nothing. ***Verifying a line number is not verifying the name beside it.***
> **THIRD — `quest_mimic_colony`: 0 commits ever, "Confirmed" from a comment.** Line 19,767 is `// ── §D02-08 …`; the real ids start on the next line (`quest_d0208_a1@21829`, five acts, `activateNode:'LIM'`). **`quest.md:391` carried the correct mapping the whole time** (`*(design: quest_mimic_colony)*`) — the maintained home doc beating the synthesis, which is the reverse of this program's usual direction.
> **FOURTH — 3 of 3 unverified negatives answerable, every one from a line already on the page.** The Workshop's *"node code unconfirmed"* is `SZG:{ num:80@8811`, **four lines above the `LIM@8815` the very next table row cites correctly**. The Quest -1 pre-boss bug is **real and still live at HEAD** (`when:st => (st.level || 1) >= 20 && !st.questMinusOne@31382`, no battle term) but keyed to `defeatedBattles['CO']` — **0 occurrences ever** — while `defeatedBattles['TLS']` sits ten lines away.
> **FIFTH — instrument 56 again.** *"Covenant Keeper (True)"* transcribes faithfully and **has never been winnable**: `curse <= -6` against `_curseScore()`'s best-possible **−5**. **§ENDING-01 (b) already owns and dates it — NOT re-filed.** The contribution is the consumer side: 18 days after the break, an author read a correct expression and reported it as *"implemented beyond Layer 43 spec."* Its cited line `25,935` is also 50 lines off the real `25,885`.
> **Kept-not-deleted:** `INN | r:128` is the node's **`num:128`** (real `{r:44,c:223}`; it is the only row of six with no `c:`) · the Crown arc is **24 quests, not 18** · *"the 500×500 world grid"* has **no such constant** at either build, though the rule attached to it is right and is kept · **Report 2 repeats `SQ` two pages after Report 1 corrects it**.
> **Corroborated without re-filing:** §AUDIT-03ah (Entry 42's undocumented 4th gate `_e42Dear >= 3`; its six keys re-confirm `auros` and `crov`) · §AUDIT-03u (already extended with the disclosure's four stale literals) · §ENDING-01 · §EPIC-01 · §DOC-02a.
> **Reachability (instrument 54):** `S_story.questMinusOne` has exactly **one** occurrence outside its default and it is **inside a string literal** — the console instruction. So §XVII Void Archaeology entire, plus the fifth ending, is reachable only by typing JS into a browser console; and with §ENDING-01 flooring the curse score, **that console-gated chain is currently the only route to any ending question but the bleakest.** *"Level 21 is undefined. That is not a bug. That is the door."*
> **Gates:** anchors · invariants · dupkeys · nodeindex · noderegs · legacycodes all **exit 0** run individually; `check:anchors` **2,928 anchors across 65 docs, 0 dead** (2,895/64 at bf). `play.html` **untouched** (user's uncommitted 2026-08-06 recolor, 9+/9−, staged around by name).
> **NEXT:** `lab-report-synthesis-part7-writing-design-philosophy.md` (`2026-06-16 14:18:26`) — closes the synthesis series. **Confirm with `stat` first.**

> **⟶ (2026-08-13) — §DOC-02au ✅ SHIPPED: the phases were right and the brakes were prose — every loop cap in the "All limits prevent runaway" table is understated by four orders of magnitude.**
> `lab-report-mega-reweave.md` (2026-06-09, the MegaReWeave pipeline — the nine-phase loop that produced the cascade §DOC-02at measured), **182 → 168 lines**. Re-derived from `stat` first; oldest at `2026-06-09 15:41:00`. Existing-Work-First: tree carried only the user's uncommitted 2026-08-06 combat recolor, **not mine, left untouched, staged BY NAME**.
> **DATING (instrument 18).** mtime `15:41:00`; birth commit **`1bfe7a6` (2026-06-09 17:36)** *"reweave: full pipeline overhaul — progress bars, caching, grid expansion, heat maps"* — the overhaul the guide describes, ~2 h after filing. Server file lived at repo root as `wbapi-server.js` then, not `src/js/` — **`git ls-tree` the commit before assuming a path, or every census reads 0.**
> **THE BEHAVIOURAL HALF IS ESSENTIALLY PERFECT.** All 9 phases (P0–P8), all 5 wither steps incl. the bridge check, all 4 derelict conditions, and **both configuration-table file pointers** verify at `1bfe7a6` — `GEO2` in the server's `reweave-all` handler (21 hits), `PRIORITY_HIGHWAYS` in `src/api/wb.js` (3 hits). `./api.sh audit --map` still live. Only slip: the opening sentence says *"seven phases"* over a nine-row table.
> **THE HEADLINE → 40th INSTRUMENT: A DOCUMENT'S SAFETY GUARANTEES ARE ITS LEAST-COPIED AND MOST-ASSUMED CLAIMS — THE AUTHOR VERIFIED WHAT THE SYSTEM *DID* AND IMAGINED WHAT WOULD *STOP* IT.** The Loop-limits table, closing *"All limits prevent runaway"*, measured against the CLI body that ships the values on every call: **`--max-rip` 5 → `50000`, `--max-fix` 5 → `50000`, `--limit` 100 → `1000000`, `--radius` 6 → `60000` — four rows off by exactly 10,000×**; `MAX_MESH` "city count +10" → `geoCities.length+1000` (100×); `MAX_CLEANUP` 20 → the loop is `for(let dp=1;dp<=2000;dp++)` (100×) **and the identifier has 0 commits in any `.js`/`.sh` file in repo history**; `MAX_WITHER` 20 → `for(let wp=1;wp<=20;wp++)` is correct **but the identifier likewise has 0 code commits ever** — both are hardcoded literals the document dignified with constant names, and their sole appearance in history is this report (`09fa5b2`, a docs sweep). **7 of 8 rows wrong; the one exact row (`--step` 4) is the only one that is not a limit.** All errors one-directional — the system looks safer. **§DOC-02at measured the consequence: 1,684 → 4,429 nodes in seven hours, two OOM fixes, content flat at 384.**
> **CORPUS (instrument 7/9) — a controlled same-day pair.** Same author, same subject, four hours apart. This report's geography is **9 of 10 glosses correct** (`CVP`→Lisbon, `SAM`→Samarkand, `TRB`→Trebizond, `HHL`→Herdholt/Iceland, `HEO`→Lejre/Denmark, `ROM`, `CON`, `FRK`→Paris; only `MLN` off — **Malindi**, not Mombasa, same coast); the sibling's was **7 of 8 wrong**. ***The difference is exactly instrument 9's predictor: here the geography was copied out of a config table, there it was reconstructed from a batch log.***
> **BUT THE RECONSTRUCTED BITS BREAK THE SAME WAY.** Only **2 of the 4** `PRIORITY_HIGHWAYS` entries shown are in the code (`CVP→SAM` ✓, `HHL→GEDI` ✓; `MLN→TRB` and `HEO→TUNPAR` ✗). **`GEDI` has 0 `NODE_MAP` entries at the birth commit and at HEAD** — the shipped array held a highway to nowhere and the engineer wrote the doubt into the note (*"if GEDI exists"*) instead of resolving it; the report copied the entry and dropped the doubt. **`TUNPAR` is a CHIMERA** — not a node, never was, while `TUN` (Tunis) and `PAR` (Paris) are both live: the reconstruction fused two real codes into one imaginary one, and annotated its own destination ***"Denmark to unknown."***
> **ROWS. §DX-02bg NEW (🟡)** — `./api.sh reweave` was not retired with its pipeline: it now issues `PUT /api/roads` → **`build-roads.js --apply`**, an unconditional write, while the guide publishes the bare invocation as *"Dry-run (reports only, no writes)"*; `reweave(pos, flags)` **destructures `flags` and never reads it**, so `--execute` is accepted and ignored. Retired-vocabulary hazard in its most expensive form — a live command that succeeds and writes. **§DX-02bh NEW (🟢)** — `src/tools/worldmap.js:224–227` still buckets regions by the retired 8–500 box-fit bounds (`minLon:-25`) while placement moved to §WALK-1.5's 90×360 full-globe grid; **exactly 1 of 155 `GEO` cities is outside it — `PDL`, Ponta Delgada, lon −25.7, missing by 0.7°** — and `citiesInRegion` filters strictly with no clamp, so it renders in **zero** regions (confirmed live: 0 hits across 19 populated cells). ***Pairs with §DOC-02at's one surviving artifact, the fix that made this overview render at all: `NaN×NaN` fails loudly, `6×6` fails silently.***
> **PLAYABILITY framing the rewrite adopts (user directive):** every node is a place the player can stand, so the pipeline's job was to guarantee all of them walkable from Birka **without littering the map with empty routing stubs**. P7's bridge check — *prune aggressively, never disconnect a named node* — is the load-bearing promise, and it was kept. The elegant idea worth remembering: **let the quests decide which roads exist** (the snail walks every desire path; untouched junctions are scaffolding). What failed was only the machinery meant to stop the loop.
> **Gates:** `check:legacycodes` OK · `check:anchors` **2,618 across 55 docs, 0 dead** (105 pre-existing stale hints, warn-only, none mine) · `./api.sh worldmap --regions` and `reachability` 416/416 run live. `play.html` **untouched**.

> **⟶⟶ §DOC-02as ✅ SHIPPED: the last file in the `2026-06-09 11:06:54` block, and the corpus's best node-code result sits on top of an arc that has never been playable for one minute of its 77 days.**
> `lab-report-wisdom-arc.md` (2026-05-28, §WISDOM-01 — Robert Greene's *Laws of Human Nature* / *48 Laws of Power* as skill-check quest mechanics, the Roen companion arc's second act), **455 → 348 lines**. Re-derived from `stat` first; **the `2026-06-09 11:06:54` block is now EMPTY** — next oldest is `lab-report-node-network-reconnection.md` (`2026-06-09 11:46:48`), a distinct mtime, so the alphabetical tiebreak retires with it.
> **DATING (instrument 18) — the report and the feature are the SAME DAY.** Report `Filed: 2026-05-28`, `Status: Design complete; implementation pending`; birth commit `e339aeb` is **2026-05-28 21:25:08 −0700**. The status line was superseded within hours of filing, which is exactly why the accuracy is so high — this is a spec read by an implementer who still had it open.
> **ACCURACY.** **8 of 8 quest IDs** (`quest_wis_00`–`_07`), **8 of 8 `_S_DEFAULTS()` flag names verbatim**, **4 of 4 unchanged stat/skill/DC triples** (W1 WIS Insight 13 · W2 WIS Insight 12 · W3 INT Investigation 11 · W5 WIS Insight 12), all **3 items** exact on icon/sell/source, the resolution's five effects in order (+600 XP, +400gp, splice Pages, push Complete Laws, knowledge entry), and **3 of 4 open questions answered by the build in the direction the report recommended** (Q1 the VS `storyRender` shadow room, Q2 commentary-in-`desc` only, Q3 `personalLegendMature` got its `KIR` epilogue). It survived the entire §ARCH-01 UQF migration; the OR-group the migration comment claims for both shadow paths is **real** — `_matchCompletionLeaf` puts `flagsAny` + `battles` in one OR-group and AND's the five-flag list against it.
> **NODE CODES (instrument 31) — 6 of 7 recovered by `num`, every label byte-identical: the corpus's best.** `DK`(7)→`LCY` · `SK`(142)→`MME` · `SB`(144)→`GCI` · `AE`(92)→`ATH` · `HL`(14)→`KIR` · **`BK`(25)→`VBY` *Broken Tooth Tavern***. The 7th, `VS`, has no `num` because **it was not a `NODE_MAP` key in the arc's own birth tree** (`grep -c "^  VS:" e339aeb` = 0) — born dead, then re-minted 2026-06-03 by `cb0fc35` as `VS: { num:279@9088` *"Visby Underground — Fence Quarter"*, a label so apt nothing looked wrong. §DX-02q's reissued-code class with a whole arc as the bill.
> **THE HEADLINE — 8 of 8 quests unreachable, and the failure is ONE FLAG, not five accidents.** `wisHookReceived` has exactly one writer, `` `S_story.wisHookReceived = true;@33520` ``, inside `` `if (node.code === 'VS' && S_story.personalLegendComplete) {@33454` ``. `VS` is the **5th of 5** occupants of cell `12,198` (primary `VBY`), so the block never renders, so `wis_01`/`_02`/`_03`/`_05` never list **even though `LCY`/`MME`/`GCI` are all primary and fine**; `_00`/`_06`/`_07` die on the node itself and `_04` on `BK`←`LHR`. **244 cells reproduced exactly** (instrument 14). The signpost is the cruel part: at reachable `KIR`, Roen says *"A library in Visby has a copy. I am going to go find it."* Every word true. **Corroborates `lab-report-kindness-calculus.md` §IV (§DOC-02r), which reached the identical 8-of-8 verdict independently — instrument 7 doing its job before a finding was re-filed as a discovery.**
> **38th INSTRUMENT — A MIGRATION REPAIRS THE REFERENCES THAT *BREAK* AND WALKS PAST THE ONE THAT SURVIVES WRONGLY, SO THE WORSE-THAN-DEAD CODE IS THE ONE THE SWEEP GUARANTEES IT WILL LEAVE BEHIND.** §2.1's table says W4 is at `BK`; §2.2's prose glosses that as *"Birka … a guild city"* and designs a highland-timber deal — **but at the report's own tree `BK` was `name:'bar (Visby)'`, the Broken Tooth Tavern, act 5.** The implementer resolved the code, discarded the prose, and shipped the Mordus/shaman stalemate the same evening (retitled *"The Stalemate Cost"*, INT **Investigation** not History). Then §WALK/§NAV-01 remapped the four codes that broke (`DK`/`SK`/`SB`/`AE`) and **skipped `BK`, because it still resolved** — so today `` `activateNode:'BK', gate:{ flags:['wisHookReceived'] }@13427` `` points at `` `BK: { num:241@9011` `` *"Birka Shore — Northern Longship Landing"*, a beach. ***The report's error came true seventy days later by a mechanism that had nothing to do with the report, and `check:noderegs` is green throughout.***
> **ROWS. §AUDIT-03y extended — one of its nine open `activateNode:'BK'` sites ADJUDICATED:** `quest_wis_04` means `VBY`, unambiguously (its `hint`, `desc`, `failText` and the hub's own tally all say Visby's Broken Tooth), 🟢 no design call — **and the repoint buys more than it costs, because `VBY` is the PRIMARY of `12,198`, so W4 becomes the arc's only standable surface. §DX-02n extended** — `wisArchiveLetter` (the shipped rename of the report's `stoic_letter`, 0 commits ever) is **1 writer, 0 readers**: the Stoic's letter of introduction to the Visby archive is written, saved across reloads and consulted by nothing, and its destination is the room you must already be standing in. **§AUDIT-03ad extended** — same block, second defect: `` `} else if (S_story.wisHookReceived && _allFive && !_p6) {@33485` `` renders the accept/fight choice **only after the other five fragments are done**, while `quest_wis_06` activates the moment `visbyUnderground` is set and its `hint` says *"Use the story panel at VS to choose"* — so the report's §V headline thesis (*"the first arc with parallel fragment collection … any order"*) holds for 5 of 6 and the sixth is silently last. **§AUDIT-03aw NEW (🟢)** — the doc-sync gap: **15 shipped quests across §WISDOM-01 + §ALCHEMY-01 have no row in any maintained home doc.**
> **INSTRUMENT 4 — five names, ZERO commits in the file's entire history:** `birkaAccessed`, `birkaRepImproved`, `stoic_letter`, *"Three Years Out"*, *"Shadow Construct"*. NOT SHIPPED, not retired; kept in Appendix A per program rule.
> **PLAYABILITY framing the rewrite adopts (user directive):** the game's verbs are walk, fight and talk; this arc adds a fourth — **read a person** — and it is the cheapest depth in the file because it requires **zero new nodes**: every fragment is a *second look* at a scene the player already walked through (Dorit's politeness, Keel's omission, the warlord's corner table). Its difficulty curve is narrative coverage, not level. **Making `VS` primary restores 8 quests, 3 items, a companion arc's second act and the `personalLegendMature` epilogue** — second-best content-per-edit ratio the program has measured, after §AUDIT-03at.
> **Gates:** `check:anchors` **2,584 → 2,606 across 55 docs, 0 dead** (105 pre-existing stale *hints*, warn-only, none mine); legacycodes / nodeindex / dupkeys / noderegs / npcregs individually, exit 0, chain never invoked, no detach artifact. `play.html` **untouched** (the working-tree diff is the user's 2026-08-06 HP-bar/playfield recolor, 9/9, unchanged by this pass).

> **⟶⟶ (2026-08-13) — §DOC-02ar ✅ SHIPPED: a 100 % identifier census over an arc that has never been completable past its second quest, and the report is the evidence that the engine has a typo.**
> `lab-report-weimar-scholar-gate.md` (2026-05-25, Layer 51 §XVI — the Weimar Scholar Gate), **209 → 377 lines** — the fifth increment that did not shrink: ~60 lines of unmeasurable design prose deleted, replaced by a 12-row two-way delta table, a verified post-mortem register and live anchors. On "continue", re-derived the queue from `stat`: two files remained in the `2026-06-09 11:06:54` block and the alphabetical tiebreak gives `weimar` before `wisdom`. Existing-Work-First: `git status` showed only the user's uncommitted 2026-08-06 HP-bar/arena recolor (left untouched); no second live session; every claim greped before it was believed.
> **THE ACCURACY RESULT — 42 of 42 named identifiers resolve (100 %), the program's first perfect census**, beating §DOC-02g's 19/20. Byte-exact at HEAD across 80 days and the whole §ARCH-01 migration: all **8** `scholars_guard` fields (`` `key:'scholars_guard'@5406` ``), the drop (`` `icon:'🔏', sell:20@5848` ``), the terrain pool, `` `function _tomeBonuses()@23407` `` and **all three** call sites (`` `const _tomeInit = _tomeBonuses().initiative@7439` `` · `` `_tomeBonuses().deathSave + _kingsSealBonus@7503` `` · `` `_tomeBonuses().atk + _lakeMagicBonuses().atk@25030` ``), **9 of 9** state flags under their specified names, **3 of 3** tome bonuses, both `quest_wm_01` access paths end to end (`` `yealFav >= 1@32289` `` → `` `S_story.archiveLetterObtained = true;@32297` ``), and every quoted Isolde/Benedikt line verbatim. **And the arc stops at 2 of 4 quests.**
> **FINDING 1 → §AUDIT-03at (🟢, one identifier).** `` `const today = S_story.dayCounter || 0;@34741` `` is the **only** occurrence of `S_story.dayCounter` in 38,712 lines and has **one commit in the file's entire history** — `194a810`, the commit that shipped this layer. Never in `_S_DEFAULTS()`, absent from the earliest surviving build, **zero writers ever**. So `today` is `0` forever: the first click pushes `0`, `` `alreadyToday = sessions.includes(today)@34742` `` is permanently true, and the button locks at 1/3 reading *"📖 The circle meets again tomorrow."* — a promise made by a clock that does not tick. `quest_wm_03` never completes and `quest_wm_04` never even **activates**. The engine's real clock is `S_story.day` and `S_story.gameDay` (22 sites) — **and this report names `gameDay`, correctly, twice.** ***The delta table earns its keep here: the spec was right and the implementation mistyped it. Read against HEAD alone the code looks self-consistent and the report looks stale; it is the other way around.***
> **FINDING 2 → §AUDIT-03au (🟢).** `` `completion:{ flags:['wmFirstResearcherKnown'] }@11106` `` against `` `set:['wmFirstResearcherKnown'] }, { kind:'reward', gold:300 }@11107` `` — the flag has **exactly one writer in the file: that quest's own `onComplete`**, and the engine states beside its evaluator that *"the declarative completion gate is the ONLY completion path."* **Born that way** (the ship tree carried `completeFn:() => !!(S_story.wmFirstResearcherKnown)` with the flag set in the same reward block; §ARCH-01 W7c transcribed the loop faithfully into UQF — ***a faithful migration of a broken contract produces a broken contract that now looks modern***). Consequences: the `atkWhileQuestActive` bonus class has never touched an attack roll; **`Marta Eilene Vass` — one occurrence, `` `Marta Eilene Vass — First Tier@27842` `` — has never rendered**, because `` `S_story.wmDoc3Unredacted && S_story.wmFirstResearcherKnown@27841` `` guards it; `` `const _vaReady = (S_story.ngPlusRun || 0) >= 1@31623` `` (Layer 52) inherits the block; and the `` `Read (unredacted)' : '📄 Read'@27850` `` label is unreachable by construction. **Second wall behind the first:** NG+ restores six fields (`` `const savedNgRun@24024` `` and neighbours) and `wmFirstResearcherKnown` is not among them, while `_vaReady` requires `ngPlusRun >= 1` **and** that flag — *two gates in series, each individually sufficient*. The intended writer is written down in the quest's own `` `hint:'Return to the lower archive and read the unredacted personnel file.'@11110` ``.
> **37th INSTRUMENT — A CENSUS PROVES A NAME EXISTS; IT CANNOT PROVE THE ENGINE READS THE NAME THE SPEC WROTE.** 42/42 resolve, reachability is clean (`` `NUE:{r:20,c:191}@9615` `` is the **sole occupant** of its cell, so unlike §CROWN-01 this is *not* a §AUDIT-03x casualty and all five `activateNode:'NUE'` quests activate), node codes are **2 of 2 by `num`** (`SQ`(35)→`NUE`, `CI`(1)→`LHR`) — and none of those instruments can see either defect, because both live in the **writer/reader** relation rather than in the name space or the map. The instrument that found them is: *for every state flag, who writes it, who reads it, and can the writer ever run.*
> **INSTRUMENT 10 AGAIN, ITS SHARPEST FORM YET.** §IV's first "what could be better" bullet — *"there is no UI showing Sessions attended: 2/3"* — is **wrong the day it was written**: `` `reading circle (' + sessions.length@34745` `` renders `(N/3)` and `` `The circle meets again tomorrow.@34744` `` disables the button, **both in the same commit**, twelve lines from the code the bullet describes. And its second bullet contains the disproof of its own conclusion — *"it sets on quest_wm_04 completion, not on re-reading Document 3"* — filed as a backtracking annoyance rather than a loop. ***When a post-mortem names a flag's only writer, go read what gates that writer.***
> **THIRD ROW → §AUDIT-03av (🟡).** `` `label:"Scholar's Quarter — Weimar"@8705` `` is what the player reads on arrival, and the file says *Weimar* **517** times; it says *Nuremberg* **five** times and **four attach to this node**, including the Warrant's Board rumor that recruits them: `` `Isolde in Nuremberg is short three of them@11084` ``. `NUE` is Nuremberg's airport code and the content is Weimar's. Not a dead code — **a live node with two mutually exclusive place names in player-facing strings**, invisible to gate #16 (`.md` only) and to gate #13 (the reference resolves fine). Recommend repointing the four strings.
> **CORROBORATIONS, with a correction. §AUDIT-03ar** is upgraded and re-sequenced: this report **specifies** Dear Friend in three separate places (§II.F, the §II.G reward column, §IV), so it is a spec→shipped delta rather than an ambiguous message — **and the row's premise needs one fix: `quest_wm_03`'s `onComplete` never runs, so Benedikt's favor is 0, not 1**, which makes `set:2` inert until §AUDIT-03at lands (the §AUDIT-03aq/§AUDIT-03ao relationship again). Also verified: Isolde has **no `dearFriend` pool at all**, so the report's "key line at Dear Friend" is her *friendly* tier and `world.md` was already right about her. **§AUDIT-03b** corroborated — `quest_wm_01` carries `npc:"archivus_sweelinck"` while its own disposition quotes Isolde; authoring metadata, inert.
> **PLAYABILITY framing the rewrite adopts (user directive):** the layer adds a **research verb** to a game whose other verbs are walk, fight and talk — its currency is reading order, not damage, and its signature item pays out at a death save, the one moment the player cannot act. What it currently delivers is the first two quests and then a button that says the circle meets again tomorrow and always will. **Four small edits restore two quests, two tomes, an NPC relationship tier, a named reveal and the entry condition for an entire later layer** — the best content-per-edit ratio the program has measured.
> **DOC SYNC.** `world.md` §Weimar Scholar Gate corrected **⚠️ PLANNED → ✅ SHIPPED** (it contradicted `story.md` §Layer 51's *✅ Implemented 2026-05-25*, and the engine's own `→ doc:` comment above `WM_ARCHIVE_DOCS` had propagated the PLANNED label back into the HTML); three NOT-SHIPPED names in that block corrected — `_applyTomeBonuses()` (0 hits; ships as `_tomeBonuses()`), `tome_rasp_annotated` (0 hits), and *"NG+-persistent"* tomes (NG+ resets inventory from `_S_DEFAULTS()`). The §XVII prerequisite line gained the unsatisfiability warning.
> **Gates:** `check:anchors` **2,497 → 2,584 across 54 docs, 0 dead** (105 pre-existing stale *hints* in older docs — warn-only, refreshable with `anchors:fix`; none of them mine); legacycodes / nodeindex / dupkeys / noderegs / npcregs run individually, exit 0, `check:walk` chain never invoked, no detach artifact. `play.html` untouched (the working-tree recolor is the user's, unrelated).
> **NEXT — §DOC-02as:** re-derive from `stat`; **1 file left** in the `2026-06-09 11:06:54` block, `lab-report-wisdom-arc.md`.

> **⟶⟶ §DOC-02ao ✅ SHIPPED: a perfect census, an arc nobody has played, and three surfaces that render only by accident.**
> `lab-report-void-shaman.md` (2026-05-25, Layer 56 §XXI — *The Void Shaman, "The Warden"*), **228 → 392 lines.** Ship commit `194a810` verified. The **direct downstream sibling of §DOC-02an**: this is the arc that consumes the `vsShamanKnown` §AUDIT-03ao proved unwritable, measured from the other end.
> **CENSUS — the cleanest report-rot result in the program.** **20 of 20 identifiers resolve (100 %)**, matching an's record; all five state fields under their specified names in one contiguous block; the monster statline exact in all six numeric fields; both Token descriptions, the intro monologue, both resolution monologues, the sub-clan outcomes, the quest `hint` and `disposition` and Benedikt's whole paragraph **byte-verbatim at 80 days** and through §ARCH-01 W7c and §VM-01-G2's hook migration. **And all four node codes are clean RENAMES of surviving nodes** — `MT`→`` `GVA:{ num:50, code:'GVA'@8733` ``, `SQ`→`` `NUE:{ num:35, code:'NUE'@8705` ``, `GC`→`` `TRD:{ num:26, code:'TRD'@8686` ``, `EG`→`` `TBS:{ num:71, code:'TBS'@8779` `` — `num` preserved on every one. ***The corpus's first 4-of-4 node-code result: previous best was §DOC-02f's single `CI`→`LHR`, and most reports score 0 of N.***
> **REACHABILITY (instrument 19) — 3 of 11 surfaces, and all three arrived by accident.** `GVA` is **alone in its cell** (`GVA:{r:23,c:186}@9641`), so the render node is fine and §AUDIT-03x is not the blocker here. The gate is `` `node.code === 'GVA' && S_story.vsShamanKnown@31696` `` **AND** `vaLastWardVisited`, and the arc holds **one live key and one dead key on an AND gate, which is the same as holding none**: `vaLastWardVisited` is written at `GVA` by `quest_va_03` and is fully reachable, while `vsShamanKnown`'s sole writer `` `set:['vsDebtSettled','vsShamanKnown']@13537` `` needs `vsDebtSettled`, whose sole writer `` `S_story.vsDebtSettled = true;@34913` `` sits inside `if (node.code === 'VS')` — and `VS:{r:12,c:198}` is behind `` `VBY:{ num:25, code:'VBY'@8684` `` in declaration order. So **Layer 56 has never rendered in play and could not have on the day it shipped.** The three surfaces that DO run — the `void_shaman` statline, its `MONSTER_DROPS` table, and Froberger Entry 26 — all arrived in `9df7a2b`, a data-audit commit with no knowledge of the arc. ***The only way to meet The Warden in this game is to walk into Kazrath's sanctum and roll him as a wandering monster: no dialogue, no Token, no mandate, no choice — everything the arc was FOR is the part that does not run.***
> **NEW FINDING → §AUDIT-03aq (🟠).** `` `const qOrder = ['warden_resolved'@26865` `` — three of the town crier's eight quest-news flags (`warden_resolved`, `vs_hollow_seal_taken`, `tl_ori_account_read`) occur **exactly twice each in 38,712 lines**, as their own `TOWN_CRIER_LINES` key and their own `qOrder` member, with **no declaration and no writer in the file's entire history**. All three were born in `4c06f05` *"Layer 62: Town Crier"* on **2026-05-24 — the day BEFORE `194a810` shipped the arcs they describe.** ***The news author invented flag names for arcs that did not exist yet; the arc author wrote different ones a day later; the two commits never met.*** Third–fifth instances of §AUDIT-03ai's inverted write-only class, and the first that are *display* reads, so they fail in silence rather than dead-ending a quest.
> **NEW FINDING → §AUDIT-03ar (🟢, one character).** `` `{ kind:'favor', npc:"benedikt_rasp", set:1 }@11097` `` is the **only** favor writer for Benedikt in the file, and its own sibling `narrative` bit announces *"Benedikt is Dear Friend"* — a tier that begins at 2 (`` `fav >= 2 ? '💛 Dear Friend'@23712` ``). Consequences compound: the message is false, Benedikt's entire `dearFriend` dialogue pool is unreachable in any run (a dead VALUE inside a live field — **§DX-02az's shape, second instance in two increments**), and §XXI's Benedikt callback at `` `_npcFavor('benedikt_rasp') >= 2@34931` `` could not fire even after §AUDIT-03ao lands. ***The detector this yields: compare a `favor` bit's `set:` against the tier its own `narrative` sibling names — the write is correct as data and wrong as intent, so no schema check can see it.***
> **NEW FINDING → §AUDIT-03as (🟡).** `9df7a2b`'s terrain audit added `` `monsters:[ P.goblin, P.hobgoblin, P.void_shaman ]@6362` `` to the sanctum roster, against the report's own §I-C insistence that the Warden and Kazrath are distinct. The same commit fixed `tier:'rare'` → `tier:"hard"` — **an enum value outside the five-member contract that shipped and survived 10 days**, and `dx02g-monster-tier-contract.test.js` names this exact monster in its header as the reason it exists. ***The one wrong data value in an otherwise byte-exact report became the founding case of a CI gate*** — recorded as a CLOSED risk, not a row.
> **THREE DELTAS KEPT.** (a) *"Shard Note #5"* is note **6** — wrong at the report's own tree, though the cited line is exact. (b) The inn dream does **not** depend on `wardensLegacyKnown`; `vaArchitectureKnown` is set from `vaLastWardVisited && entry42Written`, so the fifth ending survives this arc's death intact. (c) `reward:600` beside a `+600` inline grant looks like the §SPARK-01-FU double-pay class and is **not** — `q.reward` `is intentionally NOT read@37050`. **VERIFIED CORRECT, and promoted:** §III-C's `MT_WARDEN` synthetic-code rationale holds exactly (`` `S_story.defeatedBattles[pb.nodeCode] = true@25318` `` runs first) and is now a fenced CI contract, `` `src/scripts/check-noderegs.js:Layer 56 — Void Shaman Warden@111` ``. ***A design decision that a gate now enforces is the best outcome a lab report can have.***
> **DATING (instruments 11/18) — the sharpest citation result in the corpus, and it splits INSIDE one document.** Five inline §II citations (`8070`, `11313`, `11684`, `11709`, `11742`) are **exact at `e594848` (2026-05-25 13:29)** and at no committed tree before it, so the report was written against an uncommitted working tree ~4 h after the 09:10 ship. **Its §V File References table restates the same ten facts and lands at exactly −1 on nine of them** (`4626`/4627, `8439`/8440, `10111`/10112, `14611`/14612, `14723`/14724). ***Two passes over the same facts, four hours apart, one exact — the difference is whether the author was reading the file or reading their own draft.*** Instrument 12 (copy-vs-illustration) with the copy source being the author's own body text.
> **Doc rot found:** `world.md:280` heads this arc **⚠️ PLANNED** while `story.md:1697` heads it **✅** (already filed as §AUDIT-03ak, and this is its sharpest instance); both claim `void_shaman` is in no pool (now false, §AUDIT-03as); `story.md:1707` still says *"rare tier"*; `world.md:288` repeats the Dear Friend error; `world.md:282` gives the Warden *"6 months"* in the tunnel against the engine's eleven years (six months is the age of the carvings); `index.md:714` reads *"Warden located at MT"*. `docs/story/story-arc-investigation.md` — the maintained home doc — is correct and annotated throughout, which is the standing rule holding again.
> **Gates:** `check:anchors` **2,385 → 2,413 across 51 docs, 0 dead** (28 new anchors, all validated through `src/scripts/resolve-anchors.js` before commit — three were rejected on the first pass and repaired: a `src/scripts/`-file anchor needed the file-qualified form, `itemChain` was rejected at 33 hits as *too generic*, and a quoted-prose anchor was not a literal substring. **The §DOC-02f hazard in all three of its known shapes, caught by the resolver rather than by grep.**) `play.html` untouched.
> **NEXT:** re-derive from `stat` — expected `lab-report-wbapi-evolution.md`, **4 left in the `2026-06-09 11:06:54` block.**

> **⟶ (2026-08-12) — §DOC-02an ✅ SHIPPED: a 100 % census beside an arc that has never been played.**
> `lab-report-tilbury-visby-arcs.md` (2026-05-25, Layers 54+55 — the Tilbury Harbor Arc and the Visby Underground, shipped together as a structural pair), **237 → 370 lines.** Ship commit `194a810` verified. **Report-rot is effectively zero — and the arcs are unplayable, which is a different predicate and one nothing in the 2026-05-25 toolchain could test.** Census **32 of 32 identifiers (100 %)**, the strongest in the program; every quoted line of dialogue byte-verbatim at 79 days; `hollow_hands_guard`'s statline, its `Hollow Hands Seal` drop and its `goblin_cave` pool entry exact; all 8 state fields and all 7 quests live as UQF-1.0 through §ARCH-01 Wave 7c; all four NPC profiles under the canonical keys §AUDIT-03n's audit demands.
> **HEADLINE → §AUDIT-03ao: 1 of 7 quests can be completed.** Four independent blockers. **(1)** `tlLedgerRead` is its own precondition — `` `quest_tl_01: { id:'quest_tl_01'@11170` `` completes on `flags:['tlLedgerRead']` and the only write in 38,712 lines is `` `{ kind:'flag_write', set:['tlLedgerRead'] }@11172` `` inside that quest's own `onComplete`; the harbor board the report names as the setter grants the manifest and never touches the flag. **Identical at `194a810` (`completeFn:() => !!(S_story.tlLedgerRead)`), so wrong the day it was written** — instrument 8, and the **second instance of §AUDIT-03ai's inverted write-only class**, which two instances now make a pattern no gate can see. **(2)** three of the four render nodes are non-primary — `STN`←`LCY`, `TL`←`HOR` (13-node cell), `VS`←`VBY` (§AUDIT-03x extended). **(3)** the single live path is §BOARD-01-FU6's `unlock` referral edges, which bypass the gate — so `vsDebtProbed` stays false, and because Yva's NPC **card** is gated on it (`` `TRD:S_story.vsDebtProbed && !S_story.vsWeaponsFound@35146` ``) while her **button** is gated on quest status, ***the button to talk to Yva renders and Yva does not.*** **(4)** `vsShamanKnown`'s one writer sits in the dead `VS` block, so `` `node.code === 'GVA' && S_story.vsShamanKnown@31696` `` never opens and **Layer 56's Warden encounter — the payoff both arcs converge on — has never been playable.** Withheld: 850gp, two readables, one relic, four favor advancements, the Void Shaman reveal.
> **AND THE FIX IS THE REPORT'S OWN FIRST PARAGRAPH.** §I-A opens *"The harbor in Tilbury existed as a node (DK — Harbor Docks)"* — `DK` is `num:7` is **`LCY`, `` `label:'Harbor Docks — Tilbury'@8632` ``, the primary blocking `STN`**; and `VS`'s primary is **`VBY`, `` `label:'Broken Tooth Tavern'@8684` ``, whose node text already seats Warlord Kael Mordus**. ***Against §DOC-02r's verdict on the prosocial arcs, repointing is the right shape here — it is simultaneously reachability-correct and fiction-correct, and needs no design call.*** New test between the two shapes: **does the blocking primary already hold the fiction the blocked node was written for?**
> **INSTRUMENT 4, sharpest node result yet → §DX-02q extended: BORN DEAD, THEN REISSUED.** Neither `TL` nor `VS` was a `NODE_MAP` key at the arc's own ship commit. On 2026-06-03 `cb0fc35` — a worldbuilder import whose message reads *"2 new nodes (KHR/TUN)"* — added **four**, the two unnamed ones being `` `TL: { num:278@9085` `` and `` `VS: { num:279@9088` ``. From that commit the dead guards resolve, gate #13 passes, and §VM-01-G3 (2026-07-28) repaired the surviving symptom in good faith and recorded both arcs *revived* in `world.md`. ***A reissued code is worse than §AUDIT-03y's mis-resolving one: there is no wrong-place prose to catch it on — `VS` even reads "Visby Underground — Fence Quarter."***
> **RECOMMENDATION REGISTER 1 OF 3 SHIPPED, BY SOMEONE ELSE.** The report asked for an in-world signal that Ori had arrived; `quest_tl_02.onComplete` now narrates *"a survivor of the Harrow, three weeks on foot, asking for the Harbor Master by name"* — delivered by §BOARD-01-FU6's referral pass, not by anyone reading the register (26th instrument: the cheap medium ships). The other two verify **correct and still open**. Two smaller deltas kept: both act-number activation legs (`actNumber ≥ 4`, `≥ 5`) were **structurally dead when written** — `actNumber` is `node.act`, constant per node — and Yva's hand-written 50gp gate became **the first price in the game paid through the `cost` opcode** (`` `{ kind:'cost', gold:50, refuse:@34356` ``).
> **ALSO FILED §AUDIT-03ap** (🟢 — the `hollow_hands_guard` trophy at `` `hollow_hands_guard:   { name:'Hollow Hands Seal'@5850` `` shares a NAME with Yva's quest item and grants no flag, while delivery strips by name at `` `filter(i => i.name !== 'Hollow Hands Seal')@34914` ``; the report filed this against itself on 2026-05-25 and it is 79 days open) **and §DX-02az** (🟢 — `vonn`'s `friendly:` dialogue tier has **no favor writer anywhere in the file**: a dead VALUE inside a live field, a shape `check:deadconsts` cannot currently see).
> **Gates:** `check:anchors` **2,294 → 2,385 across 50 docs, 0 dead**; legacycodes / nodeindex / dupkeys / noderegs / npcregs individually, exit 0. `play.html` untouched (the working tree's CSS recolor is the user's, left unstaged).

> **⟶⟶ (2026-08-12) — §DOC-02al ✅ SHIPPED: its drafts died and its ideas shipped.**
> `lab-report-story-codoex-curse-of-knowedge.md` (2026-05-22, the Pinker *Sense of Style* narrative-craft review), **489 → 348 lines.** The **third VOICE-rather-than-tokens report** after §DOC-02ai/aj and the cleanest split of the three. **Of ~26 prose specimens it authored — 7 dialogue hooks, 7 profile expansions, 3 journal entries, 8 act epigraphs, an 8-line quote table, a revised premise — exactly ONE survives (4 %)**, while every passage it quoted as already-existing and marked *keep* is still live. There was no token layer to survive, so the survival rate **is** the voice rate.
> **NAME CENSUS 8/11, and all three failures are its OWN SECTION HEADINGS contradicted by its own body:** *Elara **Voss*** vs body's *Muffat* (engine: `LCY: { name:'Magistra Elara Muffat'@22464`), *Ptolemy **Crane*** vs body's *Sweelinck* (632 hits), *§IV **Finn's** Journal* vs body's *Froberger* (`const FROBERGER_JOURNAL@27184`) — **all three 0 commits ever**. ***Sharpens §DOC-02d's 5th instrument with a direction: the heading holds the invented name, the body holds the real one, and the body wins every time.*** Unlike §AUDIT-03n's split these cost nothing — none reached the engine, which is the difference between a name split in a SPEC (silently corrected) and one in a profile that ships (becomes the schema).
> **THE SURVIVOR CHANGED CHANNEL:** the revised Muffat line is word-for-word minus one clause at `entryNum:14, nodeCode:'LCY'@27198`, **moved from her mouth into the journal as reported speech** (*"Magistra Muffat said:"*). §DOC-02aj's transposition in a second form — there POV, here channel — and both times the channel change is what makes a verbatim grep read zero. **Search for the sentence, not the speaker.**
> **SHARPEST — the passage it PRAISED is byte-exact in DEAD CODE.** Its exemplar entry 41 (*"I didn't tell him I don't have a sibling"*) sits at `NUE: { num:41@22429` inside `const JOURNAL_ENTRIES@22424`, self-labelled *"dead code … superseded by FROBERGER_JOURNAL"*, **0 readers** → **§DX-02n +1** (and the family's first **content orphan** — deleting it destroys authored prose, so re-home first). The live `entryNum:41, nodeCode:'TLS'@27225` is a full rewrite keeping one clause and built on *"**The curse is** not gathering the Shards. The curse is knowing…"* — **the report's own subtitle.** *Its critical READING outlived the line it was reading.*
> **AND THE STYLE SHIPPED, MEASURABLY:** concrete-numeral share of node text **28 % → 62 %**, mean length **166 → 572 chars** (`32c10c5` → HEAD, same extraction both sides), the prescribed move visible on nodes it never named (`label:"Scholar's Quarter — Weimar"@8705` twelve candles / three days; `label:"Aldric's Forest"@8659` every 200 paces / eye height). **Correlation, not proof — no pre-report build survives** (the archive postdates it by two days). → ***30th instrument: a style guide is obeyed by its PRINCIPLES and ignored by its EXAMPLES*** — a prose spec that scores itself by verbatim adoption will conclude it failed and be wrong.
> **ALSO → §AUDIT-03am (🟡, design call):** the retitle shipped **to the starting node**. `story-ci-tagline@31319` on `LHR` reads *"Codex of Conquest: **Curse of Knowledge**"* — the second half being **Pinker's name for a prose defect**, carried into the first line every new player reads, while `index.md`/`README` and 3 engine hits call the game *The Shattered Codex*. **Two titles ship and the player sees the one no document uses.** Recommend dropping the subtitle. *A document's title is not a candidate for player-facing text — a critique's vocabulary describes the author, not the world.*
> **NEXT:** §DOC-02am — re-derive from `stat`; **7 files left** in the `2026-06-09 11:06:54` block, expected `lab-report-tattoo-progression-system.md`.

> **⟶⟶ (2026-08-12) — §DOC-02aj ✅ SHIPPED `7de58fb`: a spec that enumerates names ships its names; a spec that specifies voice ships nothing but its names.**
> `lab-report-saul-paul-vignette-spec.md` (2026-05-27, §FUTURE-01), **361 → 373 lines — and the shape of the change matters more than the count:** 260 lines of reprinted node-text drafts became a 13-row evidence table (the full drafts are one `git show` away and the pointer is in the file), and the space went to a census, four scored delta tables, two findings and a corpus correction. Existing-Work-First: `git status` carried only the user's uncommitted 2026-08-06 combat recolor (18 lines, **not mine, left untouched, staged BY NAME**); oldest mtime re-derived from `stat`; BACKLOG greped before filing, which is what turned two of three findings into EXTENSIONS rather than new rows. **The first back-to-back sibling pair in the program** — §DOC-02ai did the travel reference, this is the writing spec it fed.
> **A genuine pre-implementation lock, and the interval is measurable:** committed **06:46:50** (`7f2715a`), the arc's first two nodes opened **10:01:51** the same morning (`8f8dc2a`, *"Layer 98 §LIX: HR + KS — The Road to Kesra opens"*) — **3 h 15 m**. So this is the cleanest test in the corpus of *what an implementer actually carries out of a spec they are holding*.
> **THE HEADLINE — everything minted as a TOKEN survived; nothing specified as VOICE did.** 6 of 9 quest titles **byte-exact**, 5 of 9 quest ids byte-exact, 9 specified quests → **18** shipped, every beat present. And **0 of 13 node texts** reached the file, because **the point of view was inverted**: the spec writes the player *as* Paul in second person (**88** second-person tokens across §II, the name "Paul" **zero** times); HEAD narrates Paul in third person and reassigns "you" to a **separate travelling companion** — **113 third-person against 22 second-person**, 0 of 13 nodes in pure second person, the tell being *"Paul takes the hit before you close the distance."* ***A token is copied; a voice must be re-enacted at every sentence, and re-enactment is where the implementer's own instincts win.*** **The transposition is visible word-for-word and it EXPLAINS §DOC-02ai's straggler:** the spec's stoning sentence and `` `runs out of Lythros@11417` `` are nineteen identical words with every pronoun swapped — the sentence was **hand-transposed, not regenerated**, and a person-swap has no reason to look at a proper noun. **It was also a real trade, not a mistake:** moving the player out of Paul won the arc **verbs** — 18 quests split **9 `side` / 9 `skill_check`**, and every check is an action only a companion could take (make the opening in the crowd, reach the jailer before the sword, lower the basket). *You cannot roll Athletics to rescue yourself from your own stoning.* → **§FUTURE-01-FU2** (🟢, two lines): the arc's **first** mission card addresses the player **as Saul** — `` `The warrants are in your coat@11312` `` and *"You will not use them again"* — one sentence from *"He leaves at first light."*
> **SECOND FINDING → §DX-02av NEW (🟢): eight completion narratives the engine can never run, and the engine says so itself.** `` `function storyCheckQuests@30166` `` reaches completion only via `` `q.completion && QuestRuntime.canComplete@30187` ``, while skill checks terminate at `` `S_story.quests[questId] = 'done'@6990` ``. Census of all **2,824** UQF quests: **2,454 skill checks, all 2,454 with no `completion` block** (intended), of which **exactly 8 also carry an `onComplete[]`** — 5 in this arc, 3 in §DUNGEON-01 — each already flagged in place by the §ARCH-01 W7c migration. **Nothing is unpaid** (the `bits` chain grants at roll time); what is lost is the closing line, including *"🪨 +100 XP. He got up. Everyone else left. Timael decided to stay."* — the best sentence attached to the arc's best scene, which **no player has ever seen**, and which **misstates its own reward** (`onPass` grants `xp:150`). ***Nobody proof-reads text that never renders.***
> **CORPUS CORRECTION — §DOC-02ai's Thorn finding is WITHDRAWN, and it yields the 28th instrument.** That pass scored `` `Thorn (Permanent)@37676` `` against the travel reference's **Design Index** (*"a mechanical debuff that cannot be removed"*), found a caption with no modifier, and filed it in the §AUDIT-03v/w/y(b) cluster as its first unapplied **penalty**. But the arc has **two specs for the Thorn, same author, same day** — and this one is the **implementation** spec: *"No mechanical penalty (he functions at full capacity)… No tooltip. No explanation."* HEAD ships **4 of those 5 details exactly**, `` `Fishing Guide@13816` `` still resolves 77 days on, and the single deviation is that HEAD supplies the *explanation* the spec forbade. **The Thorn is the arc's most faithful implementation, not a defect.** ***28th instrument: when an arc has both a DESIGN INDEX and an IMPLEMENTATION SPEC, score HEAD against the one that specifies BEHAVIOUR — an index states what a feature MEANS, a spec states what it DOES, and scoring against the wrong one manufactures a defect out of faithful work.***
> **ALSO.** **§FUTURE-01-FU extended** with two fossils and a second straggler: `` `quest_prison_phillam@11449` `` carries fictional *Phillam* in a live quest id, and `` `phillippiJailerConverted@11449` `` is a **BLEND** — *Phillam*'s double-l welded onto historical *Philippi*, a token belonging to **neither** vocabulary and therefore invisible to a grep for either; and *"the waypoint"* (the spec's genericisation of Troas) is still player-facing at `` `the cloak I left at the waypoint@8503` `` and `` `most human line he writes@11502` ``. **That second one is the §V finding:** the object table's *"What it carries"* column is **design rationale written for the implementer**, and it shipped as **narration**, at two sites — ***a spec's analysis column is not inert; if it is well phrased it will be transcribed along with the thing it analyses.*** **Instrument 4 refines §DOC-02ai's diagnosis:** the naming policy was **overtaken, not abandoned** — `Herath`/`Kesra`/`Tarsis`/`Anthos` are RETIRED (3–4 commits each, reverted at `c1d5a94`) while `Aethon`/`Phillam`/`Korath`/`Ephrath` have **0 commits ever**, because those four name later-journey stops authored *after* the 2026-05-29 reversion. **§AUDIT-03x, one more twist:** `` `ATH:{ num:92, code:'ATH'@8487` `` is the arc's one unreachable node of 13, stranding `` `quest_areopagus@11461` `` — **the exact scene Voice Rule 5 nominated as its model** (*"The Aethon speech is the model"*).
> **Gates:** `check:anchors` **2,115 anchors across 46 docs, 0 dead, exit 0** (every anchor validated through `node src/scripts/resolve-anchors.js` BEFORE writing and re-run as written; 102 stale hints pre-existing). `check:legacycodes` exit 0. **The gate opened RED and the two dead anchors were §DOC-02ai's own** — `BACKLOG.md:477` quoted its six repaired anchors as counterexamples **with their `@line` suffixes intact**, which is precisely the §DOC-02f second-order trap (*the gate cannot tell a counterexample from a pointer*) recurring one increment after it was re-documented. Suffixes stripped; gate green. **Two recipe hazards recorded:** a `\b` word-boundary regex over the raw HTML **under-counts every paragraph-initial word**, because `\n` is literal two-character text in a node's `text:` field and `n` is a word character (`\bPaul\b` does not match `…correctly.\n\nPaul has been here`) — three of thirteen censuses came back silently short; and **`git show "<sha>:docs/lab-reports/<file>"` returns NOTHING for any report older than `7d3615a`**, which moved 64 reports in from the repo root — use the **root** path at the birth commit, quoted (zsh reads `$c:r` as a modifier). **Docs only — no HTML touched, so no §7½.** **PROCESS held:** user's uncommitted combat recolor staged BY NAME, never `-A`; hash recorded by a SECOND tiny commit, never an amend.
> **Next: §DOC-02ak — re-derive from `stat`, expected `lab-report-sp4-documentation-sync-pass.md`. 9 left in the `2026-06-09 11:06:54` block.**

> **⟶ (2026-08-12) — §DOC-02ai ✅ SHIPPED `05cfa22`: a world where Saul of Tarsus walks from Jerusalem to Damascus and is healed by a man named Anath.** *(Its THIRD finding — the Thorn — was withdrawn by §DOC-02aj above: that pass scored an implemented mechanic against the wrong sibling document. The first two findings stand.)*
> `lab-report-saul-paul-travel-reference.md` (undated, §FUTURE-01), **1,026 → 247 lines — the program's largest reduction.** The Saul-to-Paul historical/scriptural research: 38 travel "nodes" from Tarsus to Rome. Existing-Work-First: tree carried only the user's uncommitted 2026-08-06 combat recolor (18 lines, **not mine, left untouched, staged BY NAME**); oldest mtime re-derived from `stat`; §AUDIT-03x greped BEFORE filing, which is what caught that `ATH`←`SEA` was already on the books.
> **A NEW KIND OF REPORT — the corpus's first pure SOURCE document.** It names **zero engine symbols** and asserts nothing about `play.html`, so **it cannot be scored for accuracy against HEAD at all**; the delta table had to be inverted to *of the 38 stops it researched, which became world?* **Answer: 14 nodes · 18 quests · 23 state flags · 14 terrains · 13 of 14 reachable · 37 % coverage.** It is also the only report in 107 with **no header block** — no date, no `§` tag, no status, no cross-references — so instrument 18 dated it from its subject (arc birth `8f8dc2a`, 2026-05-27 10:01, *"Layer 98 §LIX: HR + KS — The Road to Kesra opens"*) and its same-day sibling `lab-report-saul-paul-vignette-spec.md`. **Scriptural claims were deliberately NOT adjudicated and the pass says so** — Acts 9:19 does not live in this repo, cannot rot, and is outside the program's competence.
> **THE HEADLINE → §FUTURE-01-FU NEW (🟡): the naming policy shipped half-applied, 75 days live, and the document that authorised it has been deleted.** The vignette spec mandates fictionalized proper nouns from `plan.md §FUTURE-01 Name Translation Table`, and the arc was **built that way** — `git show 8f8dc2a` has `HR:{ code:'HR', name:'herath', label:'Herath — Lower Court' }` and `KS:{ name:'kesra' }` in the live world. At **`c1d5a94`** (2026-05-29, *"story books"* — **the same broad rename commit §DOC-02g caught mis-recording Corelli's history**) **every place reverted to history and every person did not.** 13 of 13 places historical (Jerusalem 48 · Damascus 55 · Antioch 43 · Rome 93 · …); 5 of 5 fictional place names at **0** occurrences (`Herath`/`Kesra`/`Tarsis`/`Aethon`/`Phillam`); 7 of 7 people still fictional (`Barnach` 23 · `Timael` 17 · `Ezzir` 13 · `Anath` 8 · `Silar` 8 · `Lyra` 8 · `Joach` 5); **protagonist historical** (`Paul` 52 · `Saul` 7). `plan.md` was deleted by `5e48dd7`, so **these two lab reports are now the only surviving record of the policy — which is what makes the rewrite load-bearing rather than archival.** One straggler is player-facing and sits in the arc's best scene: `quest_stoning_lystra`'s `passText` still says `` `runs out of Lythros@11417` `` — one occurrence in 38,712 lines, spoken at a node labelled *Lystra — The East Gate*, traceable word-for-word to the spec's Voice Rule 3. ***A verbatim transcription is faithful to its source's RETIRED VOCABULARY, and a rename pass that greps the world will never find the word sitting inside a quest's prose.***
> **SECOND FINDING → §AUDIT-03aj NEW (🟡): fidelity as a cost nobody priced.** The two figures this reference quotes most memorably were implemented as **literal in-game days** — Acts 9:9's three blind days (`blindDaysKS`, one per sleep at `DAM`) and Gal 1:18's *"he abode with him fifteen days"* (`` `if (S_story.hrHellenistDays >= 15) S_story.hellenistsThreaten = true;@36293` ``, one per sleep at `JRS`). `quest_barnach_finds` gates on `hellenistsThreaten` and **it is the hinge** — it opens Tarsus → the Antioch commissioning → the first journey — so **16 of the arc's 18 quests sit behind those fifteen sleeps.** Mandatory floor: **18 sleeps and 84 gold, 37 % of the 49-day doom clock**, for Act 4 content, and **no surface states that the gate is a day count.** **Nothing is broken:** every counter is read, every gate resolves, no test fails. ***A number that is load-bearing in a source is not automatically affordable in the game — fidelity is a budget decision, and this one was never made.*** 3 options; recommend (b), let any sleep advance the counter once `barnachVouchedHR` is set, which preserves the scriptural number exactly for one condition.
> **THIRD — the one section that makes checkable promises, and its flagship is a caption.** The original's Design Index specifies the Thorn in the Flesh as *"a mechanical debuff that cannot be removed — three prayer attempts failed."* At HEAD `` `Thorn (Permanent)@37676` `` renders on the character sheet under `saulConverted`, quoting 2 Cor 12:9, with **no debuff, no modifier and no reader in 38,712 lines** → **§AUDIT-03v/w/y(b) cluster extended, and this is its FIRST member that promises a PENALTY rather than a reward**, so the wanted detector must scan unapplied *costs* as well as unpaid rewards. Roman citizenship — the document's *"key mechanical asset"*, three invocations — is **0 % shipped**; tentmaking is one loot item and a workshop scene; the 13-NPC companion roster shipped **5 of 13**; the hostile roster **2 of 7**. The Rejection Pattern, the one thing the original called mechanizable, **did** ship, four times.
> **ALSO.** **§AUDIT-03x — cheapest instance identified, and it needs no design call.** `ATH`←`SEA` has been one line in that row's casualty list since §DOC-02r/§DOC-02z; measuring it found the two nodes are **ten lines apart in the SAME authored block** (`` `SEA:{ num:97, code:'SEA'@8477` `` before `` `ATH:{ num:92, code:'ATH'@8487` ``), so **a declaration-order swap fixes it without touching the locale design** — unlike every other member, nothing wants a sea node and a city sharing a cell. Bill: `` `quest_areopagus: { id:'quest_areopagus'@11461` `` cannot activate, so the arc's intellectual climax never fires, plus an unreachable `sleep:true` checkpoint, plus a player who walks to Athens arriving at *"The Inner Sea"* on dry land in Achaia. **§DX-02n +1:** `` `S_story.silarJoined = true;@22534` `` is **write-only** — one writer, zero readers — so the player recruits a named companion (Silas, the source arc's second-journey partner) that the engine never consults; the `ebReturnsCompleted` shape, green under any round-trip test. **Method note worth keeping: when a stop cannot afford a node it can still afford a sentence** — Thessalonica's single occurrence in the whole file is inside the Corinth workshop quote (*"Some go to Thessalonica"*), which preserves the geography the node map dropped.
> **Gates:** `check:anchors` **2,068 anchors across 45 docs, 0 dead** — but only after a repair, and the repair is the durable lesson: **`grep -cF` is NOT a sufficient pre-check for an anchor; `node src/scripts/resolve-anchors.js` is.** Six anchors were `grep -cF`-verified unique and verbatim and the gate still called them dead — `Saul of Tarsus fell.` and `The road northeast runs out of Lythros` (line suffixes stripped here: the gate cannot tell a counterexample from a pointer, §DOC-02f), both real substrings of real lines. The resolver's own normalisation rejects some multi-word fragments that `grep` accepts (`of Tarsus fell.` and `runs out of Lythros` resolve; `Saul of` and `The road northeast runs` do not). **Refines the §DOC-02f discipline: shortest verbatim fragment, and validate it through the GATE'S resolver, not through grep.** One of the six also carried markdown bold inside the anchor (`**Lythros**`) — the §DOC-02f hazard recurring exactly as documented. All six repaired. legacycodes / nodeindex / dupkeys / noderegs / npcregs run individually, all exit 0. No HTML touched — docs only, so no §7½.
> **Next: §DOC-02aj — re-derive from `stat`, expected `lab-report-saul-paul-vignette-spec.md` (this report's own sibling, which would make it the first back-to-back pair in the program), 10 left in the `2026-06-09 11:06:54` block.**

> **⟶⟶ (2026-08-12) — §DOC-02ae ✅ SHIPPED: the roadmap that said "none of these can be started in this session," and shipped one of them thirty-three minutes later.**
> `lab-report-ponies-unicorns-aspirations-future-ideas.md` (2026-05-24), **528 → 282 lines** — the project's aspirational register: a DM's Companion Guide, a Fishing Guide, a Mission Explorer, and (appended six minutes after the file was born) a Polyphonic Pipe Organ. Existing-Work-First: tree carried only the user's uncommitted 2026-08-06 combat recolor (9 lines, **not mine, left untouched**); oldest mtime re-derived from `stat`; and **three siblings already own three of its five subjects**, so §DOC-02m (fishing), §DOC-02ad (the organ) and §DOC-02i (the archive constants) were cited rather than re-derived — instrument 7 doing its job before a measurement was spent.
> **CENSUS — nine of nine world constants EXACT at the archive `32c10c5`** (76 nodes · 8 acts · 6 named NPCs · 370 monsters · 66 terrains · 41 Froberger entries · 7 Codex Shards · 107 `_S_DEFAULTS()` fields · 14,377 lines), independently corroborating §DOC-02i's measurement of the same tree. **19 of 20 named NPCs resolve (95 %)**; the one that does not, `Leeuwenhoek`, has **0 occurrences and 0 commits ever** in the game file and lives only in the planning docs of the period. At HEAD: 416 nodes, 204 profiles, 398 monsters, 111 terrains, `SHARD_GOAL = 7@36175` unchanged, `function _missionComplete() {@23648` still exactly the twelve bits the DM's Guide proposed to explain.
> **THE HEADLINE — the document's own scheduling claim was falsified by its own next commit.** §IV ends *"None of these can be started in the current session."* Timeline: `1d1b064` **18:47:49** report born with that sentence → `13bb222` **18:54:19** §VI the organ appended (six minutes) → `030c446` **19:20:35** `5thOrgan.html` **shipped**, drawbars, ADSR, oscilloscope, voice stealing and its own IEEE report (thirty-three minutes). The closing status block — *"Implementation status: ✅ Implemented"* — now sits eleven lines below the sentence it refutes, **and both are still in the file**. ***A roadmap's estimate of its own tractability is a claim like any other, and it is the claim most likely to be wrong within the hour.***
> **SECOND → the 26th INSTRUMENT: IN A ROADMAP DOCUMENT, THE DELIVERABLE'S MEDIUM PREDICTS SHIPPING FAR BETTER THAN ITS STATED EFFORT OR PREREQUISITE.** §IV ranked the Explorer HIGH/blocked-on-an-architectural-decision and the Fishing Guide MEDIUM/blocked-on-§XII. The HIGH one shipped in **5 days** (`edit.html`, `2d42ea2`) with the decision **dissolved rather than made**; §XII shipped and the MEDIUM document never did. Scored by medium instead: **code/data 3 for 3** (Explorer · organ · the Fishing Guide as an in-game item), **prose 0 for 2** (DM's Guide · Fishing Guide as a document), 80 days. This repo converts a spec into an implementation the same day; it has no pipeline that converts a spec into a book. **Corollary worth acting on: the DM's Guide is more likely to ship as game surfaces than as pages** — the Explorer's NPC/Quest/Mission tabs are already two-thirds of its Parts II and IV, rendered from live data instead of transcribed, and a transcription would have rotted.
> **THIRD — the Explorer shipped INVERTED on all three axes it reasoned about.** (1) §III.D excluded Create/Update/Delete because *"a write interface would need validation, undo, and conflict resolution"*; HEAD ships **29 `POST` · 32 `PUT` · 4 `DELETE`** — the objection was answered by **building** the validation (422-with-field-list, verify-or-revert), not by avoiding writes. (2) §III.E enumerated exactly two data sources — externalize the consts into `codexofconquest-data.js`, or a FileReader drag-and-drop — and **the winner was outside the enumeration**: a local server parses the HTML as text and serves REST, so `play.html` still has **0** `<script src=` tags and **the architectural cost the report priced was never paid**. The 22nd instrument in its cleanest positive form: *a two-option table reads as exhaustive precisely because it is a table.* (3) §III.C's debug metadata is the one axis where the report is right and HEAD is not → **§DX-02ao NEW (🟡)**. Plus **§DX-02ap NEW (🟢)** — `edit.html:Full curl sequence (run in order)@7969` hands the author runnable raw `curl`, the exact shape prompt.md §3 forbids, invisible to §DX-02l-FU's detector because that one scans `.md` only.
> **FOURTH — INSTRUMENT 12, and it defeats the older instrument-9 heuristic outright.** §III.B's four ASCII detail panels are the most concrete-*looking* passages in the file — box-drawn, monospaced, JS paths in the right-hand column — and **0 of 14 field names are correct**: `.die`/`.dieCount`/`.mod` vs live `dmgDie`/`dmgCount`/`dmgFlat`; `tier:'low'` outside the five-value vocabulary; `WORLD_DB[].epic` = 0 occurrences; and **`goblin_scout` · `street_rat` · `pickpocket` · `fav_yael` · `quest_yael_escort` · `yaelEscortDone` all 0 commits ever**. The two survivors are the two that could be **copied** — `yaelEscortUsed` (a real `_S_DEFAULTS()` field, 13 hits) and the XP formula. ***The tell is not whether a passage looks like data; it is whether the data existed to be copied when the passage was written.*** In May 2026 the Explorer did not exist, so there was no panel to transcribe — and drawing one requires names.
> **FIFTH — the aside outperformed the section.** §V offers, in one paragraph, *"one elegant possibility: the Fishing Guide does not need to be a separate document"* — ship it as a readable item. That is what exists: `name:'Fishing Guide', icon:'📖', type:'readable'@13819` granted by Q-BAIT-00, with `const FISHING_GUIDE_TEXT =@26659` behind it, and it **gates a mechanic rather than just flavour** — `const hasGuide = (S_story.inventory@30406` reveals zone DCs. The 20–30-page document it was an aside to never shipped. Corroborated from §DOC-02m without re-filing: `const FISH_POOL = [@26504` is 20 ranks exactly as specified (+ `const NIGHT_FISH_POOL = [@26526`, 5, §XLVIII), Hooked is live, the tournament shipped with **six** ranks not five (`const TOUR_TITLES = {@26722`), `BAIT_FISH_POOL` has **0 commits ever** while the apex predator shipped as `name:'Horned Shark'@5511`, and **`YL`/`YC` were RIGHT WHEN WRITTEN** — archive `YL:{num:75, yugurt_lake}` → `BOO:{ num:75, code:'BOO'@8782`, `num`/terrain/label all preserved. §FISH-01 re-derived independently: `LYR:{ num:41, code:'LYR'@8723` (act 7) is declared 59 lines before BOO (act 3) in the same cell, so `isFishingLake:true@8782` never reads at a node the player can stand on.
> **SIXTH — a caution for whoever writes the DM's Guide Part V.** It specifies documenting *"all four endings with the specific score conditions that trigger each."* At HEAD **three of the four cannot fire** (§ENDING-01: `_curseScore()` has closed form `20 + 2 × EB bosses`, floor 20, because its `ebReturnDone` term is written only through a quest id `QUEST_DB` does not hold — §EPIC-01). ***The Guide's stated prerequisite was "the game must be content-complete"; the real prerequisite is a two-site rename.***
> **Also recorded:** §VI is the highest-fidelity spec in the corpus — `src/sources/5thOrgan.html:const N_HARM   = 6;@142` · `src/sources/5thOrgan.html:const MAX_VOX  = 12;@143` (72 oscillators, exactly the §VI.H budget), `src/sources/5thOrgan.html:function midiHz(m)@185` byte-exact, `src/sources/5thOrgan.html:drawbars:  [1.000, 0.500@161` literally §VI.C's `stopMixer`, `src/sources/5thOrgan.html:const DBAR_LABELS = [@359` the §VI.B stop-analogy column promoted into the UI, `src/sources/5thOrgan.html:attackMs:  10,@163`/`src/sources/5thOrgan.html:releaseMs: 200,@164` to the millisecond, and `src/sources/5thOrgan.html:const MOTIF = [@147` byte-exact to §VI.E's JSON in all four fields (only `ch` dropped). **What did not ship is the entire input layer** — `requestMIDIAccess` 0, the string `MIDI` 0, no JSON parser, no channel/manual/pedal split, no meantone or Pythagorean temperament — so *of the two input paths the report ranked, the winner is the one it ranked last and did not name: embed the sequence and skip the file format.* And **§DX-02am is explained here, not duplicated**: §VI.C's `1/n` was *correct as written* because no falloff control existed yet; `src/sources/5thOrgan.html:falloffDB: 6,@162` was added later, on top, and the product is 1/n².
> **Gates:** `check:anchors` **1785 → 1850 across 40 → 41 docs, 0 dead, exit 0** (all 31 report anchors verified literal + unique + line-exact + under the char cap by a scripted pre-check BEFORE writing; the 13 pointing into `src/sources/5thOrgan.html` and `edit.html` are **file-qualified**, which the §DOC-02ad pass proved is mandatory — a bare anchor into a non-game file is simply dead); legacycodes/nodeindex/dupkeys/noderegs/npcregs exit 0, run individually, chain never invoked, no detach artifact. 102 stale line hints pre-existing; `anchors:fix` NOT run. **Docs only — `play.html` untouched.**
> **Next: §DOC-02af — re-derive from `stat`; expected `lab-report-prompt-migration-arena-to-prototype.md`. 14 left in the `2026-06-09 11:06:54` block.**

> **⟶⟶ (2026-08-12) — §DOC-02ad ✅ SHIPPED: a finished instrument, a silent game, and a default registration that is a Flute wearing a Principal's label.**
> `lab-report-Polyphonic-Organ-Synth.md` (2026-05-24), **573 → 491 lines** — `5thOrgan.html`, a Web Audio additive pipe organ. **The corpus's first report whose subject is a different file entirely**, so the whole pass ran against `src/sources/5thOrgan.html` (448 lines, ship commit `030c446`) with `play.html` as the *consumer* under test. Existing-Work-First: tree carried only the user's uncommitted 2026-08-06 combat recolor (9 lines, **not mine, left untouched**); oldest mtime re-derived from `stat`. **Note a gap found on the way in: §DOC-02ac shipped as `348f5d0` but never wrote its §RESUME entry** — its full record does live in the §DOC-02 track row, so nothing is lost, but the last session ended before step 10 completed.
> **First measurement answered the first question: the deliverable had moved.** The report names `5thOrgan.html`; `ls` at the root fails; `5e48dd7` filed it under `src/sources/`. Four commits touch it, none since.
> **CENSUS — 42 of 55 identifiers (76 %), and everything transcribable is exact.** The `MOTIF` array byte-exact, `src/sources/5thOrgan.html:const MOTIF_LEN@157` = 28, the ADSR algebra including `V_peak = velocity × 0.065` at `src/sources/5thOrgan.html:vel * 0.065, when + atkS@211`, the release start `src/sources/5thOrgan.html:Math.max(when, atkEnd)@246`, the +20 ms oscillator stop `src/sources/5thOrgan.html:osc.stop(stopAt + 0.02)@250`, the oldest-first `src/sources/5thOrgan.html:voices.reduce((a, b)@203` (quoted verbatim in the original), the reuse guard `src/sources/5thOrgan.html:slot.gen !== myGen@253`, all four MIDI frequencies, the Audio EQ Cookbook coefficients, the architecture diagram, the 170-node budget, and **all ten rows of the parameter table against the live controls**. Of the 13 that do not resolve, 11 are correctly framed as future work; the other two are `ROUND_OFFSET` and `LOOKAHEAD`/`SCHEDULE_INTERVAL`, named as constants but shipped as inline literals with the right values.
> **THE HEADLINE → §AUDIO-01 NEW (🟡, design call). The game is silent and the instrument built to fix that has been finished for 79 days.** `play.html` has **0** occurrences of `AudioContext`, `createOscillator`, `BiquadFilter`, `AnalyserNode`, `new Audio`, `<audio>` **and `iframe`**; its 119 hits for *"sound"* and 21 for *"music"* are narrative prose, every one. The sibling spec is explicit that this was a game feature — *"driven by a sequencer that reads a note file and plays it like a player piano, **in the background, as the game is played**"* — and the synth's own conclusion says *"ready for embedding in `play.html` as an iframe."* Neither happened, and the repo reorg then placed the deliverable among the authoring tools, which quietly settled it. ***The architecture is still the right one for this repo: an additive organ is the only music design that survives the single-file, no-build, no-assets invariant, because the score is an array of integers and the instrument is arithmetic.*** Three options filed, do not build before the call.
> **SECOND → §DX-02am NEW (🟢), and INSTRUMENT 7 IS THE ONLY THING THAT COULD FIND IT.** `src/sources/5thOrgan.html:function harmAmp@188` is exactly as specified and the falloff term measures exactly right (1/n^1.00 at 6 dB/oct, 1/n^1.99 at 12). But `src/sources/5thOrgan.html:drawbars:  [1.000@161` is **already** 1/n and `src/sources/5thOrgan.html:falloffDB: 6,@162` multiplies on top, so the shipped default is **1/n² to within 0.8 %** — a **Flute**, while the report and its preset table both call it **Principal**. The sibling spec supplied those drawbar values with *"gain proportional to 1/n… sounds like a principal organ stop"* — **correct, because at that stage there was no falloff control at all.** ***A parameter added on top of a completed specification silently re-signs every default written against it*** — §DOC-02ab's magic-number lesson in a second subject, and unreachable from either document alone.
> **THIRD → §DX-02an NEW (🟢), two defects found by SIMULATION rather than by reading.** (a) `src/sources/5thOrgan.html:function buildSeq@277` bakes absolute seconds into every event and is called at **one** site, while `src/sources/5thOrgan.html:seqStart += (gRO()@314` re-reads the DOM every loop — so a mid-play BPM change moves the loop period and **never the event times**, the exact inverse of the documented *"BPM changes take effect on the next loop iteration."* Measured at 108 → 216: **262 of 512 events (51 %) fall into the past and fire in one 50 ms tick**, 131 simultaneous `noteOn` against a 12-slot pool. (b) The report's collision proof reads *"Track 1 MIDI notes {67, 63}; Track 2 MIDI notes {65, 62}"* — **both tracks play the same `MOTIF`**; what separates them is phase, and phase is a slider with range 1–56. Simulated across all 56: **safe only at 8–20 and 36–48, so 30 of 56 collide**, and `src/sources/5thOrgan.html:v.startTime > best.startTime@266` releases the newest match, swapping the two notes' durations. The default 14 is safe — **and the report's own test step 5 instructs "Set Round Offset to 1", the worst value in the range**, while step 6's offset 0 is unperformable (`min="1"`).
> **FOURTH — §DOC-02g's shape in its purest form yet.** §XI listed seven *"Verified behaviors"* in audible terms (*no clicks*, *smoothly brightens*, *loop transition is seamless*) and §XIII.C then states *"no browser available in this session."* Both cannot hold; the structural verifications are sound and re-confirmed, the audible ones have no basis. ***The more limiting statement was the reliable one, and it arrived two sections too late to correct the first.*** Related: the 12-voice pool **cannot be filled by the shipped demo at any offset** (two monophonic tracks, peak ~2 voices), so *"voice stealing occurs at full 12-voice texture"* describes an event the sequence cannot produce — though the pool size is right for the repertoire the sibling's budget table justifies.
> **INSTRUMENT 12, and the sharpest single instance in the program:** §IX holds **ten table rows copied off the markup and exact, then four preset registrations — *Principal · Flute · Reed · Full Organ* — with no mechanism at all**, no buttons and 0 occurrences of the four names, under one heading. Three more composed errors: the filter is **−0.21 dB** on the motif's highest partial (2352 Hz) at its 5000 Hz default, so it does nothing until ~2.4 kHz; *"removes aliasing artifacts"* is impossible for a pure `'sine'` oscillator; the Taylor bound is **6.9e−3 at π, not ~10⁻⁶** (it holds only to |x| ≲ 1.5); and the proposed `genMotif`'s `& 0xFFFFFFFF` coerces **signed**, yielding negative array indices on 2 of the first 4 iterations → `undefined` → `NaN` frequency.
> **Docs synced:** `index.md` × 3 — the Content Files row (root path → `src/sources/`, plus the not-wired-in note), the Writing & Design Philosophy row (verification summary + the three new §IDs), and the cross-ref row (adds the originating sibling spec).
> **Gates:** `check:anchors` **1695 → 1762 across 38 → 40 docs, 0 dead, exit 0** — all 40 report anchors are **file-qualified to `src/sources/5thOrgan.html`** and were verified literal + unique + line-exact + under the 80-char symbol cap by a scripted pre-check BEFORE writing. ***Two anchor hazards worth keeping: the `@line` must sit INSIDE the backticks (`` `sym@142` ``, not `` `sym` @142 ``) or the gate cannot see it at all — a silent non-anchor, not a dead one; and a file-qualified prefix eats 22 of the 80 characters, so the shortest-verbatim-fragment discipline gets stricter, not looser.*** 102 stale line hints pre-existing; `anchors:fix` NOT run. **Docs only — `play.html` and `src/sources/5thOrgan.html` both untouched;** the user's uncommitted 2026-08-06 combat recolor still sits in the tree and is **not mine — leave it.**
> **Next: §DOC-02ae — re-derive the oldest mtime from `stat`; expected `lab-report-ponies-unicorns-aspirations-future-ideas.md`, which is this report's own originating spec (§VI) and now has three §IDs pointing back at it, 15 left in the `2026-06-09 11:06:54` block.**

> **⟶ PRIOR (2026-08-12) — §DOC-02ab ✅ SHIPPED: the schema shipped and scaled 35×, the script shipped 0 of 120 lines, and the ending it was written to earn now accuses the player who earned it.**
> `lab-report-npc-dialogue-system.md` (2026-05-22), **579 → 692 lines** — Layer 42: the NPC dialogue system (World Truth · 4-state speech · the Groundhog Day ending mechanic). Existing-Work-First: tree carried only the user's uncommitted 2026-08-06 combat recolor (9 lines, **not mine, left untouched**); oldest mtime re-derived from `stat`. **The report PREDATES the earliest surviving build `32c10c5` by two days**, so instrument 8 applied throughout and the archive — not HEAD — is the reference for *"was this ever true."*
> **CENSUS — 19 of 24 identifiers (79 %), and a clean split between container and contents.** SHIPPED: `const NPC_DIALOGUES = {@10396` with the exact `meta` + four-pool schema, grown **6 → 213 entries / 144 → 1,614 quotes**; `function _getNPCDialogue(npcKey) {@23560`'s cycling selector, `return { quote: pool[count % pool.length], meta: d.meta, fav };@23639`, **unchanged in 82 days**; `function _missionComplete() {@23648` **byte-identical to the archive except two node codes**; the four-way ending nest with both specified thresholds; and §VI's Disposition proposal at **201 quests**. NOT SHIPPED: `renderNPCDialogueCard` + its three CSS classes (0 each), `weckmannPitTrainingDone` (0), **and the entire dialogue script — all 120 lines scored mechanically against HEAD *and* the archive, 0 present in either**, plus 0 of 6 World Truths verbatim and 4 of 7 character names with 0 commits ever (`Yael Stormhook`, `Brynn Fenn`, `Tomas Quill`, `Crane`). ***The exact inverse of §DOC-02e's "100 % of its prose survives and 0 % of its field names do" — the corpus now holds one clean instance of each direction.***
> **THE FINDING — the Groundhog Day mechanic is INVERTED, and it accuses the wrong player.** The report's thesis is that the TRUE ending is reserved for whoever did the acts with no output. That nest shipped exactly. But `function _curseScore() {@28191` reads `ebReturnDone`, written only by `function _storyEbReturnBeat(ebCode) {@30358`, reachable only through `const returnId   = 'quest_' + ebCode.toLowerCase() + '_return';@35865` — an id `QUEST_DB` does not contain (**§EPIC-01**). Floor 20 ⇒ **Covenant Keeper ⛔ · Capable Warden ⛔ · Imperfect Covenant ⛔ · `} else if (curse >= 15) {@28257` ✅ always.** So the player who befriends all six Birka NPCs, reads the journal, trains at the pit and stays for Quill's song is told, word for word, *"you couldn't slow down long enough to catch them."* **The mechanic still works. It is pointed the wrong way.** Third independent reproduction of §ENDING-01 and the first from the dialogue side.
> **NEW MEASUREMENT → §ENDING-01 gains a FOURTH surface: the mission ledger itself.** `noHighCurse: _curseScore() < 10,@23660` and `allEbReturns: Object.keys(NPC_DIALOGUES).length > 0@23656` are **both pinned false** by the same cause, and `sealedVoid` is a **tautology at both call sites** (the victory screen only renders once `defeatedBattles['TLS']` is set). So `>= 8` of twelve is in play **7 of 9 genuine choices** — the design's stated slack, inverted.
> **SECOND → §DX-02n (b)'s ORIGIN.** `ebReturnsCompleted` was *specified*, by this report's §IV, as a mission bit; the engine wrote it and built the reader against the sibling `ebReturnDone`. ***A dead field usually has a document behind it, and the document usually reads as a requirement — so `grep` the lab reports for a field's name before deleting it.*** Plus two shapes in the same twelve lines: a **dead GUARD** (`Object.keys(NPC_DIALOGUES).length > 0` over an immutable non-empty literal — always true, invisible to a reader/writer census because both operands are live) and `journalHalf@23657`, a bit named *half* that asks for 9 of 41 (**22 %**).
> **THIRD — this document is §AUDIT-03n's root cause at industrial scale.** Six of seven characters are named at least two ways *inside it*, and **the ambiguity resolved four different ways in one table**: `crov` keeps the key and the flag was renamed `crovPitTrainingWins@23654`; `auros` keeps the key and the flag keeps the surname (`bruhnsDepthsReported`); `quill` keeps the key and the flag keeps the surname **misspelled** — `couperiSongReceived`, **a typo that reached `_S_DEFAULTS()` and therefore the save format, un-renameable without breaking saves**; and `pachelbel`, where the **flag's** name won and became the key. ***An ambiguous name is not a coin flip you lose once; it is a coin flip you lose again at every site that has to spell it.*** Its gentlest instance: "Deacon" survived not as a person but as `const DEACON_CODE_TEXT =@27546`, a rulebook now owned by the character who replaced him.
> **FOURTH — a magic number re-signed its own contract.** The report's `0/1/2/3` favor states shipped as three values, and §V's *"World Truth footer at favorability 2 (Friendly)"* is live as `if (fav >= 2 && dlg.meta && dlg.meta.worldTruth) {@23762` — **byte-faithful to the literal and one whole tier stricter than the spec meant**, because 2 now means Dear Friend. (The engine later added an `enemy` footer at fav ≥ 1, restoring the two-stage reveal by another door.)
> **Kept, deliberately:** all 120 unshipped dialogue lines stay verbatim in §VI under a banner — §DOC-02p's rule, *a report holding the only copy of authored content is an archive, not a stale claim.* That is why this increment GREW; the prose around them was cut hard.
> **Checked and NOT filed** (existing-work-first): `meta.name` on 21 of 213 entries is **correct by design** — only the 10 profile-less NPCs need it and all 10 have it (§NPC-01-SF2 exactly covered); `quest_drunk_fight` is still dangling but is already §DX-02o.
> **Docs synced:** `docs/story/story-arc-npc-dialogues.md` — 6 corrections (the stale `line 7204` + *"120 lines"* scope claim → 114 of 1,614 across 6 of 213; *"20 quotes, 5 per state"* → 19, the dearFriend pools are 4; **three dead quest ids** in the Intro Quest column — `quest_yael_escort`/`quest_pachelbel_goods`/`quest_auros_depths`, 0 occurrences each — repointed to the live ids in `const npcQuests = { yael:['quest_slums_cleanup','quest_city_watch_patrol']@23642`; *"Dear Friend+ (level 3)"* → fav 2). Its dialogue **transcript itself was verified accurate against HEAD** — *the maintained home doc tracked the code and the lab report did not.*
> **Gates:** `check:anchors` **1622 → 1670 across 36 → 38 docs, 0 dead** (all anchors verified literal + unique + line-exact by a scripted pre-check BEFORE writing); legacycodes/noderegs/nodeindex/dupkeys/npcregs exit 0, run individually, chain never invoked, no detach artifact. 102 stale line hints pre-existing; `anchors:fix` NOT run. **Docs only — `play.html` untouched.**
> **Next: §DOC-02ac — `lab-report-npc-speak-sdk.md`, 18 left in the `2026-06-09 11:06:54` block (re-derive from `stat`).**

> **⟶ PRIOR (2026-08-12) — §DOC-02y ✅ SHIPPED: three companion arcs almost perfectly intact, and an engine comment that denies eight node records while quoting their labels.**
> `lab-report-narrative-arcs-brynn-bruhns-yael.md` (2026-05-25), **178 → 374 lines** — Layers 70/72/74: Brynn's lamp at the inn, Bruhns's confession before the last fight, Yael's named report. Ship commit `43610d3` (07:06). **Third increment to GROW** — the source is short design prose and the growth is a 15-row delta table, a reachability closure and six findings. The archive `32c10c5` predates the feature by nine hours, so instrument 18 applied.
> **CENSUS — an unusually faithful implementation.** **4 of 4 scene consts live**, **5 of 5 new state flags live under their specified names on one contiguous line** (`brynnKeeperStoryTold: false, brynnLightChoiceMade: false, brynnLightKept: false,@23117`), and **3 of 3 trigger predicates VERBATIM** after 79 days and a total coordinate migration — Beat 1 `if (node.code === 'TLL' && _npcFavor('brynn') >= 1@32815` + `actNumber >= 2`, Beat 2 at `>= 2`, and Yael's triple `if (node.code === 'LHR' && _npcFavor('yael') >= 2@32489` + `actNumber >= 6` + `yaelEscortUsed`. **All four cited line numbers exact**, dating the document to the window `585be8f`…`0a131f5` (2026-05-25 13:12–13:35) — **the same afternoon tree §DOC-02w's Luck report resolved into.**
> **THE FINDING → §AUDIT-03af SCOPED AND ANSWERED, and it is the sweep that row asked for.** `const birkaNpcs@35139` closes *"**No NODE_MAP entry ever existed for CI/IN/TV/BA/CY**"* and §NPC-01-SF4 adds *"…for CQ/SQ/GC either."* **Eight claims; SEVEN ARE FALSE** at `32c10c5` — all seven are real `NODE_MAP` entries; only `CQ` holds. ***And the comment transcribes those entries' labels — "The First Inn", "Birka Tavern", "The Rough Bar", "Neon Undercity" — character for character off the records the same sentence denies.*** **But every remap is a CORRECT RENAME** (`num` + terrain key + label triple-match: `IN`→`TLL` 2, `TV`→`MHQ` 3, `BA`→`LLA` 4, `CY`→`HKG` 6, `CI`→`LHR` 1, `SQ`→`NUE` 35, `GC`→`TRD` 26) — §DOC-02f's *"the node survived, only the key was renamed"* across seven pairs. **So the verdict is the OPPOSITE of §DOC-02u's `LJ3`: fix the comments, keep the remaps.** ***A migration comment's "never existed" is the least reliable sentence in a codebase — the one claim the migrator had no reason to check, because the remap already worked; its danger is the STANDARD it sets for the next remap, which may have no label to match on.***
> **SECOND FINDING → §AUDIT-03x EXTENDED, and a CHANGE OF UNIT.** Yael's patrol addendum shipped, was remapped correctly (`nodeSlug:'MSY'@27733`, gated on `yaelNamedReportDelivered`) — and `MSY:{r:25,c:206},@9672` is the **9th of 12** occupants of the program's largest casualty cell, behind `WG0`. §DOC-02h scored that cell for §CROWN-01 at 7 stranded; the full list is `WG0 · HW1 · HJ1 · HG1 · HJ2 · HN1 · HJ3 · HCA · MSY · SDQ · OTP · DBN` — **11 of 12 stranded, from FOUR independent tracks** (`SDQ:{ num:16@8664` The Crones' Domain, `OTP:{ num:62@8761` Trench Titan — an **§EPIC-01** battleground — and `DBN` Danube Ferry Crossing). ***A cell-primacy casualty is not an arc-level event: the unit of damage is the CELL, not the feature, so scoring §AUDIT-03x arc by arc undercounts it by construction.*** Closure totals **416 / 244** reproduced (instrument 14).
> **THIRD — this report is the PRIMARY SOURCE that made §AUDIT-03n's repair correct, and nothing connected them.** §3.1's flat *"Character note: Commander Seraphine Bruhns and Commander Auros are the same character"* is exactly what justifies HEAD's `const fav = _npcFavor('auros');@34994`. The report's own spec says `_npcFavor('bruhns')` — **0 occurrences**, one of §AUDIT-03n's five surname-keyed gate sites — and without the repair `fav` is 0 and **Bruhns says nothing at the last fight, for every player, forever.** §AUDIT-03n found it 64 days later from two disagreeing `npcOrder` lists. ***A corpus can hold the answer to a defect in prose and no gate can read prose — that is the argument for the delta table, not because prose is unreliable but because it is unindexed.***
> **FOURTH — instrument 13, and the engine wins again.** The report specifies a four-branch Act VIII farewell on two flags with *"one inert branch… unreachable by design… the dead branch costs nothing."* HEAD ships a **three-flag priority chain with zero inert branches** whose first branch is a **catch-up delivery**, so the player who never found Beat 1 still gets the lamp at the door and the epilogue reads truthfully. Scored against the report's own thesis (*the arcs close because the characters close them*), the shipped shape serves it better than the spec. → **§DX-02ae** for its one hazard: that branch writes `brynnKeeperStoryTold` from inside a `text: () => {…}` getter, safe only because `_renderNpcCard` latches first.
> **FIFTH — instrument 12, 15th consecutive.** Transcribed material exact; composed material carries every error. **Three identifiers with 0 commits ever**: `fav_brynn` (in the report's **opening sentence**, as one of three favor accessors), `NODE_ARRIVAL_QUOTES` (the named injection point) and **`TC_BRYNN_LAMP` — whose FEATURE shipped**, under no const name at all, inside `TOWN_CRIER_LINES.quests`. Plus §6's *"eight state flags"* against five in its own tables (eight only if you count `fav_brynn`), two paraphrased Yael choice labels, and §4.2 praising the *"intentionally passive voice"* of an epilogue line that is active and begins with Yael's name (`Yael kept three copies@28117`) — instrument 10.
> **REACHABILITY (instrument 19): 3 of 4 surfaces render.** `TLL` alone in `10,204` ✅ · `LHR` `list[0]` ✅ · `TLS` alone in `26,181` ✅ · `MSY` ⛔. Both gate flags close cleanly (one writer each: `S_story.yaelEscortUsed = true;@23798`, `S_story.s29LineDelivered = true;@32670`). ***And note WHY it survived so well: the layer added no nodes, so the migration that stranded eleven nodes in one cell had almost nothing of it to catch — structural minimalism turned out to be a survival trait.***
> **Also filed/extended:** **§AUDIT-03s +2 player-facing hits** — `YAEL_NAMED_REPORT_SCENE.setup` says *"from the **CI** door"* and `const S29_AUROS_THEORY@27050`'s comment says *"fires at **CY**"*; `CI` is the worse, resolving to a **different live node** so every existence check passes. **§AUDIT-03z (b) independently re-confirmed** from a new direction — that same comment still states the trigger as `fav_auros >= 2`, a field whose only occurrence in 38,712 lines is the comment itself.
> **Gates:** `check:anchors` **1445 → 1500 across 32 → 33 docs, 0 dead** (55 report anchors, all verified literal + unique + line-exact by a scripted pre-check BEFORE writing and re-scanned after — 0 rejected, 0 drift); legacycodes/nodeindex/dupkeys/noderegs/npcregs exit 0, run individually, chain never invoked, no detach artifact. 98 stale line hints pre-existing; `anchors:fix` NOT run. Docs only — the uncommitted 2026-08-06 user combat recolor left untouched.
> **NEXT:** §DOC-02z — re-derive the oldest report from `stat` (expected `lab-report-naval-campaign-layer.md`; **20 left** in the 2026-06-09 block).

> **⟶ (2026-08-12) — §DOC-02u ✅ SHIPPED `6ab2a08`: a near-perfect implementation of an arc that no player can enter — the program's FIRST 100 % casualty.**
> `lab-report-littoral-courts.md` (2026-05-28), **230 → 302 lines** — §SIREN-01, the Four Courts of the Littoral Sea: four Ladies who each weaponise one word (BUSY · MAYBE · FRIEND · SOON), three sea crossings, the Overseer in the fog, and a three-flag betrayal ledger. Head of the 2026-06-09 mtime block, re-derived from `stat`. **Second increment in the program to GROW** (§DOC-02g's class — the source was already short and dense, so the reduction is in the four restatements of the manipulation thesis and the growth is a 14-row delta table plus seven findings).
> **The archive PREDATES this report by four days**, so instrument 18 applied and the reference is the arc's own birth commit **`42c2f82`**.
> **CENSUS — one of the most faithful implementations measured, and it does not matter.** All **5 quests survive the §ARCH-01 UQF migration byte-exact — 25 of 25 fields**: `quest_aurel_tide: { id:'quest_aurel_tide'@11560` WIS Insight DC 12 → `aurelTideRead`/`betrayalThought`/150xp, and the same for Calice (INT Investigation 13), Mireille (CHA Persuasion 14), Solen (WIS 13, `onFail:[]`) and the Overseer (WIS 15, DC matched to the MM p.349 charm save). All **10 state flags** in the specified order in one contiguous block (`// §SIREN-01: Littoral Courts@23181` … `solenSoonRead: false, littorialComplete: false,@23185`); all **6 NPC `quoteFn` state machines**; both panels live.
> **THE FINDING → §AUDIT-03x extended, the FIRST 100 % CASUALTY.** `const CELL_GRID = (() => {@9852` strands **6 of 6 surviving nodes**: `LC1`–`LC4` and `LSO` are the 3rd–7th occupants of cell `32,203` (17 nodes, `list[0]` = `SEA`), and `LCA:{r:35,c:213},@9812` is 2nd of 2 behind `CI2`. **0 of 5 quests can activate** (`function _uqfActivateAtNode(node) {@30137` keys on `node.code`), and `{ id:'story-lca-close', nodes:['LCA'],@31350` cannot render — **the arc cannot even end.** Every prior casualty left something standing (§CROWN-01 24 of 34 quests, §DOC-02r 28 of 51); this one leaves nothing. Closure totals **416 / 244 / 172** reproduced exactly (instrument 14). ***And `quest.md` marks all five "✅ LIVE" — true of `QUEST_DB`, false of the game, which is the sharpest argument yet for §DX-02w.***
> **SECOND FINDING → §SIREN-01-FU NEW: the ocean was deleted by a commit about memory allocation.** The arc's thesis is *"The Ladies never fight. The ocean does."* All four junction nodes and all three crossings — `Sea Spawn × 2`, `Deep One × 3`, `The Serpent of the Passage`, **byte-exact at birth to the report's own table** — were removed **2026-06-10 by `a61d6eb`**, subject *"fix: P5 batchEditNode O(M) rewrite + P6.5 DELTA key crash"*, body describing only an O(N×M) allocation rewrite and a `DELTA['N']` key-casing crash. **Nothing about world content.** Every surviving node is `battle:null`. And it **cannot be reverted**: the crossings were `junction:true`, now `check:invariants` **I2**. ***Durable lesson: a deletion that is correct as TOOLING and catastrophic as CONTENT will not describe itself in the commit message — read the diff, not the subject line.*** No gate saw it; the nodes were removed cleanly.
> **THIRD FINDING → §AUDIT-03af NEW, and it is §DOC-02g's rule in live form (2nd confirmed instance): THE ENGINE'S OWN COMMENT IS WRONG ABOUT THE PAST, AND CAUSALLY SO.** `§VM-01-G1-FIX: originally keyed to 'LJ3', a dead@31341` claims `LJ3` was *"a dead node code no NODE_MAP entry ever carried"* that *"had NEVER rendered"*. At `42c2f82` the file carries `LJ3:{ num:117, code:'LJ3', name:'junction', label:'The Serpent Passage', act:4 …}` — a real entry holding the arc's hardest battle — so the panel **did render, for 13 days.** `LJ3` was **RETIRED**, not born dead. The false diagnosis licensed remapping the navigator *teaser* onto `{ id:'story-lso-trigger', nodes:['LSO'],@31345` — **the fog bank it exists to point at** — so it now announces *"something in the water to the east"* to a player standing in it. ***A dead node code is usually a RETIREMENT, and diagnosing it as born-dead licenses a remap a retirement would forbid.***
> **INSTRUMENT 18, and a rare clean result: this report was written FROM THE FILE.** *"~21,200 lines at session close"* vs `42c2f82` = **21,242** — exact within its own tilde, and the **first baseline in three increments to match a real commit** (s and t both described uncommitted trees). §II-A's **10 of 10 coordinates exact at birth**, *"gap = 4, within probe range"* included. **Zero fabricated identifiers.** Instrument 12's gradient never appears — there is nothing composed here to be wrong.
> **Two smaller results.** (a) **The misspelling became the schema:** `littorialComplete` (sic) is live in `_S_DEFAULTS()` and both LCA `quoteFn` branches; the report spells it that way throughout, including the `lp` command in its own header, while the arc, every label, all five home docs and its own title say *Littoral*. Persisted to `localStorage`, so renaming breaks saves — §DOC-02d's lesson in field-name form. (b) **All five deferred sync items CLOSED by measurement** (§VI marked maps/story/world/quest/index ⚠️ PENDING) — and `world.md:493` already annotates *"`LJ3` (historical — no live node)"*, §AUDIT-03m working on this very arc. Third increment to find a residual list stale in the "already shipped" direction.
> **Also confirmed not-a-defect (existing-work-first):** the five `npc:"lady_aurel"`-style keys resolve nowhere else, but per **§AUDIT-03b** `q.npc` is authoring metadata only — no row filed. And `checkFailFlag` is **RETIRED** (3 hits, all in migration comments), but the comment that replaced it records the whole translation including *"solen_horizon had no checkFailFlag → onFail:[]"*, independently confirming the report's §II-B note 71 days later — **a migration that writes down what it mapped to what is why the quest table could be scored 25/25 instead of guessed at.**
> **Gates:** `check:anchors` **1238 → 1263 across 28 → 29 docs, 0 dead** (27 report anchors, all verified literal + unique by a scripted pre-check BEFORE writing and re-scanned after; 0 rejected). legacycodes/nodeindex/dupkeys/noderegs/npcregs exit 0, run individually, chain never invoked, no detach artifact. 98 stale line hints pre-existing; `anchors:fix` NOT run. Docs only — the uncommitted 2026-08-06 user combat recolor left untouched.
> **Next: §DOC-02v — re-derive from `stat` (expected `lab-report-loot-drop-system-v2.md`; 24 files remain in the 2026-06-09 block, 87 reports overall).**

> **⟶ (2026-08-12) — §DOC-02s ✅ SHIPPED `f622342`: the strongest survival the program has measured, fully reachable — and the arc's own stated payoff is the one thing that never shipped.**
> `lab-report-la-riva-grief-arc.md` (2026-05-26), **432 → 309 lines** — Layer 78 "La Riva" (the grief arc at Fishmonger's Row) plus the distributed vignette layer, the romance architecture and the hour-counter wiring. Head of the 2026-06-09 mtime block, re-derived from `stat`.
> **CENSUS: 43 of 46 identifiers resolve (93 %), every quoted string verbatim after 78 days, and the arc is FULLY REACHABLE — the first §DOC-02 increment in six to find no §AUDIT-03x casualty.** `AMS` is **alone** in cell `17,184` (unconditionally `list[0]`) and `CDG` is `list[0]` in `21,182`; every gate flag has a live writer. Exact: all four state fields on one line in the specified order (`connieMet: false, fishmongerRowRestored: false, laRivaComplete: false, frCatKillCount: 0,@23122`), the 2.5 s activation delay (`S_story.quests['quest_la_riva_01'] = 'active';@25377` behind `S_story.catKingDefeated = true;@25373`), `countMin:[{ path:'frCatKillCount', min:5 }]@13795` + `itemsAll:["Vincenzo's Net"]`, `reward:500`, `corridor:true@31928` at `count:1`, both items at `sell:0`, Vincenzo's Net `description` **byte-identical**, Kenickie's *"Yeah. Okay. I'll hold onto this."* byte-identical, all six `const NPC_ROMANCE_PREAMBLES = {@27479`, all four quoted Froberger fragments, all three rewritten quest dispositions, `const ROMANCE_QUOTES@22379` = **21**, `const FROBERGER_JOURNAL = [@27184` = **41**.
> **Instrument 8, SIXTH consecutive result — `FR` was RIGHT WHEN WRITTEN.** At the birth commit `dcb72cb` the file carries `FR:{ num:79, code:'FR', name:'ruins', label:"Fishmonger's Row", act:1, W:'CQ' …}`; §WALK/§NAV-01 renamed it to `AMS:{ num:79, code:'AMS'@8801` preserving `num`, terrain key, label and node text, and deleted the compass fields. **`FR` is still absent from `docs/maps/node-index.md`'s LEGACY CODE MAP while `CQ`/`DF`/`CY` are all in it** — the §AUDIT-03q blind spot §DOC-02m already recorded; this report is that mapping's origin document.
> **THE FINDING → §GR-FU (🟢 no design call): the arc raises Kenickie to Dear Friend and the ending cannot name him.** `{ kind:'favor', npc:'kenickie', set:3 }@34414` clears the `>= 2` dearFriend threshold at the arc's final beat; `const SWEELINCK_NAMING_LINES = {@27237` holds six keys (`yael · brynn · quill · pachelbel · crov · auros`) and not his. Act V states the purpose outright — *"The Covenant Keeper ending names each person helped by name… To confirm that the things they were carrying were seen"* — so **the whole five-act structure resolves on a naming line that was deferred in 2026-05-26 and never written** (its own suggested text has 0 occurrences). **Compounded by §ENDING-01:** that ending is itself unreachable, so the missing line sits behind a screen no player can see; fixing §EPIC-01 surfaces this rather than resolving it.
> **Second defect → §DX-02x:** `S_story.npcFavorability['aldo_sardino'] = (S_story.npcFavorability['aldo_sardino'] || 0) + 1;@31907` bypasses `function _setNpcFavor(key, level) {@23462` — the absolute, only-ever-raises setter that emits the tier line — **while the same arc one node away uses the VM `favor` opcode and its comment cites that helper as canonical.** One arc, two nodes, two mechanisms; the §DX-02t "right on every line it duplicates, wrong in what it omits" class.
> **THREE OF FOUR DEFERRED ITEMS CLOSED, and one closed BETTER than specified.** §XI proposed a `ruins` → `partial_market` terrain swap for `fishmongerRowRestored` and marked it deferred; what shipped is `textVariants:[{flag:'fishmongerRowRestored'@8802` — a node-text swap reading *"Two of the three blocks are still rubble. The third has a market in it — one stall…"* — so **the flag went from write-only to read**, `partial_market` has 0 occurrences and was never needed, and the resolution stays *witnessing, not restoration*. NG+ Entry 42 also shipped (loop verified 2026-07-07); Entries 17/29 hold.
> **Hour counter — 3 of 5 rows stale, ALL RETIRED, none never-shipped** (instrument 4): `storyConfirmSleep` +8 / reset exact (`@36300`/`@36301`); `storyShortRest` exact; **battle** +1/+1 shipped at battle **start** (`function _storyRollInit() {@24624`), not victory; **`storyQuestHunt` RETIRED** by §TIMELESS-01 (`7952752`, 3 commits, born `e594848`); **`storyMove` RETIRED** by §WALK/§NAV-01. And the report **understates itself**: it calls the 16h/24h thresholds display-only, but `0) >= 24) {@25046` now imposes combat **disadvantage** at 24h awake, surfaced in the pre-battle warning — the counter became a mechanic.
> **CORPUS RULE — NUMBERS FROM AN UNCOMMITTED TREE (instrument 18, 3rd refinement).** The report's baseline *"18,324 lines at session close"* and its two Vinnie line citations (7857, 15265) are mutually consistent and match **no commit**: `213d14b` (14:00) is 18,130 with the lines at 7855/15250, and the session's own `dcb72cb` (16:49) is 18,462 with them at 7953/15448. All three describe a working tree that was never committed. ***Score such numbers UNVERIFIABLE rather than wrong, and never use them to date a report — use the birth commits of the things it describes.*** (One report, three build moments: La Riva `dcb72cb`, the romance layer `9684ff6` 10:39, `INN_DREAMS` `28dae66` two days earlier.)
> **House-move note, and the §DOC-02n precondition applied in BOTH directions:** §III's five-act prose was **compressed to a design table** because `story.md §GRIEF AND CORRUPTION` holds the prose verbatim and marks it *"✅ Implemented 2026-05-26"* — but the per-act **object function and gap analysis** were **KEPT**, because `story.md` does not carry them and the report is their only copy.
> **Next: §DOC-02t — re-derive the head of the 2026-06-09 block from `stat` (expected `lab-report-leveling-flashbang-condition-economy.md`). 89 reports remain.**

> **⟶ (2026-08-12) — §DOC-02q ✅ SHIPPED: a shop that works exactly as written, and a statistics ledger that forgets everything at the one boundary it was built to survive.**
> `lab-report-kenickie-chronicle.md` (2026-05-25), **227 → 218 lines** — Layer 75 (§XL Kenickie's Black Market at the Cat Quarter) plus Layer 77 (§XLII the Chronicle, a dual run/career statistics ledger). Head of the 2026-06-09 mtime block, re-derived from `stat`. Modest reduction (the source was already short and dense); the substance added is a 32-row two-way delta table, a recommendation register and three findings.
> **Both systems POSTDATE the earliest surviving build (`32c10c5`), so instrument 8 could not adjudicate them — their four birth commits did, and all four fall on the report's own date.** `4090c82` built the Ally Cat arc; `cab8865` (10:16) the 5-field chronicle; `e1c91e8` (10:20) a **4-row** game-over summary; `aef1650` (10:35) the 10-field version, `_STAT_ZERO`, and the **Hit rate** row. The report describes the 10-field shape, so **it was written after every commit it recommends changes to** — which is what makes its recommendation register measurable.
> **§XL: 16 of 19 claims exact, and it is REACHABLE.** All 7 Kenickie dialogue lines byte-identical since `194a810`; the four shop items exact on price (18/28/45/135), effect (`catchBonus:2` · `catchBonus:3`+`sizeUp` · heal 10 · heal 25) and both flavour strings; bait stacking matches on `name` **and** `type==='bait'`; the shop is an in-place expansion via `cqDiv.insertAdjacentElement('afterend', shopDiv);@33071`, not a modal, exactly as specified. **The one risk the report registered did not fire** — `const POTION_TIERS = {@24306` still prices the same two potions at 50 and 150, so the hardcoded 45/135 are still exactly 10% under after 79 days. **Instrument 8, fifth consecutive result:** `CQ` was a real `NODE_MAP` key at `4090c82` (`num:77`, `cat_quarter`, Jimmy Two-Tails, the Beefy Tom battle, the Tiny Fedora) and was renamed to `CDG:{ num:77@8798` preserving all of it; `CDG` is declared before `LIM`, `FRK` and `FRS` in cell `21,182`, so it is `list[0]` and **not a §AUDIT-03x casualty.**
> **THE FINDING → §CHRON-01 (a), no design call.** `function storyNewGamePlus() {@24001` preserves six named fields and then calls `Object.assign(S_story, _S_DEFAULTS())`; **`careerStats` is not among them, so New Game+ zeroes the career ledger** — the exact transition the report gives as its whole rationale (*"a player on their fourth run is a different player than one on their first"*). The **respawn** path is correct (`const _survivingCareerStats@23925`), so the bug is one path wide. **Born broken:** the same six-name list is in `cab8865`, the chronicle's own birth commit. **And it hides itself** — `ngPlusRun` survives while the ledger does not, and the character sheet blanks the career column when `const isFirstRun = (cs.deaths || 0) === 0;@37728`, so an NG+3 player reads a sheet byte-identical to someone who has never died: no error, no empty column, no zero. Fix is one line. **§CHRON-01 (b), small writing call:** `_statTally('sleeps', 1);@36297` and `_statTally('daysAdventuring', 1);@36298` are adjacent, unconditional and are each field's only writer, so the two are permanently equal — and both are printed, on both surfaces. Only `S_story.gameDay = (S_story.gameDay || 0) + 1;@36271` is genuinely a separate counter.
> **NEW CORPUS RESULT — a report's RECOMMENDATION REGISTER can be stale on the day it is written.** Of four *"What Could Be Better"* items, **two had already shipped**: the *"add a This Run group to the character sheet"* request is answered by the two-column `This Life` / `All Lives` sheet in `cab8865`, four commits earlier; and *"attacksAttempted and attacksHit are tracked but no hit rate is computed"* is answered by `['Hit rate',@23861` in `aef1650` — **the same commit that created `attacksAttempted` and `attacksHit`.** The recommendation asks for a feature that could not have existed one commit earlier and did exist in the commit that gave it inputs. §DOC-02j's inversion at a finer grain: *read against HEAD alone, both rows look like live gaps.*
> **Instrument 12 at its cleanest, and the report refutes ITSELF twice.** Everything copyable is exact (7/7 dialogue lines, 4/4 prices, 4/4 effects, 10/10 stat field names, both discounts, the `has-data` mechanism, the respawn semantics); every error is composed. **`_trackStat(field, amount)` has 0 occurrences and 0 commits ever** — the writer is `function _statTally(key, n) {@23915`, which the report never names, though its architectural claim about that writer (one function, no direct mutation) **holds exactly across 14 call sites**. The quoted `_S_DEFAULTS()` block shows `careerStats: _STAT_ZERO(),`; the file has `careerStats: { kills:0@23146`, a ten-field literal, at birth and at HEAD — **the duplication the factory was adopted to prevent is what shipped.** The ten row labels score **3 of 10** against a nine-row surface. And twice the transcribed half is right while the summary is wrong: §I-A calls `quest_cat_05` the Cat-King fight when it is the **Don Fluffissimo** fight (`title:'Tommy: The Cat-King Cometh'@13758` is `quest_cat_06`, and both titles are identical at `4090c82`) while §IV reasons correctly from the bait flavour that the Don dies there; and §I-B calls Kenickie *"a single-state encounter"* with no favorability while the profile it describes had `impartial`/`friendly`/`dearFriend` **at birth**, its `dearFriend` line being the arc-completion voice the report says does not exist. `{ kind:'favor', npc:'kenickie', set:3 }@34414` has since made him a Dear Friend for real.
> **Also filed: §DX-02n +1, and a shape worth naming** — `kenickieMarketUsed: false,@23120` has three occurrences (declaration, `S_story.kenickieMarketUsed = true;@33056`, nothing else) and the greeting variation it gates has **0 commits ever**: *a write-only flag whose absent reader was specified in prose*, which is precisely the member a human reviewer waves through because the report defends choosing it.
> **Next: §DOC-02r — re-derive the head of the 2026-06-09 block from `stat` (expected `lab-report-kindness-calculus.md`). 91 reports remain.**

> **⟶ (2026-08-12) — §DOC-02p ✅ SHIPPED: the origin specification of the Curse Score, implemented almost perfectly, describing a system that has not been able to run for 75 days.**
> `lab-report-game-story-codex-of-conquest.md` (2026-05-22), **453 → 333 lines** — the narrative architecture for the full story arc: Pinker's Curse of Knowledge as theme, Froberger's journal as a mirror the player reads in fragments, a derived **Curse Score** computed from Epic Battleground return behaviour, and two ending variants. Oldest remaining report by `stat`, re-derived per the §DOC-02 ordering rule.
> **Census: 10 of 11 named identifiers live under their specified names (91 %), and the transcription is the cleanest a NARRATIVE document has produced in the program.** `function _curseScore() {@28191` is this report's §VI pseudocode **term for term and threshold for threshold** — `(startedNotReturned * 3) + (neverStarted * 1) - (allComplete ? 5 : 0)`, `const allComplete = returnsComplete === 20;@28203`, `curse <= 0` → Covenant, `curse >= 15` → Cursed — reading exactly the two containers §X named (`const started = !!(S_story.quests[primaryId])@28197` or a defeated battle). **17 of 17 quoted journal entries are byte-identical** to `const FROBERGER_JOURNAL = [@27184` after 82 days; **all four `SWEELINCK_DIALOGUE_VARIANTS` brackets are exact** with 3 of 4 texts byte-exact (the 4th differs by the word *"A"*); the pickup toast is byte-exact at `Journal — Entry ' + entry.entryNum + ' found.@30223`; `NUE:{ num:35@8705` `act:6` confirms the report's *"SQ (Node 35), Act VI"* on **both** fields; and **12 of 12 Epic Battleground NPCs match on name, code AND `category`** with both quoted payments exact.
> **Instrument 8 delivered its largest result yet: 29 of 29 node codes were RIGHT WHEN WRITTEN** (17 journal + 12 EB, all present at `32c10c5`), and **0 of 29 resolve at HEAD** — every one cleanly renamed, none of them the §AUDIT-03m *worse-than-dead* class. Fourth consecutive increment where the archive turned a "dead code" list into a rename list.
> **THE FINDING → §ENDING-01 (c), and it triples that row's surface.** §DOC-02o established the score floor of 20; this pass traced what else reads the score. `const SWEELINCK_DIALOGUE_VARIANTS = [@27229` is selected by `cs >= v.minScore && cs <= v.maxScore` at `id:'story-sweelinck-variant'@31373`, so **only the `minScore:15` variant can ever render** and the three graded mid-run lines are dead; `const COVENANT_STANDING_LABELS = [@27356` brackets at −6/0/7/14/∞, so **every player reads "Wanderer" on the character sheet for the whole run** and 4 of 5 labels are unreachable; and `vic-returns').textContent@28223` prints **0 / 20**. ***The design's stated purpose was that the ending must never be an ambush — the mid-run panel exists so the player can change course — and the shipped game instead tells them from their first reading that they are already lost.*** All three are released by the §EPIC-01 rename, which is now unambiguously the top row.
> **Filed §AUDIT-03ac — the §AUDIT-03v/w cluster's 9th instance and its worst shape.** `⚡ Destroy the housing — STR Ceremonia Roll DC 15@31825` advertises *"(bypass Auros, +curseScore)"*, runs `S_story.curseScore = (S_story.curseScore || 0) + 5;` and prints *"[curseScore +5]"* — while `_curseScore()` recomputes from `_EB_CODES` and never reads it. Three occurrences, zero readers, **not declared in `_S_DEFAULTS()`**. ***The previous eight instances promise a mechanic that does not exist, so a grep finds nothing and the reader knows; this one names a mechanic that DOES exist, under its real identifier, and writes into a shadow of it — so a grep finds a writer, a reader and a player-facing label, and the loop reads closed.***
> **Filed §EPIC-04 (small writing call)** — the engine states two counts of the same in-fiction document on two reachable surfaces: Mordus's `mentions Birka. Twice.@26496` against Entry 26's `mentions Birka forty times.@27210`. **Both are verbatim at the archive, so it was born contradictory**, and the report is not wrong — it quotes each correctly from its own source, which is exactly what surfaced the disagreement.
> **Extended §AUDIT-03x with a new casualty class — collectible NARRATIVE, invisible to any quest-side census.** `function storyCheckJournal(node) {@30209` matches on `node.code`, so cell primacy binds it too: **35 of 41 entries reachable, 6 blocked**, and **4 of the 6 are collectibles this report specifies by name**. All ten `readAloud` entries survive, which is why the loss reads as thin content rather than a bug. **Extended §DX-02n +2** — `const JOURNAL_ENTRIES = {@22424` (5 authored quotes, 1 occurrence, 0 readers; **its "dead code" comment is TRUE**, a useful negative control for the detector §AUDIT-03aa wants — and its five texts are an earlier draft, so archive before deleting), plus the `curseScore` field.
> **Instrument 12 at a new resolution, and the report refutes itself.** ~80 transcribed facts, 1 error (a one-word variant drift); every other error in composed framing prose. The §I epigraph attributed to *"Froberger's Journal, Entry 1"* has **0 commits ever** — the engine's Entry 1 is the gate-officer entry and always was — and *"The journal has forty-two entries"* is refuted by the report's own transcribed Entry 41 text, *"I wrote it in forty-one entries"*, and by its own movement boundaries (1–14 / 15–28 / 29–41). ***`lab-report-friendships-with-magic.md`, verified one increment earlier and written the same week by the same hand, miscounts the same journal in the same way: both documents transcribe it perfectly and both get the total wrong in prose written from memory.***
> **Evidence:** report **453 → 333 lines**, IEEE-shaped (abstract · design-intent + playability argument · method · as-built inventory · **28-row two-way delta table** · six findings · verdict · defects), **~35 new `symbol@line` anchors, all verified literal + unique BEFORE writing** (4 were rejected and rewritten by the pre-check — 2 non-unique, 1 wrong line, 1 non-literal because of an escaped apostrophe). The 17 verbatim journal entries, the 4 variant texts and the 2 ending texts were **deleted per the §DOC-02h house move after measuring 17/17 and 3/4 byte-exact** — the live constants are the single source of truth and the §DOC-02n precondition is satisfied. **`NODE_MAP=416` cross-checked (instrument 14)** before any reachability delta was trusted. Docs-only: no HTML, no tests; the uncommitted 2026-08-06 user combat recolor was left untouched and unstaged.
> **Next: `lab-report-kenickie-chronicle.md`** — re-derived from `stat`, the head of the 2026-06-09 bulk-copy block, resolved alphabetically. **92 reports remain.**

> **⟶⟶ (2026-08-12) — §DOC-02o ✅ SHIPPED: the report's thesis names a payoff, and the payoff is unreachable — the game declares four endings and can produce exactly one, the failure one.**
> `lab-report-friendships-with-magic.md` (2026-05-22, updated 05-24), **329 → 363 lines** — the eight-hour session postmortem: Layer 39–40 verification, Layers 41–42 (Birka NPC Favorability; the world-truth dialogue system and the four-branch victory screen) and five new systems. Oldest remaining report by mtime, re-derived from `stat`, alphabetically first in the 2026-06-09 block. **Existing-Work-First:** `git status` showed only the user's uncommitted 2026-08-06 combat recolor (HP bar + playfield zones) — untouched.
> **Census: 34 of 40 named identifiers resolve (85 %), every survivor under its ORIGINALLY SPECIFIED name, and ZERO transcription errors anywhere in the document.** Systems 3 and 5 (the EB negotiation CHA check) are byte-exact including the `#44ee88`/`#051a0d`/`#22cc66` triple, the `💢 N non-lethal damage — HP X / Y` format string and the `He's Fine — Continue →@4915` label; `function _weaponScore(w) {@24577` is byte-identical archive → HEAD across 82 days; `70 (14 base × 5 magic tiers)` is still exact. The 6 dead symbols split cleanly by instrument 4: **5 RETIRED** (`_bfsPath`, `storyCorridorTravel`, `_stalkedMonsterPick`, the Warp message — now *forbidden* by invariant #3 — and `btn-hunt-toggle`) and **1 NEVER SHIPPED** (`btn-dpad-stalk`, **0 commits ever**).
> **THE FINDING → §ENDING-01 (NEW, no design call, resolved by fixing §EPIC-01).** `function _curseScore() {@28191` is byte-identical to the day it was written. What broke is underneath it: its `returned` term reads `S_story.ebReturnDone`, whose only writer `S_story.ebReturnDone[ebCode] = true;@30363` sits inside the `_storyEbReturnBeat` §EPIC-01 proved unreachable. With `returnsComplete` pinned at 0 and `(allComplete ? 5 : 0)@28204` never paid, the score is the closed form **`20 + 2 × (EB bosses defeated)`, range 20–60**. So `if (missionDone && curse <= 0)` (**Covenant Keeper — the ending that names every person you helped**), `else if (curse <= 0)` (Standard Covenant) and the trailing `else` (Mixed) are **all unreachable**, while `Sweelinck sets the journal on the table@28258` — *"The Void is sealed. The curse is not in the Void."* — **fires 100 % of the time**; Sweelinck's Last Question collapses identically to `Were you alone by choice?@28276`. Two of the twelve `_missionComplete()` bits are permanently false (`allEbReturns: Object.keys(NPC_DIALOGUES)@23656`, `noHighCurse: _curseScore() < 10@23660`). **Engine-rot, dated:** at `32c10c5` the epic ids resolved, so all four endings were reachable — `c1d5a94` broke them 75 days ago.
> **§ENDING-01 (b) is independent and born-broken (small design call):** `missionDone && curse <= -6@28229` gates the *"Covenant Keeper (True)"* standing against a best-possible score of **−5**. Unsatisfiable by construction, at the archive as well as HEAD.
> **§FISH-02 CORRECTED and given its origin document.** Instrument 14: `const _BASE_WEAPONS = [@24471` holds **14** entries and `const WEAPON_ITEMS = [0, 1, 2, 3, 4]@24494` flatMaps five tiers, so the set is **70, not 60**, and the unreachable count is **56 of 70**. This report *is* the spec §FISH-02 reversed — the archive's `_rollMonsterWeaponDrop` is byte-exact to it, comment and all (`// Finders Keepers: no level/magic restrictions`), and HEAD's adds `w.magicBonus === 0` plus `const deg = Math.min(0, d6 - 5)@24592` under `FC06: monster drops capped at base tier@24586`. **The original rationale is still live three lines above the call that violates it** — `no level/magic gate (Finders Keepers)@25426`. **New sub-finding F2b:** the −1…−4 penalty reaches attack and damage through `S.weapon.flatMod = mw.magicBonus@24669`, but `_wpCopy.magicBonus > 0 ? ' +'@25442`, `const tag = mw.magicBonus > 0 ? dieStr@30917` and `it.magicBonus > 0 ? ' +' + it.magicBonus + ' atk/dmg'@31094` all print it **only when positive** — a Wrecked Long Sword reads as a plain Long Sword on every surface but the Character Sheet.
> **§AUDIT-03aa extended — a tombstone that outlived its own corpse.** `_updateHuntBtn / storyToggleHunt removed@38113` asserts §TIMELESS-01 deleted both functions; `function _updateHuntBtn() {@38074` and `function storyToggleHunt() {@38085` are declared **39 and 28 lines above that comment** and `_updateHuntBtn()` is called from `storyRender`. §DOC-02c's retired-vocabulary hazard contradicted within one screenful. Two milder instances the same pass: `// Waypoint BFS direction (§CELL-09)@37520` above a `wpDir = _roadGridDir(@37524` call, and the Finders Keepers comment above. Argues for a mechanically checkable detector: *a comment asserting removal of a symbol that still resolves.*
> **§EPIC-03 narrowed by measurement, not by a new row.** `lab-report-epic-battlegrounds.md` (same day) says negotiation failure is not punished; **this report explicitly designs the punishment** — 1d4 non-lethal, floor 1, NPC pays the ceiling regardless — and the engine implements this one line for line. So the damage is deliberate and documented; what stays open is **ceiling == floor on 16 of 20 contracts**, which makes negotiating strictly dominated there. Also confirmed: `const roll   = Math.floor(Math.random() * 20) + 1;@30292` and the 1d4 at `S_story.hp     = Math.max(1, (S_story.hp || 1) - dmg);@30311` are the two unseeded `Math.random()` writes into persisted HP (§DX-02m), and they trace to this spec, written before §VM-01-B.
> **§FISH-01 impact note:** Yael's Level-1 tutorial monologue sends every new player to Yugurt for the rod — the feature §FISH-01 shows can never render.
> **Instrument 12 at its finest grain: 6/6 quoted formulae exact, 14/15 DOM identifiers exact, 2 of 9 summary figures wrong.** The single fabricated id (`btn-dpad-stalk`) shares a sentence with a real one (`btn-hunt-toggle`) — **half copied, half recalled.** And the journal count is wrong **twice in the summary** (§I and the §VII table say 17) while the report's own prose says 41 and `const FROBERGER_JOURNAL = [@27184` has held 41 at the archive and at HEAD — *the narrative passage was right and the metrics table was wrong*, inverting §DOC-02h's rule. **Lineage table: all 18 cited reports existed when written**; 3 were deleted by `120d617` (2 commits each), and the misspelled `codoex-curse-of-knowedge` filename is literal on disk.
> Gates: `check:anchors` **758 → 812 across 22 → 23 docs, 0 dead**; legacycodes / nodeindex / dupkeys / noderegs / npcregs exit 0, run individually, chain never invoked, no detach artifact. 98 stale line hints pre-existing; `anchors:fix` NOT run. **Next: §DOC-02p — re-derive the oldest from `stat` (expected `lab-report-game-story-codex-of-conquest.md`). 92 reports remain.**

> **⟶ 2026-08-12 — §DOC-02l ✅ SHIPPED `62b162a`: the spec shipped almost perfectly, then a rename broke the one link nobody greps for — the engine COMPUTES its quest ids, and forty of them stopped existing 75 days ago.**
> `lab-report-epic-battlegrounds.md` (2026-05-22), **753 → 344 lines**. Oldest remaining report by mtime, re-derived from `stat`, alphabetically first in the 2026-06-09 block. **Existing-Work-First:** `git status` showed only the user's uncommitted 2026-08-06 HTML recolor (left untouched, not mine); `git log` showed §DOC-02k as the last ship; the report's own subject was grepped before a line of it was read.
> **The most faithfully implemented specification the program has measured.** 20/20 monster keys, **20/20 statlines byte-exact in all five combat fields (100 facts, 0 errors)**, 20/20 labels, 20/20 NPC names, 20/20 payment floors, 20/20 `tier:'deadly'`, stated ranges exact (AC 14→21, HP 195→472). **And the FIRST report in the program whose node codes were RIGHT WHEN WRITTEN** — `git show 32c10c5:play.html` carries all twenty (`EF`…`EG`) with `num:` 52–71 in the report's own table order, each a literal dead-end, later renamed to `PRN`/`INV`/… by §WALK/§NAV-01 preserving `num`, terrain, label and boss on all twenty, and already translated correctly in all 20 rows of `docs/maps/node-index.md` §LEGACY CODE MAP. Every prior increment found 0-of-N codes dead as written; **instrument 8 is the only thing that can tell a rename from an error.**
> **THE FINDING → §EPIC-01, and it is the highest-player-impact open row in the repo.** Seven sites compute an epic quest id by concatenation (`'quest_' + <node code>.toLowerCase() + '_primary'|'_return'`). All seven are byte-identical to the archive, **where they resolved**. `c1d5a94` (2026-05-29) renamed the `NODE_MAP` keys, the `EB_NPC_DIALOGUE` keys, `_EB_CODES`, every `completion:{battles:[…]}`, every `waypointNode` and even the `failText` prose — **and left the forty `QUEST_DB` ids as `quest_ef_*`**. The runtime now computes `quest_prn_primary`, which does not exist. **All 40 authored epic quests are orphaned** (`type:'epic'` is excluded from arrival-activation by design, and the chips address the phantom namespace), and **the entire payment layer is unreachable**: `_storyEbReturnBeat` has one caller, gated on a key whose only writer is guarded by `if (QUEST_DB[returnId])` — falsy — so **no gold, no reward item and no return beat for any of the twenty contracts**, `ebReturnDone` permanently empty, `allEbReturns` (≥5) permanently unachievable, and the parent chip stuck at *"⚔ EPIC … awaits"* forever. The boss still dies (combat never touches `QUEST_DB`); the player is just never paid. **No gate can see it — `check:questgraph` walks `QUEST_DB` against itself and never evaluates a constructed id.**
> **Also filed:** **§AUDIT-03x extended** — 8 of 20 chains unreachable under co-location (5 battlegrounds + 4 quest-givers, `HAV` both ends; `BK` is the §AUDIT-03y node again), and §AUDIT-03x's **416 / 244 / 172** confirmed by independent recomputation. **§AUDIT-03v/w cluster +10, the widest instance yet** — ten reward items whose `desc` names a permanent mechanic, each occurring exactly once in 38,712 lines (its own declaration), with `type`/`sell` dropped at the grant so seven of them cannot even be sold. **§DX-02n +1** (`kazrath_journal`, 1 occurrence / 0 consumers). **§EPIC-02** (40 `failText` strings print a live node code *and* put the NPC at the battleground instead of their own `npcNode`). **§EPIC-03** (negotiation strictly dominated — pass and fail both pay the ceiling, fail also costs 1–4 HP, against a spec that said failure is not punished; plus two unseeded `Math.random()` calls into persisted `S_story.hp` → **§DX-02m**, and a hardcoded-masculine failure panel rendered for twenty NPCs, ten of them women).
> **MEASUREMENT HAZARD — this pass's own mistake, recorded because it nearly shipped.** A first reachability run reported 399 nodes / 155 non-primary and filed a "correction" to §AUDIT-03x. The extraction regex capped node codes at four characters, so six-character keys (`FSLINN`, `MESFRY`) were missed in `NODE_MAP` while their trailing four characters (`LINN`, `SFRY`) matched in `NODE_COORDS` — seventeen phantom "coords-only" codes and a plausible, entirely false delta. **`check:dupkeys` printing `NODE_MAP=416` caught it after the report was already written.** ***A census regex is a claim like any other: check its total against a gate that already counts the same set before trusting a delta derived from it.***
> **Evidence:** IEEE-shaped (abstract · 6-instrument method · as-built inventory · node-code translation · **35-row two-way delta table** · 5 findings · 20-row NPC contract register · 7-item NOT-SHIPPED appendix · defect table); ~25 new `symbol@line` anchors, each verified literal and unique **before** writing and none carrying a pipe inside a markdown table; `check:anchors` **551 → 574 across 19 → 20 docs, 0 dead**; `check:legacycodes` / `nodeindex` / `dupkeys` / `noderegs` / `npcregs` all **exit 0**, run individually — the `check:walk` chain was never invoked, no detach artifact. 95 stale line hints are pre-existing; `anchors:fix` NOT run. **Docs-only: no HTML, no tests, no §7½ — the 935/4 (940) Playwright baseline stands untouched.** **Next: §DOC-02m — `lab-report-fish-with-dnd.md` (re-derive from `stat` first), then the 2026-06-09 block alphabetically. Highest-value non-§DOC-02 row is now §EPIC-01.**

> **⟶ PRIOR (2026-08-11) — §DOC-02k ✅ SHIPPED `9d3e06f`: the one field-name this design lock got wrong is the one it spells two ways WITHIN ITSELF — and the engine followed the table, not the prose.**
> `lab-report-dungeon-ten-themes.md` (2026-05-26), **326 → 295 lines**. Oldest remaining report by mtime (re-derived from `stat`, alphabetically first in the shrinking 2026-06-09 block). **Existing-Work-First:** the same uncommitted user recolor is still in the tree (HP-bar gradient / light-green playfield / theme-ink die numerals, self-labelled *"user recolor 2026-08-06"*) — **left untouched and unstaged**; one live claude process; `git log` showed no prior work on this file.
> **The subject shipped, and the transcription is near-perfect.** Census: **49 of 57 named identifiers resolve (86 %)**, and **all 26 rows of the §New State Fields Summary table shipped under their specified names, in the specified order, in one contiguous block** headed `` `// §DUNGEON-01: Ten Dungeon Themes@23157` ``. Both P3+ nodes the report deferred to *"a later layer if needed"* shipped too — `` `SZG:{ num:80@8811` `` (Scholar King's Workshop) and `` `LIM:{ num:81@8815` `` (Mimic Meadows). **All 8 dead identifiers have 0 commits in the file's entire history — every one NEVER SHIPPED, none retired**, so there is no report-rot here at all.
> **THE HEADLINE, and it refines the program's error predictor rather than confirming it.** The single state field that does not resolve is `cyMadnessDecoded`, from the §Implementation Phases prose list — **0 commits ever** — while the §New State Fields Summary table's `` `cyMaintenanceDecoded: false, cyOriginKnown: false@23167` `` is live with a writer and three readers. **26 of 26 table rows shipped; 26 of 27 prose-list rows.** §DOC-02j found a *table* was its report's worst section and concluded the predictor is **copy-vs-illustration, not table-vs-trace**; this pass is the confirming case from the other side — **this** table is a *specification meant to be transcribed*, so it was, character for character, while the narrated implementation order supplied one name from memory. ***The question is never what shape the passage is; it is whether the author was copying or composing.***
> **FINDING 2 → §DX-02u (new) + §AUDIT-03y (extended): two of the eight shipped five-act chains die at Act III, for two unrelated reasons, and no gate can see either.** (a) `quest_d0209_a3` is `` `completion:{ flags:['voidFluxCleared'] }@21636` `` and **`voidFluxCleared` occurs 5 times in 38,712 lines — one default, one quest desc, three gate reads, and NO WRITER**; Act IV gates on it and Act V on Act IV's flag, so acts III–V are unreachable. **The consequence is worse than a stalled chain:** Act I sets `voidFluxActive = true` and Act V's pass is the *only* clearing site, so a player who passes a DC 10 Arcana check is **permanently inside the inversion field** — every potion and Brynn's Loaf for the rest of the run routes through the `[Void Flux] Heal → Hurt` branch and nets +0/+1 HP. **This is the INVERSE of the §DX-02n family: those fields are write-only, this one is READ-ONLY — a broken dependency, not dead weight — so a `check:deadconsts` scoped to unread fields steps straight over it.** (b) `quest_d0205_a3` is `` `completion:{ battles:['BK'] }@21579` `` and at HEAD `` `BK: { num:241@9011` `` is *"Birka Shore — Northern Longship Landing"*, a beach with **no `battle` field**, with nothing anywhere writing `defeatedBattles['BK']` — so acts III–V are unreachable and `` `S_story.mazeSolvedChecks = 3;@21582` `` can never run. **§AUDIT-03y's *worse-than-dead* class doing MECHANICAL damage rather than narrative damage: the code resolves, so `check:noderegs` passes it perfectly**, and it is independent of §AUDIT-03x — repointing BK's cell would not create the battle.
> **FINDING 3 → §DUNGEON-01-FU (new), and a 13th instrument: A DESIGN DOC'S REVERSAL IS A CLAIM LIKE ANY OTHER.** §3 is the report's longest and most confident section — Tribbles *"do NOT multiply"*, are *"not Star Trek references"*, *"do not occupy inventory slots"*, and the Brynn overflow line *"is removed from the IN node render block"*. At HEAD every Fuzzy Tribble is an inventory object whose `desc` asserts the opposite (*"You are fairly certain it is multiplying"@21850* plus *"Soft. Multiplying."* ×3), the counter **and** the inventory items are both incremented, and the Brynn line ships at `_tc >= 10` — **relocated from the inn to the meadow panel, which is why a search of the IN block finds nothing**. And instrument 4 supplies the reason it reads as authoritative: **`tribbleOverflow` has 0 commits ever — the flag §3 declares it is removing was never implemented.** The §2 mimic roster is the same shape with the opposite outcome: the four passive statlines have 0 commits each, yet all four creatures live at `LIM` **as prose** in the node text. ***The narrative was copied; the invented statblocks were not.***
> **Also filed.** **§AUDIT-03v/w/y(b)/aa cluster, 7th instance and the first stated TWICE** — `aurosBlueprintKnown` occurs 4× (default, one writer, **two readers that only render the promise back at the player**: Act IV's passText *"Auros's left pauldron has -4 AC"* and the persistent panel `` `_wkHint.textContent = '📜 Blueprint known@31964` ``); **no combat code reads it**, so the AC is never reduced and one of the surfaces persists until the fight it names. **§DX-02m, named instance** — `` `const _cyRoll = Math.ceil(Math.random()@32534` `` and both Codex Core rolls draw the **unseeded** stream into persisted state (`cyMadnessRoll`, `codexCoreChosen`, `defeatedBattles['TLS']`, `curseScore`) while the UQF path all eight chains use draws the seeded one at `` `const d20  = Math.ceil(E.rng() * 20)@22248` `` — **invariant #6 obeyed by the quest-authored rolls and broken by the two hand-authored hooks, because §VM-01-B moved the d20 in `_resolveQuestUQF` and never reached surfaces that rolled their own.** **§DX-02n +6 zero-reader fields** (`voidFluxImmunityChoice`, `mazeSolvedChecks`, `voidMazeEntered`, `tribbleGladesFed`, `cyMadnessTable`, and the sharpest — `` `S_story.spiritDefeated = false;@21799` ``, **a `_legacy_fn` that sets a defeat flag to `false` at the moment of victory**, inert either way so no test can distinguish the bug from the intent).
> **Four of the five locked decisions HELD, all byte-exact** — §4 madness-is-flavour-only, §6 the Prior Carrier as a separate entity with no profile and a `type:'flavor'` token (`` `Prior Carrier's Token@35026` ``, three branches, `priorCarrierSpoke = false` on Ignore), §7's whole `_missionComplete()` analysis (**12 keys, `>= 8`, `sealedVoid` the only CO check, neither `catKingDefeated` nor `sevenShards` present — the report's negative claims verified**) and its locked Destroy-path fix at `` `S_story.defeatedBattles['TLS'] = true;@31845` ``. **The one that did not hold is the only one that was a NARRATIVE decision rather than a data-shape decision** — nothing in the schema enforced it, so the item `desc` drifted freely.
> **Evidence:** IEEE-shaped, **326 → 295 lines**; ~75 lines of pre-implementation prose replaced by a **41-row two-way delta table**, six numbered findings, a decision register and a 7-item NOT-SHIPPED appendix kept verbatim in intent. **~28 new `symbol@line` anchors, every one verified literal + unique BEFORE writing** (one escaped-pipe anchor was caught and rewritten in the table — the §DOC-02f hazard, avoided rather than discovered); `check:anchors` **489 → 517 across 18 → 19 docs, 0 dead**; `check:legacycodes` / `nodeindex` / `dupkeys` / `noderegs` / `npcregs` all **exit 0, run individually — the `check:walk` chain was never invoked**, no detach artifact. **Docs only, no HTML edit, no §7½.** **Next: §DOC-02l — the next-oldest by `stat` (re-derive; `lab-report-epic-battlegrounds.md` at time of writing), then the 2026-06-09 block alphabetically.**

> **⟶ (2026-08-11) — §DOC-02h ✅ SHIPPED `8986fa8`: the most faithfully implemented arc in the program is 70% unplayable, and the report got every coordinate right.**
> `lab-report-crown-three-hags.md` (§CROWN-01, Layer 105, 2026-05-28), **1,216 → 288 lines**. Census of 118 identifiers against HEAD plus the archive at the birth commit `00fe35c`. **Report-rot: essentially zero.** All 9 `NODE_MAP` records exact (`num` 121–129, labels, `act:3`, terrain, all three battles, `sleepCost:8`); all 13 specified `_S_DEFAULTS()` fields under their specified names; `_innKindness`/`_addCroneMark` **byte-identical** to §III.F; **all 34 specified quests live as UQF-1.0 with every prose field byte-identical** across 75 days and the §ARCH-01 migration; all 18 DCs and 18 XP awards exact; the INN `quoteFn`'s five states byte-identical; the §VII conversion tiers exact; **23 of 23 monster statlines exact**. I deleted the 490-line verbatim quest-text section — it verified exact, and `QUEST_DB` is the single source of truth, so a second copy can only rot.
> **The finding is engine-rot and it is total.** §II specified **nine distinct cells** and defended the spacing node-by-node against four named neighbours; the archive confirms **all nine** coordinates plus **all five** neighbour coordinates exactly as written. At HEAD, §WALK/§NAV-01's 90×360 migration has collapsed them onto **two** cells — `25,206` holds 12 nodes, `22,191` holds `KTM INN`. `CELL_GRID` builds each cell in `NODE_MAP` declaration order and only `list[0]` can become `currentCode`, so **WG0 and INN are reachable and HW1 · HJ1 · HG1 · HJ2 · HN1 · HJ3 · HCA are not**. Measured: **24 of 34 quests cannot activate**, `croneMarks` cannot exceed 0, Glut's Gift is never granted, the conversion and the Leviathan never render, two of three combat nodes never fight, and `quest_shore_01` — the **§LXX Atlantean Shore entry** — is stranded behind `HCA_BOSS`. **→ §AUDIT-03x, extended with the first complete-arc casualty and the archive proof that the collision was not the arc's doing.** That evidence also argues against option (3): repointing these 24 `activateNode`s to their primary would put the whole arc on WG0, where none of it happens.
> **Instrument 9's gradient, fourth consecutive report, cleanest instance yet.** ~600 transcribed facts, zero errors — and **every** error sits in a **composed** passage: §A.8's state-field list (13 of 17 names never existed), §A.4/§A.6's quest ids (**7 of 7** never existed), §III.G's assertion that a guard fired (it never did — `storyCollectLoot` flips `visited[code]` first, and §VM-01-G-FU-a's engine comment records the panel as dead from birth, so the *transcription* was exact and the *behaviour claim* was false), and §A.7's combat coupling with no reader. **Instrument 4 sharpens it: all 20 of those identifiers have 0 commits in the file's entire history.** The features shipped; only the names are invented — written from intent, hours after the code. ***New durable rule: a report's INVENTORY earns trust and its SUMMARY does not, even one page later, same hand, same day. Verify the summary against the body it summarises.***
> **Also filed:** **§DX-02n** +2 (`waneStoneDCReduced` — 1 writer, 0 readers, discounting a `retryable:false` quest; `Marsh Seaweed` — one occurrence in the file, 0 consumers, `desc` naming a craft that does not exist — which widens `check:deadconsts` a **third** time, now to inventory item objects). **§AUDIT-03v/w/y(b) cluster** +2, and they **widen the detector past gold to any named mechanical effect**: the Leviathan burn button *charges* an item for a *"+5 ATK this fight"* whose only reader in 38,707 lines is `_rollSkill`, and the buff then **leaks** into the next unrelated skill check. Five instances in five increments; the gold-only formulation would have caught two. **§DX-02r NEW** — the activation grammar has `questsAttempted` and `questsDone` but **no `questsFailed`**, though `'failed'` is written at `@7001`, so §CROWN-01's three failure-consolation quests activate for players who *passed*. Invariant-#4 shape: widen the grammar, never patch the quest.
> **Gates:** `check:anchors` **358 → 384 across 16 docs, all resolve, 0 dead.** One new anchor was rejected as **too generic** (`questsAttempted`, 27 hits) and tightened to `` `g.questsAttempted.every` `` — the §DOC-02f shortest-verbatim-fragment discipline meeting its **other** failure mode: an anchor can be too *short* as well as non-literal, and the gate reports both.
> **Next: `lab-report-documentation-system-design.md`** (re-derive the ordering from `stat` — every verified report gets a fresh mtime and leaves the 2026-06-09 block). No design call.
>

> **⟶ (2026-08-11) — §DOC-02g ✅ SHIPPED `70d49a0`: a code that RESOLVES is not a code that is RIGHT — and the lab report was the only source in the repo that had the history correct.** `lab-report-corelli-merchant.md` (Layer 61, the cross-act vendor), **193 → 196 lines** — the first increment that did not shrink, because the source was already short; the prose came down, a 12-row delta table and four findings went in. **Strongest survival yet: 19 of 20 identifiers resolve (95%)**, beating §DOC-02d — all 4 state fields, both helpers, all 5 item keys with every `readText` verbatim, `fav = min(3, purchase_count)`, the monotonic `count < index` gate, the three-tier opener — every one under its original name. **And 0 of 5 node codes are correct.** The mechanism survived a world migration untouched; the geography did not. **(1) The report is the correct record and two LIVE sources are wrong.** The engine comment above `CORELLI_APPEARANCES` and `world.md` both claim the old codes were `TL/RD/IS/WM/IN`; the archive shows **`DK/RD/BK/SQ/IN`** — the report's own list — and no header comment existed to misread until the repoint commit wrote one. First corpus correction where the wrong source is **the engine itself**; **new rule: a migration commit's comment is a claim about the past, written from memory rather than from the diff — check `git show <sha>^`.** And it is causal: believing stop 3 was `IS` is why `BK` was left alone. **(2) §AUDIT-03y — the `BK` collision, §AUDIT-03m's "worse than dead" class found for the first time in ENGINE DATA:** legacy `BK` = Broken Tooth Tavern, Visby = live `VBY`; `BK` still resolves, as a **Birka beach**, so `check:noderegs` passes while Corelli says *"Visby. Well."* and `NPC_DIALOGUE.BK` has Mordus say *"You're not from Visby"* — plus `'BK'` in `VENDOR_NODES` and 12 field references, with `VBY` in none of them. **(3)** `BK` also shares cell (10,197) with `LHR` and is non-primary (§AUDIT-03x), so stop 3 can never fire and **`encoded_letter` is unobtainable** — killing the retroactive decode, the feature the report's own post-mortem rated best; repointing to `VBY` fixes it **without** waiting on the design call. **(4) §AUDIT-03y (b)** — `false_warrant` is a **200gp purchase with no surface**: RETIRED not never-shipped (`git log -S`: shipped `194a810`, host `storyCorridorTravel` deleted by `85cc43e`/§CELL-11A), still sold at reachable stop 2, no Use button and no Read button. Third §AUDIT-03v/w-class instance in four increments — close all three with a detector. **The §DOC-02f internal gradient repeats exactly:** the transcribable is perfect — the *deleted* RD node verifies **5/5** against the archive — while the narrated is wrong, and **three of those were wrong the day it was written**: "modal" (always `storyMsg` + `insertAdjacentElement`), "remains readable" (`readableItems` filtered `type === 'readable'` at birth too), and **§4.2's pacing critique describes a shop that cannot exist** (stop 5 is `itemLabel:null` + `return`, at birth and now). **New instrument 10: a report's SELF-CRITICISM is a claim like any other — a post-mortem can be wrong about the thing it is apologising for.** Two-way sync in the same increment (engine comment + five `world.md` corrections + two ⚠️ callouts); filed **§AUDIT-03y** and **§DX-02q** (a `check:noderegs` phase for semantic collisions — the failure that passes "does it resolve?" perfectly). `check:anchors` **350 → 358 across 15 docs, all resolve**. **Next: `lab-report-crown-three-hags.md`.** *(Gate hazard re-confirmed: two `check:walk` runs in one Bash call reproduce the detach artifact every time — run it once, redirected to a file.)*

> **⟶ (2026-08-11) — §DOC-02d ✅ SHIPPED `bb9bd58`: the strongest survival in the program — 91% of its symbols resolve, 0% of its node codes do, and the report is the traceable origin of an identifier defect that later cost a whole audit row.** On "continue", took `lab-report-birka-beginner-arc.md` (345 lines, 2026-05-22) by the §DOC-02 ordering rule — the oldest remaining file in the 2026-06-09 bulk block, alphabetically first after a/b/c, re-derived from `stat` as the preamble requires. **Existing-Work-First:** the same uncommitted user recolor is still in the tree (HP-bar gradient / light-green playfield / theme-ink die numerals, self-labelled *"user recolor 2026-08-06"*) — **left untouched and unstaged**, this increment is docs-only; every number re-measured from HEAD, never read off a doc table. **Method was §DOC-02c's four-part census** (batched `grep -c` symbol census · node-code resolution against `docs/maps/node-index.md` · mechanism re-read at each live definition · `git log -S` history probe on every dead symbol). **The headline inverts §DOC-02c exactly: 29 of 32 named symbols resolve — 91% — while all 8 node codes are dead as written.** Every state field shipped under its specified name (`npcFavorability`, `roughWhiskeyUsed`, `yaelEscortUsed`), every function under its specified name (`_setNpcFavor`, `_npcFavor`, `_renderNpcCard`), six of seven quests shipped as UQF-1.0, all three gold rewards exact (80/40/60), all seven items live, and **both `_setNpcFavor` log lines are verbatim at HEAD** — *"looks at you differently now"* / *"says your name when you walk in"*. The mechanism then **outgrew its own spec by an order of magnitude**: §NPC-01-B derived the card-render map from `BIRKA_NPC_PROFILES` rather than hand-listing it, taking the relationship surface from ~20 NPCs to **204 profiles / 213 dialogues** with no change to the contract this report defined. **So the failure mode here is not rot; it is naming — and that is the row's reusable finding.** Five of the eight node codes were **born dead**, and the engine says so in its own source at `` `const birkaNpcs@35139` ``: *"No NODE_MAP entry ever existed for CI/IN/TV/BA/CY, so these cards previously rendered nowhere"* (§AUDIT-03p class, fixed by §PLAY-01-D/G; `CI` is now live as an entirely different node, Chancery Court `num:429`, so it passes every *"does this code exist?"* check while the sentence stays wrong). **And the report minted the identifier split §AUDIT-03n later had to repair:** it introduces the bard as *"Bard Tomas Couperin"* and then calls him **Quill** fourteen times, so he shipped as three identifiers at once — key `quill`, `name:'Bard Tomas Couperin'`, quest `quest_couperin_lute`; *"Pit Master Weckmann"* shipped under the key **`crov`**, a token the report never mentions; and the heading **"THE SIX BIRKA NPCs"** is followed by **five** profile sections, with Auros (`name:'Commander Seraphine Bruhns'`) never characterised and listed in the implementation notes as *"Auros-extended"*. §AUDIT-03n measured the bill fourteen months of engine-time later: seven registries keyed to the surnames the favor ledger never writes, **21 unreachable entries**, plus the victory screen's own `npcOrder` showing the **stranger** epilogue for Quill, Weckmann and Bruhns at any favor. *Durable lesson: a design doc that uses two names for one character mints two keys — the engine cannot tell they are the same person, and a favor written under one is invisible to a gate reading the other. Fix the name in the doc before the doc becomes the schema.* **Seventeen deltas recorded; four are the interesting kind.** (1) **`quest_drunk_fight` was never a quest and is left dangling** in `_hasActiveQuestFor`'s `npcQuests` — filed as §DX-02o. (2) **Two of the arc's three specified gates were never authored:** pit training's *"requires 1+ Birka quest complete"* and Void Below's *"requires Auros AND Weckmann both Friendly"* both ship `gate:{}` — while a **sibling quest in the same block** (`quest_brynn_firewood`) *does* gate on `favorMin:{brynn:1}`, so this is an authoring omission, not a missing capability, and the arc's finale is available on arrival. (3) **The drunk pit fight shipped its mechanics exactly and its consequences not at all:** `` `adv = 'dis'@25044` ``, `` `const drunkBonus = S_story._drunkFight ? 3 : 0@25071` ``, the one-time latch and the item consumption are all verbatim, but the nat-20 flavor line and Weckmann's *loss* reaction have **0 commits ever**, and because `_drunkFight` is consumed only in `_storyBattleVictory`, **losing the drunk fight produces no reaction at all**; the promised early Pit Legend Token also never existed (the branch grants favor and nothing else). (4) **The report's Step 4, "New Information", has no scene** — Auros's rune-tablet reading (*"the second district boundary… we are currently at the third"*) returns 0 hits and 0 commits, so the arc's thematic payload was written and never built. Two further deltas are expansions rather than losses: the three favorability states became **four tiers** (`impartial · questActive · friendly · dearFriend`) and §NPC-01-D added a second route to Friendly with no quest at all (`` `TALK_TO_FRIENDLY = 3@23513` `` talks on distinct game-days), and the two authored Rough Whiskey reactions became **eighteen** (six NPCs × three tiers) behind a *second* flag, `roughWhiskeyActive`. **What held is the whole design half, and it is load-bearing at HEAD:** favorability as a small integer with permanent visible effects; the dear-friend "second personal act", formalised as a six-entry `dearFriendBits` table whose every member is an act this report invented; and the Sweelinck payoff, which shipped **stronger** than specified — `_birkaVar` is resolved first and overrides all four curse-score bands, so at three or more friends the Birka variant is unconditional. **Its thesis is executable:** 8 of `_missionComplete`'s 12 bits are Birka relationship acts and the ending needs 8 of 12, so *"Not experience points. Not gold. Five names"* is a threshold in the shipped game, not a mood. **Three defects filed. §AUDIT-03u:** two player-facing strings still say the world has **forty-two nodes** — the Map Shop's Real Map (`` `STN:{ num:9@8636` ``) and **Yael's Level-1 tutorial monologue** (`` `yael: { meta:@10397` ``), the game's first paragraph of instruction — against **416** live; forty-two was the report-era battleground count in the retired 26×16 space, and the fix should describe scale without a literal rather than swap in a number that will rot the same way. **§AUDIT-03v:** Brynn promises a free room in `TLL`'s node text *and* as `quest_brynn_ledger`'s reward, and `storySleep` charges `sleepCost:5` both times, because the only free-lodging exemption is hardcoded to a different node and a different flag (`` `node.code === 'INN' && S_story.freeBookingUnlocked@36213` ``) — a double-*promise* with no payer; small design call, grant the nights or rewrite both strings. **§DX-02o:** the quest-id reference gate — **302 distinct `quest_*` literals in the file, 301 resolve, 1 does not**, and no gate can see it, because `check:questgraph` walks `QUEST_DB` against itself and `check:noderegs` mentions `QUEST_DB` zero times; wants a `check:questrefs` phase with explicit `NOT_A_QUEST_ID` classification (the two legitimate `startsWith` prefixes and the `quest_item` type string). **Evidence:** report **345 → 280 lines**, IEEE-shaped (abstract · method · as-built inventory · 17-row delta table · node-code resolution · the naming section · defects · what it got right · scope note · Appendix A keeping every NOT SHIPPED line verbatim), **50 new `symbol@line` anchors**; `check:anchors` **284 anchors across 12 docs, 0 stale**; `check:legacycodes` **OK**; `npm run check:walk` reached its final `&&` link on the piped run — **all 16 gates green** — while the redirect form hung at 0% CPU, the known harness detach artifact, so **use the piped form**. **Docs-only: no HTML, no tests, no §7½ — the 935/4 (940) Playwright baseline stands untouched.** **Next: §DOC-02e** — `lab-report-ceremonia-roll-skill-checks.md`, then the rest of the 2026-06-09 block alphabetically (`circuit-map-theory` remains §DOC-02c's sibling — same Layer-9 subject, so its census will be fast and its delta table will overlap).
>

> **⟶ (2026-08-11) — §DOC-02c ✅ SHIPPED `cc4a07e`: the first report in the program that fails as a description of the engine — and it fails at the thesis, not at the edges.** On "continue", took `lab-report-battleground-circuit-path-quest.md` (889 lines, 2026-05-21) by the §DOC-02 ordering rule — the oldest remaining file in the 2026-06-09 bulk block, alphabetically first after a/b, re-derived from `stat` as the preamble requires. **Existing-Work-First:** the same uncommitted user recolor is still in the tree (HP-bar gradient / light-green playfield / theme-ink die numerals, self-labelled *"user recolor 2026-08-06"*) — **left untouched and unstaged**, this increment is docs-only; every number re-measured from HEAD, never read off a doc table. **Method was §DOC-02b's four-part census plus a fourth instrument this row adds to the program: a `git log -S` history probe on every symbol the census marks dead.** That probe is the only way to separate **RETIRED** (shipped, then removed — the report was true when written) from **NOT SHIPPED** (never existed under that name — the report was written from intent, not from the file), and *a report cannot make that distinction about itself.* **The headline inverts §DOC-02b exactly: 16 of 38 named symbols resolve — 42% survival against b's 91% — and of the 49 node codes it tabulates, none resolves to the node it names.** All three components carrying the thesis are gone. §TIMELESS-01 (`7952752`, 2026-06-26) deleted `HUNTING_GROUNDS` and the whole Stalk mechanic, leaving two tombstone comments; §WALK/§NAV-01 deleted all seven corridor symbols (`CORRIDOR_CELLS`, `CORRIDOR_TERRAIN`, `buildCorridorMap`, `_routeSegments`, `storyCorridorTravel`, `_wireGlyph`, `_corridorOnComplete`) **and** the `N`/`S`/`E`/`W` compass fields the wire mesh was drawn from — so the corridor layer had no graph left to render even had it survived. **Twelve deltas recorded; three are the interesting kind.** (1) **The §IX-C "implementation" listing never existed:** `git log -S` returns **0 commits ever** for `stalkModal`, `#story-stalk-overlay` and `#stalk-terrain-name`, while the feature they describe shipped three days later (`32c10c5`, 2026-05-24) under entirely different names — `storyStalk(nodeCode)`, `#story-stalk-modal`, `#btn-stalk-wait` — and §TIMELESS-01's own removal inventory records that shipped modal as *"already never shown — legacy/dead."* (2) **Two of its specified designs are now forbidden, not merely absent:** the Warp travel mode ("instant transit, no encounter") is banned by hard invariant #3, stated in the engine itself at `` `no jump travel. checkpointNode@26048` ``, and the junction nodes J1–J7 became a **CI failure** (`check:invariants` I1/I2, §DX-01a) with the tool that minted them deprecated and refused (§DX-01d). (3) **`targetTerrain` and `targetKeys` have 0 hits at HEAD *and in history*** — the quest-to-terrain coupling of §V, which is the report's own Principle 3 and the load-bearing half of its "closed information loop", **never had data to run on**; quests are UQF-1.0 (§ARCH-01). Two further formulas measured **invented rather than stale**, the same class §DOC-02b found in the same subject area: the quest-scaled `min(0.90, 0.10 + activeQuests × 0.05)` has no expression at any point (rate is per-terrain data, `` `const TERRAIN_ENCOUNTER_RATE@9892` ``), and the fixed tier vector `{35,35,25,4,1}` matches **no band** of the live 6-band `` `function _notorietyWeights@38212` `` — not even the lowest (`40/35/20/4/1`). **The most reusable finding is a name collision.** §KG-01 (`8168f0e`, 2026-07-08) introduced a **different mechanic under the retired feature's name** twelve days after §TIMELESS-01 removed it: live "Hunt Mode" is a d-pad toggle that doubles the wilderness encounter rate to a 0.8 cap with an 80/20 bias toward monsters at or below player level (`` `huntMode: false@23083` ``, `` `function _monsterLevel@38231` ``), sharing nothing with the corridor Hunt/Warp dialog but the word. A reader who greps `huntMode` from this report lands on a mechanic it does not describe. *Durable lesson: a retired feature's vocabulary is not free to re-use — the doc that named it does not get updated, so the collision is silent and permanent.* **What held, for 82 days and 6.1× the file:** `WORLD_DB`'s `{label, icon, monsters}` shape byte-exact with monster **object references** pre-resolved (no string lookup at encounter time, exactly as specified); `NODE_MAP.name` as the terrain key — the one structural relation every later track preserved; all 10 `NodeEntry` fields; the five-tier vocabulary, now the CI-pinned §DX-02g contract; and the story-vs-grinding XP taxonomy, whose *distinction* survives even though the battleground layer between them does not. **One node code is worse than dead:** `CI` resolves to a live but entirely different node (`num:429`, "Chancery Court — The Officer's Pen"), so every *"does this code exist?"* check passes while the sentence stays wrong — the §AUDIT-03m hazard class in its purest form. **Two defects filed. §AUDIT-03t: 36 of 416 nodes (8.7%) carry `act:NaN`**, found by checking the report's own `NodeEntry` typedef claim *"`act` — story act (1–8)"*, and the three readers fail three different ways without throwing — `` `S_story.actNumber = node.act || 1@34573` `` silently act-gates all 36 as **Act 1** (NaN is falsy), `` `ACT_NAMES[node.act]@34602` `` indexes a 9-element array with NaN so the act badge renders **"— undefined —"** and `#s-node-act` renders the bare word **"undefined"**, and the map panel renders **"Act NaN"**; no gate can see this, because `check:noderegs` checks that node *references* resolve, never that a node's own scalar fields are well-formed. **§DESIGN-04: the thesis question has no successor surface** — `HUNTING_GROUNDS` was deleted and nothing replaced it, so with 398 monsters across 111 terrains and 416 nodes there is no bestiary, no terrain index, no habitat surface, and the "lost in a field" failure mode the report was written to eliminate is by its own criteria unaddressed; filed as a **design call** with three options, because the modern game may legitimately answer it through quest `activateNode` and the Warrant's Board instead. **Evidence:** report **889 → 214 lines** (76% reduction), IEEE-shaped (abstract · method · as-built inventory · 12-row delta table · node-code resolution · the name collision · defects · what it got right · scope note · the 49 codes kept verbatim as NOT SHIPPED), **24 new `symbol@line` anchors**; `check:anchors` **234 anchors across 11 docs, 0 stale**; `check:legacycodes` **OK**; `npm run check:walk` reached its final `&&` link (`check:legacycodes OK`) on the piped run — **all 16 gates green** — while the redirect form hung at 0% CPU, the known harness detach artifact, so **use the piped form**. **Docs-only: no HTML, no tests, no §7½ — the 935/4 (940) Playwright baseline stands untouched.** **Next: §DOC-02d** — `lab-report-birka-beginner-arc.md`, then the rest of the 2026-06-09 block alphabetically; note `circuit-map-theory` is this report's sibling (same 2026-05-21 Layer-9 subject), so its census will be fast and its delta table will overlap.
>

> **⟶ (2026-08-11) — §DOC-02a ✅ SHIPPED `e784721`: the lab-report verification program opens on the oldest report, and the arc it checked shipped almost exactly as specified — the value is in the six things that did not.** On "continue" with a new user directive (verify the lab reports oldest-first against the HTML, shorten them to IEEE shape, backlog what isn't covered), took `lab-report-ally-cat.md` — Layer 44, the Cat Quarter — resolved as oldest by the ordering rule the new §DOC-02 preamble now states: **62 of the 109 reports share one mtime** (`2026-06-09 11:06:54`, a bulk copy), so within that block "oldest" is alphabetical. **Existing-Work-First:** `play.html` carries an **uncommitted user recolor** (HP bar → brown/gold gradient, playfield zones → light green, die numerals → theme ink, all self-labelled *"user recolor 2026-08-06"*) — **left untouched and unstaged**, this increment is docs-only; two claude processes; every number below re-measured from the file, never read off a doc table (§AUDIT-03m). **The verification's own headline: the design shipped, and the report was still wrong in six places.** Verified exact and unchanged since 2026-05-25 — all **10 monster statlines**, all **10 drops**, the `cat_quarter` pool (10 slots, `fluffy_cat` doubled, `cat_king` correctly withheld from the random table), the 7 keys appended to `alley`, the node's `num`/`label`/`act`/`npc`/`loot`/`sleep`/`battle`, the `NPC_DIALOGUE` opener verbatim, `catKingDefeated:false`. **The six deltas:** Q-CAT-01's *"+ Tiny Fedora trophy"* and Q-VOID's *"+ Void-Singed Whisker ×3"* were **never granted** (both live only as drops — a reward a player was promised in a spec and has never received); the Q-CAT-03 **merge mechanic** — *"both Honchos in one battle loads a second `taz_devil` wave"* — **has no code at all** and survives only as narration, which is why Jimmy's *"the merge thing is NOT a mechanic I enjoy"* is literally true; the **levels 3–5 are unenforced** (no `level`/`minLevel` on any of the seven); the vendor landed as a `NODE_HOOKS` launcher rather than the specified node vendor array; and the `HUNTING_GROUNDS` entry's whole registry was deleted by §TIMELESS-01. **The risk register is the lesson:** 4 of its 5 filed risks held, and the one that did not — *"`num:77` — verify no existing node uses 77"* — **was never verified and is still colliding**, `MJF` and `CDG` both at 77 (display-only today; three read sites, all cosmetic) → **§AUDIT-03r**. **The second defect is a class, not an incident: dead node codes inside player-facing STRINGS, which no gate can see** — gate #16 scans `*.md` only, gate #13 phase 6 scans comparison literals only — **6 player-visible occurrences**, five of them this arc saying *"the DF node"* for what is now `ZRH` (Sandy's dialogue, `quest_cat_void`'s `desc`/`disposition`/`failText`, the `CQ_BOSS` victory line) → **§AUDIT-03s**, filed with its scope limit stated up front (`the XX node` is ONE phrasing; measure the others before fixing the gate's shape). **Two doc errors corrected in-increment:** `index.md` called `S_story.catKills` *"nodeCode → kill count"* when it is keyed by **monster key** — the thing all four `countMin` gates actually read — and `world.md` §Layer 46 carried two stale bare HTML line pointers (`8026` / `~11799–11885`; actual `8798` / `13689`–`13785`) plus the retired `R04,C17`, now `symbol@line` anchors. **A gate caught me mid-edit and that is worth recording:** rewriting that `world.md` line put *"Section IX"* next to a backticked live code, and `check:legacycodes` **phase 2 failed it** — the §AUDIT-03q signal (*a line already naming a live node code is talking about places, so the codeish token beside it is one too*) firing on a real new hit, in a doc that had been green for a week. Rephrased to `§IX`. **Evidence:** report **239 → 227 lines**, IEEE-shaped (abstract · method · as-built · delta table · risk outcome · defects), **15 new `symbol@line` anchors** with 2 hints hand-corrected rather than left to `--fix` (§DX-01e-FU: `--fix` picks the NEAREST hit); `check:anchors` **181 anchors across 9 docs, 0 stale**; `check:legacycodes` **OK** (`docs/lab-reports/` is HISTORY by classification — the report's `CQ`/`SL`/`DF` mentions are annotate-never-rewrite by design); `check:walk` ran the full 16 twice with no FAIL, then the **detach artifact returned on the 3rd, redirected run** — `check-mover-behaviour.js` parked at **0.0% CPU** with the log empty (**4th sighting**, first one where the redirect also swallowed the printed ✓) — killed and re-run alone: **exit 0, 41,515 agreeing decisions, 0 content mismatches**. **Docs-only: no HTML, no tests, no §7½, Playwright untouched — the 935/4 (940) baseline stands.** **Next: §DOC-02b** — `lab-report-architecture-full.md`, then the rest of the 2026-06-09 block alphabetically; one report per increment, since this one produced 2 audit rows and 2 corrections on its own.
>

> **⟶ PRIOR (2026-08-04) — §AUDIT-03p ✅ SHIPPED `ea19a88`: the Void's "first crack" line now reaches 13 nodes instead of 5, and gate #13 can finally see inside a function. The row was right about the eleven dead keys; it could not know that four of the sixteen were never nodes at all.** On "continue" with §AUDIT-03d still waiting on its staging call, took the engine row from the board's next-ups (the other candidate — §AUDIT-03m's 7 PENDING docs — is still open and still needs no design call). Existing-Work-First: tree clean at `cd033cb`, WBAPI **stopped** before an inline-JS hand-edit (Hazard #1), one claude process, premise re-measured live rather than trusted. **`_voidFlavorLine`'s function-local `CLUSTER` had 11 of 16 keys dead, so the Layer 59 crack line silently rendered at 5 of the 16 places it was authored for — since `cc562f5`.** Eight remapped through the **generated** LEGACY CODE MAP and were **corroborated by label, not just `Node #`** (`DK`→`LCY` is literally *"Harbor Docks — Tilbury"*). **The finding the row could not contain: `SH`/`PH`/`MH`/`WM` matched no `NODE_MAP` key at the very commit that authored them, and appear nowhere in `maps.md`'s 92-row legend either** — written from memory, born dead, so *"it used to work"* is not a safe assumption about an old table. `WM` was later minted as a real node that happens to fit its cluster; the other three are **dropped, not guessed**. One judgment call stated openly: `ZRH` joins its own `defi_land` district's `wild` bucket rather than the `weimar` its legend row implied — *"the scholars' candles"* at a quarter with no scholars is new wrong content, not a repair. **Coverage 5 → 13 live nodes.** **The durable half is the gate:** `check:noderegs` **phase 5** applies the same explicit `LOCAL_NODE_KEYED`/`NOT_NODE_KEYED` classification to **indented** `const/let/var NAME = {` literals — phases 1–2 matched `^const` only, which is exactly why a rotting 16-key table sat inside a function for months under a green gate. 3 local node tables, 2 compass ones. **Evidence:** `check:walk` **16/16 exit 0** · `--selftest` **6/6** (two new plants: a dead key in a classified local table, and an unclassified local table) · **negative control** — the gate against the HEAD `CLUSTER` exits 1 naming exactly the eleven, restored green with an empty diff · new `audit03p-void-flavor-cluster.test.js` **10/10** with a **positive control** (against HEAD, **6 of 10 fail**, and the 4 that pass are precisely the already-live `LHR`/`WM` cases — so the render tests are not vacuous) · Playwright **787 passed / 4 failed** = the documented `worldbuilder-crud-arrays` four plus the known `multiplayer-presence:171` flake, **7/7 green in isolation**, not a fifth red. Docs: `story.md` gains the live cluster→node table + the born-dead note; `index.md`/`prompt.md` §7 record phase 5; `anchors:fix` refreshed the 12 hints my +7 lines drifted. **Next: §AUDIT-03m's 7 PENDING docs (230 refs, mechanical, no design call) · §AUDIT-03d (still the gating row — needs the staging call, releases the 983 quests §AUDIT-03e holds) · §DX-02j / §AUDIT-03o / §DX-01e-FU2 / §AUDIT-03i (design calls) · §VM-01-G2b or the G4 child report (largest, the active engineering track).**
>

> **⟶ PRIOR (2026-08-04) — §AUDIT-03k ✅ SHIPPED `1c4f547`: Yael was two people. The alias map collapses all seven display-name splits, and gate #14's new phase makes the eighth impossible to create quietly.** On "continue" with §AUDIT-03d's staging question still unanswered, took the measured row that needed no design call. Existing-Work-First: tree clean at `344151f`, server stopped, one other claude process, and **every claim re-derived from the live registries before an edit** — the row's mapping was not carried. **The premise held and the scan found two things the row did not.** A name/occupation-subset sweep over all 68 distinct `NODE_MAP` inline npc slugs returned **7 candidates**: the six §AUDIT-03c recorded plus `city_fence`⇄`pachelbel`, which **no name rule can derive** (the slug shares not one word with *Fence Pachelbel*) and which exists only because the table is explicit — and one that looks exactly like an alias and is not. **`ship_captain` @SEN matches `captain_smollett_sen`'s occupation exactly, but Smollett captains the Hispaniola at HMS and SEN is the *Tilbury Star*.** Two ships, two captains, 8 quests that must not merge: **a role collision is not an identity**, and a heuristic left to itself would have merged them. Four pairs are corroborated independently by `NODE_NPC_KEYS`' own node→key column, three by `profile.node` or an exact name match. **Shipped:** `WBAPI.NPC_ALIASES` + `npcCanonicalKey()`; the seven slugs **out of `npcKeyVocab()`** (297→290), so a quest still carrying one advise-warns rather than validating beside the real key; `editField` normalizes **(quest, npc) only** — scoping it wider would normalize `NODE_MAP`'s own inline npc, which is the §AUDIT-03h bug; `_buildIndexes` indexes under the canonical key, which is the part that covers a **hand-authored nested UQF block** (Hazard #3 quests never touch the API); the create path normalizes before the world-logic check; and each write path **reports** the collapse instead of silently rewriting the author's input. **Corpus:** the 5 `city_guard_captain` quests collapsed to `yael` — she is one person again at **22**. **A defect found on the path, in the verifier this time:** `saveAndVerify` compared the reloaded field to **what the caller sent**, so the first live PUT answered `ok:false — field mismatch after reload: npc` on a perfectly correct write. The inverse of this repo's usual failure, and it now verifies against what `editField` actually wrote. **Evidence:** the collapse was sent **as the alias** and `yael` landed on disk — the round trip *is* the acceptance test; HTML `git diff` = **5 lines**; all three write paths round-tripped live (batch · PUT · create), probe quest deleted with 0 residue; `./api.sh audit` **errors 0**; `check:walk` **15/15 exit 0**, gate selftest **6/6** including both new alias plants; `audit03k-npc-aliases.test.js` **6/6** with **all 6 failing on the stashed pre-fix tree** (restored, `shasum -c` OK); npc/audit family **29/29**; full Playwright **769 passed / 5 failed** = the documented `worldbuilder-crud-arrays` four **plus one pre-existing red this row proved at HEAD** (§DX-02l-FU below), and **0 flaky** — the +19 over the last recorded 750 are this row's 6 and earlier rows' additions. **No §7½ — nothing in the game reads `quest.npc`, which is exactly why this rotted quietly: the split is invisible from the player's side and only ever wrong on the authoring side.** Two sibling tests were stale after the collapse and moved with it, annotated not rewritten — including `audit03c`'s assertion that said in so many words *"the slug still resolves — this is §AUDIT-03k, not a broken key"*, which now asserts the opposite on purpose. **The lesson: a key that resolves can still be the wrong key.** *Does it exist?* and *is it the one this character is filed under?* are different questions, and gates #13/#14 only ever asked the first. **Side finding filed, not swept: §DX-02l-FU** — `dx02l-save-snapshots-cli.test.js:190` fails at HEAD on a clean tree, because CONTRIBUTING Hazard #7 (written by §DX-02l itself) now says *"verifying §DX-02k needed raw `curl`"* on the same line as `POST /api/save`; the standing Playwright baseline is **five** reds until that is settled. **Next: §AUDIT-03d (still the gating row — needs the staging design call; it releases 983 quests), else §DX-02j (measured, needs a per-group design call), §AUDIT-03o (measured, design call), §DX-01e-FU2 (design call), §DX-02l-FU (small), §AUDIT-03i/m, or §VM-01-G2b / the G4 child report.**
>

> **⟶ PRIOR (2026-07-31) — §AUDIT-03n ✅ SHIPPED: the ending called three of your six friends strangers no matter how you played. The row named 12 dead entries in 4 registries; there were 29 references across 7, and the six it missed were in live code.** On "continue" with §AUDIT-03d's design question still unanswered, took the row the last board named first among the no-design-call alternatives — measured, and player-visible. Existing-Work-First: tree clean at `1be43be`, server stopped, 2 other claude processes present but the game file untouched. **The premise held; the scope understated by 2.4×, the same direction as §AUDIT-03j.** A full-depth scan (every object literal at any depth with all-lowercase keys, against the live 297-key `npcKeyVocab()`) found `couperin`/`weckmann`/`bruhns` keying **seven** registries at exactly 3-of-6 each — the row's four plus **`SWEELINCK_NAMING_LINES`, `ROUGH_WHISKEY_REACTIONS`, `FROBERGER_TRACES`** = 21 unreachable entries. **The dimension the row missed entirely was live code:** the epilogue's `npcOrder` and **five `_npcFavor('bruhns')` gate sites** — the §XXXVII CO scene at TLS, the S6 Weckmann/Auros joint conversation (**which ANDs a live key with a dead one**, so it was unsatisfiable), the S29 theory line, the L45-J Froberger beat, and **the only delivery button for the "Undercity Survey (Partial)" the player is actually granted**, making that key item a dead end. **Identity re-derived from the live profiles rather than carried:** `quill` = Bard Tomas Couperin · `crov` = Pit Master Weckmann · `auros` = Cmdr Seraphine Bruhns, and the ledger's writers spend exactly the six profile keys — the surnames key nothing anywhere. **Shipped:** all 27 sites renamed in one line-anchored pass with per-line assertions (all-or-nothing, alignment preserved); the §AUDIT-03j-style comment above the seven tables; **`src/scripts/check-npcregs.js` = `check:walk` gate #14**, the npc mirror of #13 — registry keys (incl. the nested `TOWN_CRIER_LINES.npcs`), **explicit** classification of all 36 all-lowercase tables (unlisted FAILS), `_npcFavor()`/`npcFavorability[]` literals, and `npcOrder` arrays — **deliberately NOT npc-valued fields**, since `NODE_MAP`'s inline `npc` is supposed to be a display name (§AUDIT-03h). **Evidence:** `check:walk` **14/14 exit 0**; 4-phase `--selftest`; **negative control on the real pre-fix file** — the gate named **all 29**, matching the independent measurement exactly, file restored byte-identical; `audit03n-npc-registries.test.js` **6/6** and **all 6 fail on the pre-fix file** (neither behaviour test is vacuous); NPC/audit family 45/45; full Playwright **732 passed / 4 failed** = the documented crud-arrays four, nothing new. **§7½ DONE:** the victory scroll now renders the earned lines for Quill, Weckmann and Auros instead of the three stranger lines, and the Auros survey button renders at HKG (1, was 0); 0 page errors. **The lesson recorded: two `npcOrder` lists disagreed, and that was the tell** — when two lists of the same thing disagree, distrust the one nothing else corroborates. Docs: `story.md` §Epilogue, `world.md` §Cross-References, `prompt.md` §7 (13→14), `index.md`, plus **dated KEY-DRIFT NOTES on the three lab reports** whose design-time blocks still show surname keys (annotate, don't rewrite — §DX-02c/§AUDIT-03m precedent). **Side finding filed, not swept: §AUDIT-03o** — favor **3 is unreachable for 5 of the 6** NPCs (every writer measured), so the `entries[3]` epilogues this row just unlocked sit behind a tier nothing grants; needs a design call, and overlaps §RIPPLE-01-C / §NPC-01-D. **Next: §AUDIT-03d (still the gating row — needs the staging design call; it releases 983 quests), else §AUDIT-03o (measured, but a design call), §DX-02g (2 field PUTs), §DX-01e (line-anchor drift — the last open §DX-01 row), §AUDIT-03i/k/m, or §VM-01-G2b / the G4 child report.**
>

> **⟶ PRIOR (2026-07-30) — §AUDIT-03h ✅ SHIPPED: the last 10 display-name `npc` values are keys now — and the one the row could not resolve turned out not to be an NPC.** On "continue" took the row that needed no design call (§AUDIT-03d is still the gating row and still needs its staging decision). Existing-Work-First: tree clean at `f96b4e5`, WBAPI server down, a 2nd claude process present but the game file untouched by it; the row's premise re-measured live before any edit — **exactly** the 10 quests it names, and no others. **The premise held; two of the row's four guessed keys did not.** `Gret Orrens` → **`gret`**, not `gret_orrens`; `Pier Falk` → **`pier`**, not `pier_falk` — both MISS against the live 297-key `npcKeyVocab()`, which is why the keys were measured rather than slugified. **The row's open question — "`bog_mudwhistle` may need a profile or a different anchor" — dissolved on measurement: Bog Mudwhistle is not an NPC.** He is a **`NPC_TOUR_OPPONENTS`** row (`key:'bog'`, *Subsistence Fisher*), a Yugurt-tournament minigame opponent living in a **fifth** registry `npcKeyOk` does not read — and the sibling `quest_tour_*` family, each quest **titled after an opponent** (`quest_tour_03` is literally *"Bog's Terms"*), **already anchors every one of its quests to `emmer`**. So `quest_guide_04` — beat 4 of Emmer's own arc, in which Bog heckles Emmer's cast — takes `emmer` like its five siblings; no profile authored, no taste exercised. **Shipped:** `guide_01–05`→`emmer` · `guide_06`→`the_fisherman` · `scar_01/03/04`→`gret` · `scar_02`→`pier`, each corroborated twice (profiles at the arcs' own `activateNode`s SSJ/NUE, **and the arcs' own `onComplete` favor bits already spending the exact keys**; `the_fisherman` is registries 2 *and* 3 landing on one slug). Applied in ONE save through the `POST /api/batch/npc` handler's own sequence run in-process (§AUDIT-03c precedent — byte-identical to the server PUT, no daemon), **dry-run in memory first** and only written once it returned 10/10 `inserted:false` with favor bits untouched. **Evidence:** pre/post **golden parse diff across all 7 collections = exactly 10 entry diffs, every one `fields=npc` alone, 0 lost / 0 gained**; all **16** nested `favor` npc values byte-identical; file diff exactly **10+/10−**; quests still **2,853**; **`./api.sh audit` errors 0 / warnings 48** unchanged and `advise` clean on the sampled quests; `check:walk` **12/12 exit 0**; **373 Playwright green, exit 0**, incl. new `audit03h-npc-normalize.test.js` **5/5** with a **negative control** (a re-planted `"Emmer Finch"` is caught). The §AUDIT-03b pin is now `toEqual([])` as that row instructed. **No §7½ — `quest.npc` has zero quest-level client reads (§AUDIT-03b), so nothing is player-visible.** Docs: `quest.md` gained the write-the-key-never-the-display-name rule, the 5-row derivation table, and the ⚠️ **the key is not always the name slugified** warning plus its inverse — **`NODE_MAP`'s inline `npc` is *supposed* to be a display name** (`SSJ.npc = 'The Fisherman'`); normalizing *that* would be the bug, since it is what makes the key resolve. That inverse also caught an over-broad source guard in my own first test draft, which is why the guard is now scoped to the `WORLDBUILDER:QUEST_DB` markers. **Side finding filed, not swept: §AUDIT-03i** gains Bog Mudwhistle **and the registry-5 question** — the five named, quoted, player-faced tournament opponents sit outside `npcKeyOk` entirely, and `bog` is a *near-miss trap* (a real `key:` in a real registry, just not one of the four). **Next: §AUDIT-03d (still the gating row — needs the staging design call), else §AUDIT-03i/j/k/m, §DX-01c/d/e, §DX-02b/c/d, or §VM-01-G2b / the G4 child report.**
>

> **⟶ PRIOR (2026-07-29) — §AUDIT-03g ✅ SHIPPED: the last 68 quests get an NPC anchor — `./api.sh audit` reports `errors: 0` for the first time, and not one of them was defaulted.** On "continue" took the row that needed no design call from the user (§AUDIT-03d is still the gating row and still needs its staging decision). Existing-Work-First: tree clean at `f08f70f`, a 2nd claude process present but the game file untouched by it, and the premise re-proved live before any edit (**exactly 68** quests with no `npc`, matching the row). **The derivation, family by family — no bulk default (that is the rot this family exists to clean up):** the load-bearing discovery is that **a §D02 Epic-Battleground *approach arc* inherits its battleground's giver** — `quest_ea_primary`/`_return` at RAI already carry `captain_selene_draketide` and `quest_eg_primary` at BK already carries `warlord_kael_mordus`, so `d0201_*`/`d0209_*` (10) and `d0205_*` (5) resolve from the corpus, not from taste. Then **`auros` ×24** (Commander Seraphine Bruhns — the profile key behind the inline npc of *both* HKG and TLS, the named figure of the Workshop schematics, and the Warrant thread's own anchor via `quest_signal_01`: `d0207_*`, `d0204_*`, `d0210_*`, `d0206_*`, `quest_void_below`, the 3 Void-tide bounties) · **`archivus_sweelinck` ×9** · **`yael` ×8** · **`johannes_von_weisheit` ×5** (the Event Horizon Station literally speaks in `quest_math_03`'s `onComplete`) · **`watcher_gvw` ×2** · **`city_guard_captain` ×2** · **`brynn` ×2** · **`crov` ×1**. Applied in ONE save via `./api.sh batch-npc`; all 68 took the `insertStringField` path (none had a top-level `npc:`), and the 68 entries were pre-scanned for trailing `//` comments first — the insert appends a comma after the entry's last line, so a comment-terminated entry would have produced a syntax error. **Evidence:** pre/post **golden parse diff across all 7 collections = exactly 68 entry diffs, every one `fields=npc` alone, 0 lost / 0 new / 0 collateral**; `./api.sh audit` **errors 0**; quests still 2,853; the unresolvable-key list still exactly the 10 §AUDIT-03h display names; `check:walk` **11/11 exit 0**; **333 Playwright green** (`quest-runtime-uqf` 303/303 + new `audit03g-npc-coverage.test.js` 4/4 + `audit03b-npc-anchor` 4/4 + `npc-card-map` 22/22); Hazard #1 respected. **No §7½ pass — `quest.npc` has zero quest-level reads in the game client (§AUDIT-03b), so nothing here is player-visible.** Docs: `quest.md` gained the coverage statement + the per-family derivation table. **Side findings filed, not fixed: §AUDIT-03i** +4 unkeyed rendered speakers (the **Mother Mimic** — LIM's presiding character, which is why `d0208_*`'s anchor is provisional · the **Crimson Warrant** · **Sir Jullean** · the **Inquisitor construct**) and new **§AUDIT-03k** (one character, two accepted keys — `yael` vs `city_guard_captain`, and the same shape for `brynn`/`auros`/`archivus_sweelinck`). **Next: §AUDIT-03d (still the gating row — needs the staging design call), else §AUDIT-03c/h/k, §DX-01c/d/e, §DX-02b/c/e, or §VM-01-G2b / the G4 child report.**
>

> **⟶ PRIOR (2026-07-29) — §AUDIT-03b ✅ FULLY SHIPPED: all 203 `long_john_silver_sen` mis-stamps re-anchored; the NPC-key vocabulary and the WBAPI field patcher fixed underneath.** On "continue" executed the plan the prior session recorded. Existing-Work-First: tree clean at `221a1ef`, 203 stamps confirmed live, a 2nd claude process present but the game file untouched by it. **Shipped:** the ~12 OPEN per-quest calls resolved by reading each quest's text; the whole 203-quest mapping re-derived and applied (**203/203 correct · 7 genuine Silver quests kept · 196 changed · 0 unresolved**). **Three defects fixed on the way, two of them in the row's own premises:** (1) `_npcOk`/advise accepted only 2 of the 4 registries that hold real speakers → replaced both duplicated server closures with one shared **`WBAPI.npcKeyOk`/`npcKeyVocab()`** over BIRKA_NPC + NODE_MAP inline + NPC_DIALOGUES + `EB_NPC_DIALOGUE` (parsed for the first time — it lives outside the WORLDBUILDER markers); vocabulary **270 → 297**. (2) **`patchStringField` was not depth-aware** — the first batch said `patched:196, errors:[]` but 6 writes silently no-op'd because the first `npc:` token in those entries is a *nested* `{kind:'favor', npc:…}`; had the target differed from the nested value it would have **rewritten the favor bit instead**. Fixed with a string/comment-aware `firstTopLevelMatch` depth map; proof of zero collateral = all 16 `favor` npc values byte-identical before/after, game diff exactly 196+/196−. (3) the row's `hunt2→tessie` claim was wrong (Tessie is not one of the 20 EB givers; she is prose-only) → `the_elder_fisherwoman`, the FU6 referral source. **API-first:** `POST /api/batch/npc` existed but had no wrapper → added **`./api.sh batch-npc <updates.json>`** (one save for the whole batch, not 196 rewrites). **Evidence:** `check:walk` **11/11 green** · `quest-runtime-uqf`+`npc-card-map` **325 passed** · new `audit03b-npc-anchor.test.js` **4/4** · quests still **2,853** · Hazard #1 respected (js edits with the server stopped; fresh PID per write session; `_monsterLevel` signature survived). Docs: `quest.md` gained the "`npc` field — authoring metadata, not gameplay" section (4 registries + anchoring rule + `batch-npc` recipe). Side findings logged as new rows, **not** fixed inline: **§AUDIT-03g** (68 quests with no npc anchor at all — the audit error that caused the original bulk default), **§AUDIT-03h** (10 quests holding display-name values like `"Emmer Finch"`, now pinned by the new test), **§AUDIT-03i** (named rendered speakers with no registry entry — Roen at 92 refs, Tessie, the Warden, Fehn, Pembury/Aldous, Solm, Tommy; needs a design call). **Next: §AUDIT-03c (the `710bb75` dead-code remap audit) / 03e / 03g / 03h, §DX-01c/d/e, §DX-02b/c, or §VM-01-G2b / the G4 child report.**
>

> **⟶ PRIOR (2026-07-28) — §AUDIT-03b ANALYSIS COMPLETE, ZERO EDITS MADE — ⚠️ INCOMPLETE, paused to save tokens. The whole derivation is recorded in the §AUDIT-03b row below; next session executes it.** Existing-Work-First: tree clean at `2de4e7b`, 203 stamps confirmed live (201 double-quoted + 2 single-quoted at ~11413/11424), a 2nd claude session was running (s000, since 8:55AM) but tree stayed clean. **Established (the row's step 1): `q.npc` is authoring-metadata ONLY — the game client `play.html` has ZERO quest-level `.npc` reads.** Readers: edit.html (7 display reads), server `_questsByNpc` index, `advise` (unresolvable key = **warning** only, `wbapi-core.js` ~811), audit (**error** only when npc missing entirely — this is why `ea02faf` bulk-stamped a default), NPC delete-guards, export. **⇒ no gameplay impact, no golden-state diff needed; fix = 203 flat-field PUTs (`./api.sh put quest <id> npc=<key>`), restart server first (Hazard #1).** Key discoveries: (1) the **7 `sen_c*` quests are LEGITIMATELY Silver's** (every scene is built on him — keep); (2) NPC_DIALOGUES has 154 keys incl. `jimmy`/`sandy_cat`/`kenickie`/`rennau`/`solvak`/`yva`/`isolde_voss`/`benedikt_rasp`/`vonn`/`emmer` — but `_npcOk`/advise only accept birkaNpcs + inline `node.npc` keys, so those stamps will advise-warn (design point in the row); (3) EB givers are canonical in `EB_NPC_DIALOGUE` keyed by EB node (FLR→Harbormaster Tula … TBS→Warlord Kael Mordus, WRO→**Tessie** — who is the hunt2 arc's named speaker). **Next: execute the mapping in the §AUDIT-03b row** (restart server → scripted PUT loop → reparse to assert 0 remaining mis-stamps minus the 7 kept → `check:walk` → doc-sync quest.md note → commit `feat(§AUDIT-03b)` + say). ~12 quests still need a per-quest call (marked OPEN in the row) — resolve them by reading their texts/dialogues before the PUT loop.
>

> **⟶ PRIOR (2026-07-28) — §NPC-01-SF4 SHIPPED: dead codes `CQ`/`SQ`/`GC` remapped to `CDG`/`NUE`/`TRD` — §NPC-01 is now FULLY CLOSED (A–D + SF1–6 all shipped).** Resumed `feat/npc-01-card-map` on "read the backlog and continue." The one open §NPC-01 item was SF4, flagged "needs a user decision — the quests carry no `activateNode` so the home node must be chosen from each cluster's arc geography." **Grep-before-building dissolved the ambiguity:** each cluster's arc has exactly ONE render-gate node in the live file — **CQ→CDG** (Layer 44 Ally Cat Arc gates on `node.code==='CDG'`; Jimmy is CDG's own NODE_MAP `npc`), **SQ→NUE** (Layer 51 Weimar Scholar Gate gates on `'NUE'`), **GC→TRD** (Layer 55's "Find Yva (50gp)" scene gates on `'TRD'`) — and the 6 NPCs' own `NPC_DIALOGUES.meta.node` fields (never consulted by the render map) **independently declare exactly those three nodes**. Presented the derived mapping via Ask; **user confirmed "remap as derived."** Implemented as a **pure key remap, gating verbatim**: `CQ:_cqNpcs`→`CDG:_cqNpcs`; `GC:…`→`TRD:…` (same `vsDebtProbed && !vsWeaponsFound` window); SQ merged into the existing curated NUE entry via `NUE:_nueNpcs.concat(_sqNpcs)` (NUE was already curated with gret/pier — the only non-trivial part); `_curatedGoverned` unchanged; §NPC-01-SF4 comment block added beside the §PLAY-01-G one. No collisions (no profile declares `node:'CDG'`/`'TRD'`). `NODE_NPC_KEYS`' own dead codes are a separate legacy map (map-color/farewells) — out of scope, not touched. **Evidence:** `npc-card-map.test.js` **+6 → 22/22** (CDG fresh/Jimmy-only · CDG quests-complete/all-3 · NUE fresh/Isolde-joins-Benedikt-gated · NUE wmArchiveComplete/Benedikt-appears · TRD window both sides · source guard no-dead-keys); `check:walk` sub-checks parity/behaviour/terrain/roomsparity/questparity/gateast/rng/questgraph all green; only the documented J14/J15 + TGS/SPB baseline reds (unchanged). Render-only, outside all parity fences, RNG N/A; Hazard #1 respected (`:1367` verified stopped). Docs synced: `story-arc-npc-dialogues.md` (Dead-code remap note) + SF4 row `[x]` + the §NPC-01 Open-at-a-Glance line → **FULLY CLOSED**. **Next: a fresh track — §VM-01-G (the migration front, biggest/riskiest, 🟢 ASK), §BOARD-01-D (ripple/codex merge, optional), a §POT-PROMOTE track (§TIDE-01 / §CODEX-01 / §RIPPLE-01 — the §NPC-01 unlock they waited on is now complete), or the remaining §DX-01 rows (a/c/d/e + the §DX-01f `[x]`-row bulk migration).**
>

---

> **Archive:** closed / completed work is archived in [plan-archive.md](plan-archive.md); the *how-we-work* rules live in [CONTRIBUTING.md](../../CONTRIBUTING.md).

---

*© 2026 Paul Richeson — MIT License.*
