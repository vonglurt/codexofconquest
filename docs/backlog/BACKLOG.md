> **This file was split into six phase backlogs on 2026-08-23.** It no longer carries rows itself — it carries the directive, the routing table, and the cross-phase §RESUME chronology. Every open row, track record and theme bucket from the former single file lives in exactly one of the six. **Completed increments were condensed out of all seven files on 2026-08-25:** one line each in **[`../archive/backlog-resume-history.md`](../archive/backlog-resume-history.md)** (per phase) and in the chronology table below (cross-phase); full prose for recent closes stays in [`plan-archive.md`](plan-archive.md), and the pre-condensation text is in `git log -- docs/backlog/`.<!-- SPDX-License-Identifier: MIT — Copyright (c) 2026 Paul Richeson -->
# BACKLOG — Outstanding Work (routing index)

> **This file was split into six phase backlogs on 2026-08-23.** It no longer carries rows itself — it carries the directive, the routing table, and the cross-phase §RESUME chronology. Every row, track record, theme bucket and history entry from the former single file lives verbatim in exactly one of the six; the split was verified line-exact (0 lines lost, 0 duplicated).

> **This file is the list of outstanding activities.** The *how-we-work* rules live in **[CONTRIBUTING.md](../../CONTRIBUTING.md)**; closed/shipped work is archived in **[plan-archive.md](plan-archive.md)**. Split out of the former `plan.md` on 2026-07-09.
>
> **⚠️ Before starting ANY row — Existing-Work-First (CONTRIBUTING.md § Existing-Work-First Policy):** `git status` + `git log` first (uncommitted/fresh work matching the row = it's done or in progress — finish it, don't redo it); check for a second live session (`ps aux | grep claude`) before editing shared files; grep to *disprove* the row's premise (rows go stale — four rows have closed as ALREADY SHIPPED). A §RESUME entry says where the last session *stopped*, not what the tree looks like *now*.
>
> **Legend:** `[ ]` open · `[~]` in-progress / recurring · `[x]` closed (kept here for context; migrate to plan-archive.md over time).

> **⚠️ Existing-Work-First now spans six files.** `grep -n '§DX-02xx' BACKLOG*.md` before you conclude a row is unclaimed — a row and the §RESUME entry that closed it may sit in different phase files.

## Standing Directive — Code Comments (user directive 2026-08-23)

> **Binding on every increment in this file.** Where this and a §-row's own instructions disagree, this wins.

**The failure this prevents:** writing a comment that narrates the change being made right now. That context is real, but it expires the instant the change merges — the defect it describes no longer exists, so the comment becomes a story about a problem no future reader can observe. It is a changelog entry in the wrong file, and a third copy of text already required in the commit body and the PR description.

- **CC-1 (MUST NOT)** Write a comment describing a change, a fix, a defect, its cause, or what the code used to do. No *"was / now / previously / instead of"*, no *"this fixes"*, no *"needed because otherwise"*, no *"note that we no longer"*.
- **CC-2 (MUST)** Apply the **survival test** to every comment before writing it: *would this still be true and useful to someone reading this file a year from now, who never saw the diff?* If it only makes sense beside the diff, it is changelog — delete it and put it in the commit body.
- **CC-3 (MUST)** Default to **zero** comments. Declarative config — Terraform, DNS records, k8s manifests, CI YAML, Helm values — is self-describing and takes none. A resource named `dmarc-example-com` does not need a comment saying it is the DMARC record.
- **CC-4 (MAY)** Comment only when a future editor would actively break something without it: a non-obvious external constraint, a required out-of-band manual step, an invariant the surrounding code cannot show. **One line.** If it needs a paragraph it belongs in a design doc, not inline.
- **CC-5 (MUST)** Before every commit, re-read the comment lines added: `git diff --cached | grep '^+' | grep -E '#|//|/\*'`. Each hit must pass CC-2 on its own. **Deleting is always an acceptable outcome.** *"I already wrote it"*, *"it is only one line"*, and *"this one is genuinely useful"* are not exemptions — the last one is the exact thought that precedes every violation.
- **CC-6 (MUST)** Applies to comments **edited** as well as added. When a change invalidates an existing comment, the default action is **DELETE**, not rewrite it into a new narrative.

> **⚠️ KNOWN TENSION, flagged not resolved — needs a user call.** This repo currently runs an *opposite* convention in `play.html`: **§AUDIT-03n / §DX-02c "annotate, don't rewrite"**, which deliberately preserves defect history inline (e.g. `const NODE_NPC_KEYS`'s 8-line comment explaining that 21 of 26 rows named retired node codes, or `_renderFinalMap`'s §AUDIT-03e note on why the node filter is held at the historical set). Those are exactly the *"what the code used to do"* narratives CC-1 forbids, and several are load-bearing: they are the only record of why a value is held where it is, and deleting them would re-open closed audit rows. **Until the call is made, the working rule is:** CC-1..CC-6 bind all **new** comments; existing §AUDIT-/§DX-annotated blocks are **not** swept under CC-6 — they are a documented archaeology convention, not accidental changelog. Filed as **§DX-02fa** below.

---

## The Six Phase Backlogs

Split by **subsystem**, not by track prefix (121 of the 175 sections were `§DX-02`, so prefix would not have divided anything). Each file restates the directive above in full and is self-contained: its own open rows, its own track records, its own §RESUME history.

| Phase | Backlog | Open rows | Closed (archived) | Scope |
|---|---|---:|---:|---|
| **1** | [Playable Truth](BACKLOG-1-playable-truth.md) | 4 | 14 | Shipped content the player can never reach or complete: unreachable act-gated beats, inert counters, gate flags with no writer, quest deadlocks, endings out of range. |
| **2** | [Engine & Systems](BACKLOG-2-engine-systems.md) | 45 | 59 | Rules and mechanics: combat math, XP and economy, progression and New Game+, save/state defaults, the render pipeline, the quest VM and its migration fronts. |
| **3** | [Content & Narrative](BACKLOG-3-content-narrative.md) | 19 | 46 | Authored strings and the people who say them: NPC dialogue and relationships, favor scales, arc beats, epilogue and ending prose, lines with no lookup path. |
| **4** | [World & Navigation](BACKLOG-4-world-navigation.md) | 8 | 18 | The map and moving on it: node registration and labels, terrain tables and encounter rates, GEO anchors and projection bounds, road nets, movement results. |
| **5** | [Platform & Tooling](BACKLOG-5-platform-tooling.md) | 31 | 25 | Everything outside the game loop: the WBAPI server and its endpoints, the worldbuilder and editors, the HTML parse/export layer, multiplayer mesh, the NPC-speak service. |
| **6** | [Verification & Documentation](BACKLOG-6-verification-docs.md) | 52 | 24 | What proves the rest is true: CI gates and parity fences, Playwright and MUD suites, the anchor resolver, engine comments, maintained-doc rot, and the §DOC-02 lab-report verification program. |

> **Phase order is a suggested work order, not a dependency chain.** Phase 1 is content the player cannot reach — the defects that make shipped work invisible — so it pays first. Phase 6 is what proves the other five stayed true. Any phase can be picked up on its own.

---

## Open at a Glance — where each track now lives

The per-track glance bullets moved with their tracks. This table says which file to open.

| Track | Phase backlog |
|---|---|
| §DOC-02 — Lab-report verification program | [Phase 6 — Verification & Documentation](BACKLOG-6-verification-docs.md) |
| §VM-01 — The Quest VM → *No Word for Wait* | [Phase 2 — Engine & Systems](BACKLOG-2-engine-systems.md) |
| §BOARD-01 — The Warrant’s Board | [Phase 2 — Engine & Systems](BACKLOG-2-engine-systems.md) |
| §POT-PROMOTE — 12 planned tracks | [Phase 2 — Engine & Systems](BACKLOG-2-engine-systems.md) |
| §DX-01 — Developer-Experience & repo-health (incl. §DX-01j lab-report index) | [Phase 6 — Verification & Documentation](BACKLOG-6-verification-docs.md) |
| §DX-02 — engine/data contract tails | [Phase 5 — Platform & Tooling](BACKLOG-5-platform-tooling.md) |
| §AUDIT-03d + h/i/j/k/m — quest-data appended-field rot | [Phase 1 — Playable Truth](BACKLOG-1-playable-truth.md) |
| §EDITOR-04 — the mission wizard | [Phase 5 — Platform & Tooling](BACKLOG-5-platform-tooling.md) |
| §MESH-01-REVIEW tails | [Phase 5 — Platform & Tooling](BACKLOG-5-platform-tooling.md) |
| §IDEA-01 — Idea Generator | [Phase 3 — Content & Narrative](BACKLOG-3-content-narrative.md) |
| §NAV-01-FU — deferred map centering | [Phase 4 — World & Navigation](BACKLOG-4-world-navigation.md) |

Closed tracks (full records in `plan-archive.md`): §NPC-01 (2026-07-28) · §PLAY-01 · §DEATH-01 · §XP-01 · §CLEANUP-01 · §KG · §1367 · §GR/§GR-D · §MBIT-02 · §MESH-01/-02 · §MP-CHAT-GLOBAL/§MP-MAPTABS/§MAP-NAV · §NAV-01 · §WALK · §ARCH-01 · §BOARD-01-FU1–8.

---

## §RESUME — cross-phase chronology

> **208 entries, newest first** (`grep -cE '^\| [0-9]+ \| 2026-' BACKLOG.md`; both this number and the table's *Open rows* column are asserted by `check:walk` gate #19, §DX-02gs). One line per closed increment: read top-down for *what happened last*, then open the phase file named on the right. Full prose for recent closes lives in [`plan-archive.md`](plan-archive.md); the per-phase condensed history is in [`../archive/backlog-resume-history.md`](../archive/backlog-resume-history.md). The phase backlogs carry open rows only.

| # | Date | Increment | Headline | Full entry in |
|---:|---|---|---|---|
| 1 | 2026-08-26 | §DX-02z ✅ | **A missing price and a cheap price were the same value at the call site.** All six condition-cost lookups read `CONDITION_GOLD[…] || 20`, and **20 gp is the pre-Layer-18 scale the ×100 repricing existed to delete** — so a thirteenth condition item added without a price row would have shipped silently at a price that puts a three-round ADV-plus-status effect below a healing potion. **Latent, not live, and measured as such:** the two tables are a clean **12 ↔ 12 bijection** at HEAD, so nothing shipped was ever mispriced. Worth a gate rather than a comment for two independent reasons — the tables are **~2,200 lines apart and neither names the other**, and conditions are **not inventory-gated**, so gold is the only brake on the strongest pre-battle effect in the game and the price table carries the entire balance load alone. Shipped `check-condition-prices.js` as **`check:walk` gate #20**: both directions fail (an unpriced item **and** an orphaned price), a reinstated `||` **or** `??` fallback fails, and a **missing table is a failure rather than a vacuous pass**. All six fallbacks replaced by `_condCost(match)`, which throws — `CONDITION_GOLD[` occurrences **6 → 1**, the survivor being the guard's own lookup (the row said five sites; there are six). The stale `// filtered CONDITION_ITEMS matching current inventory` comment — contradicted by the engine's own comment eight lines below — is swept. **selftest 8/8 · `check:walk` 19 → 20/20 green · `npm test` 1060 passed, exit 0.** | [Phase 2](BACKLOG-2-engine-systems.md) → [archive](plan-archive.md) |
| 2 | 2026-08-26 | §AUDIT-03ae ✅ | **Four surfaces disagreed about what a story attack roll is, and every identifier involved resolved.** `S_story.atkBonus` was specified as a *levelling* bonus and shipped as a **cache of the STR modifier**, so the character sheet's `strMod + prof + atkBonus` counted STR twice, printed a breakdown that summed to something else, and the engine added a third reading on top. Measured live at HEAD (STR 16 / DEX 12 / L5 / longsword): **sheet +9, its own parts 6, combat +7**. After: **+6 / +6 / +6**. The row named three surfaces; a fourth — the **multiplayer duel export** — carried the same double-count, and there turned out to be **two** story→simulator syncs with the identical omission, not one. **The row's own premise needed one correction:** a *finesse* weapon masked the DEX substitution via `max(str, dex)` — and chasing that found the same defect on the sibling control, `#weapon-finesse`, which ships `checked`, has no source but the dice roller, and **no story weapon declares finesse at all**, so every story weapon always was one. A STR 8 / DEX 16 character rolled **+3** against a sheet printing **−1**. Both syncs now own both controls. `S_story.atkBonus` had no readers left and is deleted with both writers. **Which weapons ought to be finesse is a content call and was NOT made — filed as §WEAP-FIN.** **No gate can find this class**: the field had readers *and* writers and every line was live; the defect was in what it MEANT at each reader, so the pin asserts an equality between two surfaces rather than a value at one. **New `audit03ae-attack-bonus-agrees.test.js` 11/11 over 4 characters × 2 weapon kinds · a contract lock that was pinning the cache re-expressed · `check:walk` 19/19 · `npm test` 1060 passed, exit 0.** | [Phase 2](BACKLOG-2-engine-systems.md) → [archive](plan-archive.md) |
| 3 | 2026-08-26 | §DX-02y ✅ | `S_story.acBonus` was a **read-only field: two declarations, exactly one reader — the `(S_story.acBonus || 0)` term inside `_calcPlayerAc()` — and zero writers in the entire history of the repository**. Born dead, not retired: Layer 18 specified +1 AC at L5, +1 at L8 and +2 at L10 and none of it was ever built, so the term contributed 0 to a number the player reads off the character sheet. **This is the inverse of the write-only dead-constant class** — a live consumer with a broken dependency, which a gate scoped to *unread* fields walks straight past. Closed by **deletion (a)**, not by wiring: wiring it would have been a difficulty change to every character in the game dressed as a repair. **3 → 0 occurrences · 38,703 → 38,701 lines · the number the player sees provably unmoved**, since the deleted term was `+ 0` for every reachable state. Two inert `acBonus: 0` seeds swept from `helpers.js` and `capture-screens.mjs`. **The doc defect was one-sided and only deleting the field found it** — §DOC-02t corrected *"`atkBonus` and `acBonus` are updated immediately"* in `mechanics-combat.md` and left the identical sentence standing in `design/mechanics.md`, so a reader who greps meets the wrong copy first; six maintained docs synced, two lab reports annotated, three dead anchors de-anchored. **New `dx02y-ac-has-no-level-term.test.js` 4/4 pins the shape, not the number · `check:walk` 19/19 · `npm test` 1048 passed, exit 0** (1 flaky, filed as §DX-02ia). | [Phase 2](BACKLOG-2-engine-systems.md) → [archive](plan-archive.md) |
| 4 | 2026-08-26 | §DX-02ee ✅ | `retryGateDays: 0` was **not a value the engine could read**: the sole consumer is `S_story.day < att.lastDay + (q.retryGateDays || 1)@6813`, and `0 || 1` is `1`, so **42 quests carried the one-day cooldown their authors had written `0` to remove** and the field could not express one of its two written values. Closed by call **(b)** — all 42 zeros deleted, the coercion kept, so *absent* and `1` mean the same thing and every written value is honourable; **zero behaviour change, which is the point**. (a) `??` would have made 42 quests same-day-retryable unreviewed, and for the three Void-tide hunts the accident is the better threat model. **The write path could not express the write:** `null clears a field` held for **quoted** values only, so `retryGateDays=null` was refused as *not found* on a field plainly present — `src/js/wbapi-core.js:function removeStringField@437` gained an unquoted-scalar pass, guarded at both ends, and **refuses arrays and objects** rather than half-removing one. **One pin was reading the defect as the specification** — Wave 1l asserted the `0`s, green for the life of the bug. **42 → 0 zeros, 21 ones unchanged, round-tripped from disk · new `dx02ee-scalar-field-clear.test.js` 8/8 with a negative control · `./bin/api audit` errors 0 · `check:walk` 19/19 · `npm test` 1045 passed, exit 0.** | [Phase 1](BACKLOG-1-playable-truth.md) → [archive](plan-archive.md) |
| 5 | 2026-08-25 | §DX-02en ✅ | the game's best ending was **one point out of reach from the repository's first commit**: `_curseScore()`'s floor is −5 and both `Covenant Keeper` and the *Covenant Keeper (True)* ending asked for ≤ −6. **Measured before at `000ebb3`, through the engine's own functions over all 231 partitions: `Covenant Keeper` 0 states, `Warden` 1, `Keeper` 14, `Watcher` 30, `Wanderer` 186** — the run that does the hardest thing the game asks was handed *"You carry the work with you."* Shipped (a): `Covenant Keeper` `maxScore: -6 → -5`, `_isTrue` `curse <= -5`. **And the half the row left open is decided against leaving `Warden` decorative** — at `-5` its bracket `-5 < score ≤ 0` is provably empty (0 is unreachable), so it moved to `maxScore: 3`, the band its own text already describes. The row's *own mandated assertion* — every label earned by at least one reachable state — is what forced it. **After: 1 · 4 · 10 · 30 · 186, every rung earned**, the sole `Covenant Keeper` state being exactly the 20-of-20 run. **F2 shipped in the same pass:** all four `endingEl` branches now append `_covenantStanding().label`, so the label the player carried for 40 hours is spoken back — including the Cursed Seal echo, which said *"the Warden who arrived here"* while the sheet read Wanderer. **The row's own arithmetic was one value out and the new test caught it:** the reachable set is `{ −5 } ∪ [1, 58] ∪ { 60 }`, not `[1, 60]` — 59 needs `3s + n = 59` with `s + n ≤ 20` — corrected in the row, `story.md` and the lab report. **Latent until §EPIC-01**, which owns `ebReturnDone`'s missing writer, and said so rather than claimed as a live win; **§ENDING-01 (b) closed by this row.** `check:anchors` went red on nine docs quoting the retired `curse <= -6` anchor — five took the §DX-02hw quote escape, two were rewritten as live description. **New `dx02en-covenant-standing.test.js` 4/4 · `check:walk` 19/19, 10.0s · `npm test` 1037 passed, 0 failed, 3.7m.** | [Phase 1](BACKLOG-1-playable-truth.md) → [archive](plan-archive.md) |
| 6 | 2026-08-25 | §DX-02fz ✅ | `check:walk` hung at gate 4 **after printing gate 4's ✓**, intermittently, across three sessions, with three causes proposed and each disproved by the next measurement — the gate's logic, a live WBAPI server, and finally *"only the `npm` wrapper hangs"*. **That last conclusion is wrong.** A bounded 32-trial probe at HEAD: `npm … >/dev/null` **6/8 hung**, `npm … | cat` **0/8**, and the case the row had ruled out — bare **`node scripts/check-mover-behaviour.js >/dev/null` — hung 1/8.** **The mechanism is named, not correlated:** a hung process was caught and `sample`d — `node::Environment::Exit → DisposePlatform → NodePlatform::Shutdown → WorkerThreadsTaskRunner::Shutdown → uv_thread_join → __ulock_wait`, with a live `node-V8Worker` thread. **`process.exit()` on Node v26.6.0 disposes the V8 platform from inside the exit handler and blocks forever joining a worker that never finishes.** That explains the ✓-then-hang (the work and flush are done, the process never reaps), why stdio shape only shifts the odds, and the strays found alive at 7 h 15 min. **Fixed and measured against a paired control:** success-path `process.exit(0)` removed from all **18** gate scripts, 24 sites. 40 trials each, back to back, same machine — **control hung 17 of its last 18; treatment 0 of 40**; 40 more across five converted scripts, 0. Every branch path re-exercised (`--selftest`, `--census`, `--legacy`, `--snapshot`) and a **planted drift** proves a failure path still exits 1. **Why `check:walk` had already stopped hanging:** §DX-02hi's runner spawns `sh -c '<raw script>'` with stdout on a **pipe**, dodging both discriminators by accident — now stated as load-bearing. Rule 0 added to `CONTRIBUTING.md` § Test-Run Rules. **Residual filed as §DX-02hy:** `process.exit(1)` is the same deadlock. **`check:walk` 19/19 · `npm test` 1033 passed, 0 failed.** | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 7 | 2026-08-25 | §DX-02gi ✅ | `CONTRIBUTING.md` said *"run `say "<commit subject line>"` … the **subject line only**, via macOS `say`"*; `resume.md` §2.6 and §0 say `src/bin/say.sh`, **never raw macOS `say`**. Both files are binding — `CONTRIBUTING.md` is reading #4, *"the policies, verbatim and binding"* — so a session reading in order got both. **Shipped (a): `CONTRIBUTING.md` cedes the rule to `resume.md` §2.6.** The repo **ratifies** rather than decides it — `say.sh` enqueues to the `sayd.sh` daemon and returns immediately, so announcements serialise and a long increment never blocks on speech, and the rule naming raw `say` is dated *learned 2026-07-03*, before the script existed. **The "verbatim subject line" clause is settled by counting binding surfaces, not taste:** `AGENTS.md`, `resume.md` §0 and `resume.md` §4 step 11 all say **written for the ear** — three to one — now stated explicitly in §2.6 with a worked example. **A second raw-`say` the row had not found:** the *Loop vs. Ask Rule* prescribed `say "If you say yes: …"` in a runnable block, which the row's own verify criterion would have passed over because it is not commit-time. Corrected. **Found and deliberately not filed:** `CONTRIBUTING.md`'s `./wbapi-toggle.sh` and `./api.sh` references are already **§DX-02gh** (489 dead references, Phase 5, open). **`grep -n 'say "' CONTRIBUTING.md resume.md | grep -v say.sh` now returns one line, and it is prose.** **`check:walk` 19/19 · `npm test` 1033 passed, 0 failed.** | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 8 | 2026-08-25 | §DX-02gj ✅ | `seedAndLoad` + `dismissContinue` was not a neutral starting state — `dismissContinue` reaches `storyRender`, which renders NPC cards, so any state a render *writes* is advanced by one before the test observes anything. **Reproduced rather than taken on trust:** re-seeding `dx02aj` at `TLL` with the reset removed gives **`brynn` red and every other NPC green in the same loop**, the predicted signature; with the reset in place the file is green at `TLL` too, so the fix is the reset, not the node. **The footprint is four times what the row measured.** The inline reset cleared **2** things; enumerating every `S_story[…] =` inside `_renderNpcCard` *and its callee* `_getNPCDialogue` gives **9 shapes**, including the visit counter `npcVisitCounts[key]` that the row's own "general shape" paragraph names — **seven of the nine were reset by nothing.** **And the row's model of which entities are hit is too narrow:** seeded at `TLL`, `yael` — pinned to `LHR`, which the row calls clean — comes up carrying `act8FarewellYael` and `yaelOnboardingSeen`. The selectivity is per-KEY as well as per-entity. Shipped `resetNpcRenderState(page, keys)` (clears all nine, returns what was set going in) and the durable half, `expectNpcRenderStateClean(page, keys)`, which **fails at the setup line naming the cause** — proved by planting the contamination and reading the message. `dx02aj` routes through both at all five measuring sites and its private copy of the rule is gone. **`check:walk` 19/19 · `npm test` 1033 passed, 0 failed.** | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 9 | 2026-08-25 | §DX-02gs ✅ | the routing table's **Open rows** column was maintained by hand **inside the file it counts**, and the row records five prior drifts. **The sixth was measured at HEAD and it had drifted in both columns again** — phase 5 cell 31 vs actual 30, phase 6 cell 55 vs actual 54, and the §RESUME header **191 entries** vs **199** rows. A row archived without decrementing and one filed without incrementing are each silent and do not cancel, so no reader can correct for it. Shipped **`check-backlogcounts.js` as `check:walk` gate #19**: open rows are `^### §` headings **between** a phase file's `## §BACKLOG — Open Items` marker and its first `## Track records` / `## §RESUME` marker, and **the range is the load-bearing half** — four selftest checks pin it, because this row's own first census counted whole files and named a phase-2 cell that had been correct all along. Failures name the phase, both numbers and the direction; a table that has **lost** a phase row is caught rather than silently counted short. **Asserts only, never rewrites** — a checker that repairs what it audits makes its verdict depend on run order (§DX-02fx). **Not derived, and said so:** the *Closed (archived)* column stays by hand, because `plan-archive.md` is not split by phase. The three drifted numbers were corrected and every *18 gates* claim swept to **19**. **It proved itself immediately** — archiving this row took phase 6 from 54 to 53 and the gate went red until the cell followed, which is exactly the failure mode it was filed about. **Selftest 10 checks · `check:walk` 19/19, 9.9s · `npm test` 1033 passed, 0 failed.** | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 10 | 2026-08-25 | §DX-02gu ✅ | the clean-tree invariant — a full `npm test` leaves the tree naming only the increment's own edits — had been restored **by hand three times** (§DX-02gm's 2,883-hint doc sweep, §DX-02gn's screenshot on a tracked path) and asserted nowhere, both rows found by accident. It cannot be guarded from inside the suite: a Playwright test cannot snapshot the tree around the run that contains it. **The call is *where*, and re-deriving killed one option's premise:** (b), the CI step, assumed CI runs the suite — **no workflow does**, `walk-invariants.yml` runs `check:walk` and `test:mud` only, the same finding §DX-02ht reached from the trace side. So (b) is **blocked**, not *after*, and the row says so instead of leaving a recommendation that reads as scheduled. Shipped **(c)**: `src/scripts/cleantree.js` as npm `pretest`/`posttest` — snapshot `git status --porcelain` before, diff after, **warn** and name every path the run dirtied that was clean going in. **It matches on path, not status code**, so a file already dirty is the increment's work — proved by planting two changes and getting back exactly the one the run would have caused. Two limits stated in the file rather than hidden: npm skips `posttest` on a **failing** suite, and the CI half is blocked. Named `cleantree:selftest`, not `check:cleantree` — the latter is rejected option (a)'s name. **`npm test` 1033 passed, 0 failed, 3.7m, posttest silent · selftest 5 checks · `check:walk` 18/18, unchanged (deliberately not a gate).** | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 11 | 2026-08-25 | §DX-02gx ✅ | `check:battlepools` direction 2 tested `tier:'deadly'` while 48 monsters at other tiers met the identical condition unremarked, so its exemption list read as a defect ledger. **Re-deriving moved the number and found a tier:** the row measured 50 of 384 across four tiers; at HEAD the same rule gives **51 of 419 across five** — `trivial`, 32 entries, 1 unreachable, which the row did not know existed. **The call is (b), and the repo settles it:** `monsters.md` heads the collection *"Dark Fantasy Bestiary II — ✅ Implemented"* and catalogues it **by category** — a library, not an inventory — while `deadly` is different in kind, a 187+ HP statblock authored for one fight. (a) would manufacture 49 rows nobody asked for; (c) would throw away the check that caught `void_warlord` and `bruxa_corvo_bianco`. **Shipped (b) plus the thing that makes (b) honest:** the header and direction 2 now state the policy instead of implying the whole pool was walked, and **`--census`** prints per-tier reachability on demand, so the 49 are one command away rather than a doc figure that rots. `indexSource()` and `isReachable()` are extracted and read by **both** gate and census, so the census cannot drift from the rule enforced. Selftest **7 → 9**. **It unblocks §DX-02gw without deciding it** — the shared question *is an unplaced named monster a defect* is answered **no** below `deadly`, so that row's remaining two members are a retire-or-keep call, not a find-a-host one. **`check:walk` 18/18, 10.0s · `npm test` 1033 passed, 0 failed.** | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 12 | 2026-08-25 | §DX-02hx ✅ | **NOT A DEFECT — my own row, disproved by reading the quest the gate is attached to.** Filed during §DX-02hh: the TLS final battle needs 7 shards via `_finalBattleReady` while *"the TLS quest"* lists at `gate:{ shardsMin:6 }`, so the mission appears one shard before its Fight button works. `shardsMin:6` occurs **exactly once** in `play.html` and is **not on the final-battle quest** — it is Act I of the five-act skill-check arc **§D02-10, *"The Seventh Shard"***, which is about acquiring the seventh, so six is the coherent number and there is no Fight button. The final quest `mq_7` *"The Reckoning"* is `gate:{}`, always listed, the requirement carried in its hint; the 7 is enforced once, by node data, and nothing competes with it. **How it went wrong:** §DX-02hh grepped `shardsMin`, found one hit at an `activateNode:'TLS'` quest, and inferred *"the TLS quest"* from the node code. The arc's own header comment is two lines up. **Grepping the field found the value; reading the quest found the meaning.** Shipped the correction to `spec-engine.md` §Gate Locks, which now names `mq_7`'s empty gate and the §D02-10 arc separately. Also grounded and also not a defect: the arc awards no shard because shards are minted in exactly one place, from node loot. **`check:walk` 18/18 · anchors 4118 → 4123, 0 dead.** | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 13 | 2026-08-25 | §DX-02hg ✅ | `world.md`'s Layer 59 heading read **⚠️ PLANNED** for a layer that is mostly live — the fifth such heading after §DOC-02ar, §DOC-02bz and §AUDIT-03ak's four. **The ground extended the row.** It measured two halves; re-deriving `Layer 59` against `play.html` instead of reading the section found **four** live pieces — the NPC pressure lines, the two statically-rostered `voidTainted` monsters, **`_voidFlavorLine@26954`** (a per-node line appended to the story box on every render), and **the mercy window**, which spends `void_mercy_count` instead of raising pressure. **The last two had no entry in the section at all**, so the doc was not merely mis-headed, it was short two shipped mechanics. Only `_applyVoidPressureMonsters()` was never built, and it still greps to 0. **The design call — split the section or state the split in the heading — is settled by the file's own precedent:** `world.md:172` already carries the qualified form (*"Core mechanic implemented … Tournament circuit is ⚠️ PLANNED follow-on"*), and both halves already have their own subheadings, so cutting the section in two would duplicate its framing for nothing. Shipped the qualified heading, a status-corrected parenthetical naming all four live anchors, per-subheading verdicts, and a new **Void Flavor Lines and the Mercy Window** section for what was missing. All 8 new anchors resolve (4110 → 4118, 0 dead). **`check:walk` 18/18, 10.4s · `npm test` 1033 passed, 0 failed.** | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 14 | 2026-08-25 | §DX-02hh ✅ | `spec-engine.md §Gate Locks` said the locks are *"checked inside `cellMove` using `destCode`… identical to the old `storyMove` checks"*. `cellMove` is a thin caller over `Mover.move` with **no gate branch of any kind**, and `storyMove` is `storyMove_LEGACY`, called by nothing. The **table was right and kept**; the framing was replaced — free movement is inviolable, and these are mission gates read in one place, `storyCheckQuests`. **Attributing each of the ten rows to its enforcer is what found the defects.** Eight resolve to a UQF `gate:{…}` — including the discovery that `barnachVouchedHR + hellenistsThreaten` is **two separate quests, one flag each, with no quest requiring both**. **Two enforce nothing:** `escapedDamascus` is written as a `mission_bit` and read by no gate, and `defeatedBattles['KIR']` is read **once**, to choose which sentence a Dunfall panel prints. Both are the §AUDIT-03bj shape and are now marked inert. The `any → TLS` row is enforced by **node data, not a gate** — `NODE_MAP.TLS.finalBattle:{minLevel:20,minShards:7}` via `_finalBattleReady` — while its quest lists at `shardsMin:6`, so **the mission appears one shard before its Fight button works**; filed as **§DX-02hx**. `### Time Cost` was false in the same direction (*"every `cellMove` call advances `hoursElapsed`… by 1"* vs `cellMove`'s own §TIMELESS-01 comment) and was rewritten. **The deferred 🟡 — tombstone or exempt the history sections — is settled by existing precedent, not taste:** §DX-02c / §AUDIT-03m already say history is annotated, never rewritten, so `spec-engine.md` §Layer 7 and `spec-migration.md` §X-C each got a one-line RETIRED note pointing at the live section and kept their original bullets. **Running the row's own verify criterion found two more live sites it had not named** — a pseudocode comment at `:323` and the function table at `:930`, the latter directly contradicting `maps.md:723`. **`check:walk` 18/18, 10.0s · `npm test` 1033 passed, 0 failed.** | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 15 | 2026-08-25 | §DX-02hw ✅ | a doc could not **quote** a dead anchor: in a code span the quotation is an anchor, so §DX-02hv's own closing commit went red on **7** of them — `ANCHOR_RE` cannot tell a ship record from a live pointer. The escape that worked (keep the `@N` outside the span — *`` `edit.html` ``@5881*) was written down nowhere. **My own recommendation was half wrong and the measurement said so:** the row proposed documenting the escape *plus* exempting archived ship records, but `plan-archive.md` holds **128 live symbol anchors** in 794 KB and `BACKLOG.md` another 17 — exempting the archive would blind the gate to 128 real pointers to buy an escape for a handful of quotations. History is annotated rather than rewritten as to its *numbers*, not as to whether its *symbols* still resolve. Shipped the escape as a stated rule in `CONTRIBUTING.md` and `docs/design/index.md` gate #15, and **selftest check #9 `selftest[quote]`** as its witness — a quoted anchor is not audited while a live one in the same doc still is. Selftest **9 → 10**. Two dead-anchor bullets corrected to match what the gate now says. **It caught itself on the way in:** the new bullet's first draft put *`` `…` ``@1234* in a span, minting a live anchor whose symbol is `…` and taking the corpus 4113 → **4114**; the count was the only thing that noticed. **`check:walk` 18/18, 10.0s · `npm test` 1033 passed, 0 failed.** | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 16 | 2026-08-25 | §DX-02hv ✅ | the gate chain and the suite were **already red at HEAD** — `check:walk` 17/18, `npm test` 1031 passed/**2 failed** — on one dead anchor, `` *`edit.html`@5881* ``, found while verifying §DX-02he and confirmed pre-existing by stashing that diff. **The row's own first guess was disproved by the ground:** `resolve-anchors.js:91` already honours a `<path>:<symbol>` prefix, so the convention had no hole. *`edit.html`@5881* has **no `:symbol` part at all** — it never matched, fell through to the default target, and was searched for as the literal string `edit.html` inside `play.html`. **It was never renamed; it was never qualified — and the gate's message said "renamed"**, which is precisely why it survived being read. The pass line had the same defect at scale: *"4113 anchors … all resolve in `play.html`"*, when **496 of them across 31 other files** do not. Shipped: the two docs now read `` `edit.html:probeServer().then@5881` `` (unique at `:5881`; `probeServer()` alone has three hits); every anchor carries the **file it was actually resolved in**, so the failure reads *"NOT in the file they point at"* with `— not in <target>` per row and the pass line reads **`(3617 in play.html, 496 file-qualified across 31 other file(s))`**; and a dead anchor that is a **bare repo path that exists** now gets the real remedy — *"An anchor names a SYMBOL. A bare path has none, so it falls through to the game file"* — instead of the rename story. **Proved by planting the original anchor back** and reading the message, then restoring the file. Selftest **7 → 9**. **`check:walk` 18/18, 10.1s · `npm test` 1033 passed, 0 failed, 3.8m.** | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 17 | 2026-08-25 | §DX-02he ✅ | `check-battlepools`'s green line said **`3 exemptions, each owned by an open row`** and one of the three is owned by design, not by a row — `_bruhns`, whose table value is the sentence *"by design — the `_isFinalBoss` leg loads `BOSS_COMMANDER_AUROS`, not the key"*. **The count was right; the clause after it was wrong**, and a reader taking the line as a ledger goes hunting for a third open row — which is exactly the search that produced the row. Shipped **`exemptionSummary()`**, partitioning both exemption tables' values on `/^§/`: the pass line now reads **`(2 exemptions owned by an open row · 1 by design)`**, and the literal count is out of the string, so it cannot drift from the tables again. Selftest **5 → 7 checks**, both new ones keyed off the live tables rather than a fixed number. **A red baseline was found and not absorbed:** `check:walk` **17/18** and `npm test` **1031 passed, 2 failed**, all three failures being the one dead anchor `` `edit.html:probeServer().then@5881` `` — confirmed **pre-existing at `4cc201a`** by stashing the diff and re-running, filed as **§DX-02hv** at the top of Phase 6, and it is the next row. | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 18 | 2026-08-25 | §DX-02hu ✅ | the editor specs were racing a world load nobody had asked for. The row said *"wait for `#load-splash` to be hidden"*; a probe spec logging the loader's real timeline on an idle machine says the splash **never rises** and `#welcome-screen` stays up for a full 6s — while §DX-02ht's traces show the opposite, `.scol-inner` streaming real node names over the button being clicked. Both true: `edit.html:probeServer().then@5881` is `probeServer().then(up => { if (up) loadFromServer(); })`, so **the editor auto-loads the entire world only if something answers `:1367`** — the overlay a spec faces is decided by ambient state, not test state. **The dependency ran the other way from the guess:** blocking the probe alone turned `worldbuilder-crud-arrays` **red, 4 failed**, because `#welcome-screen` starts *visible* and is dismissed only as a side effect of that load — the file had relied for months on an auto-load it never mentions, to clear an overlay it never waits for. **These specs were passing for a reason none of them states.** Shipped `openEditor(page, opts)` and routed all **52** `goto('/edit.html')` sites across **7** files through it: probe blocked (the hermetic idiom `mesh-connections-ui` already uses), welcome hidden, splash **asserted** rather than awaited; `{live:true}` opts back in. **Verified under the condition that produced the flake:** family `--repeat-each=3 --retries=0` under **8 spin loops on 8 cores** → **426 passed, 0 failed**; full suite **1033 passed, 0 flaky, 0 traces**. **And it made the suite faster** — 52 page loads stopped fetching a world they never used: **7.4m → 5.6m (§DX-02hq) → 4.3m**. **A measurement error of mine is recorded rather than buried:** the first post-fix run read **8.4m** and looked like a regression, but `kill $LOADPIDS` had failed to reap the load-test spin loops (`jobs -p` in a non-interactive subshell) and it was taken at **load average 38** — a wall-time number is only as good as `uptime` when it was taken. `check:walk` **18/18, 10.6s**. | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 19 | 2026-08-25 | §DX-02ht ✅ + §DX-02hu | evidence for a flake without paying for it on every run — and it diagnosed three on its first outing. **The row's own plan was measured and rejected:** promoting `retain-on-failure` suite-wide took the suite **5.6m → 7.1m (+27%)**, giving back most of what §DX-02hq won, and trimming it (`snapshots:false, sources:false`) also measured **7.1m** — the cost is the tracer running at all. **No CI absorbs it:** neither workflow runs the Playwright suite, so the only runner is a developer running it after every row. **A second hypothesis, mine, was also disproved:** the §DX-02hf close said `fullyParallel` *"probably made this more likely"* — with `fullyParallel: false` the suite returned **`1031 passed, 2 flaky`**, worse, so the `worldbuilder-*` specs are load-sensitive either way and §DX-02hq's win was restored rather than sacrificed to a guess. Shipped instead: **`trace: process.env.TRACE ? 'retain-on-failure' : 'off'`** (one command away — `TRACE=1 npm test --prefix src`) plus a file-scoped trace on all seven `worldbuilder-*` specs, joining `multiplayer-presence` — **8 of 88 files**, the four that had flaked this session among them. Proven with a planted first-attempt-only failure: **1 flaky, 1 `trace.zip`**, where the identical real flake had left **0**. **Then it worked:** the confirming run gave `1029 passed, 4 flaky` and **4 traces**, and *reading* them collapsed three into one named defect — `#load-splash` (`edit.html:303`, `inset:0; z-index:200`) is still up when the spec clicks, Playwright's own log saying *"element is visible, enabled and stable … `<div id="load-splash">` subtree intercepts pointer events"* → **§DX-02hu**. **Five sightings across three sessions produced five tally marks and zero diagnoses; four traces produced one root cause in an afternoon.** | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 20 | 2026-08-25 | §DX-02hf ✅ + §DX-02ht | a header said five, the collection held seven — and the shipped dialogue had been saying *"all seven"* the whole time. `SHARD_NOTES@27171`'s `→ doc:` pointer read *"(5 shard_note readable items; one per Codex Shard location)"* and the object holds **7**, `shard_note_1..7`, each key once. **The doc side agrees and so does the game:** `story.md`'s table runs to row 7 (The Sarabande Key, `NUE`, Froberger himself) and Auros at `story.md:581` says *"find the Codex Shards. **All seven.**"* **Where the 5 came from is three lines below the comment** — the completion reward reads *"Seven people carried the pieces. **Five of them** knew what they were carrying."* A five in the neighbourhood of a seven, in a header written from memory; the fifth measured instance of the §DX-02gs shape. One word, engine JS only, `git diff --stat` **1 file, 1 insertion, 1 deletion**, no world-data write. `check:walk` **18/18**; suite **1032 passed, 1 flaky**. **That flaky is filed, not swept:** `worldbuilder-mesh.test.js:62` failed its first attempt, passed the retry, left **no trace** — §DX-02hb's `retain-on-failure` was scoped to one file — and passed **4/4 alone, four times**. **Stated plainly: §DX-02hq's `fullyParallel` probably made it likelier**, since the same session's `workers:'100%'` run flaked `worldbuilder-crud-arrays:173` the same way → **§DX-02ht**. **Tooling error recorded en route:** three re-runs issued from the repo root instead of `src/` made `npx` resolve a *different* Playwright from `~/.npm/_npx/`, reporting *"did not expect test.describe() to be called here"* — a message naming neither the cause nor the cwd. | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 21 | 2026-08-25 | §DX-02hs ✅ | `check:spdx`, gate #18 — the licence header now has something behind it. Filed one row earlier, when §DX-02hn's sweep of all 136 tracked JS files established that **nothing would have caught either sighting** of the same stray-`require`-above-the-header prepend: in `mud-harness.mjs` it was a hard `SyntaxError` above a shebang and left a CI job red for **72 commits** (§DX-02hi fault 3); in `multiplayer-presence.test.js` CommonJS swallowed it and **nothing saw it for two days** — identical cause, and **the silent one is the one that hides**. `src/scripts/check-spdx.js` asserts SPDX is line 1 of every tracked `*.js`/`*.mjs`/`*.cjs`/`*.sh`, or line 2 under a shebang; `vendor`/`node_modules`/`build` skipped; `EXEMPT` empty and a **stale exemption fails**, the rule borrowed from gate #17. **Its 8 selftest checks replay both real sightings** rather than inventing synthetic ones. **Non-vacuous by replanting the actual defect**, not by assertion: `✗ [displaced] multiplayer-presence.test.js:2 … line 1 is: "const path = require('path');"`, EXIT=1, green on restore — the finding names the file, the line **and the text that displaced it**. **Wired in LAST on purpose:** `check:battlepools` is documented as *"gate #17"*, and inserting ahead of it would have silently renumbered a doc reference — the §DX-02gs shape closed twice already this session. `✓ 18/18 gates green · wall 23.1s`, **147 files clean**. Docs synced: new gate row, runner *"the 17 gates"* → 18, `17/17` → `18/18`, and `AGENTS.md`'s gate line (which also still said *"~10s"*, now ~23s). | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 22 | 2026-08-25 | §DX-02hn ✅ + §DX-02hs | the licence header that spent two days on line 2. `multiplayer-presence.test.js` line 1 was a stray `const path = require('path');` sitting **above** `SPDX-License-Identifier` and `'use strict'` — the same prepend that hit `mud-harness.mjs`, where it was a hard `SyntaxError` and left a CI job red for **72 commits** (§DX-02hi fault 3). Here CommonJS swallowed it and **no gate saw it for two days**. Deleted; `path` joins the require block beside `child_process`; SPDX back on line 1. **The sweep its provenance note asked for was run rather than skipped** — all **136** tracked `*.js`/`*.mjs`, testing for *line 1 is a `require`/`import` while SPDX sits below*: **exactly one hit, this file**, so the class is closed at HEAD. What the sweep also established is that **nothing would have caught either sighting**, and that the cheap silent one is the one that hides → **§DX-02hs**, a `check:spdx` gate in the shape of `check:legacycodes`, with a `--selftest` that plants a displaced header. Verify: `head -1` is the licence line; `--repeat-each=2` → **14 passed**. | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 23 | 2026-08-25 | §DX-02hp ✅ | the Count column was remembered, not measured. The row named **one** wrong cell in `design/index.md`'s test table (`multiplayer-presence` said **2 tests**, the file has **7**); following its own *"check the whole column while there"* found **five of eleven** — navigation **37→35**, fishing **~10→9**, worldbuilder-walk **73→89** (the only cell that was *under*, sixteen tests added beneath a frozen total), presence **2→7**, worldbuilder-mesh **2→4**. Counted with `npx playwright test <file> --list`, not `grep`, because a `test()` inside a loop or a describe is not greppable — **and that command now sits above the table**, with the fact a reader most needs and it never said: **this is a selected list of 12 files, not the suite** (`--list` → **1046 declared across 91 files, 1033 run**). Every wrong cell fails the same way — the number was true on the day the row that wrote it closed — the sixth measured instance of the §DX-02gs shape. Two descriptions were **widened, not just renumbered**: presence had omitted all five `§MESH-01-FU` specs, which is *why* its count froze. **Two false claims in the same header were corrected en route**, both contradicted by measurements taken three times today: the walk gate reads **17/17**, not `6/6`, and the standing ⚠️ *"check:walk does not currently complete — it stalls at gate 4"* is struck through — §DX-02hi's `run-gates.js` gives every gate a **120 s deadline**, so a hang is a failure now, and three runs today returned **`✓ 17/17` in 23.0–23.4s**. **§DX-02fz left open on purpose** — closing it is that row's job. Verify: the same script re-run reports **`11 countable cells, 0 disagree`**. Docs only. | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 24 | 2026-08-25 | §DX-02hq ✅ + §DX-02hr | the suite was seven minutes because a 303-test file was one serial chain. Raised by the author mid-session — *"i want the tests to go quicker"* — and Playwright had been printing the answer at the bottom of every run: `Slow test file: quest-runtime-uqf.test.js (5.7m)` of a **7.4m** wall. It parallelises **files, not tests**, so the file holding **303 of 1033** tests *is* the critical path — and it declares **99 describes with zero `beforeAll`/`beforeEach`/serial hooks**, every test on its own `{ page }`. `--fully-parallel` alone: **5.4m → 1.8m**. **Turning it on suite-wide broke three more files with the exact bug §DX-02hm had just fixed by hand** — `beforeAll` runs once *per worker*, and `dx02l` (13897), `mesh-ledger` (13893/4/5) and `mesh-duel` (13896) each spawned on a bare constant: **5 failed, 1 flaky**, all `✗ WBAPI server not running`. So the port map left the files for **one** place: `helpers.js` `workerPorts(block, count)` over `PORT_BLOCKS` (presence 13900, ledger 14100, duel 14200, snapshots 14300, 100 each) keyed on `TEST_PARALLEL_INDEX`, throwing by name on an unknown block; `watchChildren()` beside it, so `stdio: 'ignore'` no longer lets a dying child take its reason with it. **`workers: '100%'` was measured and REJECTED** — 333s vs 329s, inside the noise on 4P+4E cores, while flaking a `worldbuilder-crud-arrays` click; the numbers sit in the config beside the setting so it is not re-tried. **Checked the shared `:1367` is not raced:** the two writing worldbuilder specs stub the write in-page. **7.4m → 5.6m, 1033 passed, 0 flaky**; the four spawning specs at `--repeat-each=4` now **112 passed** where one worker was the ceiling. `check:walk` **17/17, 23.0s**. Remaining lever measured and filed as **§DX-02hr**: `seedAndLoad` is **326 ms of a ~450 ms test** — ~1000 cold re-parses of a **5.3 MB** `play.html` — worth 30–40% but it trades the fresh-engine guarantee. | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 25 | 2026-08-25 | §DX-02hm ✅ | one port pair per worker: the presence spec could not be run twice at once. The row measured `--repeat-each=4` at `378f95f` as **`17 passed, EXIT=0`** where 28 were expected; **re-measured at `3c0e9f7` it is `14 failed / 14 passed`** — §DX-02hb's `retries: 0` one commit earlier had already closed the *silent* half, because retries were re-landing the colliding tests in a different worker slot. What remained is that the file **cannot be run by more than one worker at all**: `MP_PORT`/`TRK_PORT` were module constants, so worker 0 binds and every other worker's spawn dies of `EADDRINUSE` **unheard** under `stdio: 'ignore'` — its `beforeAll` poll then succeeds anyway, against worker 0's server, and its `afterEach` session sweep races a stranger's. Shipped a port pair per worker off `TEST_PARALLEL_INDEX` (`13891 + i*2` / `13892 + i*2`, same suffix on both `PEERS_CACHE_FILE` paths), plus `exit`/`error` listeners so the timeout error names the dead child and the worker instead of only a port. Same command after: **28 passed, EXIT=0, and 1.0m → 30.7s** — the wall time is the second proof, the file is now genuinely parallel. `api audit` **ok, 0 errors**; `check:walk` **17/17, 23.4s**; suite **1033 passed, EXIT=0**. | [Phase 6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 26 | 2026-08-25 | §DX-02hb ✅ + §DX-02hm/hn | the flake that was never diagnosed, because the suite threw the evidence away. The row said the failure *"is reproducing more often than not"* at HEAD; **two consecutive full runs returned `1033 passed, EXIT=0, 0 flaky`**, and the mesh file under 12 spin loops on 8 cores returned **42/42**. The real defect is the one assertion in `:174` that cannot wait — the `☺` minimap glyph, a bare `expect` racing `_mpRepaintMaps@29290`'s ~100 ms debounce, **captured failing at `:195` for the first time in six sightings**. Nobody had read that error because `playwright.config.js` sets **no `trace`** and `retries: 1` turns it into `1 flaky` + `EXIT=0` — proven with a planted first-attempt-only failure: **3 runs, 3× `1 flaky`, exit 0, zero traces**. Shipped (a) `expect.poll` on `:195`, plus file-scoped `trace: 'retain-on-failure'` and `retries: 0` on the `§MESH-01a` block; **rejected the row's (b)** — `workers` is global, so a separate project needs `dependencies`, and a mesh coin-flip would then **skip 1026 tests**. Same planted flake now: `1 failed`, `EXIT=1`, 1 trace. | [Phase 6](BACKLOG-6-verification-docs.md) |
| 27 | 2026-08-25 | §DX-02hi ✅ + §DX-02hj | the check-in process had three faults, and the row named a fourth that was not real. **The filed cause — a stale server holding 1367 — is wrong:** no gate in the chain opens a socket or spawns anything; all seventeen are pure reads over `play.html`. **(1) `check:dupkeys` was 73.5 s of the chain's ~104 s**, computing each line number as `text.slice(0, idx).split('\n').length` once per key over megabyte sections. Newline offsets once + binary search → **0.45 s**, with 2,000 sampled offsets showing **0 disagreements** against the old expression and the gate's 4-check selftest intact. **(2) A hang was neither pass nor fail** — `npm run a && npm run b && …` has no deadline, so a stuck gate reads as work in progress. `scripts/run-gates.js` runs each gate as a child over its own package.json script string with a **120 s deadline**, buffered output printed in chain order, per-gate timings, and — since pure reads are safe to parallelise — 6 jobs: **wall 76 s → 10.1 s**. Its own selftest plants a hang, a red, a green and an output check on every run. **(3) `WALK invariants` had failed on EVERY push for 72 commits** — the notification noise — and not in the gates: `mud-harness.mjs` carried `const path = require('path');` **above the shebang** of an ESM file that already imports `path`, landed `e99a003` 2026-08-23. Under it, `ROOT` resolved to `src/` so every path read `src/src/…`, and the TTL scenario assumed a **700 ms** TTL outlasts a round trip — **the server's prune is correct** (probe: 700 ms prunes after a 3 s gap, 700000 ms keeps both), the harness was pruning the session it kept touching. TTL 6 s + touches on **elapsed time**, and an explicit throw if it is ever too slow. Harness **green for the first time since 2026-08-23**. **(4) And CI found a fourth:** with 1–3 fixed the run got further and crashed before listening — `ENOENT … build/milepoints/wbapi-server.log`. The server opened its log stream under **generated, gitignored `build/`** with no `mkdir`, so **a fresh clone could never start it**; every machine here already had the directory. Guarded, and reproduced locally by deleting `build/milepoints` first. The `x-coc-worldhash` assertion vs the shipped `X-R2H-*` → **§DX-02hj** (🟡, wire format). `check:walk` **17/17 EXIT=0**; suite **1032 passed**, `:174` red again — **§DX-02hb's sixth sighting**. | [P6](BACKLOG-6-verification-docs.md) → [archive](plan-archive.md) |
| 28 | 2026-08-25 | §AUDIT-03al ✅ + §DX-02hh/hi | a section written so a validator would go green, describing a constant retired two days later. `story.md §Gate Locks` was SP4's **one new doc section**, and at HEAD `GATE_LOCKS` and `_canTravelTo` are **0 occurrences** each. The mechanism is not merely missing but unbuildable as described: `cellMove@28358` is a thin caller over `Mover.move` that reads `res.destCodes[0]` only to pick the node to render and **has no gate branch at all**. Three tombstones for this exact constant already existed — `maps.md:502`/`:757`, `index.md:497`, `docs/notes/docs-node-network.md:177` — **written for a different copy of the section**, which is the whole finding. Deleted whole. **Second site, found by grepping the constant instead of the section:** `index.md:581` listed `GATE_LOCKS` as a row of the **live constants table** while `index.md:497` in the same file recorded it removed — **the read-before-every-row doc contradicting itself**; row deleted. **The check the row asked for was run and it fails → §DX-02hh:** `spec-engine.md:354`'s ten-row table is right (they are `gate:{flags:[…]}` quest gates, the Mission-Gating Policy working) but its framing sentence attributes the check to `cellMove`, and its `Time Cost` neighbour is contradicted by `cellMove`'s own *§TIMELESS-01: movement is timeless* comment. **No world-data change.** `api audit` errors 0; **40 green gate lines, EXIT=0**; suite **1032 passed, EXIT=0** with `:174` `1 flaky` — **§DX-02hb's fifth sighting**, and the first to survive its retry. **Two `check:walk` runs hung past 10 minutes against a stale server holding 1367**, green once the port cleared → **§DX-02hi** (no gate in the chain has a timeout, so a held port reads as *still working*). | [P1](BACKLOG-1-playable-truth.md) → [archive](plan-archive.md) · [P6](BACKLOG-6-verification-docs.md) |
| 29 | 2026-08-25 | §AUDIT-03ak ✅ + §DX-02hf/hg | the residual set the row measured is not the residual set at HEAD. The row named **four** `⚠️ PLANNED` headings in `world.md` describing features live since 2026-05-26. **Two of the four had already been repaired** — by the same program that filed the row: `world.md:229` carries *"Status corrected 2026-08-13 by §DOC-02ar"* (Layer 51) and `:243` *"Status corrected 2026-08-17 (§DOC-02bz)"* (Layer 52), each naming the identical contradiction. **The two survivors are the two the row called sharpest**, and both shipped: Layer 56 (`story.md:1708` ✅ vs `world.md` PLANNED — engine holds `vshamanFound@23141`, `void_shaman@5410` at the AC 15 / HP 65 the doc block itself specifies, the quest block and the combat outcome) and Layer 57 (`story.md:1727` ✅ — `SHARD_NOTES@27171`, the auto-add, the reader, `FROBERGER_JOURNAL@27196`). Promoted to `✅ Layer N` with dated correction notes in the shape §DOC-02ar established; **no HTML edit**, as the row specified; nine new engine anchors, `check:anchors` **EXIT=0**. **Found by measuring rather than transcribing:** `SHARD_NOTES`' header says *5* and it holds **7** → **§DX-02hf**; and a **fifth** PLANNED heading the row never named, Layer 59 at `world.md:1197`, half live and half already annotated as never built → **§DX-02hg** (🟡 — one verdict on that heading is wrong either way). **The row's own lesson, turned on the row:** *a verification pass tests what it changes and assumes what it leaves* — half of this residual list had gone stale by that exact mechanism. `api audit` errors 0; **40 green gate lines, EXIT=0**; suite **1032 / 1**, `multiplayer-presence.test.js:174` — **§DX-02hb twice in a row**, 7 of 7 alone each time, and that row no longer reads as a rare flake. | [P1](BACKLOG-1-playable-truth.md) → [archive](plan-archive.md) · [P6](BACKLOG-6-verification-docs.md) |
| 30 | 2026-08-25 | §FUTURE-01-FU2 ✅ | the arc's first mission card, and the quest that had already answered the question. The row said `quest_road_damascus` — the first §LIX text a player reads — addresses the player **as Saul** in two adjacent lines, one sentence from *"He leaves at first light."* **True, and the design call it reserved was not open.** The same quest's `failText` already ships the companion phrasing: *"He has the warrants in his coat."* `disposition` and `passText` are third-person throughout, and the host node `JRS@8445` narrates Saul in third person on the screen that grants the loot. **Three of the quest's five prose fields already agreed; two did not** — so the increment is those two strings and nothing else. Shipped through `./bin/api put quest`: `desc` by `editField`, the `onComplete` narrative by `editStructuredField` from a JSON body, because a structured field cannot be addressed by `key=value`. **Round-tripped from disk, not from the response** — `git diff` is **2 lines, one file**; `grep 'your coat'` in the quest block **2 → 0**; the re-spaced `onComplete` line is the known **§DX-02ga** reformat and moved no anchor. **The mechanic is what let the residue survive review:** `activateCond` requires *Three Jerusalem Warrants* in `S_story.inventory`, so the player literally carries them — party inventory, not Saul's coat. **Four doc anchors named the old literal and `check:anchors` went red on the write** — the gate working. The two lab-report sites and the closed §DOC-02aj row are records of a measurement, so the quoted old text stays as prose and only the live anchor was repointed. `./bin/api audit` errors **0**; `check:walk` **40 green lines, EXIT=0**; suite **1032 passed / 1 failed, EXIT=1** — `multiplayer-presence.test.js:174`, the **third sighting of §DX-02hb**, which passed **7 of 7, EXIT=0** re-run alone immediately after and is recorded in that row. | [P1](BACKLOG-1-playable-truth.md) → [archive](plan-archive.md) |
| 31 | 2026-08-25 | §DX-02hd ✅ | the last symptom of the two-shapes blindness, and there were two readers, not one. The row said `GET /api/monster/:key/drop` logs `undefined  ·  0gp` for the **13** weighted-array drops. **True, and one site short.** Captured live at `e8c3317`: the `/drop` route printed `drop:   undefined  ·  0gp` (`wbapi-server.js@6433`) — and so did `get monster` itself, `terrains+quests: 1 terrains · 0 quest refs ·  drop: undefined` (`@10867`), the more-travelled reader and the first command run on any monster. **Shipped one shared formatter, `dropLogLine(drop)`, and both readers call it** — array branch `weighted table ×4  ·  Void Ichor Vial, Warden Sigil Shard, Nullstone Bead, Cracked Void Staff`; object branch byte-identical to the old template, so the 386 single trophies log exactly as before (`taz_devil` → `🌀  Furball Crown  ·  18gp`). **No world-data change** — `play.html` untouched. **4-case regression test**, pure-node: the helper is lifted out of the server source text and evaluated, so no server starts. It pins both branches, the `undefined`-free degradation, and that the one-shape template survives in **exactly one** place — inside the helper — so a third reader cannot be added with an inline copy. **Non-vacuous by mutation — 4 of 4 fail with the fix stashed.** `api audit` errors 0; **18 gates EXIT=0**; suite **1033 passed, EXIT=0**. An earlier run of the same suite went **1032 / 1 flaky** on `multiplayer-presence.test.js:174` — **the first reproduction of §DX-02hb**; that row stays open with the pointer added. **§DX-02ha, §DX-02hc and §DX-02hd are three symptoms of one cause** — `MONSTER_DROPS` publishes two literal shapes and nothing in the tooling said so out loud. The helper's comment now does. | [P5](BACKLOG-5-platform-tooling.md) → [archive](plan-archive.md) |
| 32 | 2026-08-25 | §DX-02hc ✅ + §DX-02hd | the update half of a bug the repo had already closed twice. The row said `PUT /api/monster/:key/drop` **corrupts** the **13** weighted-array drops. Grounding it found that was the *second* defect, and the first is far larger: **the route never persisted for any of the 399.** Measured on `taz_devil`, an **object-form** drop the row considered safe — `./bin/api drop taz_devil sell=19 --update` → **`ok:true`, `sell:19`**; disk → **`sell:18`**; and the response's own escape hatch, `./bin/api save`, left `play.html` **byte-identical** (`git status` clean). The whole route body was `Object.assign(WBAPI.monsterDrops[key], body)`, and `save()` writes the patched `_rawSrc` it never touched. **`wbapi-core.js:1556` already states the lesson**, about the DELETE family: *"a write path that reports success without persisting … the failure is silent because nothing ever throws."* §DX-01c closed the **create** half, §DX-01d/i the **delete** half; **the update half was still open and nothing had looked.** **Shipped:** (1) `WBAPI.replaceEntrySource` — the missing source-level primitive, reusing `deleteEntrySource`'s marker location, its **nested-section clamp** (`MONSTER_DROPS` sits *inside* `MONSTER_POOL`'s anchors) and its **verify-or-revert** key-set guard unchanged; (2) the route now writes through it and terminates in **`saveAndRestart`**, making `wb.js:1293`'s standing promise (*"you do NOT need to run save after a put/post/del"*) true here for the first time — and **refuses with 400** against a weighted table, naming its size, because a flat `{name,icon,sell}` body cannot address one. **Round-tripped from disk:** `was:` echoes the old line, `autoSaved:true`, and `grep` shows `sell:19`. The `taz_devil` probe was a measurement, not the change, and was reverted — **the row ships no world-data change, only the write path.** **6-case regression test** modelled on its sibling `dx01di-delete-persists.test.js`, pure-node against an in-memory copy; **non-vacuous by mutation — 5 of 6 fail with the fix stashed.** `api audit` errors 0; **18 gates EXIT=0**; suite **1028 passed, EXIT=0**. Filed **§DX-02hd** (the GET route's log line prints `undefined` for the same 13 — the cosmetic end of the same two-shapes blindness that produced §DX-02ha's false measurement). | [P1](BACKLOG-1-playable-truth.md) → [archive](plan-archive.md) · [P5](BACKLOG-5-platform-tooling.md) |
| 33 | 2026-08-25 | §DX-02ha ✅ CLOSED (premise disproved) + §DX-02hc | the drop was there all along, in a shape the measurement could not see. The row said `void_shaman` is fought at `TRD` and carries no `MONSTER_DROPS` row. **Both halves are false.** `MONSTER_DROPS@5798` holds a **weighted table of four** — Void Ichor Vial `28/4`, Warden Sigil Shard `22/3`, Nullstone Bead `18/3`, Cracked Void Staff `10/2` — and `battKillEvent@7050` reads it (`Array.isArray(_rawDrop) ? _pickDrop(_rawDrop) : _rawDrop`). The row's **own re-derive command** settles it: `./bin/api list monster --has-drop false` returns **0**, not 3 — all **399** monsters carry a drop. And `TRD@8688` is `key:'goblin'`; only the **label** names the Shaman, who is fought at the synthetic `MT_WARDEN@31748` and via `epic_goblin_cave@6364`. **Why §DX-02fy's count said otherwise:** it read `MONSTER_DROPS` in the **single-object** shape, and **13 of 399** use an **array** shape — the same one-of-two-literals blindness §DX-02fy had just fixed in `check-battlepools`. A grep that knows one shape reports a **false absence**, and a false absence reads exactly like a defect. **No code changed; the measurement was the increment.** **The real defect underneath it — §DX-02hc:** `PUT /api/monster/:key/drop` is `Object.assign(monsterDrops[key], body)`, so against the 13 array-form drops it writes `.name`/`.sell`/`.icon` as **non-index properties** and returns **`ok:true`** with the untouched table echoed back. **Proven by probe, not asserted** — `./bin/api drop void_shaman name="ZZZ-probe" … --update` → `ok:true`, four entries unchanged, `grep -c ZZZ-probe play.html` = **0**; PUT-only, never saved, discarded by restart. The mandated write path loses an author's edit for 13 monsters and reports success — the §DX-02gy family, worse. `api audit` errors 0; **18 gates exit 0**; suite **1023 passed**. | [P1](BACKLOG-1-playable-truth.md) → [archive](plan-archive.md) |
| 34 | 2026-08-25 | §DX-02fy ✅ + §DX-02gy/gz/ha/hb | the battle whose monster was never written, and the gate that could not see its own fix. `RUH` was the only one of **47** battle nodes whose `battle.key` resolved in neither pool; `desert_wanderer` occurred **once** in `play.html`, in RUH's own `battle` field, and the guarded opponent load meant the fight kept **every field of the previous monster**. Shipped **option (a)**, and the node's fiction chose it, not the row's preference: the `arabia` roster is elementals and beasts, `RUH`'s text is a man alone for an unrecorded season, and a *Wanderer* is a **person**. `ac:13 hp:72 atk:6 2d8+3 medium` — inside the `ifrit`/`sand_wraith` band, under the `gigascorpion` ceiling, a clone of neither. Given a drop because **47 of the 50** battle keys carry one (`void_shaman` does not → **§DX-02ha**). **The API could not express the drop**, so the verb was added to `src/api/wb.js` first rather than falling back to curl — the server route existed and the CLI could not reach it. **The real find:** `check-battlepools` read pool rows with **single quotes only** and `./bin/api post monster` writes **double** — so a monster on disk, in `MONSTER_POOL`, written through the *mandated* path, was invisible to the gate meant to prove it reachable. Fixed both literal reads, added a 5th selftest, **non-vacuous by mutation**. Exemptions **4 → 3**; `api audit` errors 0; **18 gates exit 0**; suite **1023 passed**. One run went **1 failed / 1022** on a MESH spec — stash/isolate/restore/re-run proved it a flake, filed **§DX-02hb**. Also filed **§DX-02gy** (`put monster` accepts unknown fields, writes them inert, reports `ok`) and **§DX-02gz**. | [P1](BACKLOG-1-playable-truth.md) → [archive](plan-archive.md) |
| 35 | 2026-08-25 | §DX-02gw (2 of 3) + §DX-02gx | the row's own premise did not survive being measured, and that was the increment. Took `dragon_of_fyresdal` meaning to host it at `PDL` on the strength of *"island territory"*. **Two measurements killed it before a line was written.** (1) `ACE` already holds `bossKey:'ancient_sea_dragon'` and `story.md:2208` hangs **Q58, Island Elder Maris, *"parent node `PDL`"*** off it — *"The dragon arrived when Maris was fourteen"* — so a second dragon at `PDL` contradicts shipped story; all **20** epic nodes are taken. (2) Applying the gate's own reachability rule to **every** tier: **50 of 384** monsters are unreachable — easy 6/60, medium 16/148, hard 26/123, deadly 2/53 — and `monsters.md:320` calls the collection *"Dark Fantasy Bestiary II"*, catalogued by category. **`MONSTER_POOL` is a library, not an inventory**, so *statted, priced, unfought* is not by itself a defect. What made 1 of 3 real was a **shipped promise pointing at the monster** (`TLS`'s label; `world.md:798`'s *"guardian challenge"*) — the remaining two have none, anywhere. Retagged **🟡 → ASK**, recommendation *retire both*, with an explicit **do not author a host node**. Filed **§DX-02gx** (the `deadly` cutoff is a sampling artifact). Found en route and fixed: **`TERRAIN_POOLS` does not exist in `play.html`** — the collection is `WORLD_DB`; the wrong identifier was in 4 places incl. the gate's own header and entry 2 below, and it took `check:anchors` **red** to surface it. Also `AGENTS.md`, and a fifth measured instance of §DX-02gs (57 sections, table read 53). | [P1](BACKLOG-1-playable-truth.md) · [P6](BACKLOG-6-verification-docs.md) |
| 36 | 2026-08-25 | §DX-02gw (1 of 3) | the west wing gets the bruxa the docs already fought there. `bruxa_corvo_bianco` occurred **twice** in `play.html` — pool row `@5787`, drop row `@6264` — and no `battle.key`, `P.<key>` or `bossKey` reached it. The row left *which node hosts the fight* open; `CLJ` was settled by the repo, not by taste: `monsters.md:496` annotates the key **"ancient estate"**, `vampire_castle` in `WORLD_DB` already lists `P.bruxa`, `CLJ.text` ends *"What she does in the west wing is her own business"*, and **`world.md:798` had already shipped the fight in prose** — *"Elise Mourne uses Dodge … during her guardian challenge"* — against a node whose `battle` was `null`. Corvo Bianco is the estate: the name is her title, and `battle.label` carries it. `node.battle` is **opt-in**, so the toll, the Token and the free sleep are untouched. Unreachable deadly **3 → 2**, exemptions **5 → 4**, exemption retired in the same commit so the gate proves the fix. 17 gates exit 0, selftest 4/4, suite **1023 passed**. **2 of 3 remain** — `dragon_of_fyresdal`, `slyzard_matriarch`. | [P1](BACKLOG-1-playable-truth.md) |
| 37 | 2026-08-24 | §DX-02gv + §WAVE-01 | a 210-HP deadly-tier monster with a priced shard and no fight, and the gate that walks the two collections both ways. `void_warlord` occurred **twice** in 38,700 lines —… | [P1](BACKLOG-1-playable-truth.md) |
| 38 | 2026-08-24 | §AUDIT-03bi + §AUDIT-03au | the game's fifth ending stops being downstream of a command you have to type into a browser console. `S_story.questMinusOne` gates the Entry 42 chain — five… | [P1](BACKLOG-1-playable-truth.md) |
| 39 | 2026-08-24 | §DX-02gn + §DX-02eu | the suite's screenshot overwrote a file git was watching, and the fix was to move the write, not delete the file. `living-world-l44.spec.js:210` wrote… | [P6](BACKLOG-6-verification-docs.md) |
| 40 | 2026-08-24 | §DX-02gt + §AUDIT-03bj | the suite was red at HEAD and the gate chain was green, because they do not test the same thing. `npm test --prefix src` measured **2 failed / 1016 passed** — Waves 2q… | [P6](BACKLOG-6-verification-docs.md) |
| 41 | 2026-08-24 | §AUDIT-03bh-FU + §DX-02gt | the search the hint named already existed, and it set no flag. `quest_tl_01` (*Rennau: The Ledger*) waited on `tlLedgerRead`, written only by its own `onComplete` — 0… | [P6](BACKLOG-6-verification-docs.md) |
| 42 | 2026-08-24 | §AUDIT-03bk + §AUDIT-03bj | the quest that could be offered and never finished, and the host record its design lost. `quest_inn_05` (*The Return*) completed on `flags:['innDeparted'] ∧… | [P1](BACKLOG-1-playable-truth.md) |
| 43 | 2026-08-24 | §AUDIT-03bj + §AUDIT-03bk | the prover that spent 27 days proving the world is not finishable and reporting success. `QuestRuntime` activates a quest when its `gate.flags` are all true, and **55… | [P1](BACKLOG-1-playable-truth.md) |
| 44 | 2026-08-24 | §DX-02bs + §PAUL-01 | the soft-lock gate learns the one writer idiom it could not see, and the row's own count of its false positives was one short. `check:questgraph` folds host flag-writers… | [P6](BACKLOG-6-verification-docs.md) |
| 45 | 2026-08-24 | §AUDIT-03bh + §AUDIT-03bh-FU | the Muffat → Signal → Antecedent chain completes, and the one member that would not yield to a completion term was reverted rather than forced. `QuestRuntime` completes… | [plan-archive.md](plan-archive.md) |
| 46 | 2026-08-24 | §DX-02go + §DX-02ge | the last inert field on a granted readable is gone, and the increment's real lesson is about anchors, not fields. The sibling §DX-02ge filed one increment earlier:… | [P1](BACKLOG-1-playable-truth.md) |
| 47 | 2026-08-24 | §DX-02ge + §DX-02gk | the item-grant vocabulary is exactly what the engine reads, and the disproof was already sitting in the file. `readableKey` was authored on one item, kept in lockstep by… | [P1](BACKLOG-1-playable-truth.md) |
| 48 | 2026-08-24 | §DX-02gl + §DX-02gb | the Dear-Friend act table names only acts that can grant the step, and the ending's score did not move by a bit. `DEAR_FRIEND_BITS.auros` was unreachable by… | [P1](BACKLOG-1-playable-truth.md) |
| 49 | 2026-08-24 | §DX-02gm + §DX-02fz | the anchor gate's own acceptance test writes only inside its own scope now, and removing its sweep exposed the two assertions the sweep was satisfying.… | [P6](BACKLOG-6-verification-docs.md) |
| 50 | 2026-08-24 | §DX-02gk + §DX-02gb | the ending scorer reads the Dear-Friend act table instead of respelling it, and the fix's real cost was paid in doc anchors, not in code. `_missionComplete` spelled **6… | [P1](BACKLOG-1-playable-truth.md) |
| 51 | 2026-08-24 | §DX-02gb + §DX-02fc | the Dear-Friend step is granted once, and either order earns it. `_setNpcFavor` ran its `dearFriendBits` auto-upgrade **inside `if (level === 1)`** and the function is… | [P1](BACKLOG-1-playable-truth.md) |
| 52 | 2026-08-24 | §DX-02aj + §AUDIT-03n | the NG+ Dear-Friend memory line waits for the second visit, and the harness that first said otherwise was the thing at fault. `_renderNpcCard` wrote the greeting latch… | [P1](BACKLOG-1-playable-truth.md) |
| 53 | 2026-08-24 | §DX-02gd + §DX-02ge | eleven items spelled one field three ways, and the tooltip the fix depends on did not exist. Fifteen item literals carried a `description:` key; the file's **only**… | [P1](BACKLOG-1-playable-truth.md) |
| 54 | 2026-08-24 | §DX-02cm + §ARCH-01 | the last inline quest completer is gone, and *"design debt, not a reproduced bug"* was half wrong. `quest_la_riva_02`'s six AMS effects are an `onComplete` chain… | [P1](BACKLOG-1-playable-truth.md) |
| 55 | 2026-08-23 | §DX-02fb + §DX-02ga | `crov` reaches Dear Friend + 3, and the row's own prescription was the wrong fix. One bit — `{kind:'favor',npc:'crov',add:2}` appended to `quest_pit_training.onComplete`… | [plan-archive.md](plan-archive.md) |
| 56 | 2026-08-23 | §DOC-METHOD + §DX-01k | the resumption method written down and measured — `lab-report-ieee-prompt-resumption-context-architecture.md`. The corpus is **~3.4 M tokens**; the core that answers… | [P6](BACKLOG-6-verification-docs.md) |
| 57 | 2026-08-23 | §RELEASE-01 + §7 | `lab-report-ieee-dns-apex-github-pages-community.md` registered in all three references (`docs/README.md` 115 · `index.html` 115 write-ups · a new *Infrastructure &… | [P6](BACKLOG-6-verification-docs.md) |
| 58 | 2026-08-23 | §DX-02cy + §DX-02fy | the kill-counter writer. `S.opp.key` → `S.enemy.key` at four sites; every kill counter in the game had been inert for 90 days and 19 quests were unreachable behind it.… | [P1](BACKLOG-1-playable-truth.md) |
| 59 | 2026-08-24 | §DX-02cm | the exception that outlived the deferral, and the difference between debt and an unexercised bug… | [P1](BACKLOG-1-playable-truth.md) → [archive](plan-archive.md) |
| 60 | 2026-08-23 | §DX-02fb | the ceiling that held at 2 since the first commit, and the one-bit fix this row prescribed that would have made it 1… | [P1](BACKLOG-1-playable-truth.md) → [archive](plan-archive.md) |
| 61 | 2026-08-23 | §DOC-METHOD | the corpus is 3.4 M tokens, the core that answers it is 51 k, and one backlog line is 234,650 bytes… | [P6](BACKLOG-6-verification-docs.md) |
| 62 | 2026-08-23 | §RELEASE-01 | the record that prescribes a remedy this repository does not apply, and the index that stopped counting at 80 of 115… | [P6](BACKLOG-6-verification-docs.md) |
| 63 | 2026-08-23 | §DX-02cy | the counter that had been monotone at zero for ninety days, and the gate that refused to let the docs stay wrong… | [P1](BACKLOG-1-playable-truth.md) |
| 64 | 2026-08-23 | §DOC-02dc | the child design pass whose one missing opcode was the last one the VM ever needed, whose only carried nu… | [P2](BACKLOG-2-engine-systems.md) |
| 65 | 2026-08-23 | §DOC-02db | the design that survived eleven slices and 26 days at 100% of its symbols, whose one unmodelled shape was… | [P2](BACKLOG-2-engine-systems.md) |
| 66 | 2026-08-23 | §DOC-02da | the report whose architecture survived 85 days and a 3.15× file intact, whose operations were reversed fo… | [P5](BACKLOG-5-platform-tooling.md) |
| 67 | 2026-08-23 | §DOC-02cz | the most completely-shipped lock the program has scored, whose best Froberger trace has never been read b… | [P3](BACKLOG-3-content-narrative.md) |
| 68 | 2026-08-23 | §DOC-02cy | the most faithfully-transcribed lock the program has scored, whose off-screen character is named by nobod… | [P3](BACKLOG-3-content-narrative.md) |
| 69 | 2026-08-22 | §DOC-02cx | the design lock whose every string survived ninety days byte-for-byte, whose best ending has never once f… | [P3](BACKLOG-3-content-narrative.md) |
| 70 | 2026-08-22 | §DOC-02cw | the content report whose every number survived 45 days, whose road was laid by the method it forbids, who… | [P4](BACKLOG-4-world-navigation.md) |
| 71 | 2026-08-22 | §DOC-02cv | the origin lock with a perfect anchor score and a census that measured half the corpus, whose two "pure t… | [P2](BACKLOG-2-engine-systems.md) |
| 72 | 2026-08-22 | §DOC-02cu | the design lock whose data table shipped byte-identical and is still byte-identical a month later, whose … | [P2](BACKLOG-2-engine-systems.md) |
| 73 | 2026-08-22 | §DOC-02ct | the design lock that named an invariant in its first design call and violated it in its fourth section, a… | [P2](BACKLOG-2-engine-systems.md) |
| 74 | 2026-08-22 | §DOC-02cs | eighteen of twenty-one anchors exact and the three misses are exactly the three the author hedged with a … | [P2](BACKLOG-2-engine-systems.md) |
| 75 | 2026-08-22 | §DOC-02cr | the design lock whose code shipped byte-identical and is still byte-identical a month later, and whose sp… | [P2](BACKLOG-2-engine-systems.md) |
| 76 | 2026-08-22 | §DOC-02cq | every one of the prover's own seven corpus figures reproduced exactly on the pinned build, and the increm… | [P2](BACKLOG-2-engine-systems.md) |
| 77 | 2026-08-22 | §DOC-02cp | eighteen line numbers, eighteen exact — the cleanest anchor result the program has scored — and the one p… | [P2](BACKLOG-2-engine-systems.md) |
| 78 | 2026-08-21 | §DOC-02co | the lock held whole, both consumers it predicted arrived by name, and the one thing it never re-measured … | [P2](BACKLOG-2-engine-systems.md) |
| 79 | 2026-08-21 | §DOC-02cn | 27 of 28 line numbers correct against a build that was never committed, and all four wrong figures are in… | [P2](BACKLOG-2-engine-systems.md) |
| 80 | 2026-08-21 | §DOC-02cm | every code block byte-exact, 33 of 33 anchors resolving, the acceptance suite still 5/5 thirty days on — … | [P2](BACKLOG-2-engine-systems.md) |
| 81 | 2026-08-21 | §DOC-02cl | the thesis was right, the plan was right, it shipped whole in nine hours — and the one census row it coun… | [P3](BACKLOG-3-content-narrative.md) |
| 82 | 2026-08-21 | §DOC-02ck | the most accurate document in the corpus is wrong about the one sentence its whole surface argument rests… | [P3](BACKLOG-3-content-narrative.md) |
| 83 | 2026-08-21 | §DOC-02cj | the report got four of its own counts wrong and three of its four open questions right, and the repo buil… | [P2](BACKLOG-2-engine-systems.md) |
| 84 | 2026-08-21 | §DOC-02ci | the parent review shipped five of seven faces the day it was written, and every one of its four errors is… | [P2](BACKLOG-2-engine-systems.md) |
| 85 | 2026-08-18 | §DOC-02ch | the string is byte-exact at 37 days, the reason given for the fix beside it is false, and the road it sig… | [P3](BACKLOG-3-content-narrative.md) |
| 86 | 2026-08-18 | §DOC-02cg | the increment shipped ten of ten and deferred five of five, and the sentence it put on the opening screen… | [P2](BACKLOG-2-engine-systems.md) |
| 87 | 2026-08-18 | §DOC-02cf | the lock has not drifted a byte in 37 days, and half of what it shipped has never once returned true.… | [P2](BACKLOG-2-engine-systems.md) |
| 88 | 2026-08-18 | §DOC-02ce | shipped the goal whole, and then a design reversal landed on one of its two surfaces.… | [P2](BACKLOG-2-engine-systems.md) |
| 89 | 2026-08-18 | §DOC-02cd | the corpus's first perfect dating result, on a feature that ships the XP and withholds the telling.… | [P2](BACKLOG-2-engine-systems.md) |
| 90 | 2026-08-18 | §DOC-02cc | the cleanest ship in the corpus, and every failure in it is a claim about its own surroundings.… | [P2](BACKLOG-2-engine-systems.md) |
| 91 | 2026-08-18 | §DOC-02cb | the most faithfully executed lock in the corpus, describing a feature no player has ever reached.… | [P3](BACKLOG-3-content-narrative.md) |
| 92 | 2026-08-18 | §DOC-02ca | four of five recommendations shipped inside 28 days, every derived number is wrong, and the fifth recomme… | [P5](BACKLOG-5-platform-tooling.md) |
| 93 | 2026-08-17 | §DOC-02bz | the arc is built exactly as written, and it has never been playable.… | [P3](BACKLOG-3-content-narrative.md) |
| 94 | 2026-08-17 | §DOC-02by | a design lock written 23 minutes before the code, exact in every number six weeks on, and wrong about one… | [P4](BACKLOG-4-world-navigation.md) |
| 95 | 2026-08-17 | §DOC-02bx | seven locked decisions, four non-goals, forty-two days, and the only thing that drifted was the labels.… | [P5](BACKLOG-5-platform-tooling.md) |
| 96 | 2026-08-17 | §DOC-02bw | every increment it planned shipped in four days, every constant and formula still measures exact, and the… | [P5](BACKLOG-5-platform-tooling.md) |
| 97 | 2026-08-17 | §DOC-02bv | 2026-08-17 — §DOC-02bv ✅ SHIPPED: the corpus's best-verified report, and the only one whose entire forwar… | [P5](BACKLOG-5-platform-tooling.md) |
| 98 | 2026-08-17 | §DOC-02bu | a method document scored against the corpus it produced — the tables are perfect, the census is wrong by … | [P5](BACKLOG-5-platform-tooling.md) |
| 99 | 2026-08-17 | §DOC-02bt | every statistic in the diagnosis re-derives exactly, and the one sentence nobody filed as a row cost four… | [P4](BACKLOG-4-world-navigation.md) |
| 100 | 2026-08-17 | §DOC-02bs | the report was exact on every number it counted, and its subject was deleted six days after it closed.… | [P5](BACKLOG-5-platform-tooling.md) |
| 101 | 2026-08-17 | §DOC-02br | (`4a97e50`): a design lock that scored twelve of twelve and had its runtime three minutes later — and the… | [P5](BACKLOG-5-platform-tooling.md) |
| 102 | 2026-08-17 | §DOC-02bq | the design was right, the encoding was inert, and the engine had been demonstrating the correct form seve… | [P5](BACKLOG-5-platform-tooling.md) |
| 103 | 2026-08-17 | §DOC-02bp | the miscount was fixed inside a day, in a gate, and four documents were never told.… | [P5](BACKLOG-5-platform-tooling.md) |
| 104 | 2026-08-14 | §DOC-02bo | the report's own census counts a sentence of prose as a field, and the patcher it designs is immune to ex… | [P5](BACKLOG-5-platform-tooling.md) |
| 105 | 2026-08-14 | §DOC-02bn | the corpus's most accurate report, and the one sentence in it nobody measured is the origin of §AUDIT-03x… | [P4](BACKLOG-4-world-navigation.md) |
| 106 | 2026-08-14 | §DOC-02bm | two hours from spec to closed series, twelve of twelve pointers exact, and the one assertion that is red … | [P6](BACKLOG-6-verification-docs.md) |
| 107 | 2026-08-14 | §DOC-02bl | the removal shipped completely, and was undone twelve days later by a track that reused every one of its … | [P4](BACKLOG-4-world-navigation.md) |
| 108 | 2026-08-14 | §DOC-02bk | the program's first FULLY-SHIPPED verdict, its highest symbol score (43/43 node codes), and the one excep… | [P4](BACKLOG-4-world-navigation.md) |
| 109 | 2026-08-14 | §DOC-02bj | its geography is byte-perfect and its arithmetic reverses its own thesis. THE 2026-06-16 BLOCK IS NOW EMP… | [P4](BACKLOG-4-world-navigation.md) |
| 110 | 2026-08-14 | §DOC-02bi | every filename it catalogued is real, and it counted sixty-five of seventy-one.… | [P6](BACKLOG-6-verification-docs.md) |
| 111 | 2026-08-14 | §DOC-02bg | 59 of 62 citations exact and six node codes wrong, and the engine's own comments are why.… | [P3](BACKLOG-3-content-narrative.md) |
| 112 | 2026-08-13 | §DOC-02be | an unusually strong transcriber and an unreliable narrator — 31 of 31 citations exact, and every error in… | [P4](BACKLOG-4-world-navigation.md) |
| 113 | 2026-08-14 | §DOC-02bd | everything it borrowed is exact, everything it counted is wrong, and the world it describes is three time… | [P4](BACKLOG-4-world-navigation.md) |
| 114 | 2026-08-13 | §DOC-02bc | the one row it certified in its own words is the one row its own line numbers disprove.… | [P2](BACKLOG-2-engine-systems.md) |
| 115 | 2026-08-13 | §DOC-02bb | everything it read is right, and everything it added up is wrong.… | [P2](BACKLOG-2-engine-systems.md) |
| 116 | 2026-08-13 | §DOC-02ba | the diagnosis outlived the remedy by two months; the remedy lasted four hours.… | [P2](BACKLOG-2-engine-systems.md) |
| 117 | 2026-08-13 | §DOC-02az | the report is correct and the ground moved.… | [P4](BACKLOG-4-world-navigation.md) |
| 112 | — | §DOC-02ax | the algorithm the report describes is real, and it is in the caller.… | [P4](BACKLOG-4-world-navigation.md) |
| 113 | — | §DOC-02av | the repair reported success and wrote it to a different file.… | [P4](BACKLOG-4-world-navigation.md) |
| 118 | 2026-08-13 | §DOC-02au | the phases were right and the brakes were prose — every loop cap in the "All limits prevent runaway" tabl… | [P3](BACKLOG-3-content-narrative.md) |
| 115 | — | §DOC-02at | the report was right about every number and wrong about what the numbers meant, because it chose a conver… | [P4](BACKLOG-4-world-navigation.md) |
| 119 | 2026-06-09 | §DOC-02as | the last file in the `2026-06-09 11:06:54` block, and the corpus's best node-code result sits on top of a… | [P3](BACKLOG-3-content-narrative.md) |
| 120 | 2026-08-13 | §DOC-02ar | a 100 % identifier census over an arc that has never been completable past its second quest, and the repo… | [P3](BACKLOG-3-content-narrative.md) |
| 121 | 2026-08-13 | §DOC-02aq | the most accurate document the program has measured, dated to twenty-two minutes by a file inside its own… | [P5](BACKLOG-5-platform-tooling.md) |
| 122 | 2026-08-13 | §DOC-02ap | the tightest dating result in the corpus, a reference manual whose invented examples are the healthy part… | [P5](BACKLOG-5-platform-tooling.md) |
| 120 | — | §DOC-02ao | a perfect census, an arc nobody has played, and three surfaces that render only by accident.… | [P3](BACKLOG-3-content-narrative.md) |
| 123 | 2026-08-12 | §DOC-02an | a 100 % census beside an arc that has never been played.… | [P3](BACKLOG-3-content-narrative.md) |
| 124 | 2026-08-12 | §DOC-02am | the defect was a missing CALL, not a missing SYMBOL.… | [P2](BACKLOG-2-engine-systems.md) |
| 125 | 2026-08-12 | §DOC-02al | its drafts died and its ideas shipped.… | [P3](BACKLOG-3-content-narrative.md) |
| 126 | 2026-08-12 | §DOC-02ak | a verification pass tests what it CHANGES and assumes what it LEAVES.… | [P6](BACKLOG-6-verification-docs.md) |
| 127 | 2026-08-12 | §DOC-02aj | a spec that enumerates names ships its names; a spec that specifies voice ships nothing but its names.… | [P3](BACKLOG-3-content-narrative.md) |
| 128 | 2026-08-12 | §DOC-02ai | a world where Saul of Tarsus walks from Jerusalem to Damascus and is healed by a man named Anath. *(Its T… | [P3](BACKLOG-3-content-narrative.md) |
| 129 | 2026-08-12 | §DOC-02ah | the panel whose whole premise is being exact shipped a line count that was already 1,158 lines stale, and… | [P5](BACKLOG-5-platform-tooling.md) |
| 130 | 2026-08-12 | §DOC-02ag | the document that turned quest content from code into data, and the one section of it that describes some… | [P5](BACKLOG-5-platform-tooling.md) |
| 131 | 2026-08-12 | §DOC-02ae | the roadmap that said "none of these can be started in this session," and shipped one of them thirty-thre… | [P3](BACKLOG-3-content-narrative.md) |
| 132 | 2026-08-12 | §DOC-02ad | a finished instrument, a silent game, and a default registration that is a Flute wearing a Principal's la… | [P3](BACKLOG-3-content-narrative.md) |
| 133 | 2026-08-12 | §DOC-02ab | the schema shipped and scaled 35×, the script shipped 0 of 120 lines, and the ending it was written to ea… | [P3](BACKLOG-3-content-narrative.md) |
| 134 | 2026-08-12 | §DOC-02aa | a flag written and read eleven lines apart deleted the middle layer of a three-layer design, and nobody n… | [P2](BACKLOG-2-engine-systems.md) |
| 135 | 2026-08-12 | §DOC-02z | the program's most faithful content spec, into a world that moved out from under four of its nodes.… | [P4](BACKLOG-4-world-navigation.md) |
| 136 | 2026-08-12 | §DOC-02y | three companion arcs almost perfectly intact, and an engine comment that denies eight node records while … | [P3](BACKLOG-3-content-narrative.md) |
| 137 | 2026-08-12 | §DOC-02x | the report that wrote the repo's operating manual, audited by the program it recommended.… | [P6](BACKLOG-6-verification-docs.md) |
| 138 | 2026-08-12 | §DOC-02w | the mechanism is perfect, the design is unreachable, and the decision that scoped it was reverted by a co… | [P2](BACKLOG-2-engine-systems.md) |
| 139 | 2026-08-12 | §DOC-02v | the migration shipped complete, was silently reverted the same afternoon by a commit about book imports, … | [P2](BACKLOG-2-engine-systems.md) |
| 140 | 2026-08-12 | §DOC-02u | a near-perfect implementation of an arc that no player can enter — the program's FIRST 100 % casualty.… | [P3](BACKLOG-3-content-narrative.md) |
| 141 | 2026-08-12 | §DOC-02t | the design the report explicitly FORBADE is the design that shipped — and two Layer-18 stat fields drifte… | [P2](BACKLOG-2-engine-systems.md) |
| 142 | 2026-08-12 | §DOC-02s | the strongest survival the program has measured, fully reachable — and the arc's own stated payoff is the… | [P3](BACKLOG-3-content-narrative.md) |
| 143 | 2026-08-12 | §DOC-02r | a 100 % symbol census over a feature that is 55 % unreachable — and the arc's hardest check is a button.… | [P2](BACKLOG-2-engine-systems.md) |
| 144 | 2026-08-12 | §DOC-02q | a shop that works exactly as written, and a statistics ledger that forgets everything at the one boundary… | [P3](BACKLOG-3-content-narrative.md) |
| 145 | 2026-08-12 | §DOC-02p | the origin specification of the Curse Score, implemented almost perfectly, describing a system that has n… | [P3](BACKLOG-3-content-narrative.md) |
| 146 | 2026-08-12 | §DOC-02o | the report's thesis names a payoff, and the payoff is unreachable — the game declares four endings and ca… | [P3](BACKLOG-3-content-narrative.md) |
| 147 | 2026-08-12 | §DOC-02n | the design was a TRADE — take magic weapons away from monsters, give them back through fishing — and only… | [P2](BACKLOG-2-engine-systems.md) |
| 148 | 2026-08-12 | §DOC-02m | the report's design premise was right, was built, and was then extended fourfold — and nobody can reach a… | [P2](BACKLOG-2-engine-systems.md) |
| 149 | 2026-08-12 | §DOC-02l | 2026-08-12 — §DOC-02l ✅ SHIPPED `62b162a`: the spec shipped almost perfectly, then a rename broke the one… | [P3](BACKLOG-3-content-narrative.md) |
| 150 | 2026-08-11 | §DOC-02k | the one field-name this design lock got wrong is the one it spells two ways WITHIN ITSELF — and the engin… | [P3](BACKLOG-3-content-narrative.md) |
| 151 | 2026-08-11 | §DOC-02j | a report whose own appendix says its subject was never built, and six of its seven mechanics are live cod… | [P2](BACKLOG-2-engine-systems.md) |
| 152 | 2026-08-11 | §DOC-02i | the report that designed the documentation system was right about every number it measured and wrong abou… | [P6](BACKLOG-6-verification-docs.md) |
| 153 | 2026-08-11 | §DOC-02h | the most faithfully implemented arc in the program is 70% unplayable, and the report got every coordinate… | [P3](BACKLOG-3-content-narrative.md) |
| 154 | 2026-08-11 | §DOC-02g | a code that RESOLVES is not a code that is RIGHT — and the lab report was the only source in the repo tha… | [P3](BACKLOG-3-content-narrative.md) |
| 155 | 2026-08-11 | §DOC-02f | the accuracy of a design document is not uniform across it — the transcribed half had a zero error rate a… | [P4](BACKLOG-4-world-navigation.md) |
| 156 | 2026-08-11 | §DOC-02e | the first report in the program that was RIGHT about something HEAD still gets wrong — and the first pair… | [P2](BACKLOG-2-engine-systems.md) |
| 157 | 2026-08-11 | §DOC-02d | the strongest survival in the program — 91% of its symbols resolve, 0% of its node codes do, and the repo… | [P3](BACKLOG-3-content-narrative.md) |
| 158 | 2026-08-11 | §DOC-02c | the first report in the program that fails as a description of the engine — and it fails at the thesis, n… | [P3](BACKLOG-3-content-narrative.md) |
| 159 | 2026-08-11 | §DOC-02b | the biggest report in the directory turned out to be a survival result, not a rot result — and its most u… | [P6](BACKLOG-6-verification-docs.md) |
| 160 | 2026-08-11 | §DOC-02a | the lab-report verification program opens on the oldest report, and the arc it checked shipped almost exa… | [P3](BACKLOG-3-content-narrative.md) |
| 161 | 2026-08-06 | §VM-01-G-FU-f2 | the three engine-special asks ANSWERED by the user (each per the filed recommendation) and the one that c… | [P2](BACKLOG-2-engine-systems.md) |
| 162 | 2026-08-06 | §SPARK-01-FU | + §LXX-01-FU ✅ SHIPPED `3338def`: the four double-pays die in one slice — LCY Aldous + DA2/DA3/DSF each p… | [P2](BACKLOG-2-engine-systems.md) |
| 163 | 2026-08-05 | §VM-01-G-FU-f | the SSJ tournament (Yugurt Cabin, §XLV) moved as the family's THIRD zero-delta no-op — one 124-line verba… | [P2](BACKLOG-2-engine-systems.md) |
| 164 | 2026-08-05 | §VM-01-G-FU-e | the §LXX family (CAN/DA2/DA3/DSJ/DSF) moved — four verbatim hooks + three NODE_PANELS entries + the famil… | [P2](BACKLOG-2-engine-systems.md) |
| 165 | 2026-08-05 | §VM-01-G-FU-d | the harbor chains (§SPARK-01/02 · §WHODUNIT-01 · §NAVAL-01 · §PORT-01/02 at LCY/SEN/GCI/DNF/MME) moved — … | [P2](BACKLOG-2-engine-systems.md) |
| 166 | 2026-08-05 | §VM-01-G-FU-c | the §ALCHEMY-01/§WISDOM-01 Roen-arc stack moved as the family's SECOND zero-delta no-op — six verbatim ho… | [P2](BACKLOG-2-engine-systems.md) |
| 167 | 2026-08-05 | §VM-01-G-FU-b | the §HUNT-01/02 stack moved as a PROVABLE NO-OP — four verbatim hooks, five NODE_PANELS entries, zero ver… | [P2](BACKLOG-2-engine-systems.md) |
| 168 | 2026-08-05 | §VM-01-G-FU-a | the §CROWN-01 stack moved — seven combat verbs, two verbatim hooks, two once-panels — and the arc's own g… | [P2](BACKLOG-2-engine-systems.md) |
| 169 | 2026-08-05 | §VM-01-G-FU | ✅ SHIPPED `02ff4aa`: the engine-region Class-F tail censused end to end, ZERO dead references, and gate #… | [P2](BACKLOG-2-engine-systems.md) |
| 170 | 2026-08-05 | §VM-01-G4d | the D3 concurrent menu + the two Class-E strays, and the §VM-01-G4 slice plan is COMPLETE. On "continue" … | [P2](BACKLOG-2-engine-systems.md) |
| 171 | 2026-08-04 | §VM-01-G4c | the button mode gets its first consumers, and "13 D1 verbs" measured down to 4. On "continue" took the bo… | [P2](BACKLOG-2-engine-systems.md) |
| 172 | 2026-08-04 | §VM-01-G4b | the first `choice` in the game's history, and the schema grew one mode because a choice has no button. On… | [P2](BACKLOG-2-engine-systems.md) |
| 173 | 2026-08-04 | §VM-01-G4a | the `choice` opcode has a host end at last, and the `cost` leaf says the price out loud. On "continue" th… | [P2](BACKLOG-2-engine-systems.md) |
| 174 | 2026-08-04 | §VM-01-G4 | CHILD LAB REPORT ⚠️ DESIGN LOCKED PENDING ONE ASK `f340143` (the ASK is now ANSWERED — see NEWEST): the `… | [P2](BACKLOG-2-engine-systems.md) |
| 175 | 2026-08-04 | §VM-01-G2b | the ctx design that blocked this slice was ONE FIELD — and moving the 29 blocks read five narrative beats… | [P2](BACKLOG-2-engine-systems.md) |
| 176 | 2026-08-04 | §AUDIT-03q | the born-dead code class is a gate now — and the reading pass that filed this row had itself left one beh… | [P6](BACKLOG-6-verification-docs.md) |
| 177 | 2026-08-04 | §AUDIT-03m-FU | the PENDING tail is empty, and the two docs that were already "swept" turned out to be gate-green over 35… | [P6](BACKLOG-6-verification-docs.md) |
| 178 | 2026-08-04 | §AUDIT-03p | the Void's "first crack" line now reaches 13 nodes instead of 5, and gate #13 can finally see inside a fu… | [P3](BACKLOG-3-content-narrative.md) |
| 179 | 2026-08-04 | §AUDIT-03m | the two live narrative docs stop speaking 26×16, and the sweep is a measured tool with a gate — but the c… | [P6](BACKLOG-6-verification-docs.md) |
| 180 | 2026-08-04 | §DX-02l-FU | the assertion went red on the prose that records the defect it checks for. It now flags only what an auth… | [P5](BACKLOG-5-platform-tooling.md) |
| 181 | 2026-08-04 | §AUDIT-03k | Yael was two people. The alias map collapses all seven display-name splits, and gate #14's new phase make… | [P3](BACKLOG-3-content-narrative.md) |
| 182 | 2026-08-03 | §DX-02l | the deliberate-backup endpoint got the `./api.sh` wrapper it never had — and the sweep the row asked for … | [P5](BACKLOG-5-platform-tooling.md) |
| 183 | 2026-08-03 | §DX-02k | the argless `save()` was not a stray test call. It was the server's own per-write path, and it wrote a 5.… | [P5](BACKLOG-5-platform-tooling.md) |
| 184 | 2026-08-03 | §DX-02h | + §DX-02i ✅ SHIPPED TOGETHER `a047348`: the row said one monster was unreachable; 55 are. The doc that ma… | [P5](BACKLOG-5-platform-tooling.md) |
| 185 | 2026-08-03 | §DX-01e-FU | the seed inbox's 36 pointers were migrated, and 35 of them pointed at unrelated code. The four claims the… | [P6](BACKLOG-6-verification-docs.md) |
| 186 | 2026-07-31 | §DX-01e | a doc anchor now names a SYMBOL, and 43 of the 50 anchors in the live docs were pointing somewhere else. … | [P6](BACKLOG-6-verification-docs.md) |
| 187 | 2026-07-31 | §DX-02g | two monsters carried a tier the engine has no entry for, and all four readers of that field answered with… | [P2](BACKLOG-2-engine-systems.md) |
| 188 | 2026-07-31 | §AUDIT-03n | the ending called three of your six friends strangers no matter how you played. The row named 12 dead ent… | [P3](BACKLOG-3-content-narrative.md) |
| 189 | 2026-07-31 | §AUDIT-03j | the engine's own node references resolve, and gate #13 keeps them that way. The row named 9 dead keys in … | [P4](BACKLOG-4-world-navigation.md) |
| 190 | 2026-07-30 | §DX-01d | + §DX-01i ✅ SHIPPED TOGETHER: every `del` in the API reported success and changed nothing on disk. Not ju… | [P6](BACKLOG-6-verification-docs.md) |
| 191 | 2026-07-30 | §DX-02f | the two undocumented reds were STALE TESTS, not mechanic drift. The Playwright red set is now four, in on… | [P6](BACKLOG-6-verification-docs.md) |
| 192 | 2026-07-30 | §DX-01c | `./api.sh post monster` works. WBAPI Hazard #2 is retired, and with it the standing API-first exception t… | [P5](BACKLOG-5-platform-tooling.md) |
| 193 | 2026-07-30 | §AUDIT-03h | the last 10 display-name `npc` values are keys now — and the one the row could not resolve turned out not… | [P3](BACKLOG-3-content-narrative.md) |
| 194 | 2026-07-29 | §AUDIT-03l | the node-code reference is generated now, and the 26×16-era tables are quarantined instead of quietly wro… | [P4](BACKLOG-4-world-navigation.md) |
| 195 | 2026-07-29 | §AUDIT-03c | the `710bb75` remap audit closes — 6 of 8 remaps were right, and the *table that caused them* is the real… | [P4](BACKLOG-4-world-navigation.md) |
| 196 | 2026-07-29 | §DX-02e | the §KG corridor road was never broken — the test had pinned generated coordinates. One of the two carrie… | [P4](BACKLOG-4-world-navigation.md) |
| 197 | 2026-07-29 | §AUDIT-03g | the last 68 quests get an NPC anchor — `./api.sh audit` reports `errors: 0` for the first time, and not o… | [P3](BACKLOG-3-content-narrative.md) |
| 198 | 2026-07-29 | §AUDIT-03e | the `code = key` backfill — 287 nodes stop sharing ONE `undefined` state slot, and five §KG NPCs speak fo… | [P4](BACKLOG-4-world-navigation.md) |
| 199 | 2026-07-29 | §AUDIT-03b | all 203 `long_john_silver_sen` mis-stamps re-anchored; the NPC-key vocabulary and the WBAPI field patcher… | [P3](BACKLOG-3-content-narrative.md) |
| 200 | 2026-07-28 | §AUDIT-03b | , ZERO EDITS MADE — ⚠️ INCOMPLETE, paused to save tokens. The whole derivation is recorded in the §AUDIT-… | [P3](BACKLOG-3-content-narrative.md) |
| 201 | 2026-07-28 | §AUDIT-03f | `removeFns` is comment-aware — `quest_sea_01` + `quest_sb_01` restored to the WBAPI parse; parse-parity i… | [P5](BACKLOG-5-platform-tooling.md) |
| 202 | 2026-07-28 | §AUDIT-03a | same session as §DX-01a below: `check:dupkeys` is gate #11 of `check:walk`; the last-key-wins rot class i… | [P6](BACKLOG-6-verification-docs.md) |
| 203 | 2026-07-28 | §DX-01a | `check:walk` is fully green for the first time since 2026-07-08 (§JUNK-01 closed with it). On "continue" … | [P6](BACKLOG-6-verification-docs.md) |
| 204 | 2026-07-28 | §DX-01f | the `[x]`-row bulk migration — BACKLOG.md is now genuinely-open work only. On "continue" took the backlog… | [P6](BACKLOG-6-verification-docs.md) |
| 205 | 2026-07-28 | §VM-01-G3 | Class-C quest activation is declarative — and the silent-rot thesis proved out three more times. On "cont… | [P2](BACKLOG-2-engine-systems.md) |
| 206 | 2026-07-28 | §VM-01-G2 | NODE_HOOKS — 7 Class-E bespoke blocks (~440 lines) leave storyRender as verbatim registered hooks, proven… | [P2](BACKLOG-2-engine-systems.md) |
| 207 | 2026-07-28 | §VM-01-G | (`cf2c17c`) + G1 NODE_PANELS SHIPPED (`25c3710`) + 2 dead-panel fixes (`bf6f486`, `7a130cd`). Resumed `fe… | [P2](BACKLOG-2-engine-systems.md) |
| 208 | 2026-07-28 | §NPC-01-SF4 | dead codes `CQ`/`SQ`/`GC` remapped to `CDG`/`NUE`/`TRD` — §NPC-01 is now FULLY CLOSED (A–D + SF1–6 all sh… | [P3](BACKLOG-3-content-narrative.md) |

## Archived §RESUME entries

> *§RESUME entries for 2026-07-22/23 (§NPC-01-D Talk verb · §NPC-01-SF6 · §POT-PROMOTE · the cleanup/prompt.md-polish passes · §DX-01b/g · §BOARD-01-VOID-GATE · §VM-01 A/B/C/D/E/F/F-FU) moved verbatim → plan-archive.md §"Archived 2026-07-28 — §DX-01f".*
>
> *Earlier §RESUME entries — **2026-07-21 and before** (§VM-01 Inc-A-open + track-open, all §BOARD-01-FU, §CLEANUP-01 landing, §PLAY-01/§DEATH-01/§XP-01, §MESH-01/02, and the older operational notes) — archived verbatim to **plan-archive.md** § "Archived 2026-07-23 — §RESUME snapshots". Standing operational guidance lives in CONTRIBUTING.md; the no-jump-travel invariant in prompt.md §6.*


---

> **Archive:** all closed / completed work (§WALK, §TIMELESS-01, §WBAPI-01, §EDITOR-01-D / -02 / -FU, §CELL-14, the full §ARCH-01 wave-by-wave history, **§NAV-01 Inc a–g + diagnosis/layer-stack/data-shapes, §MESH-01 in full, §MESH-02 (a)–(j), the five 2026-07-07 Game Content closes (§GR · §DESIGN-03 · §DUNGEON-01 · §MATH-01 · §FUTURE-01) + the fishmongerRowRestored payoff + the §MBIT-02 bitLabel record**, and prior §RESUME snapshots) lives in **[plan-archive.md](plan-archive.md)** — the 2026-07-03 sections, the three 2026-07-06 sections, the **2026-07-07 section** (§MESH-02 + Game Content close pass), the **2026-07-23 sections** (§PLAY-01 + the pre-07-22 §RESUME snapshots), and the **2026-07-28 §DX-01f section** (the closed-`[x]`-row bulk: §NPC-01 · §CLEANUP-01 body · §VM-01 A–F-FU · §BOARD-01 0/A/B/C + FU1–8 · Mechanics · Multiplayer · Game Content · the 07-22/23 §RESUME snapshots) each record the verification (commits + greps + green gates) run before their blocks were moved.

---

*© 2026 Paul Richeson — MIT License.*
