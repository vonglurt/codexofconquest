<!-- SPDX-License-Identifier: MIT — Copyright (c) 2026 Paul Richeson -->
# BACKLOG — Phase 1: Playable Truth — content that exists and cannot be reached

> **Phase 1 of 6.** Split out of `BACKLOG.md` on 2026-08-23; `BACKLOG.md` is now the routing index and holds the chronological §RESUME map across all six files.
>
> **Scope of this file.** Shipped content the player can never reach or complete: unreachable act-gated beats, inert counters, gate flags with no writer, quest deadlocks, endings out of range.
>
> **Siblings:** **1. Playable Truth** *(this file)* · [2. Engine & Systems](BACKLOG-2-engine-systems.md) · [3. Content & Narrative](BACKLOG-3-content-narrative.md) · [4. World & Navigation](BACKLOG-4-world-navigation.md) · [5. Platform & Tooling](BACKLOG-5-platform-tooling.md) · [6. Verification & Documentation](BACKLOG-6-verification-docs.md)

## The Backlog Directive — restated in full

> **This file is the list of outstanding activities for Phase 1 (Playable Truth).** The *how-we-work* rules live in **[CONTRIBUTING.md](../../CONTRIBUTING.md)**; closed/shipped work is archived in **[plan-archive.md](plan-archive.md)**. Split out of the former `plan.md` on 2026-07-09.
>
> **⚠️ Before starting ANY row — Existing-Work-First (CONTRIBUTING.md § Existing-Work-First Policy):** `git status` + `git log` first (uncommitted/fresh work matching the row = it's done or in progress — finish it, don't redo it); check for a second live session (`ps aux | grep claude`) before editing shared files; grep to *disprove* the row's premise (rows go stale — four rows have closed as ALREADY SHIPPED). A §RESUME entry says where the last session *stopped*, not what the tree looks like *now*.
>
> **Legend:** `[ ]` open · `[~]` in-progress / recurring · `[x]` closed (kept here for context; migrate to plan-archive.md over time).

> **⚠️ This file is one of six.** A row's neighbours may live in a sibling phase file, and the Existing-Work-First grep MUST cover all six — `grep -n '§DX-02xx' BACKLOG*.md` — before you conclude a row is unclaimed. The §RESUME entry that closed a row may sit in a different phase file than the row itself.

## Standing Directive — Code Comments (user directive 2026-08-23)

> **Binding on every increment in this file.** Where this and a §-row's own instructions disagree, this wins.

**The failure this prevents:** writing a comment that narrates the change being made right now. That context is real, but it expires the instant the change merges — the defect it describes no longer exists, so the comment becomes a story about a problem no future reader can observe. It is a changelog entry in the wrong file, and a third copy of text already required in the commit body and the PR description.

- **CC-1 (MUST NOT)** Write a comment describing a change, a fix, a defect, its cause, or what the code used to do. No *"was / now / previously / instead of"*, no *"this fixes"*, no *"needed because otherwise"*, no *"note that we no longer"*.
- **CC-2 (MUST)** Apply the **survival test** to every comment before writing it: *would this still be true and useful to someone reading this file a year from now, who never saw the diff?* If it only makes sense beside the diff, it is changelog — delete it and put it in the commit body.
- **CC-3 (MUST)** Default to **zero** comments. Declarative config — Terraform, DNS records, k8s manifests, CI YAML, Helm values — is self-describing and takes none. A resource named `dmarc-example-com` does not need a comment saying it is the DMARC record.
- **CC-4 (MAY)** Comment only when a future editor would actively break something without it: a non-obvious external constraint, a required out-of-band manual step, an invariant the surrounding code cannot show. **One line.** If it needs a paragraph it belongs in a design doc, not inline.
- **CC-5 (MUST)** Before every commit, re-read the comment lines added: `git diff --cached | grep '^+' | grep -E '#|//|/\*'`. Each hit must pass CC-2 on its own. **Deleting is always an acceptable outcome.** *"I already wrote it"*, *"it is only one line"*, and *"this one is genuinely useful"* are not exemptions — the last one is the exact thought that precedes every violation.
- **CC-6 (MUST)** Applies to comments **edited** as well as added. When a change invalidates an existing comment, the default action is **DELETE**, not rewrite it into a new narrative.

> **⚠️ KNOWN TENSION, flagged not resolved — needs a user call.** This repo currently runs an *opposite* convention in `play.html`: **§AUDIT-03n / §DX-02c "annotate, don't rewrite"**, which deliberately preserves defect history inline (e.g. `const NODE_NPC_KEYS`'s 8-line comment explaining that 21 of 26 rows named retired node codes, or `_renderFinalMap`'s §AUDIT-03e note on why the node filter is held at the historical set). Those are exactly the *"what the code used to do"* narratives CC-1 forbids, and several are load-bearing: they are the only record of why a value is held where it is, and deleting them would re-open closed audit rows. **Until the call is made, the working rule is:** CC-1..CC-6 bind all **new** comments; existing §AUDIT-/§DX-annotated blocks are **not** swept under CC-6 — they are a documented archaeology convention, not accidental changelog. Filed as **§DX-02fa** below.

---

## Open at a Glance — Phase 1 tracks

- **`[ ]` §AUDIT-03d + h/i/j/k/m — quest-data appended-field rot (NEW 2026-07-28, surfaced by §VM-01-G3)** — **c ✅ shipped 2026-07-29** (the `710bb75` remap audit: `CQ`=`CDG` and `FR`=`AMS` both right, `SF`→`LCY` was the only wrong one and G3 had already fixed it; 5 of the 8 remapped lines no longer exist; **0 of 2,853 quests name a non-existent node** in `activateNode` *or* `waypointNode`, now gate-fenced; one split-key fix on `quest_cat_06`; **root cause filed as §AUDIT-03l** — `maps.md`'s legend table has 73/76 dead codes) · **a ✅ shipped** (`check:dupkeys` gate #11) · **b ✅ shipped 2026-07-29** (all 203 `long_john_silver_sen` mis-stamps re-anchored; shared 4-registry `npcKeyOk`; depth-aware `patchStringField`; `./api.sh batch-npc`) · **f ✅ shipped** (comment-aware `removeFns`). · **e ✅ shipped 2026-07-29** (the `code = key` backfill: 287 nodes stop sharing one `undefined` state slot; the §KG NPCs talkable at last; activation opened selectively behind a structural seam) · **g ✅ shipped 2026-07-29** (the last 68 unanchored quests derived family-by-family — `./api.sh audit` **errors 0**; the "§D02 approach arc inherits its battleground's giver" rule). **h ✅ shipped 2026-07-30** (the 10 display-name values normalized — `gret`/`pier` not `gret_orrens`/`pier_falk`, and Bog Mudwhistle turned out to be a tournament opponent, not an NPC; **0 unresolvable `npc` values corpus-wide**, the §AUDIT-03b tolerated list is now empty). **j ✅ shipped 2026-07-31 `c5f9079`** (the row's 9 dead keys were **42 across 5 registries** — `NODE_NPC_KEYS` 21/26 and all 12 `NPC_FAREWELLS` route keys were registries it never named, and favor tinting + farewells had worked at **LHR alone**; both columns fixed, 7 junction vignettes rehomed by their own text, Corelli's dead stop 2 revived, Brannick given the SEN slot he could never reach; **`check:walk` gate #13 `check:noderegs`**, whose classification is explicit precisely because a percentage heuristic is blind to an *entirely* dead table). **n ✅ shipped 2026-07-31** (the npc-key twin of j: the row's 12 entries in 4 registries were **29 references across 7 registries plus five live `_npcFavor('bruhns')` gate sites** — the ending called Quill/Weckmann/Bruhns strangers at any favor, the S6 joint conversation ANDed a live key with a dead one, and a granted key item's only delivery button was unreachable; **`check:walk` gate #14 `check:npcregs`**, the npc mirror of #13; the tell was two `npcOrder` lists that disagreed). **k ✅ shipped 2026-08-04 `1c4f547`** (one character, one key: `WBAPI.NPC_ALIASES` collapses **7** display-name splits — the row's 6 plus `city_fence`⇄`pachelbel`, which no name rule derives — the slugs leave `npcKeyVocab()` (297→290), all three write paths normalize and *report* the collapse, and Yael is one person again at 22 quests; **`ship_captain` is NOT an alias** — Smollett captains the Hispaniola at HMS while SEN is the Tilbury Star, so **a role collision is not an identity** and the classification is explicit; `check:npcregs` **phase 5**; found en route that `saveAndVerify` verified against the caller's input, failing a correct write). Open: **imported multi-act chains mass-activating per node (d, design call — now the gating row: its staging releases the 983 quests §AUDIT-03e holds)** · unkeyed named speakers incl. Roen + the Mother Mimic + the tournament cast (i, design call) · the ending-map predicate (e-FU, design call) · **favor 3 unreachable for 5 of the 6 NPCs, so the `entries[3]` tier is dead content (o, NEW 2026-07-31, design call)** · **l ✅ shipped 2026-07-29** (the node-code reference is generated — `npm run nodes` → `docs/maps/node-index.md`, 416 nodes + an 83-row LEGACY CODE MAP; `check:walk` gate #12 keeps it honest; `maps.md`'s six 26×16-era sections quarantined ⚠️ HISTORICAL) · **m ✅ shipped 2026-08-04 `b399318`** (the prose half of §AUDIT-03l: 4,663 naive hits were mostly noise — `DC` is a difficulty class 435×, `EB` is *Epic Battleground* 163× — so `src/scripts/legacy-codes.js` counts only node-context tokens with the ambiguous codes classified explicitly; `world.md` 77 annotations + `story.md` 193, **HISTORY docs annotated never rewritten** (2,287 refs left alone, the tool refuses to write one), **`check:walk` gate #16 `check:legacycodes`**, and the finding that mattered more than the codes — `world.md` described a **retired navigation model** (0 of 416 nodes carries `N`/`S`/`E`/`W`; `J1`–`J7` deleted), so annotating alone would have laundered a false claim; it now carries a historical-model banner. ~~Open tail: 7 PENDING live docs / 230 refs~~ → **m-FU ✅ shipped 2026-08-04** (below)) · **m-FU ✅ shipped 2026-08-04 `79987d3`** (the PENDING tail closed AND the reason it existed: `nodeContextLine` is a **LINE** test, so *"Write Entry 42 at CI"* — the commonest way a story doc names a place — was invisible, and `world.md`/`story.md` sat **gate-green over 35 codes** the first sweep never saw; four hand-classified cues added (place preposition · trailing place-noun · sole parenthetical `(IN)` · code column `— EB |`), the `/` guard dropped after measuring that **all 218** slash-preceded hits are node runs, and three counter-exemptions in the other direction — `CI` is *continuous integration* on a build line, `SW`/`SE` are compass in a compass run (the sweep briefly turned *"N, S, E, W, SW, spire"* into a swamp), `CQ→CDG` is a stated mapping whose left side must not be rewritten. **All 7 PENDING docs swept, `PENDING` now EMPTY, 10 docs fenced, 0 live refs.** The finding that outranks the tooling: **annotation without verification launders a wrong claim** — 4 of the 6 rows in `world.md`'s Act VIII Homecoming table named the wrong node, and §AUDIT-03m had already annotated one into a tidy, wrong `` `VBY` ``; the engine's own `birkaNpcs` roster settles it, and *"Tell Pachelbel at SH"* is invisible to the tool **by construction** because `SH` was never a `NODE_MAP` key at all — §AUDIT-03p's born-dead class, in the docs) · **p ✅ shipped 2026-08-04 `ea19a88`** (the same rot in the LIVE engine, one scope level down: `_voidFlavorLine`'s **function-local** `CLUSTER` had **11 of 16 dead node keys**, so the Void's first-crack line rendered at 5 of the 16 authored places since `cc562f5` — coverage now **13, all live**; eight remapped and label-corroborated, and **four keys — `SH`/`PH`/`MH`/`WM` — were never `NODE_MAP` keys at the commit that authored them**, written from memory and born dead, so three are dropped rather than guessed; `check:noderegs` **phase 5** now classifies **indented** declarations too, closing the reason gate #13 read green over a rotting table for months).  · **q ✅ shipped 2026-08-04 `28c6f8a`** (the born-dead class in docs — codes that resolve in **neither** registry, so gate #16 is blind to them by construction. Measured first: of the 11 filed tokens **only `SH` is a live trap**, `PH`/`MH` sit backticked in `story.md`'s own §AUDIT-03p note, and the other **eight are design-era arc IDs** reaching live prose only through `quest.md:527`, which already says so — no sweep warranted. The finding: §AUDIT-03m-FU fixed *"Tell Pachelbel at SH"* by reading and left *"at `LLA` or SH"* on the same page, because **the preposition belongs to the code the tool can already see** — closed by a new signal, *a line already naming a live node in backticks is talking about places*, and fixed against `birkaNpcs` rather than guessed. `src/scripts/legacy-codes.js` **phase 2**, explicit `BORN_DEAD`/`NOT_A_NODE_CODE` tables — **19 tokens fire, `NG` 47× and never a node** — plus the shape nobody had named: a real historical code the `maps.md` legend never listed (`FR`→`AMS`). Limit stated and asserted: **two letters only**, so a three-letter code written from memory is not caught) See [§AUDIT-03](#audit-03--quest-data-appended-field-rot-new-2026-07-28-surfaced-by-vm-01-g3).

---
## §BACKLOG — Open Items (Phase 1)



### §DX-02ha — a set-piece boss drops nothing, and it is the only real one that does (NEW 2026-08-25 during §DX-02fy, 🟢 no design call)

- [ ] **§DX-02ha — `void_shaman` is fought at `TRD` and has no `MONSTER_DROPS` row.** **Measured at `ebf6863`:** of the **50** distinct `battle.key`s in `play.html`, **47 carry a drop**. The three that do not are `_bruhns` (by design — the `_isFinalBoss` leg never reads the key), `desert_wanderer` (created with one in §DX-02fy) and **`void_shaman`**. `TRD`'s battle label is *"Goblin ×4 + Hobgoblin ×2 + Void Shaman"* — a named Void-arc boss at a Shard node — and defeating it yields no trophy.
> **Re-derive:** collect `battle:{…key:…}` and the keys of the `MONSTER_DROPS` block, then diff. `./bin/api list monster --has-drop false` filters the same set server-side.
> **Fix:** `./bin/api drop void_shaman name="…" sell=<gp> icon=<emoji>` — the verb added in §DX-02fy. The `sell` band for the node's tier is `djinn` 35 / `gigascorpion` 35. **The name is the only call** and the Void arc supplies it.
> **Provenance:** §DX-02fy, measuring whether a node-battle monster conventionally carries a drop before giving one to `desert_wanderer`. 47 of 48 real ones do.

### §DX-02gw — the Warlord was one of four; one of the other three is fought now, and the other two turned out not to be the same kind of thing (NEW 2026-08-24 during §DX-02gv · 1 of 3 shipped 2026-08-25 `5d0bee8` · **remaining 2 retagged 🟡 → ASK 2026-08-25**)

- [ ] **§DX-02gw — `bruxa_corvo_bianco`, `dragon_of_fyresdal` and `slyzard_matriarch` are statted, priced and named by nothing.** **Measured at `4bac567`:** the new `check:battlepools` gate carries exactly **3** `UNREACHABLE_DEADLY` exemptions, and they are these. All three sit in the same four-line `// deadly` block of `MONSTER_POOL` that `void_warlord` sat in — `bruxa_corvo_bianco@5787` (`ac:18, hp:187, 3d8+7`), `dragon_of_fyresdal@5788` (`ac:18, hp:243, 3d10+8`, the highest-HP monster in the pool), `slyzard_matriarch@5790` (`ac:16, hp:187, 3d10+6`) — each with a priced drop in the block at `MONSTER_DROPS@6264-6267` (Bruxa Heart `sell:75`, Fyresdal Dragon Scale `sell:90`, Matriarch Scale Trophy `sell:75`). **Each is referenced exactly twice in the file: its pool row and its drop row.** No `battle.key`, no `P.<key>` in any terrain pool, no `bossKey`. §DX-02gv found and fixed the fourth member of that block and filed these rather than doing them inline.
> **Fix:** unlike §DX-02gv these have no node already pointing at them — `TLS` at least carried a label promising the Warlord. The call per monster is **which node hosts the fight**, and the names answer it: all three are Witcher-corpus imports (Corvo Bianco, Fyresdal, the slyzard), so the host should be a node whose arc came from that corpus — `vendor/1367-sources/index.md` is the registry to read before choosing. **Recommendation:** one increment per monster, each a `./bin/api put node <code> battle=…` plus the exemption retired from `UNREACHABLE_DEADLY` in `src/scripts/check-battlepools.js` — the gate then proves the fix, because a reachable monster still listed as exempt fails it.
> **Provenance:** §DX-02gv, on measuring how many deadly monsters the new gate's direction-2 walk actually caught: four, not one.
> **Shipped 1 of 3 — 2026-08-25 `5d0bee8`: `bruxa_corvo_bianco` → `CLJ`.** **Measured at `6726358`:** `check:battlepools` carried **5** exemptions, **3** of them `UNREACHABLE_DEADLY` and owned by this row; `grep -c bruxa_corvo_bianco play.html` = **2** (pool row `@5787` + drop row `@6264`). The design call — *which node hosts the fight* — was settled by the repo in four places, none of them a guess: `docs/design/monsters.md:496` annotates the key **"ancient estate"** and `CLJ` is the only estate-castle vampire node; `vampire_castle@6309` (the `WORLD_DB` terrain row) already lists `P.bruxa`; `CLJ.text` ends *"What she does in the west wing is her own business"*; and `docs/archive/plan-archive-verbatim-2026-06-12.md:2261` recorded the design as *"No battle — toll is the alternative; Elise unpaid is a Bruxa with centuries of practice; west wing holds what east wing guests…"*. **`docs/design/world.md:798` had already shipped the fight in prose** — *"Bruxa Elise Mourne uses Dodge when retreating during her guardian challenge"* — against a node whose `battle` was `null`. **Corvo Bianco is the estate, not a second character:** the display name is Elise Mourne's title, and `battle.label` (`'Bruxa Elise Mourne — the West Wing'`) carries the reconciliation. **The epic layer was never the home:** all **20** `epic_*` nodes already hold a `bossKey` (`grep -c "bossKey:'" play.html` = 20). **Safe because `node.battle` is opt-in** — a card behind `btn-start-battle@38295`, latched once in `S_story.defeatedBattles`, read at `storyPreBattle@36388` — so the toll, the Toll Token and the free sleep are untouched; refusing the toll is the fight. Written through `./bin/api put node CLJ battle=…` and round-tripped **from the file on disk**, not the server buffer. **Measured after:** unreachable deadly **3 → 2**, exemptions **5 → 4**, and `bruxa_corvo_bianco`'s exemption is retired in the same commit so the gate proves the fix. 17 gates exit 0, `--selftest` 4/4, `npm test --prefix src` **1023 passed** with the server stopped.
> **Still open — 2 of 3, and NO LONGER 🟡. Retagged ASK 2026-08-25 after the ground disproved the row's own framing.** The 2026-08-25 pass took `dragon_of_fyresdal` intending to host it at `PDL` (node 19, the only non-epic `islands` node with no `battle`) on the strength of `monsters.md:497`'s *"island territory"*. **Two measurements killed that plan before a line was written, and they generalise to `slyzard_matriarch` too.**
>
> **(1) The island-dragon niche is already occupied by shipped story.** `ACE` (58, `epic_islands`, *Isle of the Wyrm Crown*) holds `bossKey:'ancient_sea_dragon'`, and `docs/design/story.md:2208` hangs a whole arc off it — **Q58, Island Elder Maris, *"parent node `PDL`"***, whose wound is *"The dragon arrived when Maris was fourteen. Five generations of her family fished the eastern shoals."* A second dragon at `PDL` contradicts Q58. The same holds for the draconid summit: `KTM` (67, `epic_mountains`) is `summit_wyrm`, `maps.md:201`. **`grep -c "bossKey:'" play.html` = 20 — all 20 epic nodes are taken.**
>
> **(2) `MONSTER_POOL` is a library, not an inventory — so "statted, priced, unfought" is not by itself a defect.** Re-derived at `b35bc9e` with the gate's own reachability definition (a `battle.key`, a `P.<key>`, a `bossKey`, or any mention beyond the pool + drop rows), **50 of 384 monsters are unreachable, at every tier**: easy 6/60, medium 16/148, hard 26/123, deadly 2/53 — `royal_wyvern` and `koshchey` sit unreached right beside `dragon_of_fyresdal`. `docs/design/monsters.md:320` names the collection ***"Dark Fantasy Bestiary II — ✅ Implemented"*** and catalogues it by category (Ogroids, Vampires, Necrophages, Draconids…). The only thing that distinguishes the two remaining members from 48 others is that **the gate happens to check `tier:'deadly'`** — filed as **§DX-02gx** (Phase 6).
>
> **What actually made 1 of 3 real, and why these two are not it.** Neither `void_warlord` (§DX-02gv) nor `bruxa_corvo_bianco` was closed because it was deadly and unreached — each was closed because **a shipped promise already pointed at it**: `TLS.battle.label` read *"→ Void Warlord"*, and `docs/design/world.md:798` already described *"Elise Mourne … during her guardian challenge"* at a node whose `battle` was `null`. **Neither remaining member is named by any node text, any design doc, any quest or any archive entry** — `grep -rniE 'fyresdal|slyzard' docs vendor` returns only the `monsters.md` stat rows and this backlog. Picking a host for them is authoring a creature the world has not asked for.
>
> **RECOMMENDATION (needs the author).** Preferred: **retire both** — delete the two `MONSTER_POOL` rows and the two `MONSTER_DROPS` rows, and delete `UNREACHABLE_DEADLY` from `src/scripts/check-battlepools.js` with them, so the gate's direction 2 goes to zero exemptions and stays honest. Alternative, if the bestiary is meant to be a library that outruns the map: **keep both, and change the gate** rather than the data — §DX-02gx. **Do not** author a host node for either; that is the option the ground argues against, and it is the one a fast reading of this row would pick.


### §VM-01-G2b-FU — Five act-gated Birka beats that can never fire (NEW 2026-08-04, 🟡 ASK)

- [ ] **§VM-01-G2b-FU — re-gate the five unreachable act-legged narrative beats.** **Measured at ship time of §VM-01-G2b:** `storyRender` assigns `S_story.actNumber = node.act || 1` on **every** render, before the npc-row region, and every Birka-region node is `act:1` — so an `actNumber >= N` (N ≥ 2) leg inside one of these blocks is permanently false. Dead: `birka-lamp-inquiry` (`>= 2`, `TLL`) · `birka-brynn-heartwood-letter` (`>= 4`, `TLL`) · `birka-yael-named-report` (`>= 6`, `LHR`) · `birka-s54-joint-witness` (`>= 7`, `LHR`) · `birka-quill-couperin-farewell` (`=== 8`, `LLA`). **The cascade:** `birka-lamp-choice` (Beat 2 of the §XXXV vigil arc) gates on `brynnKeeperStoryTold`, and the dead Beat 1 is that flag's **only writer** — so the choice, `brynnLightKept`, the §XXV farewell branch and the §XXVII Town Crier line are all unreachable too. **Why this is an ASK and not a fix:** §VM-01-G3 retired the identical dead legs from the quest stanzas by **dropping** them (the gate's other leaves already carried the staging), but these are narrative beats whose act leg IS the staging — something has to replace it, and the right signal (shards? a quest completion? a mission bit? a day threshold?) is a design decision. **And the docs disagree with the code on more than the act:** `world.md` §XXXIX designs the Yael Named Report for `LLA`/`HKG` at Act IV+, while the shipped block fires at `LHR` at Act VI+ — §AUDIT-03m-FU's lesson says do not annotate or "fix" an unverified claim into a confident-looking one, so neither number is to be trusted without corroboration. **Pinned:** `uqf-npc-row-hooks.test.js` asserts all five gates, both node acts and the cascade, so the fix must update the test. **Docs already annotated** (not rewritten) at all five sites in `world.md` and `story.md`.

### §SIREN-01-FU — the Littoral Courts lost their entire combat layer to a worldbuilder commit, and the shape it used is now a CI failure (NEW 2026-08-12 during §DOC-02u, 🟠 design call)

- [ ] **§SIREN-01-FU — the arc's stated thesis is *"The Ladies never fight. The ocean does,"* and at HEAD there is no ocean.** All four junction nodes (`LJ0`/`LJ1`/`LJ2`/`LJ3`) and with them all three sea crossings — `battle:{label:'Sea Spawn × 2', key:'sea_serpent', count:2}`, `battle:{label:'Deep One × 3', key:'deep_one', count:3}`, `battle:{label:'The Serpent of the Passage', key:'sea_serpent', count:1}`, every one **byte-exact at birth to the report's own §II-A table** — were removed on **2026-06-10 by `a61d6eb`**, whose subject line is *"fix: P5 batchEditNode O(M) rewrite + P6.5 DELTA key crash"* and whose body describes only an O(N×M) string-allocation rewrite and a `DELTA['N']` key-casing crash. **It says nothing about world content.** Every surviving node is `battle:null`; what remains is the language half of a design whose whole point was the alternation.
> **It cannot be restored as written.** The crossings were `junction:true`, which `check:invariants` **I2 fails outright** (*"junctions were bulk-deleted in §WALK-1/§CELL-05"*), so a verbatim revert is a red gate. **Three options, all needing the call:** (1) re-express the three crossings as `battle` fields on real coastal nodes between the courts; (2) fold the encounters onto the courts themselves as arrival battles, which breaks the court/sea contrast the arc is built on; (3) accept a court-only arc and **cut the "the ocean does" thesis from `world.md`/`story.md`** so the docs stop describing a structure that is gone.
> ***The durable lesson, and it outranks the row: a deletion that is correct as TOOLING and catastrophic as CONTENT will not describe itself in the commit message. Read the diff, not the subject line.*** No gate could have caught it either — the nodes were removed cleanly, so `check:noderegs` and `check:questgraph` both stayed green.
> **Blocked behind §AUDIT-03x** for playtesting: all six surviving nodes are non-primary, so nothing here is verifiable in-game until cell primacy is resolved. **§AUDIT-03af (b) is downstream of whichever option is chosen.**

### §DX-02ft — The Homecoming cannot happen: eleven act-gated beats are unreachable, and `actNumber` is not what the content thinks it is (NEW 2026-08-23 during §DOC-02db, 🔴 ONE DESIGN CALL for all eleven)

- [ ] **§DX-02ft — six Act VIII farewell beats, three gift items and ~11 KB of authored prose can never fire; the five §VM-01-G2b-FU beats are the same defect and should be fixed in the same call.** 🔴 **The mechanism:** `` `  S_story.actNumber = node.act || 1;@34556` `` is the **only** writer of that field — `actNumber` is *the `act` of the tile you are standing on*, not campaign progress. `` `const ACT8_FAREWELL_BEATS = {@26885` `` fires from `` `function _renderNpcCard(key, container) {@23684` `` on `(S_story.actNumber || 1) === 8`, and `_renderNpcCard` is called from exactly one place — `` `      keys.forEach(k => _renderNpcCard(k, npcRowDiv));@35162` `` — downstream of the assignment in the same render. The six NPCs are pinned to their `BIRKA_NPC_PROFILES` node, and **`LHR` · `TLL` · `MHQ` · `LLA` · `HKG` are all `act:1`**. **`act:8` occurs exactly once in the whole game — `TLS`, the Convergence platform** — where none of them stands. **Lost:** Yael's witness network (*"Go do the other kind."*), Brynn's *"I made too much bread today — I always do when I'm worried,"* Quill's settled ledger, Pachelbel's sketch, Weckmann's tincture, Auros's warning — plus **Brynn's Loaf** (heal 8), **Pachelbel's Sketch** (readable) and the **Champion's Tincture** (advantage on next attack), none of which any save has ever held. `birka-quill-couperin-farewell` is dead **twice** — once as a §VM-01-G2b-FU hook at `LLA`, once through `_renderNpcCard` — so the same scene has two dead paths and no live one.
> **Blast radius, quantified.** Sixteen sites compare `actNumber` against a threshold ≥ 2. **Seven sit inside a node-pinned block and not one is a real gate:** six are pinned to `act:1` nodes and are permanently FALSE (the five G2b-FU beats + `_renderNpcCard`), the seventh is pinned to `NUE` (`act:6`) and is permanently TRUE — vestigial, the class §VM-01-G3 already retired from the quest stanzas. The other nine are unpinned and read *"am I standing somewhere act-N-or-later,"* a proxy for progress that merely correlates. **`index.md` and `mechanics.md` both describe the field correctly** (*"derived from current node's `act` field"*) — this is a rare case where better docs would not have helped, because the content was authored against the name, not the definition.
> **Correction to §VM-01-G2b-FU:** its *"`brynnKeeperStoryTold`'s only writer is the dead Beat 1"* is literally wrong — there is a **second** writer at `` `        S_story.brynnKeeperStoryTold = true;@26896` ``, inside `ACT8_FAREWELL_BEATS.brynn.text()`. It is dead by the same mechanism, so the cascade holds, but the second writer is its own hazard: **a text getter that mutates persisted save state** — a display path with a side effect, invisible to anyone grepping logic for assignments.
> **THE CALL (one, covering all eleven).** Something must replace the act leg, and the right signal is a design decision, not a guess: (a) **a real campaign-progress counter** (`S_story.storyAct`, advanced by act-boundary quest completions) — fixes all sixteen sites at once and is the only option that makes `actNumber >= N` mean what every author assumed; (b) **re-gate the eleven beats individually** on shards held / a named quest completion / a mission bit / a day threshold — no new field, but eleven separate content decisions; (c) **fire the homecoming from the doorstep** — gate the six farewells on *arriving at `TLS`* having ≥ 1 favour, which preserves the fiction (a last round of goodbyes) at the cost of the "return home" staging. **(a) recommended** — the other fifteen sites are the argument. **Docs already annotated, not rewritten** (§AUDIT-03m-FU): `world.md` § *The Homecoming* and `story.md` § *Act VIII Farewell Beats* both flipped from ✅ Implemented to ⚠️ SHIPPED BUT UNREACHABLE, with the mechanism stated. **`world.md` also named a flag that does not exist** — `act8FarewellWeckmann`, 0 occurrences; the live flag is `act8FarewellCrov` (doc used the character name, code uses the profile key) — corrected in the annotation. **Not yet pinned by a test:** `uqf-npc-row-hooks.test.js` pins the five G2b-FU gates; the six Act VIII beats have no pin, so the fix must add one.
> **Provenance:** §DOC-02db, findings F1–F4. **Supersedes nothing** — §VM-01-G2b-FU stays open and should be closed by this row's call.

### §DX-02en — the game's best ending is one point out of reach, and has been since the first commit (NEW 2026-08-22 during §DOC-02cx, 🟡 ONE DESIGN CALL, two thresholds)

- [ ] **§DX-02en — `_curseScore()`'s floor is −5; "Covenant Keeper" needs ≤ −6.** 🟡 **the arithmetic is settled; the call is which number moves.** `function _curseScore()@28193` returns `(startedNotReturned * 3) + (neverStarted * 1) - (allComplete ? 5 : 0)`, so the reachable set over all 231 partitions of the 20 EB codes is **`{ −5 } ∪ [1, 60]`** — 0 is unreachable too. Two consumers ask for ≤ −6: the top rung of `const COVENANT_STANDING_LABELS = [@27358` (*"The people you helped are the reason this works."*) and `const _isTrue = missionDone && curse <= -6@28231`, the "Covenant Keeper (True)" ending. **Neither has ever fired.** Proven in Chromium through the engine's own `_curseScore()`/`_covenantStanding()`: label hits across 231 states are Wanderer 186 · Watcher 30 · Keeper 14 · Warden 1 · **Covenant Keeper 0**. The perfect 20-of-20 run — the hardest ask in the game — renders `Warden · "You carry the work with you. It shows."` on the character sheet.
> **It was never right.** Both constructs are **byte-identical at `32c10c5`** (the repository's initial commit, 2026-05-24) **and at HEAD**. This is not drift; the number was wrong on the day it was written and 90 days of edits went around it.
> **THE CALL — which number moves?** (a) **Move both thresholds to `-5` (recommended):** two characters, and the run that earns the label gets the label. Side effect: the `Warden` bracket then holds nothing, because −5 is its only member — so `Warden` should either take `maxScore: 0` unchanged (it becomes decorative) or be retired. (b) **Deepen the bonus** — `allComplete ? 10 : 0` → floor −10, which makes `≤ −6` live *and* keeps a gap between Covenant Keeper and Warden, but changes the meaning of every score in the −10..0 band and needs a second look at the ≤ 0 naming-ceremony gate. (c) **Add a second negative term** (e.g. −1 per NPC at Dear Friend, capped) so the standing reads the relationship ledger the ending is *about*, not only the EB ledger — the most faithful to the design intent and the most work. **(a) is the honest default**; (c) is the one worth an hour if the standing is meant to mean what §VII says it means.
> **The second half of the row, same fix window — §VII's payoff is not shipped.** The report's argument is *"the player sees 'Warden' in their sheet for 40 hours, then Sweelinck says 'you were a Warden' in the final scene."* **No ending branch names the player's standing.** The word `Warden` is spoken in exactly one branch — the Cursed Seal echo's *"the Warden who arrived here was capable, dedicated, and efficient"* — and that branch fires at `curse >= 15`, where the sheet reads **Wanderer**. One interpolation into the four `endingEl.textContent` strings closes it.
> **Verify:** there is **no test anywhere** touching `_curseScore`, `_covenantStanding`, `_buildEpilogueScroll` or the perk tree. Add `src/tests/integration/dx02en-covenant-standing.test.js` pinning (i) the reachable bounds of `_curseScore()` and (ii) that **every** `COVENANT_STANDING_LABELS` entry is returned by at least one reachable state. Assertion (ii) would have failed on 2026-05-24.
> **Provenance:** §DOC-02cx, finding F1 + F2, `docs/lab-reports/lab-report-endings-and-echoes.md` §IV/§V.

### §DX-02ee — a zero written to mean "off" is read through `|| 1` and means "default" (NEW 2026-08-22 during §DOC-02ct, 🟡 42 quests change behaviour, ONE DESIGN CALL)

- [ ] **§DX-02ee — `retryGateDays: 0` is unreachable: the only consumer coerces it to the one-day default, so 42 quests silently carry a cooldown their authors wrote `0` to remove.** 🟡 **the fix changes live behaviour for 42 quests, so it needs a call.** The retry cooldown is decided in one place — `S_story.day < att.lastDay + (q.retryGateDays || 1)@6811`, read by `function _ceremoRetryBlocked(questId)@6806`, which the quest card consults to disable the roll button after a failed skill check. `0` is falsy, so `(0 || 1)` is `1`: writing `retryGateDays: 0` is byte-for-byte equivalent to omitting the field. **Measured at HEAD:** `grep -o "retryGateDays: *0"` returns **42** entries (22 carry `1`), and in Chromium a forced failure of `quest_void_tide_42@21315` on day 42 leaves `_ceremoRetryBlocked` **true** the same day and **false** on day 43 — the exact behaviour of the default the author was trying to override.
> **Where it was found, and why it is not obviously a bug.** §BOARD-01-FU8 gave all three Void-tide hunts `retryable:true, retryGateDays:0`, intending an immediate retry. What shipped is a one-day gate — and because the doom clock advances at exactly one site, `S_story.day = Math.min(49, S_story.day + 1)@36253` inside `function storyConfirmSleep()@36227`, a retry costs one sleep and therefore one day of a closing window. **For that increment the accident is a better threat model than the design**, which is precisely why the row needs a decision rather than a patch.
> **DESIGN CALL — honour the zero, or delete it.** (a) **Honour it:** change `||` to `??`. One character-pair, and it makes **42 quests** newly retryable on the day they fail — a real difficulty change across the corpus, unreviewed. (b) **Delete the field** from all 42 entries and keep the coercion, so the data says what the engine does; zero behaviour change, and `retryGateDays` keeps exactly one meaning. (c) **Honour it selectively** — `??` plus an audit that re-authors any of the 42 whose difficulty depended on the accidental gate. **Recommend (b)**, on the §DX-02c annotate-do-not-erase principle applied to data: a field that cannot express one of its two written values is worse than no field. **Verify:** after (b), `grep -c "retryGateDays: *0"` is 0 · `warrants-board.test.js` 25/25 and `quest-runtime-uqf` 303/303 unchanged · a browser probe of any former-zero quest still blocks the same-day retry.

---

## Track records (Phase 1)

### §AUDIT-03 — Quest-data appended-field rot (NEW 2026-07-28, surfaced by §VM-01-G3)

> §VM-01-G3 found that the bulk audit waves `a721254` ("80+ passText/failText/activateNode/desc/hint fields patched") and `ea02faf` ("wire npc field on all 985 quests") APPENDED fields to entries that already declared them — JS last-key-wins silently flipped authored values — and `710bb75` ("Fix 8 activateNode errors: SF→LCY, CQ→CDG, FR→AMS") remapped at least two dead codes to the WRONG target. G3 fixed the ~20 quests in its five arcs; these rows are the corpus-wide sweeps. All are data-only audits — grep/parse first, fix through `./api.sh` where expressible (⚠️ Hazard #1 for hand-edits).

- [x] **§AUDIT-03a ✅ SHIPPED 2026-07-28 — `check:dupkeys` is `check:walk` gate #11; the 2 corpus survivors fixed.** New `src/scripts/check-dupkeys.js`: string/template/comment-safe token scanner over all 11 WORLDBUILDER sections (~4,800 entries), stack-of-maps per object literal so a duplicate key at ANY nesting is caught while sibling array elements/function-body braces never false-positive; `--selftest` proves clean-pass + planted-dup-caught. **Corpus findings: exactly 2 survivors** (beyond G3's arcs): `quest_la_riva_01` + `quest_no_fishing_sign`, both authored `activateNode:null` + appended `activateNode:"CDG"` — both quests are CODE-activated (la_riva_01 by the Cat-King-defeat handler with Kenickie's staged monologue; no_fishing_sign by the coupon pull-tab), so the shadowing append mass-activated them at CDG, bypassing their staging. Fixed per the G3 pattern: appended line removed, authored `null` kept (hand-edit — dup-removal isn't expressible via PUT; server confirmed stopped, Hazard #1). Their `npc:"long_john_silver_sen"` stamps left for §AUDIT-03b (scope fence). **Wired into `package.json` `check:walk` (selftest + audit), which is the single CI step in walk-invariants.yml — an append can never silently flip an authored value again.** Evidence: `check:walk` all 11 gates green; `quest-runtime-uqf` 303/303; prompt.md §7 gate count synced 10→11. Note: MONSTER_POOL count reads 796 because that section legitimately holds both `MONSTER_POOL` + `MONSTER_DROPS` objects (2×398 — both audited); ITEM_DB is genuinely empty; FISH_POOL is array-rooted (fields audited at element level, entry count reads 0 — cosmetic).
- [x] **§AUDIT-03b ✅ SHIPPED 2026-07-29 — all 203 mis-stamped quests re-anchored to their real NPC; the vocabulary and the field patcher fixed underneath.** `ea02faf` had bulk-defaulted `npc:"long_john_silver_sen"` onto **203** quests. **Established first (step 1, prior session): `q.npc` is authoring metadata only** — the game client has ZERO quest-level `.npc` reads, so no gameplay risk and no golden-state diff was needed; consumers are worldbuilder display, `_questsByNpc`, `advise` (unresolvable key ⇒ *warning*), `audit` (**missing** field ⇒ *error* — the pressure that caused the bulk default), and the NPC delete-guards.
  **Result: 203/203 re-anchored correctly · exactly the 7 genuine Silver quests kept (`sen_c1a2/a3`, `sen_c4a1/a2/a3`, `sen_c7a1/a2` — every scene is built on him) · 196 changed · 0 unresolved · 0 collateral.**
  **Three defects found and fixed on the way (the row's own premises included):**
  1. **The accepted vocabulary was too narrow.** `_npcOk` (duplicated in two `wbapi-server.js` closures) and `advise` accepted only BIRKA_NPC + normalized `NODE_MAP` inline `npc` — so real, rendered speakers living in `NPC_DIALOGUES` (`jimmy`/`solvak`/`yva`/`rennau`/`kenickie`/`isolde_voss`/`benedikt_rasp`/`sandy_cat`) and the 20 `EB_NPC_DIALOGUE` quest-givers would all have advise-warned. Replaced with **one shared `WBAPI.npcKeyOk` / `npcKeyVocab()` in `src/js/wbapi-core.js`** over four registries (BIRKA_NPC · NODE_MAP inline · NPC_DIALOGUES · EB_NPC_DIALOGUE), both server closures now delegate to it, and `EB_NPC_DIALOGUE` is parsed for the first time (it lives OUTSIDE the WORLDBUILDER markers, so it is read off the raw source). **Vocabulary 270 → 297 keys**; a bogus key is still rejected.
  2. **`patchStringField` was not depth-aware — a silent no-op class.** The first batch reported `patched:196, errors:[]` but **6 writes never landed**: `editField('quest', id, 'npc', …)` matched the FIRST `npc:` token anywhere in the entry, which for `quest_wm_02/wm_03/tl_01/tl_03/vs_01/vs_02` is the **nested** `{ kind:'favor', npc:'…' }` inside `onComplete`. Those six happened to have a target key equal to the nested value, so the write was value-preserving (quote style only) — but any quest where they differed would have had its **favor bit rewritten instead**. Fixed with a new `firstTopLevelMatch(body, re)` helper: a single string- and comment-aware pass builds a depth map, and only a match at brace depth 0 of the entry body can be chosen (comment-awareness per the §AUDIT-03f lesson). Re-ran the 6 → all correct, favor bits untouched. **Proof of zero collateral across the whole corpus: all 16 `kind:'favor', npc:` values byte-identical before vs after (git-diff extraction), and the game-file diff is exactly 196 insertions / 196 deletions.**
  3. **The row's `hunt2 → tessie` derivation was wrong.** Tessie is not an `EB_NPC_DIALOGUE` giver — enumerating the object gives 20 givers and she is absent; she exists only in `quest_hunt2_*` prose. Re-anchored `hunt2_01–04` to **`the_elder_fisherwoman`**, the §BOARD-01-FU6 referral source that hands the WRO thread off (the `hunt_01` narrative that names Tessie's road).
  **The ~12 OPEN per-quest calls were resolved by reading each quest's text** (recorded in the mapping script): `spark_01/02/05`→`magistra_elara_muffat` (LCY anchor; Inspector Wren-Pembury/"Aldous" has no key) · `spark2_01–05`→`mairén_fionn` (DNF anchor; Commissioner Fehn has no key) · `wis_00/06/07`→`solvak` and `wis_04`→`warlord_kael_mordus` and `alch_02`→`elder_fionn` (Roen has no key anywhere — §AUDIT-03i) · `wis_03`→`captain_vera_keel` (GCI's own NPC; the text says "Keel talked about the commission") · `vs_warden`→`solvak` · `va_01–04`→`benedikt_rasp` (va_04's text names him outright) · `ng_01–03`→`city_guard_captain` (LHR anchor) · `ca_01`→`magistra_elara_muffat` · BOO trio + `no_fishing_sign`→`the_fisherman` · `shore_01`→`the_tide_reader` and `sunken_02`/`depth_01`→`the_stone_inscription` (chain-sibling consistency) · `cat_06`→`jimmy_two-tails` ("Tommy" has no key) · `solm_01`→`proprietor_dusk` (the Map Shop scene) · `mol001_act2`→`bolli_bollason` (his household's men hold the ford) and `mol001_act3`→`gudrun_osvifr` (Helgi Osvifsson unkeyed → the chain namesake, = act1's own stamp). The anchoring rule is now written into `quest.md`.
  **API-first honored:** the purpose-built `POST /api/batch/npc` endpoint existed but had no wrapper, so **`./api.sh batch-npc <updates.json>`** was added to `src/api/wb.js` (+ both help blocks) — one parse and one save for the whole batch instead of 196 full-file rewrites. Hazard #1 respected throughout: the `src/js/*.js` edits were made with the server stopped, then the server was restarted on a fresh PID before each write session, and the `_monsterLevel` signature survived both saves.
  **Evidence:** `check:walk` **11/11 green** (exit 0, incl. `check:dupkeys` + the §AUDIT-03f parse-parity phase) · `quest-runtime-uqf` + `npc-card-map` **325 passed** · new permanent `src/tests/integration/audit03b-npc-anchor.test.js` **4/4** (the 7-keep corpus assertion · the only-10-tolerated-unresolvable-keys pin · the 4-registry `npcKeyOk` · the depth-aware-patcher regression) · `node --check` clean on `wbapi-core.js`/`wbapi-server.js`/`wb.js` · quest count **2,853** unchanged. Docs synced: `quest.md` gained an "`npc` field — authoring metadata, not gameplay" section (the four registries + the anchoring rule + the `batch-npc` recipe). Side findings logged as **§AUDIT-03g/h/i** below rather than fixed inline.
- [x] **§AUDIT-03c ✅ SHIPPED 2026-07-29 — the `710bb75` remap audit closes: 6 of 8 remaps were right, 5 of them no longer exist, and the table that caused the bug is the real finding.** **Verdict** (§NPC-01-SF4 method — each quest's own prose + `waypointNode` + the hook/panel that renders its buttons): **`CQ` = `CDG`** (The Cat Quarter) and **`FR` = `AMS`** (Fishmonger's Row) are both correct, each corroborated three ways — the node's own `label` + inline `npc`, the quest's `waypointNode`, and the engine's own comments (`_nodeHookLaRivaRow` is headed *"FR node render"* and guards on `node.code === 'AMS'`; the drop handler is headed *"FR kill tracking"* on `pb.nodeCode === 'AMS'`; the boss-button panel is headed *"CQ node render"* on `node.code === 'CDG'`). Only **`SF → LCY` was wrong** — already fixed by §VM-01-G3 (`SF` = the Storefront = `STN`). **The prior session's open question — "should one arc sit at two nodes?" — is answered and moot:** the La Riva arc is legitimately **bi-nodal** (Kenickie sends you east from `CDG` · Connie + Aldo at `AMS` · the account book goes back to Kenickie at `CDG`, hence `la_riva_03`'s `waypointNode:'CDG'`), and **5 of the 8 remapped lines no longer exist** — they were the appended duplicates §AUDIT-03a / §VM-01-G3 removed, so all three `la_riva` quests are `activateNode:null`, staged by code (`01` by the `CQ_KING` defeat handler, `02`/`03` by the AMS hooks). **The audit's real product — a corpus scan: 0 of 2,853 quests name a node that does not exist**, and it covered **`waypointNode`, which the original audit never checked at all.** **One data fix:** `quest_cat_06` was anchored to `jimmy_two-tails` (CDG's inline-name slug) while its three siblings use the `NPC_DIALOGUES` profile key `jimmy` — one character filed under two `_questsByNpc` headings (§AUDIT-03k); collapsed to `jimmy` via an in-process `WBAPI.editField` write (the §TIDE-01-A path, byte-identical to the server's PUT; no daemon, per the no-background-processes rule). **Most likely source of the whole class, measured and filed as §AUDIT-03l: `maps.md`'s LEGEND table — the doc an author reads to look up a node code — has 73 of 76 rows naming codes that do not exist** (it predates the airport-code naming; its `Node #` column is still correct and is the recovery key). The circumstantial case is strong: those three codes appear nowhere else, and `quest_tl_01` carries `activateNode:"SF"` beside a live `waypointNode:'STN'` **for the same node** — one field read off the table, the other off `NODE_MAP`. Neutralized today with a hazard banner above the table; the remap is its own increment. **Evidence:** golden parse diff across all 7 collections = **exactly 1 entry diff, `quest_cat_06`, `fields=npc` alone**, 0 lost / 0 gained, still 2,853 quests; new permanent `src/tests/integration/audit03c-node-refs.test.js` **4/4** (both fields resolve · the 3 dead codes stay dead + the labels that identify them · the 8 audited quests hold their re-derived targets · cat_06 on the profile key), with a **negative control** (re-planting `activateNode:"SF"` + `waypointNode:"CQ"` in memory — both caught, so the guard is not vacuous); `check:walk` **11/11 exit 0**; **367 Playwright green, exit 0 both runs** (`quest-runtime-uqf` + panels/hooks/activation 322/322 · the audit/NPC set 45/45). No §7½ (nothing player-visible — `quest.npc` has zero quest-level client reads per §AUDIT-03b, and no node reference moved). Docs: `quest.md` (both arc sections + the `npc` section's six measured alias pairs), `world.md` §XIX, `maps.md` banner.
- [ ] **§AUDIT-03d — imported multi-act chains mass-activate per node (the ath05 class).** The imported chain corpus (ath/bgw/cai/ist/sen/waw/blq…, dozens of chains) uses `gate:{}` + per-act `activateNode`, so EVERY act at a node activates on first arrival (NUE fresh lists ~25 `ist_*`/`bgw_*`/`cai_*` acts at once; VS now lists `ath05_act3`+`act4` — pre-existing live behavior G3 made visible at VS, deliberately left). If staging is wanted, the acts' own `mission_bit` flags (`ath05a1`…) are ready-made `gate:{flags:[…]}` chain links — a mechanical, per-chain sweep with the §VM-01-G3 golden-diff method. Needs a design call first: the acts are also written to work as standalone vignettes.
- [x] **§AUDIT-03f ✅ SHIPPED 2026-07-28 `6a69087` — `removeFns` made comment-aware; both quests restored to the parse; parse-parity now gate-fenced.** Root cause was NOT the quests' own `onComplete` arrays: `removeFns` processed comments as code, and the preceding §ARCH-01 migration comments' arrow-fn examples (`activateCond:()=>true` / `completeFn:()=>…`) triggered the fn-stripper, which swallowed to the next depth-0 comma — the entire following entry — leaving `: null,` on a `//` line (valid JS → silent drop). Fix: comments copy through verbatim in `removeFns`' main loop + both fn-body scanners (`src/js/wbapi-core.js`). Golden diff old↔new parse: +2 restored, 0 lost, 4 entries regain `fn:null`/`activateCond:null` keys the old scanner ate. Regression test as specified: `check:dupkeys` phase 2 (§AUDIT-03f parse-parity) asserts textual-keys == parsed-keys across 9 object sections, selftest incl. the comment-trap + a simulated drop. Evidence: 2851→2853 parsed · `check:walk` 11/11 · `npm run stats` 2,853 · `./api.sh get quest quest_sea_01`/`quest_sb_01` both 200. Original row follows.
  **§AUDIT-03f (original) — `quest_sea_01` + `quest_sb_01` are INVISIBLE to the WBAPI parse (found 2026-07-28 while building §AUDIT-03a).** Cross-checking textual depth-1 QUEST_DB keys (2,853) against `WBAPI.questDb` (2,851) found exactly these two dropped. Measured: `_splitQuestBlocks` DOES find both → the loss is inside `parseSanitized(qSrc,'QUEST_DB')` (the removeFns/sanitize-eval path mangles their entries — both carry `onComplete:[…]` op arrays; §SPARK-01 Wave-1m migrations). **Consequence:** both quests are un-listable and un-editable through the entire WBAPI surface (`./api.sh`, server PUTs, stats undercounts by 2) — the API-first policy silently can't reach them. Fix in `src/js/wbapi-core.js` (comment-aware parsing is the known hazard class), add a regression test asserting textual-count == parsed-count (the §AUDIT-03a scanner provides the textual side), and re-run `npm run stats`. **Risk:** medium (parser core; restart server after edits per patchStringField lesson).
- [x] **§AUDIT-03e ✅ SHIPPED 2026-07-29 — the `code = key` backfill; 287 nodes stop sharing one `undefined` state slot, and the dead-activation zone is opened selectively.** The row's proposed fix was right and its premise was **too narrow**: quest activation was only one of ten consumers. Every per-node state map (`visited`, `defeatedBattles`, `sleptAtNodes`, `junctionsSeen`, `pendingBattle.nodeCode` → the corpse record) and every `NPC_DIALOGUE` / `VENDOR_NODES` / `NODE_PANELS` / `CORELLI_APPEARANCES` / `NODE_COORDS` / `_bfsGridPath` / `_boardSeed` lookup keys on `node.code`, so at the 287 code-less nodes they **all shared the single `undefined` slot**. Proved live against HEAD before any edit: a second code-less node paid **0 exploration XP** (`S_story.visited` held one key, `"undefined"`), all five other code-less battles read as **already defeated** after one win, and **all five §KG NPC dialogues failed to resolve** — Volkov/Roshkova/Grimka/Iosif/Lena had never been talkable in live play. The Rest card read a constant wrong *"The First Inn (TLL) — 166 moves"* at every one of the 287 (`_bfsGridPath(undefined,…)` can't start). **Shipped:** one normalisation line after the `NODE_MAP:END` marker (`code = key`; the data section keeps its 129 authored fields — this is **not** 287 new fields), plus the `NODE_CODE_BACKFILLED` set captured immediately before it. **Safety audit that made it a one-liner:** no entry in the corpus has `code !== key` (so the 129 authored ones are untouched by construction); of the 50 distinct `node.code === 'LITERAL'` comparisons exactly **one** targets a code-less node (the BK approach panel — a dead §D01-01 panel that now renders, plus BK's dead vendor and 1 of Corelli's 5 scheduled stops); `'XX'` is a comment; the four parity fences read node **keys** via `world.cellCodes`, not `NODE_MAP.code`, so they never moved; and no CI script depends on the field. **The §AUDIT-03d seam:** 2,283 quests named a backfilled node, **990 with a vacuous `gate:{}`** — WM alone would have fired **151** in a single arrival, act4/act5 finales ahead of their own act1 (today's worst LIVE case is NUE at 31). Rather than default that design call, activation is unblocked by a structural line — **vacuous `gate:{}` AND no `completion:` clause ⇒ held** (a free-floating vignette with no objective, which is exactly 03d's corpus); anything with a real gate or a real completion activates. Measured result: **5** quests newly fire on arrival across all 287 nodes (the §KG and §MATH-01 chain heads + `mq_4` at BK — all authored, all previously dead), worst node **1**; 1,295 more now stage properly behind their own gates. **Evidence:** a 12-combo pre/post golden harness (temp, deleted) — the 4 coded controls **byte-identical**, quest counts **0→0 at all 12** (incl. WM), and every single diff a genuine fix (Nearest-Inn corrected at 8/8, Talk enabled at the 4 §KG NPC nodes, TVR's board slate re-seeded off the shared `"undefined|day"` seed); `check:walk` **11/11 exit 0**; **~450 Playwright green** (`quest-runtime-uqf` 336/336 · new `audit03e-node-code.test.js` 7/7 · activation/panels/hooks/npc-card-map/kg/death/nav/rng/xp); §7½ screenshot of Roshkova speaking at KMS for the first time, 0 page errors. **Two tests updated, neither weakened:** `npc-card-map` asserted the defect itself as §NPC-01-B's premise (*"many derived nodes omit node.code"*) — re-expressed against `NODE_CODE_BACKFILLED` plus a new assertion that `code` now resolves at every derived node; the §MATH-01 test's hand-patched `{...NODE_MAP.EHZ, code:'EHZ'}` now exercises the real path. **Docs:** maps.md §NODE_MAP (the backfill note + authoring rule + 422→416), quest.md (a new "Arrival activation and the §AUDIT-03e seam" section with the hold/activate table), world.md §KG (the shipped-dead warning), prompt.md §7 (second baseline red). Side findings filed, **not** fixed inline: **§AUDIT-03e-FU** (what the ending map should show), **§DX-02e** (the pre-existing kg-zones road red), **§AUDIT-03j** (7 permanently-dead `JUNCTION_VIGNETTES` + 1 `NPC_DIALOGUE` key + Corelli's LDE stop naming nodes that do not exist). **Next: §AUDIT-03d is now the gating row — its staging design releases 983 held quests.** Original row follows.
  **§AUDIT-03e (original) — code-less `NODE_MAP` entries as a dead-activation zone.** 291 of 418 entries omit `code`; `storyCheckQuests` keys activation on `node.code`, so ANY quest whose `activateNode` points at a code-less node silently never auto-activates — exactly how the whole §XX Visby arc + §XIX Vonn path died (G3 fixed VS/TL). Sweep: cross-reference every `activateNode` value against entries lacking `code`, add the missing `code` fields (or backfill `code` = key at parse time, one line in the loader — likely the better fix; check `_getNodeMapColor`-class consumers first).
  **⟶ BLAST RADIUS MEASURED 2026-07-29 (§AUDIT-03b session, premise re-confirmed live, no edits):** **287 of 416** `NODE_MAP` entries carry no `code:` field, and **2,285 of 2,853 quests** name one of them in `activateNode`. The mechanism is confirmed end-to-end: `storyRender` passes `NODE_MAP[S_story.currentCode]`, `storyCheckQuests` → `_uqfActivateAtNode(node)` → `_questsByNode(node.code)` — so at a code-less node `node.code` is `undefined` and the arrival-activation lookup returns nothing. Spot-checked code-less nodes include **BK, SPB, KMS, ZVD, FBR** (the whole §KG corridor) while LHR/VS/TL/CDG do carry `code`. ⚠️ **2,285 is an upper bound on the damage, not a count of dead quests** — many of those quests reach `active` by another path (the `unlock` opcode, a code handler, a chain sibling), and that is exactly what the sweep must separate. Confirm the `code = key` backfill at load is safe for every `node.code` consumer (there are ~40 `node.code === 'XX'` comparisons in the migrated `NODE_HOOKS`/`NODE_PANELS` regions alone) before applying it, then golden-diff activation per §VM-01-G3's method.
- [ ] **§AUDIT-03e-FU — what should the ending map (`_renderFinalMap`) actually warm? (found 2026-07-29 during §AUDIT-03e).** The Act-close overlay built its "warmth grid" from `Object.keys(NODE_MAP).filter(k => NODE_MAP[k].code)` — a well-formedness guard that *accidentally* selected exactly the 129 entries carrying an authored `code:`. After the backfill that filter would widen to all 416 and push the 9-column / 28px grid from ~15 rows to ~47, far past the overlay's viewport, so §AUDIT-03e **pinned it to the historical 129** (`!NODE_CODE_BACKFILLED.has(k)`) and pinned that in the source guard test. That set is an accident, not a design: it is *"the nodes an author happened to type a redundant field on."* Decide what the ending should show — every visited node? every node in the acts the player reached? a curated 81-cell 9×9? — then replace the pin with the real predicate and re-check the grid geometry. **Needs a design/aesthetic call.** **Risk:** none (cosmetic, one function, single-use overlay).
- [x] **§AUDIT-03j ✅ SHIPPED 2026-07-31 `c5f9079` — the engine's own node references resolve now, and gate #13 keeps them that way. The row named 9 dead keys in 3 registries; live measurement found 42 across 5.** Existing-Work-First: tree clean at `4c79868`, WBAPI server stopped, a 2nd claude process present but the game file untouched by it; the row's premise re-measured live before any edit. **It held, and understated the scope ~5×.** Every one of the 42 was written in the retired 26×16 codes, and every one failed *silently* — a missed lookup renders nothing. **`NODE_NPC_KEYS` 21 of 26 rows** (the row never named this registry) → favor map-tinting and farewells answered at **LHR alone**; **`NPC_FAREWELLS` all 12 `<from>_to_<to>` route keys** (never named either) → only each NPC's `default` line could ever fire; `JUNCTION_VIGNETTES` 7 of 8; `CORELLI_APPEARANCES` stop 2 of 5; `NPC_DIALOGUE`'s `MS_SPARK`. **33 of the 42 were mechanically derivable and doubly corroborated** — §AUDIT-03l's LEGACY CODE MAP resolves the codes, and each `NODE_NPC_KEYS` EB row *encodes its own legacy code in its value* (`EH:'eh_npc'`, and `EH`→`INV`). **Two discoveries changed the shape of the fix.** (1) **The table's OTHER column was dead too:** its npc keys were the profiles' SURNAMES — `couperin` = Bard Tomas **Couperin** = `quill`, `weckmann` = Pit Master **Weckmann** = `crov`, `bruhns` = Commander Seraphine **Bruhns** = `auros` — proved independently by the corpus holding exactly **13 favor-granting npc keys**, which include `quill`/`crov`/`auros` and never the surnames. A code-only remap would have shipped the half-true kind §AUDIT-03l warns about, so both columns were fixed. (2) **The farewell mechanism is alive:** `S_story.currentCode` advances ONLY on node arrival, so a route key means *last node you stood on → next node you reach*, at any walking distance — it never needed the cell adjacency the live geo world no longer provides. **Shipped:** `NODE_NPC_KEYS` → 5 rows that resolve on both columns (the 20 `E*:'e?_npc'` placeholders removed as a **proven no-op** — `_npcFavor('eh_npc')` was always 0 and EB nodes are tinted by the separate live `ebReturnDone` branch; `crane` named no NPC anywhere and went with its dead code); all 12 route keys + 3 owner keys live, with **one content override** (`CI_to_SQ` says *"Long road to Tilbury"* but SQ=NUE=Weimar → pointed at Tilbury's `LCY`, the §AUDIT-03c rule that the prose is the authority); the 7 vignettes rehomed onto live road nodes **derived from each scene's own text** (the Cartographer tears off *"the coastline"* → `J13` The Western Sea Road; the child's toy → `BNX` The Eastern Bend, the bend Tessie's surviving WRO line warns about; *"Paid in full. —S."* → `BLW` a toll gate); Corelli's stop 2 → `WRO`; and `MS_SPARK` folded into the SEN slot behind `_senHoldSpeaker()` so **both** Tilbury Star voices reach the player. **One dependent bug fixed, not detoured:** `storyShowNpc` read only `d.quote`, so all **41** `quoteFn` entries rendered the literal string *"undefined"* in the d-pad NPC modal — required for the SEN merge to be correct, and it revives 40 other speakers in that path. **The gate — `src/scripts/check-noderegs.js`, `check:walk` #13 — is built around the blind spot that let this rot survive:** "flag tables where most keys look like node codes" cannot see a table that is *entirely* dead, which is exactly what `NODE_NPC_KEYS` (5/26) and `JUNCTION_VIGNETTES` (1/8) had become, so classification is **explicit** — every codeish top-level table must be listed in `NODE_KEYED` or in `NOT_NODE_KEYED` **with a reason**, and an unlisted one fails. 4 phases (registries · classification · fields · routes). **Evidence:** `check:walk` **13/13 exit 0**; the gate's 4-phase `--selftest` catches a planted defect in each phase; **negative control with the REAL historical defects** — replanting `LJU` and `nodeCode:'LDE'` drove it to exit 1 naming both, game file restored byte-identical; new `audit03j-node-registries.test.js` **8/8** (incl. the two *behavioural* claims the repair rests on — that dropping the EB placeholders changes no colour, and that the SEN predicate switches on the spark arc and nothing else); **352 green** across `quest-runtime-uqf` + `npc-card-map` + `uqf-node-panels`/`-hooks` + `audit03c`/`03e`/`03l`; full Playwright **725 passed / 5 failed** = the documented four in `worldbuilder-crud-arrays` + the known `multiplayer-presence` flake (**7/7 alone**, and I ran `--retries=0`). **§7½ DONE — this one IS player-visible:** temp harness (deleted) screenshotted Old Faeron speaking at DAR, Corelli's button at WRO, and Brannick in the d-pad modal that used to say *undefined*, plus `_getFarewell`/`_getNodeMapColor` answering at 5 homes instead of 1; **0 page errors**. Hazard #1 respected (`:1367` verified stopped). **Docs:** `story.md` (the 8-row vignette derivation table + the `NPC_FAREWELLS`-vs-`ACT8_FAREWELL_BEATS` distinction + the one-slot-per-node rule), `world.md` (the `NODE_NPC_KEYS` finding + Corelli's live stop list), `prompt.md` §7 (12→13 gates + why the classification is explicit), `index.md`. **Side finding filed, not swept: §AUDIT-03n** — the same rot in the npc-key dimension. Original row: **§AUDIT-03j — registry entries naming nodes that do not exist at all (found 2026-07-29 during §AUDIT-03e).** Distinct from the code-less class 03e fixed — these keys have **no `NODE_MAP` entry whatsoever**, so they are permanently dead and the backfill cannot revive them: `JUNCTION_VIGNETTES` holds **7** (`LJU`, `SPU`, `TGD`, `TOS`, `MMX`, `ANC`, `LDE`) of its 8 keys pointing at nothing (only one resolves); `NPC_DIALOGUE` holds `MS_SPARK`; and `CORELLI_APPEARANCES` schedules a stop at `LDE` — so **2 of Corelli's 5 appearances never fire** (BK was the other, now revived by 03e). Either build the nodes, remap each entry to the node its content actually describes (the §NPC-01-SF4 method), or delete the dead entries — and consider a `check:walk` gate asserting every node-keyed registry key resolves in `NODE_MAP`, which would have caught all nine. **Risk:** low (dead data; a remap is content-visible, so decide per entry).
- [x] **§AUDIT-03g ✅ SHIPPED 2026-07-29 — all 68 unanchored quests re-anchored by derivation; `./api.sh audit` reports `errors: 0`.** The standing audit error that provoked the `ea02faf` bulk-default is gone, and it was closed **without** a bulk default: each of the 68 was derived from evidence, one family at a time, and the derivation is recorded in `quest.md` + pinned by a test. **The load-bearing discovery: a §D02 Epic-Battleground *approach arc* inherits its battleground's giver** — `quest_ea_primary`/`_return` at RAI already carry `captain_selene_draketide` and `quest_eg_primary` at BK already carries `warlord_kael_mordus`, so `quest_d0201_*`/`d0209_*` (10) and `d0205_*` (5) resolve from the corpus rather than from taste. The rest: **`auros` ×24** (Commander Seraphine Bruhns — the profile key behind the `NODE_MAP` inline npc of *both* HKG and TLS, the named figure of the Workshop schematics, and the corpus anchor of the Warrant thread via `quest_signal_01`) — `d0207_*`, `d0204_*`, `d0210_*`, `d0206_*`, `quest_void_below`, the 3 §BOARD-01-FU8 Void-tide bounties; **`archivus_sweelinck` ×9** (the Inquisitor gauntlet at his own node NUE · `clr_01_act5`'s named "archivist at Weimar" · `d0208_*` **provisionally** — see §AUDIT-03i); **`yael` ×8**, **`johannes_von_weisheit` ×5** (the Event Horizon Station literally speaks in `quest_math_03`'s `onComplete`), **`watcher_gvw` ×2**, **`city_guard_captain` ×2**, **`brynn` ×2**, **`crov` ×1**. Applied in ONE save via `./api.sh batch-npc` (the §AUDIT-03b wrapper; `patchStringField` correctly fell through to `insertStringField` for all 68 since none had a top-level `npc:` — 68 `inserted:true`, 0 errors). **Evidence:** pre/post **golden parse diff across all 7 collections = exactly 68 entry diffs, every one `fields=npc` alone, 0 lost / 0 new / 0 collateral**; quests still **2,853**; corpus-wide unresolvable keys still exactly the 10 §AUDIT-03h display names (list unchanged); `./api.sh audit` **errors 0 / warnings 48** (the one npc-ish warning is the pre-existing BIRKA_NPC-side `watcher_gvw` has-no-`NPC_DIALOGUES` note, untouched by this row); `check:walk` **11/11 exit 0**; new permanent `src/tests/integration/audit03g-npc-coverage.test.js` **4/4** + `audit03b-npc-anchor` 4/4 + `npc-card-map` 22/22 = **30 passed**; `_monsterLevel` signature survived the write (Hazard #1 respected — fresh server PID for the write session, stopped again before Playwright). Docs: `quest.md` §"The `npc` field" gained the coverage statement + the per-family derivation table. **Side findings filed, not fixed inline: §AUDIT-03i** gains four more unkeyed rendered speakers (the Mother Mimic · the Crimson Warrant · Sir Jullean · the Inquisitor construct) and **§AUDIT-03k** (one character, two accepted keys).
- [x] **§AUDIT-03h ✅ SHIPPED 2026-07-30 — the 10 display-name `npc` values normalized; the corpus now holds ZERO unresolvable keys and the tolerated list is empty.** The row's premise held exactly (10 quests, the listed values) — **but two of its four guessed keys were wrong, which is why the keys were measured against the live vocabulary before any write:** `Gret Orrens` → **`gret`**, not `gret_orrens`; `Pier Falk` → **`pier`**, not `pier_falk` (both MISS in the 297-key `npcKeyVocab()`). **The row's one open question — "`bog_mudwhistle` may need a profile or a different anchor" — is answered from the corpus, not by authoring a profile: Bog Mudwhistle is not an NPC at all.** He is an `NPC_TOUR_OPPONENTS` row (key `bog`, *Subsistence Fisher*, competence Average), a Yugurt-tournament minigame opponent that lives in none of the four registries — and the sibling `quest_tour_*` family, whose quests are each **titled after an opponent** (`quest_tour_03` is literally *"Bog's Terms"*, desc *"Bog Mudwhistle fishes to eat"*), **already anchors every one of its quests to `emmer`**. So `quest_guide_04` — beat 4 of Emmer's own six-quest arc, in which Bog heckles Emmer's cast — takes `emmer` like its five siblings, no new profile needed. **Shipped mapping:** `quest_guide_01/02/03/04/05` → `emmer` · `quest_guide_06` → `the_fisherman` · `quest_scar_01/03/04` → `gret` · `quest_scar_02` → `pier`. Each is corroborated twice over: `emmer`/`gret`/`pier` are `BIRKA_NPC` profiles at the arcs' own `activateNode`s (SSJ / NUE), and the arcs' **own `onComplete` favor bits already spend the exact keys** (`{kind:'favor', npc:'emmer'}` in `quest_guide_06`, `{kind:'favor', npc:'gret'}` in `quest_scar_04`); `the_fisherman` is an `NPC_DIALOGUES` key **and** SSJ's inline `npc` normalized — registries 2 and 3 landing on the identical slug. **Applied in ONE save** through the `POST /api/batch/npc` handler's own sequence (`editField` → `_buildIndexes` → `save` → copy → reload), run in-process per the §AUDIT-03c precedent (byte-identical to the server's PUT, no daemon); the whole batch was **dry-run in memory first** and only written once it came back 10/10 `inserted:false` with the favor bits untouched. **Evidence:** pre/post **golden parse diff across all 7 collections = exactly 10 entry diffs, every one `fields=npc` alone, 0 lost / 0 gained / 0 collateral**; all **16** nested `kind:'favor'` npc values **byte-identical**; game-file diff exactly **10 insertions / 10 deletions**; quests still **2,853**, nodes 416; **`./api.sh audit` errors 0 / warnings 48** (unchanged from §AUDIT-03g) and `advise` on the sampled quests now returns `errors=[] warnings=[]`; `check:walk` **12/12 exit 0**; **373 Playwright green, exit 0** (`quest-runtime-uqf` + panels/hooks/activation + the audit/NPC set), incl. new permanent `src/tests/integration/audit03h-npc-normalize.test.js` **5/5** — the 10 keys · zero unresolvable *and* zero display-name-shaped values corpus-wide · the derivations re-asserted from the live corpus (profile nodes, favor keys, `bog` rejected, every `quest_tour_*` on `emmer`) · a **negative control** (re-planting `"Emmer Finch"` in memory is caught, so the guard is not vacuous) · a QUEST_DB-scoped source guard. Hazard #1 respected (server verified stopped; `_monsterLevel` signature survived the write). **The §AUDIT-03b pin is now `toEqual([])`** as that row instructed. **No §7½ — `quest.npc` has zero quest-level reads in the game client (§AUDIT-03b), so nothing here is player-visible.** Docs: `quest.md` §"The `npc` field" gained the write-the-key-never-the-display-name rule + the 5-row derivation table + the **⚠️ the key is not always the name slugified** warning and its inverse (`NODE_MAP`'s inline `npc` is *supposed* to be a display name — normalizing `SSJ.npc` would be the bug, since it is what makes `the_fisherman` resolve; pinned). **Side finding filed, not fixed: §AUDIT-03i** gains Bog Mudwhistle + the `NPC_TOUR_OPPONENTS` registry-5 question. Original row follows.
  **§AUDIT-03h (original) — 10 quests hold un-normalized display-name `npc` values (found 2026-07-29).** `quest_guide_01/02/03/05` = `"Emmer Finch"`, `quest_guide_04` = `"Bog Mudwhistle"`, `quest_guide_06` = `"The Fisherman"`, `quest_scar_01/03/04` = `"Gret Orrens"`, `quest_scar_02` = `"Pier Falk"` — capitals + spaces, so they resolve in NO registry and advise-warn forever. Normalize to the real keys (`emmer`, `the_fisherman`, `gret_orrens`, `pier_falk`, … — verify each against `WBAPI.npcKeyVocab()`; `bog_mudwhistle` may need a profile or a different anchor). Currently **pinned** by `src/tests/integration/audit03b-npc-anchor.test.js` as the only tolerated unresolvable keys, so the list can't grow silently — update that assertion when you fix them. **Risk:** low.
- [ ] **§AUDIT-03i — named, rendered speakers with no NPC registry entry at all (found 2026-07-29).** §AUDIT-03b could not anchor these quests to their real speaker because the speaker exists only as quest prose: **Roen** (the Ardley/pilgrimage thread NPC — **92 source references**, a `disposition:`-quoted recurring companion across `quest_wis_00–07` + `quest_alch_01–07`), **Tessie** (the `hunt2` relay-road driver — the §AUDIT-03b analysis wrongly believed she was an `EB_NPC_DIALOGUE` giver; she is not, the 20 EB givers are enumerated and she is absent), **the Warden** (`quest_vs_warden`), **Commissioner Halvard Fehn** (`quest_spark2_*`), **Inspector Wren-Pembury / Aldous** (`quest_spark_01/02/05`), **Analyst Solm** (`quest_solm_01`), **Tommy** (`quest_cat_06`). Each was anchored to its arc/node NPC instead — honest, but the index says "Roen's 11 quests belong to Elder Fionn / Solvak." Either author real `NPC_DIALOGUES` entries (Roen most clearly earns one) or accept the anchors and record why. **Needs a design call** — authoring new NPCs is content work, not an audit. **Risk:** low-medium (new NPC profiles ripple into the §NPC-01 card map).
  **⟶ FOUR MORE FOUND 2026-07-29 (§AUDIT-03g).** (1) **The Mother Mimic** — LIM's presiding character, described at length in the node's own `text` ("She watches you arrive the way an expert watches an amateur make their first correct move") and the actor of `quest_d0208_a3/a4`; LIM's `NODE_MAP.npc` is `null`, so she is in no registry and the 5 `d0208_*` quests are anchored **provisionally** to `archivus_sweelinck`. The cheapest fix is registry 2 — set `LIM.npc = "The Mother Mimic"` — but that **renders an NPC card at LIM** (the §NPC-01-B derived map), so it is a player-visible content change, not an audit edit. (2) **the Crimson Warrant** — the board voice that signs the `disposition` of all three §BOARD-01-FU8 Void-tide bounties; anchored to `auros` via the `quest_signal_01` Warrant thread. (3) **Sir Jullean** — the title character of `quest_sir_jullean` (anchored to LHR's `city_guard_captain`). (4) **the Inquisitor construct** — the speaker of all three `quest_inquisitor_*` (anchored to NUE's `archivus_sweelinck`).
  **⟶ ONE MORE, PLUS A REGISTRY QUESTION, FOUND 2026-07-30 (§AUDIT-03h).** (5) **Bog Mudwhistle** — the heckler of `quest_guide_04` ("The Middle Way Sneers") and the title character of `quest_tour_03` ("Bog's Terms"). He is **not** merely unkeyed: he lives in a **fifth** node-adjacent registry the vocabulary does not read — **`NPC_TOUR_OPPONENTS`**, the six-strong Yugurt tournament cast (`pip` · `renard` · `bog` · `vera` · `dirk` + the `master` tier), each with `key`/`name`/`title`/`competence`/`quote`. §AUDIT-03h anchored both quests to `emmer` on the corpus precedent (every `quest_tour_*` already does), which is honest but leaves five named, quoted, player-*faced* characters — you compete against them — outside `npcKeyOk`. **The design call:** either (a) teach `npcKeyVocab()` a 5th registry over `NPC_TOUR_OPPONENTS` (cheap, but it widens the shared vocabulary that `advise`/`audit`/the delete-guards all read, and these are minigame opponents, not conversational NPCs), (b) give the tournament cast real `NPC_DIALOGUES` profiles (content work — and it renders cards at SSJ via the §NPC-01-B derived map, so it is player-visible), or (c) accept the `emmer` anchor and record why (what §AUDIT-03h did as the interim). **Risk:** low. **Note the near-miss trap this shares with §AUDIT-03k:** `bog` *looks* like a valid key because it is a real `key:` field in a real registry — it just isn't one of the four.
- [x] **§AUDIT-03k ✅ SHIPPED 2026-08-04 `1c4f547` — one character, one key: the alias map is live, the split is collapsed, and gate #14 grew the phase that stops the next one. The row's premise held; its measured tail found a seventh pair the earlier scan missed and an eighth that only LOOKED like one.** Re-measured before any edit (the pair list was re-derived from the live registries, never carried from the row): a name/occupation-subset scan over all 68 distinct `NODE_MAP` inline npc slugs found **7 candidates**, of which the §AUDIT-03c measurement had recorded 6 — `city_fence`⇄`pachelbel` is invisible to any name rule (the slug shares no word with *Fence Pachelbel*) and survives only because the map is **explicit**, while **`ship_captain` @SEN is NOT an alias**: it matches `captain_smollett_sen`'s occupation exactly, but Smollett captains the **Hispaniola at HMS** and SEN is the *Tilbury Star* — two ships, two captains, 8 quests that must not be merged. **A role collision is not an identity**, which is exactly why the classification is explicit rather than heuristic. Four of the seven pairs are corroborated independently by `NODE_NPC_KEYS`' own node→key column (LHR→`yael`, TLL→`brynn`, MHQ→`quill`, LLA→`pachelbel`), the other three by `profile.node` or an exact name match. **Shipped:** `WBAPI.NPC_ALIASES` + `npcCanonicalKey()` in `src/js/wbapi-core.js`; the seven slugs **removed from `npcKeyVocab()`** (297→290) so a quest still carrying one advise-warns instead of validating alongside the real key; `editField` collapses **(quest, npc) only** on write — scoped deliberately, since normalizing `NODE_MAP`'s own inline npc would be the §AUDIT-03h bug; `_buildIndexes` indexes `_questsByNpc` under the canonical key, which is what covers a **hand-authored nested UQF block** (Hazard #3 never touches the API); the create path normalizes before the world-logic check (a create serializes a fresh literal and never reaches `editField`); and every write path now **reports** the collapse (`aliased` on PUT/batch, `aliasedNpc` on create) rather than silently rewriting what the author sent. **Corpus:** the 5 `city_guard_captain` quests — the `quest_ng_01/02/03` Froberger set, `quest_sir_jullean`, `quest_courier_release` — collapsed onto `yael` via `./api.sh batch-npc`, so she is one person again at **22 quests** (was 17 + 5). **A defect found on the way, in the verifier rather than the writer:** `saveAndVerify` compared the reloaded field against **what the caller sent**, so the first live PUT returned `ok:false — field mismatch after reload: npc` on a write that was entirely correct — the inverse of this repo's usual failure. It now verifies against what `editField` actually wrote. **Evidence:** the collapse was sent **as the alias** (`npc=city_guard_captain`) and `yael` landed on disk — the round trip IS the acceptance test; `git diff` on the HTML = **5 lines, nothing else**; live round trips on all three write paths (batch → 5 patched · PUT → `aliased{city_guard_captain→yael}` + `verified ok` · POST create → `aliasedNpc{commander_bruhns→auros}`), probe quest deleted with 0 residue; `./api.sh audit` **errors 0**; `check:walk` **15/15 exit 0** with the gate's own `--selftest` catching **6/6** planted defects including both new alias shapes; new `audit03k-npc-aliases.test.js` **6/6**, and **all 6 fail on the stashed pre-fix tree** (restored, `shasum -c` OK); the npc/audit family **29/29**. **Two sibling tests were stale after the collapse and moved with it, annotated not rewritten:** `audit03g`'s ANCHORED map (the derivation had been right about the character and wrong about the key — the 68 total is unchanged) and `audit03c`'s `jimmy_two-tails` assertion, which said in so many words *"the slug still resolves — this is §AUDIT-03k, not a broken key"* and now asserts the opposite on purpose. **The durable half is gate #14 phase 5**, because nothing else can see this class: the key resolves, the write succeeds, the game is correct, and only an authoring surface nobody looks at is wrong. It fails on any authored `npc:` alias **and** on a NEW inline display name that collides with a live profile and is classified in neither `NPC_ALIASES` nor `NOT_AN_ALIAS`. **The lesson: a key that resolves can still be the wrong key — "does it exist?" and "is it the one this character is filed under?" are different questions, and only the second one notices a person split in two.** Docs: `quest.md` §"The `npc` field" (the 7-row alias table + the role-collision rule + the corrected anchoring row), `prompt.md` §7 (gate #14 gains phase 5), `index.md` (gate row + `wbapi-core` NPC-identity surface). **Side finding filed, not swept: §DX-02l-FU** (a pre-existing red, proved at HEAD without this row's changes). Original row follows. **§AUDIT-03k — one character, two accepted `npc` keys (found 2026-07-29 during §AUDIT-03g).** Guard Captain **Yael Scheidemann** is reachable as *both* `yael` (registry 1 — the `BIRKA_NPC` profile at LHR, occupation "city guard captain") *and* `city_guard_captain` (registry 2 — LHR's `NODE_MAP` inline npc string). Both pass `npcKeyOk`, and the corpus already splits: `yael` on `mq_1`/`sq_battling`/`sq_leveling`/`sq_2` + the 8 §AUDIT-03g anchors, `city_guard_captain` on the `quest_ng_01/02/03` Froberger set + the 2 §AUDIT-03g desk-guard scenes. So `_questsByNpc` indexes one person under two headings. The same shape holds wherever a profile and its node's inline string name the same NPC (`brynn`/`innkeeper_brynn`, `archivus_sweelinck`/`archivus_ptolemy_sweelinck`, `auros`/`commander_bruhns` — all four keys live in the 297-key vocabulary today). **Fix shape:** teach `npcKeyVocab()` an alias map (inline-string key → profile key) and normalize on write, so registry 2 resolves *to* registry 1 instead of alongside it; then one `batch-npc` pass to collapse the existing splits. **Risk:** low-medium (touches the shared vocabulary that `advise`/`audit`/the delete-guards all read; needs the §AUDIT-03b tests green after).
  - **↳ MEASURED 2026-07-29 (§AUDIT-03c) — the pair list is longer than this row assumed, and the live corpus damage is smaller.** Matching every registry-2 slug against the profile names in registries 1+3 finds **six** alias pairs, not four: `jimmy_two-tails`⇄**`jimmy`** (CDG — *Jimmy "Two-Tails" Carbonara*) · `innkeeper_brynn`⇄**`brynn`** (TLL) · `commander_bruhns`⇄**`auros`** (HKG **and** TLS — one slug, two nodes) · `archivus_ptolemy_sweelinck`⇄**`archivus_sweelinck`** (NUE) · **new:** `bard_tomas_couperin`⇄**`quill`** (MHQ — *Tomas Couperin*) · **new:** `city_fence`⇄**`pachelbel`** (LLA — *Fence Pachelbel*), plus this row's `city_guard_captain`⇄`yael` (LHR), which is the only pair where **both** sides carry quests. **Of the six, exactly one had a live split** — `jimmy_two-tails` on `quest_cat_06`, collapsed to `jimmy` by §AUDIT-03c. The other five slugs hold **0** quests today, so the alias map is now a *prevention* job, not a cleanup one: the only corpus pass still owed is the `yael`/`city_guard_captain` decision. **Do not "fix" the near-misses the same scan surfaced** — `the_court_registrar` (JRS, 2 quests), `captain_vera_keel` (GCI, 5), `johannes_von_weisheit` (EHZ, 5), `pit-master_grimka` (ZVD, 3) are registry-2-only anchors for characters with **no** profile at all; they are correct as-is (and the §AUDIT-03i class if anyone wants them keyed). Measurement recipe kept in `quest.md` §"The `npc` field".
- [x] **§AUDIT-03l ✅ SHIPPED 2026-07-29 — the node-code reference is GENERATED now, and the 26×16-era tables are quarantined instead of quietly wrong.** **The row's premise held and its proposed fix did not survive measurement.** Re-measuring the legend live found **81 of 92** rows dead (not 73/76 — the stricter first regex had skipped annotated rows like `| BK | 25 | bar (Visby) |`), **plus 11 more naming nodes the world removed** (`RD`, `J2`–`J7`, `LJ0`–`LJ3` — §WALK junction stubs). More importantly the row's recipe — *"look each `Node #` up and rewrite the code column"* — would have produced a **half-true** table: the `Grid Cell` column is the retired **26×16** projection while live `NODE_COORDS` runs **r 2–73 / c 154–249**, so remapping only the codes would have left an authoritative-looking doc whose coordinates are fiction. And `num` alone is **not** a sufficient key: **num 77 is held by two nodes** (`MJF` and `CDG`) and several terrain keys were renamed (`epic_goblin`→`epic_goblin_cave`, `desert_caravan`→`arabia`), so every match needs terrain/label corroboration. **Shipped instead — generate + quarantine:** new **`src/scripts/node-index.js`** / **`npm run nodes`** emits **`docs/maps/node-index.md`** from the same `wbapi-core` parse the `:1367` server reads (the `npm run stats` §DX-01g pattern) — all **416** nodes with code · `Node #` · terrain · act · **live cell** · 🛏 · label · inline NPC, plus a **LEGACY CODE MAP** (**83 recovered · 11 unresolved**) that is the Rosetta stone for every older doc, lab report and commit message. It auto-flags the nastiest class: **a legacy code that IS a live key but for a different node** — **`CI`** (Birka's streets historically; the *Chancery Court* live) and **`BK`** (the *Broken Tooth Tavern* = `VBY` historically; *Birka Shore* live). A naive "does this code resolve?" check passes on both. **New `check:walk` gate #12, `check:nodeindex`** — the committed index must be byte-identical to a fresh generation, so it cannot rot the way the legend did. **`maps.md` restructured:** a READ-THIS-FIRST table at the top routing every live question to its live source, the node-code section replaced by a pointer to the generated index, and **all six 26×16-era sections kept verbatim but re-headed ⚠️ HISTORICAL** (TINY MAP · FULL MAP · LEGEND · NODE NETWORK · COORDINATE INDEX · SLEEP NODES — the last of which listed 8 of the live **38** `sleep:true` checkpoints). **`index.md`:** the Town Cross-Reference remapped to live keys, with two corrections beyond the rename — its **Inn** column named `SF`/`IS`, *neither of which is a `sleep:true` node* (the real checkpoints are `SEN` and `NAS`), and Visby's `BK` was the Broken Tooth Tavern = `VBY`, not the live `BK`; `VENDOR_NODES` corrected `BA/MQ/SF/IS/BK` → `LLA/LGW/STN/PDL/BK`; the doc map gained the generated file. **Evidence:** `check:walk` **12/12 exit 0**; new `src/tests/integration/audit03l-node-index.test.js` **5/5** (index ≡ fresh generation · every live node present with live num+terrain · every LEGACY CODE MAP target resolves *and is justified by the legacy `Node #`* · both collisions stay flagged · `maps.md` routes to the index and marks its own tables historical); **two negative controls** — appending a bogus row to the committed index, and inserting a real node into `NODE_MAP` — each made gate #12 exit 1, and the game file was restored byte-identical (`git diff` empty). Docs synced: `prompt.md` §4 (the *never read a code off a doc table* authoring rule), §7 (11→12 gates), §9 cheat sheet. **Follow-up left open: §AUDIT-03m** below. Original row follows. **§AUDIT-03l — the docs answer "what is this node's code?" with codes that do not exist (found 2026-07-29 during §AUDIT-03c; this is the most likely *cause* of the 03c bug, not a sibling of it).** `maps.md`'s **LEGEND — Two-Letter Code Reference** is the table an author consults to write an `activateNode`, and **73 of its 76 rows name a code with no `NODE_MAP` entry.** It was written before the world adopted airport codes and was never migrated: `SL`→`BMA` · `CI`→`LHR` · `IN`→`TLL` · `TV`→`MHQ` · `BA`→`LLA` · `CR`→`KRN` · `CY`→`HKG` · `DK`→`LCY` · `MQ`→`LGW` · **`SF`→`STN`** · `MS`→`SEN` · `AL`→`GOT` · **`CQ`→`CDG`** · `AT`→`RAI` · … (full list reproducible in seconds — see the recipe below). **The `Node #` column is still correct and is the recovery key:** every legacy row maps to exactly one live key by `NODE_MAP[k].num`, so the remap is mechanical, not a judgment call. Three traps: (a) **`CI` resolves** — but to the Chancery Court (num 429), not Birka's streets, so a naive "does the code exist?" check passes while the row is still wrong; (b) `RD` (num 78) has **no live node at all**; (c) the table's **⚠️ PLANNED markers are stale** — the `CQ` row still says the Ally Cat arc is *"not yet in HTML"* when 7 quests have shipped at `CDG` for months. **Blast radius beyond that table:** the same legacy codes appear in `maps.md`'s ASCII region grid + adjacency tables, `index.md`'s region table (`CQ`, `SF`) and `VENDOR_NODES` line (says `BA/MQ/SF/IS/BK`; live is `LLA/LGW/STN/PDL/BK`), `world.md`, and `story.md` — plus the `Q-FR-01`-style **design IDs**, which are quest identifiers and should be **left alone**. **Recipe:** parse the table rows, look each `Node #` up in `WBAPI.nodeMap` by `num`, and rewrite the code column (script kept in the §AUDIT-03c session notes; it printed all 73 in one pass). **Consider a `check:walk` gate** asserting every node code cited in a docs table resolves — this pairs naturally with §AUDIT-03j's proposed registry-key gate. **Interim mitigation shipped 2026-07-29:** a hazard banner above the table naming the drift, the recovery key, and the `710bb75` incident it caused. **Risk:** low (docs only) but **high value** — this is a live authoring trap that has already produced one wrong-city bug that survived two months.
- [x] **§AUDIT-03n ✅ SHIPPED 2026-07-31 — the ending stops calling three of your six friends strangers, and gate #14 keeps every NPC reference honest. The row named 12 dead entries in 4 registries; live measurement found 29 references across 7 registries plus five live gate sites.** **The row's premise held and its scope did not** — the same understatement as §AUDIT-03j, and in the same direction. A full-depth scan (every object literal at any depth whose keys are all-lowercase, matched against the live 297-key `npcKeyVocab()`) found the three surnames keying **seven** registries, not four: the row's `NPC_EPILOGUES` / `NPC_NG_PLUS_GREETINGS` / `NPC_ACT_THREE_LINES` / `NPC_CROSS_REFS` **plus `SWEELINCK_NAMING_LINES`, `ROUGH_WHISKEY_REACTIONS` and `FROBERGER_TRACES`**, each exactly 3-of-6 dead — **21 authored entries unreachable.** **And the row missed the dimension that mattered most: six references in LIVE CODE.** The epilogue's own `npcOrder`, and **five `_npcFavor('bruhns')` gate sites** guarding whole scenes that could therefore never fire — the §XXXVII Bruhns CO scene at TLS, the S6 Weckmann/Auros joint conversation at HKG (**which ANDs `_npcFavor('crov') >= 1` with `_npcFavor('bruhns') >= 1`, so one live key and one dead one made the condition unsatisfiable**), the Layer-67b S29 theory line, the L45-J Froberger cross-item beat, and **the "🗺 Show Auros the undercity survey" delivery button — the only consumer of a key item the player is actually granted** (`31091`), so that item was a dead end in the inventory. **Identity re-derived independently, not carried from §AUDIT-03j:** the live profiles read `quill` = *Bard Tomas Couperin* (MHQ) · `crov` = *Pit Master Weckmann* (HKG) · `auros` = *Cmdr Seraphine Bruhns* (HKG), and the ledger's writers (`_setNpcFavor` + the VM `favor` opcode) spend exactly the six profile keys — the surnames appear in no registry at all. **Shipped:** 21 registry keys + `npcOrder` + 5 gate sites renamed in one line-anchored pass with per-line assertions (27/27 matched or nothing writes), value-column alignment preserved; an explanatory comment above the seven tables in the §AUDIT-03j house style; **new `src/scripts/check-npcregs.js` = `check:walk` gate #14**, the npc mirror of #13 — 4 phases (registry keys incl. one nested path `TOWN_CRIER_LINES.npcs`; **explicit** classification of all 36 all-lowercase top-level tables across `NPC_KEYED`/`VOCAB_SOURCE`/`NOT_NPC_KEYED`, an unlisted one FAILS; `_npcFavor('…')`/`npcFavorability['…']` literals; `npcOrder` arrays). **It deliberately does NOT check npc-*valued* fields** — `NODE_MAP`'s inline `npc` is *supposed* to be a display name (§AUDIT-03h's inverse lesson), and quest anchors are already pinned by `audit03h-npc-normalize`. **Evidence:** `check:walk` **14/14 exit 0**; 4-phase `--selftest`; **negative control with the real historical defects** — the gate run against the pre-fix file named **all 29**, exactly matching the independent measurement, and the file was restored byte-identical; new `audit03n-npc-registries.test.js` **6/6**, and **all 6 fail on the pre-fix file**, so neither behaviour test is vacuous; the NPC/audit family **45/45**; full Playwright **732 passed / 4 failed** = the documented `worldbuilder-crud-arrays` four and nothing else. **§7½ DONE — player-visible, and it is the point of the row:** at dear-friend favor the victory scroll now renders *"Quill settled the debt. He plays the song every Friday"*, *"Weckmann started a second class on Thursdays"* and *"Auros finished the theory… submitted it jointly with a Scholar King archivist"* in place of the three stranger lines; the Auros survey button renders at HKG (count 1, was 0); 0 page errors on both loads. **The tell, recorded as the lesson: the engine held two `npcOrder` lists that disagreed** — the town-crier one already used the real keys. When two lists of the same thing disagree, the one corroborated nowhere else is the wrong one. Docs: `story.md` §Epilogue (the consequence + the 3-row rename table), `world.md` §Cross-References, `prompt.md` §7 (13→14 gates + the exclusion), `index.md` doc map, and **dated KEY-DRIFT NOTES on the three lab reports whose design-time code blocks still show the surname keys** (annotate, don't rewrite — the §DX-02c/§AUDIT-03m precedent). **Side finding filed, not swept: §AUDIT-03o** — favor **3 is unreachable for 5 of the 6** NPCs, so the `entries[3]` epilogues this row just unlocked are still gated behind a tier nothing grants. Original row follows.
  **§AUDIT-03n (original) — four MORE registries key their entries to NPC names the favor ledger never spends (found 2026-07-31 during §AUDIT-03j; the npc-key twin of that row, and a measured extension of §AUDIT-03k).** §AUDIT-03j fixed `NODE_NPC_KEYS`/`NPC_FAREWELLS`, whose values were the profiles' SURNAMES rather than the profile keys. **The same three dead keys — `couperin` (=`quill`), `weckmann` (=`crov`), `bruhns` (=`auros`) — still key 12 entries across four more registries:** `NPC_EPILOGUES`, `NPC_NG_PLUS_GREETINGS`, `NPC_ACT_THREE_LINES`, `NPC_CROSS_REFS` (measured: 3 of 6 keys dead in each; `NPC_VOID_PRESSURE_LINES`/`NPC_NG_MEMORY_LINES`/`NPC_ROMANCE_PREAMBLES` are clean). **The sharpest consequence is in the ending:** the epilogue builder walks `npcOrder = ['yael','brynn','couperin','pachelbel','weckmann','bruhns']` and reads `S_story.npcFavorability[key]`, which is **always 0** for three of them — so the endgame renders the *stranger* variant for Quill, Weckmann and Bruhns no matter how close you got, while the fav≥2/fav≥3 lines authored for them are unreachable. (Note a sibling `npcOrder` at the void-pressure site already uses the real keys — the two lists disagree, which is the tell.) **Fix shape:** rename the keys to the profile keys in all four registries + the epilogue `npcOrder`, then extend `check:noderegs` with a 5th phase (or a sibling `check:npcregs`) asserting every npc-keyed registry key resolves in the 4-registry `npcKeyVocab()` — the npc mirror of gate #13, which would have caught all 12. **Risk:** low (renames + a gate); the lines already exist and are authored. **Verify:** an epilogue test at fav 3 for `quill`/`crov`/`auros` must render the dear-friend variant, not `entries[0]`.
- [x] **§AUDIT-03p ✅ SHIPPED 2026-08-04 `ea19a88` — the Void's first crack now reaches 13 places instead of 5, and the blind spot inside the blind spot is closed: gate #13 reads function-local registries. The row's premise held exactly; what it could not know is that four of the sixteen keys were never nodes at ANY point in the file's history — they were born dead.** **Measured before touching anything** (Existing-Work-First: tree clean at `cd033cb`, server **stopped** — Hazard #1 — one claude process): the row's 11-of-16 count re-derived live against the 416-key `NODE_MAP`, and then each dead key traced through `docs/maps/node-index.md`'s LEGACY CODE MAP **plus** `git show cc562f5` (the Layer 59 commit that introduced the table) and the pre-remap `NODE_MAP` at that commit. **The finding worth more than the remap:** `SH`, `PH`, `MH` and `WM` matched **no `NODE_MAP` key at the commit that authored them** and appear nowhere in `maps.md`'s 92-row historical legend either — the table was written from *memory*, not from the world, so a quarter of it never fired even on the day it shipped. `WM` was later minted as a real node (Weimar Archive) that happens to fit its own cluster; `SH`/`PH`/`MH` never were and are **dropped, not guessed at**. The other eight remapped mechanically and each was **corroborated by label, not just by `Node #`** (`SL`→`BMA` Birka Slums · `CR`→`KRN` Birka Crypt · `BA`→`LLA` Rough Bar · `DK`→`LCY` *Harbor Docks — Tilbury* · `MQ`→`LGW` *Tilbury Market Quarter* · `MT`→`GVA` Mountain Pass · `HM`→`DUS` Frequency Row · `DF`→`ZRH` Unbanked Quarter). **One judgment call, stated rather than buried:** `ZRH` is filed under `wild`, not the `weimar` its legend row implied — it is a `defi_land` node whose two district siblings (`MUC`, `DUS`) already read `wild`, and *"The scholars' candles are burning down fast today"* at a quarter with no scholars would be new wrong content, not a repair. **Coverage 5 → 13 nodes, all live.** **The gate:** `check:noderegs` phase 5 (§AUDIT-03p) applies the SAME explicit classification — `LOCAL_NODE_KEYED` / `NOT_NODE_KEYED`, an unlisted codeish local literal **fails** — to indented `const/let/var NAME = {` declarations. Phases 1–2 only ever matched `^const`, which is the entire reason a 16-key table could rot for months inside a function while the gate reported green; the file has **3** such node tables (`CLUSTER`, `_vaSites`, `birkaNpcs` — the latter two already clean) and 2 compass ones (`ARROWS`/`DELTAS`, classified with a reason). **Evidence:** `check:walk` **16/16 exit 0**; `check:noderegs --selftest` **6/6**, the two new plants being a dead key in a classified local table *and* an unclassified one; **negative control** — the gate run against the HEAD `CLUSTER` fails **exit 1** naming exactly the eleven dead keys, restored green with an empty diff; new `audit03p-void-flavor-cluster.test.js` **10/10** (source: every key live, every value a real `CRACK` bucket, the three phantoms absent; render: the crack line reaches `LHR`+`BMA`+`LCY`+`WM`+`GVA` at pressure 3, the ≥6 named-place line reaches `LCY`; two controls — pressure 2 renders nothing, and `CDG`, in no cluster, stays silent at pressure 6), and a **positive control** proving the tests are not vacuous: run against HEAD, **6 of the 10 fail** and the 4 that pass are exactly the already-live `LHR`/`WM` cases. Full Playwright **787 passed / 4 failed** — the documented `worldbuilder-crud-arrays` four, plus the known `multiplayer-presence.test.js:171` flake, which is **7/7 green in isolation** and is not a fifth red. Docs synced: `story.md` §Pressure Cascade gains the live cluster→node table with the never-existed-keys note, `index.md` + `prompt.md` §7 record phase 5, and `npm run anchors:fix` refreshed the 12 hints my +7 lines drifted. **Original row follows.** **§AUDIT-03p — a LIVE engine table is keyed to dead 26×16 node codes, and gate #13 cannot see it (found 2026-08-04 during §AUDIT-03m; the §AUDIT-03j class, one scope level down).** `_voidFlavorLine(nodeCode)` — called from the node renderer at `play.html:31747` — switches on a **function-local** `const CLUSTER` map (`play.html:26776`) of 16 node codes. **Measured against the live 416-key `NODE_MAP`: 11 of the 16 are dead** — `SL`, `CR`, `BA`, `SH`, `PH`, `DK`, `MQ`, `DF`, `MT`, `MH`, `HM`. Only `LHR`, `STN`, `NUE`, `WM`, `MUC` resolve. The function returns `''` on a miss, so at void pressure ≥ 3 the Birka/Tilbury/Weimar/wild **flavor line silently never renders** at eleven of the sixteen places it was authored for — dead player-facing content, failing the way §AUDIT-03j's 42 references failed. **Why gate #13 (`check:noderegs`) is green anyway, and this is the interesting part:** it classifies and checks **top-level** registries; `CLUSTER` is declared *inside a function body*, so it is invisible to the scan. **`SL`/`CR`/`BA`/`DK`/`MQ`/`DF`/`MT`/`HM` remap mechanically** through `docs/maps/node-index.md`'s LEGACY CODE MAP (→ `BMA`/`KRN`/`LLA`/`LCY`/`LGW`/`ZRH`/`GVA`/`DUS`); **`SH`, `PH` and `MH` are in no map at all** and need identification from the cluster they were filed under, not a guess. **Fix shape:** remap the resolvable eight, classify the three unknowns explicitly, then extend gate #13 with a **function-local** node-keyed-object phase — the same widening §AUDIT-03n was to #13's registry list. **Do not batch this with the doc sweep**: it edits the game file (Hazard #1 — stop the server, commit early) and it is player-visible, so it needs §7½. **Risk:** low-medium. **Verify:** the eight remapped codes resolve; a void-pressure render at one repaired node shows the flavor line where it previously showed nothing; `check:walk` green with the new phase catching the pre-fix table as a negative control.
- [ ] **§AUDIT-03o — favor level 3 is unreachable for 5 of the 6 canonical NPCs, so the `entries[3]` tier is authored-but-dead content (found 2026-07-31 during §AUDIT-03n; needs a design call).** §AUDIT-03n unlocked the fav≥2 *and* fav≥3 lines for Quill/Weckmann/Bruhns by fixing their keys — but **measurement of every writer to the ledger shows level 3 is only ever granted to two characters.** The writers are exactly: `_setNpcFavor(key, level)` (whose literal call sites are `_setNpcFavor('yael', 3)`, `('crov',1)`, `('solvak',1)`, `('yva',1)`), the VM's `favor` opcode (corpus-wide the `set:` values are **only 1 and 2** — 10 × `set:1`, 4 × `set:2`), the dear-friend auto-upgrade (`npcFavorability[key] = 2`, both sites), and one hardcoded `npcFavorability['kenickie'] = 3`. **So `brynn`/`quill`/`pachelbel`/`crov`/`auros` cap at 2**, and everything gated on 3 is dead: their `NPC_EPILOGUES[3]` lines (the richest ending text in the file), `FROBERGER_TRACES.crov`'s `minFav:3`, and the `weckmann_class` arc-epilogue condition `_npcFavor('crov') >= 3`. **This is a design call, not an audit fix** — three honest readings: (a) tier 3 is *meant* to be a rare apex only Yael reaches, in which case the other five NPCs' `[3]` entries should be demoted to `[2]` or deleted rather than left as dead authoring; (b) each NPC deserves a third personal act that grants 3 (content work, and the natural companion to §NPC-01-D / §RIPPLE-01-C's "the favor track is too thin to earn its ladder"); or (c) the dear-friend auto-upgrade should promote to 3 when a *second* personal bit lands. **Do not default it** — (a) deletes authored content and (b) is an arc each. **Coordinate with §RIPPLE-01-C and §NPC-01-D**, which are already scoped to widen this same track. **Risk:** low to measure, medium to author. **Verify:** whatever the choice, a test asserting the top tier each NPC can actually reach matches the top tier its tables author.
- [x] **§AUDIT-03q ✅ SHIPPED 2026-08-04 `28c6f8a` — the class no gate could see is now a gate, and the reading pass that opened this row had itself missed one.** The row's premise held exactly: `check:legacycodes` is driven by the LEGACY CODE MAP, which records codes that *were* nodes and were renamed, so it has nothing to say about a code that was never a node at all — the §AUDIT-03p born-dead class. **Measured first, as the row asked.** Of the eleven tokens filed (`SH` `PH` `MH` `SK` `SB` `AO` `LT` `HR` `KS` `DR` `TS`), **exactly one is a live trap**: `SH` in `world.md`. `PH`/`MH` appear only in `story.md`'s own §AUDIT-03p note, backticked on an explanatory line; the other eight appear in **no live doc at all** — they are design-era arc codes from `lab-report-saul-paul-vignette-spec.md` and `lab-report-naval-campaign-layer.md`, reaching live prose only through `quest.md:527`, which already labels them *"design-era codes … shipped under real-world names"*. `quest.md` is HISTORY: annotate, never rewrite. So the row's guess — *"several are probably arc identifiers, not node codes"* — measured true for 8 of 11, and **no sweep was warranted**. **The finding.** §AUDIT-03m-FU caught *"Tell Pachelbel at SH"* by reading and fixed it; on the same page, **`world.md:600` still read *"Player asks Pachelbel at `LLA` or SH"***. Neither the line test nor a place cue can see that token, because the preposition belongs to the code **next to** it — a hand-read pass and a line-scoped detector have the same blind spot. The new signal closes it: **a line that has already put a live node code in backticks is talking about places, so a codeish token beside it is one too.** Fixed against the ENGINE, not the legacy map (the §AUDIT-03m-FU discipline): the lute handover is keyed to `key === 'pachelbel'` on the NPC card, and `birkaNpcs` puts Pachelbel at `LLA` — so the beat happens wherever he stands, and `SH` is annotated as never-a-node rather than guessed onto a tidy live code. **Shipped:** `src/scripts/legacy-codes.js` **phase 2** — a two-letter token resolving in **neither** registry, in a node context, in a SWEEP doc; `eachProseLine` + `localCue` extracted so both phases share one instrument (§DX-01d's *one scanner for gate and writer*). Classification is EXPLICIT for the third time in this family and for the third time for the same reason — a percentage heuristic is blind to a token that is *always* jargon: **19 tokens fire across the 10 swept docs and `NG` fires 47 times without ever being a node**, so each sits in `NOT_A_NODE_CODE` with a reason or in `BORN_DEAD`. The phase catches a second shape by the same test: a **real** historical code the `maps.md` legend never listed, so `npm run nodes` never put it in the map (`FR` = Fishmonger's Row → `AMS`). **The limit is stated, not discovered:** it hunts **exactly two letters** — the shape of the retired code space and of all eleven filed tokens. Widening to three admits a technical doc's whole acronym vocabulary (`NPC` 151× · `DOM` · `ATK` · `HTML`) for no measured gain, and every extra token is human classification work; **a three-letter code written from memory is NOT caught**, and a test asserts that rather than letting the silence read as coverage. **Evidence:** `check:walk` **16/16 exit 0** (bare run, not piped) · five controls planted one at a time in a SWEEP doc — a bare born-dead code exit 1, an unclassified `ZQ` exit 1 *("classify it in BORN_DEAD or NOT_A_NODE_CODE")*, and `Q-TL-01` / `NG+` / *"Act IV … 20 HP"* beside a live node code each exit 0 · `audit03m-legacy-codes.test.js` **15/15** (+5), with a **positive control**: keep the new tool, restore HEAD's `world.md`, and **3 tests fail** — gate #16's own assertion, the phase-2 classification test and the engine-corroboration test — while the gate exits 1 · full Playwright **796 passed / 4 failed** (the documented `worldbuilder-crud-arrays` four; the run printed 795/5 because `multiplayer-presence.test.js:171` did not retry green, and that file passes **7/7** on its own). Docs synced: `prompt.md` §7, `index.md` gate row, this row. **Coordinates with §AUDIT-03p**, which owns the engine-side half of the same class.
- [x] **§AUDIT-03m ✅ SHIPPED 2026-08-04 `b399318` — `world.md` and `story.md` stop speaking 26×16, and the sweep is a measured tool with a gate rather than a one-off pass. The row's premise held; its warning about a blind replace was the whole design problem, and the sharpest finding was that the CODES were the symptom, not the disease.** **Measured first, never carried from the row:** a naive word-boundary scan over every `.md` returns **4,663 hits — and most are noise.** `DC` is a difficulty class **435** times, `EB` abbreviates *Epic Battleground* **163** times, and `IS`/`AT`/`CO`/`ST` sit inside ordinary English. So `src/scripts/legacy-codes.js` (`npm run legacy`) reads the LEGACY CODE MAP out of the **generated** `docs/maps/node-index.md` (§AUDIT-03l's gate #12 keeps it live — the map is never re-typed) and counts a token only in a **node context**, with the ambiguous codes classified **explicitly** — the §AUDIT-03j/n house style, for the same reason: a percentage heuristic is blind to a code that is *wholly* ambiguous. That took 4,663 → **2,517 real references, 230 of them in live docs.** **Every doc is classified SWEEP / PENDING / HISTORY, and an unclassified one FAILS** — four turned up on the first run and were classified by reading them. **HISTORY is annotated, never rewritten** (§DX-02c / §AUDIT-03n precedent): lab reports, `plan-archive.md`, BACKLOG and `maps.md`'s quarantined tables hold **2,287** references and are left alone — the tool **refuses** `--annotate` on one, and a test proves it. **Shipped:** `world.md` **77 annotations / 81 lines** and `story.md` **193 / 160**, in two forms because one form cannot serve both cases — sparse lines get inline `` `LHR` (historical `CI`) ``, a run of 3+ codes gets live codes plus **one** trailing key (`*(historical: `CI`=`LHR` · …)*`), since ten interleaved parentheticals is a wall, not an annotation. **The finding that mattered more than the codes:** `world.md`'s node sections describe a **retired navigation model**, and annotating them alone would have laundered a false claim into live-looking codes. Measured at HEAD: **0 of the 416 live nodes carries an `N`/`S`/`E`/`W` field** (§CELL-01 stripped them), and `J1`/`J6`/`J7` are absent — so *"Node connections from CI (**verified against `NODE_MAP`**): N→SL · E→IN"* was true when written and is not a claim anyone can re-check. It now carries a **historical-model banner** naming all three retirements (edge graph · junction stubs · the `R03,C16` 26×16 projection, live coords being r 2–73 / c 154–249) and pointing at `./api.sh location <CODE>` as the authority; the junction-vignette paragraph was rewritten to say the stubs are **deleted** and that §AUDIT-03j rehomed seven of them. **Three tool bugs the review caught before they shipped, each a real one:** it wrote **inside fenced code blocks** (12 lines of a `world.md` milepoint block), it **nested backticks** on already-backticked codes (```` ``J2` (historical …)` ````, 11 lines), and it missed a **single-code table cell** (`| Yael | CI | … |`). All three are fixed in the tool and pinned by tests — the output was re-generated from a clean `git checkout`, never hand-patched around. **Evidence:** `check:walk` **16/16 exit 0** (new gate #16 `check:legacycodes`); **negative control on the real doc** — a bare *"north of SL"* appended to `world.md` turned the gate **red (exit 1)**, restored with `shasum -c` **OK** and an empty `git diff`; new `audit03m-legacy-codes.test.js` **7/7**, of which **two are controls**: a **positive** control feeding six shapes that must still be caught (a heading run · a direction sentence · `Node CI` · an arrow list · a table cell · a bare code on an explanatory line) and a **negative** control feeding nine that must not be (`DC 10` · *bait search DC reduced* · *EB engagement audit* · `Q-TL-01` · an already-annotated code · a dense-form legend · a backticked code on an explanatory line · a table header · ordinary English) — because **a detector narrowed into uselessness and a deleted detector look identical in a green run** (§DX-02l-FU's lesson, applied deliberately). Full Playwright **778 passed / 4 failed / 0 flaky** = the documented `worldbuilder-crud-arrays` four and nothing else (771 + these 7). **No HTML touched, no §7½** — docs + tooling only; the WBAPI server stayed stopped (no writes needed). **Two limits, asserted rather than left to be discovered:** an ambiguous code is flagged only on a node-context line (precision over recall — the price of not mangling English), and **a fenced block is code**, so a dead code inside a *runnable example* escapes the gate. One did: `docs/notes/docs-dev-environment.md`'s `./api.sh get node CY` — a copy-pasteable command against a node that has not existed for months — **fixed by hand to `HKG`**, and the class is recorded here rather than papered over. **Open tail: the 7 PENDING live docs (230 references)**, reported by name and count on every gate run — `docs/notes/docs-node-network.md` 95 · `story-arc-epic-battlegrounds` 41 · `story-arc-investigation` 27 · `story-arc-npc-dialogues` 22 · `story-arc-coastal` 13 · `story-flowchart` 12 · `story-arc-ngplus` 8. They are PENDING, not SWEEP, because **a gate that fails on work nobody has done yet is a red light you learn to ignore**; moving a file to SWEEP is the next slice and needs no design call. **Side finding filed, not swept: §AUDIT-03p** — a *live engine* table with the same rot, which gate #13 cannot see. Original row follows. **§AUDIT-03m — the prose still speaks 26×16 (found 2026-07-29 during §AUDIT-03l).** §AUDIT-03l fixed the *tables an author looks a code up in*; the legacy codes still appear in **running prose and cross-references** across `world.md`, `story.md`, `docs/story/*`, `docs/notes/*` and the lab reports (`"N of CI"`, `"east of DS"`, `Q-FR-01`-style design IDs, etc.). **Deliberately not swept here:** a blind token replace over prose is how you turn `AT`/`IS`/`SE`/`CA` into nonsense inside English sentences, and the **design IDs (`Q-TL-01`, `Q-FR-02`, §XIX/§XX arc names) are identifiers and must NOT be remapped.** Do it per-document, reading each hit, with `docs/maps/node-index.md`'s LEGACY CODE MAP as the key — and prefer *annotating* (`CI` → `` `LHR` (historical `CI`) ``) over silent replacement so the older records stay readable. **Risk:** low; **value:** medium (the authoring trap itself is closed by 03l's gate — this is legibility, not correctness).
- [ ] **§AUDIT-03r — `num:77` is held by two nodes (found 2026-08-11 during §DOC-02a).** `` `MJF:{ num:77@8784` `` (The Shale Drop, act 3) and `` `CDG:{ num:77@8798` `` (The Cat Quarter, act 1) both carry `num:77`. The Ally Cat lab report's own 2026-05-25 risk register said *"verify no existing node uses 77"* — it was not verified, and the collision has stood since. **Measured impact today: cosmetic.** `node.num` has exactly three read sites, all display (`s-node-num` HUD pad, the trail list, the travel destination line), so nothing looks a node up *by* `num` and nothing breaks; but it is a duplicate value in a field whose name and zero-padded rendering both read like a primary key, and the next reader who treats it as one gets the wrong node silently (Hazard #2 class). `maps.md:112` already records the collision in prose — this row is to **decide and enforce**: either renumber one node (a display-only change, but `num` is authored in ~416 places and the sequence has meaning to nobody but the HUD), or declare `num` explicitly non-unique and add a one-line comment saying so. **A gate is cheap either way** — `check:dupkeys` already walks `NODE_MAP` entries; a duplicate-`num` assertion is a few lines, and it is the only thing that stops the third one. **Needs a design call** (renumber vs. declare). **Risk:** low. **Verify:** whichever choice, a test that fails on a second node sharing a `num`.
> **⟶ §DOC-02ch (2026-08-18) WIDENS THIS ROW BY 13×: the collision is not one pair, it is FOURTEEN.** Real-parser census at HEAD (`src/js/wbapi-core.js` `W.load`, 416 nodes): **14 `num` values are held by two nodes each** — `77` (MJF/CDG, the pair this row was opened on) plus a **contiguous 420–432 block**: `420` ECF/BGZ · `421` BTH/TOL · `422` REL/CDN · `423` DAR/VLC · `424` MGR/GEO · `425` DKN/PHY · `426` CLK/GHC · `427` STB/LGE · `428` GLD/ADN · `429` CI/RGS · `430` DNG/MGZ · `431` CHC/STR · `432` PRH/AUG. The run is unbroken, which reads as **two importer batches that both started numbering at 420**, not as thirteen independent slips — so a renumber pass is one batch, not fourteen decisions. `docs/maps/node-index.md`'s own LEGACY CODE MAP header still warns only that *"num 77 is held by two nodes"*, and it uses `Node #` as its **matching key** — so the other 13 pairs are a live ambiguity in the tool that recovers historical node codes. `429` is the pair found in situ: `` `CI: { num:429, name:"city"@9229` `` (Chancery Court — The Officer's Pen) against `RGS`. **Add to this row's scope:** whichever way the decision goes, enforce it with a `check:invariants` assertion that `num` is unique, so the next importer batch cannot re-open it silently.
- [ ] **§AUDIT-03s — dead node codes in PLAYER-FACING engine strings, which no gate can see (found 2026-08-11 during §DOC-02a).** `check:legacycodes` (gate #16) scans `*.md` only; `check:noderegs` phase 6 (gate #13) scans **comparison literals and battle codes** only. Neither can see a retired code sitting inside a quoted string the player *reads*. **Measured: 7 occurrences of the `the XX node` shape across the HTML, 6 of them player-visible.** Five belong to the Ally Cat arc and all say *"the DF node"* for what is now `ZRH`: `sandy_cat`'s `NPC_DIALOGUES` line, `quest_cat_void`'s `desc` + `disposition` + `failText`, and the `CQ_BOSS` victory `storyMsg`. Two more are elsewhere: a quest `desc` saying *"Win the CY node battle"* (`CY`→`HKG`) and a tunnel `storyMsg` naming `DF`. One further hit (`the EB node`) is a source comment, not player text. **The fix is two-sided:** (a) rewrite the six strings to name the place, not the code — *"the strays near the Unbanked Quarter"* reads better than any code and cannot rot again, which is the §AUDIT-03m-FU discipline (fix against the world, don't launder a code into a tidier code); (b) add a **phase 7** to `check:noderegs` that scans *string literals* in the HTML for a legacy code in a node context, with the family's explicit-classification style so an intentional mention must be listed. Note the scope limit before starting: `the XX node` is one phrasing — the sweep should first measure how many *other* phrasings carry a dead code (`at XX`, `north of XX`, a bare parenthesised code) before the gate's shape is fixed, exactly as §AUDIT-03m-FU had to widen from a LINE test to four hand-classified cues. **Risk:** low (prose + one scanner phase). **Verify:** the six strings changed and eyeballed live at `CDG`; a planted control string exits 1. **+1, 2026-08-12 (§DOC-02aa), and it is a new sub-shape: a live code naming the WRONG PLACE.** `hint:'Visit the City Inn to find the open page.',@11051` (`quest_ng_02`, Layer 50) is not a dead code at all — it is plain English, and it is wrong: the Entry 42 panel renders at `LHR` = **City Streets — Birka** (`if (node.code === 'LHR' && (S_story.ngPlusRun || 0) >= 1 && S_story.priorQuestMinusOne@34619`), while *The First Inn* is `TLL`. The error is **original**: the birth commit reads `node.code === 'CI'` and the same hint, and `CI` was `num:1, name:'city'`, never an inn. So the report, the home doc and the player-facing hint all inherited one author's wrong mental map of a node they had correctly transcribed. **This is the shape phase 7 cannot catch by design** — there is no code in the string to resolve — which is worth stating in the row rather than discovering later: the gate handles *dead codes in prose*, and a *wrong place-name in prose* needs a human or a `q.activateNode` → node-label cross-check. **+1 more shape, 2026-08-12 (§DOC-02ab): a retired node code preserved in an IDENTIFIER.** `returnedToCI: !!(S_story.visited && S_story.visited['LHR'] && S_story.level >= 5),@23662` — the property NAME still says `CI` while the value it reads was migrated to `LHR`. Not player-facing, so the cheapest member of the family, but invisible to **every** gate by construction: `check:legacycodes` scans `.md`, `check:noderegs` scans node *references* and a bare object key is neither, `check:anchors` scans docs. Worth one line in whatever phase 7 becomes — an object key that is a legacy code sitting beside a value that resolves is the exact shape that makes *"does this code exist?"* the wrong question.
> **EXTENDED 2026-08-12 (§DOC-02r) — the §WHODUNIT-01 resolution line is on a dead node.** `Ord the passenger is owed an apology@33611` (*"…the cook has declined to give"*) is the only place in 38,712 lines that closes the arc's moral beat, and it renders inside `node.code === 'SEN'`, which `LCY` suppresses (§AUDIT-03x). Not a dead *code* — a live, correct string on unreachable ground, which is the same invisibility with the opposite cause. Worth keeping in this row because the detector wanted for §AUDIT-03s (scan the **engine-string universe**, not just `.md` and not just comparison literals) is the one that would surface it.
> **EXTENDED 2026-08-13 (§DOC-02be) — `YC` ×6 in engine COMMENTS, and a 2026-08 migration minted two of them.** `YC` was a real node: `YC:{ num:76, code:'YC', name:'yugurt_cabin', label:'Yugurt Cabin', act:3` at `32c10c5` (2026-05-24, earliest surviving build), **renamed to `SSJ`** — same `num`, same terrain key, same label — by `c1d5a94` (2026-05-29 22:45). **RETIRED, not born-dead**; only the archive can say so (instrument 8). Both consumers gate correctly on `node.code === 'SSJ'`; only the prose beside them is stale: `// ── Rod Shop at YC ──@32877`, `Yugurt Tournament chain (activate at YC@13889`, `const NPC_TOUR_OPPONENTS = [@26710`'s *"(YC tournament…)"*, and `// ── §XLV TOURNAMENT section (YC only, requires Fishing Rod) ────@34048`. **The count GREW, which is the part worth the row:** 4 sites at `89fa13b` (2026-06-16) and **6 at HEAD**, because §VM-01-G2b's hook migration copied both comments onto its two new call sites (`_runNodeHook('birka-rod-shop'@35195`, `_runNodeHook('ssj-tournament'@35795`) while correctly rewiring the code under them. *A migration repairs the references that break and carries the one that doesn't* (instrument 38) — a comment cannot break, so nothing forces the read. **This is the cheapest member of the family (6 comment edits, no player text)** but it is the one that shows the class is still **growing**, which is the argument for the phase-7 detector rather than a one-time sweep. Cost of leaving it: `lab-report-synthesis-part4-monsters-fishing.md` copied *"rod shop at YC"* into a design document three weeks after the node stopped existing, and a reader looking `YC` up in `docs/maps/node-index.md` finds nothing at all.

- [ ] **§AUDIT-03t — 36 `NODE_MAP` entries carry `act:NaN`, and two readers render `undefined` to the player (found 2026-08-11 during §DOC-02c).** Traced from the battleground report's `NodeEntry` typedef claim *"`act` — story act (1–8)"*. **Measured: 36 of 416 nodes (8.7%) have `act:NaN`** — `num` 181–443, the Avignon/Genoese, Nottingham, Bethune/Camelot and Roncevaux blocks, incl. `` `CI: { num:429@9229` ``. **Three readers, three different failures, none of which throws:** `` `S_story.actNumber = node.act || 1@34556` `` silently coerces to **Act 1** (NaN is falsy) so every one of these nodes is act-gated as if it were Birka; `` `ACT_NAMES[node.act]@34590` `` indexes a 9-element array (`` `const ACT_NAMES@9417` ``) with NaN and yields `undefined`, so the act badge renders **"— undefined —"** and `#s-node-act` renders the bare word **"undefined"**; and the map panel's info line renders **"Act NaN"**. This is the Hazard-#2 class in its data form — *a read of a real-but-wrong value never throws*. **The fix is two-sided:** (a) assign each of the 36 nodes a real act — most are late-arc historical content whose act is inferable from its neighbours, but a handful (the Chancery/Camelot restoration set) may be deliberately act-less and want an explicit sentinel instead, which is the one design call in the row; (b) add a gate — `check:invariants` is the natural home — failing on any `NODE_MAP` entry whose `act` is not an integer in `ACT_NAMES`' index range, since **no existing gate can see this**: `check:noderegs` checks that node *references* resolve, never that a node's own scalar fields are well-formed. **Risk:** low for the gate, low-medium for (a) — `actNumber` feeds act-gated quest/CSS behaviour, so changing 36 nodes off their silent Act 1 could surface content that was never reachable. **Verify:** the gate exits 1 on a planted `act:NaN`; the badge eyeballed live at `CI` and `RON` per §7½.
- [ ] **§AUDIT-03u — the world has 416 nodes; two player-facing strings still tell the player it has forty-two (found 2026-08-11 during §DOC-02d).** Both are stale by 10×, and both are *onboarding* text — the two places the game states its own scale to a new player. (a) The Map Shop hands over the Real Map: *"he reaches under and sets the Real Map beside it — **all forty-two nodes** — and pushes it toward you"* (`` `STN:{ num:9@8636` ``). (b) Yael's Level-1 tutorial monologue, the game's first paragraph of instruction, opens *"Check your MAP. The known world has **forty-two nodes** and you can get lost in it, badly, fast"* (`` `yael: { meta:@10397` ``). **Forty-two was the report-era battleground count in the retired 26×16 space** — §DOC-02c's Appendix A lists exactly 42 codes — so both sentences are fossils of a map that was replaced by §WALK/§NAV-01's 90×360 geo grid. `npm run stats` says **416** nodes / **111** terrains. **Mechanical, with one judgement call worth making deliberately:** do **not** swap in `416`, which will rot the same way the moment a node is added — rewrite both to describe scale without a literal (*"the known world is larger than it looks"*, *"every node the cartographers have"*). **Related class, not yet measured:** a hardcoded world-scale number in *any* player-facing string is the same defect, and nothing greps for it; if a survey is cheap, count the others in the same pass. **Risk:** none (prose only). **Verify:** `grep -c "forty-two nodes"` → 0; both strings eyeballed live at `STN` and `LHR` per §7½.
- [ ] **§AUDIT-03aj — the §FUTURE-01 arc's spine is behind fifteen mandatory sleeps at one node, and nothing tells the player (found 2026-08-12 during §DOC-02ai).** 🟡 The Saul-to-Paul arc implements two of its source document's most quotable figures as **literal in-game days**. (a) Acts 9:9's three blind days → `` `S_story.blindDaysKS = (S_story.blindDaysKS || 0) + 1;@36273` ``, incremented on each sleep at `DAM` while `saulConverted && !anathSightRestored`; Anath arrives on day 3; `DAM` is `sleepCost:3`. (b) Gal 1:18's *"he abode with him fifteen days"* → `` `if (S_story.hrHellenistDays >= 15) S_story.hellenistsThreaten = true;@36276` ``, incremented on each sleep at `JRS`, `sleepCost:5`. **`quest_barnach_finds` gates on `flags:['hellenistsThreaten']` and it is the hinge of the whole arc** — it opens Tarsus, which opens the Antioch commissioning, which opens the first journey, so **16 of the arc's 18 quests sit behind those fifteen sleeps.** Mandatory floor: **18 sleeps and 84 gold — 37 % of the 49-day doom clock** — for Act 4 content a player reaches with days already spent. **Nothing here is a bug:** every counter is read, every gate resolves, no test fails; it is a design consequence of fidelity that was never priced. **Three options, design call:** (a) reduce `>= 15` and keep the fifteen days in the prose, where they cost nothing; (b) let ANY sleep advance `hrHellenistDays` once `barnachVouchedHR` is set, so the wait runs concurrently with other play; (c) accept the price and **say so in the UI**, since no surface currently states that the gate is a day count at all. **Recommend (b)** — it preserves the scriptural number exactly, keeps the doom clock honest, and costs one condition. **Verify:** a save with `barnachVouchedHR` reaches `hellenistsThreaten` without 15 sleeps at `JRS`; the arc completes inside a normal run. **Related:** the arc's other reachability problem is `ATH`←`SEA` under §AUDIT-03x (see the cheapest-instance note on that row).
- [ ] **§FUTURE-01-FU — the arc's naming policy shipped half-applied and the document that authorised it has been deleted (found 2026-08-12 during §DOC-02ai).** 🟡 `lab-report-saul-paul-vignette-spec.md` opens *"All proper nouns use the fictionalized names from `plan.md §FUTURE-01 Name Translation Table`"*, and the arc was built that way — `git show 8f8dc2a` has `HR:{ code:'HR', name:'herath', label:'Herath — Lower Court' }` and `KS:{ code:'KS', name:'kesra', label:'Kesra — Lower City' }` in the live world. At **`c1d5a94`** (2026-05-29, *"story books"* — the same broad rename commit §DOC-02g caught mis-recording Corelli's history) **every place reverted to history and every person did not.** Measured at HEAD: **13 of 13 places historical** (Jerusalem 48 · Damascus 55 · Antioch 43 · Athens 42 · Rome 93 · …), **5 of 5 fictional place names at 0 occurrences** (`Herath`/`Kesra`/`Tarsis`/`Aethon`/`Phillam`), **7 of 7 people still fictional** (`Barnach` 23 · `Timael` 17 · `Ezzir` 13 · `Anath` 8 · `Silar` 8 · `Lyra` 8 · `Joach` 5), and **the protagonist historical** (`Paul` 52 · `Saul` 7). So the shipped world is one where *Saul of Tarsus walks from Jerusalem to Damascus and is healed by a man named Anath.* **`plan.md` was deleted by `5e48dd7`, so the two lab reports are now the only record of the policy.** **One sub-item needs NO design call and should ship regardless:** `quest_stoning_lystra`'s `passText` still says `` `runs out of Lythros@11417` `` — one occurrence in 38,712 lines, spoken at a node labelled *Lystra — The East Gate*, and traceable to the vignette spec's Voice Rule 3 (*"After the stoning at Lythros: he gets up"*), i.e. **a verbatim transcription carried a retired identifier past a rename that only greped the world.** **Design call on the rest:** finish the reversion (rename the seven people to Barnabas/Timothy/Elymas/Ananias/Silas/Lydia/…) or restore the fiction (rename the thirteen places and the protagonist back). **Recommend finishing the reversion** — 14 of 21 proper nouns already went that way, the protagonist is the loudest of them, and a half-applied policy is the only outcome that cannot be defended to a player. **Also record (no action needed):** the reversion left inert fossils in four registers — engine comments (`§LIX: KS`, `§LXIV: LT`, `§LXII: AO`), DOM ids (`` `story-ks-conversion@31334` `` · `` `story-lt-stoning@31339` `` · `` `story-ao-namechange@31329` ``), state field names (`blindDaysKS`, `barnachVouchedHR`, `hrHellenistDays`) and quest ids (`quest_shipwreck_melta`). None resolves wrongly and no gate can see them; they are logged so a future reader does not reconstruct a node map that no longer exists. **EXTENDED 2026-08-12 (§DOC-02aj — the sibling vignette spec).** Two more fossils, and the second is a new shape: `` `quest_prison_phillam@11449` `` carries the fictional *Phillam* in a live quest id, and its mission bit `` `phillippiJailerConverted@11449` `` (1 writer / 2 readers) is a **BLEND** of both schemes — *Phillam*'s double-l welded onto historical *Philippi*, a token belonging to neither vocabulary and therefore invisible to a grep for either. **And a SECOND player-facing straggler, in the arc's last scene:** the spec genericised *Troas* to *"the waypoint"*, and the reversion never restored it — `` `the cloak I left at the waypoint@8503` `` (Rome node text) and again in `quest_rome_arrest`'s passText at `` `most human line he writes@11502` ``. Same mechanism as `Lythros`: prose transcribed by hand, and a rename pass that greps the world never reads a quest's sentences. **Instrument 4 refines the diagnosis:** the policy was not abandoned halfway, it was **overtaken** halfway — `Herath`/`Kesra`/`Tarsis`/`Anthos` are RETIRED (3–4 commits each, reverted at `c1d5a94`) while `Aethon`/`Phillam`/`Korath`/`Ephrath` have **0 commits ever**, because those four name second- and third-journey stops authored *after* the 2026-05-29 reversion.
- [ ] **§FUTURE-01-FU2 — the arc's first mission card addresses the player as Saul, one sentence away from calling him "he" (NEW 2026-08-12 during §DOC-02aj).** 🟢 **no design call, two lines.** The §FUTURE-01 arc's shipped frame is *player-as-travelling-companion*: node texts narrate Paul in third person and "you" is a separate character with their own hit points (*"Paul takes the hit before you close the distance"*). Measured across the 13 arc node texts: **113 third-person tokens against 22 second-person, 0 of 13 written in pure second person**; across the 18 arc quests, **99 prose fields, 21 mixing both persons** — nearly all of it legitimate, because a companion narrating Paul's action must use both. **The exception is `` `quest_road_damascus@11310` ``, the arc's first mission, which addresses the player *as Saul*:** `` `The warrants are in your coat@11312` `` in the `desc`, and the completion narrative one line above it — *"The warrants are still in your coat. You will not use them again."* — while the sentence between them reads *"He leaves at first light."* The warrants are Saul's; nobody else can be carrying them. **This is the residue of the original vignette spec, which wrote the player AS Paul in second person throughout** (88 second-person tokens, the name "Paul" zero times) — so the one place that POV survived is the one place it contradicts the arc's own frame, and it is the first §FUTURE-01 text a player reads. **Fix:** repoint both to the companion (*"The warrants are in his coat" / "He will not use them again"*), or — if the opening beat is deliberately first-person-as-Saul — say so in the node text and make `desc` agree with itself. **Verify:** grep the arc block for `your coat`; 0 hits. **Related:** §FUTURE-01-FU (same reversion), and the transposition mechanism is what carried `Lythros` past the rename.
- [ ] **§AUDIT-03am — the first line every new player reads names a writing-craft diagnosis, not the game (NEW 2026-08-12 during §DOC-02al).** 🟡 **design call — pick one title.** `story-ci-tagline@31322` is a `NODE_PANELS` entry on **`LHR`, the starting node**, and it renders *"LETS GO — Codex of Conquest: Curse of Knowledge"* above *"CodexOfConquest.com · MIT Public Domain"*. The first half is a deliberate and successful retitle — `lab-report-story-codoex-curse-of-knowedge.md` §I argues that *The Shattered Codex* frames the game as retrieval while **Codex of Conquest** frames the Void as a conqueror and the seven Shards as surrender documents, which is the emotional architecture the ending depends on. **The second half is the lab report's own subtitle, and it is Steven Pinker's term for a prose defect** — the framework the report used to critique the writing, not a phrase from the world. It reached the player because the tagline was copied from the report's title line whole. Meanwhile `index.md`, `README.md` and every doc header call the game ***The Shattered Codex***, and `Shattered Codex` has 3 live hits in the engine, so **the game currently ships two titles and shows the player the one no document uses.** Present at `32c10c5`, the earliest surviving build, so it has been the opening line for the whole measured history. **Options:** (a) *"Codex of Conquest"* alone — keeps the retitle that works, drops the diagnosis; (b) restore *"The Shattered Codex"* and reconcile the docs the other way; (c) keep both deliberately and record the subtitle as an in-world phrase, which would want the journal's entry-41 usage (*"The curse is not gathering the Shards. The curse is knowing"*, `entryNum:41, nodeCode:'TLS'@27227`) promoted somewhere a player meets earlier. **Recommend (a).** ***The transferable part: a document's TITLE is not a candidate for player-facing text, however good the phrase — a critique's vocabulary describes the author, not the world.***
- [ ] **§AUDIT-03ak — four `⚠️ PLANNED` headings in `world.md` describe features that have been live since 2026-05-26, and one of them is marked ✅ in `story.md` on the same day (NEW 2026-08-12 during §DOC-02ak).** 🟢 **no design call.** SP4's Phase 1 cleared 21 stale `PLANNED` markers and preserved eight layers as *"features that do not yet exist in the HTML"* — **and all eight had implementation markers in the commit SP4 itself shipped** (`9684ff6`). Four of those headings survive at HEAD, 78 days on: `world.md:227` Weimar Scholar Gate (Layer 51), `:239` Void Archaeology (52), `:280` The Void Shaman (56), `:292` Codex Shard Origin Stories (57). Layer 56 is the sharpest — `world.md:280` says `#### ⚠️ PLANNED — The Void Shaman` while `story.md:1697` says `#### ✅ Layer 56 — The Void Shaman`, so **the two home docs contradict each other about one feature**, which is the exact defect Phase 1 ran to remove, in the two files it ran against. The evidence was never subtle: `_S_DEFAULTS()` carries `// Layer 51: Weimar Scholar Gate@23128`, `// Layer 52: Void Archaeology` and `// Layer 56: Void Shaman` as **section comments naming the layers by number**, with their state fields beneath — and Phase 1's own stated method was *"cross-reference the feature against `_S_DEFAULTS()`; state fields are definitive evidence of implementation."* One grep of the pass's own designated evidence source returns *implemented* for three of the four. **Fix:** promote the four headings to `✅ Layer N — Implemented` in `world.md` with the engine anchors above; no HTML edit. **Transferable lesson (the §DOC-02ak instrument): a verification pass tests what it CHANGES and assumes what it LEAVES** — the residual set is where a sync pass's errors concentrate, because preserving a row costs nothing and so buys no scrutiny.
- [ ] **§AUDIT-03al — `story.md §Gate Locks` documents a deleted constant, a function that never existed at HEAD, and a mechanic invariant #1 forbids — and an annotation pass dressed it in current node codes (NEW 2026-08-12 during §DOC-02ak).** 🟢 **no design call — delete the section.** `story.md:348` is the one new doc section SP4 created (its §VI counts it: *"New doc sections created: 1"*), written to give `GATE_LOCKS` a documentation home so Phase 4's validator would go green. At HEAD: **`GATE_LOCKS` = 0 occurrences** (retired at `5123f5a`, 2026-05-28 — *two days after SP4 shipped*), **`_canTravelTo()` = 0 occurrences**, and the section's claim that travel is *blocked* when an item is absent is not merely absent from the engine but **forbidden**: invariant #1 refuses a step for exactly two reasons, `oob` and `impassable`, which `cellMove` honours at HEAD. It still cites *"HTML line 10864."* **Three passes walked over it.** §NAV-01's 2026-07-03 sync found this exact defect — `index.md:24` records *"stale GATE_LOCKS section retired"* and *"`GATE_LOCKS` gone from code (docs claimed 4 live gates)"* — retired the `maps.md` copy and **missed the `story.md` copy SP4 had minted**. §AUDIT-03m then annotated its four legacy pairs into tidy live codes (`` `KRN` (historical `CR`) `` → `` `HKG` (historical `CY`) ``, +3). ***§AUDIT-03m-FU's laundering lesson at full strength: annotation without verification converts a wrong claim into a confident-looking live one*** — the earlier instance mislabelled one NPC's node; this one dressed a deleted constant and a banned mechanic in current codes. **Fix:** delete `story.md §Gate Locks`; the live gate-condition table in `docs/spec/spec-engine.md:354` is the surviving description and needs its own check that those are *mission* gates, not movement gates.
- [ ] **§DX-02aw — the F4 and F6 function tables are the last doc tables using bare line numbers, they sit outside `check:anchors`, and one has drifted 20,220 lines under a "verified" stamp (NEW 2026-08-12 during §DOC-02ak).** 🟡 **no design call on the diagnosis.** `docs/mechanics/mechanics-economy.md:423` (F4) and `docs/spec/combat.md:467` (F6) carry their pointers in a `| Function | Line |` **column** rather than as `` `symbol@line` `` anchors, so gate #15 (`check:anchors`, §DX-01e) cannot see them — the anchor resolver only audits the backticked `symbol@number` form. Measured drift at HEAD: F4's `storyPreBattle(node)` reads **16179** against a live **36399** (**+20,220 lines**); F6's `roll(sides)` reads 5483 against 6417, `rollN` 5487 against 6421. Row counts have drifted too — F4 holds **26** rows and F6 **55**, against the 29 and 23 the tables' own headers claim. And F4 still carries SP4's verification stamp verbatim: *"All 29 line numbers verified against `play.html` (17,762 lines) · 2026-05-26 (SP4 annotation pass)"* — a green badge above 26 stale rows. **Root cause is a cadence, not a mistake:** SP4 §V-C prescribed *"after any session that adds ≥20 lines to the HTML, re-grep the F4 and F6 tables before committing"* — a manual instruction with no gate behind it, which never ran. Same class as §DOC-02i's FC04 and as §DX-02s's Doc Health Badge (still ✅ beside `HTML line count | 36,933` against a live 38,712). ***A check that depends on a human running it reports green when nobody ran it.*** **Fix:** convert both tables to `` `symbol@line` `` anchors, which puts ~80 pointers under gate #15 and retires the cadence entirely; then `npm run anchors:fix`.
- [ ] **§AUDIT-03ao — a two-arc, seven-quest design shipped 79 days ago and exactly one of its quests can be finished (NEW 2026-08-12 during §DOC-02an).** 🟠 **no design call; four independent blockers, all mechanical.** Layers 54+55 — the Tilbury Harbor Arc and the Visby Underground — verify **32 of 32 identifiers live, every quoted line byte-verbatim at 79 days, the monster statline and drop exact**, and are nonetheless unplayable. **(a) `tlLedgerRead` is its own precondition.** `` `quest_tl_01: { id:'quest_tl_01'@11170` `` completes on `completion:{ flags:['tlLedgerRead'] }`, and the **only** write to that flag in 38,712 lines is `` `{ kind:'flag_write', set:['tlLedgerRead'] }@11172` `` — inside that same quest's `onComplete`, which runs *after* the completion test passes. The harbor board, which the lab report names as the thing that sets it, grants the Harrow Manifest and never touches the flag (`` `if (!S_story.tlManifestFound) {@34796` `` … `hbBtn.remove()`). **Wrong the day it was written**: at the arc's own ship commit `194a810` the shape was identical (`completeFn:() => !!(S_story.tlLedgerRead)`, sole writer in the id-keyed reward block), so this is not §ARCH-01 migration rot. **This is the SECOND instance of §AUDIT-03ai's class — the write-only defect inverted, a completion flag nobody can write — and two instances make it a pattern no gate can see: the symbol resolves, the quest parses, `BIT_CONTRACTS` validates.** Consequence: `tlLedgerRead` is permanently false → tl_02's gate never opens → `` `S_story.tlLedgerRead && _tqs['quest_tl_02'] === 'active'@34824` `` never renders → tl_03 never unlocks → Ori never appears → Rennau never leaves Neutral → Vonn never enters the NPC row (`` `TL:S_story.tlLedgerRead ? ['vonn'] : []@35128` ``). **(b) three of the four render nodes are non-primary** — see the §AUDIT-03x extension for the cell table and the repoint recommendation. **(c) the one live path makes a character invisible on it:** `quest_vs_02` is reachable only via §BOARD-01-FU6's `unlock` edges from `` `quest_pachelbel_shipment:@21279` `` / `quest_couperin_lute`, so `vsDebtProbed` is false on that route — and the NPC row gates Yva's *card* on it (`` `TRD:S_story.vsDebtProbed && !S_story.vsWeaponsFound@35129` ``) while her *button* (`` `id:'trd-yva'@34335` ``) gates on the quest's status. **The button to talk to Yva renders and Yva does not.** Two surfaces for one character, two conditions, one dead. **(d) the payoff both arcs exist for has never been played** — see (b). **Withheld from the player: 850gp of quest rewards, two readable items, one relic, four favor advancements, and the Void Shaman reveal.** **Fix order:** repoint (b) first — it is the cheapest and it makes (a) observable; then give the harbor board the `tlLedgerRead` write it always implied; (c) is a one-line gate alignment. **Docs to correct in the same increment:** `quest.md:473`/`:560` mark all six rows `[✅ LIVE]` and `world.md:204`/`:263` head both sections ✅ LIVE on the strength of §VM-01-G3's partial 2026-07-28 revival; `docs/story/story-arc-coastal.md` (a SWEEP doc) still carries the two structurally-dead act legs verbatim — *"§XX is Act V-gated"* (`:30`), *"Fires at `STN` … when `tlLedgerRead && actNumber >= 4`"* (`:117`) — plus *"Vonn … caps at Friendly"* (`:54`, see §DX-02az); and `index.md:91` lists the arcs' nodes as the retired `DK · SF · GC` in a **table column**, the §DX-02aw shape that gate #16 cannot reach.

- [ ] **§AUDIT-03ap — a monster drops a quest item that grants nothing and shares its name with the real one (NEW 2026-08-12 during §DOC-02an).** 🟢 **small design call: should the drop be a path, or should it stop pretending to be one?** `` `hollow_hands_guard:   { name:'Hollow Hands Seal'@5850` `` gives every kill in `` `P.hollow_hands_guard ] }@6312` `` (the `goblin_cave` pool, i.e. `TRD` — a **primary**, so this is the arc's most reachable surface) an item named exactly like the quest item Yva grants at `` `id:'trd-yva'@34335` ``. Collecting it sets no flag: `vsWeaponsFound` is written only inside her bit chain. Worse, the delivery step strips by name — `` `filter(i => i.name !== 'Hollow Hands Seal')@34897` `` — so a farmed trophy is silently consumed alongside the real seal. **The lab report filed this against itself on 2026-05-25** (*"collecting it this way has no effect… a monster-drop path to the same flag would reward players who explore GC before finding Yva"*) and it has been open for 79 days. Options: (1) make the drop an alternative source — `itemsAll:['Hollow Hands Seal']` as a second `completion` route for `quest_vs_02`, honouring the 50gp path as the *fast* one; (2) rename the trophy (*"Hollow Hands Tally"*) so the two items stop colliding; (3) drop the drop. **Cheapest correct move is (2) regardless of the call on (1)** — the name collision is a defect under every option.

- [ ] **§AUDIT-03aq — three town-crier headlines are keyed to flags that have never had a writer, and they sit at the TOP of the priority list (NEW 2026-08-13 during §DOC-02ao).** 🟠 **no design call.** `` `const qOrder = ['warden_resolved'@26867` `` is the town crier's quest-news priority list, read by `_getTownCrierLine()` on every inn rest below `voidPressure 6`. Three of its eight members — **`warden_resolved`, `vs_hollow_seal_taken`, `tl_ori_account_read`** — occur exactly **twice each in 38,712 lines**: once as a `TOWN_CRIER_LINES.quests` key and once as the `qOrder` member that reads it. **No `_S_DEFAULTS()` declaration, no writer, no commit in the file's entire history that ever wrote one** (`git log -S` returns a single commit each, `4c06f05` *"Layer 62: Town Crier"*, 2026-05-24, which is the commit that created them). So `S_story[flag]` is `undefined` for all three, the loop falls through, and the three lines have never rendered. **The mechanism is a scheduling accident worth remembering: Layer 62 shipped the CONSEQUENCE on 2026-05-24 and `194a810` shipped the CAUSE on 2026-05-25** — the news author invented flag names for arcs that did not exist yet, and the arc author, one day later, wrote different ones (`wardensLegacyKnown`, `vsWeaponsFound`, `tlMissingShipSolved`). The two commits never met and nothing could tell them apart. **Third, fourth and fifth instances of §AUDIT-03ai's inverted write-only class (a read with no writer)** — the first three were completion flags; these are *display* reads, which is why they fail silently rather than dead-ending a quest. **No gate can see it:** `check:deadconsts` as proposed censuses unread *fields*; this is the mirror — a **read of a field that was never declared**, which is exactly the READ-only shape §DX-02y opened. **Fix (three one-word edits):** repoint each `qOrder` member and its `TOWN_CRIER_LINES.quests` key to the flag its own arc writes. **Verify:** `for f in warden_resolved vs_hollow_seal_taken tl_ori_account_read; do grep -c "$f" play.html; done` → 0 0 0. **Sequence after §AUDIT-03ao** — the two Visby/Tilbury lines are pointless until those arcs are reachable, but the edit is free to make now. **Also fix in the same breath:** `` `say the MT pass is open@26770` `` names the retired code `MT` in a **player-facing string** (→ §AUDIT-03s, which now stands at 8 hits).
- [ ] **§AUDIT-03ar — the reading circle announces "Benedikt is Dear Friend" and sets his favor to 1, which is Friendly (NEW 2026-08-13 during §DOC-02ao).** 🟢 **no design call, one character.** `` `{ kind:'favor', npc:"benedikt_rasp", set:1 }@11097` `` is `quest_wm_03`'s onComplete and the **only** favor writer for `benedikt_rasp` anywhere in the file; its sibling `narrative` bit on the same line reads *"📘 Scholar Kings' History (Tome) obtained. Benedikt is Dear Friend."* The threshold is unambiguous and stated twice — `` `fav >= 2 ? '💛 Dear Friend'@23713` `` for the card badge and `` `if (fav >= 2) pool = d.dearFriend;@23570` `` for dialogue. **Three consequences, in increasing order of loss.** (a) The message is false to the player's face. (b) Benedikt's entire `dearFriend:` dialogue pool — one of the best-written lines in the NPC file, *"The Scholar Kings didn't erase her. They just stopped saying the name. Froberger said it in his margin notes every time."* — is **unreachable at any point in any run**: a dead VALUE inside a live field, the §DX-02az shape, **second instance in two increments**. (c) The Layer 56 Benedikt callback at `` `_npcFavor('benedikt_rasp') >= 2@34914` `` — §XXI's intellectual close, the paragraph that reframes the Warden as an archival accident rather than a fight — can never fire even after §AUDIT-03ao unblocks the arc. **⚠️ PREMISE CORRECTED 2026-08-13 (§DOC-02ar) — this row is INERT until §AUDIT-03at lands.** `quest_wm_03`'s `onComplete` **never runs**: its completion flag is set by the reading circle, which counts against `` `const today = S_story.dayCounter || 0;@34724` `` — a field with **1 occurrence, 1 commit and 0 writers in the file's entire history** — so the circle locks at 1/3 forever. Benedikt's favor is therefore **0, not 1**, and `set:2` changes nothing on its own. Same shape as §AUDIT-03aq behind §AUDIT-03ao: the edit is free now, the effect waits. **Fix:** `set:2`. Prefer that over lowering the two gates, because the message, the badge and the dialogue tier all already agree that 2 is what was meant. **Verify:** complete `quest_wm_03`; the card reads 💛 Dear Friend and the `dearFriend` pool is drawn. **Class note:** the write is *correct as data* and *wrong as intent*, so no schema check can see it — the only detector is the one this row demonstrates, **compare a `favor` bit's `set:` against the tier its own `narrative` sibling names**.
- [ ] **§AUDIT-03as — a data audit made The Warden a wandering monster in another shaman's sanctum, and it is now the only way to meet him (NEW 2026-08-13 during §DOC-02ao).** 🟡 **small design call.** `lab-report-void-shaman.md` §I-C is explicit that the Layer-56 Warden and Act V's Void High Shaman Kazrath are separate entities under separate mandates — *"Kazrath's motivations are aggressive; the Warden's are administrative"* — and at the arc's ship commit the sanctum's terrain roster was `monsters:[]`. `9df7a2b` (2026-06-04, *"DATA AUDIT — terrain monsters"*) filled it: `` `monsters:[ P.goblin, P.hobgoblin, P.void_shaman ]@6362` ``, the terrain of `` `TBS:{ num:71, code:'TBS'@8779` `` — *Void Shaman's Sanctum*, Kazrath's own room — and gave the statline a 4-row `MONSTER_DROPS` trophy table. **Because Layer 56's scripted encounter is unreachable (§AUDIT-03ao), the random spawn is now the ONLY place The Warden exists in play**: `name:'The Warden'` renders in the battle overlay with no dialogue, no Token, no mandate and no choice — the arc's antagonist reduced to the thing the arc was written to argue against. **Nothing here is broken**; the audit was doing correct work on an empty roster and had no way to know the key was reserved. **Options:** (1) remove `P.void_shaman` from `epic_goblin_cave` and let the Warden exist only in his scene — cleanest, and free once §AUDIT-03ao lands; (2) keep it and rename the pooled instance (`void_acolyte`) so the named statline stays unique; (3) accept the overlap as flavour and record it. **Recommend (1).** **Verify:** `grep -c "P.void_shaman" play.html` → 0 under (1). **Also recorded, and CLOSED:** the same commit fixed this monster's `tier:'rare'` → `tier:"hard"` — an enum value outside the five-member contract that every tier reader silently defaulted for 10 days. `dx02g-monster-tier-contract.test.js` was written **because of this monster** and names it in its own header comment, so the risk is gated and needs no row. *The one wrong data value in an otherwise byte-exact report became the founding case of a CI gate.*
- [ ] **§AUDIT-03at — the reading circle counts sessions against a clock that does not exist, so the Weimar arc stops at 2 of 4 quests (NEW 2026-08-13 during §DOC-02ar).** 🟢 **no design call, one identifier — the highest-value single-token repair the verification program has found.** `` `const today = S_story.dayCounter || 0;@34724` `` is the **only** occurrence of `S_story.dayCounter` in 38,712 lines and has **one commit in the file's entire history** (`194a810`, the commit that shipped Layer 51); it is absent from `_S_DEFAULTS()`, absent from the earliest surviving build, and has **zero writers, ever**. So `today` is `0` on every render: the first click runs `` `S_story.wmSessionsDays.push(today);@34734` `` and thereafter `` `alreadyToday = sessions.includes(today)@34725` `` is permanently true. The button locks at 1/3 reading *"📖 The circle meets again tomorrow."* — `` `The circle meets again tomorrow.@34727` `` — and `` `if (n >= 3) S_story.wmBenediktCircleComplete = true;@34742` `` never fires. **Blast radius:** `quest_wm_03` never completes (no *Scholar Kings' History* tome / +2 initiative, no favor grant, no `wmDoc3Unredacted`) and `quest_wm_04` never even **activates**, since `` `gate:{ flags:['wmBenediktCircleComplete'] }@11106` `` is that same flag. **The engine's real clock is `S_story.day` (`` `S_story.day + '/49'@36087` ``) and `S_story.gameDay` (22 sites) — and `lab-report-weimar-scholar-gate.md` names `gameDay` correctly, twice.** The spec was right and the implementation mistyped it. **Fix:** `dayCounter` → `gameDay` at `@34741`. **Verify:** unlock the archive, read all three documents, attend the circle on three different in-game days; the counter advances 1/3 → 3/3 and `quest_wm_03` completes. **Sequences before §AUDIT-03ar** (that row's `set:2` is inert until this lands — `quest_wm_03`'s `onComplete` never runs, so Benedikt's favor is 0, not 1) **and before §AUDIT-03au.**
- [ ] **§AUDIT-03au — `quest_wm_04`'s completion condition is its own completion effect, so the Weimar arc's climax has never rendered (NEW 2026-08-13 during §DOC-02ar).** 🟢 **no design call — the intended writer is written down in the quest's own hint.** `` `completion:{ flags:['wmFirstResearcherKnown'] }@11106` `` against `` `set:['wmFirstResearcherKnown'] }, { kind:'reward', gold:300 }@11107` ``: the flag has **exactly one writer in the whole file — that `onComplete`** — every other occurrence is a read, and the engine states its own contract beside the evaluator (*"the declarative completion gate is the ONLY completion path"*). **Born that way:** at the ship tree `194a810` the shape was `completeFn:() => !!(S_story.wmFirstResearcherKnown)` with the flag set inside the same quest's reward block, and §ARCH-01 W7c transcribed the loop faithfully into UQF. ***A faithful migration of a broken contract produces a broken contract that now looks modern.*** **Three consequences.** (a) *Benedikt's Annotated Copy* is never granted, so `` `bonus:{atkWhileQuestActive:1}@11108` `` is the only carrier of that bonus and `` `_tomeBonuses().atk + _lakeMagicBonuses().atk@25032` `` is structurally always 0 — a whole designed bonus class that has never touched an attack roll; +300gp never paid. (b) `` `S_story.wmDoc3Unredacted && S_story.wmFirstResearcherKnown@27843` `` guards the substitution, so **`Marta Eilene Vass` — one occurrence in the file, at `` `Marta Eilene Vass — First Tier@27844` `` — has never rendered in any playthrough**, and `` `const _vaReady = (S_story.ngPlusRun || 0) >= 1@31626` `` (Layer 52 / §XVII) inherits the block. (c) The `` `Read (unredacted)' : '📄 Read'@27852` `` label is computed only where the body is null, and `wmDoc3Unredacted` forces the body non-null — **unreachable by construction**, at the ship tree and at HEAD. **(b2) A second wall behind the first:** `storyNewGamePlus` restores six fields (`` `const savedNgRun@24026` `` and neighbours) and resets the rest from `_S_DEFAULTS()`; `wmFirstResearcherKnown` is **not** among them, while `_vaReady` demands `ngPlusRun >= 1` **and** that flag — so even a repaired quest would have its result erased by the exact transition §XVII requires. **Fix:** move the grant to where the quest's own hint puts it — `` `hint:'Return to the lower archive and read the unredacted personnel file.'@11110` `` — i.e. set `wmFirstResearcherKnown` when Document 3 is read while `wmDoc3Unredacted`; drop the now-redundant `flag_write` from `onComplete`; add the flag to the NG+ carry list. **Verify:** after `quest_wm_03`, open the Lower Archive and read Document 3 — the name renders, `quest_wm_04` completes, +300gp and the third tome arrive, and `_vaReady` can be reached in NG+. **Blocked by §AUDIT-03at** (`quest_wm_04` cannot activate until the reading circle can finish).
- [ ] **§AUDIT-03av — one node carries a Weimar label and four Nuremberg player-facing strings (NEW 2026-08-13 during §DOC-02ar).** 🟡 **small design call — one line: which name wins.** `` `label:"Scholar's Quarter — Weimar"@8705` `` is the header the player reads on arrival, and the file says *Weimar* **517** times — the whole Layer-51 arc, the shard, Sweelinck, the journal. It says *Nuremberg* **five** times and **four of those attach to this node**, including the Warrant's Board rumor that recruits the player into the arc: `` `Isolde in Nuremberg is short three of them@11084` ``, a quest failText pointing at *"the Nuremberg archive"*, a §BOARD-01 branch fork naming *"the Scholar Kings' lower archive at Nuremberg"*, and `` `occupation:"secondary acquisitions, Nuremberg archive"@22954` `` on an NPC whose `node` is `NUE`. **`NUE` is Nuremberg's airport code; the content is Weimar's** — the §WALK/§NAV-01 geo migration picked the code and the prose never followed. At grid resolution the cell `` `NUE:{r:20,c:191}@9615` `` is ~50°N/11°E, which is between the two cities and settles nothing. This is **not** a dead code — §AUDIT-03m's family in a new dimension: a **live** node with two mutually exclusive place names in player-facing strings, invisible to gate #16 (`.md` only) and to gate #13 (references resolve fine). **Options:** (1) repoint the four strings to Weimar — cheapest, and the label, the node text, `story.md`, `world.md` and 517 hits all already agree; (2) rename the node code, which touches five `activateNode`/`waypointNode` sites plus `NODE_HOOKS`/`NODE_VERBS` ids and buys nothing; (3) declare the Scholar's Quarter to be *in* Nuremberg and rewrite the label — contradicts the arc's own name. **Recommend (1).** **Verify:** `grep -c Nuremberg play.html` → 1 (the unrelated apothecary import line).

- [ ] **§DX-02ba — a live dev-environment doc teaches the exit-67 restart protocol, which the server explicitly repudiates in its own source (NEW 2026-08-13 during §DOC-02ap).** 🟡 **no design call.** `docs/notes/docs-dev-environment.md` documents the WBAPI restart contract in **four places** — `:73` *"`POST /api/restart` saves state, exits with code 67, and `wbapi-toggle.sh`'s restart loop relaunches cleanly"*, `:226` *"`./wbapi-toggle.sh fg` — restart on exit 67"*, `:233` *"The **restart loop** inside `_run_loop` relaunches the server whenever it exits with code 67"*, and `:373` *"Runtime crashes exit with code 67 and the toggle script relaunches"*. **All four are dead.** `7e2239a` (2026-06-05 06:37, *"Zero-downtime writes: hot-reload in memory instead of process restart"*) retired the mechanism, and the server now states the repudiation in-line: `` `src/js/wbapi-server.js:The server never exits with code 67@11638` `` — *"All restart/relaunch is handled by an external process (monitor-snapshots.py keepalive or wbapi-toggle.sh). Crashes exit 1 (hard stop). POST /api/restart exits 0 (clean stop)."* **The doc also names a function that no longer exists**: `wbapi-toggle.sh` has no `_run_loop` and no restart loop of any kind — its verb table is `start · stop · restart · status · fg · tracker · toggle`, all direct. **This is a live doc, not history**, so annotate-don't-rewrite does not apply; it should be corrected. **Note the doc already half-knows** — `:72` correctly records that `EADDRINUSE` exits 0 *"(not 67)"*, so the page contradicts itself two lines apart. **Fix:** rewrite the four passages to the exit-0 + external-keepalive contract. **Verify:** `grep -c "67" docs/notes/docs-dev-environment.md` → 0 for the restart sense; `grep -c "_run_loop" wbapi-toggle.sh` → 0 confirms the dead pointer. **Class:** the §AUDIT-03m *annotation-launders-a-wrong-claim* family in its doc-prose form — a maintained page describing a mechanism whose own implementation file argues against it.

- [ ] **§DX-02bb — the WBAPI's built-in help contradicts itself in two places, and one of them states the wrong nonce TTL (NEW 2026-08-13 during §DOC-02ap).** 🟢 **no design call, two string edits.** The help system is the API's self-documenting surface (`GET /api/help[/{topic}]`, 18 topics) and it disagrees with itself twice. **(a) Nonce TTL.** The `write` topic's worked example prints `` `src/js/wbapi-server.js:expires": "60s"@2032` `` while the `nonce` topic three dozen lines later prints `Response: { "nonce": "…", "expires": 300 }` followed by *"Nonces expire after 5 minutes"*. **The code is 300**: `` `src/js/wbapi-server.js:const NONCE_TTL = 5 * 60 * 1000@48` ``. So the first example is 5× short, and the reader most likely to hit it is the one following the two-step write walkthrough — exactly the person who might discard a still-valid token. **(b) Export collections.** `` `src/js/wbapi-server.js:collection: node_map | quest_db@2123` `` lists five collections plus `all` and **omits `lake_magic`**, which the `export` topic in the same file documents at `` `src/js/wbapi-server.js:'  lake_magic   — LAKE_MAGIC_DB object'@2491` ``. **Fix:** `"60s"` → `300`; add `lake_magic` to the `:2123` list. **Verify:** `curl -s localhost:1367/api/help/write | grep expires` and `…/api/help/export | grep lake_magic` agree with the `nonce`/`export` topics. **Why it matters beyond the two strings:** `lab-report-wbapi-evolution.md` §IX argues the help system is what makes a hand-copied endpoint catalogue unnecessary — that argument is only as good as the help text's internal consistency, and nothing audits it. Consider a cheap gate: assert every documented literal that also appears as a constant (`NONCE_TTL`, the export collection list) matches its source.

- [ ] **§DX-02bc — three WBAPI endpoints have had no `./api.sh` wrapper for 76 days, so every doc that names them must instruct raw `curl` against invariant #7 (NEW 2026-08-13 during §DOC-02ap).** 🟡 **no design call.** `lab-report-wbapi-evolution.md` (filed 2026-05-29) flagged three endpoints as *"not yet wrapped in api.sh"* and gave raw-`curl` recipes for them: **`POST /api/monster/{key}/fork`** (variant monster by copy + field overrides), **`POST /api/node/{code}/move`** (rename a node code, updating `QUEST_DB` + the NPC roster), and **`POST /api/terrain/{key}/swap`** (repoint a monster key inside one terrain's `monsters` array). **At verification all three still route in `src/js/wbapi-server.js` and all three still have zero wrapper in `src/api/wb.js`** (`grep -c` on `fork`/`move`/`swap` → 0). prompt.md §3's Golden Rule is unambiguous — *"if `./api.sh` lacks a command you need, add the endpoint + a named `./api.sh` wrapper"* — and invariant #7 is *API-first*. The standing consequence is that the repo's own documentation is obliged to hand authors runnable raw-`curl`, the exact shape §DX-02l-FU narrowed a CI detector to catch in author-facing docs. **Note the trio is not idle vocabulary:** `node/{code}/move` is the operation that would have made the 26×16 → 90×360 code migration a one-command rename instead of the `§AUDIT-03c`/`03l`/`03m` archaeology it became. **Fix:** add three `./api.sh` subcommands (`fork`, `move`, `swap`) with auto-nonce, in the existing `post`-family shape. **Verify:** `./api.sh fork goblin goblin_champion ac=15 hp=18` round-trips and `./api.sh get monster goblin_champion` returns it; the three raw-`curl` blocks can then be deleted from the lab report and from `docs/api/`. **Risk:** low — the server side already exists and is unchanged.

- [ ] **§DX-02bd — `_questsByNode` indexes a quest once per node field with no dedupe, so 49 of 416 nodes over-report their quests and the delete-cascade guard names the same blocker twice (NEW 2026-08-13 during §DOC-02aq).** 🟢 **no design call, one line.** `src/js/wbapi-core.js:this._questsByNode[q[field]].push(id);@775` sits inside `for (const field of ['activateNode','waypointNode'])`. A quest whose two node fields name the **same** node is pushed twice, and `src/js/wbapi-core.js:byNode(code){ return (WBAPI._questsByNode[code]@1084` maps the raw array, so every consumer inherits the duplicate: `./api.sh list quest --node X` double-lists, `location.get(X).quests` over-counts, and `_deps.node()` — the DELETE cascade guard — reports the same blocking quest twice.
> **Measured at HEAD: 49 of 416 nodes carry duplicates**, worst `NUE` with 9 duplicates in a 186-entry list. **It has been there since the system shipped:** `lab-report-wbapi.md`'s own 2026-05-29 DELETE example returns `blockedBy.quests: ['quest_antecedent_01', …]` for `CY`, and the elided tail is `quest_signal_01` **listed twice** — the ellipsis hid the bug in the document that introduced it, for 76 days.
> **Why no gate sees it:** every id in the list resolves, the list is non-empty, and the guard still refuses the delete — it refuses it *for the right reason with the wrong arithmetic*. A duplicate is only visible if you compare `arr.length` against `new Set(arr).size`, which nothing does. **Fix:** dedupe at the push site (`if (!this._questsByNode[q[field]].includes(id))`) or build the index through a `Set` and materialise it once in `_buildIndexes()`; prefer the `Set`, since `_questsByWaypoint` and `_questsByNpc` are built in the same loop and should not drift apart. **Verify:** a test asserting `new Set(list).size === list.length` for every key of `_questsByNode`, plus a spot-check that `NUE`'s count falls from 186 to 177. **Risk:** low — the fix removes entries that were never distinct, so no consumer loses a real reference. **Player impact:** none directly (authoring-time), but an inflated `blockedBy` list is how an author decides a node is un-deletable.

- [ ] **§DX-02be — a retired node code inside a *runnable* fenced example is invisible to `check:legacycodes` by construction, and `CI` is the worse-than-dead case (NEW 2026-08-13 during §DOC-02aq).** 🟡 **no design call on the census; a small one on the fix's form.** §AUDIT-03m fixed the rule deliberately: **a fenced block is code and is never annotated**, so a dead code inside a copy-pasteable command *"is not caught by the gate and must be fixed by hand."* One was, at the time (`./api.sh get node CY` → `HKG`). Nobody has since asked how many others there are.
> **`lab-report-wbapi.md` alone held six.** `WBAPI.location.get('CI')`, `WBAPI.npcs.byNode('CI')`, the `world/CI/` export tree, the `sync` example's `world/CI/npcs/yael/…` path, `./api.sh location CI`, and `node wbapi-cli.js get location CI` — plus eleven `CY`. **`CY` is merely dead** (gone from `NODE_MAP`; the command fails loudly, which is the harmless outcome). **`CI` is the §AUDIT-03m *worse-than-dead* class in pure form:** it still exists at HEAD as `num:429`, *"Chancery Court — The Officer's Pen"*, a different place in a different act, so `./api.sh location CI` returns a fast, confident, completely wrong answer to an author who copied it out of a lab report. By `num` the true targets are `CI`→**`LHR`**(1), `CY`→**`HKG`**(6), `BK`→**`VBY`**(25).
> **Fix — census first, then decide the form.** (a) Extend `src/scripts/legacy-codes.js` with a **fenced-block phase** that *reports* (never annotates) legacy codes inside code fences across all docs, HISTORY included — reporting is the §AUDIT-03m PENDING precedent and does not violate annotate-don't-rewrite. (b) For the worse-than-dead subset specifically — a code that resolves at HEAD to a **different** node than it named — escalate from report to **fail**, since that is the only class where the reader gets a plausible wrong answer instead of an error. **Recommend both, (b) scoped to the ~5 codes the LEGACY CODE MAP can prove moved.** §DOC-02aq retired the six blocks in `lab-report-wbapi.md` rather than annotating them, on the grounds that the live command surface is `docs/api/wbapi-help.md` — but that is a per-report judgement, not a rule, and the census is what tells us whether it needs to become one. **Verify:** the new phase's count on the corpus, and a positive-control test planting `./api.sh location CI` in a scratch doc. **Risk:** low — reporting-only by default.

- [ ] **§DX-02bf — the live CLI still prescribes three repair tools whose entire input contract is a field set that was stripped to zero, and one of them returns a green badge over the empty set (NEW 2026-08-13 during §DOC-02at).** 🟢 **no design call.** §CELL-01 removed the per-node `N`/`S`/`E`/`W` link fields; §WALK-3 Inc 2 retired `rip-and-connect`, `fill-gap` and `reweave-all` to **HTTP 410**, and `src/api/wb.js:951` documents the removal properly. **Three siblings were left behind with no such fence.** (a) `node src/tools/layout-solve.js --apply` is recommended at **eight live sites** — `src/api/wb.js:947`, `:1104`, `:2896`, `:2980`, `docs/api/wbapi-help.md:81`, `docs/api/API-README.md:387` and `:453` — and `src/tools/layout-solve.js` (604 lines) opens by describing itself as *"exact constraint-propagation grid layout for **N/E/S/W networks**."* Measured at HEAD: `N:` 0, `S:` 0, `E:` 0, `W:` 0. (b) `./api.sh fix-bidirectional` runs clean and prints **`✓ 0 bidirectional violations found`** — a passing result computed over zero inputs, which an author reads as *"the link graph is healthy"* when there is no link graph. (c) `./api.sh junction-audit` still ships (see §AUDIT-03ax for what it reports).
> **The sharpest single site is `src/api/wb.js:2976–2984`**, where `layout-solve --apply` sits *inside* the help block headed **"Full world reset sequence (cell-first, §WALK-1.5 geo flood)"** — a cell-first procedure prescribing the edge-graph solver — five lines above the `NOTE (§WALK-3)` that explains the edge graph is retired. The note and the instruction contradict each other in one screen of output.
> **EXTENDED 2026-08-14 (§DOC-02bd) — two more siblings, and the retirement note sits three lines under a runnable block of them.** `fix-all-broken` and `fix-diagonal` belong to the same retired set and were not named above. Measured: **0 occurrences in `src/js/wbapi-server.js`** (against 5 and 4 respectively at `89fa13b`, so the routes are gone, not merely unused) while `src/api/wb.js` still carries both at **4 sites each** — the CLI accepts the command and there is nothing to answer it. **9 live doc sites:** `docs/api/API-README.md:65`, `:66`, `:282`–`:287`, `:401`, and `docs/api/wbapi-help.md:89`–`:90`. **The sharpest is `docs/api/API-README.md:280–290`:** a fenced, copy-pasteable block prescribing `fix-diagonal`, `fix-all-broken` and `fix-bidirectional` — closed **three lines later** by the doc's own paragraph headed *"Retired (§WALK-3)"*, which retires `reweave`/`reweave-all`/`fill-gap`/`rip-and-connect` and not these. Same class as §DX-02l-FU: **a runnable shape inside a fence is what an author copies**, and a retirement note that lists four of seven siblings reads as a complete inventory. Fold both into this row's fix — annotate or delete all five call sites, 410 the routes that still answer.
> **Why no gate sees it:** `check:anchors` resolves symbols in the HTML, not tool contracts; `check:noderegs` scans node *references*, not field *existence*; and a tool that no longer has input does not crash — it succeeds trivially. **Fix:** (a) 410 the `POST /api/layout/apply` endpoint and delete or `RETIRED:`-annotate the eight recommendation sites, matching the §WALK-3 treatment its siblings already got; (b) make `fix-bidirectional` **refuse** rather than pass when the direction-field census is zero — *"no N/S/E/W fields exist (§CELL-01); nothing to check"* — since a green result over an empty set is the §DOC-02 instrument-16 shape (an invariant enforced perfectly against an exception that cannot fire). **Recommend both; (b) is the one that prevents a false all-clear.** **Verify:** `./api.sh fix-bidirectional` exits non-zero with the explanatory message; `grep -c "layout-solve" src/api/wb.js docs/api/*.md` returns only annotated hits. **Risk:** low — authoring-time only, no player-facing behaviour. **Origin:** §DOC-02at's §VII, whose *"next structural step"* was this same command, never executed and now unexecutable.

- [ ] **§DX-02bi — the Worldbuilder's "Apply Layout" button is a live one-click destroyer of `NODE_COORDS`, and the solver it trusts scores a perfect 0/0 over an empty relation (NEW 2026-08-13 during §DOC-02av).** 🟠 **no design call on the diagnosis; the fix is a choice between 410 and a guard.** `GET /api/layout/solve` BFS-walks `nodeMap[code][N|S|E|W]`. §WALK-1/§CELL-01 removed every one of those fields: **`NODE_MAP` at HEAD has 0 `N:`/`S:`/`E:`/`W:` link fields across 416 nodes.** Re-running the shipped algorithm against HEAD's data returns **`Nodes placed 416 / 416`, `Orphans 415`**, and a validation block reading **`Aligned pairs 0 ok · 0 misaligned` / `Within 4-cell 0 ok · 0 over`** — a flawless score over nothing, which is instrument-16's shape and §DX-02bf's class a second time.
> **The destructive half is the button.** `edit.html:5008 runLayoutSolve()` renders that block, prints *"415 disconnected nodes will be placed below the main grid"* in warning yellow, and **reveals `btn-layout-apply`**. `POST /api/layout/apply` (`src/js/wbapi-server.js:7577`) validates only that each `{r,c}` is numeric, then **rewrites the entire `NODE_COORDS` section** between the ◆ markers. Since every node is an orphan, the solver's orphan pass lays all 416 in **one horizontal row** at `maxR + step*3`, stepping `oC += step` — so a single click replaces §WALK-1.5's geo-seeded 90×360 grid with a line. Every terrain-field assumption, every `ROAD_CELLS` run and every sea-mask relationship goes with it.
> **Why no gate sees it:** the gates run on the *file*, and nothing has run yet. `check:invariants`/`check:roads` would go red *after* the damage, which is post-mortem, not prevention; `check:noderegs` checks that node references resolve, not that a coordinate is sane. **Fix options:** (a) **410 `POST /api/layout/apply`** and hide the Grid Layout panel, matching the §WALK-3 treatment `reweave-all`/`rip-and-connect`/`fill-gap` already got — **recommended**, it is the same retirement its siblings received and this endpoint is strictly more dangerous than they were; (b) make `solve` **refuse** when the direction-field census is zero (*"no N/S/E/W fields exist (§CELL-01) — the edge-graph solver has no input"*) and leave `apply` for the geo-seed path that still legitimately uses it; (c) require an `--execute`-style confirm token in the body. **Recommend (a), with (b) as the minimum if any caller still needs `apply`.** Note `src/tools/layout-solve.js --apply` is prescribed at **eight sites** already catalogued by **§DX-02bf** — close these two together. **Verify:** the Grid Layout panel is absent or its Solve refuses with the explanatory message; `git diff --stat play.html` empty after clicking through the panel; `check:walk` 16/16. **Risk:** low to fix, high to leave — authoring-time only, but a single mis-click is a full-world coordinate loss recoverable only from git.

- [ ] **§DX-02bj — 5.35 MB of monitor scratch state is tracked in git, and the `.gitignore` lines a lab report credits for excluding it were never written (NEW 2026-08-13 during §DOC-02av).** 🟢 **no design call.** `milepoints/patches/_last.html` (**5,351,102 bytes**) and `milepoints/patches/_last.name` are the snapshot monitor's "previous file" scratch pair — pure transient state, regenerable, rewritten on every snapshot. **Both are tracked**, added by `1d2661d` (2026-06-04 11:27) *"License — MIT License Paul Richeson across all .sh and .md files"*: a bulk licence-header sweep that swept in a 5 MB HTML file nobody was looking at. `.gitignore` covers `milepoints/patches/*.patch`, `*.patch.log` and `_base.html.gz` — it has **never** carried `_last.html` or `_last.name`, and `git check-ignore` confirms neither is ignored at HEAD. **The origin is instructive:** `lab-report-map-audit-layout-tooling.md` §2.4 published a `.gitignore` block listing both files under the heading *"All transient files excluded"* — written the next day, asserting an exclusion that had already been made impossible, because **a `.gitignore` line cannot un-track a file that is already committed.** **Fix:** `git rm --cached milepoints/patches/_last.html milepoints/patches/_last.name` + add both patterns; check whether `src/scripts/cleanup-cruft.sh` should cover them. **Verify:** `git ls-files milepoints/patches/` is empty; `git check-ignore -v` reports both; monitor still runs a snapshot cycle. **Risk:** none — the files are regenerated on the next snapshot. **Sibling:** the repo-size question this raises (what else came in on a bulk sweep?) is worth one `git ls-files -s | sort -k4` pass, not a row of its own yet.

- [ ] **§DX-02bg — `./api.sh reweave` is a live WRITING command wearing the name of a retired nine-phase pipeline, and the guide that documents it calls the bare invocation a no-write dry run (NEW 2026-08-13 during §DOC-02au).** 🟡 **no design call on the diagnosis; a small one on the fix's form.** §CELL-06/§WALK-3 Inc 3 retired MegaReWeave — `POST /api/graph/reweave-all` returns **410** and its ~3,200-line body is deleted. **The CLI verb was not retired with it.** `src/api/wb.js:509 async reweave(pos, flags)` now issues `PUT /api/roads`, which at `src/js/wbapi-server.js:7264` *"Runs `src/scripts/build-roads.js --apply` (patches between the ◆ §NAV-01b markers)"* — an unconditional write to the game file. This is CONTRIBUTING's retired-vocabulary hazard (§DOC-02c) in its most expensive form: **not a dead reference that errors out, but a live command that succeeds and writes.**
> **The author-facing trap is exact.** `lab-report-mega-reweave.md` §Running it published, and the §DOC-02au rewrite preserves as history: `` # Dry-run (reports only, no writes) `` / `` ./api.sh reweave ``. **That exact string is now an unconditional write to `ROAD_RUNS`.** And `reweave(pos, flags)` **destructures `flags` and never reads it**, so `./api.sh reweave --execute` and `./api.sh reweave` are byte-identical in behaviour — the dry-run/execute distinction the whole guide is built around is gone with no diagnostic. A second guide, `lab-report-junction-reweave-overhaul.md`, is still in `docs/lab-reports/` documenting the same string the same way (HISTORY — annotate, don't rewrite).
> **Why no gate sees it:** the command resolves, the endpoint returns 200, `build-roads.js` is the correct tool for the road net, and `check:roads` passes afterwards. Nothing is broken — the wrong thing is done successfully, which is Hazard #2's *a write into a real-but-wrong object never throws*, promoted to the CLI surface. **Fix options:** (a) make `reweave` **require `--execute`** and print a dry-run summary otherwise, restoring the affordance every historical doc assumes — **recommended**, one `if (!flags.execute)` guard and it also makes the verb honest for anyone who learned it from the old guides; (b) rename to `roads-reweave` / fold into `./api.sh roads --apply` and 410 the bare verb, which is cleaner but breaks a name §NAV-01h ships in help text; (c) leave it and annotate. **Recommend (a); it is the only option that protects the author who copies from history.** **Verify:** `./api.sh reweave` prints a dry-run summary and exits 0 without touching the file (`git diff --stat play.html` empty); `./api.sh reweave --execute` behaves as today; `check:roads` green both ways. **Risk:** low. **Player impact:** none directly, but an accidental `build-roads --apply` rewrites `ROAD_RUNS` between the §NAV-01b markers, which is a player-visible travel change.

- [ ] **§DX-02bh — `worldmap --regions` buckets by the bounds of a projection retired by §WALK-1.5, and exactly one city has fallen off the western edge (NEW 2026-08-13 during §DOC-02au).** 🟢 **no design call, a two-number fix.** Node *placement* moved to §WALK-1.5's equirectangular 1°/cell grid — `` `row(lat) = clamp(floor(LAT_N - lat), 0, ROWS-1)` `` and `` `col(lon) = mod(floor(lon + 180), COLS)` ``, defaults `latN=70, latS=-20, rows=90, cols=360`, wrapping the full globe. The comment that introduced it names the defect it replaced: *"Replaces the old linear 8–500 box-fit (which was **NOT equal-degree: 6.47 cells/° lat vs 5.07 cells/° lon**)."* **`src/tools/worldmap.js:224–227` still carries the old box** — `minLat:-8, maxLat:68, minLon:-25, maxLon:72` — and `citiesInRegion` filters `GEO` against it with strict comparisons and **no clamp**.
> **Measured at HEAD: of the 155 cities in worldmap's `GEO` table, exactly one is outside the box** — `` `src/tools/worldmap.js:PDL: { lat: 37.7, lon: -25.7, label: 'Ponta Delgada'@217` `` — missing `minLon:-25` by **0.7°**. It is a real live node (`PDL` `num:19`, *Island Shore*, `islands`, act 4, NPC **Oracle's Apprentice**, cell `32,154`, reachable in the 416/416 flood) and it renders in **zero** regions: confirmed by running `./api.sh worldmap --regions`, 0 occurrences of `PDL` across 19 populated region cells. Everything else lands, so the failure is a silent single-city omission rather than a visibly broken grid.
> **The pairing is the lesson.** §DOC-02at's report contributed exactly one surviving artifact — the fix that turned this same overview from `NaN×NaN` into `6×6` (`src/tools/worldmap.js:1201–1202`, still byte-identical). ***`NaN×NaN` fails loudly; `6×6` fails silently. Making a broken surface render is not evidence that it renders everything*** — and nobody re-checked coverage after the repair. **Fix:** widen `MAP` to the live projection's envelope (`minLat:-20, maxLat:70`, and either `minLon:-30` for the Atlantic tail or the full `-180..180` now that placement wraps), or clamp out-of-range cities into the edge bucket rather than dropping them. **Prefer widening** — clamping would put the Azores in the same cell as Iceland. **Verify:** `./api.sh worldmap --regions` lists `PDL`; a test asserting every `GEO` key appears in exactly one region cell (the positive control that would have caught this). **Risk:** low, display-only.

- [ ] **§AUDIT-03ay — the Worldbuilder's Map Audit reports a clean graph because it is auditing a graph that has no edges (NEW 2026-08-13 during §DOC-02av).** 🟢 **no design call.** `GET /api/audit/map` runs **12 rules**; **8 of them are edge-driven** — `diagonal_exit`, `max_connections`, `dangling_link`, `bidirectional`, `direction_sign`, `long_link`, `alignment`, `axis_distance` — and every one iterates `nodeMap[code][N|S|E|W]`. `NODE_MAP` at HEAD holds **0** such fields (§WALK-1/§CELL-01), so all eight return **0 findings by construction, forever**. The `Map Audit` button in `edit.html:1030` therefore renders an empty list, `Fix All` never appears, and the Audit tab reads as a passing grade. Only `density`, `market_proximity`, `missing_coords` and `corner_misalign` still have input, and they are coordinate rules, not connectivity ones. **This is §DX-02bf's class at a second site** — an invariant enforced perfectly against an exception that cannot fire — but on the **UI** surface rather than the CLI, so the §DX-02bf fix (make `fix-bidirectional` refuse) does not reach it. **Fix:** have the map-audit handler census the direction fields first and, when the count is zero, return an explicit `notice` the panel renders instead of an empty list — *"0 N/S/E/W link fields exist (§CELL-01); 8 of 12 connectivity rules have no input and were not run"* — listing which rules were skipped. That is honest in both worlds and survives the edge graph coming back. **Sequence after §DX-02bf** (same message, same census helper) **and beside §DX-02bi** (same panel). **Verify:** the Audit tab shows the notice, not silence; `./api.sh audit --map` prints the same line; the four live rules still report normally. **Risk:** none — reporting only. **Origin:** §DOC-02av's HEAD-status pass on `lab-report-map-audit-layout-tooling.md`, whose entire §4 is the eight rules in question.

- [ ] **§DX-02bk — `POST /api/graph/nuke-junctions` is live at HEAD, decides what a junction is from the first character of the key, and against HEAD's data selects exactly one node: a content node with an NPC scene on it (NEW 2026-08-13 during §DOC-02aw).** 🟠 **no design call on the diagnosis; a small one on 410-vs-guard.** The handler (`src/js/wbapi-server.js:6629–6882`, ~250 lines) and its CLI (`src/api/wb.js:1416`) both survived §WALK-3's retirement sweep that sent `reweave-all`, `rip-and-connect` and `fill-gap` to **HTTP 410**. It classifies with `` `const jCodeRe = /^J\d+$/;` `` and builds its protection sets from quest `activateNode`/`waypointNode` and `birkaNpcs[].node`. **At HEAD exactly one key matches: `J13`** — and `J13` is not scaffolding. It is `` `J13:{ num:103, code:'J13', name:"midlands", label:'The Western Sea Road', act:4` ``, a promoted Act-4 node with two paragraphs of prose at cell `19,174`, **and the holder of the only `J`-keyed `JUNCTION_VIGNETTES` entry — `npc:'The Cartographer'`, three dialogue lines and a map-fragment gift, rendered live by `` `if (JUNCTION_VIGNETTES[node.code]) {` `` in `_renderNodeShell`'s vignette branch.**
> **Every guard passes and every gate is blind.** `J13` carries `npc:null` in its `NODE_MAP` entry, so `birkaNpcs[].node` never mentions it and `JUNCTION_VIGNETTES` is not consulted; it has 0 quest refs. The endpoint prints `` `[nuke] safety check PASSED — 0 J#### have quest/NPC refs` `` and proceeds. With `N`/`S`/`E`/`W` stripped by §CELL-01, `liveDirs` is empty, so it is classified a **dead-end** and deleted outright from `NODE_MAP` **and** `NODE_COORDS`. On the gate side: `check:invariants` **I1** tests terrain totality (`midlands` resolves), **I2** counts `junction:true` (0), **I3** tests reachability (415 nodes flood as cleanly as 416). **Nothing in the repository classifies by key prefix except the thing that deletes.**
> **Why this has never been noticed, and why that is the worrying part.** All three write sites use `` `WBAPI.saveStamped()` `` (§DX-02k), which resolves to `save(path.join(base, getStampedName()))` — a **gitignored dated sibling** (`play-2*.html`, §DX-02l). The live file is untouched, `git status` stays clean, and the operation looks inert. **But the running server's in-memory `nodeMap`/`nodeCoords` are mutated and the source string is rewritten**, so the next endpoint that calls `saveGameFile()` — the temp-and-rename that owns `GAME_FILE`, called from four other sites — commits the deletion to `play.html` under an unrelated verb. The loss is **staged, not contained**. ***This is §DOC-02av's headline mechanism with the sign flipped: the stamped-sibling path loses repairs and hides deletions.***
> **Fix options.** (a) **410 the endpoint**, the treatment its three siblings already got — recommended, since its input class (`junction:true`) has been empty since §WALK-1 and its documented follow-up phase (A\* reconnect) never existed; (b) keep it and re-key the guard to the `junction:true` **flag** plus a content census (`npc`, `battle`, `loot`, `questCount`, and a `JUNCTION_VIGNETTES` lookup), refusing when any J-keyed node has content; (c) leave it and fix the data instead — **§AUDIT-03az**. **Recommend (a) now and (c) as the permanent close**; (b) only if the reweave era is ever revived. **Close together with §DX-02bf** (same retirement sweep, same class). **Verify:** `./api.sh nuke-junctions` returns 410 with the §WALK-3 note; `check:walk` stays 16/16; `J13` and The Cartographer still render. **Risk:** none — the capability has had no valid input for fourteen months. **Origin:** §DOC-02aw's HEAD-status pass on `lab-report-junction-reweave-overhaul.md`, whose §5 this endpoint implements and cites by name.
> **EXTENDED 2026-08-13 (§DOC-02ay) — the mechanism is pinned, the ORIGIN is one section earlier than we thought, and the sibling verb is the control group.** Live dry-run at HEAD confirms the row exactly: `[nuke] questRefNodes=388  npcRefNodes=121` → `[nuke] safety check PASSED — 0 J#### have quest/NPC refs` → `[nuke] classified: safe=1  straight=0  L-shaped=0  dead-end=1`. **The guard is not reading the wrong field — it is reading a different map.** `src/js/wbapi-server.js:parts[1] === 'nuke-junctions'@6643` builds `npcRefNodes` from `WBAPI.birkaNpcs[].node` (= `BIRKA_NPC_PROFILES`), but The Cartographer lives in `const JUNCTION_VIGNETTES@26556`, which is keyed **by node code** and has **no `node:` field on any entry** — so no lookup over `.node` can ever surface `J13`, and `JUNCTION_VIGNETTES` is never consulted. This makes fix option (b)'s `JUNCTION_VIGNETTES` census not optional but the whole guard. **The control group is the sibling verb:** `POST /api/admin/delete-junctions` (`src/js/wbapi-server.js:parts[1] === 'delete-junctions'@8337`) gates on `if (!node.junction) continue` — and §CELL-05b deleted that field from every node, so it returns `toDelete: 0` and is inert. **The verb that checks what a junction *is* is harmless; the verb that checks what a junction is *called* is armed.** **True origin:** `lab-report-cell-map-mud-redesign.md` §CELL-05, which promoted `J13` and `WRO` to named midlands nodes and gave only `WRO` a real code — the exposure is two months older than the reweave era.

- [ ] **§DX-02bl — three live endpoints answer "is the world connected?" from three different graphs, and the two that are wrong are the two that write (NEW 2026-08-13 during §DOC-02ax).** 🟠 **no design call on the diagnosis; a small one on 410-vs-notice.** Measured live against HEAD in one server process, one minute apart:

| Surface | Reachability model | Answer |
|---|---|---|
| `GET /api/graph/reachability` | §WALK-1.5 land flood over passable cells (`src/js/wbapi-server.js:NOT node-to-node adjacency@5495`) | **416 / 416, 0 clusters** ✅ |
| `GET /api/graph/junction-audit` | BFS over *cell-touching* adjacency (`src/js/wbapi-server.js:function degree(code)@5385`) | **2 of 415** named reachable |
| `POST /api/graph/cluster-bridge` | BFS over `node.N`/`S`/`E`/`W` — of which there are **zero** (`src/js/wbapi-server.js:const cbAdj = new Map()@6930`) | **1 of 416**, 415 clusters |

> **`cluster-bridge` is the serious half, because it acts.** Its adjacency is built from `node[d]` for the four compass fields §CELL-01/§WALK-1 stripped to 0, so every set is empty, `bfsReach('LHR')` returns `{LHR}`, and every other node becomes its own "isolated cluster." Live dry-run at HEAD: `` `[cluster-bridge] hub=LHR  reachable=1/416  unreachable=415` `` then `` `415 isolated clusters` `` — `cluster 1: 1 nodes named=[BMA]`, `cluster 2: … [TLL]`, and so on through `cluster 415: … [TVR]`. **With `{execute:true}` it iterates all 415**, calling `POST /api/graph/smart-connect` for a plan and then **two `PUT /api/node/<code>` writes each — up to 830 writes that re-introduce the exact `N`/`S`/`E`/`W` link fields the cell-first migration removed.** Unlike §DX-02bk it does not use `saveStamped()`: the PUTs go through the normal node-write path, so the damage lands in `play.html` directly. It is published as a green CLI verb under *"§22 NETWORK HEALTH & REPAIR"* (`src/api/wb.js:3153`, usage at `src/api/wb.js:1435`, prose at `docs/api/API-README.md:295`) and shares the no-timeout streaming path with `nuke-junctions` (`src/api/wb.js:111` names the two together).
> **`junction-audit` is the reporting half, and it disagrees with itself.** It reports `namedUnreachable: 413` from the cell-touching BFS while `./api.sh reachability` reports 0 — and inside one response body `junctionDegreeDist` puts `J13` at **degree 2** (adjacent occupied cells) while `nukePreview` classifies the same node **`deadEndDelete`** (degree ≤ 1, from `liveDirs` over the empty compass fields). Two degree functions, one payload, different graphs.
> **Fix options.** (a) **410 `cluster-bridge`**, the treatment `reweave-all` / `rip-and-connect` / `fill-gap` already got — recommended, since its input class has been empty since §WALK-1 and the corridor builder it was written around (`buildHighway`) was deleted with the reweave body; (b) keep it and re-source its adjacency from the land flood, which changes it from a bridger into a no-op that reports 0 clusters; (c) leave it and add a refusal when the direction-field census is 0. For `junction-audit`, the cheap honest fix is §AUDIT-03ay's shape: census the direction fields first and emit an explicit notice — *"0 N/S/E/W link fields exist (§CELL-01); reachability below is cell-adjacency, not walkability"* — rather than a number that reads as a catastrophe. **Recommend (a) + the notice.** **Close as one retirement sweep with §DX-02bf, §DX-02bi and §DX-02bk** — all four are the same defect: *a repair tool that outlived the breakage it repaired, still confidently measuring a graph that no longer exists.* **Verify:** `./api.sh cluster-bridge` returns 410 with the §WALK-3 note; `./api.sh junction-audit` prints the notice; `check:walk` stays 16/16; `./api.sh reachability` unchanged at 416/416. **Risk:** none — reporting only, and the capability has had no valid input for fourteen months. **Origin:** §DOC-02ax's HEAD-status pass on `lab-report-highway-mesh-entry.md`, whose `buildHighway` these three endpoints are the surviving descendants of.
> **CORRECTED 2026-08-13 (§DOC-02ay) — the write hazard is NIL, and the thing that defuses it is §CELL-08.** This row said the 830 PUTs *"re-introduce the exact `N`/`S`/`E`/`W` link fields"* and that *"the damage lands in `play.html` directly."* **It cannot.** `cluster-bridge`'s two writes are `PUT /api/node/<code> {N:…}` (`src/js/wbapi-server.js:const putA = await httpReq@7036`), and the same migration that emptied the fields installed a guard that rejects them: `src/js/wbapi-server.js:_badPutFields@11043` 400s any node PUT carrying `N`/`E`/`S`/`W`/`junction`. **Proven against a live server (port 1367) at HEAD:** `PUT /api/node/WRO {"N":"J13"}` → `400 "Fields N are deprecated on node PUT…"`; `PUT /api/node/J13 {"S":"WRO"}` → 400; `POST /api/node {…,"N":"WRO"}` → 400 (`src/js/wbapi-server.js:_badNodeFields@10145`). `play.html` was byte-identical before and after (`md5 ce492bacf634e5580e5664827eaf599e`). An `--execute` run therefore prints `✗ put failed` **415 times** and writes nothing — `bridged=0`. **Severity drops from a corruption risk to a reporting defect**; fix option (a) is still right, but for honesty, not safety. **A fourth surface belongs in the table:** `GET /api/grid/reachability` (`src/js/wbapi-server.js:if (sub === 'reachability')@8623`) answers **2 / 416** — it *did* get its §CELL-06 cell-grid migration and BFSes `CELL_GRID` correctly, but over **named-node** adjacency, so in a world of 416 nodes across 32,400 cells the honest answer is permanently ~0. **That is the more interesting failure: not a stale graph, but a successful migration that produced a meaningless metric under a name that reads like walkability.** **Deeper origin:** `lab-report-cell-map-mud-redesign.md` §CELL-06, which promised to migrate five algorithms and migrated one.

- [ ] **§DX-02bn — the auto-junction rule is 68 lines of unreachable code that mints the exact entities §CELL-05 was written to destroy (NEW 2026-08-13 during §DOC-02ay).** 🟡 **no design call — delete it with the retirement sweep.** `src/js/wbapi-server.js:const autoJunctionEnabled@11054` implements a rule from the corridor era: when a node PUT sets a **4th** compass direction on a node already at degree 3, mint a new `J<n>` junction node and give it the 4th slot. It reads as live: it is 68 lines, it has `logTrace` instrumentation, a documented bypass flag (`autoJunction:false`), and it computes the next free `J` number from `/^J\d+$/` over `NODE_MAP`. **It cannot run.** Its trigger requires `dirField in body && body[dirField]` — and `src/js/wbapi-server.js:_badPutFields@11043`, **eight lines above it**, returns 400 on exactly that condition. Every path that would reach the auto-junction block is terminated by the §CELL-08 guard first. Confirmed empirically alongside §DX-02bl's correction: `PUT /api/node/WRO {"N":"J13"}` → 400, no mutation, file byte-identical. **Why it matters beyond dead weight:** this is the only code left in the repo that *creates* `J`-coded nodes, and `J`-coded nodes are the input class of §DX-02bk (`nuke-junctions` deletes by code pattern and its guard is blind to `JUNCTION_VIGNETTES`). A future edit that relaxes the §CELL-08 guard — for a migration, a bulk import, an `--force` flag — silently re-arms a junction *factory* whose output the junction *nuker* will later eat. **Fix:** delete the block and the `autoJunction` body flag with it; the `if (field === 'autoJunction') continue` skip in the field loop goes too. **Verify:** `grep -c autoJunction src/js/wbapi-server.js` → 0; `check:walk` stays 16/16; `PUT /api/node/<code> {"N":…}` still 400s. **Risk:** none — it has been unreachable since §CELL-08 shipped on 2026-06-14. **Close with §DX-02bf · bi · bk · bl** — same sweep, same defect class: *machinery that outlived the world it was built for*. **Origin:** §DOC-02ay's verification pass on `lab-report-cell-map-mud-redesign.md`.

- [ ] **§DX-02bm — `TERRAIN_ENCOUNTER_RATE` still carries a `junction` rate the engine documents having removed 3,400 lines earlier (NEW 2026-08-13 during §DOC-02ay).** 🟢 **one key, no design call, ~2 minutes.** `const TERRAIN_ENCOUNTER_RATE@9892` lists `road:0, junction:0` among its 15 terrain rates. **`junction` is unreachable.** `_inferTerrain@28385` can return exactly three things: `'ocean'` (a `SEA_LANES` cell, §WALK-1.5), `'road'` (a `ROAD_CELLS` cell, §NAV-01b), or the `name` of a live `NODE_MAP` neighbour by plurality vote — and `name:'junction'` has **0 occurrences** since §CELL-05b stripped it. The engine already says so: the comment at `play.html:6402` reads *"the 'junction' terrain was removed here — orphaned after §WALK-1/§CELL-05 deleted all junction nodes (0 nodes reference it; nothing reads `WORLD_DB.junction`)"* — the note landed, the key did not. **This is the §DX-02y read/write-census shape:** a gate scoped to *unread fields* cannot see a key that is read on every empty-cell step and simply never matches. **Fix:** delete `junction:0` from the literal; leave `road:0`, which is load-bearing (it is what makes the fungal road net a safe corridor and the wilderness a real trade-off). **Verify:** `grep -c "junction:0" play.html` → 0; `check:walk` 16/16; `check:terrain` green; encounter rates on road cells unchanged. **Risk:** none — no code path can select the key. **Neighbour:** fold into the `check:deadconsts` census §DX-02y is growing. **Origin:** §DOC-02ay's verification pass on `lab-report-cell-map-mud-redesign.md`.

- [ ] **§AUDIT-03az — finish the promotion `J13` only half received: give the Cartographer's node a key that does not read as scaffolding (NEW 2026-08-13 during §DOC-02aw).** 🟡 **small design call on the replacement code; the procedure is already written.** §9 of `lab-report-junction-reweave-overhaul.md` specified promotion precisely: assign the coordinates, merge the links, **delete the junction entry**, and give the result its own terrain key rather than `"junction"`. `J13` obeys every **field** rule — `name:"midlands"`, no `junction:true`, real label, real prose — and skipped the **identity** rule: it was promoted **in place**, keeping the `J` key. That single omission is what makes §AUDIT-03ax's two instruments disagree and what makes §DX-02bk's endpoint able to delete a live NPC scene; re-coding the node closes both at the data layer, permanently, in the one place a future tool cannot get wrong.
> **Scope is small and fully enumerable — but this is the §EPIC-01 hazard class, so enumerate before editing.** `J13` appears in exactly three places in `play.html`: the `NODE_MAP` definition, `` `J13:{r:19,c:174},` `` in `NODE_COORDS`, and the `JUNCTION_VIGNETTES` key. It has **0** quest `activateNode`/`waypointNode` refs, 0 `NODE_HOOKS`, 0 player-facing string mentions. **Grep all three registries plus `docs/` and `docs/lab-reports/` before the rename** — §EPIC-01's 40 orphaned quests are what happens when a rename misses the sites that *compute* an id. **Design call:** the replacement code. It is a west-coast sea road in Act 4 near cell `19,174`; `WSR` (Western Sea Road) matches the label, and the §DX-01e house rule is that the code should be readable as the place, not as the terrain. **Verify:** `./api.sh junction-audit` reports `Junctions: 0` with no fossil line (closing §AUDIT-03ax option (a) properly); `check:invariants` unchanged at 416/416; `check:nodeindex`, `check:noderegs`, `check:legacycodes` green; standing on the node still renders The Cartographer. **Sequence after §DX-02bk** so the destructive endpoint is fenced before the data moves. **Risk:** low, but non-zero — it is a node-code rename, and the repo has one expensive precedent.

- [ ] **§AUDIT-03ax — `J13` is the last reweave-era elbow junction, re-terrained into content, and the two instruments that count junctions disagree about it (NEW 2026-08-13 during §DOC-02at).** 🟢 **no design call, classification only.** The 2026-06 reweave cascade peaked at **4,045** `junction:true` nodes (`9833f48`); §WALK-1 (`efa8f7a`) drove it to 0 and §DX-01a (`fa8f9e4`) removed the last two stubs `J14`/`J15`. **One survivor was not deleted but *promoted*:** `` `J13:{ num:103, code:'J13', name:"midlands", label:'The Western Sea Road', act:4` `` — re-terrained from `junction` to `midlands`, given a label and two paragraphs of node text (*"The sea to the right is the same sea — different coast, same water."*), and left in the world.
> **The disagreement is measurable and live.** `./api.sh junction-audit` classifies by the **`J`-prefix on the key** and reports `Total: 416 · Junctions: 1 (0.2%) · Named: 415`. `check:invariants` I1/I2 classify by the **`junction:true` flag** and report **0**. The project's own durable notes record *"0 junctions"*. Both are correct under their own rule; neither is wrong; an author reconciling them wastes the time this row exists to save.
> **`J13` is also content-empty:** `questCount: 0`, `questIds: []`, `npc:null`, `battle:null`, `loot:null`, `sleep:false`, `links:{}` — node text and nothing else, at cell `19,174`. It is reachable (the 416/416 flood includes it), so this is not a §WALK defect; it is a node that reads as scaffolding to every tool and as a place to the player. **Fix — pick one and record it:** (a) rename the key to a non-`J` code so both instruments agree it is content (cheapest, but a node-code rename is the §EPIC-01 hazard class — grep every reader first, and `J13` has 4 hits total, only 1 of them the `NODE_MAP` definition); (b) teach `junction-audit` to classify by the **flag**, matching `check:invariants`, and report key-prefix survivors separately as *fossils*; (c) leave it and add `J13` to an explicit `NOT_A_JUNCTION` list, the #13/#14 explicit-classification house style. **Recommend (b) + (c)** — (b) makes the two instruments agree, (c) records *why* one node keeps a junction-shaped key. **Verify:** `./api.sh junction-audit` reports `Junctions: 0` with a `1 fossil key (J13)` line; `check:walk` stays 16/16. **Risk:** low. **Do NOT do (a) without the grep** — this is exactly how §EPIC-01's 40 orphaned quests happened.

- [ ] **§AUDIT-03an — the death that writes a tattoo is the one death that never resets the run ledger, so the character sheet shows "This Life" and "All Lives" as identical columns forever (NEW 2026-08-12 during §DOC-02am).** 🟠 **no design call on the diagnosis; a one-line fix with a small call on where it goes.** The Chronicle keeps two ledgers precisely so a player can compare *this* life against *all* lives. **Both resets live at exactly one site each** — `` `S_story.runStats    = _STAT_ZERO();@23935` `` and `` `S_story.hour        = 0;@23936` `` — inside `` `function storyRespawnFromCheckpoint()@23924` ``, and that function has **two callers**: the game-over modal's `btn-gameover-respawn@38310` and `` `setTimeout(() => storyRespawnFromCheckpoint(), 800);@27932` `` in `_onPitChampionLoss()`. **Ordinary combat death reaches neither.** The death-save mini-game resolves to `_storyDeathSaveCrawl()` (3 successes → survive at 1 HP) or `` `function _storyDeathSaveFall()@25952` `` (3 failures), and the fall path writes the death tattoo, tallies `` `_statTally('deaths', 1);@25997` ``, strips the player, then **respawns inline** (`S_story.currentCode = S_story.checkpointNode || 'LHR'` → `storyEnter()`). It never opens the game-over modal and never calls the respawn function. **The player-visible consequence lands at exactly the wrong moment.** The character sheet suppresses the second column while `` `const isFirstRun = (cs.deaths || 0) === 0;@37711` ``; the first death flips that false and reveals an "All Lives" column that is **element-for-element identical** to "This Life", and stays identical for the rest of the save. So the feature's one interesting claim presents as broken the first time it is shown — worse than not shipping the column. **No symbol census could catch this**: every identifier resolves, the reset function is byte-correct against its 2026-05-25 specification, and the defect is a missing *call*, not a missing symbol. **Fix options:** (a) call the two resets from `_storyDeathSaveFall()` directly, before the inline respawn — smallest and most obvious, but duplicates two lines; (b) extract a `_beginNewLife()` helper holding both resets and call it from `_storyDeathSaveFall()` and `storyRespawnFromCheckpoint()` — **recommended**, since it makes "a new life starts here" a named, greppable event and prevents the third death path from repeating the omission. **Note the hour clock is the benign half** — never resetting a *world* clock is arguably more correct than resetting it, so `hour` should be left running on the fall path even when `runStats` is reset; the two lines are adjacent but should not be treated as one behaviour. **Risk:** low. **Verify:** die in combat, respawn, open the character sheet — "This Life" reads zeros while "All Lives" retains the pre-death totals; die a second time and the columns diverge further; the game-over-modal path and the pit-championship path still behave as before.

- [ ] **§DX-02av — eight skill-check quests carry an `onComplete` chain the engine can never execute, and the engine says so in its own comments (NEW 2026-08-12 during §DOC-02aj).** 🟢 **no design call.** `` `function storyCheckQuests@30168` `` reaches its completion branch only via `` `q.completion && QuestRuntime.canComplete@30189` ``, so a quest with **no `completion` block** can never become `'complete'` — and skill checks deliberately terminate elsewhere, at `` `S_story.quests[questId] = 'done'@6990` `` on the pass path. Census of all **2,824** UQF quests: **2,454 are `type:'skill_check'` and all 2,454 carry no `completion` block** (that part is the intended design, not the defect). **Exactly 8 of them also carry an `onComplete[]` chain, which is therefore dead code**, and the §ARCH-01 W7c migration flagged every one in place with `⚠ dead in legacy too: skill_check quests never reach 'complete' in storyCheckQuests`. **Five are in the §FUTURE-01 arc** — `quest_ezzir`, `quest_governor_cyprus`, `quest_lame_lystra`, `` `quest_stoning_lystra@11407` ``, `quest_basket_damascus` — and three are §DUNGEON-01 d02xx (`quest_d0206_a5`, `quest_d0208_a4`, `quest_d0208_a5`). **Nothing is unpaid** — XP and mission bits are granted by the `bits` chain at roll time — so the loss is the closing narrative line only. It is still worth fixing, because one of the five is the best sentence attached to the arc's best scene and no player has ever seen it: *"🪨 +100 XP. He got up. Everyone else left. Timael decided to stay."* **That line also misstates its own reward** — `quest_stoning_lystra`'s `onPass` grants `xp:150`, which is what a dead message costs you: nobody proof-reads text that never renders. **Fix:** move each narrative into the quest's `onPass` (where the other four members of its chain already live) or delete it; then drop the eight `⚠` comments. **Verify:** the census above returns 0. **Class:** not a §DX-02n dead-*field* — a dead **handler on a live entry**, which widens `check:deadconsts`' proposed scope again (after quest-entry fields, parameters, item objects and `||`-chain alternatives).

> **EXTENDED 2026-08-12 (§DOC-02ah) — the "related class, not yet measured" is now measured, and the worst instance is the one panel in the game whose whole point is being exact.** The Level-20 Quest −1 disclosure (`` `Layer 49: §XIV Quest -1@31383` ``, a `NODE_PANELS` entry on `TLS`) tells the player: *"the source file is **16,024 lines** of readable JavaScript. **MONSTER_POOL has 423 entries**. **WORLD_DB has 67 terrain entries**. QUEST_DB has a completion function for each quest."* Live: **38,712 / 398 / 111 / zero**. It has **one commit in the file's entire history** (`194a810`) and has never been edited. Each literal failed differently, which is why the row is worth reading before fixing: (a) **16,024 was a real `wc -l`** — commit `4090c82` is exactly 16,024 lines, taken **100 minutes before** the commit that shipped the string; (b) **423 matches no build ever** (`MONSTER_POOL` ran 372 → 382 → 385 → 398), and neither does the `grep -c "key:'"` recipe the same panel recommends — that recipe is the one §DOC-02i recorded unreliable, and the panel hands it to the player as authoritative; (c) **67 was exact** at authoring time and rotted anyway. Two more player-facing pointers in the same block: `` `QUEST_DB has a completion function for each quest@31396` `` describes the pre-§ARCH-01 `completeFn` schema (67 of 76 quests carried one at `0a131f5`; **0 of 2,853 do now**), and `` `plan.md §XIV describes the World Creator@31410` `` names a file **deleted by `5e48dd7`**. **Fix discipline is the same and matters more here:** do not swap in `38,712` — either drop the literals (*"tens of thousands of lines"*) or, better, have the panel *compute* what it can at render (`Object.keys(MONSTER_POOL).length`, `Object.keys(WORLD_DB).length`), which is both un-rottable and thematically exactly right for a panel about reading the source. Repoint `plan.md §XIV` → `CONTRIBUTING.md` + `BACKLOG.md`, replace the `key:'` recipe with `npm run stats`, and rewrite the QUEST_DB sentence for UQF-1.0. **Verify:** `grep -c "16,024\|423 entries\|plan.md §XIV" play.html` → 0; panel eyeballed at `TLS` at L20 per §7½.
- [ ] **§AUDIT-03ai — `quest_ng_02` activates into a state the player cannot leave (found 2026-08-12 during §DOC-02ah).** 🟡 The NG+ remembrance quest gates on `ngPlusRun ≥ 1 && priorQuestMinusOne` and completes on `` `completion:{ flags:['entry42Written'] }@11048` ``, with `activateNode:'LHR'`, `onActivate:null`, `boardExempt:true`. **The only writer of `entry42Written` in 38,712 lines is the inline Entry-42 journal surface at `LHR`, and that surface carries a second, undeclared condition the quest's gate does not:** `` `const _e42Dear@34621` `` requires **three of six** named NPCs (`yael`/`brynn`/`quill`/`pachelbel`/`crov`/`auros`) at `_npcFavor ≥ 2`. So a qualifying NG+ player below that threshold gets the quest activated silently, sees it listed with `hint:'Visit the City Inn to find the open page.'`, walks to `LHR`, and finds nothing — no textarea, no buttons, no explanation, and no other completion path anywhere in the file. **This is the §DX-02n *"write-only"* defect inverted: not a field nobody reads, but a completion flag nobody can write.** It is invisible to `check:questgraph` (which walks `QUEST_DB` against itself and sees a well-formed `completion` block) and to `check:noderegs` (`LHR` resolves). **Two fixes, and the choice is a small design call:** (a) **align the gate** — add the same three-friend test to `quest_ng_02`'s gate so it never activates early (needs a `favorMin`-count leaf the grammar does not have — cf. §DX-02r's `questsFailed` shape, widen the grammar, never patch the quest); or (b) **align the surface** — drop `_e42Dear` and let any qualifying NG+ player write Entry 42, on the argument that reaching L20, finding Quest −1 *and* starting NG+ is already the hardest gate in the game and a friendship test on top of it is a third lock on the same door. **Recommend (b)** — it is one deleted condition, it removes a silent dead end rather than adding a second gate to maintain, and the sibling quests `quest_ng_01`/`quest_ng_03` both gate on `ngPlusRun` alone. **Risk:** (b) makes the game's final authored beat reachable by more players, which is the intent of Layer 50. **Verify:** an NG+ save with `priorQuestMinusOne:true` and zero favour completes `quest_ng_02` at `LHR`; integration test under `src/tests/integration/`.
- [ ] **§DX-02au — Layer 49's three self-criticisms, correct and unshipped for 79 days (found 2026-08-12 during §DOC-02ah).** 🟢 The Quest −1 lab report closed with three "What Could Be Better" items; §DOC-02ah verified all three as **still true at HEAD** and none was ever converted into a tracked row — the §DOC-02i lesson (*a recommendation register nobody carries forward reports green by silence*) repeating in a report that wrote its own recommendations down. (a) **The disclosure can pre-empt the ending.** `` `>= 20 && !st.questMinusOne@31385` `` has no battle guard, and because movement is free (invariant #1) an L20 player can walk to `TLS` and read the developer disclosure **before** fighting the Void Warlord. The guard the report asked for already exists three thousand lines away — `` `if (!S_story.defeatedBattles['TLS']) return;@28211` `` in `storyCheckVictory` — so the fix is one clause copied from the same file. **Do this one; it is the whole row's value.** (b) **The three grep commands assume the source is on disk**; a player who loaded the game from a URL cannot run any of them (`grep -c "view-source"` → 0). (c) **No repository URL occurs anywhere in the game** (`grep -oE "github\.com"` → 0), so *"the markdown files in this directory"* is unactionable for a player. (b) and (c) are the same fix — one link or one view-source button — and both interact with §AUDIT-03u's rewrite of the same paragraph, **so sequence this row after that one and do them in a single edit.** **Risk:** (a) changes when a player sees the panel; if a design call prefers the current always-available behaviour, record it and close (a) as WONTFIX rather than leaving it silent.
- [ ] **§DX-02p — a level earned by PASSING a skill check delivers no level-up (found 2026-08-11 during §DOC-02e).** `_checkLevelUp()`@25671 reads like a predicate and is a **mutator**: it advances `S_story.level` immediately and pushes the new level onto `_levelUpQueue`. Every *benefit* of that level — the hit-die roll for max HP, the ASI at 4/6/8/12/14/16/19, the Fighter feature, the level-2/9/17 surge and indomitable charges — is delivered by `_showLevelUpModal`@25615 and its continue handler, and **the queue is the only channel between them**. Every drain in the file: `@7015` (skill-check **FAIL**, §XP-01 effort XP) · `@25187` `_storyEnemyFlees` · `@25300` battle victory · `@30080` `_grantExplorationXp` · `@38442` victory-overlay return · `@38676` modal chaining. **The skill-check PASS path is not among them** — `_resolveQuestUQF`'s pass branch@6989 runs `execBits`, credits the warrant, calls `storyRender`, and never checks the queue. **It is worse than deferral:** `_grantExplorationXp` opens with `_levelUpQueue = []`@30078 and re-runs `_checkLevelUp()`, which returns immediately because the level was already advanced — so **any step onto new ground discards the pending entry**. The level is kept; its rewards are gone. **The asymmetry is the tell:** the failing branch, written later by §XP-01, does it correctly three lines away — *failing* a Ceremonia Roll can open the modal, *passing* one cannot. **Blast radius: all 2,453 `skill_check` quests**, not one arc. **The 2026-05-26 §DESIGN-03 spec called for exactly this** (*"If `_levelUpQueue.length > 0`: show level-up modal immediately"*) and it was dropped in the §ARCH-01 migration — which is the argument for the spec→shipped delta table as a permanent instrument. **Needs no design call.** **Fix:** copy the fail branch's three lines into the pass branch. **Risk:** low. **Verify:** a Playwright pin on the *property* — cross an XP threshold on a passing skill check, assert the modal opened — never on the incident (*pin the property, never the incident*, §VM-01-G4c).
- [ ] **§AUDIT-03w — the courier guard quotes a 20-gold bribe the engine cannot accept (found 2026-08-11 during §DOC-02e).** `quest_courier_release` ships `onFail:[]` — the empty array — and a `failText` reading *"'Twenty gold or a writ from the Watch Commander.' He turns the log face-down."*`@21338`. §8 of the 2026-05-26 spec designed the transaction in detail (a `'bribe_available'` quest status, a *"Pay 20gp Bribe"* secondary button, an `onFail` callback offering it when `gold >= 20`); `git log -S "bribe_available"` returns **0 commits ever**. So the game **states a price and cannot be paid it**. **This is §AUDIT-03v's class exactly** — prose that names a transaction the engine never honours — and finding a second instance in two consecutive increments makes it a **pattern worth a detector rather than two hand-filed rows**: a player-facing string that names a specific gold amount as an offer, with no `cost`/`reward` bit or `sleepCost` exemption behind it. **Small design call, two options:** (a) implement it — the grammar has supported this since §VM-01-G4a: a `choice` on the fail branch with a `{kind:'cost', gold:20, refuse:'…'}` option, where `refuse` covers the can't-afford case under the shipped refuse-at-click contract; or (b) rewrite `failText` to refuse without naming a number. **Do not do neither.** **Risk:** low either way. **Verify:** (a) `./api.sh advise quest_courier_release` + a round-trip pin that 20gp leaves the purse exactly once; (b) grep the string is gone. **Consider closing §AUDIT-03v in the same increment** — same class, same decision shape. **⟶ FOURTH AND FIFTH INSTANCES, 2026-08-11 (§DOC-02h), and they WIDEN THE DETECTOR PAST GOLD.** Both in §CROWN-01 Amendment A (`c0c952b`). (iv) **The Leviathan iodine burn CHARGES for a benefit that has no reader.** `_nodeHookHcaLeviathan@33090` renders a button labelled *"⚡🧂 Burn Charged Iodine Salt at the water's edge (+5 ATK this fight)"* (or *"+3"* for plain); clicking it **splices the item out of the inventory** and sets `` `S_story.iodineBuffActive = true; S_story.iodineBuffBonus = _hasChL ? 5 : 3;@33114` ``. In 38,707 lines `iodineBuffActive` has **exactly one reader** — `` `const iodineBonus = st.iodineBuffActive@22250` ``, inside `_rollSkill`. **No combat code reads it.** So the player pays a crafted item for a stated combat bonus that does not exist — and the buff **leaks**, since nothing at HCA consumes it, so it is silently spent by whatever unrelated skill check comes next. This is worse than §AUDIT-03v/w: those *promise* and don't deliver; this one *takes payment first*. (v) **`Marsh Seaweed`'s `desc`** — *"Burns to trace iodine salt at the inn hearth"* — names a craft no quest or handler performs (see §DX-02n (d)). **The consequence for the detector:** the class is not *"a player-facing string naming a gold price"*, it is ***a player-facing string naming any specific mechanical effect — gold, a to-hit bonus, a craft — with no bit, handler or reader behind it.*** Five instances in five increments; the gold-only formulation would have caught two of them. Note (iv) is currently masked by §AUDIT-03x (HCA is unarrivable) and goes live the moment that row is resolved — **do not let the mask defer it**. **§DOC-02n — 8th instance, and the first where a SINGLE STRING carries TWO unbacked promises:** `FISHING_GUIDE_TEXT`, the reward readable of `quest_fishing_guide`, tells the player the bait find DC is 8/12/16 by zone while the engine computes it once without reference to the zone at `S_story.fishingBaitSatchel ? 8 : 10@30493`, **and** states `Weapon bonus = floor(fish AC / 4)@26681`, a formula with 0 occurrences in the file (§FISH-02). Sharper than the usual instance: because deeper zones carry strictly better bait at an **identical** DC, the advertised risk/reward gradient is **inverted**, not merely absent — the Bank is strictly dominated the moment the Reeds unlock.
- [ ] **§AUDIT-03v — Brynn promises a free room twice and the engine charges 5gp both times (found 2026-08-11 during §DOC-02d).** Three sites, all live. (a) `TLL`'s node text has her say it on arrival: *"'Free,' she says. 'The room's free too, if you need it.'"* (`` `TLL:{ num:2@8431` ``). (b) `quest_brynn_ledger.onComplete` narrates the reward the 2026-05-22 spec called *"free lodging for 3 nights"*: *"🛏 Brynn offers 3 free nights"* (`` `Brynn offers 3 free nights@21235` ``). (c) `storySleep` charges `node.sleepCost` — **`sleepCost:5` at TLL** — with a single exemption hardcoded to a **different** node and a **different** flag: `` `node.code === 'INN' && S_story.freeBookingUnlocked@36196` ``, which is `quest_inn_06`'s reward and keys on `innmotherKindness >= 5`. **So the game states a price of zero in prose, restates zero as a quest reward, and bills 5gp on both.** This is the §SPARK-01-FU class inverted — not a double-pay, a double-*promise* with no payer — and no gate can see it, because nothing cross-checks a narrative string against the mechanic it describes. **Small design call, two clean options:** (i) **grant it** — add `S_story.brynnFreeNights` decremented in `storySleep`, exactly the shape `freeBookingUnlocked` already models, which also makes (a) true; or (ii) **rewrite both strings** so neither promises a free bed. **Do not do neither.** Prefer (i): the arc's whole point is that the relationship pays, and 5gp is the cheapest possible way to prove it. **Risk:** low. **Verify:** for (i), a Playwright pin that gold is unchanged across a TLL sleep with nights remaining and debited by 5 when they run out; for (ii), grep both strings. **⟶ SIXTH CLUSTER INSTANCE, 2026-08-11 (§DOC-02j), and it is the widest surface yet — the sleep overlay promises FULL HP RECOVERY on every inn in the game.** The Long Rest button's own hint reads `` `full HP recovery@35742` `` on a first stay and `` `heal to full & advance a day@35741` `` on a return stay. **`storySleep` does not heal to full.** It rolls `` `const _rollCount = _isFirstSleep ? 2 : 1@36238` `` d10s plus the CON modifier, floors the result at half max HP (`` `_healTotal = Math.max(_healTotal@36246` ``) and caps it at `hpMax` — so a wounded player routinely pays the `sleepCost` and stands up short of full. Unlike v/w/y(b) this is **not** authored content on one node: it is a **fixed engine string on all 38 `sleep:true` nodes**, which makes it the cluster's highest-traffic member and the argument that the wanted detector belongs in the engine-string universe (§AUDIT-03s's premise), not in quest data. **Two options, same shape as the rest:** heal to full and make the string true, or restate the hint as *"a night's rest — roll to recover"*. **Prefer the second** — the dice are the design (§DOC-02j finding F3 shows the Hit-Die model migrated here deliberately from the short rest), so it is the sentence that is wrong. **⟶ SEVENTH INSTANCE, 2026-08-11 (§DOC-02k), and the first that states the same unbacked mechanic TWICE.** `aurosBlueprintKnown` occurs **4 times** in 38,712 lines: the `_S_DEFAULTS()` declaration, one writer on `quest_d0206_a4`'s pass, and **two readers that do nothing but render the promise back at the player** — Act IV's passText (*"In the CO boss fight, Auros's left pauldron has -4 AC. Her armor was never finished there."*) and a persistent Workshop panel, `` `_wkHint.textContent = '📜 Blueprint known@31947` ``, which repeats *"Auros's left pauldron is AC -4 in the Convergence fight"* for the whole run. **No combat code reads the field**; Auros's AC is never reduced at `TLS` or anywhere else. What makes this the sharpest framing yet for the detector: the second surface is **gated on `!S_story.defeatedBattles['TLS']`**, so it is deliberately built to persist *until the fight it names* — a reminder of a mechanic that has never existed, shown right up to the moment the player would notice. **Same two options as the rest** (wire the −4 into the TLS encounter, or restate both strings as lore about unfinished armour); **prefer wiring it**, since the whole §D02-06 chain exists to earn it. Seven instances in seven increments now, across quest text, node text, engine strings and an item `desc` — the class is a **surface** problem, not a data problem.
- [ ] **§AUDIT-03x — 172 of 416 nodes can never be arrived at, and 774 quests are authored to activate on them (found 2026-08-11 during §DOC-02f; 🟡 DESIGN CALL).** §WALK-1.5 merged 1°-collided cities into **locales**: `` `const CELL_GRID@9852` `` maps each `"r,c"` to an **array** of node codes, and its own comment names the contract — *"primaryOf = list[0] is the node you arrive at, the rest are intra-cell sub-locations."* Measured at HEAD: **416 nodes occupy 244 distinct cells, so 172 are non-primary** (worst cell `32,203` holds **17**: `SEA ATH LC1 LC2 LC3 LC4 LSO DS0 DS1 DSJ DSF DA0 DA1 DA2 DA3 HER IST`). **`S_story.currentCode` is assigned at exactly two sites in the whole file** — `` `S_story.currentCode = destCode@28375` ``, where `destCode = res.destCodes[0]` (the primary), and the respawn line `` `checkpointNode || 'LHR'@26011` `` — so **a non-primary code can never become `currentCode`**, and its `text`/`npc`/`battle`/`loot`/`sleep` never render. `cellCodes()` (the full list) is read **only** by the MUD/`MOVER:CORE` kernel; every browser call site uses `` `cellCode@9861` ``, the primary. **Blast radius via quests:** `` `_uqfActivateAtNode(node)@30139` `` keys on `node.code`, and **1,260 quests carry an `activateNode` on a non-primary node** — 486 are already held by the §AUDIT-03e guard, leaving **774 across 135 nodes** that would activate on arrival and cannot. Worst: **`BK` 89 quests** (sharing a cell with the *starting node* `LHR`), **`WM` 312** (behind `ERF`), `ROM` 58 (behind `FCO`), `LDN` 34, `HCA` (behind `WG0`). Content is stranded too: of the 172, **20 carry an `npc`, 11 `sleep:true`, 10 `loot`, 9 a `battle`**. **The tell that this is a real gap and not a design intent:** the waypoint system already compensates — `storyWaypoint`/`_travelTick` accept arrival **by cell** *"(co-located locale nodes share a cell, so currentCode equality alone can miss a genuine arrival)"* `` `@38092` `` — so the player is told *"📍 You have reached the waypoint: \<label\>"* for a node the engine then declines to render; and the MUD path is honest about the collision (`` `locales share this ground@10207` ``) while the browser path is silent. **Design call, three options:** (1) **aggregate** — `_uqfActivateAtNode` and the node panel iterate `cellCodes(cell)` rather than the primary alone, so a locale's quests and content surface on arrival at the cell; (2) **a locale surface** — the primary renders an explicit "also here: …" list that switches `currentCode` among its siblings (no movement, so invariant #1/#3 are untouched); (3) **DECIDED-STAY** — sub-locations are deliberately flavour-only, in which case the 774 `activateNode` values are the defect and should be repointed to their primaries. **Do not build before the call.** **Overlaps §AUDIT-03d** — that row's staging decision releases §AUDIT-03e's held quests, and it should be made knowing 774 of them are unreachable for a *second, independent* reason; consider sequencing this row first. **Verify:** whichever option, a Playwright pin on the **property** — step onto a locale cell, assert a sibling node's quest activated (or, for (3), assert no quest names a non-primary `activateNode`) — plus a `check:walk` phase asserting the chosen invariant so it cannot silently regrow. **⟶ EVIDENCE ADDED 2026-08-11 (§DOC-02h) — the first COMPLETE ARC measured end-to-end as a casualty of this row, and the archive proves the collision was not the arc's doing.** §CROWN-01 (The Three Crowns of the Swamp, `00fe35c`) authored **nine** nodes on **nine distinct cells** and argued the spacing cell-by-cell against four named neighbours; `git show 00fe35c:play.html` confirms all nine coordinates — WG0 `{15,3}` … HCA `{32,3}` — plus EC `{19,2}`, SC `{19,4}`, OC `{23,4}`, AT `{25,6}`, EA `{27,6}` exactly as the lab report states. At HEAD the nine cells are **two**: `25,206` holds **12** nodes (`HJ2 HCA HJ1 OTP HJ3 DBN MSY HG1 HN1 SDQ HW1 WG0`) and `22,191` holds `KTM INN`. WG0 and INN are the primaries by `NODE_MAP` declaration order, so **HW1 · HJ1 · HG1 · HJ2 · HN1 · HJ3 · HCA are unarrivable — 7 of 9**. Measured blast radius for this one arc: **24 of its 34 quests cannot activate** (HW1 8 · HG1 8 · HN1 8; the 10 that can are all at INN); the 18-quest Crone Mark supply is entirely on unreachable nodes so `croneMarks` cannot exceed 0; the HG1 first-visit panel never grants Glut's Gift so `quest_glut_06` never lists; the `story-hca-marks` conversion and the Leviathan hook never render so **the arc cannot close**; two of the three combat nodes never render their battle; and `quest_shore_01` — the **§LXX Atlantean Shore entry quest**, `activateNode:'HCA'` + `gate:{battles:['HCA_BOSS']}` — can never list, so the collision strands a *downstream* arc's doorway too. **This is the argument for option (1) or (2) over (3):** repointing 774 `activateNode` values to primaries would put all 24 Crown quests on WG0, which is not where any of them takes place. **⟶ A NEW CASUALTY CLASS, 2026-08-12 (§DOC-02p): COLLECTIBLE NARRATIVE, which no quest-side census can see.** `function storyCheckJournal(node) {@30211` delivers a Froberger journal fragment by matching `e.nodeCode === node.code`, so it is bound by cell primacy exactly as `_uqfActivateAtNode` is. Walking all 41 entries of `const FROBERGER_JOURNAL = [@27186` against `const CELL_GRID = (() => {@9852`: **35 reachable, 6 blocked** — Entry 10 (`SEN`, behind `LCY`), 16 (`SDQ`, behind `WG0`), 19 (`MSY`, behind `WG0`), 21 (`NAS`, behind `VBY`), 24 (`BHD`, behind `HOR`), 37 (`HER`, behind `SEA`). **Four of the six are collectibles the arc's own design document specifies by name** (the Proprietor Dusk, Crone Wane, Luc and Oracle entries), and Entry 16 is one of the two the spec names as the *"warning the player can use"*, so half of that argument's evidence is unreadable in the shipped game. **All ten `readAloud:true` entries are reachable**, which is why the loss is invisible: the spine still plays and only the optional layer thins out. `journalHalf: (S_story.journalEntriesRead || []).length >= 9@23658` remains attainable at 35, so no mission bit is lost — this is pure authored content, ~1,100 words, with no mechanical signal that it is gone. **Under option (1) or (2) these six return for free; under option (3) they are six more `nodeCode` values to repoint, and unlike a quest they have no `activateNode` field to grep for.**
> **EXTENDED 2026-08-12 (§DOC-02r) — the largest single casualty yet, and a NEW TRANSITIVE CLASS.** Six of the nineteen nodes the six prosocial arc families activate on are non-primary: `SEN`←**`LCY`** (cell 18,180), `VS`←`VBY` (12,198), `ATH`←`SEA` (32,203, **17 nodes**), `HFT`←`ALF` and `VAW`←`ALF` (10,191), `BK`←`LHR` (10,197). That strands **17 quests directly** — and **11 more transitively**, because each arc is entered through a **single-writer flag whose only writer sits inside a blocked node's `storyRender` block**: `S_story.wisHookReceived = true;@33503` (guard `if (node.code === 'VS' && S_story.personalLegendComplete) {@33437`) kills **all 8 §WISDOM-01 quests, including `quest_wis_01`/`_02`/`_03`, which sit on LIVE nodes `LCY`/`MME`/`GCI`**; `S_story.huntHookReceived = true;@33218` (guard `if (node.code === 'HFT' && !S_story.huntHookReceived) {@33207`) kills **all 4 §HUNT-01**; `S_story.whodunitSolved = true;@12436` (inside `quest_spark_04`, on `SEN`) kills `quest_spark_05` **and the Aldous confession branch `S_story.whodunitSolved && S_story.wrenpemburyInconsistencyNoticed && !S_story.aldousConfessed@33565`**; `S_story.roenAtSea = true;@33373` severs the §ALCHEMY relay at `activateNode:'PDL', gate:{ flags:['roenAtSea'] }@13251`; `set:['seaStrangenessNoticed']@12491` severs §SPARK-01 SEA. **Total 28 of 51 (55 %): §WISDOM-01 8/8 · §WHODUNIT-01 4/4 · §HUNT-01 4/4 · §ALCHEMY-01 5/7 · §SPARK-01 SEA 3/3 · §SPARK-01 3/5 · §VS 1/2; §SPARK-02, §HUNT-02, §NAVAL-01 and §PORT-01 intact.** ***The blocking node for the largest group is `LCY` — the starting-arc node whose DC 10 cat check is the whole design's showcase.*** **This is why option (3) (repointing) is the wrong shape here too:** the arcs' content is *distributed across* the blocked nodes, so repointing collapses six scenes onto one. **The transitive half is the new work item** — a primacy-only fix list is incomplete by 11 quests. See **§DX-02w** for the gate.
> **EXTENDED 2026-08-12 (§DOC-02an) — the first PAIRED design stranded on both sides, and the first case where REPOINTING is the right shape.** Layers 54+55 (Tilbury Harbor + Visby Underground) render from four `node.code ===` blocks and **three are non-primary**: `STN`←**`LCY`** (cell 18,180 — the Rennau harbor board, the Ori verb and both `activateNode`s), `TL`←**`HOR`** (18,177, **13 nodes** — the Adjutant Vonn embargo choice, Q-TL-02's only completion surface), `VS`←**`VBY`** (12,198 — already listed by §DOC-02r, now with its full bill: the Solvak/Mordus probe AND the seal-delivery payout). Only `TRD` (Yva) and `GVA` (the Warden) are primaries. **Bill: 6 of the 7 quests in the two arcs cannot complete** (the 7th, `quest_vs_02`, survives *only* because §BOARD-01-FU6 later gave it two `unlock` referral edges, and `unlock(bit, ctx)@22313` ignores the gate). **Transitive half again, and it ends the arc:** `vsShamanKnown` has one writer, `quest_vs_03`'s `onComplete`, which needs `S_story.vsDebtSettled = true;@34896` inside the dead `VS` block — so `node.code === 'GVA' && S_story.vsShamanKnown@31699` never opens and Layer 56's whole Warden encounter (600gp, the Warden's Token, a three-way persuade/fight/neither resolution) has never been playable. ***Against §DOC-02r's finding, repointing is CORRECT here and needs no design call, because the right destination is the cell's own primary and the arc's own fiction:*** the report's §I names the harbor as `DK` = `num:7` = **`LCY`, `label:'Harbor Docks — Tilbury'@8632`** — the primary blocking `STN` — and `VS`'s primary is **`VBY`, `label:'Broken Tooth Tavern'@8684`**, whose node text already seats Warlord Kael Mordus. Repoint the Tilbury block `STN`→`LCY` (taking `TL`'s Vonn beat with it — Vonn holds the Tilbury seal) and the Visby block `VS`→`VBY`. **The distinguishing test between the two shapes: does the blocking primary already hold the fiction the blocked node was written for?** Here it does, twice.
> **EXTENDED 2026-08-14 (§DOC-02bn) — the ORIGIN DOCUMENT is now identified, and it is a review that had this problem in its hands and cleared it in half a sentence.** `docs/lab-reports/lab-report-walk15fu-grid-resolution.md` (2026-06-26 15:58, §WALK-1.5-FU(c)) is the only document that ever examined the co-location piles as a whole. Its analysis is exact — every census figure and the whole 17/21/28 separation table replay byte-for-byte — but its §6 dismissed the surviving clusters as *"already handled cost-free by the locale-list sub-location picker."* **There is no picker, and there never was:** `git log -S "sub-location picker" --all` returns **exactly one commit — `ebf88c8`, the commit that added that report to the repo** — and `destCodes[1]` has never been read by anything at any point in the file's history. On the report's own tree the client already read `` `const destCode = res.destCodes[0]@28357` ``, exactly as it does today; `` `const cellCodes  = (key) => CELL_GRID[key]@9862` `` had **one** consumer that day (the mover kernel, which computes the list and hands back something nobody unpacks), and its first real reader — the MUD room describer, §NAV-01c `3568fcc` — shipped **five days later** and describes rooms on the server. So the phrase was NOT SHIPPED rather than stale: written from intent, not from the file. **The clusters that sentence cleared are still stranded at HEAD:** `LGW` holds `LON`/`BRK`/`LDN`/`AST` (cell 18,179), `PSA` holds `PIS`/`PISNOT`/`PISGAT` (26,190), `YRK` holds seven English-midlands nodes (16,178) — and after §WALK-1.5-FU(b) executed that same report's *correct* recommendation, even Weimar came out non-primary, `WM` behind `ERF` at 19,191 with its 312 quests. **Eleven days later §MATH-01 (`32d7bb0`) hit the consequence and named it in a commit message** — `EHZ`/`ZERO`/`MONS`/`CNTR` *"were unreachable sub-locations of `JRS`'s cell, so `math_02`/`_05` could never activate"* — and fixed it by moving four nodes to their own cells, which is this row's Option-A-per-arc. **The lesson for the fix, not just the history: grep for the code that REVEALS a surface, and never for a name you supplied yourself.** Also re-confirmed independently for the fourth time: **416 nodes / 244 cells / 172 non-primary**, and re-binning those 66 shared cells at 0.25° still leaves **28 completely unchanged**, so a resolution change remains the wrong lever.
> **EXTENDED 2026-08-17 (§DOC-02by) — the design call already has a fully executed, still-green precedent, measured end to end.** §DOC-02bn named §MATH-01 as the one increment that ever repaired an instance of this row; scoring that repair at 42 days makes it a template rather than an anecdote. **Cost:** four `PUT /api/coords` calls. **Blast radius in the engine: three diff hunks** — `git show 32d7bb0 --unified=0 -- play.html` is one `NODE_COORDS` line, three more `NODE_COORDS` lines, and the `QUEST_DB` block. **Zero hunks in `SEA_RUNS`, `ROAD_RUNS`, `IMPASSABLE_CELLS` or the mover**, so Option A costs nothing in world data when a free cell exists nearby. **Durability:** all four nodes are still the sole occupant of their cell at HEAD, none is sea or road, and a regression test pins the property (`src/tests/integration/quest-runtime-uqf.test.js:§MATH-01 — quest_math_01–05 UQF@9465`, *"math nodes occupy the HKG pocket, one code per cell, cells passable"*). **Two free consequences worth planning for:** the arc's six directional hints became literally true on the grid, so the relocation *repaired prose it never touched* — pick the destination cell from the hint text and the doc sync is done; and 21 days later `fa8f9e4`'s road regeneration laid `(29,249)` against the new pocket, giving two of the four nodes an encounter-free approach nobody designed. **What §MATH-01 explicitly declined to fix is still open and is this row's cheapest remaining slice:** its §3 set aside `PKR`/`JAR`/`OLN`/`JER` as *"pre-existing, not math-related."* All four are still non-primary behind `JRS:{r:38,c:215},@9824` at HEAD, and **28 quests are authored to activate on them — `JAR` 12, `PKR` 10, `OLN` 5, `JER` 1.** ***The precedent settles the method; what is left is per-arc adjudication, and Jerusalem is the largest single cluster with a proven fix shape sitting on the same cell.***

- [ ] **§EPIC-01 — the Epic Battleground quest ids are severed from the ids the runtime COMPUTES; 40 quests orphaned and the whole payment layer is unreachable (found 2026-08-12 during §DOC-02l). NO DESIGN CALL — highest player impact of any open row.** Seven sites derive an epic quest id by string concatenation from a node code — `` `const primaryId = 'quest_' + ebCode.toLowerCase() + '_primary';@30348` ``, `` `const returnId = 'quest_' + pb.nodeCode.toLowerCase() + '_return';@36595` ``, `` `'quest_' + node.code.toLowerCase() + '_primary';@35875` `` and four more at 28196 / 30382 / 35864 / 35865. **All seven are byte-identical to the earliest surviving build, where they resolved**: `EB_NPC_DIALOGUE` was keyed `EF`, so `'quest_' + 'EF'.toLowerCase() + '_primary'` produced `quest_ef_primary`, which exists. `c1d5a94` (2026-05-29) renamed the `NODE_MAP` keys, the `EB_NPC_DIALOGUE` keys, `` `const _EB_CODES = [@28032` ``, every `completion:{ battles:[…] }`, every `waypointNode` and even the `failText` prose — **and left the forty `QUEST_DB` ids as `quest_ef_*`**. The runtime now computes `quest_prn_primary`, which does not exist. **Blast radius:** (1) **all 40 authored epic quests are orphaned** — `` `if (!q || q.type === 'epic' || !q.activateNode) continue;@37003` `` excludes them from arrival-activation by design and the chip handlers use the other namespace, so 40 titles/descs/hints/passTexts/failTexts + 20 `reward` values never render; (2) **the payment layer is unreachable** — `` `function _storyEbReturnBeat(ebCode) {@30360` `` has exactly one caller, gated on a key whose sole writer is guarded by `if (QUEST_DB[returnId])` (falsy), so **no gold, no reward item and no return beat for any of the twenty contracts**, `ebReturnDone` stays empty and the `allEbReturns` milestone (≥5) plus both end-game tallies are permanently zero; (3) the parent-node chip **sticks** at *"⚔ EPIC … awaits"* forever because nothing completes a phantom key; (4) the accept toast falls back to the raw code, *"📋 PRN"*. The combat half is unaffected — `defeatedBattles[pb.nodeCode]` never goes through `QUEST_DB`, so the boss dies and the player simply is not paid. **75 days live.** **Fix:** rename the 40 `QUEST_DB` ids to the live codes (`quest_prn_primary` / `quest_prn_return`, …) — the ids are referenced by literal in only three places — or add one legacy→live map at the four derivation points; prefer the rename, since a second name for the same node is what caused this. **No gate can see it:** `check:questgraph` walks `QUEST_DB` against itself and never evaluates a computed id — this is **§DX-02o one level deeper**, and the wanted check is *"every string a code path CONSTRUCTS as a quest id resolves"*. ***The durable lesson: a rename is applied to every place the code spells the identifier and missed where the code computes it — grep for the concatenation, not only for the name.*** **EXTENDED 2026-08-12 (§DOC-02o) — the blast radius reaches the ENDING SCREEN, which was not previously counted.** `function _curseScore() {@28193` is an eighth computed-id site (`'quest_' + code.toLowerCase() + '_primary'@28198`), and more importantly its `returned` term reads `S_story.ebReturnDone`, whose only writer is inside the unreachable `_storyEbReturnBeat`. See **§ENDING-01** — three of the game's four endings are collateral damage from this row, so §EPIC-01's player impact is larger than "no gold": it is "no good ending, ever."

- [ ] **§ENDING-01 — the game declares four endings and can produce exactly one, and it is the failure ending (found 2026-08-12 during §DOC-02o). NO design call for (a); it is released by fixing §EPIC-01.** `function _curseScore() {@28193` is **byte-identical between `32c10c5` and HEAD** — 82 days, zero drift — so this is not formula rot. It partitions the 20 Epic Battlegrounds into returned / started-not-returned / never-started and returns `(startedNotReturned * 3) + (neverStarted * 1) - (allComplete ? 5 : 0)@28206`. The `returned` term reads `S_story.ebReturnDone`, whose **only** writer is `S_story.ebReturnDone[ebCode] = true;@30365` inside `function _storyEbReturnBeat(ebCode) {@30360` — the function **§EPIC-01 proved unreachable**. Re-confirmed independently here: `quest_prn_primary` has 0 occurrences in 38,712 lines, `quest_ef_primary` exactly 1. With `returnsComplete` pinned at 0 the score has a closed form: **`curse = 20 + 2 × (EB bosses defeated)`, range 20–60, floor 20.** Consequences, all measured at HEAD: `if (missionDone && curse <= 0)` — **Covenant Keeper, the ending that reads back the name of every person the player helped — is dead**; `else if (curse <= 0)` (Standard Covenant) is dead; the trailing `else` (Mixed) is dead because the window `0 < curse < 15` cannot be entered; and `Sweelinck sets the journal on the table@28260` (*"The Void is sealed. The curse is not in the Void."*) **fires 100 % of the time**. Sweelinck's Last Question collapses the same way to `Were you alone by choice?@28278`. Two of the twelve `_missionComplete()` bits are permanently false — `allEbReturns: Object.keys(NPC_DIALOGUES)@23657` (needs ≥ 5 returns) and `noHighCurse: _curseScore() < 10@23661` — so the ≥ 8 threshold now runs against an effective 10. **Verdict: engine-rot, dated.** At `32c10c5` the epic ids resolved, `ebReturnDone` was writable and `curse = −5` was attainable, so all four endings worked when the design shipped; `c1d5a94` (2026-05-29) broke them 75 days ago. **Fix = §EPIC-01; verify by asserting `_curseScore()` can reach ≤ 0 in a seeded full-return run.** **(b) — a SECOND defect, independent of §EPIC-01 and born broken (small design call).** `const _isTrue = missionDone && curse <= -6@28231` gates the *"Covenant Keeper (True)"* standing. `(allComplete ? 5 : 0)` is the only negative term in the whole formula and is worth exactly 5, so the best attainable score in a **fully working** engine is `0 + 0 − 5 = −5`. **`curse <= -6` is unsatisfiable by construction**, and the same threshold is present at the archive — this has never been winnable and fixing §EPIC-01 will not fix it. The call is which number is intended (`<= -5`, or a larger all-complete bonus). ***The durable lesson: a reachability bug in one subsystem can silently retarget an unrelated screen's branch selection — `_curseScore()` never changed, never failed, and never threw; it simply stopped being able to return a low number.*** **(c) EXTENDED 2026-08-12 (§DOC-02p) — the blast radius is not confined to the ending screen; it reaches two graded ladders that render DURING play, which is the larger loss.** `const SWEELINCK_DIALOGUE_VARIANTS = [@27231` holds the four curse brackets `-99..3 / 4..9 / 10..14 / 15..999` exactly as its origin spec wrote them, and the selector at `id:'story-sweelinck-variant'@31376` matches `cs >= v.minScore && cs <= v.maxScore` — so against a floor of 20 **only the `minScore:15` variant is ever selected** and the three graded lines never render at NUE or TLS. The sole escape is the Birka override, which fires on three friendships regardless of score. Worse, `const COVENANT_STANDING_LABELS = [@27358` brackets at −6 / 0 / 7 / 14 / ∞, so `_covenantStanding()` returns **"Wanderer"** — *"The Void will open again. Not your fault. Not entirely."* — for **every player, on the character sheet, for the whole run**; 4 of its 5 labels are unreachable. And `vic-returns').textContent@28225` prints **0 / 20** on the victory screen for every player who has ever reached the Cosmic Realm. **The design's stated purpose was that the ending should never be an ambush** — the mid-run Sweelinck panel exists so a player can change course — and the shipped game instead tells them from their first reading that they are already lost, with nothing they can do about it. All three surfaces are released by the same §EPIC-01 fix; **verify by asserting that a seeded full-return run selects a variant with `maxScore < 15` and a standing other than Wanderer**, not merely that `_curseScore()` can reach ≤ 0. **⟶ A FOURTH SURFACE, 2026-08-12 (§DOC-02ab) — the MISSION LEDGER itself, which this row did not count.** `function _missionComplete() {@23649` scores twelve named bits and passes at `return Object.values(bits).filter(Boolean).length >= 8;@23664`. **Two of the twelve are pinned false by the same cause:** `noHighCurse: _curseScore() < 10,@23661` (floor 20) and `allEbReturns: Object.keys(NPC_DIALOGUES).length > 0@23657` (reads the same unwritable `ebReturnDone`). So the ledger is **8 of 10**, not 8 of 12. And a third is a **tautology at both call sites** — `sealedVoid` reads `defeatedBattles['TLS']`, but `_missionComplete()` is only ever called from `function _buildEpilogueScroll() {@28122` and the victory screen, and `storyCheckVictory` returns early unless that same battle is won — so the real bar is **7 of 9 genuine player choices**. The 2026-05-22 design document that specified this ledger describes 8-of-12 as deliberate slack; in play the slack is 2 of 9. **Verify alongside the other three surfaces:** a seeded full-return run must make `noHighCurse` and `allEbReturns` *attainable*, and the fix should also delete the always-true `Object.keys(NPC_DIALOGUES).length > 0` conjunct, which tests an immutable non-empty literal and is not a guard at all.
- [ ] **§EPIC-02 — 40 Epic Battleground `failText` strings name a raw LIVE node code and put the NPC in the wrong place (found 2026-08-12 during §DOC-02l). NO DESIGN CALL, but blocked behind §EPIC-01.** Every one of the 40 epic quests carries a `failText` of the form *"Woodcutter Bram is at PRN, waiting for word that hasn't come."* — two defects in one player-facing sentence: it prints an **internal node code** to the player, and it places the NPC **at the battleground** when `EB_NPC_DIALOGUE`'s own `npcNode` puts them at the parent (Bram is at `FRO`; `PRN` is the Maw he refuses to enter). This is the §AUDIT-03s class inverted: those were *dead* codes in player-facing strings, these are **live** codes, so every existence check passes while the sentence is both jargon and wrong. Also fix the *"📋 PRN"* accept-toast fallback from §EPIC-01 (4). **Low urgency only because §EPIC-01 currently prevents any of these strings from rendering** — repairing §EPIC-01 without this makes 40 wrong sentences newly visible, so sequence them together.
- [ ] **§EPIC-03 — the Epic Battleground negotiation is strictly dominated, its failure branch inverts the spec on both halves, and the failure panel is hardcoded masculine for 20 NPCs (found 2026-08-12 during §DOC-02l). SMALL DESIGN CALL: what should failing cost?** `` `const accepted = _ebNpcNegotiated ? d.paymentCeiling : d.paymentFloor;@30345` `` — the CHA check (`` `S_story.abilityScores || {}).cha) || 8;@30292` ``, DC 17) gates only the **damage**, never the gold: declining pays the floor, passing pays the ceiling, **failing pays the ceiling and takes 1–4 non-lethal damage**. So negotiating is never wrong and the check has no economic consequence. The lab report specified *"failure to negotiate: not punished beyond losing the margin; NPC doesn't become hostile"* — HEAD punishes it (a scripted beating) **and** does not withhold the margin: both halves inverted. On **16 of 20** contracts `paymentCeiling === paymentFloor`, so the exchange is cosmetic there regardless. **Two riders, neither needing the design call:** (a) `` `Math.max(1, Math.floor(Math.random() * 4) + 1)@30312` `` and the d20 three lines above are **unseeded** `Math.random()` writing persisted `S_story.hp` → a named **§DX-02m** instance, same shape as §DOC-02k's `_cyRoll`; (b) the shared failure panel is written in hardcoded masculine prose (*"Then **he** moves — knee into your gut…"*, *"Then **he** crouches down"*) and renders for whichever quest-giver was asked — **ten of the twenty are women** (Rona, Gwynne, Wane, Tula, Maris, Draketide, Wren, Zephyrine, Dora Flint, Sigrid). Same class, two smaller sites: `quest_ea_return.passText` says *"Draketide grins once. Then **he** starts making lists"* two lines below a `desc` that says *"**She** is reading in the archive"*, and Ithiel is *"her"* in `quest_ek_*` but *"his"* in `EB_NPC_DIALOGUE` and the node text. **(b) is a no-design-call fix and worth doing on its own.** **NARROWED 2026-08-12 (§DOC-02o) — half of this row is answered by measurement, not by a design call (the §DX-02n(a) move).** The premise *"failure is not punished per spec"* came from `lab-report-epic-battlegrounds.md`. A **second report written the same day**, `lab-report-friendships-with-magic.md`, specifies the punishment explicitly and at length — *"the NPC still pays but expresses their displeasure physically"*, 1d4 non-lethal, HP floor 1, ceiling paid regardless — and the engine implements **that** one line for line (anger block, calm block, `💢 N non-lethal damage — HP X / Y`, `He's Fine — Continue →@4915`). So the damage-on-fail is a **deliberate, documented, same-day override**, not drift; two lab reports simply disagree, and the engine followed the later hand. ***Corpus rule (instrument 7): before filing a spec-vs-engine gap, check whether a SIBLING report specifies the opposite — the engine may be faithful to a document you have not read.*** **What remains genuinely open is the dominance half, which neither report addresses:** `paymentCeiling === paymentFloor` on **16 of 20** contracts, so on those sixteen negotiating cannot gain a coin and can only cost 1–4 HP. The design is coherent exactly where ceiling > floor — 4 of 20. Also re-confirmed independently: `const roll   = Math.floor(Math.random() * 20) + 1;@30294` and `S_story.hp     = Math.max(1, (S_story.hp || 1) - dmg);@30313` are the two unseeded `Math.random()` writes into persisted HP (**§DX-02m**); both trace to this 2026-05-22 spec, written before §VM-01-B moved the quest-path d20 onto the seeded stream and never reached surfaces that roll their own.
- [ ] **§AUDIT-03y — the legacy code `BK` survived a migration by resolving to the WRONG live node, and three registries followed it (found 2026-08-11 during §DOC-02g).** This is §AUDIT-03m's **"worse than dead"** class, found for the first time in **engine data** rather than doc prose: legacy `BK` was the **Broken Tooth Tavern, Visby** — live **`VBY`** (`num:25, name:'bar', act:5`, `npc:'Warlord Kael Mordus'`, and the Mordus/shaman-stalemate scene in its own node text) — but `BK` is *still a live key*, now naming **"Birka Shore — Northern Longship Landing"** (`num:241, name:"beach", act:1`). Because it resolves, `check:noderegs` passes and the sentence stays wrong. Three surfaces followed it: **(a)** `CORELLI_APPEARANCES` stop 3, whose opener says *"Visby. Well."* and *"Mordus runs a tight tavern"*, firing at a beach; **(b)** `NPC_DIALOGUE.BK` = *Warlord Kael Mordus*, quote *"You're not from Visby. But you're here"*; **(c)** `VENDOR_NODES` contains `'BK'`. `VBY` appears in none of them. Plus **9 × `activateNode:'BK'`, 2 × `waypointNode:'BK'`, 1 × `nodeCode:'BK'`** to adjudicate individually — some may legitimately mean the Birka shore. **(a) and (b) are no design call** (the prose names Visby explicitly). **Compounding, not causing:** `BK` also shares cell (10,197) with the starting node `LHR` and is non-primary, so it can never be arrived at (§AUDIT-03x) — which makes `encoded_letter` (80gp) **unobtainable**, since stop 3 is its only grant path, and with it the retroactive-decode payoff. Repointing to `VBY` fixes both and does **not** wait on §AUDIT-03x. **(b) `false_warrant` is a 200gp purchase with no surface** — its "skip a corridor encounter, disabled at `voidPressure >= 7`" mechanic shipped at `194a810` and was removed with its host `storyCorridorTravel` by `85cc43e` (§CELL-11A); the item is still sold at **reachable** stop 2, renders under 💊 Consumables, and has **no Use button** (the only consumable handler tests `effect === 'advantage_next_attack'`) and **no Read button** (`readableItems` filters `type === 'readable'`; it is `type:'consumable'`). 🟡 That half is the **same class as §AUDIT-03v and §AUDIT-03w** — third instance in four increments — so close all three together with a *detector*, not a fourth hand-filed row. **⟶ A FOURTH SURFACE FOLLOWED `BK`, AND IT IS THE FIRST THAT BREAKS A MECHANIC RATHER THAN A SENTENCE (2026-08-11, §DOC-02k).** `quest_d0205_a3` — Act III of the §D02-05 Void Fracture Maze, which this report's spec places *"as an EB approach room at BK"* — ships `` `completion:{ battles:['BK'] }@21580` ``, and Act IV gates on the same key. At HEAD `` `BK: { num:241@9011` `` is the Birka beach, and **its `NODE_MAP` record carries no `battle` field at all**; **nothing anywhere in 38,712 lines writes `defeatedBattles['BK']`**. So Acts III, IV and V of that chain are **unreachable**, and `` `S_story.mazeSolvedChecks = 3;@21583` `` — Act III's `onComplete` — can never run. **This is the sharpest argument yet for the row:** every other `BK` casualty is a sentence naming the wrong place, which a reader could notice; this one is a **five-act chain silently truncated**, and `check:noderegs` passes it perfectly because the code *resolves*. It is also **independent of §AUDIT-03x** — repointing BK's cell so it can be arrived at would not create a battle on a beach — so this half needs its own adjudication: either repoint Act III's `battles` at the node the maze actually occupies, or give `BK` the encounter its five sibling chains all have.
> **⟶ FIRST OF THE NINE `activateNode:'BK'` SITES ADJUDICATED (2026-08-13, §DOC-02as) — `quest_wis_04` means `VBY`, and the repoint pays for itself. 🟢 no design call.** The row asked for these nine to be settled individually because *"some may legitimately mean the Birka shore."* **This one cannot.** `` `activateNode:'BK', gate:{ flags:['wisHookReceived'] }@13427` `` is §WISDOM-01's W4, and every string it owns names the other place: `` `hint:'Read the Mordus-shaman stalemate at Visby\'s Broken Tooth Tavern.@13426` ``, a `desc` reading *"At Visby's Broken Tooth: INT Investigation DC 12"*, a `failText` repeating it, `npc:"warlord_kael_mordus"`, and the arc hub's own fragment tally — *"W4 at Visby Tavern (INT 12)"* — agreeing from a different file region. **Confirmed against the arc's birth tree:** `e339aeb` (2026-05-28) already shipped `activateNode:'BK'` with the identical Mordus/shaman scene, when `` `BK:{ num:25, code:'BK', name:'bar (Visby)', label:'Broken Tooth Tavern', act:5` `` was still that node. **And the repoint is worth more than one identifier:** `VBY` is the **primary** of cell `12,198` while `BK` is non-primary behind `LHR`, so W4 moves from unreachable to the *only standable surface the whole arc has* (§AUDIT-03x: `VS` is 5th of 5 in that same cell).
> **⟶ AND THE MECHANISM, which is the 38th §DOC-02 instrument: A MIGRATION REPAIRS THE REFERENCES THAT *BREAK* AND WALKS PAST THE ONE THAT SURVIVES WRONGLY — so the worse-than-dead code is the one the sweep GUARANTEES it will leave behind.** §WISDOM-01 shipped with six codes: `VS`/`DK`/`SK`/`SB`/`BK`/`AE`. §WALK/§NAV-01 remapped the four that stopped resolving — `DK`→`LCY`, `SK`→`MME`, `SB`→`GCI`, `AE`→`ATH`, all four landing on the byte-identical label by `num` — and left `BK` alone **for the single reason that it still resolved.** That is not an oversight a careful sweep avoids; it is the sweep's selection criterion. Any future code-migration pass must be driven by `num`-identity, not by resolution failure, or it will reproduce this exactly. (Corollary worth keeping: the §WISDOM-01 report's own prose *mis*-glossed `BK` as *"Birka … a guild city"* while its code table said Visby; the implementer followed the code and was right on the day, and seventy days later an unrelated world edit made the report's error true.)
- [ ] **§DX-02q — `check:noderegs` cannot see a SEMANTIC collision (found 2026-08-11 during §DOC-02g).** Every phase of gate #13 asks *"does this code resolve?"* — and §AUDIT-03y is the failure that passes that test perfectly while being wrong. Wanted: a phase that asks whether the surrounding content **contradicts** the node the code resolves to — e.g. a place name in an adjacent string (`'Visby'`, `'Mordus'`) that appears in a *different* node's `label`/`text`/`npc` than the one the code names. Cheap high-signal seed: for each node-valued reference, compare capitalised place-words in the same entry against the target node's `label`; flag a mismatch that resolves elsewhere. Explicit classification in the #13/#14 house style, since flavour text legitimately names other places. **Its limit should be stated rather than discovered:** it can only catch a collision whose prose names the right place out loud.
> **EXTENDED 2026-08-12 (§DOC-02an) — the collision's worst form: a code that dies and is later REISSUED by an unrelated pass.** `TL` and `VS` were **born dead** — neither was a `NODE_MAP` key at the Tilbury/Visby arc's own ship commit `194a810` (2026-05-25 09:10:55), so `if (node.code === 'TL')` and `if (node.code === 'VS')` were unrunnable from the first minute (§AUDIT-03p class). On 2026-06-03 `cb0fc35` — a worldbuilder import whose message reads *"2 new nodes (KHR/TUN)"* — added **four** nodes, the two unmentioned ones being `` `TL: { num:278@9085` `` and `` `VS: { num:279@9088` ``, at coordinates `TL:{r:112,c:148}` / `VS:{r:78,c:213}` that were later snapped by §WALK-1.5's projection. **From that commit onward the dead references resolve, so every phase of gate #13 passes, and §VM-01-G3 (2026-07-28) then repaired the surviving symptom (`NODE_MAP.VS` had no `code` field) in good faith and recorded the arcs revived in `world.md`.** ***A reissued code is worse than §AUDIT-03y's mis-resolving one, because there is no wrong-place prose to catch it on — the label can be perfectly apt (`VS` reads "Visby Underground — Fence Quarter") while the reference is still an accident.*** Two signals a phase could use, both mechanical and neither requiring prose: (1) a `node.code ===` guard whose target's **birth commit postdates the guard's own birth commit**; (2) an import commit that adds `NODE_MAP` keys its message does not name. See §AUDIT-03ao for the live bill.
- [ ] **§DX-02r — the activation-gate grammar can say *attempted* and *done* but not *failed*, and three quests authored as failure consolation therefore activate for everyone (found 2026-08-11 during §DOC-02h).** `'failed'` is a **real, written quest status** — `` `S_story.quests[questId] = 'failed'@7001` `` sets it when a non-retryable skill check misses — and `_matchActivationLeaf`'s own comment names it: *"questsAttempted → every listed quest has a non-empty status (active/done/failed/complete)"*. But the leaf grammar offers only `` `g.questsAttempted.every@22061` `` (non-empty) and `questsDone` (terminal pass). **`questsFailed` has 0 occurrences in the file.** The consequence is live in §CROWN-01 Amendment A, whose §A.4 designs the mechanic explicitly — *"A failed skill check does not strand the player — it redirects them… Activates if quest_X_01 resolved as fail"* — as three consolation errands (`quest_whisper_kelpie`, `quest_glut_mudcrab`, `quest_wane_spawn`). All three ship `gate:{ questsAttempted:[…] }`, so **a player who PASSES the check is dispatched after the Kelpie exactly as one who fails**, and the design's whole point — that the body goes and learns what the mind missed — is erased. This is an **invariant-#4 case in its cleanest form**: the capability is missing from the *grammar*, so the fix is a new leaf term, never a `_legacy_fn` closure or a per-quest workaround. **No design call for the term itself** (`questsFailed:[ids]` → `QS[id] === 'failed'`, mirroring `questsDone` line for line); **a small call on the three quests** — repoint them to `questsFailed`, or keep them universal and rewrite the three `desc`/`hint` strings that frame them as consolation. **Measure the corpus first:** grep every quest whose text implies a fail-branch prerequisite, since three found while verifying one report is not a search. **Risk:** low — a new leaf is additive and no shipped gate uses the key. **Verify:** a `quest-runtime-uqf` pin that `questsFailed` matches `'failed'` and rejects `'complete'`/`'active'`/absent, plus (if repointed) a Playwright pin that passing `quest_whisper_01` does **not** list `quest_whisper_kelpie` while failing it does.
- [ ] **§DX-02s — the Doc Health Badge is the stalest artifact in the corpus and every row of it reads ✅ (found 2026-08-11 during §DOC-02i).** `index.md` §"Doc Health Badge" exists *because* `lab-report-documentation-system-design.md` recommended it (FC01, 2026-05-24) as *"a live count of sync-pass completion visible at a glance."* It shipped, and at HEAD it is wrong on every mechanical row: **HTML line count `36,933` vs live `38,712`** (+1,779), **"Lab reports on disk" `85` vs `107`** (+22), **"Lab reports in index" `85`** vs **87 unique filenames actually referenced** — and that last row is wrong in *both* directions. **26 reports on disk are absent from `index.md`** (the whole `vm01*` series a–g4, the `play-01*` series, `warrants-board`, `void-tide-bounties`, `death-loot-grave`, `javascript-mud`, `npc-card-map`, …) and **6 index rows name files that do not exist** — `lab-report-api-01-02-mechanics-combat-review.md`, `lab-report-loot-drop-weapon-economy.md`, `lab-report-plan-cleanup-v13.md`, `lab-report-plan-cleanup-v17.md`, `lab-report-plan-cleanup-world-builder-arc.md`, `lab-report-timeline-history-completed.md`, **all six deleted in one commit, `120d617`**, with their rows kept. The badge is not missing a procedure: `index.md` prints the repair instruction directly beneath it (*"Update this table at the start of each session: recount lab reports with `ls docs/lab-reports/lab-report-*.md | wc -l`"*). **That is the finding — a check that depends on a human running it reports green when nobody ran it**, and the drift was invisible for a month because the ✅ column is hand-typed. **The same failure is present on day one:** at `59a9e0d` the repo held **22** lab reports while `index.md` referenced **23**, and the report says "23" twice because it counted the manifest instead of the disk. **Every one of these rows is mechanically computable**, which makes this the cheapest gate in the family: `check:docindex` = `wc -l play.html` + `ls docs/lab-reports/*.md | wc -l` + a two-way set difference between the disk and the filenames `index.md` references, failing on any drift and on any phantom row. Fold the live-entity row into `npm run stats` (§DX-01g) rather than re-deriving it. **No design call.** **Risk:** low — a new gate plus one doc edit. **Verify:** the gate goes red on today's `index.md`, green after the refresh; a positive-control test that adds a phantom filename and a deleted file and proves both directions fail. **⟶ (b) a second, unrelated member found the same pass, and it does need a small call: two favor storage shapes, one gate grammar.** `_setNpcFavor` writes the `npcFavorability` map and the gate leaf `` `if (g.favorMin)@22065` `` reads **only** that map — but `` `fav_corelli: 0@23144` `` is a real `_S_DEFAULTS()` field with its own writer (`` `S_story.fav_corelli = Math.min@31795` ``, = `min(3, corelli_purchase_count)`) and two readers, verified live by §DOC-02g. It is a **private scalar the gate grammar cannot see**, so no quest can ever gate on Corelli's favor without a `_legacy_fn` closure — invariant-#4 pressure with the declarative alternative already in the language. **The call:** fold `fav_corelli` into `npcFavorability.corelli` (one writer, one migration on load), or widen `favorMin` to consult both. Invariant #4 argues for the first, since the second entrenches a second shape.
- [ ] **§AUDIT-03z — a live home doc that no gate can see, and a state field that has never existed propagated across a correct doc pointer (found 2026-08-11 during §DOC-02i).** Two members, one file class each, both instances of §AUDIT-03s's premise — *dead references outside `.md`*. **(a) `src/sources/froberger-journal-all-entries.txt` is a live home doc invisible to every gate.** The HTML names it itself: `` `const FROBERGER_JOURNAL = [@27186` `` carries `// → doc: froberger-journal-all-entries.txt`. But `src/scripts/legacy-codes.js` (gate #16) drives from an explicit `SWEEP` list of **eleven `.md` paths** and the string `.txt` does not occur in the script at all, so the file is neither SWEEP nor PENDING nor HISTORY — it is simply **out of the universe**. Consequence: all **41** of its entry headers still carry retired 26×16 codes (`[City Gate — CI]`, `[Tavern — TV]`, `[Crypt — CR]`, `[Bazaar — BA]`, `[Sewers — SE]`, `[Slums — SL]`, `[Midlands — MI]`, `[Island Settlement — IS]`, …) while `FROBERGER_JOURNAL` uses the live ones (`LHR`, `MHQ`, `KRN`, `LLA`, `SFT`, `BMA`, `MAN`, `PDL`) — the exact `CI→LHR / TV→MHQ / CR→KRN / BA→LLA / SL→BMA` remap the engine records in its own `birkaNpcs` note, recovered here independently. **And the same pass closed FC02, the 79-day-old "content comparison has not been done" item, by doing it: 39 of 41 entries are verbatim after quote/whitespace normalisation, and entries 17 and 29 are entirely different text in the two artifacts** — HTML 17 (`MAN`) *"The woman at the archive disagreed with my taxonomy of the eastern war…"* vs txt `[Midlands — MI]` *"The midland family fed me and gave me the grain-store loft…"*; HTML 29 (`PDL`) *"There is a question I should have asked before she left…"* vs txt `[Island Settlement — IS]` *"The island has seven families and one boat…"*. Not drift — the `.txt` retains superseded entries. **Fix: widen `SWEEP` to accept non-`.md` live docs** (the classification style is unchanged; only the extension filter moves), then sweep the 41 headers and re-sync entries 17 and 29 from the HTML, which is the source of truth. **No design call.** **(b) `fav_auros` — a field that never existed — is stated as a trigger condition in the engine AND in `world.md`, reached across a *correct* FC05 pointer.** `` `const S29_AUROS_THEORY =@27052` `` carries `// → doc: world.md §S29` and states the trigger as *"fires at CY when frobergerLastEntryRead && fav_auros >= 2"*. **`fav_auros` occurs exactly once in 38,712 lines — in that comment** — is not a `_S_DEFAULTS()` field, and has no reader; the live guard is `_npcFavor('auros') >= 2` at `` `s29LineDelivered) {@32652` ``. `world.md` repeats `fav_auros >= 2` at **three** lines, having inherited it across the pointer. The same comment names the node `CY`, which is not a `NODE_MAP` key (the §AUDIT-03p born-dead class; `world.md` annotates it correctly as *"at `HKG` (historical `CY`)"*, so the doc is right about the place and wrong about the field). **Neither error is reachable by any gate:** #16 scans `*.md` so an engine comment is out of scope, #13 phase 6 is comment-aware **by design** (the §AUDIT-03f lesson) and skips it, and nothing validates a state-field name in prose. **This is the sharpest available statement about the two-way-sync thesis: a bijection guarantees a link, not a truth.** Fix is three doc edits + one comment; **no design call**. Worth measuring the corpus for other `fav_<npc>` prose references before closing.
- [ ] **§DX-02t — the short rest is implemented twice, and the second copy silently withholds four of its consequences (found 2026-08-11 during §DOC-02j).** `` `function storyShortRest@25819` `` is the canonical Layer-13 path. A **second, independent implementation** lives inside the post-battle return row at `` `const _rests = () => S_story.shortRests@7133` ``, and the heal arithmetic is byte-identical between them — `` `const base = Math.floor(S_story.hpMax * 0.25)@7145` `` / `` `const heal = isInn ? base : base * 2@7146` `` against `` `const base  = Math.floor(S_story.hpMax * 0.25)@25840` `` / `` `const heal  = isInn ? base : base * 2@25841` `` (the pairs differ only in whitespace, which is exactly what a copy-paste divergence looks like). **Both spend the same `S_story.shortRests` allowance and both apply the 2× off-inn multiplier. Only one of them does anything else.** The battle-return copy does **not**: (a) call `_maybeAddKnowledgeBead`, so resting through it builds **no Necklace of Knowledge** — the collection subsystem's entire acquisition premise is *"the first time the player rests at each unique location"* and this path is a rest that does not count; (b) set `shortRestedAtNodes`, so no Necklace Token is granted **and no `restedAtMin` gate credit accrues** — and that leaf is real and consumed: `` `if (g.restedAtMin)@22075` `` gates `` `quest_d0206_a3: { id:@21791` `` on `` `restedAtMin:{ SZG:1 }@21794` ``, so **a player who rests at SZG via the post-battle button cannot open Act III of that arc** and has no way to learn why; (c) restore Action Surge charges, which `` `if (_lv >= 2) S_story.surgeCharges@25846` `` does and the engine's own comment at 24641 says is rest-gated — so the resource is spent and the recharge withheld; (d) print the Boy Scouts message, **though it applies the doubling anyway**, so the one path that most looks like roughing it is the one that never says so. **No design call** — the fix is to make the button call `storyShortRest(S_story.currentCode)` and delete the copy, which is a strict superset of what it does today. **Risk:** low, but note it re-renders (`storyRender` at the tail of `storyShortRest`) where the inline copy deliberately only calls `_updateHeal()`, so the button's own DOM row needs checking after the swap — §7½ applies. **Verify:** a Playwright pin that a post-battle short rest at a fresh node grants a bead and a token, and that `quest_d0206_a3` opens after resting at SZG by either route. **The durable half:** this is Hazard #2's shape in the control-flow dimension — two copies of one mechanic, where the copy is not *wrong* on the line it duplicates and is wrong in everything it omits, so a diff of the arithmetic proves nothing.

- [ ] **§DX-02u — `voidFluxCleared` has ZERO writers, so `quest_d0209` dies at Act III and `voidFluxActive` becomes PERMANENT (found 2026-08-11 during §DOC-02k). No design call.** `voidFluxCleared` occurs **5 times in 38,712 lines**: one `_S_DEFAULTS()` declaration, one quest `desc` that *describes* it being set (*"[voidFluxCleared flag set on victory]"*), and **three gate reads** — `` `completion:{ flags:['voidFluxCleared'] }@21637` `` on `quest_d0209_a3`, Act IV's `gate:{ flags:['voidFluxCleared'] }`, and Act V's `flagsAny`. **Nothing anywhere assigns it.** The gate leaf resolves against `S_story` directly (`g.flags.every(f => !!st[f])`), and no `mission_bit` carries that flag name, so the field is `false` for the whole run: **acts III, IV and V of the §D02-09 Void Flux chain are unreachable.** **The consequence is the row, not the stalled chain.** Act I's pass sets `S_story.voidFluxActive = true`, and Act V's pass is the **only** site that sets it back to `false`. So a player who passes a DC 10 INT/Arcana check is **permanently inside the inversion field**: both healing sites (the inventory potion Drink handler and the Brynn's-Loaf Eat handler) route through the `[Void Flux] Heal → Hurt` branch, whose arithmetic is `hp + heal - half - half` — a **net +0 or +1 HP** — for the rest of the run, with no surface anywhere telling the player why potions stopped working. **Fix, no design call:** make Act III match its five siblings, which all complete on a battle — either `completion:{ battles:['RAI'] }` (the node the whole chain targets) or write `voidFluxCleared` on the RAI victory. **The durable half, and it widens a proposed gate:** this is the **inverse** of the §DX-02n family. Those fields are **write-only** — saved, reloaded, never consulted. This one is **read-only** — three consumers and no producer — which is not dead weight but a **broken dependency**, and a `check:deadconsts` scoped to *unread* fields steps straight over it. A gate that wants to catch both must check **both directions of the reader/writer census**, and the cheapest place to run it is over gate-leaf flag names in `QUEST_DB`, where every name is a literal. **§DOC-02n — 2nd instance of the read-only class:** `S_story.fishingBaitSatchel` is declared `false` with **2 readers and 0 writers**, so the bait-satchel discount can never be earned and the Yugurt Lake find DC is permanently `10 − LuckMod` rather than the 8 the engine is written to offer. Confirms the §DX-02u finding that a `check:deadconsts` scoped to unread fields walks straight past a read-only field — the gate must census readers and writers **separately** and fail on either being zero.

- [ ] **§DUNGEON-01-FU — §3's Tribble lore reversal is contradicted by the shipped item text, and the flag it says it removed never existed (found 2026-08-11 during §DOC-02k). Small design call: keep the revision or retire it.** The §DUNGEON-01 lab report's §3 is a locked decision — *"Tribbles do NOT multiply"*, *"no `tribbleCount` growth formula"*, *"not Star Trek references"*, *"they do not occupy inventory slots — `tribbleCount` is a bare counter"*, and the Brynn overflow line *"is removed from the IN node render block"*. **HEAD contradicts four of the five in player-facing strings.** (a) Every Fuzzy Tribble is an inventory object whose `desc` asserts multiplication — `` `You are fairly certain it is multiplying@21851` `` on the `quest_d0208_a2` grant and *"Soft. Multiplying."* on the three granted by `quest_d0208_a4`. (b) Those same sites increment `tribbleCount` **and** push inventory objects, so the "bare counter" is double-bookkeeping. (c) The Brynn line ships at `_tc >= 10` in the Mimic Meadows panel — *relocated* from the inn, which is why grepping the IN block finds nothing. (d) The specified 1–2 / 3–4 / 5+ bands shipped as `>= 5` / `>= 10`, and the 5+ band's **CY** flavour line was never built. **`tribbleOverflow` itself is clean — 0 hits and 0 commits ever**, which is the point: the flag §3 declares it is removing was never implemented, so the section describes undoing something that had never been done while the *content* it believed it was deleting shipped under a different threshold in a different block. **The design call is one sentence:** does the world keep the report's corruption-not-multiplication lore (in which case four `desc` strings and one threshold line are copy edits) or accept the shipped Star-Trek reading (in which case the lab report's §3 stays as a NOT-SHIPPED record and nothing changes in the HTML)? **No engine risk either way — all five sites are strings.** **The durable half → the 13th §DOC-02 instrument: a design doc's REVERSAL is a claim like any other.** A section that says *"the previous spec had X; this is cut"* asserts both that X existed and that it is gone, and `git log -S` is the only thing that can check the first half. Also note what makes this one invisible: §2's four invented mimic statlines have **0 commits each**, yet all four creatures live at `LIM` **as prose** in the node text — **the narrative was copied and the illustrative statblocks were not**, so a reader checking the meadow against the report sees the creatures and concludes the section shipped.

- [ ] **§AUDIT-03aa — "Boy Scouts" names two different mechanics with opposite rationales, in two spellings (found 2026-08-11 during §DOC-02j).** The 2026-05-21 balance report specified one award: *"when the player rests at a non-inn location… double the standard short-rest HP recovery,"* rationale *"sleeping rough is harder… a ranger sleeps better under stars than in a tavern."* **Two mechanics now carry the name and only one of them means that.** (a) **Short rest** — `` `Boy Scouts Award — doubled!@25860` ``, fired on `!isInn`. This is the spec exactly. (b) **Long rest** — `` `Boyscout Night! Double rolls@36300` ``, fired on `` `const _rollCount = _isFirstSleep ? 2 : 1@36238` ``: a **first-visit** bonus that doubles the sleep heal dice and **fires at an inn**, which is the precise inverse of the rationale the name was coined for. A player sleeping in the softest bed in the world is told they are camping. The two also disagree on spelling (`Boy Scouts` / `Boyscout`), so neither greps the other, and the report's own term *"Boy Scouts Camping Award"* has **0 commits in the file's history** — the name in the design doc never shipped at all. **Small design call: which mechanic keeps the name.** Recommend (a) keeps it — it is the one the name describes — and (b) is renamed to something first-visit-shaped (*"First Night"*), since its actual design is a novelty bonus for a new bed, which is a good mechanic wearing the wrong word. **Risk:** low, both are display strings plus one comment. **Verify:** grep both spellings to zero overlap, and a Playwright pin on each message firing on its own trigger. **Why it is an §AUDIT-03 row and not a typo:** this is §DOC-02c's retired-vocabulary hazard in its *live* form — there, §TIMELESS-01 removed "Hunt" and §KG-01 reintroduced a different mechanic under the same name 12 days later. Here both are live simultaneously, which is worse: the collision cannot be dated, and no doc distinguishes them, so every future sentence about "the Boy Scouts bonus" is ambiguous by construction. **EXTENDED 2026-08-12 (§DOC-02o) — the sharpest instance in the family, and the first that is MECHANICALLY CHECKABLE: a tombstone that outlived its own corpse.** `_updateHuntBtn / storyToggleHunt removed@38096` states that §TIMELESS-01 removed both functions with the Hunt feature. `function _updateHuntBtn() {@38057` and `function storyToggleHunt() {@38068` are declared **39 and 28 lines ABOVE that very comment**, are live, and `_updateHuntBtn()` is called from `storyRender`. §KG-01 re-minted both names in place for a different mechanic (×2 encounter rate + an 80/20 level bias, vs the retired corridor Hunt's 6× quest-target weighting) and left the tombstone standing — so the collision here *is* datable, and the engine documents the wrong side of it. Two milder same-pass instances: `// Waypoint BFS direction (§CELL-09)@37503` sits directly above `wpDir = _roadGridDir(@37507` (`_bfsPath` has 0 occurrences), and `no level/magic gate (Finders Keepers)@25428` contradicts the callee it invokes (§FISH-02). ***This gives the row a detector that needs no judgement: a comment asserting a symbol was REMOVED, where that symbol still resolves in the file.*** Three hits today, all true positives; cheap enough to sit beside `check:deadconsts`.

- [ ] **§DESIGN-04 — nothing in the live game answers *"where do I find monster X?"* (found 2026-08-11 during §DOC-02c; 🟡 DESIGN CALL).** The 2026-05-21 battleground report was written to eliminate the "lost in a field" failure mode, and its instrument was `HUNTING_GROUNDS` — a bijective terrain→node table making the question an O(1) lookup. **§TIMELESS-01 (`7952752`, 2026-06-26) deleted it and nothing replaced it.** Measured at HEAD: no bestiary locator, no terrain index, no per-monster habitat surface — **398 monsters across 111 terrains and 416 nodes**, with the only route from "I need a wolf" to "I am fighting a wolf" being to walk until the seeded stream rolls one. By the original report's own criteria the failure mode it named is currently unaddressed. **This is a design call, not a defect**, because the modern game may legitimately answer the question a different way: quest `activateNode` puts the player where the content is, and the Warrant's Board (§BOARD-01) is already the social discovery surface. **The three options to put to the user:** (1) DECIDED-STAY — arrival-driven content is the answer and no locator is wanted; (2) a read-only **bestiary** surface listing, per monster, the terrains it appears in (data already exists — `WORLD_DB[t].monsters` inverted) with no navigation affordance; (3) route it through the Board as a bounty that names its terrain. **Do not build before the call.** Note the invariant fence on any option: a locator may *inform* movement but may never shortcut it (invariant #3, `` `no jump travel. checkpointNode@26050` ``).

- [ ] **§FISH-01 — the entire Yugurt Lake fishing sub-game is unreachable, because one node is declared 59 lines too late (found 2026-08-12 during §DOC-02m; 🔴 NO DESIGN CALL).** `` `const CELL_GRID = (() => {@9852` `` builds each cell's locale list by iterating `Object.keys(NODE_MAP)`, and only `list[0]` — the primary — can ever become `S_story.currentCode` (`` `S_story.currentCode = destCode;@28375` ``, where `destCode = res.destCodes[0]`). `` `LYR:{r:2,c:194}@9423` `` and `` `BOO:{r:2,c:194}@9422` `` occupy the same cell, and `` `LYR:{ num:41@8723` `` (Arctic Wastes — Detour, act 7) is declared **59 lines before** `` `BOO:{ num:75@8782` `` (Yugurt Lake, act 3). So `CELL_GRID['2,194'] = ['LYR','BOO']`, arriving there renders the arctic node, `` `const hasFish = node.isFishingLake;@35349` `` is never true, and `` `main: 'Cast a Line',@35353` `` — the **only** call site of `` `function storyFishing() {@30394` `` — never draws. `BOO` is `sleep:false`, so `checkpointNode` cannot reach it either. **Stranded: 11 quests** — 3 cannot activate (`quest_horned_shark` · `quest_shale_drop` · `quest_night_eel`, all `activateNode:'BOO'`) and 8 cannot complete, because their `completion` reads `fishingCatchLog`/`fishingQuestFlags` and `` `S_story.fishingCatchLog.unshift@30616` `` is the **only writer in the file** (`quest_fishing_guide`, `quest_fish_01`, and the six-part Emmer Finch arc `quest_guide_01`–`06`, whose whole subject is learning to fish). Plus **~100 authored data rows** — `` `const FISH_POOL = [@26506` `` (20) · `` `const NIGHT_FISH_POOL = [@26528` `` (5) · their 25 `MONSTER_POOL` statlines and 20 trophy drops · `` `const LAKE_MAGIC_DB = {@26538` `` (8) · `` `const BAIT_TABLES = {@26634` `` (3×6) · `` `const FISHING_RODS = [@26702` `` (4) · `` `const FISH_GOLD_VALUES = {@26694` `` (25) · `` `const FISH_SIZE_TIERS = [@26687` `` (5) — two flag writers only a fish battle reaches (`` `hornedSharkSlain = true;@7051` `` · `` `lanternEelLanded = true;@7052` ``) and the `` `id:'ke_eel_pouch'@26230` `` key event. **The cabin survives**: `` `SSJ:{ num:76@8786` `` is alone in cell `4,192`, so the Fisherman, the free bed (`sleepCost:0`) and the six-quest §XLV tournament all render — **the player can meet the Fisherman and never fish**, which is why this reads as content-missing rather than as a bug. **This is §AUDIT-03x's class and its worst single-node instance by stranded volume** (§CROWN-01 lost 7 nodes and 24 quests, but its mechanics existed elsewhere; here one non-primary node takes an entire self-contained sub-game). **It does NOT wait on §AUDIT-03x's design call** — the fix is two lines: move `BOO`'s `NODE_MAP` declaration above `LYR`'s (cheapest, and `CELL_GRID` order is the only thing that reads it), or relocate one of the two to a free adjacent cell. Verify by asserting `cellCode('2,194') === 'BOO'` and that the FISH section renders on arrival. **IMPACT NOTE 2026-08-12 (§DOC-02o) — the tutorial points at it.** Yael's Level-1 monologue in `NPC_DIALOGUES.yael.impartial` — the first substantial text a new player reads — tells them the edge they need *"the smiths in this city cannot sell"*, and to *"go north to Yugurt, to the cabin, and find the old man who keeps it — they call him the Fisherman. He hands the rod to anyone who will use it, free… The water gives up what no shop stocks."* The cabin (`SSJ`) renders and the Fisherman is there; the lake never opens its surface. **So the game's own onboarding directs every player to the one sub-game that cannot run** — which is why this reads as content-missing rather than as a bug, and why it should be fixed before any §FISH-02 option is chosen.
> **⟶ §DOC-02ch (2026-08-18) attaches the PLAYER-FACING consequence, and it raises the cost of leaving this open.** Since `cfdeb21` (2026-07-12) the game's **only tutorial speech** — `` `yael: { meta: { name:"Yael Scheidemann"@10397` ``, the 426-word onboarding monologue every fresh character is shown at `LHR` on turn one — names this destination explicitly: *"Go north to Yugurt, to the cabin … Go to the lake early."* Before §PLAY-01-D the pointer was vague enough to be harmless (*"a fishing dock … a slow river"*). **The honesty fix made the unreachable promise specific**, and it is now the first instruction a new player receives. Measured at HEAD: exactly **1 node of 416 carries `isFishingLake`** and it is `BOO`; **15 nodes carry "dock" or "river" in their label** and none is fishable. The cabin end still works — `SSJ` is alone in its cell, so the player walks the recommended route, meets the Fisherman, takes the free rod, and finds no water. ***This row is no longer only content-missing: it is the destination of the tutorial.*** Sequencing note unchanged (§FISH-01 gates §FISH-02), but the player-safety weight now sits closer to §DX-02dk than to a content backlog row.

- [ ] **§FISH-02 — the magic-weapon economy is a two-sided trade and only the TAKING half shipped: no live path grants a positive-bonus weapon (found 2026-08-12 during §DOC-02n; small design call).** `lab-report-fishing-bait-prompting.md` specified a trade — nerf monster drops to negative bonuses so that fishing becomes the sole source of positive ones. **Taking, shipped and stronger than specified:** `_rollMonsterWeaponDrop` filters its pool to `w.magicBonus === 0 && !ownedTiers@24589` and degrades by `deg = Math.min(0, d6 - 5)@24594` — **−4…0 on a d6**, against the specified −3…0 on a d4 (the prefix `Salvaged` has 0 commits ever; the shipped set is Wrecked/Rusted/Chipped/Worn). **Giving, never built:** `_fishingMagicWeaponDrop` and `ac / 4` have **0 occurrences and 0 commits ever** — the fishing victory path only overrides the drop's *sell* value with size×rarity gold, and the reward that did ship is §DROP-03's `LAKE_MAGIC_DB = {@26538`, passive stat items rather than weapons. **And the third path closed silently:** `_rollD100Loot` still branches on `w.magicBonus === row._magic@24569` and on a `dagger` type, but `_D100_TABLE` holds seven rows (potions/scroll/flashbang/gold) totalling `total weight = 100@24526` and contains **neither type** — so both branches are unreachable and `_magicTierAllowed(magic) {@24511`, whose only two callers sit inside them, is a **dead function**. **Net:** `WEAPON_ITEMS = [0, 1, 2, 3, 4]@24496` generates 5 magic tiers × 12 base weapons = 60 items and **the 48 with `magicBonus` 1–4 have no live grant path**; no positive `magicBonus` literal exists anywhere in the file; the player's main hand is permanently capped at base tier or degraded, which also makes `_WEAPON_SELL`'s +1…+4 tiers and the `minLevel: magic*5` gating dead weight. **The engine states the opposite about itself** — `FC06: monster drops capped at base tier@24588` continues *"fishing is the only source of +bonus weapons"* — §DOC-02g's rule in live form: an engine comment is a claim, not authority. **The call is which path restores them:** (1) re-add `mainweapon`/`dagger` rows to `_D100_TABLE` (smallest change, restores the pre-nerf loot vector the report wanted removed); (2) build the specified `_fishingMagicWeaponDrop(fishAc)` (matches the design intent and the player-facing `Weapon bonus = floor(fish AC / 4)@26681` promise, **but is blocked behind §FISH-01** — fishing is unreachable, so this ships the fix dark); (3) a vendor/quest grant. **Sequence after §FISH-01.** Note the nerf itself is working as intended and should NOT be reverted. **CORRECTED + EXTENDED 2026-08-12 (§DOC-02o).** (i) **Census correction, instrument 14:** the denominator *"48 of 60"* is wrong. `const _BASE_WEAPONS = [@24473` holds **14** entries and `const WEAPON_ITEMS = [0, 1, 2, 3, 4]@24496` flatMaps five magic tiers, so the generated set is **70** and the unreachable count is **56 of 70** — only the 14 `magicBonus: 0` entries have a live grant path. Same fact, right figures. (ii) **The origin document is `lab-report-friendships-with-magic.md` (2026-05-22), and the reversal is now dated.** The archive's `_rollMonsterWeaponDrop` is byte-exact to that report's spec, comment included (`// Finders Keepers: no level/magic restrictions — die constraint only`); HEAD adds `WEAPON_ITEMS.filter(w => w.die <= monsterDmgDie && w.magicBonus === 0@24589` and `const deg = Math.min(0, d6 - 5)@24594` under `FC06: monster drops capped at base tier@24588`. (iii) **A live comment contradiction inside one call chain:** the original rationale still reads `no level/magic gate (Finders Keepers)@25428`, one line above `const _wpDrop = _rollMonsterWeaponDrop@25430` — two comments ~800 lines apart asserting opposite contracts, both live. (iv) **NEW sub-finding, player-facing and independent of the design call:** the −1…−4 degradation **is** applied to attack and damage via `S.weapon.flatMod = mw.magicBonus@24671`, but three of the four surfaces that render a bonus print it **only when positive** — `_wpCopy.magicBonus > 0 ? ' +'@25444` (victory drop line), `const tag = mw.magicBonus > 0 ? dieStr@30920` (inventory tag) and `it.magicBonus > 0 ? ' +' + it.magicBonus + ' atk/dmg'@31097` (item detail). Only the Character Sheet Main Hand row signs it. **A "Wrecked Long Sword" reads as a plain Long Sword everywhere the player picks it up** while costing −4 to hit and damage. The display code was written when a bonus could only be ≥ 0 and the nerf did not revisit it — fix is three `> 0` → `!== 0` with a sign, and it is worth doing **regardless of which option (1)–(3) is chosen.** **NARROWED + EXTENDED 2026-08-12 (§DOC-02v), and the narrowing changes option (1)'s cost.** (v) **The `dagger`/`mainweapon` branches are DORMANT BY DATA, not dead code, and `_magicTierAllowed` is not a dead function.** `lab-report-loot-drop-system-v2.md` §IV-A retains them **deliberately** — *"a content author can still PUT those entry types via the server if desired, but they will not fire unless manually re-added to the table"* — and the measurement upholds it: `src/js/wbapi-server.js:WBAPI.d100Table = entries.map@3480` explicitly preserves `_magic` on write and `src/js/wbapi-server.js:function serializeD100Table(entries) {@1855` emits it, so a single `PUT /api/loot` re-arms both branches with no HTML edit. **Option (1) is therefore not a code change at all — it is one API write**, which is the cheapest of the three by a wide margin and reversible the same way. *(Corpus rule, 3rd instance: before filing a spec-vs-engine gap, check whether a sibling report specifies the thing you are about to call a defect — the −4…0 d6 recorded above as a deviation from the d4 spec is likewise **specified**, by this same report's §III-B, twelve days after the d4 one.)* (vi) **NEW, and it widens the gap past main weapons:** `const DAGGER_ITEMS = [@24460` — the +1 Royal / +2 Painite / +3 Gaping / +4 Voidsteel daggers, priced 300 / 900 / 2 500 / 8 000 gp — has **exactly one consumer in 38,712 lines**, the dormant `dagger` branch, and **no vendor stocks them**. So the unreachable set is **56 of 70 main weapons *plus* 4 of 4 magic daggers**, and `const LAKE_MAGIC_DB = {@26538`'s 8 items are dark behind §FISH-01 — **at HEAD there is no live grant path for positive-magic equipment of any kind.**
> **⟶ §DOC-02ch (2026-08-18) — the first PROOF BY EXECUTION of this row, and it confirms every static figure.** Prior passes read the code; this one drove it in the running game (throwaway probes, run and deleted). **20,000 `_rollD100Loot()` calls at level 20** returned only potions, scrolls, flashbangs and gold caches — **0 mainweapons, 0 daggers, ever**. **20,000 `_rollMonsterWeaponDrop()` calls across every damage die (4/6/8/10/12)** returned **0 positive-bonus weapons and 13,369 degraded ones** — 66.8 %, against the 66.7 % `` `const deg = Math.min(0, d6 - 5);@24594` `` predicts. Confirmed live: `WEAPON_ITEMS` = **70**, **56** with `magicBonus` 1–4; `` `const DAGGER_ITEMS = [@24460` `` = **4 entries, all with a positive `atkBonus`, and its only reader is the unreachable `_D100_TABLE` branch — the whole table is dead**, which this row implied but never stated; `` `const LAKE_MAGIC_DB = {@26538` `` = **8 items, 0 of `type:'mainweapon'`**. **New player-facing weight:** since `cfdeb21` the onboarding monologue tells every fresh character *"The smiths in this city cannot sell it; the lake can … The water gives up what no shop stocks."* The lake's eight trinkets do include real attack and AC bonuses, so this is not a lie — but it is a sentence about **weapons** in a game where the weapon half of the trade was never built, now said on turn one. Pair the fix with a re-read of that string.

- [ ] **§FISH-03 — the Fishing Guide's central table promises a per-zone difficulty gradient the code does not implement (NEW 2026-08-13 during §DOC-02be; 🟡 small design call).** `const FISHING_GUIDE_TEXT =@26661` is the reward readable for `quest_fishing_guide`, and its headline block is three DCs — *"Shore (The Bank) — DC 8 · Reeds — DC 12 · Deep (The Shallows) — DC 16"*. The bait-search roll computes `Math.max(4, (S_story.fishingBaitSatchel ? 8 : 10) - _luckMod())` **once**, at `const dc     = Math.max(4, (S_story.fishingBaitSatchel ? 8 : 10)@30493`, and the zone (`loc`) is **never consulted** — all three share one DC and **none of the three printed numbers is it.** Compounded by **§DX-02u**: `fishingBaitSatchel` has 2 readers and 0 writers, so the DC-8 branch is unreachable and the true value is a flat `10 − LuckMod` in every zone. **The design is good and only the branch is missing** — deep water *should* cost more to work, and the gradient is what makes the three zones a progression rather than three names. **The call is which way to close it:** (a) implement it — read a per-zone DC off a `{bank:8, reeds:12, shallows:16}` table beside `const _zoneMap = { bank:'shore'@30475`, which makes the readable true and gives the Deep gate teeth; or (b) correct the prose to state the one real DC and what actually varies (bait quality, not difficulty). **(a) is recommended** — the readable is a *quest reward*, and a reward that describes a mechanic the game does not have is worse than no reward. **Blocked in practice by §FISH-01** (the guide is obtainable at `SSJ`, but the search UI it describes lives behind the unreachable `BOO`), so ship it in the same pass. **Risk:** low. **Verify:** all three zones searched with the same character produce three different DC strings matching the guide; a Luck-modified run still floors at 4.

- [ ] **§DX-02v — three home docs describe the retired 2d20 fishing cast as the current mechanic (found 2026-08-12 during §DOC-02m).** `world.md:160` says *"**Current system (Layer 37):** 2d20 cast roll → fish rank 1–20 → immediate combat"*, `story.md:811` says *"**Current Mechanic (Layer 37):** … The player rolls 2d20 to determine which fish answers"*, and `monsters.md` says it three times (`:616` *"used exclusively by the fishing system (2d20 cast; fish rank must fall within [lo, hi])"*, `:753` in the milepoint trace, `:780` in the function table). **HEAD has not run that mechanic since Layer 47**, which replaced it with a four-phase Catch system (DEX cast vs DC 12 → Catch roll → size tier → Type roll for rarity) — see `` `function storyFishing() {@30394` ``. This is the §DOC-02j `LOOT_TABLE` rot direction and the more dangerous one: a reader greps the home doc and concludes the code still does it. Two riders in the same row: `` `const FISH_POOL = [@26506` ``'s own comment reads *"20 ranks, Needle Minnow → Yugurt's Leviathan"* while rank 20 is `Yugurt's Dread` and **no fish is named Yugurt's Leviathan** (rank 17 is `Barbed Leviathan Trout`); and `monsters.md:780` anchors `storyFishing()` at a bare **11537**, which is its *archive* line — the function is at 30392 — a bare-number anchor `check:anchors` cannot audit because it carries no symbol (§DX-01e). Fix: rewrite the three docs to the four-phase system, correct the engine comment, and migrate the bare anchor to `symbol@line`. **§DOC-02n extends this to `monsters.md`:** line 317 still certifies *"All 370 keys are present and unique in `MONSTER_POOL` — verified 2026-05-24"* against **398** at HEAD, and line 645 scopes bait fish to *"`isFishingLake:true` nodes (currently YL only)"*, a retired node code. **Counterpoint worth keeping:** the same file's `BAIT_FISH_POOL` rows (315, 643) are still correctly marked **⚠️ PLANNED** for a constant with 0 commits ever — 80 days on, the planning directive working exactly as designed, which is why this row is doc-rot repair and not a doc-system indictment. **EXTENDED 2026-08-12 (§DOC-02v) — one member of this row is NOT doc rot, and the distinction matters for how it is fixed.** `docs/mechanics/mechanics-economy.md:253`, `docs/mechanics/mechanics-combat.md:193` and `mechanics.md:209` all say `LOOT_TABLE` was *"removed; replaced by comment stub."* **They were true when written** — `664fcf8` (2026-06-05 11:24) synced them against a tree where `440eb5d` had deleted the array 43 minutes earlier — and became false at 13:33 that afternoon when `88d41d1` silently reverted the code (→ §DX-02aa). **The docs did not drift; the code moved back under them, with no commit message saying so.** Fix the *code* (§DROP-01-FU (a)) and all three lines become true again — do **not** rewrite them to describe the reverted state. Rider on the same file: **`mechanics.md` contradicts itself** — `:209` says the array was removed, `:1016` documents it as a live `const array[20]` (*"d20 drop table — 8 Minor / 2 Spell / 5 Healing / 3 Greater / 2 Superior"*, which is exactly what `const LOOT_TABLE = [@24443` still holds). Whichever way §DROP-01-FU goes, one of the two lines must change.

- [ ] **§AUDIT-03ab — six maintained home docs are classified HISTORY by gate #16 and are therefore exempt from the sweep they most need (found 2026-08-12 during §DOC-02m; small call).** `src/scripts/legacy-codes.js` lists `monsters.md`, `mechanics.md`, `quest.md`, `index.md`, `docs/mechanics/mechanics-combat.md` and `docs/mechanics/mechanics-economy.md` in `HISTORY_FILES`, whose stated contract is *"records of what was true when written"* — annotate, never rewrite. **But these are precisely the home docs prompt.md §2 step 6 requires every increment to sync** (*"`monsters.md` for monsters, `mechanics.md` for systems"*), and `mechanics-combat.md` is the target of **five `// → doc:` pointers in the fishing block alone**. It carries live prose reading *"Obtained from The Fisherman at **YC** (free, no gold cost). Required to trigger `storyFishing()` at **YL**"* — two dead codes in an instruction a reader would follow, unreportable **by classification**, while the file's own comment above `PENDING` asserts *"every live doc is now gate-fenced."* Same class as §AUDIT-03z (a live home doc outside every gate's universe) in a sharper form: not outside the file filter, but inside it and misfiled. The call is which of the six move to `SWEEP` (all six are maintained; `index.md` and `quest.md` are generated-ish and may want a third classification) — then sweep them, remembering §AUDIT-03m's lesson that **annotation without verification launders a wrong claim into a confident-looking live one.**
> **⟶ §DOC-02ci (2026-08-21) — FIRST CONCRETE PLAYER-FACING INSTANCE, and it is a line the classification actively protects.** `mechanics.md:854` reads *"The six curated Birka NPCs: Yael (CI), Brynn (IN), Quill/Couperin (TV), Pachelbel/Deacon (BA), Weckmann (CY), Auros/Bruhns (CY)"*. The engine was remapped off those codes by `a6a1ce7` (§PLAY-01-G, 2026-07-12) and now reads `` `const birkaNpcs = {@35122` `` → `LHR`/`TLL`/`MHQ`/`LLA`/`HKG`/`CDG`; **`IN`/`TV`/`BA`/`CY` resolve in neither registry at HEAD** (`CI` does — it is *Chancery Court*, a live node that is simply not the start node, per instrument 129). **The code fix shipped 40 days ago and the doc still carries the pre-fix addresses**, in the file `prompt.md` §2 step 6 names as the required sync target for systems work — and gate #16 cannot report it, because `mechanics.md` sits in `HISTORY_FILES` in `src/scripts/legacy-codes.js`. The count (six) and the cast are both correct; only the addresses are stale, which is exactly the shape that reads as verified.

- [ ] **§AUDIT-03ac — the Codex Core "Destroy" branch advertises a curse penalty, writes an undeclared field with zero readers, and shadows the real scoring function (found 2026-08-12 during §DOC-02p). NO DESIGN CALL.** `⚡ Destroy the housing — STR Ceremonia Roll DC 15@31828` labels the button *"(bypass Auros, +curseScore)"*; passing it runs `S_story.curseScore = (S_story.curseScore || 0) + 5;` and prints *"⚠ You broke something that could not be rebuilt. [curseScore +5]"*. **`function _curseScore() {@28193` recomputes the score from `_EB_CODES` on every call and never reads that field.** `S_story.curseScore` has **three occurrences in 38,712 lines** — the button label, its own self-referencing write, and the message string — **zero readers**, and it is **not declared in `_S_DEFAULTS()`**, so it is also an undeclared persisted field (§STATE-INIT class: the seed literal is supposed to be the single source of truth for new state). The advertised penalty for the game's sharpest moral shortcut — destroying the housing rather than confronting Commander Auros — costs the player nothing at all. **This is the §AUDIT-03v/w cluster's 9th instance and its worst shape:** the previous eight promise a mechanic that does not exist, so a grep finds nothing and the reader knows. This one names a mechanic that **does** exist, under its real identifier, and writes into a shadow of it — a reader greps `curseScore`, finds a writer, a reader and a player-facing label, and concludes the loop is closed. ***The detector this argues for is narrower and cheaper than the cluster's usual one: a persisted field whose name differs from a live function's by one leading underscore, with writers but no readers.*** **Fix (two lines, no call):** either delete the field and have the Destroy branch record its penalty through a term `_curseScore()` actually sums, or make `_curseScore()` add `S_story.curseScore` — the second is the smaller change and preserves the authored intent, but note it interacts with **§ENDING-01**, since it moves the score in the wrong direction (up), away from the endings that are already unreachable.

- [ ] **§EPIC-04 — the engine states two incompatible counts of the same in-fiction document, on two reachable surfaces, and has since the earliest surviving build (found 2026-08-12 during §DOC-02p). SMALL DESIGN CALL: which count is canon.** Warlord Mordus's negotiation line says the Void Shaman Kazrath's journal `mentions Birka. Twice.@26498` (*"You're right to press. Find out what he knows and we'll both pay better."*); Froberger's Journal Entry 26 says it `mentions Birka forty times.@27212` (*"The Master says. The Master believes."*). **Both strings are present verbatim at `32c10c5`, so this was born contradictory** — it is not drift, and no rename or migration is involved. **Both surfaces are reachable:** `negotiateLine` is read at three sites in the negotiation panel, and Entry 26 sits on `TRD`, a primary node in its cell, so `function storyCheckJournal(node) {@30211` will deliver it. The detail is plot-bearing on both ends — the count is the evidence for how deeply Birka is implicated in the Void, which is the through-line into the Auros reveal. The fix is one string; the call is whether the intended reading is *"two references, worth pressing for"* (Mordus's framing, a lead) or *"forty references, an obsession"* (Froberger's framing, a confession) — they characterise Kazrath differently, so this is a writing decision, not a typo. Found by reading a lab report that quotes **both numbers correctly, in two different sections**, which is what surfaced the disagreement: neither surface is wrong about itself.

---

## §RESUME — Phase 1 history

> **Completed work is not carried here.** The 11 closed increments for this phase are condensed in **[`../archive/backlog-resume-history.md`](../archive/backlog-resume-history.md)**; full prose for recent closes is in [`plan-archive.md`](plan-archive.md). This file carries open rows only.

> When an increment ships: write the full entry into `plan-archive.md`, add its one-line row to `../archive/backlog-resume-history.md` and to the cross-phase table in [`BACKLOG.md`](BACKLOG.md), and delete the row from this file.
