<!-- SPDX-License-Identifier: MIT — Copyright (c) 2026 Paul Richeson -->
# BACKLOG — Phase 4: World & Navigation — nodes, terrain, geography, movement

> **Phase 4 of 6.** Split out of `BACKLOG.md` on 2026-08-23; `BACKLOG.md` is now the routing index and holds the chronological §RESUME map across all six files.
>
> **Scope of this file.** The map and moving on it: node registration and labels, terrain tables and encounter rates, GEO anchors and projection bounds, road nets, movement results.
>
> **Siblings:** [1. Playable Truth](BACKLOG-1-playable-truth.md) · [2. Engine & Systems](BACKLOG-2-engine-systems.md) · [3. Content & Narrative](BACKLOG-3-content-narrative.md) · **4. World & Navigation** *(this file)* · [5. Platform & Tooling](BACKLOG-5-platform-tooling.md) · [6. Verification & Documentation](BACKLOG-6-verification-docs.md)

## The Backlog Directive — restated in full

> **This file is the list of outstanding activities for Phase 4 (World & Navigation).** The *how-we-work* rules live in **[CONTRIBUTING.md](../../CONTRIBUTING.md)**; closed/shipped work is archived in **[plan-archive.md](plan-archive.md)**. Split out of the former `plan.md` on 2026-07-09.
>
> **⚠️ Before starting ANY row — Existing-Work-First (CONTRIBUTING.md § Existing-Work-First Policy):** `git status` + `git log` first (uncommitted/fresh work matching the row = it's done or in progress — finish it, don't redo it); check for a second live session (`ps aux | grep claude`) before editing shared files; grep to *disprove* the row's premise (rows go stale — four rows have closed as ALREADY SHIPPED). A §RESUME entry says where the last session *stopped*, not what the tree looks like *now*.
>
> **Legend:** `[ ]` open · `[~]` in-progress / recurring · `[x]` closed (kept here for context; migrate to plan-archive.md over time).

> **⚠️ This file is one of six.** A row's neighbours may live in a sibling phase file, and the Existing-Work-First grep MUST cover all six — `grep -n '§DX-02xx' BACKLOG*.md` — before you conclude a row is unclaimed. The §RESUME entry that closed a row may sit in a different phase file than the row itself.

## Standing Directive — Code Comments (user directive 2026-08-23)

> **Binding on every increment in this file.** Where this and a §-row's own instructions disagree, this wins.

**The failure this prevents:** writing a comment that narrates the change being made right now. That context is real, but it expires the instant the change merges — the defect it describes no longer exists, so the comment becomes a story about a problem no future reader can observe. It is a changelog entry in the wrong file, and a third copy of text already required in the commit body and the PR description.

- **CC-1 (MUST NOT)** Write a comment describing a change, a fix, a defect, its cause, or what the code used to do. No *"was / now / previously / instead of"*, no *"this fixes"*, no *"needed because otherwise"*, no *"note that we no longer"*.
- **CC-2 (MUST)** Apply the **survival test** to every comment before writing it: *would this still be true and useful to someone reading this file a year from now, who never saw the diff?* If it only makes sense beside the diff, it is changelog — delete it and put it in the commit body.
- **CC-3 (MUST)** Default to **zero** comments. Declarative config — Terraform, DNS records, k8s manifests, CI YAML, Helm values — is self-describing and takes none. A resource named `dmarc-example-com` does not need a comment saying it is the DMARC record.
- **CC-4 (MAY)** Comment only when a future editor would actively break something without it: a non-obvious external constraint, a required out-of-band manual step, an invariant the surrounding code cannot show. **One line.** If it needs a paragraph it belongs in a design doc, not inline.
- **CC-5 (MUST)** Before every commit, re-read the comment lines added: `git diff --cached | grep '^+' | grep -E '#|//|/\*'`. Each hit must pass CC-2 on its own. **Deleting is always an acceptable outcome.** *"I already wrote it"*, *"it is only one line"*, and *"this one is genuinely useful"* are not exemptions — the last one is the exact thought that precedes every violation.
- **CC-6 (MUST)** Applies to comments **edited** as well as added. When a change invalidates an existing comment, the default action is **DELETE**, not rewrite it into a new narrative.

> **⚠️ KNOWN TENSION, flagged not resolved — needs a user call.** This repo currently runs an *opposite* convention in `play.html`: **§AUDIT-03n / §DX-02c "annotate, don't rewrite"**, which deliberately preserves defect history inline (e.g. `const NODE_NPC_KEYS`'s 8-line comment explaining that 21 of 26 rows named retired node codes, or `_renderFinalMap`'s §AUDIT-03e note on why the node filter is held at the historical set). Those are exactly the *"what the code used to do"* narratives CC-1 forbids, and several are load-bearing: they are the only record of why a value is held where it is, and deleting them would re-open closed audit rows. **Until the call is made, the working rule is:** CC-1..CC-6 bind all **new** comments; existing §AUDIT-/§DX-annotated blocks are **not** swept under CC-6 — they are a documented archaeology convention, not accidental changelog. Filed as **§DX-02fa** below.

---

## Open at a Glance — Phase 4 tracks

- **§NAV-01-FU deferred** — map "jump to region" centering (needs pannable-view design; out of scope as a low-risk follow-up).

---

## §BACKLOG — Open Items (Phase 4)

### §DX-02ek — the beginner grind band paces exactly like open wilderness, because none of its five terrains has an encounter rate (NEW 2026-08-22 during §DOC-02cw, 🟡 five table entries, ONE DESIGN CALL)

- [ ] **§DX-02ek — the §KG corridor exists to be ground, and it rolls at the generic default.** 🟡 **the numbers are trivial; the call is what they should be.** None of `soviet_checkpoint`, `komsomol_school`, `gladiator_zavod`, `skill_fabrika`, `soviet_transit` appears in `const TERRAIN_ENCOUNTER_RATE = {@9892`. Both consumers resolve them through the `?? TERRAIN_ENCOUNTER_RATE._default` guard — `let baseRate = _partyEncounterRate(TERRAIN_ENCOUNTER_RATE[terrain]@28436` and the server-side twin — so all five run at **0.15**, byte-identical to `midlands`, doubled to 0.30 under Hunt Mode.
> **Why it matters, measured.** The whole huntable surface of the band is **nine cells** (SPB `10,209`/`11,210` · KMS `12,211` · ZVD `12,212`/`13,213` · FBR `13,214`/`14,215` · TVR `12,216`/`14,216`); everything else in rows 8–16 / cols 203–220 is road (rate 0), a settlement, or sea. Level 6 costs 5,500 XP and the band's mean kill is 177.5, so a player needs ~31 average kills — about **200 steps of deliberate oscillation** between a zone node and the one cell beside it. `city` is tuned to 0.05 and `forest` to 0.25; the training band is the one place in the game where encounter frequency is load-bearing, and it is the one place that was never tuned.
> **THE CALL — what should a sanctioned-bout zone roll?** (a) **Raise the band (recommended):** give all five ~0.25–0.30, so the ramp is genuinely faster than wilderness and Hunt Mode's ×2 caps near 0.6. Fits the fiction — you are at a *training ground*, opponents are queued up. (b) **Split them:** `gladiator_zavod`/`komsomol_school` high (the sparring floors), `soviet_checkpoint`/`soviet_transit` low (they are hubs, not pits) — more faithful to §4's west→east ramp, five judgements instead of one. (c) **Declare the default correct** and close the row, recording in the lab report that the band is deliberately wilderness-paced. **(a) is the honest default** — the report never specified a rate, so nothing is being overridden, and the band's stated purpose is grinding.
> **Verify:** extend `src/tests/integration/kg-zones.test.js` with a sixth case asserting every §KG terrain key has an OWN entry in `TERRAIN_ENCOUNTER_RATE` (not a `_default` fallthrough) — the general assertion catches the next zone author too. Then re-run `npm run check:terrain` for the client/server parity fence.
> **Provenance:** §DOC-02cw, finding F6, `docs/lab-reports/lab-report-kg-russia-kindergarten-zones.md` §V/§VI.

### §DX-02el — twenty-six nodes announce themselves twice, and five of them were minted in one increment (NEW 2026-08-22 during §DOC-02cw, 🟢 five string edits, NO DESIGN CALL)

- [ ] **§DX-02el — `Nevsky Checkpoint · Nevsky Checkpoint`.** 🟢 **the header composes `nodeLabel · terrainLabel`; give the terrain a terrain name.** §KG Increment 2 gave each of its five nodes a dedicated terrain whose `label` is the node's own display name — `soviet_checkpoint:{ label:@6294` and its four siblings — so all five stutter in `#s-node-name`. **Measured in Chromium at HEAD**, every exemplar the §KG lab report modelled itself on gets it right: `City Streets — Birka · City Streets` (LHR), `The First Inn · Inn — Night` (TLL), `Station 7 (Dark) · Station 7 — Hidden Waypoint` (SVO). The terrain label is supposed to name the **kind of place**; the node label names **the place**.
> **The class is bigger than §KG.** A whole-corpus sweep (`WORLD_DB[n.name].label === n.label`) returns **26 of 416** nodes: `MAN · KIR · MSY · BGI · PDL · RAI · GIB · TRF · SID · TRD · NAS · FEZ · AHB · BEL · HER · BKK · BOO · SSJ · CDG · SZG · LIM · SPB · KMS · ZVD · FBR · TVR`. **Five of the twenty-six — 19% of the defect class — came from this one increment**, which is why it is filed here.
> **Fix (five strings, then twenty-one more if wanted):** rename the five §KG terrain labels to the kind of place — `soviet_checkpoint` → *Soviet Checkpoint*, `komsomol_school` → *Drill Hall*, `gladiator_zavod` → *Gladiator Pit*, `skill_fabrika` → *Jack-In Fabrika*, `soviet_transit` → *Transit Waystation*. Node labels stay exactly as authored. The other 21 are the same edit and can ride the same increment or a follow-up.
> **Verify:** a gate assertion — no node may satisfy `WORLD_DB[node.name].label === node.label` — added to `src/scripts/check-invariants.js` beside I1/I2, which already walks node terrains. That closes the class permanently instead of the five.
> **Provenance:** §DOC-02cw, finding F7. The §KG lab report's §7 step 5(d) said *"node cards render NPC + prose"*; they do, with the name said twice.

### §DX-02cb — seven live nodes have no lat/lon in any source, and the only thing that notices is a JSON field nobody reads (NEW 2026-08-14 during §DOC-02bn, 🟢 no design call)

- [ ] **§DX-02cb — the geo gazetteer has drifted seven nodes behind `NODE_MAP`, so §WALK-1.5's projection can no longer be re-derived for the world it produced.** 🟢 **seven gazetteer entries + one guard, ~20 minutes.** `POST /api/layout/geo-seed` resolves every node's lat/lon through a fixed priority — `GEO2` (155 anchors, inline in the server) → `gazetteer.realPlaces` → `gazetteer.anchors`, then cycle-safe chaining through `satellites`/`offEarth`. Measured at HEAD: **345 nodes resolve to their own coordinates, 64 chain to an anchor, and 7 resolve to nothing at all** — `DNF` (§CELL-14-FU Dunfall), plus `TGS`, `SPB`, `KMS`, `ZVD`, `FBR`, `TVR` (the §KG Russia corridor). All seven carry live `NODE_COORDS` and are reachable; what they lack is a *derivation*. `` `src/js/wbapi-server.js:if (!ll) { skipped.push(code)@7909` `` pushes each into a `skipped` array that is returned in the dry-run JSON and **never logged, counted against a threshold, or checked by any gate** — the summary line reports `${skipped.length} skipped` as an integer with no names and no failure. **This is not currently a data-loss bug:** the apply path merges (`for (const [code, p] of Object.entries(coords)) WBAPI.nodeCoords[code] = p`), so a regeneration leaves those seven where they are rather than clearing them. The defect is that the gazetteer is documented as the source of truth for the projection and is now **409/416 complete**, silently — so a re-seed reproduces the world *except* in seven places, and nothing tells you which. **Fix:** add the seven to `src/config/walk-geo-gazetteer.json` (six are real Russian/Baltic places with obvious coordinates; `DNF` is a §CELL-14 rename that wants an `anchors` entry with a `why`), and make the coverage explicit — either fail geo-seed when `skipped` is non-empty in apply mode, or add a `npm run` census that prints the names. **Related, not duplicate:** §DX-02bo asks whether `src/tools/worldmap.js`'s *retired 8–500 coordinate space* should be repaired or the verb retired; that is about the arithmetic, this is about the input table. If §DX-02bo is resolved by repair, gazetteer coverage is its acceptance test; if by retirement, this row shrinks to a one-line comment saying the gazetteer is frozen history. **Verify:** a geo-seed dry-run reports `0 skipped`; the dry-run's computed cells still reproduce all 416 live `NODE_COORDS` entries (the §WALK-1.5-FU(a)/(b) consistency check, re-run).

### §AUDIT-03bg — the SP/MP encounter divergence was one line in a lab report; it is now four, and the guard fences the half that never drifted (NEW 2026-08-14 during §DOC-02bm, 🟡 small design call)

- [ ] **§AUDIT-03bg — `check:terrain` proves the two sides agree about the *terrain*, and nothing at all proves they agree about the *encounter*.** 🟡 **doc sweep is 🟢; whether to widen the gate is the design call.** §WALK-5 §4.3 logged exactly one SP/MP divergence — the server draws monsters on flat tier weights because a MUD session tracks no notoriety — and `` `src/js/wbapi-server.js:const BASE_TIER_WEIGHTS@1158` `` still carries that note by name. **Three more have accrued since, and none is written down anywhere.** The client's roll at `` `function _enterEmptyCell(r, c)@28420` `` applies, in order: (1) `` `function _partyEncounterRate(base)@28667` `` — §MESH-01f halves the rate with a co-present ally and zeroes it under a sentry; (2) `` `if (S_story.huntMode) baseRate = Math.min(0.8, baseRate * 2);@28440` `` — §KG-01's Hunt Mode; (3) a draw that is *both* notoriety-weighted and §KG-01 level-biased inside `` `function _weightedMonsterPick(terrain)@38237` ``. The server applies **none** of them: `` `src/js/wbapi-server.js:s.encounter = (seededNext(s) < mres.encounter.baseRate)@8890` `` is the bare kernel rate. There is also a **fourth, ordering** difference that is deliberate and undocumented: the client suppresses a sentry-guarded encounter *before* the roll (stream not advanced) while the server voids it *after* (`sentryAt`, §MESH-01h), so the same seed diverges by one draw the moment a sentry is involved — the server comments the reason (trace replayability) but no doc names the consequence. **Why the existing gate cannot see any of this:** `src/scripts/check-terrain-parity.js` asserts the *inputs* — rate-table parse round-trip, `SEA_LANES`/`ROAD_CELLS` parse, and `terrainAt` ≡ `_inferTerrain` on all **10,440** band cells (green, 0 diffs). The **applied** rate is downstream of every one of those assertions, which is precisely where three later tracks widened the gap. **Fix (🟢 half):** record all four in `docs/mechanics/mechanics-combat.md` and the §WALK-5 lab report's divergence note as a single SP/MP table, since the server already points at that report by name. **Design call (🟡 half):** whether §MESH-01f's ally-halving and §KG-01's Hunt Mode *should* reach a MUD session at all (both are single-player progression concepts), and if so whether `check:terrain` grows a phase that pins the composed rate rather than its ingredients. **Bundled minor:** the comment at `play.html:28441` cites `wbapi-server.js:8784` for the server's matching roll; it is at **8890**. Its two neighbours at lines 6428 and 6431 (`:1147`, `:1155`) are still exact, so this is one drifted pointer, not a rotted convention — and `check:anchors` scans `*.md` only, so nothing gates a cross-file pointer written inside the engine. **Related, not duplicate:** §DX-02g is off-contract `tier` values; this is a rate composed differently on two sides of the same seeded stream. **Verify:** `npm run check:terrain` still green; `npm run check:rng` still green (the *stream* is byte-identical — 6,000 draws over 12 seeds — it is only what gets compared to it that differs).

### §AUDIT-03bb — one GEO anchor lies outside the map's own projection bounds, and both code paths that meet it fail silently (NEW 2026-08-14 during §DOC-02bj, 🟡 small design call)

- [ ] **§AUDIT-03bb — `PDL` (Ponta Delgada, Azores) is anchored at lon −25.7 against a declared western bound of −25.** 🟡 It is the **only** such entry of **155** GEO anchors, at the 2026-06-16 reference build and unchanged at HEAD. The bounds are `minLon: -25` / `maxLon: 72` on lines 226–227 of `src/tools/worldmap.js`; the anchor is on line 217 of the same file and duplicated in `src/js/wbapi-server.js` (two sites). **Two consumers meet it and neither reports anything.** (a) The region-membership test on line 263 of `src/tools/worldmap.js` is a bare comparison with no clamp (`g.lon >= b.minLon`), so **`PDL` matches no region cell at all** — it is absent from the region grid rather than misplaced in it. (b) The reweave's geo-seed phase *does* clamp: it computes column **4** and pins it to the grid minimum of **8**, so the node is placed, on the west wall, four columns from where its coordinates say. **Nothing is thrown, logged or counted** — the phase's own summary line reports it as placed like any other city. **Found because the source lab report promised the opposite:** `lab-report-world-structure-critique.md` §V.D states the seeding "will place them at the correct positions," and `PDL` is one of the four Atlantic island anchors that section is about. **Three options, and the first is not free:** (a) widen `minLon` to −27 — correct, but it re-projects **every** anchor's column and needs a full reweave plus a re-check of the 3 known cell contentions; (b) move `PDL`'s anchor to lon −25.0, accepting ~60 km of error at the map's least-trafficked edge — one character, no reweave; (c) leave the coordinate and make the two paths honest — clamp in the membership test and emit a warning when geo-seed clamps, so the next out-of-bounds anchor announces itself. **Recommend (c) then (b)** — the silent-failure half is the actual defect and is worth fixing regardless of where `PDL` ends up. **Risk:** (a) is world-wide and wants its own increment; (b)/(c) are local. **Verify:** every GEO anchor satisfies the declared bounds, or geo-seed logs each clamp; `PDL` appears in exactly one region cell.

### §AUDIT-03bc — the GEO tables' `label` column is an Earth city name, the node's `label` is the game place name, and nothing says which you are reading (NEW 2026-08-14 during §DOC-02bj, 🟢 no design call)

- [ ] **§AUDIT-03bc — 150 of 155 GEO anchors carry a label that is not the node's label, and the divergence has already produced published false findings.** 🟢 **prose only, two comment headers.** The anchor tables in `src/tools/worldmap.js` and `src/js/wbapi-server.js` are keyed by node code and carry `label:` — but that label is the **real-world city** (`TRD` → *Trondheim*), while the node's own label is the **game place** (`` `TRD:{ num:26, code:'TRD', name:'goblin_cave',     label:'Goblin Warrens'@8686` ``). They diverge in **148 of 152** entries at the 2026-06-16 build and **150 of 155** at HEAD. **The five that agree at HEAD — `STN`, `AMS`, `CDG`, `NUE`, `ACE` — are exactly the codes the source report used as its worked examples**, so the author generalised from the 3 % of the table that reads like a node registry and then reported `Trondheim`, `Molde`, `Kristiansund`, `Mosjøen`, `Santander` and `Madrid` as anchors with no node behind them. All six are nodes; two are Epic Battleground bosses (`` `SDR:{ num:54, code:'SDR', name:'epic_swamp',           label:'Sunken Altar'@8745` ``, `` `MAD:{ num:69, code:'MAD', name:'epic_heavenly_clouds', label:"Shattered Seraph's Spire"@8775` ``). **The second half of the fix is the stronger one, because it closes the question rather than clarifying it:** GEO membership is **total** — every anchor is a `NODE_MAP` node, **152/152** then and **155/155** now, zero orphans — and the seeding phase already skips-and-reports any anchor with no node, so *"anchored but not a node"* is an empty set and cannot be entered. **Fix:** one comment header on each of the two tables saying (a) `label` is the Earth gazetteer name for author orientation and is **not** `NODE_MAP.label`, and (b) every code here is a live node; if you want the game's name for a place, read `NODE_MAP`. **Also worth recording in the same header:** two anchors are one city twice (`NID` *Nidaros* and `TRD` *Trondheim*, 27 km apart) and two share a label outright (`JAR` and `JRS`, both *Jerusalem*, 11 km apart) — duplicate labels are the cheapest contention to catch before placement. **Related, not duplicate:** §AUDIT-03ba is the same root cause on a different surface — an author reads the most legible nearby text and it is the one surface no gate checks. **Risk:** none (comments only). **Verify:** both headers present; a fresh reader can answer "is `MAD` a node?" from the table alone.

### §DX-02bo — the geo-seeding tool still projects into the coordinate space §WALK-1.5 retired, and no write path checks the bounds (NEW 2026-08-13 during §DOC-02az, 🟠)

- [ ] **§DX-02bo — `./api.sh worldmap --seed` would write every node outside the walkable band.** `src/tools/worldmap.js:const GRID_MIN = 8, GRID_MAX = 500@1124` drives `src/tools/worldmap.js:function geoToGrid(lat, lon)@1126`, which linearly maps the 155-row `GEO` gazetteer onto rows/cols **8–500**. The live world is **90 × 360** (`const GEO_PROJ = { ROWS: 90, COLS: 360 }@9902`, §WALK-1.5/§WALK-2), and every one of the 416 live nodes sits in rows **2–73**, cols **154–249**. A seeded row above 89 is outside the mover's band clamp (`nr < 0 || nr >= ROWS`): the node is unreachable, and a player standing on it could not leave. The verb is published under *"§19 MAP VISUALIZATION"* with a `--dry-run` sibling, so the dry run is where the real numbers are — but the wet run is one flag away.
> **The second half is the one that makes it a 🟠 rather than a 🟡: `PUT /api/coords/{code}` has NO bounds validation of any kind.** The handler tests `typeof r === 'number'` and then tests for a collision, and that is all. There is no `r < ROWS` check anywhere on the write path — not in `PUT /api/coords`, not in `POST /api/coords/{code}/nudge`, not in `POST /api/node`. So the tool's arithmetic is the only thing standing between the gazetteer and an off-band world, and the tool's arithmetic is the retired one.
> ***The instrument this row is filed under: a claim can stay true while becoming wrong.*** `lab-report-movement-by-cells.md` §4.3 described this projection correctly in June and describes it correctly today — the code never drifted. The **world** drifted out from under it. That is a failure mode a symbol census cannot see, because every symbol resolves.
> **Fix (either, cheaply):** (a) retarget `GRID_MIN/GRID_MAX` to `GEO_PROJ` and re-derive from the same equirectangular formula §WALK-1.5 used, so `--seed` and the live projection agree by construction; or (b) if geo-seeding is retired in practice, 410 the verb the way `reweave-all` was retired — it is a repair tool that outlived its world, the §DX-02bf/bi/bk/bl/bn shape. **Either way, add the band guard to `PUT /api/coords` + `nudge` + `POST /api/node`** — that is invariant **I7** in the rewritten report, and it is currently satisfied only by luck.

### §DX-02by — `MoveResult.via` is the deleted ferry mechanism's last surviving trace: written on every step, read nowhere (NEW 2026-08-14 during §DOC-02bk, 🟢 no design call)

- [ ] **§DX-02by — a write-only field on the hottest path in the engine, and it can only ever hold one of two values that `ok` already tells you.** 🟢 **two lines, but inside a byte-parity fence.** The §WALK-2 kernel returns `via: null` when blocked and `via: 'step'` when it moves — `` `src/js/mover.js:function moverMove(world, pos, dir)@44` `` and its inlined twin at `` `function moverMove(world, pos, dir)@9937` ``. The field exists because the §WALK spec's `MoveResult` defined `via: null | 'step' | 'ferry'` to distinguish a walked step from a **ferry-edge** crossing. `95c4143` (2026-06-26, §WALK-5-FU) **deleted the ferry branch** — *"dead capability: `getMoverWorld()` never set it and there was no `FERRY_EDGES` data anywhere"* — because §WALK-1.5 had already shipped water crossings as `SEA_LANES`, walkable cells the kernel crosses like any other. **`'ferry'` has been unreachable ever since, so `via` is now a strictly redundant restatement of `ok`.** Census: **2 writers, 0 readers** across `play.html`, `src/js/mover.js`, `src/js/wbapi-server.js` and `src/tests/mud-harness.mjs`. Same class as §DX-02n's `S_story.ebReturnsCompleted` (write-only shadow), with one difference that makes it cheaper *and* more annoying: it is never persisted, so nothing round-trips it — but it lives between the `MOVER:CORE` sentinels, so **removing it means editing `src/js/mover.js`, re-inlining, and re-running `node src/scripts/check-mover-parity.js`** (currently 1,847 bytes identical). **Fix:** drop `via` from both return shapes in `src/js/mover.js`, re-inline, re-run `check:parity` + `check:behaviour`. **Or keep it and say why** — a one-line comment that the field is reserved is a legitimate outcome; what is not legitimate is a shipped enum whose second value the engine cannot produce. ***A vestigial field is the fossil of a deleted design, and it is the only place the design is still documented as live.***

---

## Track records (Phase 4)

### §NAV-01-FU — Navigable World follow-ups (parent ✅ CLOSED 2026-07-03 → plan-archive.md + `docs/lab-reports/lab-report-nav01-navigable-world.md`)

> *All five follow-ups ✅ CLOSED 2026-07-07 — row moved verbatim → plan-archive.md §"Archived 2026-07-28 — §DX-01f". One piece stays deferred by design: map "jump to region" centering (pannable-view design call; see Open at a Glance).*

---

## §RESUME — Phase 4 history (newest first)

> **20 entries**, carried verbatim from the single `BACKLOG.md` §RESUME log and filtered to this phase's subsystem. Order within this file is newest-first, as in the original. **The full cross-phase chronology — every entry in original order, with the file that now holds it — is the §RESUME index in [BACKLOG.md](BACKLOG.md).** Read that first when you need "what happened last", regardless of subsystem.

> **⟶ PRIOR (2026-08-22) — §DOC-02cw ✅ SHIPPED: the content report whose every number survived 45 days, whose road was laid by the method it forbids, whose sea-cell violation hid behind a red that was already red, and whose five NPCs could not be talked to for twenty days.** `docs/lab-reports/lab-report-kg-russia-kindergarten-zones.md`, **151 → 437 lines**, the first **content/zone** report the program has scored rather than a design lock. Shipped `65d65c0` 2026-07-08 18:20:38 — the same commit that added the report; pinned parent `65d65c0^` = **`89745ee`** (36,758 lines). Predecessor §KG-01 `8168f0e` landed **78 minutes earlier**; Increment 3 `d6aeefd` three hours later. Ordering re-derived from `stat`: `2026-07-29 11:47:51`, a block of one.
>
> **THE DATA HALF IS NEAR-FLAWLESS AND THAT IS THE FINDING'S SETUP.** Every corpus delta reproduces exactly on the parser (`src/js/wbapi-core.js`, instrument 161): nodes **413 → 418 (+5)**, monsters **392 → 398 (+6)**, terrains **106 → 111 (+5)**, worldbuilder options **10 = 5×2**, `ROAD_CELLS` **400 → 411 (+11)** — the commit's own *"+11 cells"* to the digit. All six `_monsterLevel` scores reproduce **to two decimals** (14.50 · 21.55 · 24.75 · 27.15 · 26.85 · 36.10), all five terrain pools match **order-exact**, Techno-Thug is **exactly** the mLevel 4 the report hedged as *"~4"*, L6 is **exactly** the 5,500 §1 claims, and node prose (635–739 chars) is **below** both exemplars it promised to stay under (HKG 953 · SVO 1,192). **Anchors 7 of 8 exact at offset zero**; the one miss is a range CLOSE (`L5252–6160` — MONSTER_POOL actually closes at **5681**, and 6160 is a banner between MONSTER_DROPS and WORLD_DB). `kg-zones.test.js` **5/5 green at HEAD**.
>
> ⚠ **INSTRUMENT 163 — A PRE-EXISTING RED MAKES THE EXIT CODE BLIND TO A NEW ONE.** The ship message claims *"check-invariants/roads/terrain/mover-parity all at HEAD baseline (0 new violations)."* Rebuilt ship-day trees (instrument 150; the tree is **flat** — the `src/scripts/`+`src/js/` reorg is later) put three of four gates honest — invariants identical (I1/I2 on J14/J15), terrain-parity green both sides, mover-parity byte-identical (1,847 bytes) — and the fourth **false**: `check:roads` R3 is **0 at the parent and 1 at the ship**, `✗ R3: road cell 10,207 is impassable sea`. That is the Gulf of Finland, on **the exact TLL→SPB leg §2 named as the sea-risk leg** in the same paragraph that promised land bridges. It hid because three pre-existing R2 failures (settlements 7,217 / 8,217 / 9,217, the Tungas spur) exit 1 on **both** builds — nothing in the status changed, only the violation **count**. ***Run the gate on BOTH builds and diff the violation LIST; the count is the signal, never the status.*** Instrument 147's family, new face: not a baseline that went stale — a baseline that **masked**.
>
> ⚠ **INSTRUMENT 164 — A DRIFT NOTE WRITTEN FROM A FAILING TEST MEASURES THE SAMPLE, NOT THE POPULATION.** §2's 2026-07-29 amendment says *"four cells this report's original road-lay implied (`10,205`, `10,209`, `11,210`, `14,216`) are no longer road."* Measured against the eleven the ship diff actually laid: **nine of eleven are gone** (`10,205 · 10,206 · 10,207 · 10,208 · 10,209 · 11,210 · 12,212 · 13,214 · 14,216`); exactly **two survive** (`11,212` · `12,214`). The four named are the four that made `kg-zones.test.js` red out of six sampled — the assertion list transcribed as a census. **And the same note names the R3 violation without naming its author**: it credits §DX-01a with rerouting *"off the Gulf-of-Finland sea cell 10,207 (a `check:roads` R3 violation)"* and never says the violation was shipped by the increment the note is written inside.
>
> ⚠ **INSTRUMENT 165 — WHEN A REPORT CARRIES A DATED AMENDMENT, DIFF WHAT ELSE LANDED THAT DAY.** §5's entire contribution is *"name + one signature line"* per node, and **not one of the five could be heard for 20 days, 16 hours, 45 minutes.** No §KG NODE_MAP entry carries a `code:` field, so `const _npcDial = NPC_DIALOGUE[node.code];@35818` and `!(node.npc && NPC_DIALOGUE[node.code])@35924` both read `NPC_DIALOGUE[undefined]`. **Proven in Chromium at BOTH builds**: at ship `65d65c0`, all five show `node.code` **undefined**, dialogue **not found**, **0** Talk buttons, d-pad NPC **disabled**, **0** *"Has dialogue"* hints — the card drew *🧙 Recruiter Volkov* with nothing under it; at HEAD all five are 1/1/enabled. The rescue is one normalisation line, `if (!NODE_MAP[k].code) NODE_MAP[k].code = k;@9415`, shipped by **`f08f70f` §AUDIT-03e at 2026-07-29 11:06:46** — an unrelated repo-health increment **whose own commit message names all five**: *"all five §KG NPC_DIALOGUE lookups failed — Volkov/Roshkova/Grimka/Iosif/Lena had never been talkable."* And **`b2fe418` amended §2 of this very file at 11:51:11 the same morning — 45 minutes later — without noticing that §5 had just been un-broken.**
>
> ⚠ **THE INVARIANT THIS REPORT CERTIFIED ABOUT ITS OWN METHOD IS THE ONE THAT FAILED.** §6, §7 step 4 and §8 all say the corridor was laid with `./api.sh highway --execute`. The ship diff contains **no highway call** — it is a single-line replacement of `const ROAD_RUNS = {@9883` — and the commit message says why: *"`./api.sh highway` lays sparse `junction:true` nodes … and ZERO road cells. Correct road-paving is a direct `ROAD_RUNS` edit."* Nodes did go through `post node`, so *API-first* is half-honoured; as written it is not. Four of five §8 invariants hold. **Also stale:** §2's *"monotone SE diagonal, 1-step-per-axis"* — the live router walks **TLL→SPB in 9 steps north via row 8**, FBR→TVR in **0** (adjacent), **15 steps end to end, every intermediate cell on road** — the *promise* is literally true and now property-asserted, the *geometry* is not.
>
> **Playability, and it is a small number.** The huntable surface of the entire second beginner ramp is **NINE CELLS** — SPB `10,209`/`11,210` · **KMS `12,211`, one** · ZVD `12,212`/`13,213` · FBR `13,214`/`14,215` · TVR `12,216`/`14,216`; all else in rows 8–16 / cols 203–220 is road (rate 0), settlement, or sea. The Komsomol School, whose whole fiction is sparring practice, offers **one cell** on which a cadet can be met. And none of the five terrains has an entry in `const TERRAIN_ENCOUNTER_RATE = {@9892`, so all five fall through `?? _default` to **0.15 — byte-identical to `midlands`** (0.30 hunting) while `city` is tuned to 0.05 and `forest` to 0.25. Level 6 costs 5,500; kill XP is 45 · 96 · 130 · 196 · 208 · 390 (mean 177.5) → **~31 average kills ≈ 200 steps** of oscillation. Hunt Mode itself is **exactly as specified** (`if (S_story.huntMode) baseRate = Math.min(0.8, baseRate * 2);@28440` + the 80/20 filter at `if (S_story.huntMode && _seededNext() < 0.8) {@38245`) and is the report's best idea: the band supplied the mLevel 1·2·2·3·3·4 rungs that made Hunt Mode's promise of *"a fair or easy fight"* satisfiable at all. **The L1→~L6 claim is TRUE here** (grinding is unbounded) **and false one increment later** — §AUDIT-03bl already scores the Inc-3 chain at 5,301, 199 short. *The zones let you reach L6; the quest chain does not pay you there.*
>
> **Two more player-visible misses, both filed.** All five §KG nodes stutter their own name in `#s-node-name` — `Nevsky Checkpoint · Nevsky Checkpoint` — because §4 gave each terrain the node's display name as its `label` (`soviet_checkpoint:{ label:@6294`), while every exemplar the report cites gets it right (`City Streets — Birka · City Streets`). Corpus sweep: **26 of 416** nodes stutter and **5 of the 26 were minted here** — 19% of the class in one increment. And `maps.md`, the declared home doc of `NODE_MAP`/`NODE_COORDS` and named in §7 step 6, holds **zero** occurrences of SPB/KMS/ZVD/FBR/TVR **45 days on**; `monsters.md`, `world.md` and `quest.md` all synced correctly and are still accurate. Filed **§DX-02ek** 🟡 + **§DX-02el** 🟢 + **§DX-02em** 🟢.
>
> **Gates:** `check:anchors` ✅ **4,423 → 4,450 across 106 → 107 docs, +27, 0 dead, 0 new stale** — 16 in the report (which joins the anchored set), 3 in the new rows, **8 in this §RESUME entry itself** (the count the +N line has been short by three times before) · `check:legacycodes` ✅ OK, 10 swept docs · `kg-zones.test.js` **5/5** · `check:roads` at HEAD green (R1 244/244 · R3 0 · R4 410). ⚠ **The working-tree `play.html` diff is the user's pre-existing CSS recolor (HP-bar gradient + playfield green) — NOT staged** (§DOC-02cm hazard, third session running).

> **⟶⟶ (2026-08-17) — §DOC-02by ✅ SHIPPED `7a4570e`: a design lock written 23 minutes before the code, exact in every number six weeks on, and wrong about one room's occupant.**
> On "continue", took `docs/lab-reports/lab-report-math01-completions.md` (`2026-07-07 08:58:42` — re-derived from `stat` first), the §MATH-01 completion design, closing the four-report §MESH-adjacent block. **The first pure DESIGN LOCK in the queue**: written at 08:58, shipped by `32d7bb0` at 09:22, so the document and the increment it authorises are 23 minutes apart. **130 → 299 lines / 1,207 → 3,524 words** — the original had no verification, inventory, delta table or playability section, and all the growth is those five sections.
>
> **VERDICT: shipped in full and intact at 42 days.** Four coordinates, five `completion` shapes, ten gold/XP values, five `onComplete` narrative lines and four `node.loot` strings **all byte-exact**. `mathArcComplete` still **0 occurrences** (D7 held); `∞-Fragment` still quest-less flavour. The relocation makes **6 of 6 directional hints literally true on the grid** — *"the panel is in the east wall of the Neon Undercity"* now describes a step the player can take.
>
> **NEW INSTRUMENT — READ A DESIGN LOCK'S LINE CITATIONS AGAINST THE PARENT BUILD, NOT THE SHIP COMMIT.** Its three `~L` marks (`storyCollectLoot` ~28628, the `storyRender` call ~29803, `storyCheckQuests(node)` ~34066) are wrong at its own ship commit by a **uniform +30** and **byte-exact at `32d7bb0^`**. A shared offset across several citations is a build boundary, not three independent errors — and the `~` was unearned modesty: the numbers were copied, not remembered. The same read validated its three test-file pins (W3B@8175, HOLDOUTS@8328, the census block) as exact for their day.
>
> **INSTRUMENT 93 SCORED D2's NEGATIVE CLAIM BY DIFF.** *"No mover, terrain, or IMPASSABLE change of any kind"* — `git show 32d7bb0 --unified=0 -- play.html` is **exactly three hunks**: one `NODE_COORDS` line, three more, and the `QUEST_DB` block. Zero hunks in `SEA_RUNS`, `ROAD_RUNS`, `IMPASSABLE_CELLS` or the mover. **INSTRUMENT 70: 5/5 green in 3.4 s**, including the design's own accepted edge (same-visit double completion at ZERO) and an `atNode`-holds negative.
>
> **THE ONE REAL FAULT IS A PREMISE, NOT A MECHANISM → §DX-02cv NEW (🟡).** D3 reasoned *"No `itemChain` — no recipient NPC exists ('Collect it' is the whole contract)."* `npc:"Johannes von Weisheit"@9383` was already on the EHZ node at the design-lock build, with four paragraphs of him at the console. Measured through the project's own parser: **71 of 416 nodes carry an `npc` chip and 6 have no `NPC_DIALOGUE[code]`**, so `!(node.npc && NPC_DIALOGUE[node.code])@35924` leaves their Talk button permanently disabled — `MOL` · `KSU` · `VBY` (Mordus) · `AMS` (Connie Tuna) · `EHZ` · `CNTR`, two of them this arc's and two more arc principals whose profiles are keyed by npc key rather than node code. ***A premise phrased as "no X exists" is a census: take it before you write the sentence.***
>
> **CORRECTED §DX-02as (e) — AND THIS REPORT IS THE THING THAT DISPROVES IT.** The row asserts the non-UQF residue is **50** and names 15 ids. All 15 carry `schema:"UQF-1.0"` at HEAD, `play.html` has not changed since the row was filed, and **two independent instruments return 30** — a comment- and string-aware brace walk over all **2,853** `QUEST_DB` entries, and `src/tests/integration/quest-runtime-uqf.test.js:expect(r.ids.length).toBe(30);@9382` executing in the real JS engine — which is exactly what this report's §3 predicted on 2026-07-07. The 15 false positives are single lines of **1,436–2,655 characters with `schema:` near the END**. ***A newer measurement is not automatically the better one; the corrective instrument needs its own witness.*** **Added (e-2):** the same stale sentence has a second site three lines above `q.schema === 'UQF-1.0' && q.completion && QuestRuntime.canComplete(id)@30187` — the code that completes those very quests.
>
> **EXTENDED §AUDIT-03x with the executed precedent.** §MATH-01 is the only repair of that class ever shipped: four `PUT /api/coords` calls, three diff hunks, hint prose repaired *as a side effect of picking the destination cell from the hint text*, and a regression test pinning one-code-per-cell. What it declined to fix is the row's cheapest remaining slice — `PKR`/`JAR`/`OLN`/`JER` are still non-primary behind `JRS:{r:38,c:215},@9824`, carrying **28 `activateNode` quests** (JAR 12 · PKR 10 · OLN 5 · JER 1).
>
> **TWO UNPLANNED GAINS, both in the player's favour.** §BOARD-01-FU6 (`f7350b0`, +14 d) added four `unlock` bits turning five independent collect quests into a guided 01→02→03→04→05 chain — D4's *"no sequencing"* holds to the letter (all five gates are still `{}`) and was superseded in spirit by a mechanism that did not exist when it was written. And `fa8f9e4` (+21 d) re-ran the road generator, laying `(29,249)` against the new pocket, so MONS and CNTR gained an encounter-free approach nobody designed.
>
> **GATES:** `check:anchors` **3,338 → 3,377 across 82 → 83 docs, +39 — 27 in the report, 7 in the rows (3 §DX-02cv · 2 §DX-02as · 2 §AUDIT-03x) and 5 in this §RESUME entry**, 0 dead, 117 stale = unchanged baseline (the entry's own five were counted *after* writing it, per §DOC-02bw's lesson — the first draft of this line said +34 and was short by exactly them, again). `check:legacycodes` exit 0. **`play.html` UNTOUCHED** — the user's uncommitted 2026-08-06 combat recolor is still exactly 9+/9−.
>

> **⟶ PRIOR (2026-08-17) — §DOC-02bt ✅ SHIPPED: every statistic in the diagnosis re-derives exactly, and the one sentence nobody filed as a row cost four red tests for six weeks.**
> On "continue", took `docs/lab-reports/lab-report-nav01-navigable-world.md` (`2026-07-03 12:47:32` — re-derived from `stat` first, exactly as §DOC-02bs predicted), **117 → 215 lines, 1,701 → 3,653 words** (measured with `wc` AFTER the last edit). Previously **zero** `symbol@line` anchors; now **17**. Existing-Work-First: tree carried only the user's uncommitted 2026-08-06 combat recolor (9+/9−, **not mine, left untouched**); single live session; `roads-pins.json` absent from the root, which turned out to be §CLEANUP-02 history, not a missing file.
>
> **VERDICT: SHIPPED IN FULL, a–h in order, and the strongest DIAGNOSIS result the program has recorded.** All eleven commit hashes resolve on the stated dates. Every one of the six §I statistics re-derives exactly from `git show 01c5187:play.html`: **410 nodes → 235 occupied cells**, **0.85% of 27,610 passable** band cells, nearest-neighbour **median 1**, and the headline **median 33 blind steps / p90 66 / max 102 (MLN at 73,220)** reproduced by an independent BFS from the LHR spawn — plus **2,829 quests carrying `activateNode` across 353 distinct values** through the `wbapi-core` parser. Both diagnosed bugs are verbatim in the archive: `_bfsGridDir(S_story.currentCode, wp)` at 32781 and 33062, and the 1-indexed `nr > 500 || nc > 500` clamp inside `_bfsGridPath`. The net figure is exact by the strongest possible witness — **the generator writes its own count into the file it patches**, and at both `7b503d1` and `0b341d1` that header reads *"400 road cells, 88 junctions."*
>
> **THE GATES DID NOT MOVE IN 46 DAYS.** `navigation.test.js` **35/35** and `worldbuilder-walk.test.js` **89/89** — the exact figures the report recorded at Inc e and Inc h ship. mud-harness **[M] 14/14**, every byte-equality assertion between the server room and the client `describeCell` still green, and **[N] 7/7**, exactly the "+7 hermetic" Inc g claimed. `check:roads` R1–R4, `check:terrain` A3 + B 10440/10440 diffs 0, `check:roomsparity` 10,836 bytes identical. **Guard-rails 5 of 5**, and two of them are proven rather than asserted: `src/js/mover.js` and `src/js/rooms.js` have had **one commit each since close and it was §CLEANUP-02's file move — zero content edits**; and #4 ("never hand-edit `ROAD_RUNS`") was *tested*, because the one net change since close (`fa8f9e4`, 400/88 → 410/89) went through `build-roads.js --apply`, the door the report mandated. ***A guard-rail is only proven by the change that wanted to break it.***
>
> **HEADLINE → §DX-02d, and it is a lesson about follow-up registers.** This report's open follow-up #5 says `worldbuilder-crud-arrays.test.js` *"still need[s] the live server to dismiss the welcome screen"* — **written 2026-07-03, twenty-six days before §DX-02d was filed and forty-five before §DOC-02br root-caused it as `#welcome-screen` intercepting the `renderEntity` click.** Measured at HEAD: of seven `worldbuilder-*.test.js` specs, **one** carries the hermetic `page.route` firewall (the spec Inc g wrote), two dismiss the overlay by hand, and crud-arrays has neither. ***A follow-up register is a set of predictions with expiry dates; this one was correct, specific, and unfiled, so nothing scheduled it and it aged into four carried reds.*** Register score: 3 of 5 closed within four days (#1 retired `b872b8c`, #3 done `51bc5c7` reusing `__roadDestination` exactly as proposed, #4 superseded by §MAP-NAV click-to-**travel** — auto-walk via `_navTravelTo`, never a warp, so invariant #3 is intact); #2 and #5 still open.
>
> **SECOND — a spliced sentence, and a new instrument.** §V describes the road generator as *"k-nearest (k≤3, BFS dist ≤ 30) + MST … trunk-reuse Dijkstra with costs settlement 2 / road 4 / virgin 10 / sea-lane 14."* The cost table is **byte-exact** with `const COST` in `src/scripts/build-roads.js`. The k-nearest clause has **no `k` and no `30` in that file at Inc b's own ship commit or anywhere in its history** — the shipped candidate step is an uncapped per-settlement BFS at `CAP_START = 50`, doubled until the MST connects. Both halves came from `plan.md`, which archives the pre-build **spec** (k≤3, ≤30, *"already-roaded cells cost 0.5"*) and the as-built **increment row** (MST, loops ≤8, 2/4/10/14) a few hundred lines apart. ***When a track archives its plan beside its own increment table, a later summary can splice the forecast into the record, and the seam is invisible because both sources are the same document — prefer the increment row; it was written after the code existed.*** Also **NOT SHIPPED** and wrong when written: *"3–5 prose variants per terrain"* — 14 buckets holding **2–4** (six twos, six threes, two fours), byte-identical at Inc c's own ship commit `3568fcc`.
>
> **PLAYABILITY (user directive) — the feature's whole value is the experience of distance.** The world was correct and unplayable: 27,610 walkable cells printing one sentence, an encounter roll on every step, and a waypoint arrow computed from the last named node instead of the player. §NAV-01 made the space between cities *readable* (a room per cell, deterministic so two clients read the same ground), *safer along known lines* (`road` terrain, encounter rate 0), *walkable in one gesture* (auto-travel at ~120 ms/step with four honest interrupts) and *signposted*. **The 33-step median journey went from five-to-eight forced battles and 33 clicks to one click and, along the road, none** — and the net that does it covers **1.5% of walkable ground while touching 244 of 244 settlements in one component**, which is exactly what a road is supposed to be. Roads are sugar, never rails: the open field stays fully walkable by construction.
>
> **FILED §DX-02ck** 🟢 (the road-net figure `400/88/1.4%/235` is stale in **five maintained docs** — `index.md`, `maps.md` ×2, `mechanics.md`, `docs/notes/docs-node-network.md` — twenty days after `fa8f9e4` made it 410/89/1.5%/244; **two docs already carry the correction**, so the fix exists and never propagated, and two of the stale lines also carry pre-§CLEANUP-02 paths) **and §DX-02cl** 🟢 (`_questNodes()` never invalidates while the sibling index thirteen lines below rebuilds on an entry-count change *and explains why* — this report's own follow-up #2, unactioned 46 days). **EXTENDED §DX-02d** with the provenance above. **BACKLOG grepped before all three; none existed.**
>
> **Corrections made in the report itself, no row:** the exits `kind` list omits `'lane'` (five kinds ship, and lanes are the ones §IV says stay `ocean`); `describeCell` also returns `terrain` and `landmarks`, and `body` never existed — the `body/prose` slash marks where copying stopped and recall began; *"routing origin **never** `NODE_COORDS[currentCode]`"* is overstated, since `_playerPos()` falls back to it when `playerR/playerC` are null; and the encounter floor is `city:0.05`, not 0.10, with a fifteenth key `junction:0` that is the one field terrain owning no prose bucket.
>
> **GATES:** `check:walk` **16/16 green** · `check:anchors` **3,183 → 3,201 across 77 → 78 docs, 0 dead** (**+18 = exactly the number written**: 17 in the report, 1 in §DX-02cl; 117 stale hints = unchanged baseline) · `navigation` 35/35 · `worldbuilder-walk` 89/89 · mud-harness **267 passed / 2 failed**, the 2 being the known `[D]` idle-TTL reds (§DX-02ca), unrelated and pre-existing. **`play.html` UNTOUCHED** — every measurement read-only; the user's uncommitted 2026-08-06 combat recolor is still exactly 9+/9−.
>
> **NEXT: re-derive from `stat`** — expected `docs/lab-reports/lab-report-uqf-migration-playbook.md` (`2026-07-05 14:42:46`), the §ARCH-01 UQF migration playbook. It is a **method** document rather than a feature close-out, so expect instrument 12 (copied vs reconstructed) to bite differently: its wave table and its migrator invariants are copyable, its *"zero engine changes"* claims are the negative-claim class instrument 86 covers, and §ARCH-01 is CLOSED with ~2,850 quests on UQF-1.0 — so the playbook's own rules are checkable against the corpus it produced. **Confirm with `stat` first.**

> **⟶ PRIOR (2026-08-14) — §DOC-02bn ✅ SHIPPED: the corpus's most accurate report, and the one sentence in it nobody measured is the origin of §AUDIT-03x.**
> On "continue", took `lab-report-walk15fu-grid-resolution.md` (`2026-06-26 15:58:19` — re-derived from `stat` first; §DOC-02bm named it and `stat` confirmed it), **55 → 260 lines, 855 → 2,681 words** (measured with `wc` after the last edit — the bi/bj/bl rule), rewritten IEEE-shaped with a 7-row decision register, a replayed census table and **15 `symbol@line` anchors** (the file previously had none). §WALK-1.5-FU(c) is a **decision memo, not an implementation spec**: the user had already directed *"adopt 0.25° for dense regions"*, and this report tested the direction against the data and **reversed it**. Existing-Work-First: tree carried only the user's uncommitted 2026-08-06 combat recolor in `play.html` — **not mine, left untouched and unstaged**; this increment is docs-only.
> **VERDICT: CORRECT, ADOPTED, AND STILL CORRECT — the highest-accuracy report the program has measured.** Every quantitative claim replays: **409 nodes / 215 cells / 66 shared / 260 piled**, the **17 fully / 21 partially / 28 unchanged** 0.25° separation table, `GEO2` = **155**, the **345 own-coords / 64 anchor-chained** split, `72 × 96 = 6,912` → `288 × 384 = 110,592`, and **14 of 15 numeric cells** in the pile table. Shipped **46 minutes later** as `ebf88c8`. And it is still true at HEAD: re-binning today's 66 shared cells at 0.25° leaves **28 completely unchanged** and separates only 18, so resolution remains the wrong lever.
> **A FIRST FOR THE PROGRAM — the reference tree is the WORKING tree, not a commit.** Replayed against `782e4d3`, four §2 figures were *all* wrong (211 cells / 67 shared / 265 piled). The report is not: item **(a)** had already been applied through `PUT /api/coords` and sat uncommitted until `ebf88c8` at 16:44. Re-apply it — HKG/BKK/CTU/SJO off Samarkand's `30,246` — and **all four resolve exactly**. The gazetteer half confirms it independently: moving those four from `offEarth` to `anchors` **with explicit lat/lon** turns a measured 341/68 split into the report's **345/64**. *A report written mid-increment cites a tree no commit contains; replay the increment's own uncommitted mutations before calling a figure wrong.*
> **HEADLINE — the one clause nobody measured cost two months. §6 dismissed the surviving clusters as *"already handled cost-free by the locale-list sub-location picker."* There is no picker and there never was:** `git log -S "sub-location picker" --all` returns **exactly one commit — `ebf88c8`, the commit that added this report** — and **`destCodes[1]` has never been read by anything, ever**. On the report's own tree the client already read `` `const destCode = res.destCodes[0]@28355` ``, as it still does; `` `const cellCodes  = (key) => CELL_GRID[key]@9862` `` had **one** consumer that day (the mover kernel, computing a list nobody unpacks), and its first real reader — the MUD room describer, §NAV-01c `3568fcc` — shipped **five days later**, on the server. **NOT SHIPPED, not stale.** Eleven days on, §MATH-01 (`32d7bb0`) hit the consequence and named it in its own commit message: `EHZ`/`ZERO`/`MONS`/`CNTR` *"were unreachable sub-locations of `JRS`'s cell, so `math_02`/`_05` could never activate"*. **§AUDIT-03x EXTENDED with its provenance, not re-filed** — and re-confirmed independently for the fourth time at **416 nodes / 244 cells / 172 non-primary**.
> **INSTRUMENT 72 — instrument 9's boundary can run INSIDE a single table row.** All five *numeric* columns of the pile table are exact (21/2/21 · 13/3/12 · 13/2/12 · 12/2/11), while the hand-written *cause* column beside them miscounts the anchor in **two of five** rows ("9 coordless satellites → `FLR`" is 9 satellites, 6 to `FLR` and 3 to `PRA`; "6 offEarth → `SDQ`" is 6, of which 5 reach `SDQ`). The one wrong number contradicts its own row: Atlantis is 17 nodes in **1** bin, so still-piled is **17**, not 16. *The copied/reconstructed split is not a section boundary — it can be a column boundary.*
> **THE OTHER DIRECTION (instrument 6): the cost estimate has aged in the report's favour.** §3 listed what a 0.25° world would touch — geo-seed, `CELL_GRID`, `SEA_RUNS`, the 59 `SEA_LANES`. Five days later §NAV-01b laid `` `const ROAD_CELLS = (() => {@9884` `` — **410 road cells and 89 junctions across all 244 settlement cells** — which would need a full re-lay too. Every week Option A holds, Option B gets dearer.
> **PLAYABILITY (user directive):** the grid is the unit of walking, so its resolution sets three things a player feels — how long the world takes to cross (one cell N/E/S/W, timeless), how dangerous the road is (**~4× the steps is ~4× the encounter rolls** against `` `midlands:0.15@9893` ``), and whether two real places feel like two places. The report's contribution is separating that third want from the first two costs — and then showing the want is mostly not about resolution at all. **A finer grid cannot separate identical coordinates.**
> **GATES:** `check:anchors` **3,082 anchors / 72 docs, 0 dead** (117 stale hints = the pre-existing baseline, **unchanged** — so all 15 new anchors are byte-exact; the report was the 72nd doc, having had none) · `check:legacycodes` exit 0, 10 swept docs clean · `check:terrain` green (10,440/10,440, `ROAD_CELLS` 410, `SEA_LANES` 59). ⚠ **`check:walk` chained still detaches past 400 s** — gates run individually. `play.html` **untouched**.
> **NEXT: `lab-report-wbapi01-ph3-array-patch.md` (`2026-06-26 17:48:18`)** — §WBAPI-01 phase 3, the array-field patcher, the same day's next report. **Confirm with `stat` first.**

> **⟶ PRIOR (2026-08-14) — §DOC-02bl ✅ SHIPPED `c946a01`: the removal shipped completely, and was undone twelve days later by a track that reused every one of its names.**
> On "continue", took `lab-report-timeless-movement-hunt-removal.md` (`2026-06-26 09:11:57` — re-derived from `stat` first, as the rule requires; §DOC-02bk named it and `stat` confirmed it), **140 → 204 lines, 1,459 → 1,885 words**, rewritten IEEE-shaped with a 12-row delta table. **The first report in the program that GREW** — it was already lean, and everything added is verified content the original had no section for (abstract, method, delta table, risk outcomes, three defect writeups) plus the playability rationale the user directive asks for. ⚠ **I wrote "1,876" in the commit message from a pre-edit measurement and the file is 1,885** — the bi/bj lesson caught me on the same increment I cited it in: **re-run `wc` AFTER the last edit, not before.**
> **VERDICT: SHIPPED — 4 of 4 increments plus an unplanned follow-up, all on the spec date** (`d4fae39` spec · `7d1418c` A · `7952752` B · `5a54657` C · `1d6263a` D · `790d4f3` FU, which swept Hunt/Stalk residue from 7 deeper spec docs and was never in the plan). **Pointer accuracy 13/13 byte-exact** replayed against its own stated reference commit `017d7d8` — instrument 8 used as corroboration rather than correction for once.
> **HEADLINE — 4 of 6 acceptance criteria hold, and both failures are the same event.** `8168f0e` (§KG-01, 2026-07-08) reintroduced a **different** mechanic — `baseRate × 2` capped 0.8 plus an 80/20 bias toward monsters ≤ player level, **no clock cost**, wilderness-only (`_weightedMonsterPick`'s single caller is `_enterEmptyCell`) — under the **identical** identifiers `huntMode` / `storyToggleHunt` / `_updateHuntBtn` / a 🎯 d-pad button (`btn-hunt`, was `btn-hunt-toggle`). **The engine comment on line 38113 announces the deletion of two functions defined 39 and 28 lines above it.** Only the **Stalk** half of the removal is real: 6 identifiers at 0 occurrences, 3 more surviving only inside tombstones. Ten `§TIMELESS-01` tombstones remain; **nine are true**. Movement itself is intact — `cellMove` has zero clock writers and exactly **four** `hoursElapsed` writers survive, all on the D1 keep-list.
> **This confirms a lesson the program already held rather than discovering one** — §DOC-02c recorded the §TIMELESS-01/§KG-01 vocabulary collision as a *rule* in 2026-08-11 but never filed the doc defects it causes; this pass measured them. **Instrument 7 (a report is checked against its siblings) paying out in the other direction: the sibling had already named the hazard, and the row was still open.**
> **FILED §AUDIT-03be** 🟢 (six sites certify the undone removal — the 38113 tombstone, `mechanics-combat.md:690`'s "Removed code/state" list, `docs-node-network.md:306`, `index.md:409` and `:523` — plus a **separate** defect in the same paragraph: `mechanics-combat.md:688` says *"movement and battle no longer advance the clock"* and **battle always did**, by §TIMELESS-01's own D1, at `_storyRollInit` line 24650), **§DX-02bz** 🟡 (the fishing card advertises `⏱ 1 hour` and has **never** had a writer — 6 `hoursElapsed` writers at `017d7d8`, exactly the six in the report's own §2.1 table, none fishing; 4 now, still none — plus sleep advancing `hoursElapsed` by 8 and the wall clock `hour` by 6, which is what decides night fishing) and **§AUDIT-03bf** 🟢 (`index.md:523`'s *"All 193 `S_story` fields from `_S_DEFAULTS()`"* was written by **this report's own Inc D**, `1d6263a`, when a brace-depth walk returns **486** and its own table held **205** rows — a **never-correct** figure, not drift; **493** and **208** at HEAD). **BACKLOG grepped before all three** — instrument 7's filing discipline, sixth increment running; none existed.
> **THE LESSON THE REPORT DEMONSTRATES RATHER THAN STATES** — instrument 9 and instrument 55's proximity corollary in one document: its inventory tables are **flawless** (13/13 pointers, 6/6 clock sites), and its single substantive error is **one adjective** in §2.3 calling fishing's hour a cost that "remains real" — applied to a line it transcribed *correctly*, **four lines after tabulating the very evidence that disproves it**. *A negative costs one grep, and a positive characterisation laid over a correct citation costs the same one.*
> **PLAYABILITY (user directive):** the doom clock charged an hour per cell — its largest expense on its least interesting action, and a tax on invariant #1 (free movement). Making movement free relocates the whole cost onto **decisions** the player chose, so curiosity stops competing with survival across a 416-node world. §KG-01's Hunt Mode is the second half of that trade and is consistent with it: deliberate grinding that costs no clock.
> **GATES:** `check:anchors` **3,043 anchors across 70 docs, 0 dead** (the 117 stale hints are the pre-existing baseline) · `check:legacycodes` OK, 10 swept docs. Docs-only increment: **`play.html` untouched**, and the user's uncommitted 2026-08-06 combat recolor (9+/9−) **staged around by name** — never `git add -A`.
> **NEXT: `lab-report-walk5-mud-harness.md` (`2026-06-26 13:31:07`)** — §WALK-5, the MUD multi-client harness; `index.md:179` already records it as **fully closed with all 4 increments + FU**, and it is the sibling this report's §1.2 leans on for "movement was already one-cell". **Confirm with `stat` first.**
>

> **⟶ PRIOR (2026-08-14) — §DOC-02bk ✅ SHIPPED: the program's first FULLY-SHIPPED verdict, its highest symbol score (43/43 node codes), and the one exception it asked for was taken and then given back.**
> `lab-report-terrain-field-mover-redesign.md` (`2026-06-25 18:52:53` — re-derived from `stat` first, as the rule requires), **521 → 222 lines, 4,513 → 2,429 words (−46 %)**, rewritten IEEE-shaped. The §WALK-1…§WALK-5 navigation-core spec lock: delete the junction stubs, re-project the world onto a real 1° lat/lon grid, extract one pure mover kernel for both the solo client and the MUD server, recast reweave as a read-only reachability flood, gate it all in CI, and drive it with a multi-client harness. **All five steps shipped.**
> **THE INTERVAL IS THE FINDING.** §WALK-1 committed `efa8f7a` **2026-06-25 17:43** — *an hour before this file's own mtime* — and the series closed `95c4143` **2026-06-26 13:33**: **under 20 hours, 16 commits, spec to full closure.** That is why it scores as it does. This is a document written *beside* the implementation rather than from memory of it, so instrument 9's split resolves in its favour everywhere: its inventories read like inventories. **43 of 43 node codes resolve (100 %)**, against instrument 31's record that most reports of this age score 0 of N.
> **HEADLINE — the only sanctioned exception to "no stored edges" was taken, then handed back.** §2.3 specified `FERRY_EDGES` — bidirectional land↔land crossings over sea — as *"the only sanctioned re-introduction of stored edges."* It shipped as a kernel branch, and one day later `95c4143` deleted it, with the plainest commit message in the repo: *"dead capability: `getMoverWorld()` never set it and there was no `FERRY_EDGES` data anywhere."* §WALK-1.5 had already solved it by carving **59 `SEA_LANES` cells** into walkable channels, so a crossing is **terrain, not a permission**. **The shipped navigation core contains zero stored edges** — a strictly stronger free-movement guarantee (CONTRIBUTING #1) than the spec asked for. Instrument 4 says RETIRED, not never-shipped: 11 commits for `FERRY_EDGES`, 8 for `ferryEdges`.
> **SECOND — the report audits its own predecessor and WINS, and only the archive can prove it.** §1 opens by contradicting `lab-report-cell-map-mud-redesign.md`'s recorded *"§CELL-05 complete, 0 junctions."* At `efa8f7a^` the file held **316 `junction:true`** — byte-exact, verified ten weeks later at a tree neither author could see. *A design document that audits its predecessor is rarer than one that cites it, and worth about ten times as much.* (§DOC-02ay had already verified that sibling and could not have caught this: junctions are 0 at HEAD either way.)
> **THIRD, and the new instrument — the one figure copied out of a DOCUMENT instead of the file is the only figure that is wrong.** §2.4 says *"the existing **40** in `WORLD_DB`"*. That day it held **107**; `index.md` said **66**; `npm run stats` says **111** today. The 40 traces to `docs/spec/spec-migration.md:46`, where it **correctly** describes the Layer-0 combat tracker of 2026-05 — a world three engine-generations back, and still correct in context, so **no doc fix is owed** (annotate-don't-rewrite; the false-finding was avoided by reading the source doc's own framing). ***A number copied out of a history document arrives with its date stripped off.*** Every figure this report measured itself is exact; the one it borrowed is 2.7× stale.
> **FOURTH — §6.2 shipped stronger than specified.** It asked for *"a fixed seed + scripted dir sequence"*; `check:behaviour` sweeps **41,760 (cell × dir) decisions, 0 content mismatches**, 245 deliberate edge/wrap-fringe differences. The regression lock it wanted as a trace is a proof.
> **EXACT AT HEAD, TEN WEEKS ON:** `` `const GEO_PROJ = { ROWS: 90, COLS: 360 }@9902` `` · GEO2 **155** (report said "~155") · `` `LHR:{r:10,c:197}@9472` `` · **4/4 collision predictions** (`LDN/LON/BRK → 18,179` · `HHL/ISL → 5,158` · `PIS/PSA → 26,190`) · `NODE_COORDS` 409 → **416** with **0 missing**, the invariant surviving seven new nodes · I3 **416/416, 1 component** · `junction:true` **316 → 0** · MOVER:CORE **1,847 bytes** byte-identical.
> **AS-BUILT NUMBER THE SPEC COULD NOT HAVE KNOWN:** the §2.2 locale mechanism carries **238 of 416 nodes (57 %) in 66 shared cells** out of 244 occupied; the largest is `32,203` with **17** — Athens beside sixteen Atlantis-layer interiors. What the report filed as a *risk* is now more than half the world's addressing.
> **ALL SEVEN RISK-REGISTER CALLS CORRECT** — the rarest result in the corpus; the risk register is usually where these documents fail. And the engine quotes one back at the source: `` `src/js/wbapi-server.js:the shared kernel (mover.js) decides bounds/wrap/sea@8868` `` cites *"the latent server bug fixed for free, lab report §8."*
> **DELTAS KEPT, NOT DELETED:** `terrain 'sea'` **NOT SHIPPED** (a terrain key invented for a cell you can never stand on; lanes render as the pre-existing `ocean`) · the dead reweave body **undercounted 2.7×** (~1,200 lines claimed at `5891–7075+`, **3,240** actually deleted, 12,510 → 9,270 in `d602b7f` — the P1/P4/P5 phase *names* were right) · `fix-all-broken`/`fix-bidirectional` **RETIRED, not never-shipped**, and this nearly shipped as a false finding: a path-scoped `git log -S … -- src/js/wbapi-server.js` reads **0** for both, the unscoped search reads **31** and **22**. *Instrument 4 is only as good as its pathspec.*
> **PLAYABILITY, RESTATED (user directive):** 316 *"Signpost says…"* screens → **0**; a world where Birka is north of Rome because Birka *is* north of Rome, so distance is reasoned from a map the player already carries; and one kernel serving both games, which closed a latent MUD sea-walk bug for free. The deepest point is that free movement stopped being a promise and became a *mechanism*: **if the mover has no place to consult state, it cannot be taught to refuse.**
> **FILED:** **§DX-02by** 🟢 (`MoveResult.via` — 2 writers, 0 readers; `'ferry'` unreachable since `95c4143`, so it restates `ok`; the deleted design's last live trace, and it sits inside the byte-parity fence, so removing it is a re-inline) · **§AUDIT-03bd** 🟢 (`check:invariants` counts **110** of **111** terrains — its key regex takes bare identifiers only and cannot see `'bar (Visby)'`; the NODE_MAP scan in the *same file* carries a stale-regex guard this side lacks, so gate #1 is one authored node from a false red). **§DX-02bm re-confirmed, not re-filed** (`TERRAIN_ENCOUNTER_RATE.junction:0`, already open from §DOC-02ay) — the BACKLOG grep before filing did its job for the fifth increment running.
> **GATES:** anchors **3,023 across 69 docs, 0 dead** (3,020 after the report, +3 from the two new rows) · legacycodes · invariants · dupkeys · parity · behaviour all **exit 0 individually**. `check:walk` chained detaches past 400 s — the known artifact; kill and run the gates individually. `play.html` **untouched**; the user's uncommitted 2026-08-06 combat recolor (9+/9−) staged around by name.
> **HOUSEKEEPING found on the way in:** the §RESUME log carried **three** `⟶⟶ NEWEST (2026-08-14, latest)` headers — bi and bg were never demoted when bj and bi superseded them. Both demoted to `⟶ PRIOR` here; the log now has exactly one NEWEST, as the reverse-chronological contract requires. Same family as the six recorded missing-§RESUME-entry instances: **the close order's last steps are the ones that get dropped**, and this one degrades silently because every stale header still *reads* correct on its own line.
> **NEXT:** re-derive from `stat`. Expected next-oldest is **`lab-report-timeless-movement-hunt-removal.md`** (`2026-06-26 09:11:57`) — §TIMELESS-01, the sibling this increment cites three times (it removed the clock from a step and deleted the corridor Hunt). **Confirm with `stat` before starting.**

> **⟶ PRIOR (2026-08-14) — §DOC-02bj ✅ SHIPPED: its geography is byte-perfect and its arithmetic reverses its own thesis. THE 2026-06-16 BLOCK IS NOW EMPTY.**
> `lab-report-world-structure-critique.md` (`2026-06-16 15:22:50` — the tenth and last file of the block), **172 → 372 lines, 2,733 → 4,056 words (+48 %)**, rewritten IEEE-shaped, `3cafd00`. Re-derived from `stat` first, as the rule requires. **This is the program's largest growth, and the second increment after §DOC-02be to grow its report at all** — the original was already terse and carried no delta table, no method section and no evidence, so the repetition available to cut was far smaller than the measurement needed to correct fifteen claims. One redundant appendix was folded into the delta table on a tightening pass. *The first draft of this entry said "→ 261 lines / 3,286 words" from a drafting estimate; corrected from `wc`. That is §DOC-02bi's lesson recurring on the very next increment — **measure the rewrite, never estimate it**.* Reference build `89fa13b` (12:20:47) — **33,721 lines, the status-line figure exact**; mtime 15:22:50, birth commit `120d617` at 16:31:20, a **68-minute** window — **and that commit executed both of the report's recommendations before anyone could read them.**
> **THE HEADLINE — the report names the wrong structure in its status line, its abstract, and every structural count it takes.** *"154 WORLD_DB nodes"*: `WORLD_DB` is the **terrain** registry, addressed as `WORLD_DB[node.name]` where `name` is the terrain key (`` `const WORLD_DB@6279` ``), and it held **106** terrains at this build. The node table is `NODE_MAP` at **409** (**416** at HEAD). **No structure in the file has ever held 154 entries**, and the trio 80/74/154 in §II is internally consistent and externally unrelated to the game.
> **SECOND — the "big middle" inverts under measurement.** Census taken with a brace-depth walk that skips strings and comments, because a `^CODE:\s*\{` regex returns **394** for the same object (instrument 51, and it bit this pass first). Acts run **27 · 131 · 19 · 76 · 53 · 14 · 44 · 8 · 1**, plus **36 `act:NaN`** that the report's table has no row for. Against *"Acts 3 and 4 hold 90 of 154 — 58 %"*: real **129 of 409 — 32 %**, and **Act 1 alone holds 131**, more than Acts 3 and 4 combined — **167, or 41 %, at runtime**, once §AUDIT-03t's `node.act || 1` folds the NaN nodes into Act 1. The §III table sums to **168** against its own stated 154. **4 of 9 act rows are exact** (0 · 4 · 7 · 8) and the **20 Epic Battleground boss nodes are exact**. The density inversion loses its evidence in the same pass — Act 6 has **44** nodes, not 11, so the claim rests on 44-against-53 rather than 11-against-53. *The design arguments survive; only the arithmetic under them is gone.*
> **THIRD — 0 of 6 unverified negatives, and the mechanism is a single table column → §AUDIT-03bc NEW 🟢.** §V.A's *"TRD/MOL/KSU/MJF … contain no named WORLD_DB nodes"* and §V.B's *"the Iberian Peninsula is mapped but not occupied"* — **all six are live nodes**, two of them Epic Battleground bosses (`` `SDR:{ num:54, code:'SDR', name:'epic_swamp',           label:'Sunken Altar'@8745` ``, `` `MAD:{ num:69, code:'MAD', name:'epic_heavenly_clouds', label:"Shattered Seraph's Spire"@8775` ``), and `TRD` is the Void Shaman's warrens. The claim is **structurally impossible**: every GEO code is a `NODE_MAP` node, **152/152** at the birth build and **155/155** at HEAD, zero orphans. **The cause is that GEO's `label` is the Earth city name and the node's `label` is the game place name** — `` `TRD:{ num:26, code:'TRD', name:'goblin_cave',     label:'Goblin Warrens'@8686` `` reads *Trondheim* in the anchor table — and they diverge in **150 of 155** rows at HEAD. **The five that agree — `STN`, `AMS`, `CDG`, `NUE`, `ACE` — are exactly the codes §II uses as its worked examples**, so the author generalised from the 3 % of the table that reads like a node registry and then met `Trondheim` and `Madrid`. Nothing in either file says which kind of name you are reading. Two anchors are also one city twice (`NID` *Nidaros* / `TRD` *Trondheim*, 27 km apart), and the *"four-degree band"* spans six degrees.
> **FOURTH — the transcribed half is near-flawless, two months on.** 33,721 lines exact · bounds lat −8→68 / lon −25→72 exact · **13 of 13 coordinate pairs byte-exact** (`LHR` 59.3/17.6 · `CON` 41.0/28.9 · `JAR` 31.8/35.2 · `SAM` 39.6/66.9 · `NUE` · `AMS` · `CDG` · `FRK` · `CVP` · `SDR` · `MAD` · `ACE` · `PDL`) · **39 of 39** named GEO additions real · **14 of 14** §II.B codes genuinely outside GEO and all real nodes · `SAM` easternmost TRUE · `./api.sh reweave --execute` exact · Limerick 52.7/−8.6 correct. **Instrument 62 fires again: §II.A's region table sums to 76 while the prose beside it says 72** — the data half was right the whole time, and the real expansion is **76 → 152**, both tables identical.
> **FIFTH — the one actionable recommendation names a constant deleted twenty-six hours earlier.** *"Add to `PRIORITY_HIGHWAYS` in `src/api/wb.js`"*: real at `1bfe7a6` (2026-06-09), **removed by `ff3e044` on 2026-06-15 at 13:17** — **RETIRED, not never-shipped** (instrument 4/8, and worth the check: the naive verdict would have been "invented"). The live surfaces are the `priorityHighways` request parameter on the reweave endpoint and the `./api.sh highway <from> <to>` subcommand, which already existed. **The action shipped regardless** — 42 junctions, and `NODE_MAP` grew **409 → 451** in that commit, `J14` through `J55`, forty-two of them, matching the commit message exactly. Distances corrected: CVP→ACE is **1,155 km** great-circle, not 1,500; AMS→NUE is **541 km**, not 750.
> **SIXTH → §AUDIT-03bb NEW 🟡.** `PDL` (Ponta Delgada) sits at lon **−25.7** against a declared `minLon: -25` — the only one of 155 anchors outside the projection domain, then and now. The region-membership test is a bare comparison with no clamp, so **it matches no region cell at all**; geo-seed *does* clamp, pinning its column from a computed 4 to the grid minimum of 8. Neither path says a word. So §V.D's *"the geographic seeding will place them at the correct positions"* fails for exactly one anchor, and it is one of the four Atlantic islands that section is about. **Contention measured honestly rather than alarmingly:** 149 of 152 anchors land in distinct cells; the three that contend are all genuine duplicates — `LDN`/`BRK`, `KLZ`/`CLJ` (one city in two languages), and `JAR`/`JRS`, both labelled *Jerusalem*, 11 km apart.
> **Kept-not-deleted:** `DFL` (Dunfall — Loch Harbor) was **correct at the reference build** and is RETIRED, not wrong — the report's own birth commit renamed it to `ZRH`, colliding with the live `` `ZRH:{ num:72, code:'ZRH', name:'defi_land', label:'The Unbanked Quarter'@8789` ``, and a JS object literal keeps the last key, so the harbour node silently ceased to exist. Recovered since as `` `DNF:{ num:143, code:'DNF', name:'highlands', label:'Dunfall — The Loch Harbor'@8739` ``. · *"Every node code in WORLD_DB is an IATA airport code"* is refuted two pages later by its own §II.B and by the 42 `J*` junctions. · The Void Archaeology arc is **four** quests at `GVA` (act **3**), not five in Act 6.
> **Corroborated, NOT re-filed:** **§AUDIT-03u** — the Level-20 disclosure's *"16,024 lines … 423 entries … 67 terrain entries"* was already measured and filed by §DOC-02ah on 2026-08-12; **instrument 7 stopped a duplicate row that was three sentences from being written.** Also §AUDIT-03t (the 36 `act:NaN` nodes are the missing row in §III's table) · §AUDIT-03r (`MJF` and `CDG` both `num:77`) · §AUDIT-03ba (same root cause as §AUDIT-03bc, different surface: an author reads the most legible nearby text, and it is the one surface no gate checks).
> **Restated purpose (user directive).** The original stated its mechanism and left its motive implicit; the rewrite makes it §I. The anchor system buys three things a player actually feels: **travel time means something** (node-to-node movement across a projected grid, so real distance is felt distance), **the map becomes a memory aid that transfers** (learning that the Scholar's Quarter is south of Amsterdam is learning geography that holds), and **authoring stays accountable** — you cannot add a node called "Dark Forest" without answering *which* forest, near which city, at what latitude. Kept the original's best line intact: *"The Orient is real; the Dragon Palace is where the Orient is."*
> **Gates:** `check:anchors` **3,001 anchors across 68 docs, 0 dead** (2,991/67 at bi) · legacycodes · nodeindex · noderegs · invariants · dupkeys all **exit 0 run individually** (the known `check:walk` detach artifact). `play.html` **untouched** — the user's uncommitted 2026-08-06 recolor (9+/9−) left alone and staged around **by name**.
> **NEXT:** re-derive from `stat` — **the 2026-06-16 block is EMPTY**, the first time since the program opened that a whole era has closed with no remainder. The next-oldest file is `lab-report-terrain-field-mover-redesign.md` (`2026-06-25 18:52:53`).

> **⟶⟶ (2026-08-13) — §DOC-02be ✅ SHIPPED: an unusually strong transcriber and an unreliable narrator — 31 of 31 citations exact, and every error in a sentence the author composed.**
> `lab-report-synthesis-part4-monsters-fishing.md` (`2026-06-16 13:57:03` — the cross-reference of the two Yugurt Lake design reports), **223 → 285 lines**, rewritten IEEE-shaped. Re-derived from `stat` first; fifth of the ten-file 2026-06-16 block. Reference build `89fa13b` (`12:20:47`) is **33,721 lines — the report's stated baseline, exact**; mtime `13:57:03`, commit `58f89cf` `13:57:40`, a **37-second** birth window.
> **What held, and it held completely.** **31 of 31 bare line citations resolve exactly** at the reference build — `storyFishing` 26,731 · `FISH_POOL` 24,229 · `FISH_SIZE_TIERS` 24,387 · `LAKE_MAGIC_DB` 24,261–24,269 · `BAIT_TABLES` 24,334 · rods 24,403–06 · `NIGHT_FISH_POOL` 24,251 · `pb.fish` 32,571 · `_calcLuck` 21,519 · `_luckMod` 21,525 · all seven Luck sites · `_rollMonsterWeaponDrop` 22,533 · `BOO` 8,010 · `SSJ` 8,014 · guide 24,381 · profile 21,049 · portrait 20,789 · zone unlocks 26,810–11 · sheet 33,048. Every **transcribed** census is byte-exact and **still exact at HEAD two months on**: 20 fish ranks · 5 night species with ranks 6/8/10/12/14 and all five names · **18/18 bait rows** with their catch/type/ADV/SizeUp values · 8 relics all `sell:0`, minRank 11–20 · 4 rod tiers at 20/75/200/600gp · 5 size bands · 6 tournament opponents ending at The Fisherman **+8 / 1500gp** · five monster statlines · the `isNight`, catch-roll and type-roll formulas verbatim. **All 18 named symbols still resolve at HEAD.**
> **THE HEADLINE — the closing section is titled *"What Is Structurally True Right Now"*, and what was structurally true is that no player can reach the subject.** §FISH-01 re-confirmed unchanged at HEAD: `LYR:{ num:41@8723` (act 7) is declared **59 lines before** `BOO:{ num:75@8782` (act 3), both at cell `2,194`, so `const hasFish = node.isFishingLake;@35366` is never true and `storyFishing`'s one call site never renders. The document describes a live economy — 11 quests, ~100 authored data rows, two flag writers, one key event — that is entirely dark. `SSJ:{ num:76@8786` is alone in its cell, so **the player can meet the Fisherman and never fish.** *A synthesis reads its sources for what they claim; nothing in either source could say the node ordering had eaten the subject, which is exactly why a cross-reference needs a reachability check of its own (instrument 19).*
> **SECOND — an inverted verdict standing beside an exact pointer.** *"`_rollMonsterWeaponDrop()` (line 22,533) exists but the `[-3, 0]` range change was not applied."* At line 22,533 of the reference build: `const deg = Math.floor(Math.random() * 4) - 3; // −3 to 0`, prefixed `['Rusted ','Chipped ','Worn ','']`, **three lines under a comment reading *"fishing is the only source of +bonus weapons."*** The trade had shipped in full; at HEAD it ships harder — `const deg = Math.min(0, d6 - 5);@24592` widens the floor to −4, adds `Wrecked`, and draws the seeded stream (§VM-01-B). The only real delta is cosmetic (spec's `Salvaged` at 0 shipped as an empty prefix). *Instrument 27 — a citation proves the author found the line, not that they read it.*
> **THIRD — the rename table is not a rename table.** All **7 of 7** "Original name" entries return **0 commits in the file's entire history** (`Spine Perch`, `Venom Roach`, `Razorback Carp`, `Poison Bream`, `Barbed Tench`, `Venom Pike`, `Razorfin Zander`). `FISH_POOL` shipped with its live names and never carried any other — §DOC-02m proved the source's name column was reconstructed from the table's *shape* (names 3/20, `tier` 20/20). This synthesis read that reconstruction as an editorial record and supplied a motive: *"Names shifted toward more evocative prose."* **Instrument 52 in a second subject: a wrong figure with a plausible story attached is a false map.**
> **FOURTH → §AUDIT-03s EXTENDED — `YC` is a fossil, it is `SSJ`, and the count GREW.** `YC:{ num:76, code:'YC', name:'yugurt_cabin', label:'Yugurt Cabin', act:3` was real at `32c10c5` (2026-05-24) and renamed to `SSJ` — same `num`, same terrain key, same label — by `c1d5a94` (2026-05-29 22:45): **RETIRED, not born-dead**, and only the archive can say so (instrument 8). Both consumers gate on `node.code === 'SSJ'`; only the comments are stale — **4 sites at the reference build, 6 at HEAD**, because §VM-01-G2b's hook migration copied both onto its two new call sites while correctly rewiring the code beneath (`_runNodeHook('birka-rod-shop'@35212`, `_runNodeHook('ssj-tournament'@35812`). *Instrument 38 — a comment cannot break, so nothing forces the read.* The report's own Summary says `SSJ` three pages after its body says `YC` twice.
> **FIFTH → §FISH-03 NEW 🟡 — a quest reward whose central table is fiction.** `const FISHING_GUIDE_TEXT =@26659` promises *"Shore — DC 8 · Reeds — DC 12 · Deep — DC 16"*; the code computes `Math.max(4, (S_story.fishingBaitSatchel ? 8 : 10) - _luckMod())` **once**, never consulting `loc`, so all three zones share one DC and **none of the three printed numbers is it** — and §DX-02u's `fishingBaitSatchel` (2 readers, 0 writers) makes the true value a flat `10 − LuckMod`. Recommend implementing the gradient rather than correcting the prose: the design is right, only the branch is missing, and a reward describing a mechanic the game lacks is worse than no reward.
> **SIXTH → §DX-02ac EXTENDED, fifth document.** *"Starting scores (16,12,14,10,12,8) yield Luck 12, Mod +1"* — arithmetic exact, character impossible: that statline lives only in the dead `||` fallback at `const s = S_story.abilityScores ||@23439`, and the real default `(10,8,8,8,8,8)` gives product 327,680 → **Luck 9, Mod −1**, a *penalty*. **The mechanism is the point: a cross-reference copies its predecessors' conclusions verbatim while re-checking only their pointers**, so a wrong literal propagates at the same rate as a right one and arrives looking corroborated.
> **Smaller corrections, kept not deleted:** the Lantern Eel is **not** a story-gated encounter — `night_03` is rank 10 of `NIGHT_FISH_POOL`, drawn by any night cast landing in the `large` band, and the report's own night bullet says so two pages earlier (correct grouping: **two** gated encounters, `horned_shark` at `BOO` and `cave_lurker` at `MJF`) · the relic effect list names a `catch` effect that does not exist; the sixth is `fishing_dc` (`Yugurt's Eye` cuts the search DC) · *"d20 + DEX mod **vs DC 12**"* is a **three-band** test, `const castMod = dexTot@30534` cutting at **12 and 17**, with no pass/fail · *"`EB_NPC_DIALOGUE` pool entry at 9,322"* — line exact, registry wrong (it is `NPC_DIALOGUES`; `EB_NPC_DIALOGUE` is EB-code-keyed and holds no fisherman) · *"Phase 1 = `plan.md` … the living governance protocol"* is **STALE**, true when written (split into CONTRIBUTING.md + BACKLOG.md 2026-07-09) · the third zone is `shallows` in data, `deep` in state, *"The Deep"* on the button and *"Deep (The Shallows)"* in the guide, held together by `const _zoneMap = { bank:'shore'@30473` — the report transcribes **both** vocabularies correctly and never notices they are the same three places.
> **Filed:** **§FISH-03** 🟡 NEW · **§AUDIT-03s EXTENDED** 🟢 · **§DX-02ac EXTENDED** 🟢. **Corroborated without re-filing:** §FISH-01 · §FISH-02 · §DX-02u · §DX-02v (the `FISH_POOL` comment still reads *"Yugurt's Leviathan"* against a rank-20 `name` of *"Yugurt's Dread"*).
> **Verified:** `check:invariants` · `dupkeys` · `nodeindex` · `noderegs` · `legacycodes` · `anchors` all **exit 0**, run individually (`check:walk` detaches at 600 s — the known artifact). `play.html` **untouched** (the user's uncommitted 2026-08-06 recolor is preserved; only `docs/lab-reports/` and `BACKLOG.md` were staged, by name).
>
> **NEXT: §DOC-02bf — re-derive from `stat`; expected `lab-report-synthesis-part5-npc-narrative.md` (`2026-06-16 14:04:08`), sixth of the ten-file 2026-06-16 block.**
>

> **⟶ PRIOR (2026-08-14) — §DOC-02bd ✅ SHIPPED: everything it borrowed is exact, everything it counted is wrong, and the world it describes is three times too small because of a space before a brace.**
> `lab-report-synthesis-part3-world-navigation.md` (`2026-06-16 13:50:20` — the cross-reference of 13 World & Navigation reports), **342 → 281 lines**, rewritten IEEE-shaped. Re-derived from `stat` first — **fourth of the 10-file 2026-06-16 block; six remain.** Existing-Work-First: `git status` carried only the user's uncommitted 2026-08-06 recolor of `play.html` (**not mine, left untouched and unstaged**); this increment is docs-only. **Dating:** reference `89fa13b` (12:20:47, **33,721 lines — the report's stated baseline, exact**), mtime 13:50:20, commit `64b526d` 13:50:47 — a **27-second** birth window; `120d617` at 16:31:20 removed the movement gates 2 h 40 m later.
> **What held, and it is a clean split.** **19 of 22 `symbol@line` citations line-exact** (2 near-misses both flagged `~`; one unflagged, the grid-bound check at 26,009 → 26,015). **13 of 13 TRANSCRIBED source figures exact** — 781/694/89 %/87/497/1683 · 20,936/20,493/97.9 %/443 · 268 zombie J-stubs · 419 clean nodes · 20 EB bosses · 20 EB profiles · 11×17 minimap · CHA DC 17 · road/junction rate 0 with all seven wilderness terrains inside the stated 0.15–0.35 band · *"35+ Playwright tests"* against a real **39**. Its reachability claim **re-derives stronger than stated**: a four-directional flood of the reference commit's own `NODE_COORDS` from `LHR` reaches **411 of 411**.
> **THE HEADLINE — a census that counted a formatting style, and then invented a population to explain the remainder.** The report says `NODE_MAP` holds **"127 named content nodes"**; the file it was reading holds **409**. **127 is exactly the count of entries written `CODE:{`; the other 282 are written `CODE: {`** — one space — and 127 + 282 = 409. The 282 invisible entries are not scaffolding: `ALP: { num:304 … Aleppo — Storytellers' Quarter`, `BGZ: { num:420 … Burgos`, `ANK: { num:548 … Ankara`, carrying the whole seven-quest El Cid arc and the `cai_c*` manuscript chains. The Summary then writes **126** (contradicting its own body's 127) and explains the residue as ***"the gap (285) consists of intermediate relay nodes — nodes with `r,c` coordinates but minimal content"*** — **a population that does not exist.** The real gap is **2**: the keys `E:{r:63,c:223}` and `W:{r:65,c:225}`, stripped compass fields re-parsed as node codes by that morning's §CELL-14 migration, **born at `30f18b4` 09:39 and dead at `120d617` 16:31 — a 6 h 52 m defect, and this document is the only artifact written inside it.** Instrument 9 fires hardest here: the corroborating figure was *inside the same document* — Report 11's own heading transcribes **419 clean named nodes** from a sibling written two days earlier, so 419 and 126 sit three pages apart and neither is reconciled.
> **SECOND FINDING — "the stalk/hunt duality is fully live", measured at the commit it was written against.** `storyStalk` = **0 occurrences**; `#story-stalk-modal` has one `<div>`, two bulk close-all arrays and one `classList.remove('visible')` — **nothing ever adds `.visible`**; `.stalk-chip` is **4 hits, all CSS**; `targetTerrain`/`targetKeys` are **0 at that commit and 0 in the file's entire history**, so the quest→terrain closed loop never had data (independently corroborating §DOC-02c). What *was* live is the mechanic wearing the other's name: `effectiveRate = S_story.huntMode ? 1.0 : baseRate` — **Hunt is the 100 % guarantee**, fired on every empty cell, so the guarantee shipped and the designated battleground did not. §DOC-02f's rule at full strength on a second, independent target: *grep for the code that REVEALS a surface, never the surface's own id.*
> **THIRD — one table, two fossils, one month apart, and they cross.** Report 2's table gives `WORLD_DB` = *66 terrains* (live: **106**; 66 is the value at `32c10c5`, **2026-05-24**) and `HUNTING_GROUNDS` = *42* (live: **66**; 42 is the source report's **2026-05-21** count). **The first row's number is the second row's current value.** The `42` outlived the table: it is still in the engine at `known world has forty-two nodes@10397` (Yael's Level-1 tutorial, the first instruction a new player reads) and in the Map Shop's Real Map — both already filed as **§AUDIT-03u**. *The doc-side fossil and the engine-side fossil are the same constant.*
> **RETIRED, not wrong.** Reports 5–9's whole toolchain checks out at the reference commit — `fix-all-broken` 5 · `fix-bidirectional` 3 · `rip-and-connect` 9 · `reweave-all` 7 · `layout/solve` 4 · `wither` 56 · `derelict` 14 · P0–P8 banners 21 — **including `--no-wither`, which is real** (`emit('[p7] skipped (--no-wither)')`). Instrument 4's verdict is RETIRED. At HEAD `reweave-all` and `rip-and-connect` answer **HTTP 410**, the rest are 0, and the report's coordinate advice (*"stay well within 10–490"*) is unrepresentable against `const GEO_PROJ@9902` = `{ROWS:90, COLS:360}`. Report 13's node codes fail the §DOC-02d way — 5 of its 7 named codes never existed (`OW`/`SK`/`SB`/`LD`/`BN` = 0 then and now) while **the content all shipped** as `MME` (Saltwick), `DNF` (Dunfall) and `MS` (the Bilge Mystery); *"Ben Barleigh"* is the one name with 0 occurrences ever.
> **Two new instruments. 51 — a census is only as good as the pattern that took it: prove the pattern matches the LAST entry as well as the first, because the newest content is written in the newest style and is therefore the likeliest to be invisible. 52 — when a count leaves a gap, the danger is not the gap, it is the EXPLANATION.** A wrong figure is an erratum; a wrong figure with a story attached is a false model the next author designs against. **If you cannot name three members of a residual population, you have not found a population.**
> **Filed:** **§DX-02bf EXTENDED** 🟢 — `fix-all-broken` and `fix-diagonal` are two more members of that row's retired set: **0 in `src/js/wbapi-server.js`** (5 and 4 at `89fa13b`), still 4 sites each in `src/api/wb.js`, and **9 live doc sites**; the sharpest is `docs/api/API-README.md:280–290`, a fenced copy-pasteable block of all three closed **three lines later** by the doc's own *"Retired (§WALK-3)"* paragraph, which retires their four siblings and not them. **Corroborated without re-filing:** §AUDIT-03u (the two forty-two strings), §EPIC-03 (`const canNegotiate@30268` = `ceiling > floor`, so the CHA DC 17 negotiation renders at **4 of 20** battlegrounds), §DESIGN-04 (§6 is its origin story — the anti-search primitive was CSS, so *"where do I find monster X?"* has never had an answer), §DX-02m.
> **Verified:** all **16 `check:walk` gates green** run individually (`check:walk` detached at 600 s — the known artifact; killed and re-run per gate). `check:anchors` **2,786 anchors / 62 docs, 0 dead**, and all six new `symbol@line` anchors in the report resolve **line-exact**, none in the 105-stale list. `check:legacycodes` OK. Docs-only: no HTML edit, no test run needed. **NEXT: §DOC-02be — re-derive from `stat`; expected `lab-report-synthesis-part4-monsters-fishing.md` (`2026-06-16 13:57:03`), fifth of the 10-file block, six remain.**
>

> **⟶ PRIOR (2026-08-13) — §DOC-02az ✅ SHIPPED: the report is correct and the ground moved.**
> `lab-report-movement-by-cells.md` (`2026-06-15 11:13:52` — §CELL-01–§CELL-12, the cell-grid navigation architecture, program flow and validation design), **1,608 → 448 lines.** Re-derived from `stat` first; the §DOC-02ay sibling, and the delta between the siblings *is* the finding. Existing-Work-First: tree carried only the user's uncommitted 2026-08-06 HP-bar / playfield recolor (18 lines, **not mine, left untouched, staged BY NAME**).
> **DATING — the sharpest shelf-life measurement the program has produced.** Birth commit **`79b5bee` (2026-06-15 13:32)**, 2 h 18 m after the mtime — same-day, no ambiguity. The coordinate space it is written in died **ten days later**: `153a37c` (2026-06-25, §WALK-1.5 inc 1–2 — equirectangular projection + `CELL_GRID` locale lists) and `896846c` (2026-06-26, projection applied + SEA_MASK). **`LHR` moved from `(64,224)` to `(10,197)`; the grid from 500×500 1-indexed to 90×360 0-indexed with E↔W wrap.**
> **VERDICT: the architectural thesis holds; the arithmetic does not.** Exits are still derived from adjacency — **0 cardinal and 0 diagonal fields across 416 nodes**, now defended by two HTTP 400 guards (`src/js/wbapi-server.js:_badNodeFields@10145` create, `src/js/wbapi-server.js:_badPutFields@11043` update). §6.4's hardest claim survived at 5.5 MB: **`storyCollectLoot` is still the ONLY write to `S_story.shards` in 38,712 lines** (I6). I3, I4, I5 hold. `TERRAIN_ENCOUNTER_RATE` is byte-identical; `reweave-all` still 410s and §WALK-3 has since deleted its ~3,200-line body.
> **THE HEADLINE — the report's FIRST invariant was deliberately REPEALED, and the data structure changed shape to say so.** I1 (coordinate uniqueness) is violated **172 times**: **416 nodes occupy 244 cells; 66 cells hold more than one node**, and `32,203` holds **17** (the Littoral Courts). At 1° resolution Athens and Istanbul are not two squares apart, so §WALK-1.5 kept both cities and made `CELL_GRID[key]` an **array** — `cellCode(k)` reads the primary. So I2 (`CELL_GRID["r,c"] === code`) is false for all 244 cells including the 178 single-occupant ones. Player-facing consequence the report could not have described: **`LHR` (City Streets) and `BK` (Birka Shore) are the same cell** — two rooms of one locale, which is why §NAV-01a had to redefine waypoint arrival as *standing on the destination's CELL* rather than matching its code. ***Distinguish "the claim drifted" from "the claim was repealed": the first wants a correction, the second wants the invariant rewritten.***
> **SECOND FINDING — this report is the last surviving description of the eight movement gate-locks.** §7.2 shows `cellMove` consulting quest state before allowing a step (`TLS` sealed until 7 shards, `DAM` blind days, `NUE` Tide Gate, +5). The block was **real** — present in the tree at the report's own commit — and was **deleted the next day** by `1872896` (2026-06-16). Zero occurrences remain. It contradicted `prompt.md` §6.1, the project's first invariant. Today `function cellMove(dir)@28345` is a thin caller over the shared `Mover.move` kernel and can refuse only off-grid and sea.
> **THIRD — the test plan is the program's cleanest NOT-SHIPPED result, and the repo wrote its own epitaph.** `src/tests/unit/` does not exist; **4 of 5 specified files never shipped.** The fifth, `navigation.test.js`, was **rebuilt by §WALK-4 Inc 3 in a header that names this report's fixtures as the reason**: *"The previous version hardcoded pre-§WALK-1.5 coords/adjacencies (BOO 47,223; BOO→LXF→SEN corridor) and a since-removed `hoursElapsed += 1` on movement."* Those are §12.3's three tests, named and dated. Two of the four unbuilt files were **unbuildable as specified** — their central assertions were *"zero coordinate collisions"* and *"grid count equals coord count"*, both false at HEAD by design. ***A suite written to a repealed invariant is not a coverage gap; it is a specification that would now fail correctly.***
> **FOURTH — three graphs, one prelude, and the ay finding located at its source.** §11.2 attributed the `undirAdj`/`bfsReach` construction to `GET /api/graph/reachability`. The code survives **verbatim** at `src/js/wbapi-server.js:const undirAdj = new Map()@5374` — but it is now a **prelude shared by every `/api/graph/*` sub-route**, and reachability does not use it. Land flood **416/416, one component** (what a player would call walkable) · named-node 4-adjacency **2/416** · `cluster-bridge` over the empty compass fields **1/416**. I5 therefore holds *under an algorithm the report does not describe* and fails 2/416 under the one it does.
> **ROWS FILED — four.** **§DX-02bo** 🟠 (`src/tools/worldmap.js --seed` still projects into **GRID_MIN 8 / GRID_MAX 500** while the world is 90×360 — and `PUT /api/coords`, `nudge` and `POST /api/node` have **no bounds validation at all**, so the retired arithmetic is the only guard; ***a claim can stay true while becoming wrong — the code never drifted, the world drifted out from under it***). **§DX-02bp** 🟡 (`POST /api/node` returns **201 `ok:true`** for a node whose colliding coordinates it silently declined to write — minting §8.3's own "ghost", prompt.md Hazard #5 in a live path — while `PUT /api/coords` **409s the locale model the client requires**: 172 live nodes the API refuses to author). **§DX-02bq** 🟢 (`/api/audit/map` **shadows** the promoted `buildCellGrid` with a **last-wins** copy and disagrees with the client's primary at **all 66 shared cells** — at `10,197` the audit says `BK`, everything else says `LHR`; plus **5 of its 12 checks** walk fields that number 0 and can never fire, which is what §11.3's criteria 3 and 4 actually measure). **§DX-02br** 🟢 (`navigation.test.js` suppresses encounters with `Math.random = () => 1`; the roll is `_seededNext()` — **third recurrence** of §DX-02e/§DX-02f, in the file that was rebuilt to fix the second).
> **PLAYABILITY framing the rewrite adopts (user directive):** the §CELL question was never "how do we store exits" but ***"why does the map need a second copy of itself?"*** A coordinate already says where a place is. The dividends are all player-facing: the ~27,600 walkable land cells became places you can stand (`_enterEmptyCell@28420` gives each prose, terrain, signposts, an encounter roll) so **distance became travel instead of a menu**; `road:0` against `forest:0.25`/`hag_swamp:0.35` makes the highway a genuine safe-and-slow trade-off rather than a permission; fog of war is per-CELL, a record of where you walked rather than a checklist of unlocked nodes; and free movement is structural — a step is refused for exactly two reasons, off-grid and sea.
> **Corroborated from prior passes:** §DX-02bm (`junction:0` still in `TERRAIN_ENCOUNTER_RATE`, unreachable) and §DX-02bn (`serializeNodeLiteral`'s dead `N,S,E,W` — **this report's own §9.2 predicted it as a cleanup candidate, and three months on it is the most durable sentence in the document**).
> **Gates:** `check:anchors` **2,683 across 58 docs, 0 dead** (up 57; 105 pre-existing stale hints, warn-only, none mine). One dead anchor was mine and was caught before commit — an unqualified `src/js/wbapi-server.js:function broadcastCell@129` for a **server** symbol; `check:anchors` resolves unqualified anchors against `play.html` ONLY, so a server symbol needs the qualified `src/js/wbapi-server.js:symbol@line` form (the §DOC-02ay anchor rule, re-proved). `check:walk` 16/16. `play.html` **untouched**.
> **NEXT: §DOC-02ba — re-derive the oldest mtime from `stat` (expected `lab-report-quest-data-code-separation.md`, `2026-06-16 12:17:54`, the head of the 2026-06-16 synthesis block).**

> **⟶⟶ §DOC-02ax ✅ SHIPPED: the algorithm the report describes is real, and it is in the caller.**
> `lab-report-highway-mesh-entry.md` (2026-06-10 11:10:08 — the same-component skip + mesh-entry reroute fitted to `buildHighway`), **69 → 292 lines.** Re-derived from `stat` first; **first file outside the 2026-06-09/10 reweave-era cluster is the NEXT one, not this one** — this is the era's fifth and last document. Existing-Work-First: tree carried only the user's uncommitted 2026-08-06 HP-bar / playfield recolor (18 lines, **not mine, left untouched, staged BY NAME**); `milepoints/api-cli.log` was dirtied by this pass's `./api.sh` probes and restored with `git checkout`.
> **DATING (instrument 18) — the cleanest result the program has had.** Birth commit **`e2576f6` (2026-06-10 13:30)** *"reweave: patch queue + batch saves — fix OOM during fix-bidir/grid-connect"* — **the same commit added this document and its code**, 2 h 20 m after the mtime. No dating ambiguity to resolve at all. It is also the commit §DOC-02aw measured shipping P_NUKE, so the two morning brakes landed together. (Path hazard held: the server was at repo root as `wbapi-server.js`.)
> **VERDICT: RETIRED, not wrong — with one review defect and one accounting defect.** Both specified guards shipped and both work; 9 of the 14 delta rows are exact, including all four trace **templates** byte-for-byte and every rewired call site.
> **THE HEADLINE — the O(n²) closest-pair the report claims to have written is real, is twenty lines below the call site, and is byte-identical before and after the commit.** §Implementation step 3 and the entire Performance note describe *"Closest-pair loop — O(|fromNet| × |toNet|), minimum Manhattan distance."* The shipped function does **two sequential linear scans**, under a comment that says so: *"mesh-entry selection: O(n) per side, not O(n²) … Two linear scans — safe on large (15k+) components."* The genuine double loop is **P3's own selection loop** (`unreach × reach3`), untouched by this change. ***The instrument: when a report's algorithm does not match its code, check whether it matches the code NEXT TO it — a description wrong about the diff is often right about the file, which is exactly why it survives review.*** The shipped greedy is also **not minimal**: scan 1 anchors on `origFc`, *the very cell the report's own §3.2 argues is the wrong place to measure from*, so the from-side fix is conditioned on a to-side choice made before it (worked counterexample in the rewrite: 6 cells walked where 1 would do). And **`netNodes(hub)` — implementation step 2, and named again as the mitigation — has 0 occurrences in any `.js` file in the repository's entire history**; its only appearance anywhere is this page. §DOC-02au's `MAX_CLEANUP`/`MAX_WITHER` shape exactly: *a document dignifying an unnamed expression with a constant name, then reasoning about the name.*
> **SECOND FINDING — the guard disabled the phase it was written to optimise.** Re-implementing the report's algorithm against `62a4023` (the world standing when it shipped) shows all **4,428** linked nodes in **ONE component**, so the skip fires on **7 of the 8** configured `PRIORITY_HIGHWAYS` and the eighth (`HHL→GEDI`, a highway to a node that never existed) bails one guard earlier. **P2 built nothing.** Two consequences went unrecorded: *(i)* two of the three published traces (`LHR→BGD`, `CVP→SAM`) document branches their own pairs can never reach, because the skip pre-empts them; *(ii)* **"no junctions created, no writes" is true of the function and false of the operation** — the skip returns `ok:true`, and P2's caller tests only `r.ok`, so it still runs `rewriteCoords()` (re-serialising 4,495 coord entries), `_buildIndexes()` and `batchSave()` (~9 MB write + copy + re-parse) **seven times for zero graph change, in a commit whose subject line is "fix OOM."** *The brake was fitted to the wheel that was not turning.*
> **TRACE FORENSICS — instrument 9 at its cleanest inside one short document: every INPUT copied and exact, every OUTPUT composed and fictional.** `LHR→TRB`, `CVP→SAM` and `ACT→BGD` are three literal entries of the eight in `PRIORITY_HIGHWAYS`, and the report's one geographic gloss — *"BGD (Baghdad)"* — is **correct** (gazetteer `lat 33.3 lon 44.4`; label *Baghdad — Sufi Scholar Quarter*). But `J18203` names a junction **4.6× higher than the highest that existed** (`J3977`) and 4.1× the whole node count — that scale first appears three days later at `be046d4`; `net=312` / `net=28` / `847` are three different sizes for components that were all 4,428; `direct-dist=87` for a pair **148** apart; `dist=144` for a pair **18** apart. The only internally consistent thing is the arithmetic (`87 − 12 = 75` ✓, endpoints `(44,18)→(44,30)` 12 apart ✓): ***the numbers were made to agree with each other rather than with the map.*** Also wrong: *"440 named nodes"* → **461** named (384 non-junction). Corpus rule restated: **a sample trace is the least verifiable artifact a design document can publish and the most persuasive one it can print.**
> **NODE CODES (instrument 31) — 5 of 5 byte-identical in `num` AND `label`, archive → HEAD.** `LHR`(1) *City Streets — Birka* · `TRB`(187) · `BGD`(190) · `CVP`(465) · `SAM`(191). **Incidental corroboration of §DOC-02at:** at `62a4023` the source holds 4,429 `NODE_MAP` keys but 4,428 distinct — **`ZRH` declared twice**, one commit later than at found it, which is why every parsed census that week ran one low.
> **RISK REGISTER — one risk filed, all three of its terms wrong.** The Performance note warns the O(n²) loop will not scale and proposes capping `netNodes` at the 200 nearest. The loop is not O(n²); `netNodes` does not exist; the cap was never needed. The one real hazard — a skip still paying a full serialise-save-reparse — is unmentioned. *Instrument 40's third confirmed instance in this cluster: the author audited the function they wrote and imagined the one that would stop it.*
> **ROW FILED — §DX-02bl (🟠).** Probed live at HEAD: **three endpoints, three reachability models, three answers, one server process.** `GET /api/graph/reachability` (§WALK-1.5 land flood) → **416/416, 0 clusters** ✅; `GET /api/graph/junction-audit` (cell-touching BFS) → **2 of 415** named reachable; `POST /api/graph/cluster-bridge` (BFS over `node.N/S/E/W`, of which there are **zero**) → **1 of 416, 415 isolated clusters of one node each**. ***The one that is correct is the one that refuses to repair anything; the two that are wrong are both wired to write.*** With `{execute:true}` `cluster-bridge` walks all 415 clusters and issues **up to 830 `PUT /api/node/…` writes that re-introduce the compass fields §CELL-01 stripped to zero** — and unlike §DX-02bk it does *not* use `saveStamped()`, so it lands in `play.html` directly. It is a documented green CLI verb under *"§22 NETWORK HEALTH & REPAIR"*. `junction-audit` also disagrees with itself inside one payload: `junctionDegreeDist` puts `J13` at degree 2 while `nukePreview` calls the same node `deadEndDelete` (degree ≤ 1). **Close as one retirement sweep with §DX-02bf · §DX-02bi · §DX-02bk.**
> **PLAYABILITY framing the rewrite adopts (user directive):** a junction is a room with no reason to be entered, and every unnecessary one is a keystroke the player pays on every crossing forever — ***distance should mean travel, not typing.*** The invariant underneath is the project's first (`prompt.md` §6.1, the world is always freely traversable). The design instinct — *don't build a road to a place you can already reach* — outlived every line of its implementation and is what §CELL-05/§WALK-1.5 eventually executed by abolishing routing stubs altogether. *The brake was right; it was fitted to the wrong wheel and described as a third wheel that was never there.*
> **Gates:** `check:walk` **16/16, 0 failures** (the full run printed every gate ✓; a second bare run for an exit code hit the known **detach artifact** and was killed at gate 5 — `Terminated: 15`, not a red — so the gates were re-run individually: `check:invariants` 0 · `check:nodeindex` 0 · `check:noderegs` 0 · `check:legacycodes` 0 · `anchors` 0). `check:anchors` **2,626 across 56 docs, 0 dead** (up 7 — mine: 4 in the report, 3 in the §DX-02bl row; 105 pre-existing stale hints, warn-only, none mine). `./api.sh reachability` 416/416, 0 clusters, deg1 148. `play.html` **untouched**.
> **HOUSEKEEPING found on the way in: §DOC-02au (`d2c9396`) and §DOC-02aw (`52e791b`) both shipped with full track rows but NO §RESUME entry** — the same step-10 gap recorded for §DOC-02ac/af/ag. Nothing is lost — both are recorded in full in the §DOC-02 track row at the top of this file, and both filed their rows (§DX-02bg/bh, §DX-02bk/§AUDIT-03az). **Worth a habit note: the §RESUME entry is the last step in the close order and the easiest to drop.**
> **NEXT: §DOC-02ay — `lab-report-cell-map-mud-redesign.md` (`2026-06-14 14:55:47`), the first file outside the entire 2026-06-09/10 reweave era. Re-derive from `stat` first.**

> **⟶⟶ §DOC-02av ✅ SHIPPED: the repair reported success and wrote it to a different file.**
> `lab-report-map-audit-layout-tooling.md` (2026-06-04/05, §11 appended 06-09 — the TTS queue, patch sidecar, map-audit rules, BFS grid solver), **448 → 376 lines**. Re-derived from `stat` first; oldest at `2026-06-09 17:12:46`, third of the reweave-era cluster. Existing-Work-First: tree carried only the user's uncommitted 2026-08-06 combat recolor, **not mine, left untouched, staged BY NAME**.
> **DATING (instrument 18) — a report with TWO birth commits.** §1–§10 → `bb522a6` (06-05 11:15); §11 → `1bfe7a6` (06-09 17:36), 24 min after the mtime. Each section measured against its own. The full session timeline was reconstructed (`a4372e4` say.sh 19:08 → `7f640ab` diagonals 20:18 → `c743aa7` fix endpoint 20:27 → `47a6bbb` solver 21:26 → `62613d4` UI 08:09 → `df9306f` flock 08:42 → `9b12c9f` voices 09:05 → report 11:15), and it is what makes every verdict below decidable.
> **THE TRANSCRIBABLE HALF IS NEAR-FLAWLESS.** Every code block re-measured is **byte-identical** to its source — §3's curses double-click block and §6.5's `NODE_COORDS` writer are still byte-identical at HEAD ten weeks on. §4.3/§4.4's counts reproduce **exactly** by re-deriving the published rules against the archive: **`alignment` 9 ✅, `axis_distance` 55 ✅, one-way links 36 ✅**, and *"largest: `TLS↔LHR` 40 cells, same column, rows 4 and 44"* is exact (`TLS{r:4,c:112}` · `LHR{r:44,c:112}`). All three named alignment examples are in the measured set. **§4.6's summary block is provably a PASTE** — its tally is emitted in rule-declaration order, which a mock would not get right. §6.2's BFS verifies clause by clause including `attempts < 64` and the orphan row at `maxR + step*3`. **`Nodes placed 305 / 305` is exact.** 8 of 8 Worldbuilder DOM ids live; 4 of 4 endpoints live; §9's 4-cell corridor limit is real (`for (let d = 1; d <= 4; d++)`).
> **THE HEADLINE — §4.2's repair NEVER LANDED, and it is CONTRIBUTING Hazard #5 eight weeks before the repo named it.** *"36 one-way connections were found and repaired via `POST /api/audit/map/fix`."* The **finding** is exact. The one-way count measured across **every** commit touching the HTML from 06-04 18:02 to 06-05 20:47 is **36 → 37 → 36 … then 50/49/44 as imports add nodes — never 0, never below 36.** The mechanism is three lines: `const stamp = WBAPI.getStampedName()` → `WBAPI.save(stamp)` → and `wbapi-core.js:573` is `const dest = outputPath || this.getStampedName()`, so `save(stamp)` writes **`roll2hit-v3-20260604-<hhmmss>.html`**, a dated sibling. All 36 back-links were written correctly, into a file nobody reads, and the response returned `ok:true, saved:true, note:'Changes saved and reloaded.'` **The diagonal fix in the same session persisted only because it had been done BY HAND at `7f640ab`, nine minutes before the endpoint existed.** This is the class §DX-01d/i and §DX-02k would name on 2026-07-30 and 2026-08-03; this report is its earliest written record, and it records the repair as done. ***The acceptance test for a write path is a round trip — nothing throws when it doesn't persist.***
> **41st INSTRUMENT — WHEN TWO DOCUMENTS TABULATE THE SAME PIPELINE, THE ONE IN THE UGLIER ORDER IS THE COPIED ONE.** §11 and its sibling `lab-report-mega-reweave.md` (§DOC-02au, four hours apart, same author) each publish a **nine-row** phase table and **they are not the same nine**: this one has **P1.5 coord-scan** and no P8; the sibling has **P8 final check** and no P1.5. Both omitted phases are real at `1bfe7a6` (`phaseBanner('P1.5: coord-scan', …)` ×9; `// PHASE 8 — final check`). **The pipeline has TEN phases and neither document says so → CORPUS CORRECTION to §DOC-02au's *"all 9 phases (P0–P8) verify"*.** And this report's table runs P0, P1, P1.5, **P4, P5, P2, P3**, P6, P7 — which looks like an error and is the **execution** order, confirmed by the pipeline's own banner `Road: 1/geo-seed → 2/rip-connect → 3/coord-scan → 4/fix-broken → 5/fix-bidir`. The sibling's tidy P0→P8 table was re-sorted by hand, ***and a hand that re-sorts also drops rows and invents them.*** Neither is internally inconsistent; only the pair is falsifiable.
> **INSTRUMENT 18 SHARPENED — the corpus's shortest rot interval: 2 h 10 m.** §1 documents `say -v Samantha -r 185`; `9b12c9f` had replaced it with a 13-voice random array at `RATE=190` at **09:05**, and `df9306f` had added the `fcntl` `say.lock` at **08:42** — the report committed at **11:15** omits the lock from its file table entirely, while its thesis sentence promises a queue *"without a daemon lock race."* Same author, same morning, same terminal. ***The claims to distrust hardest are about the files the session edited LATEST, because by write-up time the author has stopped re-reading what they already understand.***
> **OTHER DELTAS.** §5.2's stats block is a **mockup, not a paste** (its `Step 4` contradicts its own input's `value="8"`; a faithful re-run gives `Orphans 225` not 220 and `14 misaligned`, and `0 misaligned` is unachievable because the orphan row misaligns cross-links — which the report's own §10 explains). §6.3: components **167** not 160; *"~120"* → **116** ✅ and it *is* the LHR component; *"~185"* → **191** ✅; but ***"~105 nodes with no N/S/E/W connections" → 164, 36 % understated — and 105 is `missing_coords:105` from §4.6 one page earlier, a different property of a different set.*** Nothing in the session ever measured nodes-without-links, so the nearest available number was borrowed. §8's rule table is 11 of 12 (`corner_misalign` landed `14919b3` the same evening).
> **STATUS AT HEAD.** `NODE_MAP`: **0** `N:`/`S:`/`E:`/`W:` fields, **0** `junction:true`, 416 nodes. `reweave-all`/`rip-and-connect`/`fill-gap` → 410. `GRID_MARGIN`, `p4Deferred`, `nextNodeNum`, Tarjan → 0 occurrences; only `nextJCode` survives. **NODE CODES (instrument 31): 13 of 13 byte-identical in `num` AND `label`, `bb522a6`→HEAD — THE CORPUS RECORD** (prior best §DOC-02at, 12 of 12): `SID`(23) `OTP`(62) `BEL`(33) `LIM`(81) `KIR`(14) `NUE`(35) `HKG`(6) `LCY`(7) `GOT`(11) `SFT`(24) `TLS`(42) `LHR`(1) `BEG`(61).
> **ROWS. §DX-02bi NEW (🟠, the most serious of the pass)** — `GET /api/layout/solve` at HEAD returns **415 of 416 nodes as orphans** and validation `0 ok · 0 misaligned / 0 ok · 0 over`, a perfect score over an empty relation; the panel then prints *"415 disconnected nodes will be placed below the main grid"* and **reveals `Apply Layout`**, which `POST /api/layout/apply` executes unguarded, rewriting the whole `NODE_COORDS` section and collapsing the §WALK-1.5 geo grid into one horizontal row. Recommend 410 the endpoint (the §WALK-3 treatment its siblings got); close with **§DX-02bf**. **§DX-02bj NEW (🟢)** — `milepoints/patches/_last.html` (**5.35 MB**) and `_last.name` are **tracked in git**, swept in by a licence-header commit 8 h before this report claimed `.gitignore` excluded them; the two patterns were never added, and *a `.gitignore` line cannot un-track a committed file*. **§AUDIT-03ay NEW (🟢)** — 8 of the map audit's 12 rules are edge-driven, so the Worldbuilder's `Map Audit` button reports a clean graph over 0 edges: §DX-02bf's class at a second site, on the UI surface the CLI fix won't reach.
> **PLAYABILITY framing the rewrite adopts (user directive):** none of this is a player feature; all of it protects the project's first invariant — **the world is always freely traversable**. Under the edge graph, every way that invariant broke was *silent*: a diagonal exit is a door the corridor builder never reads, a one-way link is a road you can walk down and not back up, an off-axis pair renders no corridor at all. The map still draws, the game still loads, and a city wired last week is quietly unreachable. **The session's principle — *a broken map should be loud* — is the reason the rules exist**, and it is the same principle §DOC-02au's `NaN×NaN` vs `6×6` note turns on.
> **Gates:** `check:walk` 16/16 · `check:legacycodes` OK · `check:anchors` 0 dead. `play.html` **untouched**.
>

> **⟶⟶ §DOC-02at ✅ SHIPPED: the report was right about every number and wrong about what the numbers meant, because it chose a convergence metric that its own repair inflated the denominator of.**
> `lab-report-node-network-reconnection.md` (2026-06-09, the 89 %→100 % node-graph repair — `fix-all-broken` + bidirectional batch fix + `rip-and-connect`), **231 → 214 lines**. Re-derived from `stat` first; **first file after the `2026-06-09 11:06:54` block emptied**, mtime `11:46:48`. Existing-Work-First: tree carried only the user's uncommitted 2026-08-06 combat recolor (9/9 hunks, **not mine, left untouched, staged BY NAME**).
> **DATING (instrument 18) — a one-minute window, the tightest the program has hit after §DOC-02aj's 59.** Report mtime `11:46:48`; birth commit **`661aa29` (2026-06-09 11:48)**, *"map: full node reconnection — 89%→100% reachability, 781→1683 nodes"*. It is an uncommitted-tree report committed one minute later, so every figure is measurable at its own commit.
> **VERDICT: RETIRED, not wrong (instrument 4).** Every published number holds at `661aa29`. The *architecture* — per-node `N`/`S`/`E`/`W` link fields and `J####` elbows — was deleted by §CELL-01/§WALK-1: **1,017 / 1,015 / 918 / 915 direction fields → 0 / 0 / 0 / 0**, `junction:true` **1,300 → 0**, and `rip-and-connect` / `fill-gap` / `reweave-all` now return **HTTP 410**. The report's own §V patch is byte-identical and *running* at `src/tools/worldmap.js:1201–1202` — renders `6×6`, not `NaN×NaN`, on the same `lat -8°–68° lon -25°–72°` bounds. *Two hundred lines of graph surgery deleted; the four-line argument-parsing fix at the bottom of the page still ships.*
> **THE HEADLINE → 39th INSTRUMENT: A CONVERGENCE METRIC WHOSE DENOMINATOR IS GROWN BY THE PROCESS IT MEASURES CANNOT REPORT DIVERGENCE.** §VIII defends the rising broken count — *"the correct convergence metric is broken/total-edges ratio, not absolute count"* — and the ratio duly fell 62.4 → 59.7 → 53.8 → 47 %. But every pass spawned elbows and every elbow adds edges to the denominator. **Measured cascade:** `3ac0aed` 531/166 → **`661aa29` 1,684/1,300** → `9833f48` (same day, 18:17) **4,429/4,045** → `f1449f5` 2,875/2,491 → `efa8f7a` (§WALK-1) 410/**0** → HEAD 416/0. The commit log between the two carries two **out-of-memory** fixes (`e04e903`, `e2576f6`): the map ran the tooling out of heap. The successor report `lab-report-junction-reweave-overhaul.md`, filed 22 h later, calls the identical mechanism a cascade failure (instrument 7).
> **AND THE CONTENT COLUMN NEVER MOVED — 384 across the whole cascade.** The report records this as reassurance (*"Content nodes 383 → 383 … All 383 content nodes are reachable"*). It is the diagnosis: by 18:17 the world was **91 % scaffolding**. 8.4× growth in the map, zero growth in the game.
> **THE 1,683/383 DISCREPANCY — the report is right for a reason it could not see.** Source text at `661aa29` holds **1,684** `NODE_MAP` entries and **384** non-junction ones; both published figures are one lower because both were read from the *parsed* model, and `ZRH` is declared **twice** (`num:143` *Dunfall — The Loch Harbor* at :8024, `num:72` *The Unbanked Quarter* at :8068). Last-key-wins. Dunfall — its NPC **Mairén Fionn**, its harbor, its `sleep:true` checkpoint — existed in the file and in no runtime model from `e339aeb` (05-28) until `291c82a` (06-26) re-coded it to **`DNF`**. ***A duplicate key does not break reachability; it removes the node from the denominator, so the check passes — and passes because the node is gone.*** The rot class `check:dupkeys` (gate #11) was built for seven weeks later.
> **NODE CODES (instrument 31) — 12 of 12 byte-identical in `num` AND `label`, archive→HEAD: the corpus record** (prior best §DOC-02as, 6 of 7). **But 7 of the 8 geographic *glosses* contradict the `label` on the same source line, at the report's own commit:** `LGW` *"London airports"* = **Tilbury Market Quarter**, `STN` = **The Map Shop**, `MAD` *"Madrid"* = **Shattered Seraph's Spire**, `HAV` *"Havre"* = **Admiral's Last Cove**, `SIG` *"Sigtuna"* = **Siggeir's Hall**, `HFT` *"British Isles"* = **South Shore — The Fishermen's Village**, `CONREG` = **Constantinople**. The author read `LGW`/`STN`/`MAD`/`HAV` as **IATA airport codes** and described the world from the airline atlas. ***The two glosses that hold are the two whose codes are not IATA*** (`NID`→Nidaros; the Canterbury four). Instrument 12 in a new form: not a bad copy — a *correct* copy glossed from outside knowledge.
> **§VII IS NOT MERELY STALE — all four of its rows now measure quantities that have no definition.** Broken edges, cycling bidirectional conflicts and direction-sign violations all required the stripped fields; degree-1 dead ends re-based from links to adjacent cells (149 → 148, a coincidence across incompatible denominators, recorded so nobody reads it as continuity).
> **ROWS. §DX-02bf NEW (🟢)** — the live CLI prescribes `layout-solve --apply` at **eight sites** (its own header: *"constraint-propagation grid layout for N/E/S/W networks"*, of which there are zero), including *inside* the help block headed **"Full world reset sequence (cell-first, §WALK-1.5 geo flood)"**, five lines above the `NOTE (§WALK-3)` saying the edge graph is retired; and `./api.sh fix-bidirectional` prints **`✓ 0 bidirectional violations found`** over an empty field set — a green badge for a subsystem that does not exist (instrument 16's shape). **§AUDIT-03ax NEW (🟢)** — `J13` is the last reweave-era elbow, *promoted* rather than deleted (re-terrained `midlands`, labelled *The Western Sea Road*, given node text, `questCount:0`): `junction-audit` counts by key-prefix and says **1**, `check:invariants` counts by flag and says **0**.
> **PLAYABILITY framing the rewrite adopts (user directive):** reachability is not a graph statistic — it is the difference between a city that exists and a city you can walk to. 87 nodes, including real historical cities and quest locations, were *dark*: in the database, in the docs, unreachable by any route. The invariant defended is the project's first — **the world is always freely traversable**, a step refused only for off-grid or sea. **That invariant is intact: 416/416 reachable from `LHR`, 0 clusters.** The thesis outlived its architecture; it is now carried by a terrain-field land flood (§WALK-1.5) in which node-adjacency strays cannot exist.
> **Gates:** `check:walk` run to completion, **0 failures**, `check:legacycodes` OK (10 swept docs, 18 phase-2 tokens all classified), `check:anchors` 2,618 anchors / 55 docs / **0 dead** (105 pre-existing stale *hints*, warn-only, none mine). `./api.sh reachability` 416/416 · `broken` 93 isolated cells · `fix-bidirectional` 0 (see §DX-02bf). `play.html` **untouched**. *(Shell hazard worth keeping: in zsh `git show $c:play.html` silently becomes `${c:r}` + `oll2hit-v3.html` — the `:r` parameter modifier. Quote it: `"${c}:play.html"`.)*

> **⟶ PRIOR (2026-08-12) — §DOC-02z ✅ SHIPPED: the program's most faithful content spec, into a world that moved out from under four of its nodes.**
> `lab-report-naval-campaign-layer.md` (2026-05-28), **531 → 258 lines** — 7 nodes, 33 quests, 13 items, 11 NPCs and five reusable arc templates (§HUNT · §PORT · §SPARK · §WHODUNIT · §ALCHEMY) distilled from four tabletop-design transcripts. Ship commit `e339aeb`, same day as the filing date.
> **CENSUS — zero transcription error across ~100 identifiers.** 33/33 quest ids live as UQF-1.0 · 29/29 state flags in `_S_DEFAULTS()` · 13/13 items with **every icon and all five sell values exact** (80/20/40/13/30) · **14/14 skill checks exact in stat, skill NAME and DC** · 7/7 node records exact in `num`, terrain key, label and act against the archive **including the bidirectional compass wiring** (`OW:{ num:139, … E:'LW', W:'MS'`). **All 22 node codes are dead as written and all 22 resolve by `num`+label to a live node** — `OW`→`NWI` 139, `SK`→`MME` 142, `SB`→`GCI` 144, `DF`→`DNF` 143, `LD`→`VAW` 140, `BN`→`BNX` 141, `DA3` kept its own key, plus 15 hosts (`MS`→`SEN` 10, `LS`→`HFT` 109, `AE`→`ATH` 92, `J1`→`WRO` 43, …). **§WALK/§NAV-01 renamed this layer; it deleted nothing.** `J1` is the nicest case: it was `name:'junction'`, junction nodes became CI failure I1/I2, and instead of dying it was **reclassified to `name:"midlands"` with `num:43` and its label intact** — §HUNT-02's whole hook survived a design ban by changing terrain.
> **THE FINDING — 13 of 33 quests (39 %) unreachable, and every gate is green.** `check:noderegs` / `nodeindex` / `dupkeys` / `npcregs` / `legacycodes` all exit 0 on this material. Four hosts are non-primary (`SEN`←`LCY`, `HFT`+`VAW`←`ALF`, `ATH`←`SEA`) and three arcs die at single-writer entry flags inside a blocked node's block. **§DOC-02r reached the §HUNT-01 4/4 and §WHODUNIT-01 4/4 verdicts first, from the prosocial side — this pass reproduces both independently from the naval side and adds four results arc-shaped review could not see: §ALCHEMY-01 loses 5 of 7** (`roenAtSea`'s sole writer is `if (node.code === 'SEN' && S_story.roenMidlandsWisdom && !S_story.roenAtSea) {@33380`, so Roen never reaches the oracle, the Malta crisis, the Alchemist or the Loch Gold Flake — *the one arc built to prove the grandmother spoke literally never gets to prove it*); **the §BOARD-01-FU6 diamond loses its APEX** (`quest_hunt_01` activates and can never complete, so `{ kind:'unlock', quests:['sq_2','quest_hunt2_01'] }@12902` never fires — both arms survive, the sentence joining them does not); **cell `10,191` holds 6 nodes from 2 unrelated tracks and strands 5** (the lake sub-map + two Völsunga halls, `ODD`/`SIG`); and **`SEN` is a `sleep:true` Warrant's Board host that no player can stand on**, carrying 8 quests, a battle, loot, an NPC — two of those quests being `quest_spark_03`/`_04`, so §SPARK-01 is a casualty too. → **§AUDIT-03x / §DX-02w extended.**
> **THE METHOD RESULT — run the reachability closure in the POSITIVE direction before writing anything off.** Two surfaces rescue part of the layer. `function _bountyPostable(q, node)@37149` requires only `if (!NODE_MAP[q.activateNode]) return false;@37154` — **existence, never primacy** — so a board-postable quest completing on a *primary* node is fully playable; that is what keeps **§SPARK-01 SEA (3 quests)** alive (`quest_sea_01`, gate `{}`, `xp:100` under the Unknown tier's 250 cap, `completion:{ atNode:'NWI' }`). And `// ── QUESTS section (active quests at this node) ────@35545` is followed by `.filter(([, s]) => s === 'active')@35548` **with no node term**, so a Ceremonia roll card renders for any active quest anywhere — the only reason a board-accepted skill-check bounty is completable at all. **Final tally: 17 live by walking · 3 board-only · 13 dead.** → **§DX-02ah** (🟡 — ratify it, or scope the roll to the node; sequence after §AUDIT-03x, because scoping it today kills every stranded quest with it).
> **ALSO FILED — §DX-02ai** (🟢): two beats pay twice and announce once. `quest_sk_hull` grants `S_story.xp = (S_story.xp||0) + 200;@33928` inline, toasts *"+200 XP"*, then its own `storyRender` completes the quest and `onComplete` pays `{ kind:'reward', xp:200 }` again — **400 paid, 200 announced**; `quest_spark2_05`'s button pays +400 XP/+400 gold and the quest then pays `xpAward:600` through `if (q.type === 'side' && q.xpAward) { S_story.xp += q.xpAward; _checkLevelUp(); }@30202` — **1,000 paid, 400 announced**. The other nine button/quest pairs in the layer are clean, so it is two sites, not the arc's house style; same class as `3338def`'s four, missed because that sweep was scoped to the harbour and §LXX stacks.
> **RECOMMENDATION REGISTER: 0 of 5 ⚙️ deferrals shipped in 76 days** — boarding combat, `travelCompanion` (**0 occurrences**), charter-a-ship, investigation-lowers-DC, spare-the-monster. All five needed a *new system* rather than new data and the report said so; the triage held. **Two specified `storyMove` gates (LN→LD on `lakeLairLocated`, HL→DF on `defeatedBattles['HL']`) are now FORBIDDEN by invariant #1** — and both intents survived the ban as *mission* gates, which is the unusual outcome for that class. **One figure marked UNVERIFIABLE, not wrong:** *"~151 live"* quests has no reproducible referent (402 top-level `QUEST_DB` entries at `e339aeb`, and `wbapi-core` cannot parse the archive — the `◆◆◆ WORLDBUILDER:` anchors post-date it).
> **THE SENTENCE THAT IS ABOUT THE PROGRAM:** *the lab-report loop verifies that what was specced got written, and it has no instrument for whether what was written can be reached.* This is the cleanest demonstration of both halves at once — a perfect spec, perfectly implemented, 39 % unplayable. **And the repair is four `NODE_MAP` declarations**, not a line of content.
> **Gates:** `check:anchors` **1536 anchors / 34 docs / 0 dead** (the §DOC-02f hazard fired once on my own draft — an ellipsis inside an anchor — and was shortened to the verbatim fragment before commit); noderegs / nodeindex / dupkeys / npcregs / legacycodes all exit 0, run individually. Docs-only increment; no HTML edit, no §7½. **Note for the next session: `play.html` carries a pre-existing uncommitted user recolor (HP bar gradient + playfield zones, 9 lines) that is NOT mine and was left untouched.**
> **NEXT:** §DOC-02aa — re-derive the oldest report from `stat` (expected `lab-report-ng-plus-remembrance.md`; **20 left** in the `2026-06-09 11:06:54` block, which is 68 distinct mtimes over 107 files). **Ordering caveat found this pass, worth carrying:** two files dated 2026-06-09 are **not** in that block — `mega-reweave` (15:41:00) and `map-audit-layout-tooling` (17:12:46) — so *"22 files dated 2026-06-09"* and *"20 files left in the block"* are both true and only the second one orders the queue. **Resolve ties on the full mtime, never on the date.**
>

> **⟶ (2026-08-11) — §DOC-02f ✅ SHIPPED `ffe7720`: the accuracy of a design document is not uniform across it — the transcribed half had a zero error rate and the narrated half carried every delta, in one file by one author on one day.** On "continue", took `lab-report-circuit-map-theory.md` (696 lines, 2026-05-21, Layer 9 — Time-Warp Footpaths & Circuit Corridors) by the §DOC-02 ordering rule, re-derived from `stat`: oldest remaining mtime, alphabetically first in the shrinking 2026-06-09 block, and §DOC-02c's Layer-9 sibling as the row predicted. Rewrote it to **313 lines**, IEEE-shaped, 12-row delta table. **Existing-Work-First:** the uncommitted user recolor (HP-bar gradient / light-green playfield, self-labelled *"user recolor 2026-08-06"*) is still in the tree — **left untouched and unstaged**; this increment is docs-only. **Census: 24 of 50 named symbols resolve (48%)** — between §DOC-02c's 42% and §DOC-02e's 82% — and **0 of 26 node codes** resolve to the node they name, with `CI` the §AUDIT-03m worse-than-dead class again (live at `num:429`, "Chancery Court — The Officer's Pen", `act:NaN`). The corridor layer was deleted in three June passes (**§CELL-05** `6dea804`, **§CELL-11A** `85cc43e`, **§CELL-14** `30f18b4`) and superseded by §WALK/§NAV-01's 90×360 geo grid; Warp is now a **banned design** (invariant #3), `junction:true` a **CI failure**, and `storyMove`/`node.portal` both gone. Only one runtime fragment survives — `_setActivePath`@38195, amputated from three writes to two, still feeding the gold `mc-exit-active` exit arrow, which is the last item of the report's own §IX diagram. **The headline is not the removal, it is the internal gradient.** §IV–§V (data architecture + mesh construction) are **verbatim transcriptions with no measured error**: `WIRE_GLYPH` byte-identical in all eleven entries, `_routeSegments()` byte-identical, the seven junction coordinates exact including comment structure, and both `NODE_MAP` samples exact in *every* content field — the report's `CI` is HEAD's `LHR` (`num:1`, `name:'city'`, "City Streets — Birka", `npc:'City Guard Captain'`, `loot:'Bloodstained Map'`), the node survived and only the key was renamed. §VII (execution traces) is **narration**, and every trace-local delta lives there: `_corridorOnComplete`, `corridor-quest-count` and `corridor-pct` return **0 commits from `git log -S` across the entire repository history**; the shipped ids carry a `-name` suffix the report drops on all three; the `pendingBattle` shape is wrong in **3 of 4 fields** (`isCorridor` and the `'_corridor'` sentinel also 0 commits); and the encounter formula is missing a whole term — shipped `min(0.95, 0.1 + notoriety*0.015 + activeQuestCount*0.04)` vs the report's `min(0.9, 0.1 + activeQuestCount*0.05)`, so all five rows of its probability table are wrong. **This refines §DOC-02b's *"the half that points at code is the half that is right"* rather than confirming it: §VII points at code too — functions, DOM ids, line numbers — and it is the wrong half. The real predictor is whether the author could COPY the passage.** A table or a function body can be pasted; a ten-step call sequence must be reconstructed, and memory supplies plausible names the file never contained. **Sharpest single measurement: the Hunt/Warp dialog was built, styled, and never once shown.** At `32c10c5` the overlay, card and all three buttons exist with the exact labels the report quotes down to the emoji; every JS reference is two bulk close-all arrays and one `classList.remove('visible')` — **nothing ever adds `visible`**, no handler is bound, no field is written. The real `storyCorridorTravel` branches on the persistent `S_story.huntMode` toggle and emits a `storyMsg` either way, so there was never a per-leg choice: **the report's central thesis, *"the choice is the mechanic"*, is the one thing the layer never implemented.** Same verdict §TIMELESS-01 later recorded for the Stalk modal (*"already never shown — legacy/dead"*), reached independently, in the sibling report, about a different screen. **Corpus cross-check corrected a shipped §DOC-02 output for the first time.** The two Layer-9 reports disagree on `CI`, and the archive settles it in *this* report's favour: "The Thieves' Den" was never a node label but `HUNTING_GROUNDS['city'].displayName` — a table from §DOC-02c's own thesis — so that report's Appendix A conflated a terrain display name with a node label (`AL`/`BQ` are likewise reworded rather than transcribed). Also softened **§DOC-02c delta 7**: `targetTerrain`/`targetKeys` never existed as fields (correct), but the coupling **shipped derived** — `_getQuestTargetKeys()` walks active quests to `NODE_MAP[q.activateNode].battle.key` and `_stalkedMonsterPick` applies `BOOST = 6`, so Principle 3 did run. *A missing field is not a missing feature.* **New standing method, now accelerator six: HEAD cannot adjudicate a claim about the past — read the archive.** Both corrections came from `git show 32c10c5:play.html`, not HEAD; `git log -S` returning zero proves non-existence, absence at HEAD proves nothing about 2026-05-21. **Filed §AUDIT-03x** — traced from the report's own §VIII Step-2 rule (*"no two nodes should share the same (r,c)"*), which §WALK-1.5 deliberately retired in favour of **locales**: **416 nodes occupy 244 cells, so 172 are non-primary** (worst cell holds 17), `S_story.currentCode` is assigned at exactly **two** sites and both yield the primary, `cellCodes()` is read only by the MUD kernel, and `_uqfActivateAtNode` keys on `node.code` — so **1,260 quests target a non-primary node, 486 already held by the §AUDIT-03e guard, leaving 774 across 135 nodes that cannot activate** (`BK` 89, sharing a cell with the starting node `LHR`; `WM` 312 behind `ERF`). The waypoint code already compensates by cell and tells the player they arrived at a node the engine then declines to render. Design call; overlaps §AUDIT-03d and probably sequences before it. Also filed a sixth **§DX-02n** member (`_setActivePath`'s dead `toCode` parameter — a dead *parameter*, invisible to a top-level scan, widening `check:deadconsts` again). **Gates: 16/16 green, but only after a repair this pass had to make first.** `check:walk` hit the known harness detach artifact again (stalled after `check:behaviour` with its ✓ printed), so per the standing rule the remaining gates were re-run individually — and `check:anchors` came back **red with 7 dead anchors, 5 of them left by §DOC-02e**, whose ship record says 16/16 green. None of the six symbols had actually moved: every line existed, unchanged, at its cached number. **An anchor whose text is not a *literal* substring of its line is DEAD even when the line is alive**, and the three ways to mint one are markdown table pipe-escaping (`\|\|` for `||`), an ellipsis standing in for elided code, and whitespace normalisation (`const d20  =` has two spaces). All 7 shortened to verbatim fragments; `check:anchors` now **348 anchors across 14 docs, 0 dead**. *Two lessons recorded in the §DOC-02g accelerators: shorten an anchor, never prettify it; and when the detach artifact fires, the gates that did not run are not green.* Docs-only; no HTML edit; the 935/4 (940) Playwright baseline is untouched. **Stated scope limit:** liveness and provenance only — the 24 surviving symbols were not audited for *behavioural* drift. **Next: §DOC-02g — `lab-report-corelli-merchant.md`, then the rest of the 2026-06-09 block alphabetically.**

> **⟶ PRIOR (2026-07-31) — §AUDIT-03j ✅ SHIPPED `c5f9079`: the engine's own node references resolve, and gate #13 keeps them that way. The row named 9 dead keys in 3 registries; there were 42 in 5, and the two biggest were registries it never mentioned.** On "continue" with §AUDIT-03d's design question still unanswered, took the row the last board named first among the no-design-call alternatives. Existing-Work-First: tree clean at `4c79868`, server stopped, a 2nd claude process present but the game file untouched. **Every one of the 42 was written in the retired 26×16 codes, and every one failed silently — a missed lookup renders nothing, so there is no error to notice.** `NODE_NPC_KEYS` **21 of 26** rows dead → favor map-tinting and farewells answered at **LHR alone**; `NPC_FAREWELLS` **all 12** route keys dead → only each NPC's `default` line could fire; `JUNCTION_VIGNETTES` 7 of 8 (keyed to the `J2`–`J7`+`RD` stubs §WALK deleted); `CORELLI_APPEARANCES` stop 2 of 5; `NPC_DIALOGUE`'s `MS_SPARK`. **33 were mechanically derivable and doubly corroborated** (§AUDIT-03l's LEGACY CODE MAP, plus each EB row encoding its own legacy code in its value — `EH:'eh_npc'`). The other **9 were content-visible placements → asked, all three recommendations accepted**, then executed. **Two discoveries reshaped the fix:** (1) **the table's other column was dead too** — its npc keys were the profiles' SURNAMES (`couperin`=Bard Tomas Couperin=`quill`, `weckmann`=Pit Master Weckmann=`crov`, `bruhns`=Cmdr Seraphine Bruhns=`auros`), proved independently by the corpus holding exactly **13 favor-granting keys**, which include the profile keys and never the surnames — a code-only remap would have shipped the *half-true* artifact §AUDIT-03l warns about; (2) **the farewell mechanism is alive** — `currentCode` advances only on node arrival, so a route key is *last node → next node reached* at any distance, never the cell adjacency the geo world stopped providing. **Shipped:** both columns of `NODE_NPC_KEYS` (20 `e?_npc` placeholders removed as a **proven no-op** — always favor 0, and EB nodes tint via the live `ebReturnDone` branch); 12 routes + 3 owners live with one **content override** (*"Long road to Tilbury"* → `LCY`, not SQ=NUE, per §AUDIT-03c's prose-is-authority rule); the 7 vignettes rehomed by each scene's OWN text (coastline→`J13`, the child's toy→`BNX` = the very bend Tessie's surviving line warns about, *"Paid in full. —S."*→`BLW` a toll gate); Corelli's stop 2 → `WRO`; `MS_SPARK` folded into the SEN slot behind `_senHoldSpeaker()`. **One dependent bug fixed:** `storyShowNpc` read only `d.quote`, so **41** `quoteFn` entries printed the literal string *"undefined"* in the d-pad NPC modal. **The gate (`check:noderegs`, #13) is built around the blind spot that hid this:** a percentage heuristic cannot see a table that is *entirely* dead — exactly what `NODE_NPC_KEYS` (5/26) and `JUNCTION_VIGNETTES` (1/8) had become — so classification is **explicit** and an unlisted codeish table FAILS. **Evidence:** `check:walk` **13/13 exit 0**; 4-phase `--selftest`; **negative control with the real historical defects** (replanted `LJU` + `nodeCode:'LDE'` → exit 1 naming both, file restored byte-identical); `audit03j-node-registries.test.js` **8/8**; **352 green** across the quest/NPC/audit suites; full Playwright **725 passed / 5 failed** = the documented four + the known `multiplayer-presence` flake (**7/7 alone**; I ran `--retries=0`). **§7½ DONE — player-visible:** screenshots of Old Faeron at DAR, Corelli at WRO, Brannick in the modal that used to say *undefined*; 0 page errors. Docs: `story.md` (8-row derivation table), `world.md`, `prompt.md` §7 (12→13), `index.md`. **Side finding filed, not swept: §AUDIT-03n** — the same rot in the npc-key dimension: the same three surname keys still key 12 entries in four more registries, and because the epilogue builder reads `npcFavorability[key]` for them, **the ending renders the stranger variant for Quill, Weckmann and Bruhns no matter how close you got**. **Next: §AUDIT-03d (still the gating row — needs the staging design call; it releases 983 quests), else §AUDIT-03n (measured, and the ending is visibly wrong), §DX-02g (2 field PUTs), §DX-01e (line-anchor drift — the last open §DX-01 row), §AUDIT-03i/k/m, or §VM-01-G2b / the G4 child report.**
>

> **⟶ PRIOR (2026-07-29) — §AUDIT-03l ✅ SHIPPED: the node-code reference is generated now, and the 26×16-era tables are quarantined instead of quietly wrong.** On "continue" took the row §AUDIT-03c had just filed (§AUDIT-03d is still the gating row and still needs its staging decision). Existing-Work-First: tree clean at `d42a707`, server stopped, a 2nd claude process present but the game file untouched by it; the row's premise re-measured live before any edit. **The premise held; the row's own recipe did not survive that measurement.** The legend is **81 of 92** rows dead, not 73/76 (the first regex had skipped annotated rows like `| BK | 25 | bar (Visby) |`), **plus 11 naming nodes the world removed** (`RD`, `J2`–`J7`, `LJ0`–`LJ3` — §WALK junction stubs). And the recipe — *"look each `Node #` up and rewrite the code column"* — would have shipped a **half-true** table: the `Grid Cell` column is the retired **26×16** projection while live `NODE_COORDS` runs **r 2–73 / c 154–249**, so a code-only remap leaves an authoritative-looking doc whose coordinates are fiction. `num` is also **not** a sufficient key on its own — **num 77 is held by two nodes** (`MJF`/`CDG`) and terrain keys were renamed (`epic_goblin`→`epic_goblin_cave`, `desert_caravan`→`arabia`), so every match needs terrain/label corroboration. **So: generate + quarantine, not remap.** New `src/scripts/node-index.js` / `npm run nodes` emits `docs/maps/node-index.md` from the same `wbapi-core` parse the `:1367` server reads (the `npm run stats` §DX-01g pattern) — all **416** nodes (code · `Node #` · terrain · act · **live cell** · 🛏 · label · inline NPC) plus a **LEGACY CODE MAP** (**83 recovered · 11 unresolved**) for reading older docs. It auto-flags the nastiest class — **a legacy code that IS a live key but means a different node**: **`CI`** (Birka's streets then; the *Chancery Court* now) and **`BK`** (the *Broken Tooth Tavern* = `VBY` then; *Birka Shore* now). A naive "does this code resolve?" check passes on both, which is exactly why the trap survived. **`check:walk` gained gate #12, `check:nodeindex`**: the committed index must be byte-identical to a fresh generation. `maps.md` got a READ-THIS-FIRST routing table at the top and **all six 26×16-era sections kept verbatim, re-headed ⚠️ HISTORICAL** (its SLEEP NODES list held 8 of the live **38** checkpoints). `index.md`'s Town Cross-Reference remapped, with two corrections beyond the rename — the **Inn** column named `SF`/`IS`, *neither a `sleep:true` node* (really `SEN`/`NAS`), and Visby's `BK` was `VBY`. **Evidence:** `check:walk` **12/12 exit 0**; new `audit03l-node-index.test.js` **5/5**; **two negative controls** — a bogus row appended to the committed index, and a real node inserted into `NODE_MAP` — each drove gate #12 to exit 1, with the game file restored byte-identical (`git diff` empty). **No §7½ — no game code or data changed** (the one HTML touch was the negative control, reverted). Docs: `prompt.md` §4 authoring rule + §7 (11→12 gates) + §9, `index.md` doc map + Town table + `VENDOR_NODES`, `maps.md` restructure. **Follow-up filed, not swept: §AUDIT-03m** (the legacy codes still in running prose — deliberately left, because a blind token replace mangles `AT`/`IS`/`SE`/`CA` inside English sentences and the `Q-FR-01`-style design IDs must NOT be remapped). **Next: §AUDIT-03d (still the gating row — needs the staging design call), else §AUDIT-03h/i/j/k/m, §DX-01c/d/e, §DX-02b/c/d, or §VM-01-G2b / the G4 child report.**
>

> **⟶ PRIOR (2026-07-29) — §AUDIT-03c ✅ SHIPPED: the `710bb75` remap audit closes — 6 of 8 remaps were right, and the *table that caused them* is the real finding.** On "continue" took the row that needed no design call (§AUDIT-03d is still the gating row and still needs its staging decision). Existing-Work-First: tree clean at `b2fe418`, WBAPI server stopped, a 2nd claude process present but the game file untouched by it. **The verdict, quest by quest (§NPC-01-SF4 method — each quest's own prose + `waypointNode` + the hook/panel that renders its buttons):** `CQ` = **`CDG`** (The Cat Quarter) and `FR` = **`AMS`** (Fishmonger's Row) are both **right**, corroborated three ways each — the nodes' own labels/inline NPCs, and the engine's own comments (`_nodeHookLaRivaRow` says *"FR node render"* and guards on `node.code === 'AMS'`; the drop handler says *"FR kill tracking"* on `pb.nodeCode === 'AMS'`; the boss panel says *"CQ node render"* on `node.code === 'CDG'`). Only `SF → LCY` was wrong, already fixed by §VM-01-G3 (`SF` = the Storefront = `STN`). **The prior session's open question — "should one arc sit at two nodes?" — is answered and moot:** the La Riva arc is legitimately **bi-nodal** (Kenickie sends you east from `CDG`; Connie and Aldo are at `AMS`; the account book goes back to `CDG`), and **5 of the 8 remapped lines no longer exist** — they were appended duplicates that §AUDIT-03a / §VM-01-G3 removed, so all three `la_riva` quests are `activateNode:null` and staged by code. **Corpus scan (the audit's real product): 0 of 2,853 quests name a non-existent node** — and it checked **`waypointNode` too, a field the original audit never looked at**. **One data fix:** `quest_cat_06` was anchored to `jimmy_two-tails` (CDG's inline-name slug) while its three siblings use the profile key `jimmy` — one character, two `_questsByNpc` headings (§AUDIT-03k); collapsed to `jimmy`. **The load-bearing finding, and the most likely source of the whole `710bb75` class: `maps.md`'s LEGEND table — the doc an author reads to find a node code — is written in a pre-airport-code era, and 73 of its 76 rows name a code that does not exist** (`SF`→`STN`, `CI`→`LHR` *(which exists, but as the Chancery Court, num 429)*, `CY`→`HKG`, `AT`→`RAI`, …). Its `Node #` column is still correct and is the recovery key. `quest_tl_01` even carries `activateNode:"SF"` beside a live `waypointNode:'STN'` for the same node — one field read off the table, one off `NODE_MAP`. Filed as **§AUDIT-03l** (measured, with the recovery method) and neutralized today with a hazard banner above the table; the 73-row remap is its own increment. **Evidence:** golden parse diff across all 7 collections = **exactly 1 entry diff, `quest_cat_06` `fields=npc` alone**, 0 lost / 0 gained, 2,853 quests unchanged; new permanent `src/tests/integration/audit03c-node-refs.test.js` **4/4** (both fields resolve · the 3 dead codes stay dead + the node labels that identify them · the 8 audited quests hold their re-derived targets · cat_06 on the profile key) with a **negative control** — re-planting `activateNode:"SF"` + `waypointNode:"CQ"` in memory, both caught; `check:walk` **11/11 exit 0**; **367 Playwright green, exit 0 both runs** (`quest-runtime-uqf` + panels/hooks/activation **322/322**, and the audit/NPC set **45/45** — `audit03c` 4 · `audit03b` 4 · `audit03g` 4 · `audit03e` 7 · `npc-card-map` 22 · `uqf-activatenode-index` 4). **No §7½ pass — `quest.npc` has zero quest-level reads in the game client (§AUDIT-03b) and no node reference changed, so nothing is player-visible.** Docs: `quest.md` (both arc sections gained a node-code + audit-verdict note; the `npc` section gained the **six measured alias pairs** and the prefer-the-profile-key rule), `world.md` §XIX (the `SF` prose fixed to `STN` + the audit verdict), `maps.md` (the ⚠️ banner). **Next: §AUDIT-03d (still the gating row — needs the staging design call), else §AUDIT-03h/k/l, §DX-01c/d/e, §DX-02b/c/d, or §VM-01-G2b / the G4 child report.**
>

> **⟶ PRIOR (2026-07-29) — §DX-02e ✅ SHIPPED: the §KG corridor road was never broken — the test had pinned generated coordinates. One of the two carried Playwright reds is retired.** On "continue" took the row that needed no design call (§AUDIT-03d is still the gating row and still needs its staging decision). Existing-Work-First: tree clean at `8fc9a2e`, WBAPI server stopped, a 2nd claude process present but the game file untouched by it; **red reproduced first** (1 failed / 4 passed, `10,205 is a road cell`). **The row framed it as a coin-flip — re-lay the corridor or update the samples — and the measurement settled it:** auto-travel routes with `_roadGridPathCore` (§NAV-01d road-weighted Dijkstra, road/sea-lane cost 1, everything else 2), and all six legs `TLL→SPB→KMS→ZVD→FBR→TVR→SVO` walk **entirely on road today** — `TLL(10,204)→9,204→8,204…8,210→9,210→SPB(10,210)` (the §DX-01a reroute through TUO, off the Gulf-of-Finland sea cell `10,207`) then the SE staircase `10,211 · 11,212 · 11,213 · 12,214 · 12,215 · 13,216 · 13,217`. **Zero off-road intermediate cells on any leg**, so the row's worry — *"a player currently rolls encounters where the lab report says they shouldn't"* — was **false**. Re-laying the old row-10/11 run was rejected on a second ground as well: `ROAD_RUNS` is **generated** (`src/scripts/build-roads.js — do not hand-edit`), so hand-restored cells die at the next `--apply`. **Shipped:** the assertion rewritten to call the engine's own router per leg and require every intermediate cell to be `ROAD_CELLS` or a settlement cell, printing the full route on failure. **Evidence:** kg-zones **5/5** · `navigation` + `minimap-nav` **36/36** · `audit03e-node-code` **7/7** · `check:walk` **11/11 exit 0**; **negative control** (temp test, deleted) — deleting `11,212`/`11,213` from the live `ROAD_CELLS` made the router detour `12,211 · 12,212` and the assertion caught both, so it is not vacuous. **No HTML touched, no §7½** (test + docs increment; the player-facing claim was verified through the real router in a live page load). Docs: the §KG lab report §2 gained a dated **route-drift note** (the §DX-02c template — its cell ranges are the build-time lay, not the live net, and `src/scripts/render-region.js 8 16 195 220` shows the current one); `prompt.md` §7 now carries **ONE** Playwright red (`worldbuilder-crud-arrays`, §DX-02d) instead of two, plus the general lesson: **never pin generated data by coordinate — pin the property it was generated to satisfy.** **Next: §AUDIT-03d (still the gating row — needs the staging design call), else §AUDIT-03c/h/k, §DX-01c/d/e, §DX-02b/c/d, or §VM-01-G2b / the G4 child report.**
>

> **⟶ PRIOR (2026-07-29) — §AUDIT-03e ✅ SHIPPED: the `code = key` backfill — 287 nodes stop sharing ONE `undefined` state slot, and five §KG NPCs speak for the first time.** On "continue" took the row the prior session had measured. Existing-Work-First: tree clean at `2da7fbb`, server stopped, a 2nd claude session live but the game file untouched by it; re-proved the premise live (287/416 code-less, 0 entries with `code !== key`, 2,285 quests naming one). **The row's premise was too narrow — activation was 1 of 10 consumers.** Proved against HEAD before editing: a second code-less node paid **0 exploration XP** (`S_story.visited` held exactly one key, `"undefined"`), all five other code-less battles read **already defeated** after one win, all five §KG `NPC_DIALOGUE` lookups failed, and the Rest card read a constant wrong *"The First Inn (TLL) — 166 moves"* at all 287 (`_bfsGridPath(undefined,…)`). **Shipped:** one normalisation line after `NODE_MAP:END` (the data section keeps its 129 authored `code:` fields — not 287 new ones) + the `NODE_CODE_BACKFILLED` set captured just before it. Safety audit made it a one-liner: no `code !== key` anywhere; of 50 distinct `node.code === 'LITERAL'` comparisons exactly **one** hits a code-less node (BK — reviving its §D01-01 approach panel, its dead vendor, and 1 of Corelli's 5 stops); the parity fences read node **keys**, not `NODE_MAP.code`, so they never moved. **The one real judgment call:** 990 of the newly-reachable quests carry a vacuous `gate:{}` — WM alone would fire **151** on arrival, act4/act5 ahead of act1 (live worst today is NUE at 31). Rather than default §AUDIT-03d's design call, the seam holds on a structural line — *vacuous gate **and** no `completion:` clause* — so **5** quests newly fire across all 287 nodes (the §KG + §MATH-01 chain heads + `mq_4`, all authored, all previously dead), worst node 1, while 1,295 stage properly behind their own gates. **Evidence:** 12-combo pre/post golden harness (temp, deleted) — 4 coded controls byte-identical, quest counts **0→0 at all 12**, every diff a genuine fix; `check:walk` **11/11 exit 0**; **~450 Playwright green** (`quest-runtime-uqf` 336/336, new `audit03e-node-code.test.js` 7/7); §7½ screenshot of Roshkova speaking at KMS, 0 page errors. Two tests updated, neither weakened (`npc-card-map` had asserted the defect itself as §NPC-01-B's premise). Docs: maps.md §NODE_MAP, quest.md (new activation/seam section), world.md §KG, prompt.md §7. Side findings filed as **§AUDIT-03e-FU** (ending-map predicate), **§AUDIT-03j** (9 registry keys naming non-existent nodes — incl. Corelli's LDE), **§DX-02e** (a pre-existing kg-zones road red, proved at HEAD). **Next: §AUDIT-03d is now the gating row — its staging design releases the 983 held quests; else §AUDIT-03c/g/h, §DX-01c/d/e, §DX-02b/c/e, or §VM-01-G2b / the G4 child report.**
>

---

> **Archive:** closed / completed work is archived in [plan-archive.md](plan-archive.md); the *how-we-work* rules live in [CONTRIBUTING.md](../../CONTRIBUTING.md).

---

*© 2026 Paul Richeson — MIT License.*
