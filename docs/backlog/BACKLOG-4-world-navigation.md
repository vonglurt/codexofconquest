<!-- SPDX-License-Identifier: MIT — Copyright (c) 2026 Paul Richeson -->
# BACKLOG — Phase 4: World & Navigation — nodes, terrain, geography, movement

> **Phase 4 of 6.** Split out of `BACKLOG.md` on 2026-08-23; `BACKLOG.md` is now the routing index and holds the chronological §RESUME map across all six files.
>
> **Scope of this file.** The map and moving on it: node registration and labels, terrain tables and encounter rates, GEO anchors and projection bounds, road nets, movement results.
>
> **Siblings:** [1. Playable Truth](BACKLOG-1-playable-truth.md) · [2. Engine & Systems](BACKLOG-2-engine-systems.md) · [3. Content & Narrative](BACKLOG-3-content-narrative.md) · **4. World & Navigation** *(this file)* · [5. Platform & Tooling](BACKLOG-5-platform-tooling.md) · [6. Verification & Documentation](BACKLOG-6-verification-docs.md)

## The Backlog Directive — restated in full

> **This file is the list of outstanding activities for Phase 4 (World & Navigation).** The *how-we-work* rules live in **[CONTRIBUTING.md](../../CONTRIBUTING.md)**; closed/shipped work is archived in **[plan-archive.md](plan-archive.md)**. Split out of the former `plan.md` on 2026-07-09.
>
> **⚠️ Before starting ANY row — Existing-Work-First (CONTRIBUTING.md § Existing-Work-First Policy):** `git status` + `git log` first (uncommitted/fresh work matching the row = it's done or in progress — finish it, don't redo it); check for a second live session (`ps aux | grep claude`) before editing shared files; grep to *disprove* the row's premise (rows go stale — four rows have closed as ALREADY SHIPPED). A §RESUME entry says where the last session *stopped*, not what the tree looks like *now*.
>
> **Legend:** `[ ]` open · `[~]` in-progress / recurring · `[x]` closed (kept here for context; migrate to plan-archive.md over time).

> **⚠️ This file is one of six.** A row's neighbours may live in a sibling phase file, and the Existing-Work-First grep MUST cover all six — `grep -n '§DX-02xx' BACKLOG*.md` — before you conclude a row is unclaimed. The §RESUME entry that closed a row may sit in a different phase file than the row itself.

## Standing Directive — Code Comments (user directive 2026-08-23)

> **Binding on every increment in this file.** Where this and a §-row's own instructions disagree, this wins.

**The failure this prevents:** writing a comment that narrates the change being made right now. That context is real, but it expires the instant the change merges — the defect it describes no longer exists, so the comment becomes a story about a problem no future reader can observe. It is a changelog entry in the wrong file, and a third copy of text already required in the commit body and the PR description.

- **CC-1 (MUST NOT)** Write a comment describing a change, a fix, a defect, its cause, or what the code used to do. No *"was / now / previously / instead of"*, no *"this fixes"*, no *"needed because otherwise"*, no *"note that we no longer"*.
- **CC-2 (MUST)** Apply the **survival test** to every comment before writing it: *would this still be true and useful to someone reading this file a year from now, who never saw the diff?* If it only makes sense beside the diff, it is changelog — delete it and put it in the commit body.
- **CC-3 (MUST)** Default to **zero** comments. Declarative config — Terraform, DNS records, k8s manifests, CI YAML, Helm values — is self-describing and takes none. A resource named `dmarc-example-com` does not need a comment saying it is the DMARC record.
- **CC-4 (MAY)** Comment only when a future editor would actively break something without it: a non-obvious external constraint, a required out-of-band manual step, an invariant the surrounding code cannot show. **One line.** If it needs a paragraph it belongs in a design doc, not inline.
- **CC-5 (MUST)** Before every commit, re-read the comment lines added: `git diff --cached | grep '^+' | grep -E '#|//|/\*'`. Each hit must pass CC-2 on its own. **Deleting is always an acceptable outcome.** *"I already wrote it"*, *"it is only one line"*, and *"this one is genuinely useful"* are not exemptions — the last one is the exact thought that precedes every violation.
- **CC-6 (MUST)** Applies to comments **edited** as well as added. When a change invalidates an existing comment, the default action is **DELETE**, not rewrite it into a new narrative.

> **⚠️ KNOWN TENSION, flagged not resolved — needs a user call.** This repo currently runs an *opposite* convention in `play.html`: **§AUDIT-03n / §DX-02c "annotate, don't rewrite"**, which deliberately preserves defect history inline (e.g. `const NODE_NPC_KEYS`'s 8-line comment explaining that 21 of 26 rows named retired node codes, or `_renderFinalMap`'s §AUDIT-03e note on why the node filter is held at the historical set). Those are exactly the *"what the code used to do"* narratives CC-1 forbids, and several are load-bearing: they are the only record of why a value is held where it is, and deleting them would re-open closed audit rows. **Until the call is made, the working rule is:** CC-1..CC-6 bind all **new** comments; existing §AUDIT-/§DX-annotated blocks are **not** swept under CC-6 — they are a documented archaeology convention, not accidental changelog. Filed as **§DX-02fa** below.

---

## Open at a Glance — Phase 4 tracks

- **§NAV-01-FU deferred** — map "jump to region" centering (needs pannable-view design; out of scope as a low-risk follow-up).

---
## §BACKLOG — Open Items (Phase 4)



### §DX-02ek — the beginner grind band paces exactly like open wilderness, because none of its five terrains has an encounter rate (NEW 2026-08-22 during §DOC-02cw, 🟡 five table entries, ONE DESIGN CALL)

- [ ] **§DX-02ek — the §KG corridor exists to be ground, and it rolls at the generic default.** 🟡 **the numbers are trivial; the call is what they should be.** None of `soviet_checkpoint`, `komsomol_school`, `gladiator_zavod`, `skill_fabrika`, `soviet_transit` appears in `const TERRAIN_ENCOUNTER_RATE = {@9905`. Both consumers resolve them through the `?? TERRAIN_ENCOUNTER_RATE._default` guard — `let baseRate = _partyEncounterRate(TERRAIN_ENCOUNTER_RATE[terrain]@28591` and the server-side twin — so all five run at **0.15**, byte-identical to `midlands`, doubled to 0.30 under Hunt Mode.
> **Why it matters, measured.** The whole huntable surface of the band is **nine cells** (SPB `10,209`/`11,210` · KMS `12,211` · ZVD `12,212`/`13,213` · FBR `13,214`/`14,215` · TVR `12,216`/`14,216`); everything else in rows 8–16 / cols 203–220 is road (rate 0), a settlement, or sea. Level 6 costs 5,500 XP and the band's mean kill is 177.5, so a player needs ~31 average kills — about **200 steps of deliberate oscillation** between a zone node and the one cell beside it. `city` is tuned to 0.05 and `forest` to 0.25; the training band is the one place in the game where encounter frequency is load-bearing, and it is the one place that was never tuned.
> **THE CALL — what should a sanctioned-bout zone roll?** (a) **Raise the band (recommended):** give all five ~0.25–0.30, so the ramp is genuinely faster than wilderness and Hunt Mode's ×2 caps near 0.6. Fits the fiction — you are at a *training ground*, opponents are queued up. (b) **Split them:** `gladiator_zavod`/`komsomol_school` high (the sparring floors), `soviet_checkpoint`/`soviet_transit` low (they are hubs, not pits) — more faithful to §4's west→east ramp, five judgements instead of one. (c) **Declare the default correct** and close the row, recording in the lab report that the band is deliberately wilderness-paced. **(a) is the honest default** — the report never specified a rate, so nothing is being overridden, and the band's stated purpose is grinding.
> **Verify:** extend `src/tests/integration/kg-zones.test.js` with a sixth case asserting every §KG terrain key has an OWN entry in `TERRAIN_ENCOUNTER_RATE` (not a `_default` fallthrough) — the general assertion catches the next zone author too. Then re-run `npm run check:terrain` for the client/server parity fence.
> **Provenance:** §DOC-02cw, finding F6, `docs/lab-reports/lab-report-kg-russia-kindergarten-zones.md` §V/§VI.

### §DX-02el — twenty-six nodes announce themselves twice, and five of them were minted in one increment (NEW 2026-08-22 during §DOC-02cw, 🟢 five string edits, NO DESIGN CALL)

- [ ] **§DX-02el — `Nevsky Checkpoint · Nevsky Checkpoint`.** 🟢 **the header composes `nodeLabel · terrainLabel`; give the terrain a terrain name.** §KG Increment 2 gave each of its five nodes a dedicated terrain whose `label` is the node's own display name — `soviet_checkpoint:{ label:@6295` and its four siblings — so all five stutter in `#s-node-name`. **Measured in Chromium at HEAD**, every exemplar the §KG lab report modelled itself on gets it right: `City Streets — Birka · City Streets` (LHR), `The First Inn · Inn — Night` (TLL), `Station 7 (Dark) · Station 7 — Hidden Waypoint` (SVO). The terrain label is supposed to name the **kind of place**; the node label names **the place**.
> **The class is bigger than §KG.** A whole-corpus sweep (`WORLD_DB[n.name].label === n.label`) returns **26 of 416** nodes: `MAN · KIR · MSY · BGI · PDL · RAI · GIB · TRF · SID · TRD · NAS · FEZ · AHB · BEL · HER · BKK · BOO · SSJ · CDG · SZG · LIM · SPB · KMS · ZVD · FBR · TVR`. **Five of the twenty-six — 19% of the defect class — came from this one increment**, which is why it is filed here.
> **Fix (five strings, then twenty-one more if wanted):** rename the five §KG terrain labels to the kind of place — `soviet_checkpoint` → *Soviet Checkpoint*, `komsomol_school` → *Drill Hall*, `gladiator_zavod` → *Gladiator Pit*, `skill_fabrika` → *Jack-In Fabrika*, `soviet_transit` → *Transit Waystation*. Node labels stay exactly as authored. The other 21 are the same edit and can ride the same increment or a follow-up.
> **Verify:** a gate assertion — no node may satisfy `WORLD_DB[node.name].label === node.label` — added to `src/scripts/check-invariants.js` beside I1/I2, which already walks node terrains. That closes the class permanently instead of the five.
> **Provenance:** §DOC-02cw, finding F7. The §KG lab report's §7 step 5(d) said *"node cards render NPC + prose"*; they do, with the name said twice.

### §DX-02cb — seven live nodes have no lat/lon in any source, and the only thing that notices is a JSON field nobody reads (NEW 2026-08-14 during §DOC-02bn, 🟢 no design call)

- [ ] **§DX-02cb — the geo gazetteer has drifted seven nodes behind `NODE_MAP`, so §WALK-1.5's projection can no longer be re-derived for the world it produced.** 🟢 **seven gazetteer entries + one guard, ~20 minutes.** `POST /api/layout/geo-seed` resolves every node's lat/lon through a fixed priority — `GEO2` (155 anchors, inline in the server) → `gazetteer.realPlaces` → `gazetteer.anchors`, then cycle-safe chaining through `satellites`/`offEarth`. Measured at HEAD: **345 nodes resolve to their own coordinates, 64 chain to an anchor, and 7 resolve to nothing at all** — `DNF` (§CELL-14-FU Dunfall), plus `TGS`, `SPB`, `KMS`, `ZVD`, `FBR`, `TVR` (the §KG Russia corridor). All seven carry live `NODE_COORDS` and are reachable; what they lack is a *derivation*. `` `src/js/wbapi-server.js:if (!ll) { skipped.push(code)@7921` `` pushes each into a `skipped` array that is returned in the dry-run JSON and **never logged, counted against a threshold, or checked by any gate** — the summary line reports `${skipped.length} skipped` as an integer with no names and no failure. **This is not currently a data-loss bug:** the apply path merges (`for (const [code, p] of Object.entries(coords)) WBAPI.nodeCoords[code] = p`), so a regeneration leaves those seven where they are rather than clearing them. The defect is that the gazetteer is documented as the source of truth for the projection and is now **409/416 complete**, silently — so a re-seed reproduces the world *except* in seven places, and nothing tells you which. **Fix:** add the seven to `src/config/walk-geo-gazetteer.json` (six are real Russian/Baltic places with obvious coordinates; `DNF` is a §CELL-14 rename that wants an `anchors` entry with a `why`), and make the coverage explicit — either fail geo-seed when `skipped` is non-empty in apply mode, or add a `npm run` census that prints the names. **Related, not duplicate:** §DX-02bo asks whether `src/tools/worldmap.js`'s *retired 8–500 coordinate space* should be repaired or the verb retired; that is about the arithmetic, this is about the input table. If §DX-02bo is resolved by repair, gazetteer coverage is its acceptance test; if by retirement, this row shrinks to a one-line comment saying the gazetteer is frozen history. **Verify:** a geo-seed dry-run reports `0 skipped`; the dry-run's computed cells still reproduce all 416 live `NODE_COORDS` entries (the §WALK-1.5-FU(a)/(b) consistency check, re-run).

### §AUDIT-03bg — the SP/MP encounter divergence was one line in a lab report; it is now four, and the guard fences the half that never drifted (NEW 2026-08-14 during §DOC-02bm, 🟡 small design call)

- [ ] **§AUDIT-03bg — `check:terrain` proves the two sides agree about the *terrain*, and nothing at all proves they agree about the *encounter*.** 🟡 **doc sweep is 🟢; whether to widen the gate is the design call.** §WALK-5 §4.3 logged exactly one SP/MP divergence — the server draws monsters on flat tier weights because a MUD session tracks no notoriety — and `` `src/js/wbapi-server.js:const BASE_TIER_WEIGHTS@1161` `` still carries that note by name. **Three more have accrued since, and none is written down anywhere.** The client's roll at `` `function _enterEmptyCell(r, c)@28575` `` applies, in order: (1) `` `function _partyEncounterRate(base)@28822` `` — §MESH-01f halves the rate with a co-present ally and zeroes it under a sentry; (2) `` `if (S_story.huntMode) baseRate = Math.min(0.8, baseRate * 2);@28595` `` — §KG-01's Hunt Mode; (3) a draw that is *both* notoriety-weighted and §KG-01 level-biased inside `` `function _weightedMonsterPick(terrain)@38433` ``. The server applies **none** of them: `` `src/js/wbapi-server.js:s.encounter = (seededNext(s) < mres.encounter.baseRate)@8902` `` is the bare kernel rate. There is also a **fourth, ordering** difference that is deliberate and undocumented: the client suppresses a sentry-guarded encounter *before* the roll (stream not advanced) while the server voids it *after* (`sentryAt`, §MESH-01h), so the same seed diverges by one draw the moment a sentry is involved — the server comments the reason (trace replayability) but no doc names the consequence. **Why the existing gate cannot see any of this:** `src/scripts/check-terrain-parity.js` asserts the *inputs* — rate-table parse round-trip, `SEA_LANES`/`ROAD_CELLS` parse, and `terrainAt` ≡ `_inferTerrain` on all **10,440** band cells (green, 0 diffs). The **applied** rate is downstream of every one of those assertions, which is precisely where three later tracks widened the gap. **Fix (🟢 half):** record all four in `docs/mechanics/mechanics-combat.md` and the §WALK-5 lab report's divergence note as a single SP/MP table, since the server already points at that report by name. **Design call (🟡 half):** whether §MESH-01f's ally-halving and §KG-01's Hunt Mode *should* reach a MUD session at all (both are single-player progression concepts), and if so whether `check:terrain` grows a phase that pins the composed rate rather than its ingredients. **Bundled minor:** the comment at `play.html:28441` cites `wbapi-server.js:8784` for the server's matching roll; it is at **8890**. Its two neighbours at lines 6428 and 6431 (`:1147`, `:1155`) are still exact, so this is one drifted pointer, not a rotted convention — and `check:anchors` scans `*.md` only, so nothing gates a cross-file pointer written inside the engine. **Related, not duplicate:** §DX-02g is off-contract `tier` values; this is a rate composed differently on two sides of the same seeded stream. **Verify:** `npm run check:terrain` still green; `npm run check:rng` still green (the *stream* is byte-identical — 6,000 draws over 12 seeds — it is only what gets compared to it that differs).

### §AUDIT-03bb — one GEO anchor lies outside the map's own projection bounds, and both code paths that meet it fail silently (NEW 2026-08-14 during §DOC-02bj, 🟡 small design call)

- [ ] **§AUDIT-03bb — `PDL` (Ponta Delgada, Azores) is anchored at lon −25.7 against a declared western bound of −25.** 🟡 It is the **only** such entry of **155** GEO anchors, at the 2026-06-16 reference build and unchanged at HEAD. The bounds are `minLon: -25` / `maxLon: 72` on lines 226–227 of `src/tools/worldmap.js`; the anchor is on line 217 of the same file and duplicated in `src/js/wbapi-server.js` (two sites). **Two consumers meet it and neither reports anything.** (a) The region-membership test on line 263 of `src/tools/worldmap.js` is a bare comparison with no clamp (`g.lon >= b.minLon`), so **`PDL` matches no region cell at all** — it is absent from the region grid rather than misplaced in it. (b) The reweave's geo-seed phase *does* clamp: it computes column **4** and pins it to the grid minimum of **8**, so the node is placed, on the west wall, four columns from where its coordinates say. **Nothing is thrown, logged or counted** — the phase's own summary line reports it as placed like any other city. **Found because the source lab report promised the opposite:** `lab-report-world-structure-critique.md` §V.D states the seeding "will place them at the correct positions," and `PDL` is one of the four Atlantic island anchors that section is about. **Three options, and the first is not free:** (a) widen `minLon` to −27 — correct, but it re-projects **every** anchor's column and needs a full reweave plus a re-check of the 3 known cell contentions; (b) move `PDL`'s anchor to lon −25.0, accepting ~60 km of error at the map's least-trafficked edge — one character, no reweave; (c) leave the coordinate and make the two paths honest — clamp in the membership test and emit a warning when geo-seed clamps, so the next out-of-bounds anchor announces itself. **Recommend (c) then (b)** — the silent-failure half is the actual defect and is worth fixing regardless of where `PDL` ends up. **Risk:** (a) is world-wide and wants its own increment; (b)/(c) are local. **Verify:** every GEO anchor satisfies the declared bounds, or geo-seed logs each clamp; `PDL` appears in exactly one region cell.

### §AUDIT-03bc — the GEO tables' `label` column is an Earth city name, the node's `label` is the game place name, and nothing says which you are reading (NEW 2026-08-14 during §DOC-02bj, 🟢 no design call)

- [ ] **§AUDIT-03bc — 150 of 155 GEO anchors carry a label that is not the node's label, and the divergence has already produced published false findings.** 🟢 **prose only, two comment headers.** The anchor tables in `src/tools/worldmap.js` and `src/js/wbapi-server.js` are keyed by node code and carry `label:` — but that label is the **real-world city** (`TRD` → *Trondheim*), while the node's own label is the **game place** (`` `TRD:{ num:26, code:'TRD', name:'goblin_cave',     label:'Goblin Warrens'@8699` ``). They diverge in **148 of 152** entries at the 2026-06-16 build and **150 of 155** at HEAD. **The five that agree at HEAD — `STN`, `AMS`, `CDG`, `NUE`, `ACE` — are exactly the codes the source report used as its worked examples**, so the author generalised from the 3 % of the table that reads like a node registry and then reported `Trondheim`, `Molde`, `Kristiansund`, `Mosjøen`, `Santander` and `Madrid` as anchors with no node behind them. All six are nodes; two are Epic Battleground bosses (`` `SDR:{ num:54, code:'SDR', name:'epic_swamp',           label:'Sunken Altar'@8758` ``, `` `MAD:{ num:69, code:'MAD', name:'epic_heavenly_clouds', label:"Shattered Seraph's Spire"@8788` ``). **The second half of the fix is the stronger one, because it closes the question rather than clarifying it:** GEO membership is **total** — every anchor is a `NODE_MAP` node, **152/152** then and **155/155** now, zero orphans — and the seeding phase already skips-and-reports any anchor with no node, so *"anchored but not a node"* is an empty set and cannot be entered. **Fix:** one comment header on each of the two tables saying (a) `label` is the Earth gazetteer name for author orientation and is **not** `NODE_MAP.label`, and (b) every code here is a live node; if you want the game's name for a place, read `NODE_MAP`. **Also worth recording in the same header:** two anchors are one city twice (`NID` *Nidaros* and `TRD` *Trondheim*, 27 km apart) and two share a label outright (`JAR` and `JRS`, both *Jerusalem*, 11 km apart) — duplicate labels are the cheapest contention to catch before placement. **Related, not duplicate:** §AUDIT-03ba is the same root cause on a different surface — an author reads the most legible nearby text and it is the one surface no gate checks. **Risk:** none (comments only). **Verify:** both headers present; a fresh reader can answer "is `MAD` a node?" from the table alone.

### §DX-02bo — the geo-seeding tool still projects into the coordinate space §WALK-1.5 retired, and no write path checks the bounds (NEW 2026-08-13 during §DOC-02az, 🟠)

- [ ] **§DX-02bo — `./api.sh worldmap --seed` would write every node outside the walkable band.** `src/tools/worldmap.js:const GRID_MIN = 8, GRID_MAX = 500@1124` drives `src/tools/worldmap.js:function geoToGrid(lat, lon)@1126`, which linearly maps the 155-row `GEO` gazetteer onto rows/cols **8–500**. The live world is **90 × 360** (`const GEO_PROJ = { ROWS: 90, COLS: 360 }@9915`, §WALK-1.5/§WALK-2), and every one of the 416 live nodes sits in rows **2–73**, cols **154–249**. A seeded row above 89 is outside the mover's band clamp (`nr < 0 || nr >= ROWS`): the node is unreachable, and a player standing on it could not leave. The verb is published under *"§19 MAP VISUALIZATION"* with a `--dry-run` sibling, so the dry run is where the real numbers are — but the wet run is one flag away.
> **The second half is the one that makes it a 🟠 rather than a 🟡: `PUT /api/coords/{code}` has NO bounds validation of any kind.** The handler tests `typeof r === 'number'` and then tests for a collision, and that is all. There is no `r < ROWS` check anywhere on the write path — not in `PUT /api/coords`, not in `POST /api/coords/{code}/nudge`, not in `POST /api/node`. So the tool's arithmetic is the only thing standing between the gazetteer and an off-band world, and the tool's arithmetic is the retired one.
> ***The instrument this row is filed under: a claim can stay true while becoming wrong.*** `lab-report-movement-by-cells.md` §4.3 described this projection correctly in June and describes it correctly today — the code never drifted. The **world** drifted out from under it. That is a failure mode a symbol census cannot see, because every symbol resolves.
> **Fix (either, cheaply):** (a) retarget `GRID_MIN/GRID_MAX` to `GEO_PROJ` and re-derive from the same equirectangular formula §WALK-1.5 used, so `--seed` and the live projection agree by construction; or (b) if geo-seeding is retired in practice, 410 the verb the way `reweave-all` was retired — it is a repair tool that outlived its world, the §DX-02bf/bi/bk/bl/bn shape. **Either way, add the band guard to `PUT /api/coords` + `nudge` + `POST /api/node`** — that is invariant **I7** in the rewritten report, and it is currently satisfied only by luck.

### §DX-02by — `MoveResult.via` is the deleted ferry mechanism's last surviving trace: written on every step, read nowhere (NEW 2026-08-14 during §DOC-02bk, 🟢 no design call)

- [ ] **§DX-02by — a write-only field on the hottest path in the engine, and it can only ever hold one of two values that `ok` already tells you.** 🟢 **two lines, but inside a byte-parity fence.** The §WALK-2 kernel returns `via: null` when blocked and `via: 'step'` when it moves — `` `src/js/mover.js:function moverMove(world, pos, dir)@44` `` and its inlined twin at `` `function moverMove(world, pos, dir)@9950` ``. The field exists because the §WALK spec's `MoveResult` defined `via: null | 'step' | 'ferry'` to distinguish a walked step from a **ferry-edge** crossing. `95c4143` (2026-06-26, §WALK-5-FU) **deleted the ferry branch** — *"dead capability: `getMoverWorld()` never set it and there was no `FERRY_EDGES` data anywhere"* — because §WALK-1.5 had already shipped water crossings as `SEA_LANES`, walkable cells the kernel crosses like any other. **`'ferry'` has been unreachable ever since, so `via` is now a strictly redundant restatement of `ok`.** Census: **2 writers, 0 readers** across `play.html`, `src/js/mover.js`, `src/js/wbapi-server.js` and `src/tests/mud-harness.mjs`. Same class as §DX-02n's `S_story.ebReturnsCompleted` (write-only shadow), with one difference that makes it cheaper *and* more annoying: it is never persisted, so nothing round-trips it — but it lives between the `MOVER:CORE` sentinels, so **removing it means editing `src/js/mover.js`, re-inlining, and re-running `node src/scripts/check-mover-parity.js`** (currently 1,847 bytes identical). **Fix:** drop `via` from both return shapes in `src/js/mover.js`, re-inline, re-run `check:parity` + `check:behaviour`. **Or keep it and say why** — a one-line comment that the field is reserved is a legitimate outcome; what is not legitimate is a shipped enum whose second value the engine cannot produce. ***A vestigial field is the fossil of a deleted design, and it is the only place the design is still documented as live.***

---

## Track records (Phase 4)

### §NAV-01-FU — Navigable World follow-ups (parent ✅ CLOSED 2026-07-03 → plan-archive.md + `docs/lab-reports/lab-report-nav01-navigable-world.md`)

> *All five follow-ups ✅ CLOSED 2026-07-07 — row moved verbatim → plan-archive.md §"Archived 2026-07-28 — §DX-01f". One piece stays deferred by design: map "jump to region" centering (pannable-view design call; see Open at a Glance).*

---

## §RESUME — Phase 4 history

> **Completed work is not carried here.** The 18 closed increments for this phase are condensed in **[`../archive/backlog-resume-history.md`](../archive/backlog-resume-history.md)**; full prose for recent closes is in [`plan-archive.md`](plan-archive.md). This file carries open rows only.

> When an increment ships: write the full entry into `plan-archive.md`, add its one-line row to `../archive/backlog-resume-history.md` and to the cross-phase table in [`BACKLOG.md`](BACKLOG.md), and delete the row from this file.
