# ── Secrets — never commit ────────────────────────────────────────────────────
.env
*.env

# ── Per-machine identity, ACL and peer caches ────────────────────────────────
.wbapi-server-id
src/config/mesh-acl.json
src/config/peers-cache.json
src/config/tracker-cache.json

# ── Dependencies ──────────────────────────────────────────────────────────────
node_modules/

# ── build/ — everything generated or written at runtime ───────────────────────
#   test-results, playwright-report, the milepoints runtime dir (say-daemon
#   queue, server logs, patch pointer) and the per-deployment economy ledger.
build/

# ── vendor/ — working material, not published ────────────────────────────────
#   imported book corpus, narrative source texts, import machinery.
vendor/

# ── macOS / editor noise ──────────────────────────────────────────────────────
.DS_Store
._*
*.swp
*~

# ── Claude Code internal ──────────────────────────────────────────────────────
.claude/

# ── Stray timestamped game snapshots at root ─────────────────────────────────
play-2*.html
