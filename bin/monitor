#!/usr/bin/env bash
# SPDX-License-Identifier: MIT — Copyright (c) 2026 Paul Richeson
# Shortcut for `make monitor` — runs from the repo root wherever you call it.
cd "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)" && exec make monitor "$@"
